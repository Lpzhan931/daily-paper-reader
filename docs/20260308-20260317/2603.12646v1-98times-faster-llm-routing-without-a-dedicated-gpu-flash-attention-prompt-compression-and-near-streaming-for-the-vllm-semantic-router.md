---
title: "98$\\times$ Faster LLM Routing Without a Dedicated GPU: Flash Attention, Prompt Compression, and Near-Streaming for the vLLM Semantic Router"
title_zh: 无需专用 GPU，LLM 路由提速 98 倍：针对 vLLM 语义路由器的 Flash Attention、提示词压缩与近流式处理
authors: "Xunzhuo Liu, Bowei He, Xue Liu, Andy Luo, Haichen Zhang, Huamin Chen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12646v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对vLLM语义路由器的Flash Attention和提示词压缩优化
tldr: 本研究针对LLM语义路由在共用GPU时面临的内存与延迟挑战，提出了三阶段优化方案。通过在AMD ROCm上实现自定义Flash Attention、引入经典NLP提示词压缩技术以及优化数据流处理，将路由延迟降低了98倍（从4918ms降至50ms），并将显存占用控制在800MB以内。该方案使得在不增加专用GPU的情况下，实现高效的长文本安全分类与路由成为可能。
selection_source: fresh_fetch
motivation: 语义路由系统在与LLM推理共用GPU时，标准注意力机制的高内存占用导致长文本处理困难且延迟显著。
method: 通过实现自定义Flash Attention、应用非神经NLP提示词压缩及优化JSON序列化流处理进行三阶段改进。
result: 实现了98倍的端到端加速，将16K标记的路由时间缩短至108ms，且显存占用低于800MB。
conclusion: 该研究证明了通过系统级优化，可以在不依赖专用GPU的情况下实现高性能、低开销的LLM请求路由与安全过滤。
---

## 摘要
用于拦截 LLM 请求以进行安全分类、领域路由和 PII 检测的系统级路由器必须兼具高速和轻量化的特点：它们应为每个请求增加极低的延迟，且不应占用专用 GPU——这种昂贵的资源更适合用于 LLM 推理本身。当路由器与 vLLM 服务实例共置于同一 GPU 时，标准注意力机制的 $O(n^2)$ 内存占用使得长上下文分类（8K–32K token）变得不可能：在 8K token 时，三个并发分类器仅注意力掩码就需要约 4.5 GB 内存，远超 vLLM 剩余的可用内存。我们为 vLLM 语义路由器提出了三个阶段的优化方案，并在 AMD Instinct MI300X 上进行了基准测试，同时解决了延迟和内存问题。第一阶段：为 ROCm 上的 ONNX Runtime 开发了自定义 CK Flash Attention 算子，将注意力内存从 $O(n^2)$ 降至 $O(n)$，端到端（E2E）延迟从 4,918 ms 降至 127 ms（38.7 倍），在 SDPA 发生显存溢出（OOM）的 8K–32K token 场景下实现运行。第二阶段：采用经典 NLP 提示词压缩技术（TextRank、位置权重、TF-IDF 和新颖性评分），在无需神经推理的情况下将所有输入压缩至约 512 token，无论原始提示词长度如何，均能将延迟和 GPU 内存固定在常数水平（E2E 127 ms 降至 62 ms，2.0 倍）。第三阶段：采用具有自适应分块和零拷贝 JSON 的近流式主体处理，消除了序列化开销（E2E 62 ms 降至 50 ms，1.2 倍）。累计实现：98 倍性能提升（4,918 ms 降至 50 ms），108 ms 内完成 16K token 路由，且路由器总 GPU 占用低于 800 MB——小到足以与 LLM 服务共享 GPU，从而消除了对专用加速器的需求。第一阶段针对 AMD ROCm（NVIDIA GPU 已通过 cuDNN 拥有 FlashAttention）；第二和第三阶段与硬件无关。

## Abstract
System-level routers that intercept LLM requests for safety classification, domain routing, and PII detection must be both fast and operationally lightweight: they should add minimal latency to every request, yet not require a dedicated GPU -- an expensive resource better used for LLM inference itself. When the router co-locates on the same GPU as vLLM serving instances, standard attention's $O(n^2)$ memory makes long-context classification (8K--32K tokens) impossible: at 8K tokens, three concurrent classifiers need ${\sim}$4.5\,GB for attention masks alone, far exceeding the memory left by vLLM. We present three staged optimizations for the vLLM Semantic Router, benchmarked on AMD Instinct MI300X, that solve both the latency and the memory problem. \emph{Stage~1}: a custom CK Flash Attention operator for ONNX Runtime on ROCm reduces attention memory from $O(n^2)$ to $O(n)$ and end-to-end (E2E) latency from 4{,}918\,ms to 127\,ms (\textbf{38.7$\times$}), enabling 8K--32K tokens where SDPA OOMs. \emph{Stage~2}: classical NLP prompt compression (TextRank, position weighting, TF-IDF, and novelty scoring) reduces all inputs to ${\sim}$512 tokens without neural inference, capping both latency and GPU memory at a constant regardless of original prompt length (E2E 127$\to$62\,ms, \textbf{2.0$\times$}). \emph{Stage~3}: near-streaming body processing with adaptive chunking and zero-copy JSON eliminates serialization overhead (E2E 62$\to$50\,ms, \textbf{1.2$\times$}). Cumulatively: \textbf{98$\times$} improvement (4{,}918\,ms to 50\,ms), 16K-token routing in 108\,ms, and a total router GPU footprint under 800\,MB -- small enough to share a GPU with LLM serving and removing the need for a dedicated accelerator. Stage~1 targets AMD ROCm (NVIDIA GPUs already have FlashAttention via cuDNN); Stages~2 and~3 are hardware-agnostic.

---

## 论文详细总结（自动生成）

这篇论文针对 vLLM 语义路由器的性能瓶颈，提出了三阶段优化方案，旨在实现极低延迟且无需专用 GPU 的长文本路由。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 路由层（用于安全分类、意图识别、PII 检测等）通常作为推理的前置步骤，其延迟直接影响首字响应时间（TTFT）。
*   **研究动机**：
    *   **内存瓶颈**：标准注意力机制具有 $O(n^2)$ 复杂度。在处理 8K-32K 长文本时，仅注意力掩码就会占用数 GB 显存，导致路由器无法与 vLLM 推理实例共置（Co-locate）在同一块 GPU 上。
    *   **算力浪费**：为轻量级分类任务分配专用 GPU 非常昂贵且低效。
    *   **处理延迟**：传统的 CPU 推理或全量 JSON 序列化在处理长文本请求时速度极慢（秒级）。

### 2. 论文提出的方法论
论文提出了三个阶段的叠加优化：
*   **阶段 1：针对 ROCm 的自定义 CK Flash Attention**
    *   在 AMD GPU 上为 ONNX Runtime 开发了自定义算子，将内存复杂度从 $O(n^2)$ 降至 $O(n)$。
    *   通过图重写技术，将 ModernBERT 的局部/全局交替注意力模式映射到 AMD 的 Composable Kernel (CK) 库。
*   **阶段 2：无需神经推理的提示词压缩**
    *   使用经典 NLP 算法将长文本压缩至约 512 token。核心评分公式结合了四个信号：
        1.  **TextRank**：评估句子中心性。
        2.  **位置权重**：利用“中间迷失”现象，采用 U 型曲线加权（保留首尾）。
        3.  **TF-IDF**：识别信息密度高的稀疏词汇。
        4.  **新颖性（Novelty）**：通过质心距离识别异常句子（如注入的攻击载荷）。
*   **阶段 3：近流式主体处理（Near-Streaming）**
    *   **零拷贝 JSON**：使用 `gjson/sjson` 进行字段提取和修改，避免全量 `json.Unmarshal`。
    *   **自适应分块**：通过 Envoy 过滤器识别请求模式。若模型已指定，则直接透传；若需分类，则在接收数据块的同时并行进行 NLP 预处理。

### 3. 实验设计
*   **硬件环境**：AMD Instinct MI300X GPU (192GB HBM3)。
*   **对比基准（Benchmarks）**：
    *   **后端对比**：ONNX CPU、Candle CPU、标准 GPU (SDPA)。
    *   **配置对比**：Buffered 模式 vs. Streamed 模式；有无压缩；有无 Flash Attention。
*   **数据集**：
    *   **合成数据**：包含越狱前缀、PII 令牌和特定领域内容的 500 到 16K token 请求。
    *   **真实数据**：384 个基于维基百科文章的测试用例，涵盖 8 个领域和 4 种长度。

### 4. 资源与算力
*   **GPU 型号**：单台服务器配备 1 块 **AMD Instinct MI300X**。
*   **模型**：使用 **mmBERT-32K**（2.7 亿参数，FP16 精度），部署为三个并发的 ONNX 会话。
*   **训练时长**：文中未提及模型训练时长，重点在于推理侧的系统优化。
*   **显存占用**：优化后路由器总占用低于 **800 MB**，而 vLLM 占用约 190 GB，证明了共置的可行性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   每个配置进行 10 次连续 HTTP 请求测试，并计算平均延迟。
    *   准确性测试使用了 384 个独立测试用例。
    *   涵盖了从 512 到 32K token 的不同长度，以及并发度（C=1 到 C=20）的扩展性分析。
*   **充分性评价**：实验设计较为全面，包含了消融实验（分阶段展示提速效果）和准确性验证，客观地展示了系统在长文本下的鲁棒性。

### 6. 主要结论与发现
*   **极致加速**：三阶段优化累计将 8K token 的端到端延迟从 4,918 ms 降低至 50 ms（**98 倍提速**）。
*   **长文本支持**：在 16K token 下实现 108 ms 路由，而传统方法此时通常会 OOM 或延迟过高。
*   **准确性提升**：令人意外的是，提示词压缩不仅没降低准确率，反而通过“去噪”提升了领域分类（53% $\to$ 61%）和 PII 检测（78% $\to$ 92%）的准确性。
*   **成本节约**：通过共置部署，每年可为每个服务节点节省数千至上万美元的专用 GPU 租赁成本。

### 7. 优点
*   **系统级协同**：不只是优化模型，而是从算子、算法到网络代理（Envoy）全链路优化。
*   **硬件友好性**：虽然阶段 1 针对 AMD，但阶段 2 和 3 具有普适性，可应用于 NVIDIA 或 CPU 环境。
*   **实用性强**：解决了生产环境中“路由层比推理层还慢”的实际痛点，且代码已开源。

### 8. 不足与局限
*   **分词近似**：提示词压缩目前使用基于字符长度的启发式估计，而非精确的 Tokenizer，可能导致预算控制存在微小偏差。
*   **越狱检测风险**：实验显示压缩后越狱检测准确率有所下降（70% $\to$ 56%），虽然生产中可通过全量文本扫描弥补，但说明压缩算法对对抗性攻击的敏感度仍有待优化。
*   **内存缓冲**：在“累积”模式下仍需在内存中缓冲请求主体，对于超大规模并发下的内存压力缓解有限。

（完）
