---
title: "FlexServe: A Fast and Secure LLM Serving System for Mobile Devices with Flexible Resource Isolation"
title_zh: FlexServe：一种面向移动设备且具有灵活资源隔离的高效安全大语言模型推理系统
authors: "Yinpeng Wu, Yitong Chen, Lixiang Wang, Jinyu Gu, Zhichao Hua, Yubin Xia"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09046v1"
tags: ["query:llm"]
score: 9.0
evidence: 移动设备上快速且安全的LLM服务系统
tldr: 随着移动端大模型（LLM）的普及，保护模型权重和用户隐私免受受损操作系统的攻击至关重要。本文提出 FlexServe，一个基于 ARM TrustZone 的快速且安全的移动端 LLM 推理系统。通过引入灵活的资源隔离机制（Flex-Mem 和 Flex-NPU）以及 LLM 感知的内存管理和安全推理流水线，FlexServe 克服了传统 TrustZone 隔离带来的高开销。实验表明，该系统在首字生成时间和多模型工作流效率上均有显著提升，为移动端安全推理提供了高效方案。
selection_source: fresh_fetch
motivation: 传统的 ARM TrustZone 在保护移动端 LLM 推理时，由于内存和 NPU 隔离机制不够灵活，导致了严重的性能瓶颈。
method: 引入了灵活的资源隔离机制（Flex-Mem 和 Flex-NPU），并设计了 LLM 感知内存管理、安全推理流水线及多模型调度器。
result: 实验显示 FlexServe 在首字生成时间上比基准方案快 10.05 倍，在多模型工作流中实现了高达 24.30 倍的端到端加速。
conclusion: FlexServe 通过灵活的硬件资源隔离与软件优化，成功在移动设备上实现了兼具高安全性和高性能的大模型推理服务。
---

## 摘要
端侧大语言模型（LLM）经历了爆发式增长，与云端 LLM 相比，它们提供了更高的隐私性和可用性。在 LLM 推理过程中，模型权重和用户数据都非常宝贵，攻击者甚至可能通过攻破操作系统内核来窃取这些数据。ARM TrustZone 是移动设备上事实上的硬件隔离技术，用于保护敏感应用免受受损操作系统的侵害。然而，由于 TrustZone 对内存和 NPU 的隔离缺乏灵活性，使用 TrustZone 保护 LLM 推理会产生显著的开销。为了应对这些挑战，本文提出了 FlexServe，这是一种用于移动设备的快速且安全的 LLM 推理系统。它首先引入了一种灵活资源隔离机制，用以构建灵活安全内存（Flex-Mem）和灵活安全 NPU（Flex-NPU）。内存页和 NPU 均可在非保护模式和保护模式之间高效切换。基于这些机制，FlexServe 在 TrustZone 的安全世界（secure world）内设计了一个快速且安全的 LLM 推理框架。文中引入了 LLM 感知内存管理和安全推理流水线以加速推理，并提出了一种多模型调度器来优化多模型工作流。我们实现了 FlexServe 的原型，并将其与两种基于 TrustZone 的基准（strawman）设计进行了比较。结果表明，与基准设计相比，FlexServe 在首字延迟（TTFT）方面实现了平均 10.05 倍的加速；与启用了流水线和安全 NPU 的优化基准设计相比，实现了平均 2.44 倍的 TTFT 加速。对于多模型智能体（agent）工作流，其端到端加速比相较于基准设计和优化基准设计分别达到了 24.30 倍和 4.05 倍。

## Abstract
Device-side Large Language Models (LLMs) have witnessed explosive growth, offering higher privacy and availability compared to cloud-side LLMs. During LLM inference, both model weights and user data are valuable, and attackers may even compromise the OS kernel to steal them. ARM TrustZone is the de facto hardware-based isolation technology on mobile devices, used to protect sensitive applications from a compromised OS. However, protecting LLM inference with TrustZone incurs significant overhead due to its inflexible isolation of memory and the NPU. To address these challenges, this paper introduces FlexServe, a fast and secure LLM serving system for mobile devices. It first introduces a Flexible Resource Isolation mechanism to construct Flexible Secure Memory (Flex-Mem) and Flexible Secure NPU (Flex-NPU). Both memory pages and the NPU can be efficiently switched between unprotected and protected modes. Based on these mechanisms, FlexServe designs a fast and secure LLM inference framework within TrustZone's secure world. The LLM-Aware Memory Management and Secure Inference Pipeline are introduced to accelerate inference. A Multi-Model Scheduler is proposed to optimize multi-model workflows. We implement a prototype of FlexServe and compare it with two TrustZone-based strawman designs. The results show that FlexServe achieves an average $10.05\times$ speedup in Time to First Token (TTFT) compared to the strawman, and an average $2.44\times$ TTFT speedup compared to an optimized strawman with pipeline and secure NPU enabled. For multi-model agent workflows, the end-to-end speedup is up to $24.30\times$ and $4.05\times$ compared to the strawman and optimized strawman, respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《FlexServe: A Fast and Secure LLM Serving System for Mobile Devices with Flexible Resource Isolation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：端侧大语言模型（LLM）因隐私和可用性优势增长迅速，但模型权重和用户数据面临被受损操作系统（OS）内核窃取的风险。
*   **核心挑战**：现有的硬件隔离技术（如 ARM TrustZone）在保护 LLM 推理时存在严重的“不灵活性”：
    1.  **内存隔离僵化**：TrustZone 要求安全内存必须是连续的物理内存。为 LLM 分配 GB 级别的连续内存极其缓慢（如 8GB 需 6.44 秒），且在内存受限的移动设备上难以实现。
    2.  **NPU 无法安全使用**：现有设备通常将 NPU 静态配置在普通世界，安全世界无法调用 NPU 加速，导致只能使用 CPU 推理，性能极差。
    3.  **复杂工作流支持弱**：移动端常涉及多模型切换（如智能体工作流），传统机制难以高效管理多模型的内存切换。

### 2. 方法论
FlexServe 提出了**灵活资源隔离机制**，由 Flex-Monitor 和 FlexServe Framework 两部分组成：
*   **Flex-Mem（灵活安全内存）**：利用第二阶段页表（S2PT）实现页粒度的隔离。它允许将非连续的物理页动态切换为安全模式，支持“按需保护”和“延迟回收（Lazy Reclaim）”，避免了昂贵的连续内存分配开销。
*   **Flex-NPU（灵活安全 NPU）**：通过时间片轮转方式，在安全世界和普通世界间切换 NPU。Flex-Monitor 构建了一个隔离沙箱来复用普通世界的 NPU 驱动，并限制 NPU 仅能访问 Flex-Mem，防止数据泄露。
*   **安全推理流水线**：将推理过程拆分为内存分配、模型加载、解密和前向计算四个阶段，通过流水线并行化掩盖加载和解密延迟。
*   **LLM 感知内存管理与调度**：
    *   **层感知缓存**：根据内存压力优先保留模型的前几层。
    *   **多模型调度**：引入“零停顿缓存线（Zero-stall Cache Line）”和工作流感知预取，在模型切换时重叠加载与计算。
*   **按需保护（On-demand Protection）**：当没有安全推理任务时，通过 EL3 验证并关闭 EL2 虚拟化功能，消除运行时的虚拟化开销。

### 3. 实验设计
*   **硬件平台**：瑞芯微（Rockchip）RK3588 开发板（8 核 CPU, 6TOPS NPU, 16GB RAM）。
*   **软件环境**：Linux Kernel 6.1.57 (普通世界) + OP-TEE 4.5.0 (安全世界)。
*   **测试模型**：Llama 3.1/3.2 系列、Qwen 3 系列（参数量从 0.6B 到 8B，均采用 INT8 量化）。
*   **对比基准（Benchmarks）**：
    1.  **NW-Base**：普通世界非保护推理（性能上限）。
    2.  **Strawman**：标准 TrustZone 方案（使用连续内存分配 CMA，仅 CPU 推理）。
    3.  **Strawman-OPT**：优化版 TrustZone（使用 CMA，但启用流水线和安全 NPU）。
*   **测试场景**：单模型推理性能、不同背景内存压力下的稳定性、多模型智能体工作流（如 GUI Agent、会议助手等）。

### 4. 资源与算力
*   **算力设备**：实验在单台 RK3588 SoC 上完成，利用其内置的 NPU 进行矩阵乘法（GEMM）加速。
*   **训练时长**：本文侧重于**推理服务系统**，不涉及模型训练，因此未提及训练算力。系统本身的实现代码量较小（Flex-Monitor 约 4.5K 行 C 代码，框架约 3.3K 行）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了微观基准测试（内存分配、NPU 切换延迟）。
    *   测试了 4 种不同规模模型在 5 种输入长度下的 TTFT。
    *   测试了 0GB 到 10GB 不同背景内存负载下的系统鲁棒性。
    *   评估了 10 种真实世界的智能体工作流场景。
*   **充分性评价**：实验设计较为全面，不仅对比了原生 TrustZone，还对比了加入流水线优化后的强基准。通过引入背景内存压力测试，客观地模拟了移动端复杂的运行环境，证明了页粒度内存管理的必要性。

### 6. 主要结论
*   **TTFT 加速**：相比 Strawman 基准，FlexServe 实现了平均 **10.05 倍**的加速；相比 Strawman-OPT，实现了平均 **2.44 倍**的加速。
*   **多模型工作流**：在智能体场景下，端到端响应延迟相比 Strawman 降低了高达 **24.30 倍**。
*   **系统开销**：相比完全不保护的普通世界推理，FlexServe 的性能损失仅为 **4.41%** 左右，且对普通世界应用（如 SQLite）的干扰远小于传统 CMA 方案。

### 7. 优点
*   **架构创新**：打破了 TrustZone 必须使用连续内存的硬件限制，通过虚拟化技术实现了更灵活的资源调度。
*   **实用性强**：复用了现有的 NPU 驱动，降低了在 TEE 中重新开发复杂硬件驱动的难度。
*   **全栈优化**：从底层的页表管理到上层的模型层级缓存调度，进行了系统级的深度协同优化。

### 8. 不足与局限
*   **硬件依赖**：虽然基于 ARM 通用特性，但 NPU 驱动复用部分高度依赖于特定厂商（如瑞芯微）的硬件实现细节。
*   **安全边界**：无法防御物理攻击（如冷启动攻击）或侧信道攻击，且依然无法保护普通世界客户端本身（如果客户端被攻破，输入输出仍可能泄露）。
*   **算力瓶颈**：实验中使用的 NPU 算力较低（6TOPS），在更强大的移动 SoC 上，计算阶段可能不再是瓶颈，届时内存带宽和解密速度的影响会更显著。

（完）
