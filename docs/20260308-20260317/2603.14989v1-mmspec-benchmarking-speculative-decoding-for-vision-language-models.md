---
title: "MMSpec: Benchmarking Speculative Decoding for Vision-Language Models"
title_zh: MMSpec：视觉语言模型投机解码基准测试
authors: "Hui Shen, Xin Wang, Ping Zhang, Yunta Hsieh, Qi Han, Zhongwei Wan, Ziheng Zhang, Jingxuan Zhang, Jing Xiong, Ziyuan Liu, Yifan Zhang, Hangrui Cao, Chenyang Zhao, Mi Zhang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14989v1"
tags: ["query:llm"]
score: 10.0
evidence: 多模态模型投机采样基准测试
tldr: 针对视觉语言模型（VLM）推理延迟高的问题，本文推出了首个专门评估VLM投机采样性能的基准测试MMSpec。该基准涵盖6大任务类别和10种代表性算法。研究发现传统文本投机采样在多模态场景下表现下降，且视觉感知对大批量推理至关重要。基于此，作者提出了ViSkip，一种能动态适应视觉Token的即插即用投机采样方法，在加速推理方面达到了SOTA性能。
selection_source: fresh_fetch
motivation: 视觉语言模型推理缓慢且投机采样在多模态领域的表现尚缺乏系统性评估。
method: 建立了包含600个样本和10种算法的MMSpec基准，并提出动态适应视觉Token的ViSkip方法。
result: 实验揭示了文本专用方法在多模态下的局限性，并证明了视觉感知在提升大批量推理效率中的关键作用。
conclusion: MMSpec填补了VLM投机采样评估的空白，而ViSkip为加速多模态模型推理提供了高效的新方案。
---

## 摘要
视觉语言模型（VLMs）在多模态任务上表现出色，但由于模型规模庞大且多模态上下文较长，面临着高推理延迟的问题。投机解码（Speculative decoding）近期作为一种有效的加速技术出现，但其在 VLM 中的行为尚未得到充分理解。我们推出了 MMSpec，这是首个用于评估视觉语言模型投机解码的基准测试。MMSpec 包含跨六个任务类别的 600 个多模态样本，并在统一的评估框架下集成了十种具有代表性的投机解码算法。我们的研究揭示了三个关键发现：（1）专为纯文本大语言模型（LLMs）设计的方法在多模态场景下性能下降；（2）在较大的批处理大小（batch sizes）下，视觉感知变得越来越重要；（3）仅凭吞吐量加速并不能可靠地反映延迟性能。受这些发现的启发，我们提出了 ViSkip，这是一种即插即用的投机解码方法，能够动态地使投机过程适应视觉标记（vision tokens），并实现了最先进的性能。

## Abstract
Vision-language models (VLMs) achieve strong performance on multimodal tasks but suffer from high inference latency due to large model sizes and long multimodal contexts. Speculative decoding has recently emerged as an effective acceleration technique, yet its behavior in VLMs remains insufficiently understood. We introduce MMSpec, the first benchmark for evaluating speculative decoding in vision-language models. MMSpec contains 600 multimodal samples across six task categories and integrates ten representative speculative decoding algorithms under a unified evaluation framework. Our study reveals three key findings: (1) methods designed for text-only LLMs degrade in multimodal scenarios, (2) vision awareness becomes increasingly important at larger batch sizes, and (3) throughput speedup alone does not reliably reflect latency performance. Motivated by these findings, we propose ViSkip, a plug-and-play speculative decoding method that dynamically adapts speculation to vision tokens and achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《MMSpec: Benchmarking Speculative Decoding for Vision-Language Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（VLM）由于模型规模大、多模态上下文长以及自回归解码的特性，面临严重的推理延迟。虽然“投机解码”（Speculative Decoding）在纯文本大模型中取得了成功，但在多模态场景下的表现缺乏系统性评估和深入理解。
*   **研究动机**：
    *   **评估缺失**：现有投机解码的评估大多集中在纯文本基准上，无法捕捉多模态生成中的跨模态依赖和视觉推理特征。
    *   **标准不一**：不同研究使用的数据集、模型和实验设置各异，难以进行公平比较。
    *   **性能瓶颈**：直接将文本领域的投机解码迁移至 VLM 时，往往因忽略视觉信息导致接受率低、加速效果差。

### 2. 方法论：MMSpec 基准与 ViSkip 算法
*   **MMSpec 基准测试**：
    *   构建了一个包含 **600 个多模态样本** 的数据集，涵盖 6 大任务：通用 VQA、文本 VQA、图像描述、图表 VQA、复杂推理、多轮对话。
    *   建立了一个**统一评估平台**，集成了 10 种代表性算法（包括基于训练的 EAGLE、Medusa、MSD、ViSpec，以及无需训练的 Lookahead、PLD、SAM 等）。
*   **ViSkip 算法（核心技术细节）**：
    *   **视觉相关性估计**：通过计算解码器隐藏状态与视觉标记（Vision Tokens）之间的**交叉注意力权重（Cross-Attention Weights）**来衡量当前生成步骤对视觉信息的依赖程度。
    *   **动态自适应切换**：设定阈值 $\tau$。如果当前步骤的视觉相关性得分 $S_t$ 超过 $\tau$（即高度依赖视觉），则跳过投机过程，直接由全模型生成单标记；若低于阈值，则执行标准的投机解码。
    *   **即插即用**：该方法不需要额外训练，可直接集成到现有的投机解码框架中。

### 3. 实验设计
*   **数据集/场景**：使用了 GQA、TextVQA、COCO、CharXiv、MMMU-Pro、ConvBench 和 MM-MT-Bench 等 7 个来源的数据。
*   **基准模型**：主要在 **Qwen2.5-VL-7B-Instruct** 和 **LLaVA-1.5-7B** 上进行测试。
*   **对比方法**：
    *   **基于训练的方法**：EAGLE-1/2/3, Medusa, MSD, ViSpec。
    *   **无需训练的方法**：SAM Decoding, Lookahead, Recycling, Prompt Lookup Decoding (PLD)。
*   **评估指标**：平均接受标记数（MAT）和墙钟时间加速比（Speedup），并引入了延迟分布（CDF）分析。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **4 台 NVIDIA A100 GPU** 上进行。
*   **软件环境**：使用了统一的设备、相同的软件环境和标准化的评估协议以确保可重复性。
*   **训练时长**：文中未详细列出各算法的具体训练时长（因为主要关注推理基准测试），但提到 ViSkip 是无需训练的。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 种算法、2 个主流 VLM 模型、6 类任务、多种 Batch Size（1 到 16）以及不同的采样温度（$t=0$ 和 $t=0.6$）。
*   **充分性与公平性**：
    *   实验设计较为充分，不仅对比了吞吐量，还深入分析了延迟（Latency）和 Batch Size 的敏感性。
    *   通过在统一框架下运行开源代码，保证了对比的客观性。
    *   提供了消融实验（如附录中对阈值 $\tau$ 的影响分析）。

### 6. 主要结论与发现
1.  **文本专用方法失效**：专为纯文本设计的投机解码方法在多模态任务中加速效果有限，甚至可能导致减速。
2.  **视觉感知至关重要**：随着 Batch Size 增加，非视觉感知的方法性能大幅下降，而视觉感知方法（如 MSD, ViSpec）表现更稳健。
3.  **吞吐量不等于延迟**：高吞吐量加速比并不总是对应更好的用户感知延迟，稳定性同样重要。
4.  **ViSkip 的有效性**：通过动态识别“视觉敏感”步骤并跳过投机，ViSkip 显著提升了现有方法的加速比和延迟表现。

### 7. 优点
*   **填补空白**：填补了 VLM 投机解码缺乏统一、多模态专用基准测试的空白。
*   **洞察深刻**：揭示了视觉信息在投机验证过程中的关键作用，为后续研究指明了方向。
*   **实用性强**：提出的 ViSkip 方法简单高效，具有很强的工程落地价值。

### 8. 不足与局限
*   **样本规模**：虽然涵盖任务广，但 600 个样本在某些极端长尾场景下的代表性可能仍有提升空间。
*   **模型覆盖**：主要集中在 7B 规模的模型，对于超大规模（如 70B+）或极小规模 VLM 的表现探讨较少。
*   **阈值敏感性**：ViSkip 依赖超参数 $\tau$，不同模型和任务可能需要微调该阈值以达到最优平衡。

（完）
