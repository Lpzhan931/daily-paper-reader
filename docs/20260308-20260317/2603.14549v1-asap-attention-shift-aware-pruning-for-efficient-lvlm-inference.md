---
title: "ASAP: Attention-Shift-Aware Pruning for Efficient LVLM Inference"
title_zh: ASAP：面向高效 LVLM 推理的注意力偏移感知剪枝
authors: "Surendra Pathak, Bo Han"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14549v1"
tags: ["query:llm"]
score: 10.0
evidence: 适用于LVLM推理的KV缓存兼容剪枝方案
tldr: "ASAP 针对大型视觉语言模型（LVLM）推理中高分辨率视觉 token 带来的计算瓶颈，提出了一种无需训练且兼容 KV 缓存的剪枝方案。该方法通过动态双向软注意力掩码缓解“注意力偏移”现象，并结合加权软合并技术消除语义冗余。在保持 LLaVA-NeXT-7B 原始性能 99.02% 的同时，成功减少了约 80% 的计算量，实现了高效且近乎无损的视觉上下文压缩。"
selection_source: fresh_fetch
motivation: 现有的视觉 token 缩减方法忽略了 LVLM 中导致 token 评分偏差的“注意力偏移”现象，且未能有效处理 token 间的语义冗余。
method: 提出 ASAP 方案，利用动态双向软注意力掩码精准筛选信息，并采用加权软合并技术融合语义相似的 token。
result: "在 LLaVA-NeXT-7B 上实现了约 80% 的计算量（FLOPs）削减，同时保留了 99.02% 的原始性能。"
conclusion: ASAP 证明了通过感知注意力偏移和处理语义冗余，可以在不进行额外训练的情况下显著提升 LVLM 的推理效率。
---

## 摘要
尽管大型视觉语言模型 (LVLM) 展示了卓越的多模态能力，但处理高分辨率视觉 token 的二次方计算成本仍然是一个关键瓶颈。虽然最近的 token 削减策略试图加速推理，但这些方法未能充分利用注意力值，也未能解决 token 冗余问题。更关键的是，它们忽视了 LVLM 中固有的“注意力偏移” (attention shift) 现象，该现象会导致 token 注意力分数产生偏差。在这项工作中，我们提出了 ASAP，这是一种新颖的、无需训练且兼容 KV-Cache 的剪枝方案，全面解决了这些局限性。首先，我们通过利用动态双向软注意力掩码来缓解注意力偏移，确保选择真正具有信息量的 token，而非基于朴素注意力的选择。其次，我们认为 token 集合中的高语义冗余会降低性能。因此，我们引入了一个加权软合并组件，用于合并语义相似的 token，仅为后续层保留特征最密集的视觉补丁 (patches)。ASAP 实现了视觉上下文的几乎无损压缩，在保留 LLaVA-NeXT-7B 原始性能 99.02% 的同时，大幅削减了约 80% 的计算 FLOPs。

## Abstract
While Large Vision-Language Models (LVLMs) demonstrate exceptional multi-modal capabilities, the quadratic computational cost of processing high-resolution visual tokens remains a critical bottleneck. Though recent token reduction strategies attempt to accelerate inference, such methods inadequately exploit attention values and fail to address token redundancy. More critically, they overlook the ``attention shift'' phenomenon inherent in LVLMs, which skews token attention scores. In this work, we propose ASAP, a novel training-free, KV-Cache-compatible pruning recipe that comprehensively addresses these limitations. First, we mitigate the attention shift by utilizing a dynamic bidirectional soft attention mask, ensuring the selection of genuinely informative tokens rather than naive attention-based selection. Second, we posit that high semantic redundancy within the token set degrades performance. We therefore introduce a weighted soft merging component that merges semantically similar tokens, preserving only the most feature-dense visual patches for subsequent layers. ASAP achieves virtually lossless compression of visual context, retaining 99.02% of the original LLaVA-NeXT-7B performance while aggressively slashing computational FLOPs by ~80%.

---

## 论文详细总结（自动生成）

### 论文总结：ASAP——面向高效 LVLM 推理的注意力偏移感知剪枝

#### 1. 核心问题与研究动机
随着大型视觉语言模型（LVLM）向高分辨率演进（如 LLaVA-NeXT），视觉 token 的数量急剧增加（可达数千个），导致 Transformer 的计算成本随序列长度呈二次方增长。
*   **研究动机**：现有的 token 剪枝方法存在两个主要缺陷：
    1.  **注意力偏移（Attention Shift）**：由于旋转位置嵌入（RoPE）的距离衰减特性，模型倾向于给序列后端的 token 分配更高权重，导致前端视觉信息被误删。
    2.  **语义冗余**：图像中存在大量低信息量区域（如天空、背景），简单的 Top-k 过滤无法有效处理这些冗余，且容易造成特征稀释。
*   **核心目标**：提出一种无需训练、兼容 KV 缓存的剪枝方案，在大幅降低计算量的同时保持模型性能。

#### 2. 方法论
ASAP 框架由三个核心组件组成，旨在实现“注意力偏移感知”和“特征密集化”：
*   **显著性引导的双向注意力（SG-BiMask）**：
    *   **核心思想**：打破视觉 token 间的因果掩码限制，允许早期 token “窥视”后期 token。
    *   **技术细节**：通过预 Softmax 的对齐矩阵计算 token 的原始显著性，并进行 Min-Max 归一化。引入一个动态的、基于显著性的加权惩罚矩阵 $M_{bidir}$，将其叠加到注意力 Logits 上，从而在结构上缓解 RoPE 带来的位置偏见。
*   **显著性加权合并（Salience-Weighted Consolidation）**：
    *   **核心思想**：利用特征相似度合并冗余 token，而非直接丢弃。
    *   **技术细节**：计算隐藏状态间的余弦相似度，将语义相似的 token 合并到显著性更高的“锚点” token 中。合并过程采用加权平均，权重由前一阶段计算的显著性分数决定，以防止特征稀释。
*   **预算重新分配（Budget Reallocation）**：
    *   **核心思想**：最大化信息保留。
    *   **技术细节**：合并操作会释放出部分 token 预算位，ASAP 会从之前被剪枝的候选池中捞回显著性最高的 token 填补这些空位，确保最终保留的 token 集合既多样又特征密集。

#### 3. 实验设计
*   **模型架构**：LLaVA-1.5-13B、LLaVA-NeXT-7B（LLaVA-1.6）、InternVL 2.5-8B。
*   **数据集/基准测试**：涵盖 VQAv2、GQA（推理）、ScienceQA（科学）、TextVQA（OCR）、MME（综合感知）、MMBench（逻辑）、MMVet（复杂推理）和 POPE（幻觉评估）。
*   **对比方法**：FastV、LLaVA-PruMerge+、SparseVLM、DivPrune、VisPruner 等主流训练无关（Training-free）剪枝技术。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体训练/推理的总时长。
*   **效率指标**：论文详细报告了理论 FLOPs 比例、KV 缓存存储占用（MB）、GPU 显存占用（GB）以及 CUDA 推理延迟（ms）。例如，在 LLaVA-1.5-7B 上，ASAP 将显存占用从 14.33GB 微降至 13.96GB，推理延迟仅增加约 2ms。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同架构的模型上进行了测试，覆盖了超过 8 个主流基准测试。
*   **消融实验**：系统地验证了双向注意力模块、加权合并模块和捞回机制的各自贡献。
*   **充分性评价**：实验设计较为充分且客观。通过在不同 token 保留预算（如 128、192、640、960 tokens）下进行对比，展示了模型在极端压缩情况下的鲁棒性。同时，通过可视化对比（图 2）直观证明了其缓解空间偏见的能力。

#### 6. 主要结论与发现
*   **高效压缩**：在 LLaVA-NeXT-7B 上，ASAP 在减少约 **80% FLOPs** 的情况下，保留了 **99.02%** 的原始性能。
*   **超越基准**：在某些任务（如 MMBench）上，ASAP 的表现甚至超过了未剪枝的原始模型（Supra-vanilla performance），这表明剪枝可能起到了去除视觉噪声的作用。
*   **通用性**：ASAP 证明了 RoPE 引起的注意力偏移是 LVLM 的普遍问题，而通过结构化的注意力调整可以有效修复这一缺陷。

#### 7. 优点
*   **无需训练（Plug-and-play）**：作为一种后处理方案，无需昂贵的重训练或微调，易于部署。
*   **结构化创新**：首次从 RoPE 距离衰减的角度系统性地解决 LVLM 的剪枝偏见问题。
*   **KV 缓存兼容**：完全支持自回归生成过程中的 KV 缓存，适合多轮对话。
*   **性能平衡**：在极高的压缩比下依然能保持极高的精度，优于现有的 FastV 和 DivPrune 等方法。

#### 8. 不足与局限
*   **超参数依赖**：方法中涉及一些超参数（如最大可见度 $\lambda_{max}$ 和相似度阈值 $t$），可能需要针对不同模型进行微调。
*   **推理开销**：虽然减少了整体 FLOPs，但引入的双向注意力计算和特征相似度计算会带来微小的额外延迟（约 2ms），在极低延迟要求的场景下需权衡。
*   **长文本/多图限制**：实验主要集中在单图理解，对于极长视频或超大规模多图输入的极端情况，其内存节省效果和注意力偏移的演变尚待进一步验证。

（完）
