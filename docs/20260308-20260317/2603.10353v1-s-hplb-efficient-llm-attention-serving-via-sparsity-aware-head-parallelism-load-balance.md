---
title: "S-HPLB: Efficient LLM Attention Serving via Sparsity-Aware Head Parallelism Load Balance"
title_zh: S-HPLB：通过稀疏感知的多头并行负载均衡实现高效的大语言模型注意力推理服务
authors: "Di Liu, Yifei Liu, Chen Chen, Zhibin Yu, Xiaoyi Fan, Quan Chen, Minyi Guo"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10353v1"
tags: ["query:llm"]
score: 9.0
evidence: 稀疏感知的多头并行机制用于高效注意力服务
tldr: 针对大语言模型推理中注意力机制计算开销大的问题，本文提出S-HPLB。研究发现不同注意力头对稀疏性的敏感度不同，据此实施自适应稀疏预算。为解决异构稀疏导致的GPU负载不均，S-HPLB通过感知稀疏性的头并行负载均衡策略，在保持推理质量的同时，将注意力计算延迟降低了2.88倍。
selection_source: fresh_fetch
motivation: 不同注意力头具有异构且稳定的稀疏弹性，但统一的稀疏预算会导致效率低下，而异构预算又会引发多GPU间的负载不均衡。
method: 提出一种感知稀疏性的头并行负载均衡策略（S-HPLB），根据各头的稀疏程度优化其在多GPU上的部署。
result: 在长文本基准测试中，S-HPLB在不损失模型质量的前提下，实现了平均注意力计算延迟2.88倍的提升。
conclusion: 通过结合头自适应稀疏化与负载均衡部署，可以显著提升长文本场景下大语言模型的推理效率。
---

## 摘要
随着大语言模型（LLM）规模的增加和上下文长度的扩展，注意力计算已成为 LLM 推理服务中的关键性能瓶颈。为了实现快速的注意力计算，近期的实践通常在多个 GPU 上对注意力头进行并行化处理，并广泛采用注意力稀疏化技术来减少计算量——即在预设的稀疏度预算下选择性地计算注意力对的子集。在本文中，我们注意到 LLM 模型的注意力头通常表现出异构且稳定的稀疏弹性，这促使我们实施头自适应的稀疏度预算，以在保持高推理质量的同时获得更高的效率。然而，从系统层面来看，由于稀疏程度不同，不同头上的注意力计算时间会不一致，从而在头并行部署下产生跨 GPU 的资源气泡。为了进一步减少此类气泡，我们提出了一种名为“稀疏感知头并行负载均衡”（S-HPLB）的新型注意力部署策略。在长上下文基准测试上的实验表明，S-HPLB 可以在不降低质量的情况下，实现 2.88 倍的平均注意力计算延迟提升。

## Abstract
With the increasing volumes of Large Language Models (LLMs) and the expanding context lengths, attention computation has become a key performance bottleneck in LLM serving. For fast attention computation, recent practices often parallelize the attention heads on multiple GPUs, and also widely adopt attention sparsification to reduce the computation amount -- which selectively computes a subset of attention pairs under a preset sparsity budget. In this paper, we notice that attention heads of an LLM model often exhibit heterogeneous-yet-stable sparsity elasticities, which motivates us to enforce head-adaptive sparsity budgets to attain better efficiency while preserving high inference quality. Yet, from the system aspect, with heterogeneous sparsity levels, attention computation time on different heads would be inconsistent, yielding cross-GPU resource bubbles under head-parallel deployment. To further minimize such bubbles, we propose a novel attention deployment strategy called Sparsity-aware Head-Parallel Load Balance (S-HPLB). Experiments on long-context benchmark show that, S-HPLB can achieve a $2.88\times$ improvement in average attention computation latency without quality degradation.

---

## 论文详细总结（自动生成）

这是一份关于论文《S-HPLB: Efficient LLM Attention Serving via Sparsity-Aware Head Parallelism Load Balance》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）上下文长度的增加，注意力（Attention）计算成为推理服务的核心瓶颈（如在 128K 上下文下，Llama-3.1-8B 的注意力计算占首字返回时间 TTFT 的 70% 以上）。
*   **核心问题**：
    1.  **算法低效**：现有的稀疏注意力方法（如 Top-k）通常为所有注意力头分配统一的计算预算，忽略了不同头之间“稀疏弹性”的异构性（即某些头只需极少 Token 即可恢复大部分权重，而某些头则需要更多）。
    2.  **系统失衡**：如果采用自适应预算（如 Top-p），会导致不同注意力头的计算量不一致。在多 GPU 头并行（Head Parallelism）部署时，这会引发严重的负载不均衡（Load Imbalance），产生“长尾”效应，导致计算资源浪费。
*   **研究动机**：通过算法与系统的协同设计，既利用头的异构稀疏性提升精度，又通过智能调度消除 GPU 间的负载气泡。

### 2. 方法论
S-HPLB 提出了一个系统-算法协同设计的框架，包含两个核心组件：
*   **自适应头预算分配（Adaptive Head Budget Allocation）**：
    *   **稀疏稳定性观察**：研究发现，尽管不同头的稀疏性不同，但单个头在不同任务和上下文长度下的相对稀疏模式是稳定的。
    *   **Max-Min 预算转移策略**：通过离线分析获取各头的稀疏特征，在总计算预算固定的前提下，利用迭代算法将预算从“高稀疏头”（容易恢复权重的头）转移给“低稀疏头”（难恢复权重的头），以最小化精度损失。
*   **头并行负载均衡（Head Parallel Load Balance）**：
    *   **问题建模**：将异构预算下的头分配问题建模为经典的“多路划分问题”（Multiway Partitioning），目标是最小化各 GPU 设备间总负载的差异。
    *   **贪心启发式算法**：将注意力头按预算降序排列，依次将其分配给当前负载最小的 GPU。该算法复杂度低（$O(N \log N)$），适合在线推理环境。

### 3. 实验设计
*   **模型**：Llama-3.1-8B、Qwen2.5-7B、Qwen2.5-72B，以及长文本变体 Llama-3-8B-262K。
*   **数据集/Benchmark**：使用 **RULER** 基准测试，涵盖检索、多步追踪、聚合和问答等 13 个任务，上下文长度最高达 128K。
*   **对比方法**：
    *   **Full Attention**：使用 FlashAttention 实现。
    *   **Top-k 类（固定预算）**：StreamingLLM、MInference。
    *   **Top-p 类（动态预算）**：XAttention。

### 4. 资源与算力
*   **硬件环境**：单台服务器，配备 **8 张 NVIDIA A100 80GB GPU**，通过 NVLink 连接。
*   **处理器**：Intel Xeon Platinum 8369B CPU。
*   **软件栈**：CentOS 7, CUDA 12.4, PyTorch 2.6.0。
*   **训练/分析时长**：文中未明确提及离线 Profile 的具体耗时，但指出该过程是单次离线完成的，且预算分配算法本身运行极快。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流模型系列上进行了端到端准确率对比。
    *   测试了不同并行度（HP=2, 4, 8）下的延迟表现。
    *   进行了 **Pareto 边界分析**（准确率 vs 延迟的权衡曲线）。
    *   包含**消融实验**，专门验证了负载均衡器对延迟的优化贡献。
*   **充分性评价**：实验设计较为全面，覆盖了从 8B 到 72B 不同规模的模型，且使用了极具挑战性的 RULER 基准。对比了当前最先进的稀疏注意力方案，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **精度保持**：S-HPLB 的平均准确率与全量注意力（Full Attention）基本持平（差距在 0.5%-3.1% 以内），显著优于其他稀疏方法。
*   **延迟提升**：相比 Full Attention，注意力计算延迟降低了 **3.31x - 4.27x**；相比最强的动态稀疏基准 XAttention，延迟降低了 **2.09x - 2.88x**。
*   **负载均衡效果**：专门的负载均衡策略相比原生部署，可额外带来约 **1.26x** 的加速，有效消除了 GPU 间的计算气泡。
*   **Pareto 最优**：在相同的延迟水平下，S-HPLB 始终能提供更高的模型准确率。

### 7. 优点
*   **协同设计视角**：不只是单纯改进算法或系统，而是通过算法产生的异构性倒逼系统调度优化，解决了动态稀疏在分布式环境下的落地难题。
*   **利用稳定性**：敏锐捕捉到注意力头稀疏模式的跨任务稳定性，使得离线 Profile 具有很强的泛化性。
*   **即插即用**：基于 MInference 等现有稀疏内核构建，易于集成到现有的推理框架中。

### 8. 不足与局限
*   **离线依赖**：虽然稀疏模式相对稳定，但对于分布极度异常的输入或全新的模型架构，可能仍需重新进行离线 Profile。
*   **阶段局限**：研究重点在于 **Prefill（预填充）阶段** 的计算优化，对于 Decoding（解码）阶段的内存绑定（Memory-bound）问题及 KV Cache 压缩讨论较少。
*   **硬件环境单一**：实验主要在 A100 集群上完成，未探讨在更小显存或非 NVLink 环境（如 PCIe 连接）下的通信开销对负载均衡效果的影响。

（完）
