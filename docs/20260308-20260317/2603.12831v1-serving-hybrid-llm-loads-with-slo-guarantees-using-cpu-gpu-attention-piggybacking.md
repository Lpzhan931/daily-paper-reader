---
title: Serving Hybrid LLM Loads with SLO Guarantees Using CPU-GPU Attention Piggybacking
title_zh: 使用 CPU-GPU 注意力搭载实现具有 SLO 保证的混合 LLM 负载服务
authors: "Zizhao Mo, Junlin Chen, Huanle Xu, Chengzhong Xu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12831v1"
tags: ["query:llm"]
score: 9.0
evidence: 优化大语言模型服务的吞吐量和延迟
tldr: 针对共享集群中延迟敏感型（LS）与尽力而为型（BE）LLM服务共存导致的干扰和资源受限问题，本文提出OmniServe系统。该系统核心是Attention Piggybacking机制，能将BE服务的注意力计算动态卸载至CPU，并通过异步通信避免GPU阻塞。结合层级动态批处理策略，OmniServe在保障LS服务SLO的同时，极大提升了BE服务的吞吐量，实现了CPU与GPU资源的高效协同。
selection_source: fresh_fetch
motivation: 传统共享集群中的LLM服务因资源竞争导致延迟敏感型任务难以满足SLO，且尽力而为型任务受限于GPU内存。
method: 引入Attention Piggybacking机制将BE任务的注意力计算卸载到CPU，并采用异步通信和层级动态批处理优化计算。
result: 实验表明，LS服务的SLO达成率提升了1.48倍，同时BE服务的吞吐量提升了9.85倍。
conclusion: OmniServe通过精细化的CPU-GPU资源调度，有效解决了混合LLM负载下的干扰问题并显著增强了系统容量。
---

## 摘要
如今，服务提供商通常在共享集群中部署多种类型的 LLM 服务。虽然服务协同部署提高了资源利用率，但它为延迟敏感型（LS）服务带来了显著的干扰风险——这些服务对推理延迟有严格的 SLO 要求——并且由于可用内存有限，严重限制了尽力而为型（BE）服务的服务能力。为了解决干扰问题，现有系统通常依赖于预留余量来限制 BE 资源的消耗。然而，这种方法的粗粒度损害了延迟敏感型服务的 SLO 合规性，并由于不必要的限制，约束了尽力而为型服务的生成潜力。在本文中，我们提出了 OmniServe，这是一种新型 LLM 服务系统，它能高效利用 CPU 和 GPU 资源来减轻干扰并提高吞吐量。OmniServe 的核心是注意力搭载（Attention Piggybacking）机制，它能实时有效地将 BE 服务的注意力计算卸载到 CPU 上。该机制还促进了 CPU 和 GPU 流之间的异步通信，防止 GPU 在聚合注意力结果时被阻塞。此外，OmniServe 结合了动态批处理控制策略以适应波动的请求到达，利用逐层批处理促进密集（Dense）模块的计算。实验结果表明，与最先进的系统相比，OmniServe 将 LS 服务的 SLO 达成率提高了多达 1.48 倍，同时将 BE 服务的吞吐量提高了多达 9.85 倍。

## Abstract
Nowadays, service providers often deploy multiple types of LLM services within shared clusters. While the service colocation improves resource utilization, it introduces significant interference risks for latency-sensitive (LS) services-which have strict SLO requirements for inference latency-and severely constrain the service capacity of best-effort (BE) services due to limited available memory. To address interference, existing systems typically rely on reserving headroom to constrain BE resource usage. However, this approach's coarse granularity compromises the SLO compliance of the latency-sensitive service and unnecessarily restricts the generation potential of the best effort service.   In this paper, we propose OmniServe, a novel LLM serving system that efficiently harnesses both CPU and GPU resources to mitigate interference and improve throughput. Central to OmniServe is the Attention Piggybacking mechanism, which effectively offloads the Attention computation of BE services to CPUs on the fly. This mechanism also facilitates asynchronous communication between CPU and GPU streams, preventing GPUs from being blocked while aggregating Attention results. Additionally, OmniServe incorporates a dynamic batching control policy to adapt to fluctuating request arrivals, facilitating Dense module computation using layer-wise batching. Experimental results show that OmniServe improves the SLO attainment rate for LS services by up to $1.48\times$ while enhancing BE serving throughput by up to $9.85\times$ compared to state-of-the-art systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Serving Hybrid LLM Loads with SLO Guarantees Using CPU-GPU Attention Piggybacking》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在云端 LLM 服务中，服务商通常将**延迟敏感型（LS，如在线聊天）**和**尽力而为型（BE，如离线数据处理）**服务部署在同一集群以提高资源利用率。
*   **研究动机**：现有系统（如 Llumnix）主要通过预留 GPU 内存余量来隔离干扰，但存在两个痛点：
    1.  **干扰控制粗糙**：即使预留了内存，GPU 的计算单元（SM）和内存带宽竞争仍会导致 LS 服务的延迟波动（实验显示波动可达 50%）。
    2.  **BE 吞吐受限**：BE 服务受限于 GPU 显存容量，导致 GPU 计算资源在 LS 负载较低时无法被 BE 任务充分填补。
*   **核心问题**：如何在保证 LS 服务严格服务水平目标（SLO）的前提下，最大限度地利用闲置的 CPU 和 GPU 资源提升 BE 服务的吞吐量？

### 2. 论文提出的方法论：OmniServe
OmniServe 的核心思想是**异构资源协同与异步计算解耦**。
*   **注意力搭载机制（Attention Piggybacking）**：
    *   **核心逻辑**：将 BE 任务中计算密度较低但显存占用高的 Attention 模块卸载到 CPU 执行，而将计算密度极高的 Dense 模块（如 MLP、QKV 投影）保留在 GPU 上。
    *   **异步解耦**：GPU 流在处理 LS 任务时，不会为了等待 CPU 返回 BE 的 Attention 结果而阻塞。BE 的 Dense 计算会“搭载”在后续迭代的 GPU 算子中执行。
    *   **层级批处理（Layer-wise Batching）**：打破了传统的 Token 级连续批处理，允许在模型执行的每一层动态决定哪些 BE 请求可以加入批次，从而实现更细粒度的干扰控制。
*   **动态批处理控制与延迟建模**：
    *   系统建立了精确的模块级延迟模型。对于 Prefill 阶段，延迟 $f_{PA}$ 与计算量呈线性关系；对于 Decoding 阶段，延迟 $f_{DA}$ 则考虑了计算量和请求数量的综合影响。
    *   **调度算法**：通过二分查找等方式，在满足 LS 服务 SLO（如 TTFT 和 TPOT）的约束下，动态计算当前 GPU 能够“搭载”执行的 BE 请求最大数量。

### 3. 实验设计
*   **数据集/场景**：
    *   **LS 负载**：使用 ShareGPT 数据集模拟真实对话分布，采用泊松分布模拟请求到达。
    *   **BE 负载**：使用 LongBench-v2（长文本理解）和 DailyMails（摘要生成）模拟离线任务。
*   **Benchmark 与对比方法**：
    *   **Baseline A (Llumnix + vLLM)**：基于内存预留的 GPU 隔离方案，配合 CPU 端的独立推理。
    *   **Baseline B (NEO)**：现有的 CPU-GPU 协同推理系统，但缺乏针对混合负载的优先级控制。
    *   **Baseline C (Sarathi-Serve)**：仅使用 GPU 的 SLO 优化基准，代表了不使用 CPU 辅助时的性能上限。

### 4. 资源与算力
*   **硬件配置**：
    *   **GPU 服务器**：1 台主机，配备 4 块 **NVIDIA A100 (80GB)** GPU，通过 PCIe 连接。
    *   **CPU 资源**：主机配备 Intel Xeon Gold 6342 CPU（24 核）。
    *   **扩展资源**：另外配备 4 台纯 CPU 服务器（同型号 CPU），通过 100 Gbps RoCE 网络互联。
*   **模型规模**：测试了 **Yi-34B**（2 路张量并行）和 **Llama-2-70B**（4 路张量并行）。
*   **精度**：所有实验均采用 BF16 格式。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括：
    *   不同请求到达率（1-10 req/s）下的 SLO 达成率。
    *   不同硬件组合（1G, 1G4C）下的吞吐量对比。
    *   不同网络带宽（10Gbps vs 100Gbps）的影响。
    *   **消融实验**：验证了延迟建模的准确性（准确率 >93%）、准入控制的有效性以及系统操作开销（<1%）。
*   **充分性评价**：实验设计非常充分，涵盖了从单机到集群、从固定负载到突发流量（Bursty traffic）的多种复杂场景，对比基准选择合理，数据量足以支撑其结论。

### 6. 论文的主要结论与发现
1.  **SLO 保证**：在混合负载下，OmniServe 对 LS 服务的 SLO 达成率比 Llumnix 提升了高达 **1.48 倍**，且表现与纯 GPU 优化的 Sarathi-Serve 几乎一致。
2.  **吞吐量提升**：通过有效利用 CPU 内存和算力，BE 服务的吞吐量比现有系统提升了 **9.1 倍至 9.85 倍**。
3.  **异构优势**：实验发现 CPU 处理 Attention 虽然比 GPU 慢，但通过异步搭载机制，这种延迟可以被 GPU 的 Dense 计算完全掩盖，从而打破了“CPU 是瓶颈”的传统认知。

### 7. 优点（亮点）
*   **机制创新**：提出的 Attention Piggybacking 巧妙地利用了 LLM 逐层计算的特性，实现了 CPU 和 GPU 的真正异步并行。
*   **细粒度控制**：Layer-wise Batching 比传统的连续批处理更灵活，能更精准地压榨硬件性能而不违反 SLO。
*   **高扩展性**：系统支持分布式 CPU 参与计算，且能无缝集成到现有的 Prefill-Decode 分离架构中。

### 8. 不足与局限
*   **硬件依赖**：虽然论文证明了在 10Gbps 网络下依然有效，但大规模集群中 BE 任务的中间变量（Activation）传输仍可能受限于 PCIe 或网络带宽。
*   **模型适用性**：主要针对 Transformer 架构优化。对于非 Transformer 或具有特殊注意力机制（如线性注意力）的模型，其延迟建模和卸载策略可能需要大幅调整。
*   **CPU 算力门槛**：如果 CPU 算力极低（如旧款处理器），Attention 计算可能无法在多个 GPU 迭代内完成，导致异步队列堆积。

（完）
