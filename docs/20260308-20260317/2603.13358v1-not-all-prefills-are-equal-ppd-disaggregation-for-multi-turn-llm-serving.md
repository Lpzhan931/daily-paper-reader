---
title: "Not All Prefills Are Equal: PPD Disaggregation for Multi-turn LLM Serving"
title_zh: 并非所有 Prefill 都是平等的：面向多轮 LLM 服务的 PPD 解耦架构
authors: "Zongze Li, Jingyu Liu, Zach Xu, Yineng Zhang, Tahseen Rabbani, Ce Zhang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.13358v1"
tags: ["query:llm"]
score: 9.0
evidence: 优化多轮对话服务中的KV传输和预填充-解码解耦
tldr: 针对多轮对话中LLM推理引擎PD分离架构存在的KV缓存传输带宽瓶颈和重复计算效率低的问题，本文提出PPD架构。该架构识别出“追加预填充”对解码干扰较小的特性，通过动态路由策略将后续轮次的请求留在解码节点本地处理，从而在减少数据传输的同时显著降低首字延迟并优化系统吞吐。
selection_source: fresh_fetch
motivation: 传统PD分离架构在多轮对话中因频繁跨节点传输KV缓存导致带宽饱和，且未利用增量预填充对解码干扰较小的特性。
method: 提出PPD动态路由系统，根据SLO目标灵活决定是将后续轮次的追加预填充任务在解码节点本地执行，还是路由至预填充节点。
result: "实验显示PPD将多轮对话的首字延迟（TTFT）降低了68%，并有效缓解了高负载下的KV传输拥塞，同时保持了稳定的每字输出延迟。"
conclusion: PPD通过差异化处理预填充任务，为多轮LLM推理提供了一种兼顾灵活性与高效性的新型服务范式。
---

## 摘要
Prefill-Decode (PD) 解耦已成为现代大语言模型（LLM）推理引擎的标准架构，它缓解了两种不同工作负载之间的干扰。随着聊天机器人和智能体系统对多轮交互需求的日益增长，我们重新审视了这种情况下的 PD 架构，并发现了两个根本性的低效之处：(1) 每一轮都需要对新提示词和上一轮的响应进行 prefill；(2) prefill 节点与 decode 节点之间频繁的 KV 传输使带宽饱和，导致高延迟甚至服务降级。我们的核心见解是，并非所有的 prefill 操作都具有相同的干扰性：append-prefill（仅处理新输入的 token 并复用缓存的 KV 状态）引起的解码减速明显低于 full prefill。这促使我们将 append-prefill 路由到本地的 decode 节点。然而，通过全面分析，我们证明了没有任何一种单一的固定路由策略能同时满足所有的服务水平目标 (SLO)。基于这一见解，我们提出了 Prefill Prefill-capable Decode (PPD) 解耦架构，这是一种动态路由系统，用于决定何时利用缓存的 KV 状态在 decode 节点上本地处理第 2 轮及之后的请求。PPD 通过可配置的权重适应不同的 SLO，并能与传统的 PD 部署无缝集成。通过广泛的评估，我们展示了 PPD 在保持具有竞争力的逐 token 生成时间 (TPOT) 的同时，将第 2 轮及之后请求的首 token 延迟 (TTFT) 降低了 68%，有效缓解了高负载下的 KV 传输拥塞。我们认为 PPD 为多轮 LLM 服务提供了一种灵活且高效的范式。

## Abstract
Prefill-Decode (PD) disaggregation has become the standard architecture for modern LLM inference engines, which alleviates the interference of two distinctive workloads. With the growing demand for multi-turn interactions in chatbots and agentic systems, we re-examined PD in this case and found two fundamental inefficiencies: (1) every turn requires prefilling the new prompt and response from the last turn, and (2) repeated KV transfers between prefill and decode nodes saturate the bandwidth, leading to high latency and even service degradation. Our key insight is that not all prefill operations are equally disruptive: append-prefill -- processing only the new input tokens while reusing cached KV states -- incurs substantially less decoding slowdown than full prefill. This motivates routing append-prefill to decode nodes locally. However, through comprehensive analysis, we show that no single fixed routing strategy satisfies all Service Level Objectives (SLOs) simultaneously. Based on this insight, we propose Prefill Prefill-capable Decode (PPD) disaggregation, a dynamic routing system that decides when to process Turn 2+ requests locally on decode nodes using cached KV states. PPD adapts to varying SLOs via configurable weights and seamlessly integrates with traditional PD deployments. With extensive evaluations, we show that PPD reduces Turn 2+ time-to-first-token (TTFT) by 68% while maintaining competitive time-per-output-token (TPOT), effectively alleviating KV transfer congestion under high load. We believe PPD represents a flexible and efficient paradigm for multi-turn LLM serving.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在多轮对话场景下的推理效率问题，提出了名为 **PPD (Prefill Prefill-capable Decode)** 的动态路由解耦架构。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **背景：** 现代 LLM 推理通常采用“预填充-解码（Prefill-Decode, PD）分离”架构，将计算密集型的 Prefill 和访存密集型的 Decode 分配到不同的 GPU 池中，以减少干扰。
*   **核心问题：** 在**多轮对话**中，传统 PD 架构存在两大低效：
    1.  **重复计算：** 每一轮对话都需要重新对历史上下文进行 Prefill。
    2.  **带宽瓶颈：** Prefill 节点与 Decode 节点之间频繁传输巨大的 KV Cache，导致网络带宽饱和，增加首字延迟（TTFT）并引发服务降级。
*   **研究动机：** 作者发现并非所有 Prefill 操作对 Decode 的干扰都一样。处理新 Token 的“追加预填充（Append-prefill）”对解码速度的影响远小于处理全新上下文的“全量预填充（Full prefill）”。

### 2. 方法论
*   **核心思想：** 既然追加预填充干扰小，可以打破“物理隔离”的教条，动态地将多轮对话中的后续轮次（Turn 2+）放在 Decode 节点本地处理，从而复用本地 KV 缓存，消除网络传输。
*   **关键技术细节：**
    *   **差异化干扰分析：** 通过微架构实验量化发现，Full prefill 会导致解码减速约 48%，而 Append-prefill 仅导致约 2% 的减速。
    *   **PPD 动态路由系统：** 建立一个优化模型 $J(x; \pi, \phi)$，其中 $x$ 是路由到 Decode 节点的请求比例。系统根据当前的系统负载（QPS）、用户指定的 SLO（如更看重 TTFT 还是 TPOT）以及节点配置，动态决定每个请求的去向。
    *   **算法流程：**
        1.  **离线阶段：** 构建决策表，通过基准测试记录不同负载和路由策略下的 TTFT 和 TPOT 表现。
        2.  **在线阶段：** 路由代理（Proxy）根据请求的轮次、上下文长度和预设权重，查表决定是将请求发往 Prefill 节点（传统 PD 模式）还是留在 Decode 节点（PPD 模式）。决策开销极低（<1ms）。

### 3. 实验设计
*   **数据集/场景：**
    *   **合成负载：** 设计了 18 种合成工作负载，涵盖解码密集型、平衡型和预填充密集型场景。
    *   **真实数据集：** 使用 **ShareGPT** 和 **WildChat**（包含真实用户多轮对话记录）。
*   **Benchmark 与对比方法：**
    *   **Replica：** 传统的全复制模式（不分离）。
    *   **PD (x=0)：** 标准的预填充-解码分离架构。
    *   **Full AP-to-D (x=1)：** 强制所有后续轮次在 Decode 节点处理。
    *   **PPD：** 本文提出的动态路由方法。

### 4. 资源与算力
*   **硬件环境：** 使用了 4 台 **NVIDIA H100 80GB HBM3** GPU，通过 NVLink 连接。
*   **模型：** 主要实验基于 **Llama-3.1-8B**，验证实验扩展到了 **Qwen2.5-14B** 和 **Qwen3-30B-A3B**。
*   **训练/运行：** 论文侧重于推理服务（Serving），未提及模型训练时长，但详细记录了 3,060 组推理配置下的性能数据。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 17 种节点配置、18 种工作负载类型、10 个 QPS 等级，共计 **3,060 个测试点**。
*   **充分性：** 实验非常充分。除了基础性能对比，还进行了消融实验（如不同轮次数量、不同模型大小的影响）以及针对 SLO 权重的权衡分析。
*   **客观性：** 实验对比了多种极端情况（如 Prefill 资源极度匮乏或过剩），并使用了真实世界的数据集进行验证，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升：** PPD 将第 2 轮及之后请求的 **TTFT 降低了 68%**，同时保持了具有竞争力的 TPOT。
*   **稳定性：** 在高负载下，传统 PD 架构常因 KV 传输导致服务崩溃（成功率低于 95%），而 PPD 通过减少 75% 的网络传输量，使系统保持 100% 的成功率。
*   **无最优固定策略：** 实验证明没有任何一种固定的路由比例能同时优化所有指标，动态调整是实现 Pareto 最优的唯一途径。

### 7. 优点
*   **见解深刻：** 敏锐地捕捉到 Append-prefill 与 Full prefill 在干扰特性上的本质区别。
*   **实用性强：** 能够无缝集成到 vLLM 等现有推理框架中，且决策开销几乎可以忽略不计。
*   **灵活性高：** 允许服务商根据业务需求（如优先保证生成流畅度还是响应速度）调整权重。

### 8. 不足与局限
*   **模型规模限制：** 大部分核心结论基于 8B 模型得出，虽然在 14B/30B 上做了验证，但在超大规模模型（如 175B+）上的 KV 缓存压力和干扰特性可能有所不同。
*   **离线依赖：** 动态路由依赖于离线的基准测试表，如果硬件环境或模型发生变化，需要重新进行 Profiling。
*   **内存压力：** 将 Prefill 任务留在 Decode 节点会增加 Decode 节点的显存占用（用于存储更多轮次的 KV Cache），论文对显存极限承载能力的讨论相对较少。

（完）
