---
title: High-Fidelity Pruning for Large Language Models
title_zh: 大语言模型的高保真剪枝
authors: "Yijun Zhu, Jianxin Wang, Chengchao Shen"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08083v1"
tags: ["query:llm"]
score: 10.0
evidence: 使用自蒸馏进行重要性评估的大语言模型高保真剪枝
tldr: 针对大语言模型剪枝中传统泰勒展开法仅依赖交叉熵损失、忽略输出分布全局信息的局限，本文提出了一种高保真剪枝方法 HFPrune。该方法创新性地利用模型输出分布的信息熵作为重要性评估准则，无需额外教师模型即可实现高效的神经元评估。实验证明，该方法在 LLaMA 和 Qwen 系列模型上均优于现有剪枝技术，在降低计算开销的同时有效保持了模型的预测性能。
selection_source: fresh_fetch
motivation: 传统的泰勒剪枝方法仅基于交叉熵损失评估重要性，忽略了输出分布的其他潜在预测信息，且自蒸馏方案计算成本过高。
method: 提出以模型输出分布的信息熵作为泰勒剪枝的评估准则，在不依赖教师模型的情况下实现对神经元重要性的全局化衡量。
result: 在 LLaMA 和 Qwen 系列模型上的零样本基准测试显示，该方法在性能保持上一致优于现有的主流剪枝方法。
conclusion: HFPrune 通过引入信息熵准则，为大语言模型提供了一种简单、高效且能保持高保真度的剪枝新方案。
---

## 摘要
大语言模型（LLMs）在广泛的任务中展现了卓越的性能，但其巨大的计算和内存需求给部署带来了重大挑战。一种常见的方法是利用损失函数的泰勒展开（Taylor expansion）来估计神经元的重要性。然而，由于其依赖于 one-hot 交叉熵损失，一个关键的局限性是它仅根据分配给单个预测的下一个 token 的概率来狭隘地评估重要性，从而忽略了原始模型的其他潜在预测。解决这一问题的一个直观方案是采用自蒸馏（self distillation）准则进行重要性评估。然而，这种方法需要一个单独的教师模型进行监督，从而引入了显著的计算开销。为此，我们提出了一种简单而有效的准则——模型输出分布的信息熵（information entropy），以便在不需要额外教师模型的情况下，利用泰勒剪枝高效地评估神经元的重要性得分。与普通的交叉熵准则相比，它为泰勒剪枝提供了一个更全面的准则，以全局方式剪掉对模型预测影响最小的神经元，从而保持了模型预测能力的高保真度。在广泛的零样本（zero-shot）基准测试上的实验结果表明，我们的方法在 LLaMA 和 Qwen 系列模型上始终优于现有的剪枝方法。源代码和训练权重可在 https://github.com/visresearch/HFPrune 获取。

## Abstract
Large Language Models (LLMs) have demonstrated exceptional performance across a wide range of tasks, yet their significant computational and memory requirements present major challenges for deployment. A common approach uses Taylor expansion on the loss function to estimate neuron importance. However, its reliance on one-hot cross entropy loss, a key limitation is that it narrowly assesses importance based only on the probability assigned to the single predicted next token, thereby ignoring the other potential predictions of the original model. An intuitive solution to address this is to employ self distillation criterion for importance evaluation. However, this approach introduces significant computational overhead by requiring a separate teacher model for supervision. To this end, we propose a simple but effective criterion, information entropy of the model's output distribution, to efficiently evaluate importance scores of neurons with Taylor pruning without requirement of additional teacher. Compared to plain cross entropy criterion, it provides a more holistic criterion for Taylor pruning to prune neurons with the least impact on the prediction of model in a global manner, thereby preserving the fidelity of the model's predictive capabilities. Experimental results on extensive zero-shot benchmarks demonstrate that our method consistently outperforms existing pruning methods across the LLaMA and Qwen series models. The source code and trained weights are availabel at https://github.com/visresearch/HFPrune.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HFPrune（High-Fidelity Pruning）** 的大语言模型（LLM）结构化剪枝方法。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 参数量巨大，部署成本高。传统的泰勒剪枝（Taylor-based pruning）方法主要依赖**交叉熵（Cross-Entropy, CE）损失**来评估神经元重要性。
*   **研究动机**：
    *   **CE 的局限性**：交叉熵仅关注“正确答案”（one-hot 标签）的概率，忽略了输出分布中其他潜在预测的信息（即“暗知识”），导致剪枝过程无法完整保留模型的原始预测特性。
    *   **自蒸馏的缺陷**：虽然自蒸馏可以利用全局分布，但它需要额外的教师模型（计算开销大），且在剪枝初期由于教师与学生模型完全一致，会导致梯度为零，无法有效评分。
*   **整体含义**：提出一种更高效、高保真的剪枝准则，旨在不依赖教师模型的情况下，通过保留模型输出分布的完整性来减少性能损失。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：引入**信息熵（Information Entropy）**作为泰勒剪枝的重要性评估准则。
*   **关键技术细节**：
    1.  **信息熵准则**：定义准则 $C_H(x) = -\sum p_j(x) \log p_j(x)$。信息熵反映了模型预测的整体置信度和分布特征，是一个**无标签（Label-free）**的指标。
    2.  **重要性评分**：利用一阶泰勒展开估计移除第 $i$ 个神经元（激活值为 $h_i$）引起的信息熵变化：$I_i = |\frac{\partial C_H}{\partial h_i} h_i|$。
    3.  **剪枝对象**：重点针对 **MLP 模块**（在 Llama2-7B 中占约 68.3% 的参数）。研究发现剪枝 MLP 比剪枝注意力头更稳定，灾难性遗忘风险更低。
    4.  **流程**：
        *   在校准集上计算每个神经元的重要性得分。
        *   按比例移除得分最低的神经元（修改权重矩阵的行列）。
        *   进行简短的微调（使用 LoRA）以恢复性能。

### 3. 实验设计
*   **模型系列**：LLaMA-2 (7B), LLaMA-3.2 (1.2B, 3.2B), Qwen-2.5 (1.5B, 7B), Qwen-3 (1.7B)。
*   **数据集**：
    *   **校准**：C4 数据集（随机采样约 4.3 万个序列）。
    *   **微调**：LaMini-instruction 指令遵循数据集。
    *   **评估**：10 个零样本（Zero-shot）基准测试（如 ARC, BoolQ, PIQA, Race, TruthfulQA, Winogrande 等）。
*   **对比方法**：LLM-Pruner, LoRAPrune, LoRAP, SDMPrune, Compresso 等主流结构化剪枝方法。

### 4. 资源与算力
*   **硬件使用**：
    *   **推理/延迟测试**：单张 NVIDIA A6000 GPU。
    *   **7B 模型微调**：8 张 NVIDIA A6000 GPU（Batch size 512）。
    *   **较小模型微调**：4 张 NVIDIA RTX 4090 GPU（Batch size 256）。
*   **效率优势**：在 Llama2-7B 上，HFPrune 的剪枝过程比 SDMPrune 快约 **3 倍**，且峰值显存占用降低了 **31%**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.2B 到 7B 不同规模、不同家族（LLaMA 和 Qwen）的多个模型。
*   **消融实验**：
    *   对比了不同准则（CE vs. SD vs. 信息熵）在不微调情况下的表现。
    *   分析了输出分布的相似度（JS 散度、Top-15 Jaccard 相似度）。
    *   对比了仅剪枝 MLP 与同时剪枝 MLP+Attention 的效果。
*   **充分性评价**：实验设计非常充分，不仅有量化的准确率对比，还有推理加速比测试和分布保真度的定性分析，证明了方法的客观性和公平性。

### 6. 主要结论与发现
*   **性能卓越**：在 20% 和 30% 的剪枝比例下，HFPrune 一致优于现有方法。
*   **性能恢复**：在 Llama2-7B 剪枝 20% 参数后，经过微调，其性能甚至**超过了原始稠密模型**（59.0% vs 58.3%）。
*   **高保真度**：信息熵准则能更好地维持模型原始的输出分布形状（更低的 JS 散度）。
*   **加速明显**：30% 的 MLP 剪枝可带来约 1.47 倍的预填充（Prefill）加速。

### 7. 优点
*   **简洁高效**：无需教师模型，避免了复杂的蒸馏设置和显存开销。
*   **理论优雅**：将信息熵引入泰勒剪枝，从全局分布角度评估神经元价值，逻辑自洽。
*   **标签无关**：作为一种 Label-free 方法，它对校准数据的依赖更具普适性。
*   **工程实用**：提供了显著的显存节省和处理速度提升。

### 8. 不足与局限
*   **微调依赖**：虽然剪枝准则很强，但要达到超越原模型的性能，仍需依赖后期的指令微调阶段。
*   **剪枝粒度**：目前主要集中在 MLP 层的神经元剪枝，对于注意力机制的深度压缩探讨相对较少。
*   **任务覆盖**：主要评估了零样本常识推理任务，对于长文本生成、代码编写等复杂任务的保真度有待进一步验证。

（完）
