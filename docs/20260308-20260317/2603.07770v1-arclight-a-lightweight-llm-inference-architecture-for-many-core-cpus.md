---
title: "ArcLight: A Lightweight LLM Inference Architecture for Many-Core CPUs"
title_zh: ArcLight：一种面向多核 CPU 的轻量级大语言模型推理架构
authors: "Yuzhuang Xu, Xu Han, Yuxuan Li, Wanxiang Che"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07770v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对多核CPU的轻量级LLM推理架构
tldr: "ArcLight 是一款专为多核 CPU 设计的轻量级大语言模型推理架构。针对现有框架在多 NUMA 节点平台上因跨节点内存访问导致的性能瓶颈，ArcLight 通过集成高效的内存管理、线程调度以及精细控制的张量并行技术，显著降低了内存访问开销。实验表明，其推理吞吐量比主流框架提升了高达 46%，且具备广泛的 CPU 设备兼容性，为多核 CPU 上的 LLM 部署提供了高效方案。"
selection_source: fresh_fetch
motivation: 现有 CPU 推理框架未能充分利用多核平台的计算潜力，且忽视了跨 NUMA 节点内存访问带来的严重性能损耗。
method: 设计了全新的轻量级架构，通过优化内存管理、线程调度和引入精细的张量并行机制来缓解跨节点内存墙问题。
result: "ArcLight 突破了主流框架的性能上限，实现了最高 46% 的推理吞吐量提升，并保持了对任意 CPU 设备的兼容性。"
conclusion: ArcLight 证明了通过针对性的架构优化可以显著提升多核 CPU 在 LLM 推理任务中的表现，具有极高的实用价值。
---

## 摘要
尽管现有的 CPU 大语言模型（LLM）推理框架已趋于成熟，但它们未能充分挖掘多核 CPU 平台的计算潜力。多核 CPU 广泛部署于 Web 服务器和高端网络设备中，通常被组织成多个将核心与内存分组的 NUMA 节点。当前框架在很大程度上忽视了跨 NUMA 内存访问带来的巨大开销，限制了此类平台上的推理可扩展性和智能化应用。为了解决这一局限性，我们构建了 ArcLight，这是一种从底层开始为多核 CPU 设计的轻量级 LLM 推理架构。ArcLight 集成了高效的内存管理和线程调度，并引入了精细控制的张量并行，以缓解跨节点内存访问墙问题。实验结果表明，ArcLight 显著超越了主流框架的性能上限，推理吞吐量提升高达 46%。此外，ArcLight 保持了对任意 CPU 设备的兼容性。ArcLight 已在 https://github.com/OpenBMB/ArcLight 公开发布。

## Abstract
Although existing frameworks for large language model (LLM) inference on CPUs are mature, they fail to fully exploit the computation potential of many-core CPU platforms. Many-core CPUs are widely deployed in web servers and high-end networking devices, and are typically organized into multiple NUMA nodes that group cores and memory. Current frameworks largely overlook the substantial overhead of cross-NUMA memory access, limiting inference scalability and intelligence enabling on such platforms. To address this limitation, we build ArcLight, a lightweight LLM inference architecture designed from the ground up for many-core CPUs. ArcLight integrates efficient memory management and thread scheduling, and introduces finely controlled tensor parallelism to mitigate the cross-node memory access wall. Experimental results show that ArcLight significantly surpasses the performance ceiling of mainstream frameworks, achieving up to 46% higher inference throughput. Moreover, ArcLight maintains compatibility with arbitrary CPU devices. ArcLight is publicly available at https://github.com/OpenBMB/ArcLight.

---

## 论文详细总结（自动生成）

以下是对论文《ArcLight: A Lightweight LLM Inference Architecture for Many-Core CPUs》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 CPU 推理框架（如 llama.cpp）在多核（Many-Core）CPU 平台上无法充分发挥计算潜力。
*   **背景**：现代高性能 CPU 通常采用 **NUMA（非统一内存访问）架构**，将核心与内存划分为多个节点。跨节点访问内存的延迟远高于本地访问。
*   **研究动机**：主流框架往往忽视了“跨 NUMA 内存访问墙”问题，导致在扩展到数十个核心时，数据同步和非本地内存访问成为性能瓶颈。此外，现有框架代码日益臃肿，难以进行底层的 NUMA 感知优化。

### 2. 方法论：核心思想与关键技术
ArcLight 从底层构建了一个轻量级、模块化的推理架构，核心思想是通过精细化的资源管理消除跨节点开销：
*   **轻量化架构**：采用解耦设计，仅包含内存管理、线程管理、张量库、计算图构建和调度器五个核心模块，代码量极小（约 10 个 C++ 文件）。
*   **NUMA 感知内存管理**：放弃单一内存池，转而为每个 NUMA 节点分配本地缓冲区（Weight/Activation/KV Cache），实现张量与节点的显式绑定。
*   **多视图线程组织**：支持将线程池动态划分为多个逻辑线程组，允许并行执行多个独立的子图操作。
*   **跨 NUMA 张量并行 (TP)**：
    *   引入 **Scatter** 和 **Gather** 算子，将模型权重按节点切分。
    *   通过张量并行使计算尽量发生在节点内部，减少跨节点数据流动。
*   **异步子图执行**：优化线程同步机制，允许不同线程组在执行 TP 任务时异步运行，减少全局屏障（Barrier）带来的空闲等待。

### 3. 实验设计
*   **硬件平台**：192 核华为鲲鹏 920 (ARMv8.2) 服务器，包含 4 个 NUMA 节点。
*   **模型与量化**：Qwen3-4B 模型，采用 Q4_0 量化格式。
*   **Benchmark**：与目前最流行的 CPU 推理框架 **llama.cpp** 进行对比。
*   **测试场景**：
    1.  **单节点扩展性**：测试 6 到 48 核在单一 NUMA 节点内的表现。
    2.  **多节点并行性**：测试 2 节点和 4 节点配置下的解码吞吐量。
    3.  **长短文本测试**：对比 Prompt 长度为 15 和 300 时的性能。

### 4. 资源与算力
*   **算力设备**：实验完全在 CPU 上进行，使用了 192 核的 ARM 服务器。
*   **GPU 使用**：文中**未提及**使用任何 GPU 资源，该研究专注于 CPU 推理优化。
*   **训练时长**：不涉及模型训练，仅针对推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了内存访问带宽基准测试、单节点性能对比、多节点 TP 性能对比以及 Prefill/Decoding 阶段的消融实验。
*   **充分性**：实验涵盖了多核 CPU 推理的核心痛点（内存墙、线程同步），对比对象（llama.cpp）具有行业代表性。
*   **客观性**：通过控制变量（如相同核心数、相同量化格式）确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：在多核环境下，ArcLight 的推理吞吐量比 llama.cpp 提升了高达 **46%**。
*   **内存墙验证**：实验证实本地内存访问速度比跨节点访问快约 **4 倍**，证明了 NUMA 优化的必要性。
*   **同步优化有效性**：异步子图执行（Sync B）相比全局同步能额外带来约 5 tokens/s 的增益。
*   **计算绑定 vs 内存绑定**：在 Prefill 阶段（计算密集型），ArcLight 的优势较 Decoding 阶段（内存密集型）略有缩小，但依然保持领先。

### 7. 优点
*   **极简设计**：模块化程度高，为研究人员提供了一个透明、可高度定制（Hackable）的 CPU 推理基础框架。
*   **突破瓶颈**：针对多核 CPU 的 NUMA 架构进行了深度定制，解决了主流框架难以处理的硬件特性问题。
*   **开源贡献**：项目已开源，有助于推动 CPU 侧 LLM 部署的生态发展。

### 8. 不足与局限
*   **平台覆盖有限**：目前仅在 ARM 架构上进行了评估，尚未展示在 x86（如 Intel Xeon, AMD EPYC）平台上的优化效果。
*   **算子库依赖**：ArcLight 尚未建立完全独立的算子库，部分数学内核（如 GEMM）仍重用了 llama.cpp 的实现。
*   **模型多样性**：实验主要集中在 Qwen3-4B，对于更大参数规模（如 70B+）或不同架构模型的适配情况有待进一步验证。

（完）
