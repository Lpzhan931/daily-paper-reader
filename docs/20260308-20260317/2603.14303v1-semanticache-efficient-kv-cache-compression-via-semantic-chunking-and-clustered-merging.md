---
title: "SemantiCache: Efficient KV Cache Compression via Semantic Chunking and Clustered Merging"
title_zh: SemantiCache：通过语义分块与聚类合并实现的高效 KV 缓存压缩
authors: "Shunlong Wu, Hai Lin, Shaoshen Chen, Tingwei Lu, Yongqin Zeng, Shaoxiong Zhan, Hai-Tao Zheng, Hong-Gee Kim"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14303v1"
tags: ["query:llm"]
score: 10.0
evidence: 通过语义分块和聚类合并进行KV缓存压缩
tldr: 针对现有KV缓存压缩方法因破坏语义完整性导致性能下降的问题，本文提出SemantiCache框架。该方法通过分隔符将缓存划分为语义连贯的块，并利用贪婪种子聚类算法（GSC）将块内token聚类合并为语义核心，辅以比例注意力机制重平衡权重。实验表明，该方法在保持模型性能的同时，将推理解码速度提升高达2.61倍，并显著降低了内存占用。
selection_source: fresh_fetch
motivation: 现有的KV缓存压缩方法通常基于离散token或非语义块，容易导致语义碎片化和不可逆的信息损失。
method: 提出SemantiCache框架，通过分隔符划分语义块，并利用贪婪种子聚类算法与比例注意力机制将token合并为语义核心。
result: 在多个基准测试中，该方法实现了高达2.61倍的解码加速，并大幅减少了内存占用，同时保持了与原模型相当的性能。
conclusion: SemantiCache通过对齐语言的语义层次结构，为高效且保真的KV缓存压缩提供了一种有效的解决方案。
---

## 摘要
现有的 KV 缓存压缩方法通常作用于离散的 token 或非语义分块。然而，这类方法往往会导致语义碎片化，破坏语言上的连贯单元，从而造成不可逆的信息损失和模型性能下降。为了解决这一问题，我们提出了 SemantiCache，这是一种通过将压缩过程与语言的语义层次特性对齐来保持语义完整性的新型压缩框架。具体而言，我们首先利用分隔符（即自然的语义边界）将缓存划分为语义连贯的分块。在每个分块内，我们引入了一种计算高效的基于种子的贪婪聚类（GSC）算法，将 token 划分为语义簇。这些簇被进一步合并为语义核心，并辅以比例注意力（Proportional Attention）机制，以重新平衡合并 token 后减少的注意力贡献。在多种基准测试和模型上的广泛实验表明，SemantiCache 将推理的解码阶段加速了高达 2.61 倍，并显著减少了内存占用，同时保持了与原始模型相当的性能。

## Abstract
Existing KV cache compression methods generally operate on discrete tokens or non-semantic chunks. However, such approaches often lead to semantic fragmentation, where linguistically coherent units are disrupted, causing irreversible information loss and degradation in model performance. To address this, we introduce SemantiCache, a novel compression framework that preserves semantic integrity by aligning the compression process with the semantic hierarchical nature of language. Specifically, we first partition the cache into semantically coherent chunks by delimiters, which are natural semantic boundaries. Within each chunk, we introduce a computationally efficient Greedy Seed-Based Clustering (GSC) algorithm to group tokens into semantic clusters. These clusters are further merged into semantic cores, enhanced by a Proportional Attention mechanism that rebalances the reduced attention contributions of the merged tokens. Extensive experiments across diverse benchmarks and models demonstrate that SemantiCache accelerates the decoding stage of inference by up to 2.61 times and substantially reduces memory footprint, while maintaining performance comparable to the original model.

---

## 论文详细总结（自动生成）

以下是对论文《SemantiCache: Efficient KV Cache Compression via Semantic Chunking and Clustered Merging》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在处理长文本时，KV Cache（键值缓存）随序列长度线性增长，导致巨大的内存开销和推理延迟。
*   **研究动机**：现有的压缩方法（如剔除法或非语义合并法）通常作用于离散的 token 或固定大小的分块，忽略了语言的自然结构。这会导致“语义碎片化”（Semantic Fragmentation），即破坏了短语、从句或句子的完整性，造成不可逆的信息损失，进而降低模型性能。
*   **核心思想**：模仿人类记忆长文本的认知策略——先按语义单元（如句子）分段，再提取各段的核心概念。

### 2. 方法论
SemantiCache 提出了一个三阶段的语义层次化压缩框架：
*   **语义分块 (Semantic Chunking)**：利用自然语言的分隔符（如标点符号、换行符等）作为边界，将 KV Cache 划分为语义连贯的块。分隔符本身的 KV 状态被保留作为“结构锚点”。
*   **相似性聚类 (Similarity Clustering)**：引入**贪婪种子聚类算法 (GSC)**。在每个块内，将未分配的 token 作为“种子”，通过计算余弦相似度，将相似度高于阈值 $\tau$ 的后续 token 归入该簇。该算法只需单次顺序扫描，计算效率极高。
*   **聚类合并 (Clustered Merging)**：
    *   **均值池化**：将每个簇内的 KV 向量通过均值池化压缩为一个“语义核心”。
    *   **比例注意力 (Proportional Attention)**：为了防止合并后信息被稀释，在计算注意力分数时，在 Softmax 前的原始分值中加入 $\log s$（$s$ 为簇的大小）。这确保了合并后的 token 对注意力的贡献与其原始规模成正比。

### 3. 实验设计
*   **数据集/场景**：
    *   **LongBench**：包含 21 个数据集的多任务基准，涵盖单/多文档问答、摘要、少样本学习、合成任务和代码补全。
    *   **Needle-in-a-Haystack (NIAH)**：测试模型在大文档中检索特定信息的能力。
*   **基准模型**：Llama-3-8B-Instruct 和 Mistral-7B-Instruct-v0.2。
*   **对比方法**：
    *   **剔除类 (Eviction-based)**：StreamingLLM, H2O, SnapKV。
    *   **合并类 (Merging-based)**：CaM, D2O。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A100 80GB GPU** 上进行。
*   **说明**：文中未明确提及具体的 GPU 数量及总训练/测试时长，但考虑到其“训练无关”（Training-free）的特性，主要开销集中在推理测试阶段。

### 5. 实验数量与充分性
*   **实验规模**：在 21 个子数据集上进行了广泛测试，涵盖了 20%、35%、50% 等多种缓存预算。
*   **消融实验**：对比了“语义分块”与“固定大小分块”及“无分块（全局聚类）”的效果；研究了超参数 $\tau$ 对性能与压缩率的平衡影响。
*   **充分性评价**：实验设计较为全面，对比了当前主流的两类压缩路径（剔除与合并），并在不同架构的模型上验证了泛化性，结果具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **性能优异**：在相同的缓存预算下，SemantiCache 在 LongBench 和 NIAH 上的准确率均优于所有基线方法。
*   **高效加速**：在 32k 上下文长度下，相比全量 KV 缓存，推理解码速度（TPOT）提升了 **2.61 倍**。
*   **资源优化**：显著降低了内存占用，且由于 GSC 算法的轻量化，其首字延迟（TTFT）优于其他合并类方法。
*   **语义完整性**：实验证明，保持语义边界（分块）对于长文本理解至关重要，随机或固定位置的合并会严重损害模型逻辑。

### 7. 优点
*   **创新性视角**：首次明确提出并解决了 KV 缓存压缩中的“语义碎片化”问题。
*   **算法高效**：GSC 聚类算法简单且易于并行化，适合实时推理场景。
*   **即插即用**：无需对模型进行重新训练或微调，具有很强的实用价值。
*   **数学严谨**：通过比例注意力机制解决了合并操作带来的信息稀释问题。

### 8. 不足与局限
*   **超参数敏感性**：阈值 $\tau$ 的选择对压缩率和性能有显著影响，可能需要针对不同任务手动调优。
*   **分隔符依赖**：语义分块高度依赖预定义的分隔符集合，对于缺乏明显标点或格式特殊的文本（如某些加密文本或特定领域的原始数据），效果可能受限。
*   **语言局限性**：虽然在英文模型上表现良好，但未深入探讨在中文等其他语系（分词逻辑不同）上的表现差异。

（完）
