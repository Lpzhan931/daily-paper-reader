---
title: "OxyGen: Unified KV Cache Management for Vision-Language-Action Models under Multi-Task Parallelism"
title_zh: OxyGen：多任务并行下视觉-语言-动作模型的统一 KV 缓存管理
authors: "Xiangyu Li, Huaizhi Tang, Xin Ding, Weijun Wang, Ting Cao, Yunxin Liu"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14371v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对多任务并行VLA模型的统一KV缓存管理
tldr: 针对具身智能体在多任务并行（如操控、对话、记忆）中面临的计算冗余和资源竞争问题，本文提出了 OxyGen，一种统一的 KV 缓存管理范式。该方法将 KV 缓存视为跨任务和跨时间的共享资源，通过跨任务 KV 共享消除重复预填充，并利用跨帧连续批处理解耦语言解码与动作生成。在 π0.5 模型上的实验表明，OxyGen 实现了高达 3.7 倍的加速，在保证动作质量的同时显著提升了语言吞吐量和动作频率。
selection_source: fresh_fetch
motivation: 现有的具身智能推理系统因孤立的 KV 缓存管理，导致在处理共享观测值的多任务并行时存在严重的计算冗余和资源竞争。
method: 提出统一 KV 缓存管理范式，利用跨任务 KV 共享消除重复计算，并通过跨帧连续批处理解耦变长语言解码与定频动作生成。
result: 在 π0.5 模型上实现了 3.7 倍的推理加速，同时支持超过 200 tokens/s 的语言吞吐量和 70 Hz 的动作频率且无质量损失。
conclusion: 统一 KV 缓存管理是解决多任务视觉-语言-动作模型（VLA）在端侧高效部署、实现实时异构输出的关键技术。
---

## 摘要
具身智能体日益需要根据共享观测结果，在不同的时间约束下并行执行多项任务（如操作、对话和记忆构建）。近期的混合 Transformer (MoT) 视觉-语言-动作模型 (VLA) 在架构上支持此类异构输出，但现有的推理系统由于冗余计算和资源竞争，难以在端侧部署中实现高效的多任务并行。我们认为孤立的 KV 缓存管理是其根本原因。为此，我们提出了统一 KV 缓存管理，这是一种将 KV 缓存视为跨任务和跨时间的一等共享资源的推理范式。该抽象实现了两项关键优化：跨任务 KV 共享消除了共享观测的冗余预填充，而跨帧连续批处理则将变长语言解码与跨控制周期的固定速率动作生成解耦。我们针对最流行的 MoT VLA 模型 $π_{0.5}$ 实现了该范式，并在代表性的机器人配置下进行了评估。OxyGen 相比孤立执行实现了高达 3.7 倍的加速，在不降低动作质量的前提下，同时实现了超过 200 tokens/s 的语言吞吐量和 70 Hz 的动作频率。

## Abstract
Embodied AI agents increasingly require parallel execution of multiple tasks, such as manipulation, conversation, and memory construction, from shared observations under distinct time constraints. Recent Mixture-of-Transformers (MoT) Vision-Language-Action Models (VLAs) architecturally support such heterogeneous outputs, yet existing inference systems fail to achieve efficient multi-task parallelism for on-device deployment due to redundant computation and resource contention. We identify isolated KV cache management as the root cause. To address this, we propose unified KV cache management, an inference paradigm that treats KV cache as a first-class shared resource across tasks and over time. This abstraction enables two key optimizations: cross-task KV sharing eliminates redundant prefill of shared observations, while cross-frame continuous batching decouples variable-length language decoding from fixed-rate action generation across control cycles. We implement this paradigm for $π_{0.5}$, the most popular MoT VLA, and evaluate under representative robotic configurations. OxyGen achieves up to 3.7$\times$ speedup over isolated execution, delivering over 200 tokens/s language throughput and 70 Hz action frequency simultaneously without action quality degradation.

---

## 论文详细总结（自动生成）

以下是对论文《OxyGen: Unified KV Cache Management for Vision-Language-Action Models under Multi-Task Parallelism》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：具身智能机器人（如家庭助理）需要同时处理多种异构任务，例如在执行抓取动作的同时进行语音对话或构建长期记忆。
*   **核心挑战**：现有的混合 Transformer (MoT) 视觉-语言-动作模型 (VLA) 虽然在架构上支持多模态输出，但其推理系统通常采用“孤立执行”模式。这导致了两个效率瓶颈：
    1.  **计算冗余**：对同一帧观测值，动作和语言任务会重复进行预填充（Prefill）计算。
    2.  **资源竞争**：变长的语言解码与定频的动作生成在单一硬件上互相阻塞，难以满足实时控制的硬性时间约束。
*   **整体含义**：本文旨在通过重新设计 KV 缓存管理范式，解决多任务并行下的推理低效问题，实现高频动作控制与高吞吐语言生成的共存。

### 2. 方法论
*   **核心思想**：提出**统一 KV 缓存管理（Unified KV Cache Management）**，将 KV 缓存视为跨任务共享、跨时间持久化的一等资源。
*   **关键技术细节**：
    *   **跨任务 KV 共享（Cross-task KV Sharing）**：针对同一帧图像和指令输入，VLM 骨干网仅执行一次 Prefill，生成的 KV 缓存同时分发给动作专家（用于去噪生成）和语言专家（用于自回归解码），消除 1.4× 的冗余计算。
    *   **跨帧连续批处理（Cross-frame Continuous Batching）**：引入可恢复的生成状态（State），将语言解码从每帧控制循环中解耦。未完成的语言请求会被缓存，并在后续帧中与新请求进行批处理（Batching）解码，从而利用 GPU 的并行能力。
*   **算法流程**：
    1.  **Prefill 阶段**：处理新观测值，生成共享 KV 缓存。
    2.  **动作生成**：利用共享 KV 立即执行动作去噪。
    3.  **批处理解码**：管理器检索所有在途（In-flight）语言请求，合并 KV 缓存进行 $k$ 步并行解码。
    4.  **状态更新**：更新各请求的 Token 序列，移除已结束请求，持久化未完成状态。

### 3. 实验设计
*   **模型对象**：$\pi_{0.5}$（目前最流行的开源 MoT VLA 模型）。
*   **基准测试（Benchmarks）**：
    *   **LIBERO**：评估机器人操作任务的成功率。
    *   **DROID** & **ALOHA**：评估不同场景下的推理性能（频率与吞吐量）。
*   **对比方法**：
    *   **Baseline (Sequential)**：官方 openpi 框架的顺序孤立执行模式。
    *   **Parallel (MPS)**：利用 NVIDIA CUDA MPS 实现的进程级并行孤立执行。
*   **评估指标**：动作频率 (Hz)、语言吞吐量 (tokens/s)、显存占用、功耗及任务成功率。

### 4. 资源与算力
*   **硬件平台**：单张 **NVIDIA RTX 4090 GPU**。该配置被视为端侧/边缘机器人推理的代表性硬件。
*   **训练时长**：文中未提及模型训练时长，因为该研究专注于**推理阶段**的系统优化，直接使用了官方发布的预训练权重。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流数据集上进行了详尽的性能测试。
    *   涵盖了多种参数组合：不同的总解码长度 ($N=5 \sim 30$)、每帧解码步数 ($k=1 \sim 10$) 以及动作去噪步数 ($S=5 \sim 20$)。
    *   进行了**消融实验**，分别验证了 KV 共享和连续批处理对加速的贡献。
    *   进行了**工作负载泛化实验**，模拟了泊松分布到达和随机长度请求等复杂场景。
*   **充分性与公平性**：实验设计较为充分，通过 LIBERO 成功率测试证明了优化算法是“等效实现”，未牺牲模型精度。对比基线选择了官方开源框架，具有客观性。

### 6. 主要结论与发现
*   **大幅加速**：OxyGen 相比基线实现了高达 **3.7×** 的端到端加速。
*   **性能指标**：在保证动作频率达到 **70 Hz**（远超 50Hz 的实时要求）的同时，实现了超过 **200 tokens/s** 的语言吞吐量。
*   **能效提升**：由于减少了对 VLM 权重的重复读取和冗余计算，单位请求能耗降低了最高 **78%**，且显存增加极小（约 15%）。
*   **解耦优势**：证明了通过跨帧调度，可以有效缓解变长任务对实时任务的干扰。

### 7. 优点
*   **系统级创新**：不同于常见的模型剪枝或量化，本文从推理调度层面解决了具身智能特有的多模态冲突问题。
*   **即插即用**：不需要重新训练模型，对现有 MoT 架构的 VLA 模型具有良好的通用性。
*   **部署友好**：显著降低了机器人端侧部署的功耗和延迟，对电池驱动的移动机器人具有重要意义。

### 8. 不足与局限
*   **架构局限性**：该方法高度依赖于“共享骨干网+多专家”的 MoT 架构。如果机器人采用多个完全独立的模型组合，KV 共享的收益将消失。
*   **硬件覆盖**：实验仅限于高性能的 RTX 4090，在算力更受限的嵌入式设备（如 Jetson Orin 或计算棒）上的表现尚待验证。
*   **任务多样性**：目前仅验证了动作和语言任务，对于包含视频预测（World Model）等更高负载任务的并行调度，其复杂度和显存压力可能会显著增加。

（完）
