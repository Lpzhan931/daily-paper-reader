---
title: "GPrune-LLM: Generalization-Aware Structured Pruning for Large Language Models"
title_zh: GPrune-LLM：面向大语言模型的泛化感知结构化剪枝
authors: "Xiaoyun Liu, Divya Saxena, Jiannong Cao, Yuqing Zhao, Yiying Dong, Penghui Ruan"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.13418v1"
tags: ["query:llm"]
score: 10.0
evidence: 大语言模型的泛化感知结构化剪枝
tldr: 针对大语言模型结构化剪枝中因单一校准数据集导致的泛化性下降问题，本文提出GPrune-LLM框架。该方法通过识别神经元的分布敏感性差异，将神经元划分为行为一致的模块，并根据可靠性动态切换激活相关与无关的评估指标，最后自适应学习模块稀疏度。实验证明该方法显著提升了高稀疏度下的跨任务泛化能力，减少了对校准数据的依赖。
selection_source: fresh_fetch
motivation: 现有剪枝方法依赖单一校准集进行重要性评估，导致分布敏感神经元排挤掉对泛化至关重要的鲁棒神经元。
method: 通过将神经元划分为行为一致的模块，并根据分布敏感性动态选择评估指标及自适应分配稀疏度，实现泛化感知剪枝。
result: 实验表明GPrune-LLM在多种下游任务中一致优于现有方法，特别是在高稀疏度下表现出更强的跨任务泛化能力。
conclusion: 显式考虑神经元在不同分布下的行为差异，能有效缓解校准偏差，是提升大模型结构化剪枝鲁棒性的有效途径。
---

## 摘要
结构化剪枝被广泛用于压缩大语言模型（LLMs），但其有效性在很大程度上取决于神经元重要性评估。大多数现有方法根据单一校准数据集上的激活统计量来评估神经元重要性，这引入了校准偏差并降低了下游跨任务的泛化能力。我们观察到神经元表现出异质的分布敏感性，其中分布鲁棒型神经元在不同数据集间保持一致的排名，而分布敏感型神经元则表现出较高的跨数据集排名方差。基于此，我们识别出现有方法中的两个结构性局限。首先，在共享空间内对所有神经元进行排名会导致在校准输入上强烈激活的分布敏感型神经元占据主导地位，从而挤出对分布外任务至关重要的分布鲁棒型神经元。其次，统一应用基于激活的重要性指标可能并不可靠。在校准数据上不经常激活的分布敏感型神经元接收到的激活信号不足，无法进行准确的局部排名。为了解决这些局限，我们提出了 GPrune-LLM，这是一个泛化感知的结构化剪枝框架，它明确考虑了神经元在跨分布行为上的差异。我们首先将神经元划分为行为一致的模块以局部化排名竞争，然后根据分布敏感性和得分量级评估每个模块基于激活的指标的可靠性。对于基于激活的评分不可靠的模块，我们切换到与激活无关的指标。最后，我们自适应地学习模块化稀疏度。在多个下游任务上的广泛实验表明，GPrune-LLM 在压缩后的泛化能力方面取得了持续改进，特别是在高稀疏度下，并减少了对重要性指标选择的依赖。

## Abstract
Structured pruning is widely used to compress large language models (LLMs), yet its effectiveness depends heavily on neuron importance estimation. Most existing methods estimate neuron importance from activation statistics on a single calibration dataset, which introduces calibration bias and degrades downstream cross-task generalization. We observe that neurons exhibit heterogeneous distribution sensitivity, with distribution-robust neurons maintaining consistent rankings across datasets and distribution-sensitive neurons showing high cross-dataset ranking variance. Based on this, we identify two structural limitations in existing methods. First, ranking all neurons within a shared space causes distribution-sensitive neurons that strongly activate on calibration inputs to dominate, crowding out distribution-robust neurons critical for out-of-distribution tasks. Second, applying activation-based importance metrics uniformly can be unreliable. Distribution-sensitive neurons that infrequently activate on calibration data receive insufficient activation signal for accurate local ranking. To address these limitations, we propose GPrune-LLM, a generalization-aware structured pruning framework that explicitly accounts for neuron differences in cross-distribution behavior. We first partition neurons into behavior-consistent modules to localize ranking competition, then evaluate activation-based metric reliability per module according to distribution sensitivity and score magnitude. For modules where activation-based scoring is unreliable, we switch to an activation-independent metric. Finally, we adaptively learn module-wise sparsity. Extensive experiments across multiple downstream tasks demonstrate GPrune-LLM's consistent improvements in post-compression generalization, particularly at high sparsity, and reduced dependence on importance metric choice.

---

## 论文详细总结（自动生成）

以下是对论文《GPrune-LLM: Generalization-Aware Structured Pruning for Large Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 结构化剪枝方法高度依赖于在小型“校准数据集”上的激活统计量来评估神经元重要性。这导致了**校准偏差（Calibration Bias）**，即剪枝后的模型在非校准分布的任务上泛化性能显著下降。
*   **研究动机**：作者发现神经元具有异质的分布敏感性：
    *   **分布鲁棒神经元**：在不同数据集间排名稳定。
    *   **分布敏感神经元**：排名随数据集剧烈波动。
*   **现有局限**：
    1.  **跨行为排名干扰**：在统一空间排名时，校准集上的强激活敏感神经元会排挤掉对泛化至关重要的鲁棒神经元。
    2.  **局部排名失真**：对于在校准集上激活频率低的敏感神经元，激活信号不足以提供可靠的排名依据。

### 2. 方法论
GPrune-LLM 框架包含两个主要阶段：
*   **行为一致模块化（Behavior-consistent Modularization）**：
    *   **核心思想**：将具有相似分布敏感性的神经元聚类。
    *   **技术细节**：结合“参数相似度”（数据无关的协同激活代理）和“跨数据集排名漂移”（直接衡量分布敏感性）进行初始聚类。
    *   **优化**：使用可微细化目标函数（$L_{BCDM}$）进一步优化模块分配，确保模块内部在参数空间紧凑且在分布敏感性上一致。
*   **可靠性感知模块剪枝（Reliability-Aware Modular Pruning）**：
    *   **指标自适应**：评估每个模块的可靠性。若模块分布敏感且激活得分低（信号不可靠），则从基于激活的指标（如 FLAP）切换为与激活无关的指标（如权重范数）。
    *   **自适应稀疏度学习**：受动态稀疏训练启发，为每个模块学习独立的剪枝阈值。通过增强拉格朗日法（Augmented Lagrangian）在全局稀疏度预算下优化各模块的保留比例，并使用直通估计器（STE）解决不可微问题。

### 3. 实验设计
*   **实验对象**：LLaMA2-7B/13B、Vicuna1.5-7B、LLaVA1.5-7B（多模态模型）以及 LLaMA1-7B。
*   **评估指标**：
    *   语言建模：WikiText2 和 PTB 的困惑度（PPL）。
    *   零样本推理：7 个常识推理基准（如 BoolQ, PIQA, HellaSwag 等）。
    *   多模态：MME, MMBench, MMMU, ScienceQA。
*   **对比基准（Baselines）**：LLM-Pruner、Wanda-sp、FLAP。GPrune-LLM 作为插件应用于 Wanda-sp 和 FLAP 之上。
*   **稀疏度范围**：保留率从 80% 到 30% 不等。

### 4. 资源与算力
*   **硬件**：单张 NVIDIA RTX A6000 (48GB) GPU。
*   **耗时**：以 LLaMA2-7B 为例，行为一致模块化约需 1 小时，自适应稀疏度学习的每个训练周期（Epoch）约需 0.5 小时。
*   **校准数据**：使用 2048 个样本，序列长度为 128。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了多个模型家族、不同参数规模（7B, 13B）以及纯文本和多模态任务，实验设计较为全面。
*   **消融实验**：详细分析了模块化初始化、可微细化、指标自适应和自适应稀疏度学习四个组件的贡献。
*   **客观性**：通过在不同校准数据集组合（WikiText2, PTB, C4）下进行测试，验证了方法对校准集选择的鲁棒性。实验对比涵盖了当前主流的结构化剪枝方法，结果具有说服力。

### 6. 主要结论与发现
*   **泛化性提升**：GPrune-LLM 在所有模型和稀疏度下均优于基准方法，尤其在高稀疏度（如 50% 以下）时优势更明显。
*   **缓解崩溃**：在极端稀疏度（如 13B 模型的 30% 保留率）下，基准方法往往导致模型崩溃（PPL 激增），而 GPrune-LLM 能有效维持模型能力。
*   **指标解耦**：应用该框架后，不同基础指标（Wanda vs FLAP）之间的性能差距显著缩小，表明其纠正了结构层面的排名失真。
*   **推理加速**：在保持高性能的同时，实现了与基准方法相当的硬件推理加速（50% 稀疏度下约 1.5x 加速）。

### 7. 优点
*   **机制创新**：不同于以往单纯增加校准数据的“数据中心”方法，本文从剪枝机制本身入手，通过重构排名空间解决偏差。
*   **即插即用**：可以无缝集成到现有的重要性评估指标之上。
*   **多模态支持**：证明了该方法在视觉-语言模型（LLaVA）上的有效性，具有较强的通用性。

### 8. 不足与局限
*   **额外开销**：相比于一次性的简单剪枝（如 Wanda），该方法需要额外的模块化优化和阈值学习步骤，增加了计算成本。
*   **对 LLaMA1 效果有限**：在 LLaMA1 上的提升不如 LLaMA2 显著，作者分析这可能与早期模型神经元模块化程度较低有关。
*   **校准集依赖**：虽然减少了偏差，但仍需要两个不同的校准集（一个主集，一个辅助集用于计算排名漂移）来推断神经元行为。

（完）
