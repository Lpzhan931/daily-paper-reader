---
title: "Where Matters More Than What: Decoding-aligned KV Cache Compression via Position-aware Pseudo Queries"
title_zh: 位置比内容更重要：通过位置感知伪查询实现解码对齐的 KV 缓存压缩
authors: "Zhenxu Tian, Yi Su, Juntao Li, Min Zhang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11564v1"
tags: ["query:llm"]
score: 10.0
evidence: 通过伪查询实现解码对齐的KV缓存压缩
tldr: 针对大语言模型推理中KV缓存占用内存过大的问题，本文提出DapQ框架。现有方法多依赖预填充阶段的注意力模式，难以准确预测解码阶段的Token重要性。DapQ发现位置信息在模拟解码查询中比语义更关键，据此通过位置感知伪查询模拟输出Token，建立更精准的剔除机制。实验表明，在极低缓存预算下，DapQ仍能保持极高性能。
selection_source: fresh_fetch
motivation: 现有的KV缓存压缩方法主要基于预填充阶段的注意力模式，无法准确反映解码过程中模型对特定Token的依赖。
method: 提出DapQ框架，利用位置感知伪查询模拟解码阶段的查询向量，从而在推理前更准确地评估并剔除不重要的KV缓存。
result: "在多个基准测试中表现卓越，尤其在仅保留3% KV缓存预算时，在NIAH任务上达到了99.5%的近乎无损性能。"
conclusion: 该研究强调了位置信息在KV缓存管理中的核心作用，为实现超低内存占用的长文本推理提供了高效方案。
---

## 摘要
键值（KV）缓存对于大语言模型（LLM）的高效推理至关重要，但过长的上下文会显著增加 KV 缓存的内存占用。现有的 KV 缓存压缩方法通常依赖于预填充阶段 Prompt 观察窗口内的输入端注意力模式来评估 Token 的重要性。由于这些评估并非源自解码过程，它们无法为未来的生成保留关键 Token。直观上，一个有效的观察窗口应该镜像解码阶段的查询，以准确反映生成过程将关注哪些 Token。然而，在推理过程中，真实的解码查询本质上是不可用的。在构建伪查询以逼近真实查询时，我们发现位置信息比语义内容起着更关键的作用。基于这一见解，我们提出了通过位置感知伪查询实现的解码对齐 KV 缓存压缩（DapQ），这是一种新颖且轻量级的逐出框架，它利用位置感知伪查询来模拟输出 Token，从而为重要性评估建立有效的观察窗口。它与实际的生成上下文紧密对齐，并实现了精确的 Token 逐出。在多个基准测试和大语言模型上的广泛评估表明，DapQ 取得了卓越的性能，特别是在严格的内存限制下（例如，在仅保留 3% KV 缓存预算的情况下，在 NIAH 上实现了高达 99.5% 的近乎无损性能）。

## Abstract
The Key-Value (KV) cache is crucial for efficient Large Language Models (LLMs) inference, but excessively long contexts drastically increase KV cache memory footprint. Existing KV cache compression methods typically rely on input-side attention patterns within a prompt observation window to estimate token importance during the prefill stage. They fail to preserve critical tokens for future generation since these assessments are not derived from the decoding process. Intuitively, an effective observation window should mirror the decoding-stage queries to accurately reflect which tokens the generation process will attend to. However, ground-truth decoding queries are inherently unavailable during inference. For constructing pseudo queries to approximate them, we find that positional information plays a more critical role than semantic content. Motivated by this insight, we propose decoding-aligned KV cache compression via position-aware pseudo queries (DapQ), a novel and lightweight eviction framework that leverages position-aware pseudo queries to simulate the output tokens, thereby establishing an effective observation window for importance assessment. It aligns closely with the actual generation context and enables precise token eviction. Extensive evaluations across multiple benchmarks and LLMs demonstrate that DapQ achieves superior performance, particularly under strict memory constraints (e.g., up to nearly lossless performance 99.5% on NIAH with 3% KV cache budgets).

---

## 论文详细总结（自动生成）

这是一份关于论文《Where Matters More Than What: Decoding-aligned KV Cache Compression via Position-aware Pseudo Queries》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在处理长文本时，KV Cache（键值缓存）的内存占用呈线性增长，成为推理效率和部署的主要瓶颈。
*   **核心痛点**：现有的 KV 缓存压缩（Token 逐出）方法（如 SnapKV）通常在预填充（Prefill）阶段根据 Prompt 末尾的观察窗口来评估 Token 重要性。然而，这种“输入端”的评估与实际“输出端”解码时的查询（Query）存在失配，导致无法准确保留对未来生成至关重要的 Token。
*   **核心发现**：论文通过实验发现，在构造模拟解码阶段的“伪查询”时，**位置信息（Positional Information）对注意力模式的影响远比语义内容（Semantic Content）更重要**。

### 2. 方法论：DapQ 框架
*   **核心思想**：提出 **DapQ**（Decoding-aligned KV Cache Compression via Position-aware Pseudo Queries），通过在预填充阶段构造带有“未来位置编码”的伪查询，模拟真实的解码过程，从而实现更精准的缓存压缩。
*   **关键技术细节**：
    1.  **构造伪序列**：在原始 Prompt（长度为 $L_p$）后附加 $N$ 个伪 Token。
    2.  **位置对齐**：关键步骤是为这些伪 Token 分配它们在解码阶段本应拥有的位置 ID（即 $L_p$ 到 $L_p + N - 1$），而不是顺序递增。
    3.  **重要性评估**：模型处理该扩展序列，获取伪 Token 的 Query 向量（$Q_{pseudo}$）。计算这些伪查询对 Prompt 中所有 Key 的注意力分数并进行累加。
    4.  **精确逐出**：根据累加得分保留 Top-K 个最重要的 KV 对，随后立即丢弃所有伪 Token 及其缓存。
    5.  **解码对齐**：正式解码从位置 $L_p$ 开始，此时保留的 KV 缓存已根据未来的位置特征进行了优化。

### 3. 实验设计
*   **数据集/场景**：
    *   **LongBench & LongBenchV2**：评估长文本理解和深度推理能力。
    *   **Ruler**：测试不同长度下的长上下文有效容量。
    *   **HELMET**：涵盖长文本 QA、摘要、RAG 等多任务。
    *   **Needle-in-a-Haystack (NIAH)**：经典的大海捞针压力测试。
*   **对比方法**：
    *   **FullKV**（全量缓存）、**SnapKV**（基于窗口）、**PyramidKV**（层级动态分配）、**H2O**（基于 Heavy Hitter）、**StreamingLLM**（基于注意力锚点）、**LaCache**（阶梯状缓存）。
*   **测试模型**：LLaMA-3-8B-Instruct, LLaMA-3.1-8B-Instruct, Qwen2.5-7B-Instruct, Qwen3-8B。

### 4. 资源与算力
*   **硬件环境**：所有效率评估均在单张 **NVIDIA H20 (96G) GPU** 上完成。
*   **软件框架**：Python 3.10, PyTorch 2.6.0, Transformers 4.53.0。
*   **加速技术**：使用了 Flash-Attention 2.7.4。
*   **算力说明**：文中未明确提及总训练时长（因为该方法是 Training-free 的推理优化），但详细对比了吞吐量（tokens/s）和首字延迟（TTFT）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流模型系列和 5 个权威 Benchmark 上进行了广泛测试，涵盖了从 64 到 4096 不等的多种 KV 缓存预算设置。
*   **消融实验**：非常充分。作者分别验证了伪查询的**语义内容**（前缀、后缀、随机、无意义文本）、**窗口长度**（N 的大小）以及**插入位置**对性能的影响。
*   **客观性**：通过理论推导（证明 Query 相似度与注意力分布误差的关系）和 Recall 指标（衡量伪查询与真实查询选择 Token 的重合度）增强了实验的说服力。

### 6. 主要结论与发现
*   **位置主导性**：Query 向量的表示高度依赖于位置编码（如 RoPE），即使语义内容完全无关，只要位置正确，其注意力模式与真实解码查询的相似度依然很高（0.72 vs 0.35）。
*   **性能突破**：DapQ 在极低缓存预算下表现惊人。在 NIAH 任务中，仅保留 256 个 Token（约 3% 预算）时，准确率高达 **99.5%**，远超 SnapKV 等基准。
*   **效率优势**：DapQ 引入的额外预填充开销几乎可以忽略不计，其 TTFT（首字延迟）与 SnapKV 处于同一水平，且显著优于需要预生成的 Lookahead 方法。

### 7. 优点
*   **原理新颖**：揭示了位置信息在 KV 缓存管理中的核心地位，挑战了“语义驱动重要性”的传统认知。
*   **轻量高效**：无需微调模型，无需复杂的两阶段生成，实现简单且推理开销极低。
*   **鲁棒性强**：对伪查询的文本内容不敏感，这意味着开发者可以使用简单的固定前缀/后缀作为伪查询内容。

### 8. 不足与局限
*   **语义仍有残余影响**：虽然位置主导，但实验显示语义对齐仍能带来微小的性能提升，完全忽略语义可能在某些极端复杂的推理任务中存在风险。
*   **层级敏感度**：论文未深入探讨不同 Transformer 层对位置/语义敏感度的差异，可能存在进一步按层优化的空间。
*   **应用限制**：主要针对因果解码器（Causal Decoder）模型，对于其他架构的适用性尚未讨论。

（完）
