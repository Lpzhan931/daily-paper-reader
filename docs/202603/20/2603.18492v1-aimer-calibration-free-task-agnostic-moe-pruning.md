---
title: "AIMER: Calibration-Free Task-Agnostic MoE Pruning"
title_zh: AIMER：无需校准的任务无关 MoE 剪枝
authors: "Zongfang Liu, Shengkun Tang, Yifan Shen, Huan Wang, Xin Yuan"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18492v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对MoE模型的无校准专家剪枝
tldr: "混合专家模型（MoE）虽计算效率高，但巨大的参数量带来了沉重的存储负担。现有的专家剪枝方法通常依赖校准集，导致预处理成本高且对数据敏感。本文提出 AIMER，一种无需校准集的专家排序准则，通过计算权重矩阵的绝对平均值与均方根之比来评估重要性。在 7B 至 30B 规模的 MoE 模型上，AIMER 在 25% 和 50% 的剪枝率下表现优异，仅需约 1 秒即可完成评分，性能媲美或超越了现有的基于校准的方法。"
selection_source: fresh_fetch
motivation: 现有的 MoE 专家剪枝方法依赖校准数据集，导致预处理成本高且剪枝结果对数据分布过于敏感。
method: 提出 AIMER 准则，利用权重矩阵的绝对平均值与均方根之比作为重要性指标，实现无需数据的专家排序。
result: 在 16 个基准测试中，AIMER 仅需约 1 秒即可完成评分，且在 7B 到 30B 模型上的表现优于或等同于最先进的校准剪枝方法。
conclusion: AIMER 证明了无需校准数据的简单权重统计量即可实现高效且鲁棒的 MoE 专家剪枝，显著降低了模型部署门槛。
---

## 摘要
混合专家（MoE）语言模型在不按比例增加单 token 计算量的情况下提升了参数容量，但部署仍需存储所有专家，这使得专家剪枝对于减少内存和推理开销至关重要。现有的任务无关专家剪枝方法通常依赖于校准：它们根据校准集上的路由或激活统计数据来估计专家重要性，这使得剪枝结果对校准集的选择非常敏感，并增加了大量的预处理成本。我们提出了 AIMER（Absolute mean over root mean square IMportance for Expert Ranking），这是一种简单的无需校准的准则，能够产生清晰的层内分数分离和明显的专家分层。在 16 个基准测试中，针对 7B 到 30B 规模的 MoE 语言模型，在 25% 和 50% 的剪枝率下，AIMER 在专家评分仅需 0.22-1.27 秒的情况下，始终表现出与最先进的基于校准的专家剪枝基准相当或更强的综合性能。

## Abstract
Mixture-of-Experts (MoE) language models increase parameter capacity without proportional per-token compute, but the deployment still requires storing all experts, making expert pruning important for reducing memory and serving overhead. Existing task-agnostic expert pruning methods are typically calibration-dependent: they estimate expert importance from routing or activation statistics on a calibration set, which makes pruning outcomes sensitive to the choice of calibration set and adds substantial preprocessing cost. We introduce AIMER (\textbf{A}bsolute mean over root mean square \textbf{IM}portance for \textbf{E}xpert \textbf{R}anking), a simple calibration-free criterion that yields clear within-layer score separation and distinct expert stratification. Across 7B to 30B MoE language models at 25\% and 50\% pruning ratios over 16 benchmarks, AIMER consistently delivers competitive or stronger overall performance against state-of-the-art calibration-based expert pruning baselines with only 0.22--1.27 seconds for scoring the experts.

---

## 论文详细总结（自动生成）

### AIMER：无需校准的任务无关 MoE 专家剪枝总结

#### 1. 核心问题与整体含义（研究动机和背景）
混合专家模型（MoE）通过稀疏激活机制，在不增加单 token 计算量的前提下极大地扩展了模型参数量。然而，MoE 的部署面临巨大的挑战：所有专家参数都必须存储在内存中，导致显存占用极高。
**核心问题**：现有的专家剪枝方法（如 REAP、SEER 等）大多是“校准依赖型”的，即需要通过特定的校准数据集来统计专家的激活频率或贡献度。这带来了两个弊端：
*   **数据敏感性**：剪枝结果高度依赖校准集的选择，即使是同一来源的数据，样本量不同也会导致剪枝结果剧烈波动。
*   **高昂开销**：在剪枝前需要进行耗时的前向传播以收集统计信息，增加了部署的预处理成本。
**研究目标**：探索一种无需校准数据、仅依靠模型自身权重即可实现高效专家排序的剪枝准则。

#### 2. 方法论：核心思想与技术细节
论文提出了 **AIMER**（Absolute mean over root mean square IMportance for Expert Ranking），其核心思想是利用专家权重矩阵的统计特性来衡量其重要性。
*   **核心公式**：对于一个专家的所有参数组成的向量 $w$（长度为 $N$），其 AIMER 分数定义为：
    $$AIMER(w) = \frac{\|w\|_1 / N}{\sqrt{\|w\|_2^2 / N}} = \frac{\|w\|_1}{\sqrt{N} \|w\|_2}$$
    即：**权重的绝对平均值（Mean Absolute Value）除以其均方根（Root Mean Square）**。
*   **关键特性**：
    *   **尺度不变性**：对权重进行缩放不会改变分数，这使得不同专家之间的比较更加公平。
    *   **层内区分度**：相比于简单的权重模长（Magnitude），AIMER 能够产生更清晰的层内专家分层，减少了排序时的模糊地带。
*   **算法流程**：
    1.  对 MoE 每一层中的每个专家，提取其 Gate、Up 和 Down 投影矩阵。
    2.  计算该专家所有参数的 $L_1$ 范数和 $L_2$ 范数。
    3.  根据 AIMER 公式计算得分。
    4.  在每一层内，保留得分最低（即参数分布更具“独特性”或“稀疏潜力”）的专家，移除得分最高的专家（被认为冗余度高）。

#### 3. 实验设计
*   **模型对象**：涵盖了 7B 到 30B 规模的三个代表性 MoE 家族：
    *   OLMoE-1B-7B-0125-Instruct
    *   ERNIE-4.5-21B-A3B-PT
    *   Qwen3-30B-A3B-Instruct-2507
*   **剪枝配置**：设置了 25% 和 50% 两种专家剪枝率。
*   **Benchmark**：在 16 个基准测试上进行零样本（Zero-shot）评估，涵盖：
    *   **代码生成**：EvalPlus, LiveCodeBench。
    *   **数学推理**：GSM8K, MATH-500。
    *   **创意写作**：WildBench。
    *   **多选题（MCQ）**：MMLU, ARC, BoolQ, HellaSwag 等。
*   **对比方法**：
    *   **基于校准的方法**：REAP, EAN, Frequency, SEER（均使用 4.2M token 的 C4 数据集进行校准）。
    *   **无校准方法**：Random（随机剪枝）, Magnitude（权重模长剪枝）。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在 **两张 NVIDIA L40S 48GB GPU** 上运行。
*   **效率对比**：
    *   **AIMER**：专家评分阶段仅需 **0.22 至 1.27 秒**。
    *   **REAP（对比基准）**：专家评分阶段需要 **0.75 至 2.96 小时**。
*   **内存占用**：AIMER 在评分阶段的峰值内存显著低于基于校准的方法，因为它不需要存储激活值。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个不同架构的模型上进行了广泛测试，总计 16 个任务，涵盖了从判别式到生成式的多种能力。
*   **客观性与公平性**：
    *   所有基于校准的方法使用完全相同的校准集和 token 数量。
    *   评估采用零样本设置，排除了下游微调带来的干扰。
    *   通过雷达图展示了不同方法在各项能力上的权衡，而非仅报告平均分，体现了实验的客观性。

#### 6. 主要结论与发现
*   **性能卓越**：AIMER 在无需任何校准数据的情况下，性能媲美甚至超越了最先进的校准剪枝方法（如 REAP）。
*   **能力保持**：在 ERNIE-21B 模型上，25% 剪枝率下，AIMER 在代码任务上比最强基准提升了 29.7%，数学任务提升了 13.3%。
*   **鲁棒性**：相比于 Magnitude 剪枝，AIMER 在高剪枝率（50%）下表现更稳定，能够更好地保留特征方差（Feature Variance）。
*   **任务无关性**：AIMER 展现了极佳的通用能力平衡，不会像某些校准方法那样在特定任务（如代码）上发生崩溃。

#### 7. 优点
*   **极速预处理**：将专家评分时间从小时级缩短至秒级，极大地提升了部署效率。
*   **零数据依赖**：完全不依赖校准集，消除了因校准数据分布偏差带来的风险。
*   **简单有效**：公式简洁，易于在现有推理框架中实现。
*   **区分度高**：通过归一化处理，解决了传统权重剪枝在层内专家排序时分数过于接近的问题。

#### 8. 不足与局限
*   **任务特定场景受限**：AIMER 是为任务无关（Task-agnostic）设计的，如果目标是针对特定下游任务进行极致压缩，基于校准的方法可能仍具优势。
*   **模型规模覆盖**：实验最高仅覆盖到 30B 参数模型，尚未在千亿级（如 DeepSeek-V3 或 Mixtral-8x22B）超大规模 MoE 上验证其有效性。
*   **理论深度**：虽然实验结果强劲，但对于为什么 $L_1/L_2$ 比例能完美代表专家重要性，缺乏深层的数学理论证明。

（完）
