---
title: "EntropyCache: Decoded Token Entropy Guided KV Caching for Diffusion Language Models"
title_zh: EntropyCache：面向扩散语言模型的解码 Token 熵引导 KV 缓存
authors: "Minsoo Cheong, Donghyun Son, Woosang Lim, Sungjoo Yoo"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18489v1"
tags: ["query:llm"]
score: 9.0
evidence: 基于熵引导的扩散语言模型KV缓存
tldr: 针对扩散语言模型（dLLM）因双向注意力导致无法实现无损KV缓存且推理开销大的问题，本文提出EntropyCache。这是一种无需训练的方法，利用新解码Token分布的最大熵作为衡量缓存陈旧度的低成本指标，通过仅在必要时重新计算最近k个Token的KV状态，在保持准确率的同时实现了显著的推理加速，且决策开销极低。
selection_source: fresh_fetch
motivation: 扩散语言模型的双向注意力机制导致KV缓存难以复用，且现有近似缓存方法的决策开销随上下文长度增加而显著增长。
method: 利用解码Token的最大熵作为缓存漂移的代理信号，并根据特征波动性仅对最近解码的k个Token进行常数成本的重新计算决策。
result: "在LLaDA和Dream模型上实现了15.2倍至26.4倍的推理加速，且决策开销仅占总推理时间的0.5%。"
conclusion: EntropyCache通过极低成本的熵引导策略，有效解决了扩散语言模型推理效率瓶颈，为高效生成提供了新思路。
---

## 摘要
基于扩散的大语言模型 (dLLMs) 依赖于双向注意力机制，这阻碍了无损 KV 缓存的实现，并要求在每个去噪步骤中进行完整的正向传递。现有的近似 KV 缓存方法通过选择性地更新缓存状态来降低这一成本，但其决策开销随上下文长度或模型深度而增加。我们提出了 EntropyCache，这是一种无需训练的 KV 缓存方法，它利用新解码 Token 分布的最大熵作为恒定成本信号，用于决定何时重新计算。我们的设计基于两个

## Abstract
Diffusion-based large language models (dLLMs) rely on bidirectional attention, which prevents lossless KV caching and requires a full forward pass at every denoising step. Existing approximate KV caching methods reduce this cost by selectively updating cached states, but their decision overhead scales with context length or model depth. We propose EntropyCache, a training-free KV caching method that uses the maximum entropy of newly decoded token distributions as a constant-cost signal for deciding when to recompute. Our design is grounded in two empirical observations: (1) decoded token entropy correlates with KV cache drift, providing a cheap proxy for cache staleness, and (2) feature volatility of decoded tokens persists for multiple steps after unmasking, motivating recomputation of the $k$ most recently decoded tokens. The skip-or-recompute decision requires only $O(V)$ computation per step, independent of context length and model scale. Experiments on LLaDA-8B-Instruct and Dream-7B-Instruct show that EntropyCache achieves $15.2\times$-$26.4\times$ speedup on standard benchmarks and $22.4\times$-$24.1\times$ on chain-of-thought benchmarks, with competitive accuracy and decision overhead accounting for only $0.5\%$ of inference time. Code is available at https://github.com/mscheong01/EntropyCache.

---

## 论文详细总结（自动生成）

以下是对论文《EntropyCache: Decoded Token Entropy Guided KV Caching for Diffusion Language Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散语言模型（dLLMs，如 LLaDA、Dream）采用**双向注意力机制**，这意味着每一步去噪（Denoising）时，即使只改变一个 Token，也会影响所有位置的表示。这导致传统的无损 KV 缓存失效，推理时每一步都需要全量前向计算，开销巨大。
*   **研究动机**：
    *   现有的近似 KV 缓存方法（如 d2Cache、Elastic-Cache）虽然能减少计算，但其**决策开销**（判断何时/何处更新缓存）往往随上下文长度或模型深度增加，限制了实际加速比。
    *   作者发现：新解码 Token 的**最大熵（Maximum Entropy）**与 KV 缓存的漂移程度高度相关，可以作为一种极低成本的信号来引导缓存更新。

### 2. 方法论
EntropyCache 是一种**无需训练**的动态 KV 缓存方法，核心思想包括：
*   **基于熵的跳过策略（Entropy-based Skipping）**：
    *   在每一步解码后，计算新去掩码 Token 分布的最大熵 $E_{t+1}$。
    *   如果 $E_{t+1} \le \tau$（阈值），说明模型对当前预测很确定，缓存漂移小，下一轮跳过全量计算，复用旧缓存。
    *   如果 $E_{t+1} > \tau$，则触发全量重新计算（Full Recompute）以刷新缓存。
*   **最近 Token 重新计算（Recent Token Recomputation）**：
    *   观察发现 Token 被解码后的特征波动会持续多个步骤。
    *   在跳过全量计算的步骤中，不仅计算当前的掩码 Token，还额外重新计算**最近解码的 $k$ 个 Token** 的 KV 状态，以捕捉这种持续的波动。
*   **算法复杂度**：决策过程仅涉及 $O(V)$（词表大小）的标量计算，与上下文长度 $L$、模型深度和隐藏层维度完全解耦，实现了常数级的决策开销。

### 3. 实验设计
*   **模型对象**：LLaDA-8B-Instruct 和 Dream-7B-Instruct。
*   **数据集/场景**：
    *   **标准基准**：GSM8K (数学)、MATH500 (数学)、MBPP (代码)、HumanEval (代码)。
    *   **思维链 (CoT) 基准**：MMLU-Pro、BBH、GPQA。
*   **对比方法**：
    *   Baseline（全量计算）及并行解码 Baseline。
    *   **Fast-dLLM**（静态块缓存策略）。
    *   **Elastic-Cache**（基于层间注意力漂移的动态方法）。
    *   **d2Cache**（基于注意力 Rollout 的动态方法）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练时长**：该方法为 **Training-free**（无需训练），直接应用于预训练模型推理阶段。
*   **性能分析**：文中详细对比了决策开销（Decision Overhead），EntropyCache 的决策时间仅占总推理时间的 **0.5%**，而对比方法如 Elastic-Cache 高达 14.5%。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个主流基准测试，并在 LLaDA 和 Dream 两个不同的 dLLM 架构上进行了验证。
*   **消融实验**：
    *   对比了不同的跳过指标（最大熵、熵总和、最大置信度）。
    *   对阈值 $\tau$ 和重新计算数量 $k$ 进行了网格搜索。
    *   分析了滑动窗口大小 $w$ 对准确率和吞吐量的影响。
*   **客观性**：通过 64 个样本的聚合分析验证了熵与缓存漂移的相关性（Spearman 相关系数），实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能飞跃**：在标准任务上实现 **15.2x – 26.4x** 的加速，在长文本 CoT 任务上实现 **22.4x – 24.1x** 的加速。
*   **精度保持**：在大幅提升吞吐量的同时，准确率与 Baseline 相当，甚至在某些任务（如 HumanEval）上优于其他近似缓存方法。
*   **特征规律**：验证了“高熵解码导致大缓存漂移”和“特征波动具有多步持续性”这两个关键经验观察。

### 7. 优点
*   **极低开销**：决策成本几乎可以忽略不计，解决了以往动态缓存方法“决策比计算还慢”的尴尬。
*   **通用性强**：无需微调模型，可直接适配现有的基于掩码扩散的语言模型。
*   **扩展性好**：决策复杂度不随序列增长，非常适合长文本生成场景。

### 8. 不足与局限
*   **任务敏感性**：在代码生成任务（如 MBPP）上，默认参数下准确率有轻微下降（约 3.4%），需要针对特定任务微调阈值 $\tau$。
*   **模型覆盖**：虽然测试了主流的 LLaDA 和 Dream，但尚未在更大规模（如 70B+）或其他非掩码类型的扩散模型上验证。
*   **参数依赖**：性能高度依赖于阈值 $\tau$ 和 $k$ 的选择，不同模型和任务可能需要不同的最优配置。

（完）
