---
title: "Capability-Guided Compression: Toward Interpretability-Aware Budget Allocation for Large Language Models"
title_zh: 能力引导的压缩：面向大语言模型可解释性感知预算分配
authors: Rishaank Gupta
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16440v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过剪枝和预算分配进行能力引导的模型压缩
tldr: 本研究针对大语言模型压缩中存在的“能力盲区”问题，提出了能力引导压缩（CGC）框架。该框架利用稀疏自编码器（SAE）定义的“能力密度”指标，为不同模型组件分配差异化的压缩预算。研究证明，能力密度能有效预测组件的性能突变点，且与现有的重要性指标正交。尽管在GPT-2 Medium上的实验结果受限，但该研究为可解释性驱动的压缩提供了理论基础和新信号。
selection_source: fresh_fetch
motivation: 现有压缩方法忽略了模型组件的功能含义，导致困惑度评估无法反映推理能力损失且难以预测性能突变。
method: 提出CGC框架，通过SAE提取特征广度、熵和一致性来定义能力密度，并据此进行差异化预算分配。
result: 实验证实能力密度是与现有重要性指标正交的新信号，并能预测组件级的性能突变点。
conclusion: 该研究为可解释性驱动的压缩提供了理论框架，指明了未来能力感知压缩研究的方向。
---

## 摘要
大语言模型压缩在剪枝、量化和低秩分解方面取得了实质性进展，但所有现有方法都存在一个根本性局限：压缩预算的分配并未考虑单个模型组件在功能上编码了什么。我们将此称为“能力盲视压缩”问题，并认为它是两个已有记录的失败案例的根本原因——即基于困惑度（perplexity）的评估对推理能力损失的不敏感，以及 Ma 等人（2026）最近描述的模型性能突发相变。我们提出了能力引导压缩（CGC），这是一个通过使用稀疏自编码器（SAE）衍生的能力密度图，在 Transformer 组件之间分配差异化压缩预算的框架。能力密度是一个正式定义的标量度量，结合了组件 SAE 特征激活分布的特征广度、激活熵和跨输入一致性。我们从理论上证明，具有较高能力密度的组件表现出较低的结构冗余，并在较低的压缩率下达到各自的相变点，从而为组件级相变预测提供了第一个预压缩机制。在 GPT-2 Medium 上的实验证实，能力密度在统计上独立于 Wanda 重要性评分（Spearman rho = -0.054, n = 384 个注意力头），这确立了它是一种与所有现有重要性指标正交的全新压缩信号。我们报告了基于 PPL 的压缩比较的负面结果，并提供了一个原则性的诊断，指出 GPT-2 Medium 不足以作为完整 CGC 假设的测试平台。该理论框架、密度形式化和正交性发现为能力感知压缩研究奠定了基础。

## Abstract
Large language model compression has made substantial progress through pruning, quantization, and low-rank decomposition, yet a fundamental limitation persists across all existing methods: compression budgets are allocated without any representation of what individual model components functionally encode. We term this the capability-blind compression problem and argue it is a root cause of two well-documented failures -- the insensitivity of perplexity-based evaluation to reasoning capability loss, and the abrupt phase transitions in model performance recently characterized by Ma et al. (2026). We propose Capability-Guided Compression (CGC), a framework that addresses this by using Sparse Autoencoder (SAE)-derived capability density maps to allocate differential compression budgets across transformer components. Capability density is a formally defined scalar measure combining the feature breadth, activation entropy, and cross-input consistency of a component's SAE feature activation distribution. We prove theoretically that components with higher capability density exhibit lower structural redundancy and reach their individual phase transition points at lower compression ratios, providing the first pre-compression mechanism for component-level phase transition prediction. Experiments on GPT-2 Medium confirm that capability density is statistically independent of Wanda importance scores (Spearman rho = -0.054, n = 384 heads), establishing it as a genuinely novel compression signal orthogonal to all existing importance metrics. We report a negative result on PPL-based compression comparison and provide a principled diagnosis identifying GPT-2 Medium as an insufficient test bed for the full CGC hypothesis. The theoretical framework, density formalism, and orthogonality finding constitute a foundation for capability-aware compression research.

---

## 论文详细总结（自动生成）

### 论文总结：能力引导的压缩 (Capability-Guided Compression, CGC)

#### 1. 核心问题与研究动机
*   **核心问题**：现有的模型压缩技术（如剪枝、量化）是“能力盲视”（Capability-blind）的。它们依赖权重大小或激活方差等统计指标分配压缩预算，而不考虑组件具体的逻辑功能（如推理、事实检索或语法解析）。
*   **研究动机**：
    *   **评估局限性**：基于困惑度（PPL）的评估无法捕捉推理能力的丧失。
    *   **性能突变**：模型在达到临界压缩阈值时会发生非线性的性能崩溃（相变），而现有方法无法预测哪些组件是导致崩溃的关键。
    *   **目标**：利用机械可解释性工具（稀疏自编码器 SAE）来识别组件的功能重要性，从而实现更科学的预算分配。

#### 2. 方法论
*   **核心思想**：通过 SAE 提取模型组件的“能力密度”（Capability Density），并据此为高密度（功能丰富、冗余低）的组件分配更多的参数保留预算。
*   **关键技术细节**：
    *   **能力密度 ($\delta^{(c)}$) 定义**：由三个子指标的几何平均值组成：
        1.  **特征广度 ($\beta$)**：SAE 字典中活跃特征的数量。
        2.  **特征多样性 ($H$)**：特征激活分布的香农熵。
        3.  **跨输入一致性 ($\Psi$)**：不同语义类别输入下活跃特征集的 Jaccard 相似度。
    *   **理论证明**：论文证明了高能力密度的组件结构冗余更低，会更早达到性能相变点。
    *   **CGC 算法流程**：
        1.  **CGC-L (初始化)**：根据能力密度图生成初步的差异化预算分配。
        2.  **CGC-F (优化)**：在进化搜索算法（如 EvoPress）中加入“密度诱导预算上限”约束，防止高密度组件被过度压缩。
        3.  **集成**：作为一层分配逻辑，叠加在现有的相变规避框架（如 Ma et al. 2026）之上。

#### 3. 实验设计
*   **实验模型**：GPT-2 Medium (355M 参数)。
*   **数据集**：使用 WikiText-103 进行 SAE 训练、激活收集和困惑度（PPL）评估。
*   **Benchmark 与对比方法**：
    *   **Uniform**：均匀剪枝（基准）。
    *   **Wanda**：基于权重和激活范数的流行剪枝指标。
    *   **Inverted**：反向分配（故意压缩高密度组件，作为负对照）。
    *   **CGC-L**：本文提出的密度比例分配方法。

#### 4. 资源与算力
*   **硬件**：实验在单台消费级显卡 **NVIDIA T4 GPU** 上完成。
*   **训练时长**：为 384 个注意力头各训练了一个独立 SAE，每个 SAE 在 65,536 个 token 上训练了 5 个 epoch。
*   **算力评价**：计算成本相对较低，旨在证明方法的可复现性。

#### 5. 实验数量与充分性
*   **实验规模**：对 GPT-2 Medium 的全部 384 个注意力头进行了详尽的密度映射和消融分析。
*   **正交性实验**：计算了能力密度与 Wanda 指标的 Spearman 相关性（$n=384$）。
*   **充分性评价**：
    *   **客观性**：作者非常坦诚地报告了在 GPT-2 上的 **负面结果**（PPL 表现未超过均匀剪枝）。
    *   **局限性**：实验仅限于一个小模型和一个指标（PPL），缺乏在大模型（如 LLaMA）和推理任务上的验证。

#### 6. 主要结论与发现
*   **信号正交性**：能力密度与 Wanda 重要性评分的相关性极低（Spearman $\rho = -0.054$），证明它是一个**全新的、独立于现有统计指标的压缩信号**。
*   **相变预测**：理论上建立了能力密度与组件级相变点的联系，为预压缩阶段的风险评估提供了工具。
*   **负面结果诊断**：在 GPT-2 Medium 上，CGC 的 PPL 表现略逊于均匀剪枝。作者分析认为这是因为 GPT-2 组件功能过于均匀、PPL 对能力损失不敏感以及 SAE 训练不够深入导致的。

#### 7. 优点
*   **理论创新**：首次将机械可解释性（SAE）与模型压缩预算分配直接挂钩，提出了“能力密度”这一形式化概念。
*   **前瞻性**：为解决模型压缩中的“性能突变”问题提供了理论解释和预防机制。
*   **学术诚实**：详细分析并报告了负面结果，并为后续研究指明了修正方向（如在 LLaMA-7B 上验证）。

#### 8. 不足与局限
*   **模型规模限制**：GPT-2 Medium 缺乏复杂的推理电路，导致能力密度信号的差异化优势无法体现。
*   **评估指标单一**：仅依赖 PPL 无法体现 CGC 在保护“推理能力”方面的潜在优势。
*   **SAE 质量**：由于算力限制，SAE 训练较浅，可能未能提取出足够精细的单语义特征。
*   **应用范围**：目前主要讨论了结构化剪枝，尚未深入探讨量化和 KV 缓存压缩中的应用。

（完）
