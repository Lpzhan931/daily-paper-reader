---
title: "SJD-PAC: Accelerating Speculative Jacobi Decoding via Proactive Drafting and Adaptive Continuation"
title_zh: SJD-PAC：通过主动草拟与自适应延续加速投机雅可比解码
authors: "Jialiang Kang, Han Shu, Wenshuo Li, Yingjie Zhai, Xinghao Chen"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18599v1"
tags: ["query:llm"]
score: 10.0
evidence: 通过主动草拟加速投机解码
tldr: 针对文本生成图像中投机雅可比解码（SJD）在复杂区域接受率低的问题，本文提出SJD-PAC框架。该框架通过主动草拟策略提升高熵区域的局部接受率，并引入自适应持续机制在拒绝后继续验证序列，避免频繁重采样。实验表明，该方法在保持图像质量无损的前提下，实现了3.8倍的推理加速，显著提升了自回归视觉生成的效率。
selection_source: fresh_fetch
motivation: 视觉生成的高熵特性导致SJD在复杂区域的草拟令牌接受率较低，成为限制推理吞吐量的瓶颈。
method: 该方法结合了提升高熵区域局部接受率的主动草拟策略，以及在拒绝后仍能持续验证序列的自适应持续机制。
result: 实验证明SJD-PAC在标准基准测试中实现了3.8倍的加速，且完全保持了目标分布和图像质量。
conclusion: SJD-PAC通过优化草拟与验证环节，有效克服了SJD的效率瓶颈，为无草拟模型的自回归生成加速提供了新思路。
---

## 摘要
投机雅可比解码 (SJD) 提供了一种无需草图模型的方法来加速自回归文本到图像合成。然而，视觉生成的高熵特性导致复杂区域的草图标记接受率较低，形成了严重限制整体吞吐量的瓶颈。为了克服这一问题，我们引入了 SJD-PAC，这是一个增强的 SJD 框架。首先，SJD-PAC 采用主动草拟策略来提高这些挑战性高熵区域的局部接受率。其次，我们引入了一种自适应延续机制，在初始拒绝后仍能维持序列验证，从而避免了完全重采样的需要。通过协同工作，这些优化显著增加了每步的平均接受长度，在严格保持目标分布的同时提升了推理速度。在标准文本到图像基准测试上的实验表明，SJD-PAC 在保证无损图像质量的前提下实现了 3.8 倍的加速。

## Abstract
Speculative Jacobi Decoding (SJD) offers a draft-model-free approach to accelerate autoregressive text-to-image synthesis. However, the high-entropy nature of visual generation yields low draft-token acceptance rates in complex regions, creating a bottleneck that severely limits overall throughput. To overcome this, we introduce SJD-PAC, an enhanced SJD framework. First, SJD-PAC employs a proactive drafting strategy to improve local acceptance rates in these challenging high-entropy regions. Second, we introduce an adaptive continuation mechanism that sustains sequence validation after an initial rejection, bypassing the need for full resampling. Working in tandem, these optimizations significantly increase the average acceptance length per step, boosting inference speed while strictly preserving the target distribution. Experiments on standard text-to-image benchmarks demonstrate that SJD-PAC achieves a $3.8\times$ speedup with lossless image quality.

---

## 论文详细总结（自动生成）

以下是对论文《SJD-PAC: Accelerating Speculative Jacobi Decoding via Proactive Drafting and Adaptive Continuation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：自回归（AR）文本到图像（T2I）生成模型虽然质量高，但推理速度极慢（需逐个预测数千个标记）。
*   **研究背景**：
    *   **投机解码（SD）**通常需要额外的草拟模型，且在图像生成这种“高熵”任务中接受率极低。
    *   **投机雅可比解码（SJD）**是一种无需训练、无损的加速方案，但其加速比通常仅为 2 倍左右。
*   **研究动机**：作者分析发现 SJD 的瓶颈在于“单标记接受”现象严重（近 50% 的步骤只接受一个标记）。一旦某个位置拒绝，后续草拟序列会因上下文不匹配而失效，导致加速效率低下。

### 2. 方法论
SJD-PAC 提出了两个协同机制来增强 SJD，且保持了数学上的**严格无损性**：
*   **主动草拟（Proactive Drafting, PD）**：
    *   **核心思想**：针对拒绝点后的上下文失效问题，不再生成单一的草拟链，而是主动构建一个局部的、多样化的候选路径。
    *   **技术细节**：在拒绝位置 $i$ 处，构建一个深度为 $D$、分支因子为 $K$ 的浅层宽树（K-ary Tree），随后在树的末端延伸出长链。这增加了下一轮迭代中至少有一条路径被接受的概率，缓解了级联拒绝。
*   **自适应延续（Adaptive Continuation, AC）**：
    *   **核心思想**：打破标准 SJD 在遇到第一个拒绝标记时立即停止验证的限制。
    *   **技术细节**：即使位置 $i$ 的标记被拒绝并重采样，AC 仍会继续验证后续位置 $j > i$ 的标记。
    *   **理论依据**：实验发现图像标记具有强烈的“局部性”，即远处上下文的改变对当前位置分布的影响（全变分距离 dTV）会迅速衰减。因此，保留后续有效的草拟标记是可行的，这显著增加了单次前向传递的平均接受长度。

### 3. 实验设计
*   **模型与基准**：
    *   **模型**：Lumina-mGPT (7B)、Emu3 (8B)。
    *   **数据集**：MS-COCO 2017 验证集、PartiPrompts。
*   **对比方法**：
    *   **无损方法**：EAGLE-2、原始 SJD。
    *   **有损方法**：LANTERN++、GSD、SJD2。
*   **评估指标**：FID（图像质量）、CLIP-Score（图文对齐）、步数压缩比（Step Compression）、端到端实际加速比（Wall-clock Speedup）。

### 4. 资源与算力
*   **训练成本**：该方法是**无需训练（Training-free）**的，因此没有训练算力消耗。
*   **推理算力**：
    *   论文提到了在 Lumina-mGPT 7B 上的显存占用约为 **18.7 GB**（与原始模型持平）。
    *   单步推理耗时约 44.3ms（相比原始 AR 的 40.8ms 略有增加，但通过减少总步数实现加速）。
    *   **未明确说明**：文中未具体列出测试所用的 GPU 型号（如 A100 或 H100）及具体数量。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流大模型和两个标准数据集上进行了测试，涵盖了质量与速度的双重评估。
*   **消融实验**：非常充分。作者分别验证了 PD、AC 以及窗口长度 $L$ 对性能的贡献，并对 PD 的超参数 $K$（分支数）和 $D$（深度）进行了敏感性分析。
*   **公平性**：对比了多种最新的有损和无损加速方案，并采用了官方参数或报告的最优值，实验设计较为客观。

### 6. 主要结论与发现
*   **加速效果**：SJD-PAC 在 Lumina-mGPT 上实现了 **3.80倍** 的实际加速，在 Emu3 上实现了 **3.25倍** 加速。
*   **质量保持**：FID 和 CLIP-Score 与原始模型几乎一致，证明了其**严格无损**的特性。
*   **超越有损方法**：在某些情况下，SJD-PAC 的加速比甚至超过了需要牺牲图像质量的有损方法（如 GSD）。
*   **分布改变**：PD 和 AC 成功将接受长度的分布从“短尾”推向了“长尾”，显著提升了每一步的效率。

### 7. 优点
*   **无损且无需训练**：不需要额外的草拟模型训练，也不改变原模型的输出分布，易于部署。
*   **解决核心痛点**：精准识别并解决了 SJD 在高熵视觉生成任务中接受率低的瓶颈。
*   **高效的验证逻辑**：AC 机制充分利用了图像生成的空间局部性特征，具有很强的启发性。

### 8. 不足与局限
*   **超参数敏感性**：加速效果依赖于窗口长度 $L$、树深度 $D$ 和分支因子 $K$ 的精细平衡，不同模型可能需要重新调优。
*   **计算开销抵消**：虽然步数压缩比很高（如 4.51x），但由于单步验证逻辑变复杂（KV Cache 更新、树构建），实际端到端加速比（3.80x）会有所折扣。
*   **硬件依赖**：加速效果受限于 GPU 的并行处理能力，在计算受限（Compute-bound）的场景下，增加窗口长度带来的收益会递减。

（完）
