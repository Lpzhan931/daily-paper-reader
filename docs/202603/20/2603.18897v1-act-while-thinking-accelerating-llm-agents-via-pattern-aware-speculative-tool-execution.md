---
title: "Act While Thinking: Accelerating LLM Agents via Pattern-Aware Speculative Tool Execution"
title_zh: 边思边行：通过模式感知的投机性工具执行加速大语言模型智能体
authors: "Yifan Sui, Han Zhao, Rui Ma, Zhiyuan He, Hao Wang, Jianxun Li, Yuqing Yang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18897v1"
tags: ["query:llm"]
score: 9.0
evidence: 模式感知的投机工具执行方法
tldr: LLM智能体在执行任务时通常采用串行的“模型推理-工具调用”循环，导致严重的延迟瓶颈。本文提出PASTE，一种模式感知的投机工具执行方法。PASTE利用智能体请求中稳定的控制流和可预测的数据依赖性，通过提前投机执行工具来掩盖延迟。实验表明，该方法显著降低了任务完成时间并提升了吞吐量，是加速智能体服务的有效方案。
selection_source: fresh_fetch
motivation: 现有的LLM智能体采用严格的串行执行模式，导致LLM必须在每一步等待外部工具执行，产生了巨大的延迟瓶颈。
method: 提出PASTE方法，通过识别稳定的应用级控制流和工具间的数据依赖关系，实现工具的投机执行以掩盖延迟。
result: "实验结果显示，PASTE将平均任务完成时间缩短了48.5%，并将工具执行吞吐量提高了1.8倍。"
conclusion: PASTE证明了利用智能体行为模式进行投机执行是加速LLM智能体任务处理的有效途径。
---

## 摘要
以大语言模型（LLM）驱动的智能体正成为自主任务解决的主流范式。与标准的推理负载不同，智能体运行在严格串行的“LLM-工具”循环中，LLM 在每一步都必须等待外部工具的执行。这种执行模型引入了严重的延迟瓶颈。为解决这一问题，我们提出了 PASTE，一种模式感知的投机性工具执行方法，旨在通过投机执行来隐藏工具延迟。PASTE 基于以下洞察：尽管智能体请求在语义上具有多样性，但它们表现出稳定的应用级控制流（重复出现的工具调用序列）和可预测的数据依赖关系（工具间的参数传递）。通过利用这些特性，PASTE 通过投机性工具执行提升了智能体服务的性能。与最先进的基准方法相比，实验结果表明 PASTE 将平均任务完成时间缩短了 48.5%，并将工具执行吞吐量提高了 1.8 倍。

## Abstract
LLM-powered agents are emerging as a dominant paradigm for autonomous task solving. Unlike standard inference workloads, agents operate in a strictly serial "LLM-tool" loop, where the LLM must wait for external tool execution at every step. This execution model introduces severe latency bottlenecks. To address this problem, we propose PASTE, a Pattern-Aware Speculative Tool Execution method designed to hide tool latency through speculation. PASTE is based on the insight that although agent requests are semantically diverse, they exhibit stable application level control flows (recurring tool-call sequences) and predictable data dependencies (parameter passing between tools). By exploiting these properties, PASTE improves agent serving performance through speculative tool execution. Experimental results against state of the art baselines show that PASTE reduces average task completion time by 48.5% and improves tool execution throughput by 1.8x.

---

## 论文详细总结（自动生成）

### 论文总结：Act While Thinking (PASTE)

#### 1. 核心问题与整体含义（研究动机和背景）
当前大语言模型（LLM）智能体（Agent）的运行模式遵循严格串行的“推理-工具调用”循环（LLM-Tool Loop）。LLM 生成工具调用指令后，系统必须停下来等待外部工具（如搜索、代码运行、API调用）执行完毕并返回结果，LLM 才能进行下一步推理。
*   **痛点**：工具执行时间占总请求时间的 35% 到 61%，导致严重的端到端延迟和昂贵的计算资源闲置。
*   **核心目标**：打破串行依赖，通过“边思边行”（在 LLM 思考时投机性地预执行工具）来加速智能体任务处理。

#### 2. 方法论：核心思想与关键技术
论文提出了 **PASTE**（Pattern-Aware Speculative Tool Execution），其核心思想是利用智能体行为的可预测性进行投机执行。

*   **模式元组（Pattern Tuple）**：将智能体行为抽象为 `(context, prediction, function, probability)`。
    *   **Context**：基于工具调用序列的上下文（脱离具体语义，关注调用链结构）。
    *   **Prediction**：预测下一个可能调用的工具。
    *   **Function**：符号化值映射函数，用于预测工具参数（例如：从搜索结果的 JSON 中提取第一个 URL 作为下载工具的参数）。
    *   **Probability**：基于历史数据的置信度。
*   **两阶段模式挖掘**：
    1.  **结构挖掘**：识别频繁出现的工具调用序列（如 `git clone` 后通常接 `git checkout`）。
    2.  **符号依赖推断**：推断参数如何在工具间传递，实现参数的“延迟绑定”。
*   **机会主义调度器（Opportunistic Scheduler）**：
    *   **优先级划分**：将任务分为“权威调用”（Agent 真实发出的）和“投机调用”（系统预测的）。
    *   **资源隔离**：投机任务仅利用系统空闲资源（Slack Resources），一旦发生资源竞争，立即抢占或终止投机任务。
    *   **晋升机制（Promotion）**：如果 Agent 随后发出的真实请求与正在运行的投机任务匹配，则将其晋升为权威任务，直接复用结果。

#### 3. 实验设计
*   **场景与数据集**：
    *   **DeepResearchBench**：深度研究任务。
    *   **SWE-bench**：软件工程/代码修复任务。
    *   **ScholarQA**：科学研究问答。
*   **智能体平台**：Virtual-Lab、Qwen-DeepResearch、Gemini-cli。
*   **对比基准（Baselines）**：
    *   **ORION**：针对 Serverless DAG 优化的系统。
    *   **SpecFaaS**：通用的投机性 Serverless 执行框架。

#### 4. 资源与算力
*   **硬件配置**：使用了 4 个节点，每个节点配备 96 个 vCPU（AMD EPYC 7V13）和 512 GB 内存。
*   **GPU 资源**：每个节点 8 张 NVIDIA A100-80G GPU，**总计 32 张 A100 GPU**。
*   **模型**：包括 Gemini-2.5-Pro、GPT-5.2（API 形式）以及本地部署的 Qwen-DeepResearch-30B。
*   **训练/挖掘时长**：文中未明确说明模式挖掘的具体耗时，但指出预测和调度开销极低（小于 100ms）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大主流智能体应用领域（研究、编程、科学），进行了超过 20,000 次投机操作审计。
*   **维度覆盖**：包括端到端延迟、工具执行延迟、吞吐量、扩展性（并发 session）、预测准确率、副作用安全性以及资源开销分析。
*   **客观性**：使用了真实的生产环境轨迹（Azure Functions trace）来模拟请求到达，并保持了硬件和 LLM 配置的严格一致，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **性能提升**：PASTE 将平均端到端延迟降低了 **48.5%**，工具执行吞吐量提升了 **1.8 倍**。
*   **延迟隐藏**：通过将工具执行与 LLM 推理重叠，减少了约 67% 的工具等待时间。
*   **预测质量**：Top-1 准确率约为 27.8%，但 Top-3 召回率达 43.9%，综合命中率高达 **93.8%**（证明了多路径投机的有效性）。
*   **安全性**：成功拦截了所有具有潜在副作用的投机调用，未对最终任务结果产生任何负面影响。

#### 7. 优点与亮点
*   **解耦控制流与数据流**：通过符号化函数解决工具参数预测难题，比单纯预测工具类型更具实用价值。
*   **资源感知调度**：不盲目投机，而是利用“计算间隙”进行机会主义执行，保证了不干扰正常推理任务。
*   **晋升机制**：巧妙地将投机结果转化为实际性能增益，避免了重复计算。

#### 8. 不足与局限
*   **冷启动问题**：对于全新的、从未见过的智能体工作流，系统需要积累历史轨迹才能挖掘出有效模式。
*   **副作用处理依赖策略**：虽然有拦截机制，但对于某些复杂且不可逆的外部副作用（如发送邮件、金融交易），仍需用户手动配置严格的白名单/黑名单策略，自动化程度受限。
*   **资源浪费风险**：在资源极度紧张的场景下，错误的投机仍会带来微小的 CPU/内存开销，尽管文中证明了这种开销在可接受范围内。

（完）
