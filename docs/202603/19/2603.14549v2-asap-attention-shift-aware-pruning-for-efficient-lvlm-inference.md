---
title: "ASAP: Attention-Shift-Aware Pruning for Efficient LVLM Inference"
title_zh: ASAP：用于高效 LVLM 推理的感知注意力偏移剪枝
authors: "Surendra Pathak, Bo Han"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14549v2"
tags: ["query:llm"]
score: 9.0
evidence: 针对视觉语言模型的注意力偏移感知剪枝
tldr: "针对大型视觉语言模型（LVLM）处理高分辨率图像时计算开销巨大的问题，本文提出ASAP，一种无需训练且兼容KV缓存的剪枝方案。ASAP通过动态双向软注意力掩码缓解“注意力偏移”现象，并利用加权软合并减少语义冗余。该方法在保持LLaVA-NeXT-7B约99%性能的同时，大幅减少了约80%的计算量，实现了高效推理。"
selection_source: fresh_fetch
motivation: 现有的视觉Token减少策略忽略了LVLM中导致Token评分偏差的“注意力偏移”现象以及Token间的语义冗余。
method: 提出ASAP方案，结合动态双向软注意力掩码以精准筛选Token，并采用加权软合并技术融合语义相似的视觉块。
result: "在LLaVA-NeXT-7B上实现了近乎无损的压缩，保留了99.02%的性能并减少了约80%的计算FLOPs。"
conclusion: ASAP为LVLM提供了一种高效、无需训练且兼容现有缓存机制的推理加速新途径。
---

## 摘要
尽管大型视觉语言模型（LVLMs）展现出卓越的多模态能力，但处理高分辨率视觉 token 的二次方计算成本仍然是一个关键瓶颈。虽然近期的 token 减少策略试图加速推理，但这些方法未能充分利用注意力值，且未能解决 token 冗余问题。更关键的是，它们忽视了 LVLMs 中固有的“注意力偏移”现象，该现象会导致 token 注意力分数产生偏差。在这项工作中，我们提出了 ASAP，这是一种新型的、无需训练且兼容 KV 缓存的剪枝方案，全面解决了这些局限性。首先，我们通过利用动态双向软注意力掩码来缓解注意力偏移，确保选择真正具有信息量的 token，而非基于朴素注意力的选择。其次，我们认为 token 集合中高度的语义冗余会降低性能。因此，我们引入了一个加权软合并组件，用于合并语义相似的 token，仅为后续层保留特征最密集的视觉补丁。ASAP 实现了几乎无损的视觉上下文压缩，在保留 LLaVA-NeXT-7B 原始性能 99.02% 的同时，大幅削减了约 80% 的计算 FLOPs。

## Abstract
While Large Vision-Language Models (LVLMs) demonstrate exceptional multi-modal capabilities, the quadratic computational cost of processing high-resolution visual tokens remains a critical bottleneck. Though recent token reduction strategies attempt to accelerate inference, such methods inadequately exploit attention values and fail to address token redundancy. More critically, they overlook the ``attention shift'' phenomenon inherent in LVLMs, which skews token attention scores. In this work, we propose ASAP, a novel training-free, KV-Cache-compatible pruning recipe that comprehensively addresses these limitations. First, we mitigate the attention shift by utilizing a dynamic bidirectional soft attention mask, ensuring the selection of genuinely informative tokens rather than naive attention-based selection. Second, we posit that high semantic redundancy within the token set degrades performance. We therefore introduce a weighted soft merging component that merges semantically similar tokens, preserving only the most feature-dense visual patches for subsequent layers. ASAP achieves virtually lossless compression of visual context, retaining 99.02% of the original LLaVA-NeXT-7B performance while aggressively slashing computational FLOPs by ~80%.

---

## 论文详细总结（自动生成）

这是一份关于论文《ASAP: Attention-Shift-Aware Pruning for Efficient LVLM Inference》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型视觉语言模型（LVLM）在处理高分辨率图像时会产生数千个视觉 Token，由于 Transformer 的注意力机制具有二次方复杂度，这导致了巨大的计算开销和推理延迟。
*   **研究背景**：
    *   **注意力偏移（Attention Shift）**：由于旋转位置嵌入（RoPE）的距离衰减特性，模型倾向于给序列后端的 Token 分配更高权重，导致前端的视觉 Token 被错误地忽视。
    *   **语义冗余**：图像中存在大量重复或低信息量的区域（如天空、背景），现有的剪枝方法未能有效处理这种冗余。
    *   **现有缺陷**：现有的剪枝方法要么是文本无关的（在视觉编码器端剪枝），要么忽视了注意力偏移，导致关键视觉信息丢失。

### 2. 方法论
ASAP 是一种**无需训练（Training-free）**且**兼容 KV 缓存**的剪枝框架，包含三个核心组件：
*   **显著性引导的双向注意力掩码（SG-BiMask）**：
    *   **核心思想**：打破传统的因果掩码（Causal Mask）限制，允许早期的视觉 Token “窥视”后方的 Token。
    *   **技术细节**：利用预 Softmax 的对齐矩阵计算每个 Token 的原始显著性，通过 Min-Max 归一化投影到 [0, 1] 区间，生成动态的软双向掩码。这使得重要的视觉锚点能够跨越位置偏差进行信息传播，修正了 RoPE 带来的空间偏见。
*   **显著性加权合并（Weighted Soft Merging）**：
    *   **核心思想**：减少语义冗余。
    *   **技术细节**：基于隐藏状态的余弦相似度识别冗余 Token。不同于简单的平均，ASAP 采用基于显著性分数的凸组合进行加权融合，确保融合后的特征向结构更关键的锚点对齐，防止特征稀释。
*   **预算重新分配与回收（Salvage Mechanism）**：
    *   **核心思想**：最大化信息保留。
    *   **技术细节**：当冗余 Token 被合并后，会腾出部分预算名额。ASAP 会从之前被剪枝的候选池中回收显著性分数最高的 Token 来填补这些空位，确保在固定 Token 预算下保留最丰富的信息。

### 3. 实验设计
*   **模型基准**：LLaVA-1.5 (13B)、LLaVA-NeXT (7B)、InternVL 2.5 (8B)。
*   **数据集/任务**：涵盖了 8 个主流基准测试，包括 VQAv2、GQA（组合推理）、ScienceQA（科学推理）、TextVQA（OCR）、MME（综合感知）、MMBench、MMVet 等。
*   **对比方法**：FastV、LLaVA-PruMerge+、SparseVLM、DivPrune、VisPruner 等最先进的即插即用剪枝技术。
*   **评估指标**：准确率、理论 FLOPs 比例、KV 缓存存储量、GPU 显存占用及 CUDA 推理延迟。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 或 H100）及数量。
*   **推理效率**：由于该方法是**无需训练**的，不涉及训练时长。实验重点展示了推理阶段的开销：
    *   在 LLaVA-1.5-7B 上，将 Token 减少到 128 个时，计算量从 3.817 TFLOPs 降至 0.836 TFLOPs。
    *   推理延迟方面，ASAP 相比 FastV 仅增加了约 **2 毫秒** 的微小开销，但显著提升了准确率并减少了 15% 的存储空间。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个不同架构的 LVLM 上进行了验证，证明了通用性。
    *   针对不同剪枝预算（如保留 128、192、640、960 Token 等）进行了多梯度对比。
    *   **消融实验**：系统地分解了双向掩码、加权合并和回收机制的贡献，证明了各模块的有效性。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了准确率，还深入分析了 FLOPs 比例和实际系统资源占用，并提供了定性的可视化结果（如热力图对比）来解释为何 ASAP 能捕捉到被其他方法忽略的微小物体。

### 6. 主要结论与发现
*   **近乎无损的压缩**：在 LLaVA-NeXT-7B 上，ASAP 在减少约 **80% 计算量** 的情况下，保留了 **99.02%** 的原始性能。
*   **超越基准**：在某些任务（如 MMBench）上，剪枝后的模型性能甚至超过了原始全量 Token 模型，这表明剪枝可能起到了去除视觉噪声的作用。
*   **解决偏移**：成功证明了通过双向注意力机制可以有效抵消 RoPE 带来的位置偏见，使模型能关注到图像背景中的远端物体。

### 7. 优点
*   **即插即用**：无需任何微调或额外训练，部署成本极低。
*   **架构感知**：深入分析了 RoPE 对多模态模型的影响，从底层机制上解决了注意力偏移问题。
*   **高效平衡**：在大幅降低 KV 缓存和计算压力的同时，保持了极高的推理精度。
*   **兼容性强**：完全兼容现有的 KV Cache 机制和多轮对话场景。

### 8. 不足与局限
*   **推理微增**：虽然 FLOPs 大幅下降，但动态掩码计算和 Token 合并逻辑引入了极小的额外延迟（约 2ms），在极高性能要求的实时场景中需权衡。
*   **层级选择**：实验中将剪枝位置固定在 LLM 的第 2 层，虽然这是基于前人经验，但未深入探讨在不同层进行动态剪枝的潜在收益。
*   **极端压缩风险**：尽管在 80% 压缩率下表现优异，但在更极端的压缩（如保留极少量 Token）下，对于需要极高空间分辨率的任务（如复杂 OCR）可能仍存在信息丢失风险。

（完）
