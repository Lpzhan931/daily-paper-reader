Title: On the Computational Hardness of Transformers

URL Source: https://arxiv.org/pdf/2603.11332v1

Published Time: Fri, 13 Mar 2026 00:14:43 GMT

Number of Pages: 46

Markdown Content:
# On the Computational Hardness of Transformers 

Barna Saha ∗

University of California San Diego 

bsaha@ucsd.edu 

Yinzhan Xu ∗

University of California San Diego 

xyzhan@ucsd.edu 

Christopher Ye ∗

University of California San Diego 

czye@ucsd.edu 

Hantao Yu †

Columbia University 

hantao.yu@columbia.edu 

Abstract 

The transformer architecture has revolutionized modern AI across language, vision, and beyond. It consists of L layers of multi-head attention, where each layer runs H attention heads in parallel and feeds the combined output to the subsequent layer. In attention, each token within an input of length N is represented by an embedding vector of dimension m.Computationally, an attention mechanism primarily involves multiplying three N × m matrices, while applying a softmax operation to the intermediate product of the first two matrices. Asignificant body of work has been devoted to analyzing the time complexity of attention, leading to several recent advances. On the other hand, known algorithms for transformers compute each attention head indepen-dently. This raises a fundamental question that has recurred throughout theoretical computer science under the guise of “direct sum” problems: can multiple instances of the same problem be solved more efficiently than solving each instance separately? Many answers to this ques-tion, both positive and negative, have arisen in fields spanning communication complexity and algorithm design. Thus, a key challenge in understanding the computational hardness of trans-formers is to determine whether their computation can be performed more efficiently than LH 

independent evaluations of attention. In this paper, we resolve this question in the negative, and give the first non-trivial compu-tational lower bounds for multi-head multi-layer transformers. In the small embedding regime (m = N o(1) ), computing LH attention heads separately takes LHN 2+ o(1) time. We estab-lish that this is essentially optimal under the Strong Exponential Time Hypothesis (SETH). In the large embedding regime ( m = N ), one can compute LH attention heads separately using 

LHN ω+o(1) arithmetic operations (plus exponents), where ω is the matrix multiplication ex-ponent. We establish that this is optimal, by showing that LHN ω−o(1) arithmetic operations are necessary when ω > 2. Our lower bound in the large embedding regime relies on a novel application of the Baur-Strassen theorem, a powerful algorithmic tool underpinning the famous backpropagation algorithm. 

> ∗

Supported by NSF HDR TRIPODS Phase II grant 2217058 (EnCORE Institute). 

> †

Supported by NSF grant CCF2008733, ONR grant N00014-22-1-2713, NSF Grant CCF-2238221 and a Columbia Center of Artificial Intelligence Technology grant. 

1

> arXiv:2603.11332v1 [cs.CC] 11 Mar 2026

# 1 Introduction 

The Transformer architecture [VSP +17] is used for a wide range of tasks such as natural language processing [RWC +19] and computer vision [DBK +21, KMR +23], and has achieved state-of-the-art performance for numerous applications. The crucial building block of transformers is the attention head, which captures pairwise relationships between input tokens and thus is desirable for processing language inputs. Concretely, given input X ∈ RN ×m, the attention head consists of query, key and value embedding maps Q, K, V : RN ×m → RN ×m, where m is known as the embedding dimension, and computes 

Att Q,K,V (X) = softmax( Q(X)K(X)⊤)V (X) ∈ RN ×m,

where the softmax operator is defined as 

softmax( v) = 1

PNi=1 exp( vi) · (exp( v1), . . . , exp( vN )) 

and is applied row-wise on matrices. A transformer consists of L attention layers, where each attention layer consists of H parallel attention heads that takes the output X of the previous layer as input, sends it to all attention heads, and aggregate 1 the outputs of all attention heads and X

before finally applying a row-wise multi-layer perceptron (MLP) function. While the transformer is undoubtedly powerful, it is also computationally expensive, with at-tention complexity scaling quadratically with respect to input length N . The trivial algorithm for attention requires O(N 2m) time, 2 and the trivial algorithm for transformer requires O(LHN 2m)

time, even before accounting for the time required to compute embedding and MLP maps. 3 It has been proved that N 2−o(1) time is necessary for computing attention when m = Ω(log N ) as-suming the Strong Exponential Time Hypothesis ( SETH ) [KWH23, AS24], while the trivial algo-rithm of computing matrix products explicitly is optimal for all m under a generalization of the 

OV Hypothesis [GHS +26]. Due to the lack of fast attention algorithms, numerous works have tried to address this bottleneck by proposing subquadratic alternatives to attention mechanism [KKL20, CLD +21, ZGD +20b, LYS +25]. However, all these subquadratic alternatives suffer from accuracy drops as the model scales up, and it has been proved that no subquadratic alternative can capture all pairwise relationship up to some approximation [AY25]. Consequently, our work is primarily concerned with the following question. 

Question 1: How efficiently can a standard transformer model be computed? 

While [KWH23, AS24, GHS +26] address this problem for a single attention head, we are in-stead concerned with transformers, which consist of many layers of multi-head attention. Prov-ing lower bounds against transformers has been a critical challenge. Indeed, transformers appear to be significantly more complex than attention heads. Even while considering expressivity (i.e., which functions can be computed), there are relatively plentiful techniques to prove lower bounds            

> 1Attention outputs can either be aggregated by sum or by concatenation. In Section C, we reduce summation aggregation to concatenation aggregation. Thus, we may consider summation aggregation without loss of generality.
> 2Following practical values, we always assume m≤O(N).
> 3The time needed to compute MLPs eventually depends on its hidden dimension dhid . For inputs in Rm, the time for computing MLP in practice should roughly be O(md hid ). In this paper, we obtain lower bounds for transformers that do not have MLPs and whose embedding maps can be computed in linear time O(N m ).In Section B, we describe how to generalize our lower bounds to transformers with popular MLPs. Note that this only strengthens the lower bound, as these are specific instances of transformers.

2against attention or single layer transformers (see e.g. one-way communication [SHT23] and split-VC dimension [KUJ +25]), but only a recent breakthrough established strong lower bounds against decoder-only transformers with many layers [CPW25]. Understanding the complexity of transformers is further motivated by known lower bounds on specific tasks that we hope transformers can handle. There are many simple, fundamental prob-lems that require large transformers. For example, it is conjectured that any transformer that is capable of computing 3-SUM requires polynomial size (i.e. mHL = N Ω(1) ) [SHT23, SHT24b]. Fur-thermore, shallow decoder transformers cannot compute function composition without polynomial width [CPW25]. If we nevertheless want to use transformers to compute such functions (e.g., 3-SUM naturally captures interactions between triples of input tokens), it is crucial that large transformers can be computed as efficiently as possible. Beyond its obvious practical motivation, studying the complexity of transformers is also an inter-esting theoretical question on its own. Understanding the complexity of transformers is essentially understanding whether we can compute many attention heads faster than computing them one by one. One can ask such questions more generally: 

Question 2: Can the cost of solving many copies of the same problem simultaneously be amortized more efficiently than solving each copy separately? 

This is a recurring question in theoretical computer science, typically known as the “direct sum” problem, and there are numerous positive and negative examples to this question. On the positive side, a famous result in communication complexity gives a partial function with complexity Θ(log n) but constant O(1) amortized complexity when computing many instances of the function at the same time [FKNN95]. Recently, a breakthrough obtained a similar result for total functions [MS25]. A degree n polynomial can be evaluated on n points in ˜O(n) time [Fid72] in contrast to the naive ˜O(n2) algorithm. Similarly, the product of an n × n matrix with n distinct vectors can be computed in O(n2.372 ) time [ADV +25] in contrast to O(n3) which is required to compute them all separately. Moreover, to compute a transformer we do not need to compute each attention head explicitly. For example, the output of a layer is the sum of H attention heads. For matrix products, given n pairs of n × n matrices {Ai, B i}, the sum of products, P 

> i

AiBi can be computed in O(n3.251 ) time, much faster than computing each product separately, which takes 

O(n3.372 ) time [ADV +25]. There is an equal abundance of results on the negative side. For example, monotone func-tions with independent inputs cannot be computed more efficiently than computing each sepa-rately [GF81]. Similarly, [Bsh89] give a direct sum theorem for certain classes of quadratic systems. [BB19] also obtain a strong direct sum theorem for randomized query complexity. [MWY13] use strong direct sum theorems to obtain distributed and streaming lower bounds for various sketching and estimation tasks. In contrast to the matrix-vector multiplication problem, it is widely hypoth-esized that the (min , +) -product of an n × n matrix with n vectors cannot be computed in strongly subcubic time [VW18]. Question 2 falls naturally within this long line of research: asking whether transformers admit a direct sum theorem. In this work, we essentially resolve Question 1 (or Question 2 in the case of attention and transformer) in the negative: we give the first non-trivial lower bounds for multi-head multi-layer transformer computation and show that the naive algorithm of computing each attention head separately is essentially optimal. 31.1 Our Contributions 

We give stronger lower bounds for both the small embedding dimension ( m = N o(1) ) case and the large embedding dimension ( m = N ) case. In the small embedding dimension case, the naive algorithm runs in LHN 2+ o(1) time while the previously known conditional lower bounds on atten-tion [AS24, GHS +26] rule out algorithms in LHN 1−o(1) + N 2−o(1) time. We show a significantly improved conditional lower bound, matching the naive algorithm. 

Theorem 1.1 (Informal Theorem 3.1) . Under the 3-OV Hypothesis or SETH , any algorithm com-puting L layer, H head transformers with summation aggregation and embedding dimension m =Ω (log N ) requires 

LHN 2−o(1) 

time. The lower bound holds even if the transformer has no MLPs. 

The 3-OV Hypothesis states that finding a triplet of orthogonal vectors among a set of n vectors from {0, 1}Θ(log n) requires n3−o(1) time. The 3-OV Hypothesis is a special case of the k-OV Hy-pothesis, which has been used in many lower bounds e.g. [BRS +18, Li21, Bon22, ATYZ24, DLV25]. Furthermore, it is a consequence of the Strong Exponential Time Hypothesis ( SETH ) [Wil04], a strengthening of P̸ = NP and a popular conjecture in fine-grained complexity that states that there is no 2(1 −ε)n algorithm for satisfiability on n variables [IP01] for any constant ε > 0. SETH im-plies essentially optimal lower bounds for problems including edit distance [BI15], graph diameter [RV13], dynamic programming problems [ATYZ24], approximate closest pair problems [ARW17], subset sum [ABHS19] and many other problems. Even though many previous theoretical works model transformer embedding dimension to be roughly O(log N ), recent work also considers the scenario where embedding dimension is close to O(n). [YSBS +25] finds that for many practical graph datasets, the number of graphs (input length) could be well smaller than the embedding dimension of the transformers that are used in practice. For larger embedding dimension m the bound in Theorem 1.1 does not get better. For example, when the embedding dimension m is the same as the input length (i.e. m = N ), the best running time becomes worse: the naive upper bound is O(LHN 2m) = O(LHN 3), and with fast matrix multiplication, the upper bound can be improved to LHN ω+o(1) , where ω denotes the matrix multiplication exponent. An alternative lower bound can be obtained by using the existing conditional lower bound of N ω−o(1) for a single attention computation [GHS +26]. Nevertheless, there remains a large gap between the best lower bound max {LHN 2−o(1) , N ω−o(1) } and the best upper bound LHN ω+o(1) , and the time complexity of transformer in this setting remains poorly understood. In this work, we show that computing transformers is as hard as computing Θ( LH ) instances of matrix multiplication, thus obtaining a matching LHN ω−o(1) lower bound without any additional hypotheses, as long as ω > 2.Our result is shown in an extended version of the arithmetic circuit model, which is a gener-alization of the well-studied arithmetic circuit model (see e.g., [BCS13] for a book on arithmetic circuits). In the standard arithmetic circuit model, an algorithm only uses standard arithmetic operations {+, −, ×, / }.Since attention computation involves taking exponents, it is not clear how to compute attention at all using a standard arithmetic circuit, i.e. a standard arithmetic circuit cannot perform attention computation as simulating exponential function could already be hard enough. Thus, it is natural to also allow exponential gates and logarithmic gates in the arithmetic circuits we consider, and we call them extended arithmetic circuits (eACs). We remark that lower bounds against extended arith-metic circuits are stronger than lower bounds against arithmetic circuits, as any arithmetic circuit 4is an extended arithmetic circuit. Furthermore, all known algorithms for transformer computation exist in the extended arithmetic circuit model. With fast matrix multiplication, it is easy to obtain an extended arithmetic circuit of size 

LHN ω+o(1) that simulates H-head L-layer transformer inference with embedding dimension N .We show that this is essentially optimal. In particular, any extended arithmetic circuit computing transformers requires size LHN ω−o(1) .

Theorem 1.2 (Informal Theorem 5.1) . Any extended arithmetic circuit computing L-layer H-head transformers with summation aggregation and embedding dimension m = Ω( N ) has size at least 

LHN ω−o(1) 

when ω > 2. The lower bound holds even if the transformer has no MLPs. 

We remark that when ω = 2 (i.e. transformers with m = O(N ) can be computed in LHN 2+ o(1) 

time), we can instead apply Theorem 1.1 to obtain a matching LHN 2−o(1) lower bound. For extension of our results to concatenation aggregation, see Section C. Our lower bound on transformers shows that when embedding dimension is large, no signifi-cant improvement is possible outside fast matrix multiplication for computing transformers in the extended arithmetic circuit model. We provide a new way of proving lower bounds for multilayer transformer, which has been a challenging direction in this literature. 

1.2 Technical Overview 

In this section, we give an overview of our techniques. 

Small Embedding Dimension. We begin with our lower bound in the small embedding di-mension regime m = N o(1) . Consider a 3-OV instance A = {a1, . . . , a N }, B = {b1, . . . , b N }, C =

{c1, . . . , c LH } ⊂ { 0, 1}d for some d = Θ(log n). Note that the three sets have different sizes, and we call it an unbalanced 3-OV instance. It is known that under the standard 3-OV Hypothesis, such instances require LHN 2−o(1) time. We construct a transformer TC with L layers, H heads per layer, and embedding dimension O(d) such that TC (A, B ) (with some simple postprocessing) determines whether (A, B, C ) contains an orthogonal triple. First, we specify the input to the transformer: 

X =



a⊤ 

> 1

b⊤ 

> 1

1

... ... ...

a⊤ 

> N

b⊤ 

> N

101×d 01×d 2

 .Except the last token, each token consists of one vector from A, one vector from B, and the constant 

1. We now describe TC which has input dimension 2d + 1 . TC will consist of L layers and H heads per layer and embedding dimension m = 2 d + 2 . For every head, we set V to be 0 in the first 2d + 1 

columns, so that the output of the ℓ-th layer, denoted as X(ℓ), contains X = X(0) in the first 2d + 1 

columns. In particular, we may assume that the input to every attention head contains X in the first 2d + 1 columns. We now describe the embedding map of each head. Since |C| = LH , we can index C = {ch,ℓ }

and identify each vector in C with one head in the transformer. Consider the h-th head in the ℓ-th layer. We define embedding maps qh,ℓ , k h,ℓ , v h,ℓ : Rm → Rm as follows. Note that since our maps 5do not depend on the m-th coordinate, the following maps remain well defined for the first layer which is a map Rm−1 → Rm.

qh,ℓ : u 7 → − (u1, . . . , u d, 0, . . . , 0) ,kh,ℓ : u 7 → (ud+1 · ch,ℓ [1] , . . . , u 2d · ch,ℓ [d], 0, . . . , 0) ,vh,ℓ : u 7 → (0 , . . . , 0, u 2d+1 ).Note that we can implement the above maps with appropriate weight matrices WQ,h,ℓ , W K,h,ℓ , W V,h,ℓ ∈

Rm×m for all h, ℓ . Applying the above map to all N tokens, we obtain 

Qh,ℓ (X) = 



−a⊤ 

> 1

01×(d+2) 

... ...

−a⊤ 

> N

01×(d+2) 

01×d 01×(d+2) 

 , Kh,ℓ (X) = 



b⊤ 

> 1

⊙ ch,ℓ 01×(d+2) 

... ...

b⊤ 

> N

⊙ ch,ℓ 01×(d+2) 

01×d 01×(d+2) 

 , Vh,ℓ (X) = 



01×(2 d+1) 1

... ...

01×(2 d+1) 101×(2 d+1) 2



where ⊙ denotes the entry-wise product of two vectors and 0a×b (resp. 1a×b) denotes an a × b

submatrix filled with constant 0 (resp. 1). Thus, the output of the h-th head in the ℓ-th layer is 

softmax( Qh,ℓ (X)Kh,ℓ (X)⊤)Vh,ℓ (X). To simplify the exposition slightly, we instead consider hard-max attention. The hardmax function maps a vector v to a uniform distribution over its maximum elements. That is, for a vector v with Imax (v) = {i s.t. v i = max j vj }, hardmax( v)i = 1

> |Imax (v)|

if i ∈ Imax (v) and hardmax( v)i = 0 otherwise. A hardmax attention head then outputs Zh,ℓ := hardmax( Qh,ℓ (X)Kh,ℓ (X)⊤)Vh,ℓ (X) where hardmax is applied row-wise. It is known that hardmax attention can be approximated with softmax attention [SHT24b], so it is essentially without loss of generality to consider hardmax attention. In fact, by scaling the entries of Qh,ℓ and Kh,ℓ by an appropriate factor on the order of O(log N HL ), we can ensure that each attention head outputs 

Yh,ℓ such that 

Yh,ℓ − hardmax( Qh,ℓ (X)Kh,ℓ (X)⊤)Vh,ℓ (X) ∞ ≪ 1poly( N HL ) . (1) Then, denoting Zh,ℓ as the last column of hardmax( Qh,ℓ (X)Kh,ℓ (X)⊤)Vh,ℓ (X), we claim Zh,ℓ is a vector whose i-th entry is 2 if ai and ch,ℓ are not in any orthogonal triple (i.e. there are no bj such that ai, b j , c h,ℓ are orthogonal) and at most 32 if ai, c h,ℓ participate in an orthogonal triple. This follows as in the former case, the set of maximum indices in the i-th row is only N + 1 where Vh,ℓ (X)

contains a 2; in the latter case, the set of maximum indices contains at least one other entry, and thus hardmax averages over at least one row where V contains 1. Summing over all h, ℓ, i , we have P

> h,ℓ

P 

> i

Zh,ℓ [i] = 2 N HL if there is no orthogonal triple and at most 2N HL − 12 if there is. From (1), we can use P

> h,ℓ

P 

> i

Yh,ℓ [i, m ] to determine if A, B, C contains an orthogonal triple. 

Large Embedding Dimension. We now describe our lower bound for extended arithmetic cir-cuits that compute transformers with large embedding dimension ( m = N ). Recall that computing a single attention head is in N ω+o(1) time, as we require two matrix products, one to compute QK ⊤

and one to compute the product of softmax( QK ⊤) and V . It is then easy to see that we can use 

O(LH ) instances of matrix multiplication to compute a transformer with L layers, H heads, and embedding dimension N .The matching lower bound is much more challenging. While it is not too difficult to show that we can reduce matrix multiplication to a single attention head with embedding dimension 

N (one can, for example, set Q ← A, K ← B⊤ and V ← I so that the attention head outputs 6softmax( QK ⊤). We can use a simple denormalization trick to obtain obtain exp( QK ⊤) instead (see Section A), from which we can compute AB T , it is not clear how to use a single transformer to simultaneously compute many independent instances of matrix multiplication. Consider as an example a transformer with a single layer and H heads. The output of this transformer is the sum of all attention heads in this layer. As discussed earlier, the sum of matrix products can be computed more efficiently than computing each matrix product separately (e.g. the sum of N matrix products can be computed in O  N 3.251  time while computing each product separately requires O  N 3.372 

time. Thus, if we directly repeat the reduction of a single matrix product to a single attention head for all LH heads in our transformer, the output of the transformer only computes the sum of LH 

attention outputs, which has dimension Θ( N 2), in contrast to the Θ( LHN 2) dimension required to express LH distinct matrix products. The key tool we require is the Baur-Strassen theorem [BS83], which was originally designed to show lower bounds in the arithmetic circuit model. Perhaps the most famous application of the Baur-Strassen theorem is the backpropagation algorithm, a fundamental building block of efficient training of neural networks [RHW86]. The Baur-Strassen theorem has also found applications in algorithm design [CGS15, Fis24]. We give a new application of the Baur-Strassen theorem: to obtain lower bounds for transformers. The Baur-Strassen theorem states that given an arithmetic circuit of size s computing a function f : Rn → R, there is an arithmetic circuit of size O(s) computing all the partial derivatives of f with respect to its n inputs. In particular, Baur-Strassen gives us a powerful tool to extract more information from the partial computations within our algorithm, which provides an avenue to obtain the required Ω( LHN 2) dimensional output. In standard settings, an arithmetic circuit (Definition 2.1) consists of +, −, ×, / gates, and the size of a circuit is the total number of gates. As discussed previously, since the key non-linearity in attention is the softmax function which requires exponentiation, we equip our arithmetic circuits with exp and ln gates, and call them extended arithmetic circuits . Our first step is to give an extension of the Baur-Strassen theorem: if there is an extended arithmetic circuit of size s computing a function f : Rn → R, then there is an extended arithmetic circuit of size O(s) computing all the partial derivatives of f .Equipped with this tool, we now describe our lower bound construction. For simplicity, we describe our construction with what we call denormalized attention heads, which replaces the row-wise softmax with entry-wise exp . In particular, given Q(X), K (X), V (X), a denormalized attention head outputs exp( Q(X)K(X)⊤)V (X) (see Section A for a more detailed discussion on denormal-ized attention heads). In the proof of Theorem 1.2 (see Theorem 5.1), we modify the construction given below to ensure that the matrix product can be extracted from a standard attention head (with normalization). Equipped with denormalized attention heads, we may construct a trans-former T that outputs a vector T (X) ∈ RN with entries T (X)i = PLH k=1 

PNj=1 exp(( AkB⊤ 

> k

)ij )

where Ak, B k ∈ RN ×m are arbitrary N × m matrices for k ∈ [LH ]. Our transformer simply con-sists of H denormalized attention heads in each layer, where the h-th head in the ℓ-th layer sets 

Qh,ℓ (X) ← A(ℓ−1) H+h, Kh,ℓ (X) ← B(ℓ−1) H+h and Vh,ℓ (X) ← 1N ×1 is the vector of all 1. (Techni-cally Vh,ℓ (X) should be an N ×m matrix, but here we do not need rest of the columns and can simply pad them with 0’s.) Then, exp( Qh,ℓ (X)Kh,ℓ (X)⊤)Vh,ℓ (X) = exp( A(ℓ−1) H+hB⊤

> (ℓ−1) H+h

) · 1N ×1 is the vector consisting of row sums of exp( A(ℓ−1) H+hB⊤

> (ℓ−1) H+h

). We then conclude by summing up the outputs of all heads in each layer, and proceed inductively across all L layers. Suppose there is an extended arithmetic circuit (eAC) of size s that computes the transformer 

T . We note that while our transformer T has fixed embedding and MLP maps, and thus can be computed by a fixed circuit, the embedding and MLP maps may be parameterized by up to O(m)

weights which are additionally provided as input to the circuit. See Section 2.2 for more detailed 7discussions on our models. Given the above transformer, our goal is now to apply the extended Baur-Strassen theorem to extract useful derivatives. We begin by modifying the input slightly. Introducing auxiliary input variables Ckij for k ∈ [LH ] and i, j ∈ [N ], we define Ck to be the N × N

matrix with entries Ck[i, j ] = Ckij . Then, we define for all k ∈ [LH ] matrices ˜Ak, ˜Bk ∈ RN ×2N

where ˜Ak =  Ak Ck

 , ˜Bk =  B I .First, we observe that ˜Ak, ˜Bk can be computed efficiently in O  N 2 time. We now describe how to use the transformer T to compute the matrix product AkB⊤ 

> k

for all 

k ∈ [LH ]. Running the transformer T and summing all entries of the output vector we obtain 

F ←

> LH

X

> k=1
> N

X

> i,j =1

exp 

 ˜Ak ˜B⊤

> k



> ij



=

> LH

X

> k=1
> N

X

> i,j =1

exp 



AkB⊤

> k

 

> ij

+ Ckij 



.Now, consider the derivative of F with respect to Ckij .

∂F ∂C kij 

= ∂∂C kij 

exp 



AkB⊤

> k

 

> ij

+ Ckij 



= exp 



AkB⊤

> k

 

> ij

+ Ckij 



.Now, if we set Ckij ← 0 for all k ∈ [LH ] and i, j ∈ [N ],

∂F ∂C kij 

= exp 



AkB⊤

> k



> ij



.Thus, by applying the extended Baur-Strassen theorem, we obtain the entries of exp  AkB⊤

> k

. From, here, applying O  LHN 2 ln gates, we obtain the desired LH matrix products. In particular, we obtain an eAC of size O  s + LHN 2 that computes LH independent matrix products. The final ingredient we require is a lower bound on the size of any eAC that computes LH 

independent matrix products. While it is well-known that a standard arithmetic circuit requires 

LHN ω−o(1) size to compute LH independent matrix products (see Lemma 4.5), the lower bound for extended arithmetic circuits is a bit more subtle. At first glance, an extended arithmetic circuit may be significantly more powerful than a standard arithmetic circuit, and may be able to compute matrix products using fewer than N ω gates. We show that when restricted to low degree functions, any extended arithmetic circuit of size s can be simulated by a standard arithmetic circuit (without 

exp and ln gates) of size O(s). That is, extended arithmetic circuits exhibit no advantage when computing low degree functions. Our result extends that of Strassen [Str73] (see [Blä13] for an exposition), which states that division gates are not useful for computing low degree functions, and may be of independent interest. 

1.3 Further Related Work 

We review two lines of work in the theoretical study of transformers: (1) the expressivity of trans-formers, i.e. what functions can transformers compute and learn; (2) complexity of transformers i.e. how efficiently can transformers (or approximations of transformers) be computed? 8The Expressivity of Transformers. There is a long line of work studying the expressivity of transformers. Several works, initiated by [HHG +20, YPPN21], establish the advantage of transform-ers over recurrent architectures in tasks such as parsing hierarchical structure [YPPN21], sparse av-eraging [SHT23], in context learning [WDL25], copying [JBKM24], and document similarity [AY25]. Despite their expressive power, lower bounds against transformers show that many functions cannot be computed with small transformers. Lower bounds against transformers typically rely on communication complexity [SHT23, PNP24, SHT24b] for depth one lower bounds, or reductions to other computational models [MS23, PNP24, SHT24b]. Notably, [SHT24b] connects the transformer model with the Massively Parallel Computation model by proving an equivalence between trans-former and MPC protocol under certain parameters, and thus provide lower bounds on transformers using problems that are hard in MPC. In addition, a hard function for transformers with depth ˜O(1) 

was exhibited [CPW25]. [Hah20] establish that hardmax transformers cannot compute parity or dyck languages. 

Algorithms and Hardness for Transformer Computation. The exact computation of at-tention is fairly well understood, with quadratic lower bounds established in the small embedding regime [KWH23] and extended to more generic settings [AS24, AY25, GHS +26]. In light of these lower bounds, subquadratic algorithms have been developed in the special settings of small entries [AS24] and constant embedding dimension [GHS +26]. Given the hardness of exact attention computation, a long line of work has studied efficient ap-proximations of attention, including [BMR +20], Reformer [KKL20], [KVPF20], Big Bird [ZGD +20a], Scatterbrain [CDW +21], Performer [CLD +21], KDEFormer [ZHDK23], HyperAttention [HJK +24], and Polysketchformer [KMZ24]. More recently, [YA25] provides a new direction of computing large input length transformers with smaller input length transformers. Practical attempts at improving transformer performance typically turn towards hardware op-timization, including techniques such as FlashAttention which optimizes I/O complexity [DFE +22, FEN +23, SY24] and speculative decoding which leverages parallelism [LKM23]. 

# 2 Preliminaries 

Notations. Let [n] denote the set {1, . . . , n }. Let R≥0 denote the set of non-negative reals and 

R>0 denote the set of positive reals. R≤0, R<0 are defined analogously. Our logarithmic function 

log is the base 2 log unless otherwise specified. For a ring R, let R[x1, . . . , x n] denote the set of polynomials over n variables with coefficients in R, and let R[[ z]] = {P∞ 

> n=0

anzn|an ∈ R} denote the set of formal power series over z with coefficients in R.Let ei denote the i-th basis vector with 0 in every coordinate but 1 in the i-th coordinate. Let 

In denote the identity matrix of dimension n. For any matrix A, denote the (i, j )-th entry with 

Aij or A[i, j ]; we typically use the latter if the matrix name itself has a subscript. Let A[i; ] denote the i-th row of A and A[; j] the j-th column of A. For a vector v ∈ Rn, let diag( v) denote the 

n × n matrix with entries diag( v)ii = vi. For a matrix A, let A⊤ denote its transpose and A−1 its inverse. For any function f , let f (A) denote the matrix obtained by applying f to each entry of A.Similarly, let A ≤ B denote that Aij ≤ Bij for every entry of A, B . Let A ⊙ B denote the entry-wise product between two matrices. Let 0a×b, 1a×b denote the a × b matrix consisting of all zeros and all ones respectively. Let In denote the n × n identity matrix. 9The softmax operator is defined as 

softmax( v) = 1

PNj=1 exp( vj ) · (exp( v1), . . . , exp( vN )) ∈ RN

for v ∈ RN . We also define the hardmax operator as 

hardmax( v)[ i] = 1 [i ∈ Imax (v)] 

|Imax (v)|

where Imax (v) = {j : vj = max v}. Given a matrix A, we abuse notation and use softmax( A) and 

hardmax( A) to denote the matrix after we apply softmax and hardmax( A), respectively, on each row of A.

2.1 Arithmetic Circuits 

Definition 2.1. An arithmetic circuit (AC) is a sequence of operations {g1, . . . , g s} such that each 

gj ← y ◦ z for ◦ ∈ { +, −, ×, / } and y, z ∈ { gi}i<j ∪ { xi} ∪ R with a subset of gates {gik } denoted the output gates. Given input x, denote C(x) as the values on the output gates. An arithmetic circuit 

C computes F : Rn → Rm if C(x) = F (x) for all x ∈ Rn.The matrix multiplication exponent ω is defined as the smallest constant such that the product of two n by n matrices can be computed by an arithmetic circuit of size O(nω+ε) for any ε > 0.We will also consider a more general class of circuits, which include exponential and logarithmic gates. 

Definition 2.2. An extended arithmetic circuit (eAC) computing F : Rn → R is a sequence of operations {g1, . . . , g s} such that each gj is one of the following forms: 1. gj ← y ◦ z for ◦ ∈ { +, −, ×, / } and y, z ∈ { gi}i<j ∪ { xi} ∪ R.2. gj ← exp( y) for y ∈ { gi}i<j ∪ { xi} ∪ R.3. gj ← ln( y) for y ∈ { gi}i<j ∪ { xi} ∪ R>0.

2.2 Transformers 

We first define a self-attention head, the core primitive of a transformer. Let N denote the context (input) length, m the embedding (hidden) dimension, and din the input dimension. Recall that following practical values, we assume m = O(N ). For an input X ∈ RN ×din , we refer to each row of X as a token. 

Definition 2.3 (Attention) . A self-attention head is a mapping fQ,K,V : RN ×din → RN ×m defined by 

fQ,K,V (X) = softmax( Q(X)K(X)⊤)V (X)

and parameterized by row-wise query , key , and value embeddings Q, K, V : RN ×din → RN ×m (e.g., 

Q(X) = ( Q(X1), . . . , Q (XN )) . Let Att Nm,d in denote the set of all self-attention heads with input dimension din , embedding dimension m and input length N . When m = din , we denote this as 

Att Nm.10 X

din 

N Q(X)

m

K(X)

Q(X)K(X)⊤

N

V (X)

m

Figure 1: An attention head with input X ∈ RN ×din , input length N and embedding dimension 

m. Q(X), K (X), V (X) are obtained from X with row-wise embedding maps. Then, the output is computed by softmax( Q(X)K(X)⊤)V (X).Figure 1 illustrates an attention head. In practice, and in our constructions, embedding functions typically take the form Q(X) = XW Q (analogously K(X) = XW K , V (X) = XW V ) for some weight matrices WQ, W K , W V ∈ Rdin ×m.Hardmax attention has also been widely studied in the literature [MSS22, SHT24b, JSLC25], especially in theoretical work. For each query, hardmax attention only allows it to attend to the keys that achieve the maximum inner product (highest correlation). 

Definition 2.4 (Hardmax Attention) . A hardmax-attention head is a mapping f hard  

> Q,K,V

: RN ×din →

RN ×m defined by 

f hard  

> Q,K,V

(X) = hardmax( Q(X)K(X)⊤)V (X).In general, we use (self-)attention to mean softmax attention (Definition 2.3) unless otherwise specified. Finally, we define the multi-layer perceptron (MLP), which is a shallow neural network. By the universal approximation theorem which states that any continuous function can be approximated by a MLP (with enough neurons), we model MLP as continuous functions on vectors. 

Definition 2.5. An MLP is a continuous function Ψ : Rm1 → Rm2 for m1, m 2 > 0.Given a matrix A, we abuse notation and denote Ψ( A) as the result of applying Ψ row-wise on matrix A. We now formally define transformers. 

Definition 2.6 (Transformer) . A transformer is a mapping T : RN ×din → RN ×dout specified by attention heads {fℓ,h }L,H ℓ,h =1 and element-wise MLP layers Ψℓ : Rm → Rm for ℓ ∈ [L]. Upon input 

X ∈ RN ×din , the transformer computes intermediate embeddings X(1) , . . . , X (L) with 

X(ℓ) = Ψ ℓ X(ℓ−1) +

> H

X

> h=1

fℓ,h 



X(ℓ−1) !

∀ℓ ∈ [L],

and returns T (X) = Ψ O(X(L)) for an output MLP ΨO : Rm → Rdout (let X(0) := X for notational convenience). 11 X(0) 

din 

N

m

Ψ1

m

Ψ2

m

Ψ3 ΨO

dout 

Figure 2: A transformer with H = 3 and L = 3 . Attention heads are denoted with light gray and MLPs are denoted by Ψ. Residual connections, denoted with dotted lines, additionally add X(ℓ−1) 

to X(ℓ).A hardmax transformer is defined analogously, replacing each attention head with a hardmax-attention head. Note that in this definition, we sum up the result of each attention head, which is considered in many theoretical works (e.g., [SHT23, SHT24b, SHT24a]). In the more standard version used in practice, the results of the attention heads are concatenated. In addition, in sum aggregation, the attention head dimension equals the embedding dimension m, while in concatenation aggregation, the attention head dimension equals m/H . As we can easily obtain the sum from the concatenation using a linear MLP, all our lower bounds can be adapted to the concatenation version. See Section C for details. In this paper, we focus on attention mechanisms when proving hardness results, and show lower bounds for transformers without MLPs. In Section B, we show that our results hold also for transformers with standard MLPs found in practice, even when all MLPs take linear time to compute, i.e. given input x ∈ Rm, computing Ψ( x) takes O(m) time for MLP Ψ. Figure 2 illustrates an example of a transformer with 3 heads and 3 layers. Finally, we are ready to define what it means for an algorithm to compute transformers. 

Definition 2.7. An algorithm computes transformers with context length N , embedding dimension 

m, L layers, H heads in each layer, input dimension din and output dimension dout if given any such transformer T and any input X ∈ RN ×din , the algorithm outputs T (X).For circuits, the definition is a bit more subtle. If we were to attempt to say a circuit C computes a transformer, then the circuit must be able to compute all possible embedding and MLP maps, then the circuit must encode many functions at once, which is too strong of a condition to require. Instead, we aim to give lower bounds against circuits meeting a much weaker requirement: we show lower bounds against circuits C that compute fixed transformers. Although we consider fixed transformers, we remark that the embedding map may be parameter-ized. This corresponds in practice to the learned weights of a model. Formally, consider an attention head fℓ,h with embedding maps Qh,ℓ , K h,ℓ , V h,ℓ . Then, we require that (Qh,ℓ )i = g(wQh,ℓ , X i) where 12 g is an function computable by an eAC, wQh,ℓ denotes the weights associated to this map, and Xi

denotes the i-th row of the input to the attention head. In our constructions, we require that g is an eAC of size O(m). Let W denote the concatenation of all the weights of the transformer, and for any eAC transformer T , let TW (X) be the computation of T with weights W on input X. Finally, we define what it means for a circuit to compute a transformer. 

Definition 2.8. Let T be a transformer with context length N , embedding dimension m, L layers, 

H heads in each layer, input dimension din and output dimension dout . Suppose T has weights 

W and input X. A circuit C computes T if for any input X ∈ RN ×din and any weights W ,

C(X, W ) = TW (X).To obtain meaningful lower bounds against small eAC computing transformers, we show that transformers can compute functions that are hard for extended arithmetic circuits. Of course, to have a meaningful lower bound, we note that the transformer itself must also be computable with an eAC. 

Softmax Simulates Hardmax. We introduce a useful primitive for Transformer computation and show that softmax attention can simulate hardmax attention when there is a sufficient gap between the maximum value and the second largest value. This is similar to a result of [SHT24b]. 

Lemma 2.9. Let f ∈ Att Nm be a self-attention unit with embedding functions Q, K, V such that 1. V (X) ∈ { 0, 1, 2}n×m, and 2. for every X ∈ RN ×m and i ∈ [N ]:

A(X)i,i ′ ≤ max  

> i′′

A(X)i,i ′′ − 1, ∀i′̸ ∈ Imax (A(X)i),

where A(X) = Q(X)K(X)⊤.Then there exists an attention unit f ′ ∈ Att Nm satisfying 

f ′(X) − hardmax( A(X)) V (X) ∞ ≤ 1poly( N )

for an arbitrarily small inverse polynomial. 

The idea behind the simulation is simple: by scaling each query vector in Q by a sufficiently large constant c = Θ(log N ), we ensure that any probability assigned to non-maximum indices by the softmax operation is exponentially small with respect to c, i.e. less than 1poly( N ) for any polynomial. We leave the proof to Section A. 

2.3 Fine-Grained Complexity 

We state our major hypotheses in fine-grained complexity theory in this section. See Section 1 for more discussions on their applications. 

Hypothesis 2.10 (Strong Exponential Time Hypothesis ( SETH )) . For any ε > 0, there exists a positive integer k such that k-SAT requires Ω(2 (1 −ε)n) time, where n is the number of variables in the CNF. 

13 We say that k vectors v1, . . . , v k ∈ { 0, 1}d are orthogonal if for all j ∈ [d], vℓ[j] = 0 for some 

ℓ ∈ [k]. In other words, their k-wise inner product is 0. The k-OV problem is then defined as follows: given A1, . . . , A k ⊂ { 0, 1}d each of size n, determine if there exists v1 ∈ A1, . . . , v k ∈ Ak

such that v1, . . . , v k are orthogonal. The trivial algorithm of checking all k-tuples of vectors requires 

O(nkd) time. The k-OV Hypothesis states that for d = Θ(log n), this is essentially optimal: i.e. any algorithm requires nk−o(1) time. 

Hypothesis 2.11 (k-OV Hypothesis) . Computing k-OV on sets of n vectors with dimension Θ(log n)

requires nk−o(1) time. 

The k-OV Hypothesis follows from the popular Strong Exponential Time Hypothesis ( SETH ) [IP01] which states that satisfiability over n variables requires 2(1 −o(1)) n time [Wil04]. Our lower bound is based on the 3-OV Hypothesis, a special case of k-OV for k = 3 .The unbalanced k-OV problem is defined as follows: given k sets of vectors A1, . . . , A k of size 

S1, . . . , S k, determine if there exist v1 ∈ A1, . . . , v k ∈ Ak such that v1, . . . , v k is orthogonal. The trivial algorithm of checking all k-tuples requires O(S1S2 . . . S kd) time. Again, the Unbalanced 

k-OV Hypothesis states that this is essentially optimal and it is well-known that the Unbalanced 

k-OV Hypothesis is equivalent to the k-OV Hypothesis. 

Hypothesis 2.12 (Unbalanced k-OV Hypothesis) . k-OV on sets S1, . . . , S k of size ns1 , . . . , n sk with dimension d = Θ(log n) requires ns1+··· +sk −o(1) time. 

It is clear that the Unbalanced k-OV Hypothesis implies the k-OV Hypothesis, by setting s1 =

· · · = sk = 1 . The following lemma shows the converse direction, and we include a proof in Section A for completeness. 

Lemma 2.13. Let 0 < s 2 . . . , s k ≤ 1. Let A1, . . . A k ⊂ { 0, 1}d for d = Θ(log n) with |A1| = n

and |Ai| = nsi for all i > 1. Then, any algorithm computing k-OV on A1, . . . , A k requires time 

ns1+... +sk −o(1) under the k-OV Hypothesis. In particular, the Unbalanced k-OV Hypothesis holds under the k-OV Hypothesis. 

# 3 Lower Bounds for Small Embedding Dimension 

We begin with lower bounds for the small embedding dimension setting (i.e. m = N o(1) ). Our lower bound is based on the k-OV Hypothesis (or SETH ). First, we show that under the k-OV Hypothesis, when the embedding dimension is small (e.g. 

m = Θ(log n)), there is no algorithm that computes transformers significantly more efficiently than the naive algorithm that computes each head separately. 

Theorem 3.1 (Formal Theorem 1.1) . Let m = Θ(log N ) and L, H = poly( N ). Any algorithm computing transformers with input length N , embedding dimension m, L layers, H heads in each layer, and input and output dimension m up to entry-wise additive error 110 N requires LHN 2−o(1) 

time under the 3-OV Hypothesis. The lower bound holds even if the transformer has no MLPs. 

In the above statement, we require L, H = poly( N ) since in 3-OV the sizes of the three sets must be polynomially related. We remark that this is the interesting regime of the problem since when L, H = N o(1) , we obtain optimal lower bounds directly from the hardness of a single attention head. 14 Lemma 3.2. Suppose there exists an algorithm A that computes any transformer with input length 

N + 1 , embedding dimension 2d + 2 , L layers, H heads in each layer, input and output dimension 

2d + 2 , up to entry-wise additive error 110 N in time B, then there exists an algorithm that solves unbalanced 3-OV with dimension d and size N, N, LH in time ˜O(LHd + B).

First, we observe that Theorem 3.1 follows from Lemma 3.2. 

Proof of Theorem 3.1 assuming Lemma 3.2. Suppose for contradiction that there is an algorithm computing any transformer with input length N + 1 , embedding dimension m = 2 d + 2 , L layers, 

H heads in each layer, input and output dimension m up to entry-wise additive error 110 N in time O  LHN 2−ε for some ε > 0. Then, consider an unbalanced 3-OV instance with dimension 

d = Θ(log N ) and size N, N, LH . Lemma 3.2 implies that there is a ˜O(LHd + LHN 2−ε) algorithm for this problem, which contradicts the 3-OV Hypothesis. We now prove Lemma 3.2 to complete the proof of Theorem 3.1. 

Proof of Lemma 3.2. Consider a 3-OV instance (A, B, C ) with |A| = |B| = N and |C| = LH . We construct the following transformer T with m = din = dout = 2 d + 2 . Suppose we have an algorithm computing T up to entry-wise additive error 110 N . Let the input X ∈ R(N +1) ×din be 

X =



a⊤ 

> 1

b⊤ 

> 1

1 0

a⊤ 

> 2

b⊤ 

> 2

1 0

... ... ... ...

a⊤ 

> N

b⊤ 

> N

1 00⊤ 0⊤ 2 0



,

where ai, b i are vectors in A, B . Additionally, we index C = {ch,ℓ : 1 ≤ h ≤ H, 1 ≤ ℓ ≤ L} and define 

Qh,ℓ (X) = −



a⊤ 

> 1

01×(d+2) 

a⊤ 

> 2

01×(d+2) 

... ...

a⊤ 

> N

01×(d+2) 

01×d 01×(d+2) 



, K h,ℓ (X) = 



b⊤ 

> 1

⊙ c⊤ 

> h,ℓ

01×(d+2) 

b⊤ 

> 2

⊙ c⊤ 

> h,ℓ

01×(d+2) 

... ...

b⊤ 

> N

⊙ c⊤ 

> h,ℓ

01×(d+2) 

01×d 01×(d+2) 



, V h,ℓ (X) = 



01×(2 d+1) 1

... ...

01×(2 d+1) 101×(2 d+1) 2



such that Qh,ℓ (X) ∈ R(N +1) ×m, K h,ℓ (X) ∈ R(N +1) ×m, V h,ℓ ∈ R(N +1) ×m where b ⊙ c ∈ Rd is the element-wise product of two vectors a, b .We claim that creating the transformer requires O(dHL ) time since each attention head can be created in time O(d) time (i.e. the embedding maps can be specified in O(d) time). In addition, we can encode Qh,ℓ (X), K h,ℓ (X) as desired. In particular, Qh,ℓ , K h,ℓ : Rm → Rm apply the following maps to each row: 

Qh,ℓ : v 7 → − (v1, . . . , v d, 0, . . . , 0) ,

Kh,ℓ : v 7 → (vd+1 · ch,ℓ [1] , . . . , v 2d · ch,ℓ [d], 0, . . . , 0) ,

Vh,ℓ : v 7 → (0 , . . . , 0, 0, v 2d+1 ).15 Observe that the above maps can be expressed using weight matrices in Rm×m:

WQ,h,ℓ =

 −Id×d 0d×(d+2) 

0(d+2) ×d 0(d+2) ×(d+2) 



,

WK,h,ℓ =



0d×d 0d×(d+2) 

diag( ch,ℓ ) 0d×(d+2) 

02×d 02×(d+2) 

 ,

WV,h,ℓ =



02d×(2 d+1) 02d×1

01×(2 d+1) 101×(2 d+1) 0



so that Qh,ℓ (X) = XW Q,h,ℓ and analogously for Kh,ℓ , V h,ℓ .Firstly, we observe that the first 2d + 1 columns will never change throughout the computation. We remind the readers that X(ℓ) is the output of ℓ-th transformer layer. 

Claim 3.3. For all ℓ, the first 2d + 1 columns of X(ℓ) are the first 2d + 1 columns of X.Proof. The claim follows as the first 2d+1 columns of Vh,ℓ (X) are 0 for all h, ℓ . As a result, the output of each head consists of 0 in the first 2d + 1 columns. Then, since X(ℓ) = X(ℓ−1) + P 

> h

fh,ℓ (X(ℓ−1) )

where fh,ℓ denotes the h-th attention head in the ℓ-th layer, we have that the first 2d + 1 columns of X(ℓ) are the first 2d + 1 columns of X(ℓ−1) . By induction, the first 2d + 1 columns of X(ℓ) are the first 2d + 1 columns of X(0) = X.Secondly, we claim that the given hardmax transformer computes the 3-OV instance. Notice that this suffices because of Lemma 2.9, as an algorithm computing any softmax transformer can approximate any hardmax transformer with the same dimension. Consider the (h, ℓ )-th attention head. Note that 

(Qh,ℓ (X)Kh,ℓ (X)⊤)i,j =

(

0 if max( i, j ) = N + 1 

−ai · bj · ch,ℓ otherwise .

For 1 ≤ i ≤ N , the maximum entry in the i-th row is 0, since (Qh,ℓ (X)Kh,ℓ (X)⊤)i,N +1 = 0 .Recalling that Imax (A(X)i) denotes the set of indices attaining maximum value in the i-th row, we have that |Imax (A(X)i)| ≥ 2 if (ai, c h,ℓ ) participate in an orthogonal triple and |Imax (A(X)i)| = 1 

otherwise. In particular, let ti := ti, (h,ℓ ) denote the number of j such that ai · bj · ch,ℓ = 0 . Then we have 

hardmax( Qh,ℓ (X)Kh,ℓ (X)⊤)Vh,ℓ (X) = 



0 · · · 0 t1+2 

> t1+1

0 · · · 0 t2+2 

> t2+1

... . . . ... ...

0 · · · 0 tN +2  

> tN+1

0 · · · 0 N +2  

> N+1



In particular, for every i ≤ N , the i-th entry in the last column is 2 if ti = 0 and at most 32 otherwise. Lemma 2.9 then implies that there is a transformer such that each attention head computes an output Yh,ℓ that approximates the hardmax attention head with embeddings Qh,ℓ , K h,ℓ , V h,ℓ well. In particular, the output of the h-th head in the ℓ-th layer, denoted Yh,ℓ , satisfies 

Yh,ℓ − hardmax( Qh,ℓ (X)Kh,ℓ (X)⊤)Vh,ℓ (X) ∞ ≤ 110 N HL (2) 16 for some inverse polynomial in L, H, N .Let Zh,ℓ denote the last column of hardmax( Qh,ℓ (X)Kh,ℓ (X)⊤)Vh,ℓ (X). If the 3-OV instance is a no-instance (i.e. there is no orthogonal triple), then every Zh,ℓ [i] = 2 , i.e. 

X

> h,ℓ N

X

> i=1

Zh,ℓ [i] = 2 N HL .On the other hand, if the 3-OV is a yes-instance (i.e. there is an orthogonal triple), then there exists some i, (h, ℓ ) where Zh,ℓ [i] ≤ 32 i.e. 

X

> h,ℓ N

X

> i=1

Zh,ℓ [i] ≤ 2N HL − 12 .Let us then consider the output X(L), in particular focusing on the last column, since the first 

2d + 1 columns are exactly those of X = X(0) by Claim 3.3. Observe that our transformer outputs for all 1 ≤ i ≤ NX(L)[i, m ] = X(0) [i, m ] + X

> h,ℓ

Yh,ℓ [i, m ] = X

> h,ℓ

Yh,ℓ [i, m ].Since our algorithm computes the given transformer up to entry-wise additive error 110 N , we in fact obtain in the last column a vector ˆX such that | ˆXj − X(L)[i, m ]| ≤ 110 N for all i ∈ [N ].Thus, from the computed output of the transformer, we can compute in O(N ) time PNj=1 ˆX[j]

such that 

> N

X

> i=1

ˆX[i] − X

> h,ℓ N

X

> i=1

Zh,ℓ [i] ≤

> N

X

> i=1

ˆX[i] −

> N

X

> i=1

X(L)[i, m ]

+

> N

X

> i=1

X(L)[i, m ] − X

> h,ℓ

Yh,ℓ [i, m ]

+ X

> h,ℓ

Yh,ℓ [i, m ] − X

> h,ℓ

Zh,ℓ [i]

≤ 0.2.Here, we apply the triangle inequality, apply the approximation error of our algorithm for the first term, our equation above for the second term and (2) for the third term. Note that from the output of the transformer is a (N + 1) × m matrix, but we sum only the first N entries of the last column. Thus, the sum PNj=1 ˆX[j] is at least 2N HL − 0.2 if there is no orthogonal triple and at most 

2N HL − 0.3 otherwise. Therefore, we can compute the output of the 3-OV instance. We conclude with a simple conditional lower bound which suggests that efficient transformers require large size to compute 3-OV , suggesting that small size transformers cannot handle some instances of 3-wise interaction. 

Theorem 3.4. Under the 3-OV Hypothesis, any transformer with linear time embedding and MLP maps computing d-dimensional 3-OV requires LHm = N 1−o(1) .

17 Proof. Suppose there is a transformer T with embedding dimension m = Θ(log N ), L layers, H

heads in each layer, input dimension m, output dimension m that computes 3-OV with dimension 

m. Recall that in an efficient transformer, all embedding and MLP maps require ˜O(m) time, so that each attention head is computed in O(N m + N 2m) = O(N 2m) time. Following the standard algorithm, this transformer can be computed in time ˜O(N 2mHL ). In particular, under the 3-OV 

Hypothesis, this requires LHm = N 1−o(1) .

# 4 Tools for Algebraic Circuits 

In this section, we establish some tools for (extended) arithmetic circuits which will be used in our lower bound proofs for large embedding dimensions. While eAC is a more powerful class in general (e.g. AC cannot compute exp( x)), we show that the additional gates are not necessary for computing matrix multiplication. More generally, we argue that we can in fact use standard arithmetic circuits to simulate an eAC that computes low degree functions. For our application, we give the result for quadratic functions. The proof generalizes that of [Str73] which states that division gates are unnecessary for quadratic functions. 

Theorem 4.1. Let F = {F1, . . . , F m} where Fi = Pnk,ℓ =1 tikℓ xkxℓ. Suppose there is an eAC C of size s computing F . Then there is an AC of size O(s) computing F .Proof. Let C be an optimal eAC for computing F , and let W = ( w1, . . . , w s) be an ordering of all gates in C where earlier gates are computed first. Without loss of generality, viewing each 

wi(x1, . . . , x n) as a function, we can assume wi̸ = 0 for any i. Furthermore, since C computes F for all inputs x ∈ Rn, C is well defined for all x. In particular, if wi is ever provided as input to a ln 

gate, we have that wi(x1, . . . , x n) > 0 for all x. Similarly, if wi is ever provided as a denominator to a / gate, we have that wi(x1, . . . , x n)̸ = 0 for all x. By substituting ˜xi ← xiz, we can view each gate wj as a formal power series ˜wj ∈ R(x1, . . . , x n)[[ z]] with coefficients that are functions on 

x1, . . . , x n. We can do this as all the formal power series are well-defined when C is well-defined under all the inputs x ∈ Rn. First, the sum, difference, and product are well defined for any two formal power series, and exponentiation of a formal power series is well-defined (we will show the exact formulation later). For a function f corresponding to some gate, and its formal power series ˜f ,observe that ln( ˜f ) is well defined as long as ˜f has positive constant coefficient (see exact formulation later). The constant coefficient of ˜f is f (0 , . . . , 0) , and our assumption that inputs to ln gates are positive on all inputs ensures that f (0 , . . . , 0) > 0, which implies the constant coefficient of ˜f as needed. For a division gate wj = g/h , our assumption that any gate input to the denominator of a division gates is non-zero for all inputs ensures that the constant coefficient of ˜h is h(0 , . . . , 0) ̸ = 0 .The following fact then guarantees that the formal power series of wj is well defined. 

Fact 4.2. Let R be a ring. A power series ˜h(z) = P 

> i

aizi ∈ R[[ z]] is invertible if and only if a0̸ = 0 .Furthermore, the inverse is given by 

1˜h(z) = 1

a0

 1 + q + q2 + q3 + . . . 

where q = − P∞  

> i=1 ai
> a0

zi.

Viewing the gates as formal power series, we argue that the desired output can easily be com-puted from the low order coefficients of the power series of the relevant gates. Specifically, since 18 all Fi have degree at most 2, we can add a new variable z to all the input xi and only look at the coefficients for z0, z 1, z 2. Suppose wj is an output gate Fi. Then, we have 

Fi = wj (x1, . . . , x n) = 

> n

X

> k,ℓ =1

tikℓ xkxℓ.Notice that 

wj (x1z, . . . , x nz) = 

> n

X

> k,ℓ =1

tikℓ xkxℓz2,whose formal power series is simply itself. Hence, we can compute Fi from the coefficient of z2 in the formal power series ˜wj .We have shown that from the second coefficient of the power series corresponding to the output gates we can compute F efficiently. While we will not be able to compute the full power series of each gate as there could be infinitely many terms, we describe how to compute the low-order coefficients, i.e. the coefficients of 1, z, z 2, for all ˜wi. We proceed by induction. We denote ˜wi = P 

> j≥0

cj zj

and compute c0, c 1, c 2. For the input gates, wi(x1, . . . , x n) = xj for some xj so that ˜wi = xj z. In particular, c0 = c2 = 0 and c1 = xj . This requires O(1) gates. 

Case 1: Addition and Subtraction. Suppose ˜wi = ˜ gi + ˜hi where ˜gi, ˜hi are power series with 

˜gi =

> ∞

X

> j=0

uj zj , ˜hi =

> ∞

X

> j=0

vj zj .The first three coefficients can easily be combined via addition and subtraction i.e. cj = uj ± vj for all j. Again, this requires O(1) gates. 

Case 2: Multiplication. Suppose ˜wi = ˜ gi · ˜hi where ˜gi = P∞ 

> j=0

uj zj and ˜hi = P∞ 

> j=0

vj zj .Then, if ˜wi = P 

> j

cj zj , we have c0 = u0v0, c1 = u0v1 + u1v0, and c2 = u2v0 + u1v1 + u0v2. This requires O(1) gates. 

Case 3: Division. Suppose ˜wi = ˜ gi/˜hi where ˜gi = P∞ 

> j=0

uj zj and ˜hi = P∞ 

> j=0

vj zj . Since 

v0̸ = 0 , we obtain the inverse via Fact 4.2 as 

1˜hi

= 1

v0

 1 + q + q2 + q3 + . . . 

with q = − P∞ 

> j=1
> vj
> v0

zj . In particular, we have 

˜gi

˜hi

= 1

v0

(u0 + u1z + u2z2 + . . . ) · 1 + 



− v1

v0

z − v2

v0

z2 − . . . 



+



− v1

v0

z − . . . 

2

+ . . . 

!

= ( u′ 

> 0

+ u′

> 1

z + u′

> 2

z2 + u′

> 3

z3 + . . . ) ·



1 +  −v′

> 1

z − v′

> 2

z2 − . . .  +  −v′

> 1

z − . . . 2 + . . . 



where we set u′ 

> j

= uj 

> v0

and v′ 

> j

= vj 

> v0

. Finally, we have 

c0 = u′

> 0

c1 = u′ 

> 1

− u′

> 0

v′

> 1

c2 = u′ 

> 2

− u′

> 1

v′ 

> 1

+ u′

> 0

(v′21 − v′

> 2

).This requires O(1) gates. 19 Case 4: Exponent. Suppose ˜wi = exp(˜ gi) where ˜gi = P∞ 

> j=0

uj zj . Then, using the formal power series of exp( x) = P∞  

> k=0 xk
> k!

, we obtain 

exp(˜ gi) = exp 

X

> j≥0

uj zj



= exp( u0) exp 

X

> j≥1

uj zj



= exp( u0)

X

> k≥0

1

k!

X

> j≥1

uj zj



> k

 .

Note that for every fixed j ≥ 0, there are finitely many terms in the above sum that contribute to the coefficient of zj , so the formal power series is well-defined. In particular, 

c0 = eu0

c1 = eu0 u1

c2 = eu0



u2 + u21

2



.This requires O(1) gates. Even though the formulas use exp( u0), it is a constant and can be implemented in an AC. 

Case 5: Logarithm. Suppose ˜wi = ln(˜ gi) where ˜gi = P 

> j≥0

uj zj . Then applying the power series identity ln(1 + x) = P∞

> k=1

(−1) k+1 xk 

> k

, we have 

ln(˜ gi) = ln u0 + ln 

1 + X

> j≥1

uj

u0

zj



= ln u0 + X

> k≥1

(−1) k+1 

k ·

X

> j≥1

uj

u0

zj



> k

Again, for every fixed j ≥ 0, there are finitely many terms in the above sum that contribute to the coefficient of zj , so the formal power series is well-defined. In particular, 

c0 = ln u0

c1 = u1

u0

c2 = u2

u0

− 12

 u1

u0

2

.This requires O(1) gates. Similarly as the exp case, we only use ln on a constant, so it can be implemented in the AC model. Overall, our circuit requires O(s) gates to compute all the relevant coefficients. The next tool we require is the Baur-Strassen Theorem, which states that given an AC that computes a function, we can compute all the partial derivatives with a similar sized circuit. 20 Theorem 4.3 ([BS83]) . Let F : Rn → R be a function and F ′ = ( ∂F ∂x 1 , . . . , ∂F ∂x n ). If there is an AC 

of size s computing F , there is an AC of size O(s) computing F ′.

After Baur and Strassen [BS83] obtained this result, Morgenstern [Mor85] showed a simplified constructive proof for the Baur-Strassen theorem. Following the Morgenstern’s proof strategy, we generalize the Baur-Strassen theorem to eAC. 

Theorem 4.4 (Extended Baur-Strassen Theorem) . Let F : Rn → R be a function and let F ′ =n ∂F ∂x 1 , . . . , ∂F ∂x n

o

. If there is an eAC of size s computing F , there is an eAC of size O(s) computing 

F ′.Proof. Let {g1, . . . , g s} be a minimum size eAC that computes F . Let F (i) be the function computed by gi, . . . , g s. Note that F (i) has s−i+1 operations and has input variables {x1, . . . , x n, g 1, . . . , g i−1}.We prove the desired claim with induction. For notational simplicity, we define for 1 ≤ j ≤ n + s,

zj = xj if j ≤ n and zj = gj−n if j > n . We claim that partial derivatives of F (i) can be computed with an eAC of size 5( s − i + 1) + 2 . Then, the partial derivatives of F = F (1) can be computed with a circuit of size O(s).We begin with the base case. Note F (s+1) (x1, . . . , x n, g 1, . . . , g s) = gs has partial derivatives   

> ∂F (s+1)
> ∂g s

= 1 and ∂F (s+1)  

> ∂z

= 0 for any z ∈ { xi} ∪ { g1, . . . , g s−1}. These values are simply constants, so we can compute them using an eAC of size 1. While we need gates to store the two distinct values, we do not need to store all derivatives separately. In subsequent computations, we simply remember which value to use at the appropriate gate. This leads to 2 additional gates in the computed circuit. We now proceed with the inductive case. Assume that ∂F (i+1)  

> ∂z j

is computed for all j ≤ n + i.Then, F (i) is the eAC F (i+1) with input gi substituted with some operation defined in Definition 2.2. For any variable zj for j ≤ n + i − 1, we can compute ∂F (i) 

> ∂z j

by the chain rule, 

∂F (i)

∂z j

= ∂F (i+1) 

∂z j

+ ∂F (i+1) 

∂g i

∂g i

∂z j

.The case where gi corresponds to an operation from {+, −, ×, / } is already handled in the Morgen-stern’s proof for standard Baur-Strassen Theorem [Mor85]. Hence, we focus on the case where gi

corresponds to an exp or ln gate. Suppose gi involves input zk for some k ≤ n + i − 1, and if j̸ = k, then ∂F (i) 

> ∂z j

= ∂F (i+1)  

> ∂z j

. Hence, it suffices to compute ∂F (i) 

> ∂z k

.If gi = exp( zk) for some k ≤ n + i − 1, we have 

∂F (i)

∂z k

= ∂F (i+1) 

∂z k

+ ∂F (i+1) 

∂g i

exp( zk)

where we have ∂F (i+1)  

> ∂z k

, ∂F (i+1)  

> ∂g i

computed by induction and exp( zk) can be computed with one oper-ation. Summing requires an additional operation. Similarly, if gi = ln( zk), we have 

∂F (i)

∂z k

= ∂F (i+1) 

∂z k

+ ∂F (i+1) 

∂g i

1

zk

where the above term can be computed by 3 operations. This proves the inductive claim. Our final ingredient is a lower bound on the size of any arithmetic circuit that computes inde-pendent matrix products. 21 Lemma 4.5. For any integer ℓ ≥ 1, any arithmetic circuit computing ℓ independent n × n × n

matrix products has size ℓ · nω−o(1) .

Its proof follows from standard application of the asymptotic sum inequality [Sch81]. We include a proof of this lemma and necessary definitions for completeness. A tensor T over three sets of variables X = {x1, . . . , x |X|}, Y = {y1, . . . , y |Y |}, Z = {z1, . . . , z |Z|}

is defined as 

T =

> |X|

X 

> i=1
> |Y|

X

> j=1
> |Z|

X

> k=1

aijk xiyj zk

for some coefficients aijk ∈ R. An algebraic circuit computing the tensor is an algebraic circuit that, given inputs X, Y , outputs P|X|

> i=1

P|Y | 

> j=1

aijk xiyj for every k ∈ [|Z|].The rank R(T ) of a tensor T is defined as the smallest integer r such that T can be represented as rX

> ℓ=1



> |X|

X

> i=1

αℓ,i xi

 

> |Y|

X

> j=1

βℓ,j yj



> |Z|

X

> k=1

γℓ,k zk



for some values αℓ,i , β ℓ,j , γ ℓ,k . The asymptotic rank ˜R(T ), which is upper bounded by R(T ), is defined as lim m→∞ (R(T ⊗m)) 1/m , where T ⊗m denotes the m-th tensor power of T .The matrix multiplication tensor for n × n × n matrix products is 

> n

X

> i=1
> n

X

> j=1
> n

X

> k=1

xij yjk zik ,and we denote it by ⟨n, n, n ⟩. We also denote the disjoint union of ℓ copies of n×n×n as ℓ⊙⟨ n, n, n ⟩.Now we are ready to state the asymptotic sum inequality. 

Theorem 4.6 (Asymptotic Sum Inequality [Sch81]) . For integers ℓ, n ≥ 1,

ℓ · nω ≤ ˜R (ℓ ⊙ ⟨ n, n, n ⟩) .

We now prove Lemma 4.5. 

Proof of Lemma 4.5. Suppose there is an arithmetic circuit of size O(ℓ · nω−ε) for ℓ ⊙ ⟨ n, n, n ⟩ for some ε > 0. By definition, this circuit computes ℓ independent instances of n × n × n matrix products. It is known that the rank of a tensor is at most twice the size of a circuit computing it [Str73] (see also, e.g, [Blä13, Theorem 4.7]), so R(ℓ⊙⟨ n, n, n ⟩) ≤ O(ℓ·nω−ε), which further implies 

˜R(ℓ ⊙ ⟨ n, n, n ⟩) ≤ O(ℓ · nω−ε). Hence, by the asymptotic sum inequality [Sch81], 

ω ≤

log 

 ˜R(ℓ ⊙ ⟨ n, n, n ⟩)/ℓ 



log n

≤ log ( O(nω−ε)) log n

≤ δ + ( ω − ε) log n

log n

= ω − ε + δ/ log n, 

where δ is a constant depending on the constant hidden in the big-O notation. As we can take 

n → ∞ , this is a contradiction. Hence, ℓ ⊙ ⟨ n, n, n ⟩ does not have an arithmetic circuit of size 

O(ℓ · nω−ε) for any ε > 0, i.e., it has size at least ℓ · nω−o(1) .22 5 Lower Bounds for Large Embedding Dimension 

In this section, we prove lower bounds against transformers with large embedding dimension in the extended arithmetic circuit model. Recall Theorem 1.2: 

Theorem 5.1 (Formal Theorem 1.2) . There exists a transformer T with input length N + 1 , em-bedding dimension m = 2 N + 3 , L layers, H attention heads per layer, input dimension and output dimension 2N + 3 satisfying the following: 1. T is computable by an eAC , each embedding is computable by an arithmetic circuit of size 

O(m), and all MLP maps are the identity map. 2. Any eAC computing T has size at least LHN ω−o(1) − O  LHN 2.

We begin with defining a transformer that computes row sums of matrix products. First, we give a simple attention head that computes the (normalized) row sums of a single matrix product after taking entry-wise exponential. Our attention head also outputs the normalization factors, which we will later use to recover the entries of the matrix product. 

Lemma 5.2. Let m ≥ N . There is an attention unit Att MatMul with input length N + 1 , embed-ding dimension m′ = max( m + 1 , 2N + 3) , input dimension 2N + 3 such that for any N × m

matrices A, B and vector D ∈ RN , there exist embedding weights such that given input X =Z1 IN 1N ×1 0N ×1

Z2 01×N 0 1



∈ R(N +1) ×(2 N +3) where Z1 ∈ RN ×(N +1) , Z 2 ∈ R1×(N +1) are arbitrary matrices, the output Y ∈ R(N +1) ×m′

satisfies 

Y [i, j ] = 

  

> NN+1

if i = N + 1 and j = 1   

> D[j−1]
> N+1

if i = N + 1 and 1 < j ≤ N + 1  

> Si
> 1+ Si

if i ∈ [N ] and j = 1  

> D[j−1] 1+ Si

if i ∈ [N ] and 1 < j ≤ N + 1 0 o/w

where Si = PNj=1 exp( AB ⊤)ij for i ∈ [N ]. Furthermore, all embedding maps can be computed by arithmetic circuits of size O(N m ).Proof. Consider the embeddings 

Q(X) = 

 A 0N ×1

01×m 0



, K(X) = 

 B 0N ×1

01×m 0



∈ R(N +1) ×(m+1) 

so that 

exp 



Q(X)K(X)⊤

=

exp( AB ⊤) 1N ×1

11×N 1



Observe that the embedding maps can be expressed as Q(X) = XW Q, K (X) = XW K for matrices 

WQ, W K ∈ R(2 N +3) ×(m+1) :

WQ =



0(N +1) ×m 0(N +1) ×1

A 0N ×1

02×m 02×1

 , WK =



0(N +1) ×m 0(N +1) ×1

B 0N ×1

02×m 02×1



23 If m′ > m + 1 , we can append columns of zeros to WQ, W K and therefore Q(X), Q (K) without changing the value of Q(X)K(X)⊤. Then, consider the embedding 

V (X) = 

1N ×1 0N ×1 0N ×1 . . . 0N ×1

0 D1 D2 . . . DN



∈ R(N +1) ×(N +1) .Observe that V can be expressed as V (X) = XW V where 

WV =



0(2 N +1) ×1 0(2 N +1) ×1 0(2 N +1) ×(N −2) 0(2 N +1) ×1

1 0 01×(N −2) 00 D1 . . . DN



As before, if m′ > N + 1 , we can append columns of zeros to WV and therefore V (X), which only appends columns of zeros to the output Y . Note that Q(X), K (X), V (X) can be computed from X

with eAC (in fact AC) circuits of size O(N m ) with the relevant matrices A, B and vector D encoded into the weights. Then, let Y ∈ R(N +1) ×m′

denote the output of attention and Si := P 

> j

exp( AB T )ij .Observe that the outputs are as claimed. Next, we show how to compute the sum of many matrix products. 

Lemma 5.3. Let L, H ≥ 1 and m ≥ N . There is a transformer T with input length N +1 , embedding dimension m′ = max( m+1 , 2N +3) , L layers, H heads in each layer, input dimension 2N +3 , output dimension m′ such that for any collection of matrices Ak, B k ∈ RN ×m for k ∈ [LH ] and vectors Dk ∈

RN , there exist embedding weights such that given input X =

0N ×(N +1) IN 1N ×1 0N ×1

01×(N +1) 01×N 0 1



,the output Y ∈ R(N +1) ×m′

has entries 

Y [i, j ] = 

  

> LHN N+1

if i = N + 1 and j = 1 

PLH k=1    

> Dk[j−1]
> N+1

if i = N + 1 and 1 < j ≤ N + 1 

PLH k=1  

> Ski
> 1+ Ski

if i ∈ [N ] and j = 1 

PLH k=1   

> Dk[j−1] 1+ Ski

if i ∈ [N ] and 1 < j ≤ N + 1 0 o/w

where Ski = P 

> j

exp( AkB⊤ 

> k

)ij . Furthermore, all embedding maps can be computed by an arithmetic circuit of size O(LHN m ) and the MLPs can be taken to be the identity map. 

The final stipulation on the size of circuits computing the embedding and MLP maps ensures that these computations do not constitute a computational bottleneck for computing the transformer. 

Proof. First, we remark that in the construction of Lemma 5.2, we may assume the input dimension is in fact m′ by appending columns of zeros to the input X and rows of zeros to the weight matrices 

WQ, W K , W V without changing the computed embeddings Q(X), K (X), V (X).We construct a transformer that computes S0, S 1, . . . , S L ∈ R(N +1) ×(N +1) where for ℓ > 0, Sℓ is the matrix satisfying 

Sℓ[i, j ] = 

  

> ℓHN N+1

if i = N + 1 and j = 1 

PℓH k=1    

> Dk[j−1]
> N+1

if i = N + 1 and 1 < j ≤ N + 1 

PℓH k=1  

> Ski
> 1+ Ski

if i ∈ [N ] and j = 1 

PℓH k=1   

> Dk[j−1] 1+ Ski

if i ∈ [N ] and 1 < j ≤ N + 1 0 o/w

24 where Ski = PNj=1 exp( AkB⊤ 

> k

)ij and X denotes the input to the transformer. We proceed inductively. In the base case S0 = X, the input to the transformer. Suppose Sℓ−1

is computed. For any h ∈ [H], Lemma 5.2 implies that we obtain an attention head Att (h,ℓ ) ∈

Att N +1  

> m+1 ,2m+1

that outputs a matrix Y (h,ℓ ) such that 

Sℓ = Sℓ−1 + X

> h

Y (h,ℓ ).This follows as Y (h,ℓ ) is only non-zero in the first N + 1 columns, and we show in Lemma 5.2 that the input to the attention head can be arbitrary in the first N + 1 columns. Summing over H heads and the output of the previous layer, we obtain that the output of the 

ℓ-th layer is Sℓ as desired. Finally, we observe that T (X) is exactly SL, so we define the output MLP 

Ψ as the identity map, i.e. each row v of SL is mapped to ψ(v) = v. Note that every embedding can be computed by an arithmetic circuit of size O(N m ).We are now ready to prove Theorem 5.1. 

Proof of Theorem 5.1. Suppose there is an eAC of size s that computes the the transformer T with context length N + 1 , embedding dimension 2N + 3 , L layers, H heads per layer, input and output dimension 2N + 3 specified in Lemma 5.3 (note that this transformer satisfies the first desired property). We construct an eAC with LHN 2 additional inputs, denoted as Ckij and Dki where 

k ∈ [LH ] and i, j ∈ [N ]. We will now describe an eAC consisting of three parts: a preprocessing stage to construct the input to the transformer, the circuit that simulates the transformer, and a postprocessing stage that sums up the outputs of the transformer. We begin with the preprocessing stage. For k ∈ [LH ], define Ck to be the N × N matrix with entries Ck[i, j ] = Ckij and Dk to be the dimension N vector with entries Dk[i] = Dki . Then, given 

Ak, B k ∈ RN ×N we construct matrices ˜Ak, ˜Bk ∈ RN ×2N as follows: 

˜Ak =  Ak Ck

 , ˜Bk =  Bk I

Observe that there is an extended arithmetic circuit of size O  LHN 2 that computes ˜Ak, ˜Bk for all k ∈ [LH ].Let T be the transformer with input length N + 1 , embedding dimension 2N + 3 , L layers, H

heads per layer, input dimension 2N + 3 , and output dimension 2N + 3 given by Lemma 5.3 whose output Y is a matrix whose entries are 

Yi, 1 =

> LH

X

> k=1

Ski 

1 + Ski 

for i ∈ [N ] and 

Yi,j = X

> k

Dk[j − 1] 1 + Ski 

for i ∈ [N ] and 1 < j ≤ N + 1 where Ski = P 

> j

exp 

 ˜Ak ˜B⊤

> k

 

> ij

. Recall that we denote the size of the eAC that computes T to be s. With N additional gates, we can sum up the first column and obtain an eAC of size O  s + LHN 2 that computes 

f



{Ak, B k, C k, D k}LH k=1 



=

> N

X

> i=1

Yi, 1 =

> N

X

> i=1

X

> (h,ℓ )

Y (h,ℓ ) 

> i, 1

=

> N

X

> i=1
> LH

X

> k=1

Ski 

1 + Ski 

.25 Now, consider the partial derivatives of f . In particular for any k ∈ [LH ], i 0, j 0 ∈ [N ] with k

identified with (h, ℓ ), the partial derivative of f with respect to Cki 0j0 is 

∂f ∂C ki 0j0

= ∂∂C ki 0j0

> N

X

> i=1
> LH

X

> (h,ℓ )

Y (h,ℓ )

> i,1

= ∂∂C ki 0j0

Y (h,ℓ )

> i0,1

= ∂∂C ki 0j0

Ski 0

1 + Ski 0

=

> ∂S ki 0
> ∂C ki 0j0

(1 + Ski 0 )2

= exp(( AkB⊤ 

> k

)i0j0 + Cki 0j0 )(1 + Ski 0 )2

where in the second and fifth equality, we only include summands that involve Cki 0j0 , and in the fifth equality, we use 

( ˜Ak ˜B⊤ 

> k

)i0j0 = ( AkB⊤ 

> k

)i0j0 + Cki 0j0.Then, if we set the value of all auxiliary Ckij to 0, we obtain 

∂f ∂C ki 0j0

= exp(( AkB⊤ 

> k

)i0j0 )(1 + Ski 0 )2 .Similarly, by summing up Yi,i +1 , we obtain an eAC that computes the function 

g



{Ak, B k, C k, D k}LH k=1 



=

> N

X

> i=1
> LH

X

> k=1

Dk[i]1 + Ski 

.Taking the partial derivative with respect to Dk[i0],

∂g ∂D k[i0] = ∂∂D k[i0] Y (h,ℓ )[i0, i 0 + 1] = 11 + Ski 0

.Applying the Extended Baur-Strassen Theorem (Theorem 4.4) to both f and g, there is an eAC of size O(s + LHN 2) that computes all partial derivatives of f and g. Then, we can easily compute 



AkB⊤

> k



> i0j0

= ln 

 ∂f ∂C ki 0j0



− 2 ln 

 ∂g ∂D k[i0]



for all k ∈ [LH ] and i0, j 0 ∈ [N ] with O  LHN 2 additional gates. So far, we have obtained an eAC of size O(s + LHN 2) that computes AkB⊤ 

> k

for k ∈ [LH ]. By Theorem 4.1, there also exists an AC of size O(s + LHN 2) that computes AkB⊤ 

> k

for k ∈ [LH ], i.e., it computes LH independent matrix products of size N × N × N . Then, by Lemma 4.5, any AC that computes these independent matrix products has size LHN ω−o(1) . Therefore, we must have 

s ≥ LHN ω−o(1) − O(LHN 2) as desired. 26 6 Discussion and Open Questions 

We provide lower bounds against the efficient computation of transformers. In particular, we show that in the small embedding regime m = N o(1) , an L layer transformer with H heads per layer requires LHN 2−o(1) time under the 3-OV Hypothesis, thus establishing a conditional lower bound stating that the standard LHN 2+ o(1) algorithm is optimal up to sub-polynomial factors, improving significantly upon the previous lower bounds based only on the hardness of attention [KWH23, AS24, GHS +26]. In the large embedding regime, we establish that even when the MLP and embedding layers can be computed with small circuits, a circuit computing a transformer can compute many independent matrix products and must be large. In particular, even when the MLP and embedding functions can be computed by linear sized circuits, computing an L layer transformer with H heads per layer and embedding dimension N , a circuit of size LHN ω±o(1) is both necessary and sufficient. Finally, we conclude with some open questions. First, an interesting direction is to extend Theorem 5.1 to any algorithms in the Word-RAM model (i.e. generalizing the lower bound beyond the extended arithmetic circuit model). Second, while Theorem 5.1 rules out an arithmetic circuit that improves upon the naive algorithm for embedding dimension m = N , it is an interesting open question to rule out any arithmetic circuit improving upon the standard algorithm (when sped up with fast matrix multiplication) for any polynomial m. We remark that our techniques imply a lower bound stronger than that of Theorem 1.1 for most polynomial m. In particular, we can construct a transformer with embedding dimension m that computes LH independent products of m × N and N × m matrices. Thus, any circuit that computes transformers with embedding dimension m = N a for a > 0 requires 

LHN ω(1 ,a,a )−o(1) size. Here, ω(a, b, c ) denotes the rectangular matrix multiplication exponent, i.e. the smallest constant such that for any ε > 0, there is an arithmetic circuit of size O  nω(a,b,c )+ ε

that computes an na × nb × nc matrix product. The question then is to resolve the gap between this lower bound and the LHN ω(1 ,a, 1)+ o(1) upper bound. We remark that even for a single attention head, there is no known reduction from N × m × N rectangular matrix multiplication to attention computation with embedding dimension m.Finally, our work focuses on worst-case lower bounds. Are there input distributions under which we can develop faster algorithms? A similar question can be asked for structural assumptions: what structural assumptions (beyond those known for attention computation [AS24, GHS +26]) admit efficient algorithms? 

# Acknowledgments 

We would like to thank anonymous reviewers for many helpful comments and suggestions. 

# References 

[ABHS19] Amir Abboud, Karl Bringmann, Danny Hermelin, and Dvir Shabtay. Seth-based lower bounds for subset sum and bicriteria path. In Proceedings of the 30th Annual ACM-SIAM Symposium on Discrete Algorithms (SODA) , pages 41–57, 2019. 4 [ADV +25] Josh Alman, Ran Duan, Virginia Vassilevska Williams, Yinzhan Xu, Zixuan Xu, and Renfei Zhou. More asymmetry yields faster matrix multiplication. In Proceedings of the 2025 Annual ACM-SIAM Symposium on Discrete Algorithms (SODA) , pages 2005– 2039. SIAM, 2025. 3 27 [ARW17] Amir Abboud, Aviad Rubinstein, and Ryan Williams. Distributed PCP theorems for hardness of approximation in P. In Proceedings of the 2017 IEEE 58th Annual Sympo-sium on Foundations of Computer Science (FOCS) , pages 25–36. IEEE, 2017. 4 [AS24] Josh Alman and Zhao Song. Fast attention requires bounded entries. In Proceedings of the 37th International Conference on Neural Information Processing Systems . Curran Associates Inc., 2024. 2, 4, 9, 27 [ATYZ24] Josh Alman, Ethan Turok, Hantao Yu, and Hengzhi Zhang. Tensor Ranks and the Fine-Grained Complexity of Dynamic Programming. In Proceedings of the 15th Innovations in Theoretical Computer Science Conference (ITCS 2024) , pages 4:1–4:23, 2024. 4 [AY25] Josh Alman and Hantao Yu. Fundamental limitations on subquadratic alternatives to transformers. In Proceedings of the 13th International Conference on Learning Repre-sentations, ICLR , 2025. 2, 9 [BB19] Eric Blais and Joshua Brody. Optimal separation and strong direct sum for randomized query complexity. In Proceedings of the 34th Computational Complexity Conference, CCC , pages 29:1–29:17, 2019. 3 [BCS13] Peter Bürgisser, Michael Clausen, and Mohammad A Shokrollahi. Algebraic complexity theory , volume 315. Springer Science & Business Media, 2013. 4 [BI15] Arturs Backurs and Piotr Indyk. Edit distance cannot be computed in strongly sub-quadratic time (unless seth is false). In Proceedings of the 47th Annual ACM Symposium on Theory of Computing , pages 51–58, 2015. 4 [Blä13] Markus Bläser. Fast matrix multiplication. Theory Comput. , 5:1–60, 2013. 8, 22 [BMR +20] Tom B. Brown, Benjamin Mann, Nick Ryder, Melanie Subbiah, Jared Kaplan, Prafulla Dhariwal, Arvind Neelakantan, Pranav Shyam, Girish Sastry, Amanda Askell, Sand-hini Agarwal, Ariel Herbert-Voss, Gretchen Krueger, Tom Henighan, Rewon Child, Aditya Ramesh, Daniel M. Ziegler, Jeffrey Wu, Clemens Winter, Christopher Hesse, Mark Chen, Eric Sigler, Mateusz Litwin, Scott Gray, Benjamin Chess, Jack Clark, Christopher Berner, Sam McCandlish, Alec Radford, Ilya Sutskever, and Dario Amodei. Language models are few-shot learners. In Neural Information Processing Systems, NeurIPS , 2020. 9 [Bon22] Édouard Bonnet. 4 vs 7 sparse undirected unweighted diameter is seth-hard at time n 4/3. ACM Transactions on Algorithms (TALG) , 18(2):1–14, 2022. 4 [BRS +18] Arturs Backurs, Liam Roditty, Gilad Segal, Virginia Vassilevska Williams, and Nicole Wein. Towards tight approximation bounds for graph diameter and eccentricities. In 

Proceedings of the 50th Annual ACM SIGACT Symposium on Theory of Computing ,pages 267–280, 2018. 4 [BS83] Walter Baur and Volker Strassen. The complexity of partial derivatives. Theor. Comput. Sci. , 22:317–330, 1983. 7, 21 [Bsh89] Nader H Bshouty. On the extended direct sum conjecture. In Proceedings of the 21st Annual ACM Symposium on Theory of Computing , pages 177–185, 1989. 3 28 [CDW +21] Beidi Chen, Tri Dao, Eric Winsor, Zhao Song, Atri Rudra, and Christopher Ré. Scat-terbrain: Unifying sparse and low-rank attention. In Neural Information Processing Systems, NeurIPS , 2021. 9 [CGS15] Marek Cygan, Harold N Gabow, and Piotr Sankowski. Algorithmic applications of baur-strassen’s theorem: Shortest cycles, diameter, and matchings. Journal of the ACM (JACM) , 62(4):1–30, 2015. 7 [CLD +21] Krzysztof Marcin Choromanski, Valerii Likhosherstov, David Dohan, Xingyou Song, Andreea Gane, Tamás Sarlós, Peter Hawkins, Jared Quincy Davis, Afroz Mohiuddin, Lukasz Kaiser, David Benjamin Belanger, Lucy J. Colwell, and Adrian Weller. Re-thinking attention with performers. In International Conference on Learning Repre-sentations, ICLR , 2021. 2, 9 [CND +23] Aakanksha Chowdhery, Sharan Narang, Jacob Devlin, Maarten Bosma, Gaurav Mishra, Adam Roberts, Paul Barham, Hyung Won Chung, Charles Sutton, Sebastian Gehrmann, Parker Schuh, Kensen Shi, Sasha Tsvyashchenko, Joshua Maynez, Abhishek Rao, Parker Barnes, Yi Tay, Noam Shazeer, Vinodkumar Prabhakaran, Emily Reif, Nan Du, Ben Hutchinson, Reiner Pope, James Bradbury, Jacob Austin, Michael Isard, Guy Gur-Ari, Pengcheng Yin, Toju Duke, Anselm Levskaya, Sanjay Ghemawat, Sunipa Dev, Henryk Michalewski, Xavier Garcia, Vedant Misra, Kevin Robinson, Liam Fedus, Denny Zhou, Daphne Ippolito, David Luan, Hyeontaek Lim, Barret Zoph, Alexan-der Spiridonov, Ryan Sepassi, David Dohan, Shivani Agrawal, Mark Omernick, An-drew M. Dai, Thanumalayan Sankaranarayana Pillai, Marie Pellat, Aitor Lewkowycz, Erica Moreira, Rewon Child, Oleksandr Polozov, Katherine Lee, Zongwei Zhou, Xuezhi Wang, Brennan Saeta, Mark Diaz, Orhan Firat, Michele Catasta, Jason Wei, Kathy Meier-Hellstern, Douglas Eck, Jeff Dean, Slav Petrov, and Noah Fiedel. Palm: Scaling language modeling with pathways. J. Mach. Learn. Res. , 24:240:1–240:113, 2023. 36 [CPW25] Lijie Chen, Binghui Peng, and Hongxun Wu. Theoretical limitations of multi-layer transformer. In Proceedings of the 66th IEEE Annual Symposium on Foundations of Computer Science (FOCS) , page to appear, 2025. 3, 9 [DBK +21] Alexey Dosovitskiy, Lucas Beyer, Alexander Kolesnikov, Dirk Weissenborn, Xiaohua Zhai, Thomas Unterthiner, Mostafa Dehghani, Matthias Minderer, Georg Heigold, Sylvain Gelly, Jakob Uszkoreit, and Neil Houlsby. An image is worth 16x16 words: Transformers for image recognition at scale. In Proceedings of the 9th International Conference on Learning Representations, ICLR , 2021. 2 [DFE +22] Tri Dao, Dan Fu, Stefano Ermon, Atri Rudra, and Christopher Ré. Flashattention: Fast and memory-efficient exact attention with io-awareness. Advances in neural information processing systems , 35:16344–16359, 2022. 9 [DLV25] Mina Dalirrooyfard, Ray Li, and Virginia Vassilevska Williams. Hardness of approx-imate diameter: Now for undirected graphs. Journal of the ACM , 72(1):1–32, 2025. 4[FEN +23] Daniel Y. Fu, Elliot L. Epstein, Eric Nguyen, Armin W. Thomas, Michael Zhang, Tri Dao, Atri Rudra, and Christopher Ré. Simple hardware-efficient long convolutions for sequence modeling. In Proceedings of the 2023 International Conference on Machine Learning, ICML , pages 10373–10391, 2023. 9 29 [Fid72] Charles M. Fiduccia. Polynomial evaluation via the division algorithm the fast fourier transform revisited. In Proceedings of the 4th annual ACM symposium on Theory of computing , pages 88–93, 1972. 3 [Fis24] Nick Fischer. Deterministic sparse pattern matching via the baur-strassen theorem. In Proceedings of the 2024 Annual ACM-SIAM Symposium on Discrete Algorithms (SODA) , pages 3333–3353. SIAM, 2024. 7 [FKNN95] Tomás Feder, Eyal Kushilevitz, Moni Naor, and Noam Nisan. Amortized communica-tion complexity. SIAM Journal on computing , 24(4):736–750, 1995. 3 [GF81] Giulia Galbiati and Michael J Fischer. On the complexity of 2-output boolean networks. 

Theoretical Computer Science , 16(2):177–185, 1981. 3 [GHS +26] Shreya Gupta, Boyang Huang, Barna Saha, Yinzhan Xu, and Christopher Ye. Sub-quadratic algorithms and hardness for attention with any temperature. In Proceedings of the 14th International Conference on Learning Representations, ICLR , 2026. 2, 4, 9, 27 [Hah20] Michael Hahn. Theoretical limitations of self-attention in neural sequence models. 

Trans. Assoc. Comput. Linguistics , 8:156–171, 2020. 9 [HHG +20] John Hewitt, Michael Hahn, Surya Ganguli, Percy Liang, and Christopher D. Manning. Rnns can generate bounded hierarchical languages with optimal memory. In Proceedings of the 2020 Conference on Empirical Methods in Natural Language Processing, EMNLP ,pages 1978–2010, 2020. 9 [HJK +24] Insu Han, Rajesh Jayaram, Amin Karbasi, Vahab Mirrokni, David Woodruff, and Amir Zandieh. Hyperattention: Long-context attention in near-linear time. In The Twelfth International Conference on Learning Representations , 2024. 9 [IP01] Russell Impagliazzo and Ramamohan Paturi. On the complexity of k-sat. Journal of Computer and System Sciences , 62(2):367–375, 2001. 4, 14 [JBKM24] Samy Jelassi, David Brandfonbrener, Sham M. Kakade, and Eran Malach. Repeat after me: Transformers are better than state space models at copying. In Proceedings of the 41st International Conference on Machine Learning, ICML , 2024. 9 [JSLC25] Selim Jerad, Anej Svete, Jiaoda Li, and Ryan Cotterell. Unique hard attention: Atale of two sides. In Proceedings of the 63rd Annual Meeting of the Association for Computational Linguistics (Volume 2: Short Papers), ACL , pages 977–996, 2025. 11 [KKL20] Nikita Kitaev, Lukasz Kaiser, and Anselm Levskaya. Reformer: The efficient trans-former. In International Conference on Learning Representations, ICLR , 2020. 2, 9 [KMR +23] Alexander Kirillov, Eric Mintun, Nikhila Ravi, Hanzi Mao, Chloe Rolland, Laura Gustafson, Tete Xiao, Spencer Whitehead, Alexander C Berg, Wan-Yen Lo, et al. Seg-ment anything. In Proceedings of the IEEE/CVF international conference on computer vision , pages 4015–4026, 2023. 2 [KMZ24] Praneeth Kacham, Vahab Mirrokni, and Peilin Zhong. Polysketchformer: Fast trans-formers via sketching polynomial kernels. In Forty-first International Conference on Machine Learning , 2024. 9 30 [KUJ +25] Alexander Kozachinskiy, Felipe Urrutia, Hector Jimenez, Tomasz Steifer, Germán Pizarro, Matías Fuentes, Francisco Meza, Cristian B. Calderon, and Cristóbal Rojas. Strassen attention, split vc dimension and compositionality in transformers, 2025. 3 [KVPF20] Angelos Katharopoulos, Apoorv Vyas, Nikolaos Pappas, and François Fleuret. Trans-formers are rnns: Fast autoregressive transformers with linear attention. In Interna-tional Conference on Machine Learning, ICML , 2020. 9 [KWH23] Feyza Duman Keles, Pruthuvi Mahesakya Wijewardena, and Chinmay Hegde. On the computational complexity of self-attention. In International Conference on Algorithmic Learning Theory , pages 597–619. PMLR, 2023. 2, 9, 27 [Li21] Ray Li. Settling seth vs. approximate sparse directed unweighted diameter (up to (nu) nseth). In Proceedings of the 53rd Annual ACM SIGACT Symposium on Theory of Computing , pages 1684–1696, 2021. 4 [LKM23] Yaniv Leviathan, Matan Kalman, and Yossi Matias. Fast inference from transformers via speculative decoding. In International Conference on Machine Learning , pages 19274–19286. PMLR, 2023. 9 [LYS +25] Jingwen Liu, Hantao Yu, Clayton Sanford, Alexandr Andoni, and Daniel Hsu. Fast attention mechanisms: a tale of parallelism. In The 39th Annual Conference on Neural Information Processing Systems , 2025. 2 [Mor85] Jacques Morgenstern. How to compute fast a function and all its derivatives: a variation on the theorem of baur-strassen. SIGACT News , 16(4):60–62, 1985. 21 [MS23] William Merrill and Ashish Sabharwal. The parallelism tradeoff: Limitations of log-precision transformers. Transactions of the Association for Computational Linguistics ,11:531–545, 2023. 9 [MS25] Simon Mackenzie and Abdallah Saffidine. Refuting the direct sum conjecture for total functions in deterministic communication complexity. In Proceedings of the 57th Annual ACM Symposium on Theory of Computing , pages 572–583, 2025. 3 [MSS22] William Merrill, Ashish Sabharwal, and Noah A. Smith. Saturated transformers are constant-depth threshold circuits. Transactions of the Association for Computational Linguistics , 10:843–856, 08 2022. 11 [MWY13] Marco Molinaro, David P Woodruff, and Grigory Yaroslavtsev. Beating the direct sum theorem in communication complexity with implications for sketching. In Proceedings of the 24th annual ACM-SIAM symposium on Discrete algorithms , pages 1738–1756. SIAM, 2013. 3 [PNP24] Binghui Peng, Srini Narayanan, and Christos Papadimitriou. On limitations of the transformer architecture. In Proceedings of the 1st Conference on Language Modeling ,2024. 9 [RHW86] David E. Rumelhart, Geoffrey E. Hinton, and Ronald J. Williams. Learning represen-tations by back-propagating errors. nature , 323(6088):533–536, 1986. 7 31 [RV13] Liam Roditty and Virginia Vassilevska Williams. Fast approximation algorithms for the diameter and radius of sparse graphs. In Proceedings of the 45th annual ACM symposium on Theory of computing , pages 515–524, 2013. 4 [RWC +19] Alec Radford, Jeffrey Wu, Rewon Child, David Luan, Dario Amodei, Ilya Sutskever, et al. Language models are unsupervised multitask learners. OpenAI blog , 1(8):9, 2019. 2[Sch81] Arnold Schönhage. Partial and total matrix multiplication. SIAM J. Comput. ,10(3):434–455, 1981. 22 [Sha20] Noam Shazeer. GLU variants improve transformer. arXiv preprint arXiv:2002.05202 ,2020. 36 [SHT23] Clayton Sanford, Daniel J. Hsu, and Matus Telgarsky. Representational strengths and limitations of transformers. In Advances in Neural Information Processing Systems 36, NeurIPS , 2023. 3, 9, 12 [SHT24a] Clayton Sanford, Daniel Hsu, and Matus Telgarsky. One-layer transformers fail to solve the induction heads task. CoRR , abs/2408.14332, 2024. 12 [SHT24b] Clayton Sanford, Daniel Hsu, and Matus Telgarsky. Transformers, parallel computa-tion, and logarithmic depth. In Proceedings of the 41st International Conference on Machine Learning, ICML , 2024. 3, 6, 9, 11, 12, 13 [Str73] Volker Strassen. Vermeidung von divisionen. Journal für die reine und angewandte Mathematik , 264:184–202, 1973. 8, 18, 22 [SY24] Barna Saha and Christopher Ye. I/O complexity of attention, or how optimal is FlashAttention? In Proceedings of the 41st International Conference on Machine Learn-ing, ICML , 2024. 9 [Tea24] Llama Team. The Llama 3 herd of models. CoRR , abs/2407.21783, 2024. 36 [TMH +24] Gemma Team, Thomas Mesnard, Cassidy Hardin, Robert Dadashi, Surya Bhupati-raju, Shreya Pathak, Laurent Sifre, Morgane Rivière, Mihir Sanjay Kale, Juliette Love, et al. Gemma: Open models based on gemini research and technology. arXiv preprint arXiv:2403.08295 , 2024. 36 [VSP +17] Ashish Vaswani, Noam Shazeer, Niki Parmar, Jakob Uszkoreit, Llion Jones, Aidan N. Gomez, Lukasz Kaiser, and Illia Polosukhin. Attention is all you need. In Neural Information Processing Systems NeurIPS , 2017. 2, 44 [VW18] Virginia Vassilevska Williams and R Ryan Williams. Subcubic equivalences between path, matrix, and triangle problems. Journal of the ACM (JACM) , 65(5):1–38, 2018. 3[WDL25] Kaiyue Wen, Xingyu Dang, and Kaifeng Lyu. Rnns are not transformers (yet): The key bottleneck on in-context retrieval. In Proceedings of the 13th International Conference on Learning Representations, ICLR , 2025. 9 [Wil04] Ryan Williams. A new algorithm for optimal constraint satisfaction and its implica-tions. In Proceedings of the 31st International Colloquium on Automata, Languages and Programming (ICALP) , 2004. 4, 14 32 [YA25] Hantao Yu and Josh Alman. Two heads are better than one: Simulating large trans-formers with small ones. CoRR , abs/2506.12220, 2025. 9 [YPPN21] Shunyu Yao, Binghui Peng, Christos H. Papadimitriou, and Karthik Narasimhan. Self-attention networks can process bounded hierarchical languages. In Proceedings of the 59th Annual Meeting of the Association for Computational Linguistics and the 11th International Joint Conference on Natural Language Processing, ACL/IJCNLP , pages 3770–3785, 2021. 9 [YSBS +25] Gilad Yehudai, Clayton Sanford, Maya Bechler-Speicher, Orr Fischer, Ran Gilad-Bachrach, and Amir Globerson. Depth-width tradeoffs for transformers on graph tasks. In The 39th Annual Conference on Neural Information Processing Systems , 2025. 4 [ZGD +20a] Manzil Zaheer, Guru Guruganesh, Avinava Dubey, Joshua Ainslie, Chris Alberti, San-tiago Ontanon, Philip Pham, Anirudh Ravula, Qifan Wang, Li Yang, and Amr Ahmed. Big bird: transformers for longer sequences. In Proceedings of the 34th International Conference on Neural Information Processing Systems , 2020. 9 [ZGD +20b] Manzil Zaheer, Guru Guruganesh, Kumar Avinava Dubey, Joshua Ainslie, Chris Al-berti, Santiago Ontañón, Philip Pham, Anirudh Ravula, Qifan Wang, Li Yang, and Amr Ahmed. Big bird: Transformers for longer sequences. In Neural Information Processing Systems, NeurIPS , 2020. 2 [ZHDK23] Amir Zandieh, Insu Han, Majid Daliri, and Amin Karbasi. Kdeformer: Accelerating transformers via kernel density estimation. In International Conference on Machine Learning , pages 40605–40623. PMLR, 2023. 9 33 A Omitted Proofs 

We present omitted proofs in this section. 

Lemma 2.9. Let f ∈ Att Nm be a self-attention unit with embedding functions Q, K, V such that 1. V (X) ∈ { 0, 1, 2}n×m, and 2. for every X ∈ RN ×m and i ∈ [N ]:

A(X)i,i ′ ≤ max  

> i′′

A(X)i,i ′′ − 1, ∀i′̸ ∈ Imax (A(X)i),

where A(X) = Q(X)K(X)⊤.Then there exists an attention unit f ′ ∈ Att Nm satisfying 

f ′(X) − hardmax( A(X)) V (X) ∞ ≤ 1poly( N )

for an arbitrarily small inverse polynomial. Proof of Lemma 2.9. Fix c ≫ log N for some sufficiently large constant. Define embedding maps 

Q′(v) = c · Q(v) to scale the Q embedding by c and K′(v) = K(v), V ′(v) = V (v) to be exactly as in 

f . Note that these embedding maps can be expressed as weight matrices that are either the m × m

identity Im or a scalar of Im. Fix an index i and assume without loss of generality 1 ∈ Imax (A(X)i).Let A(X)i denote the i-th row of A(X). Then, 

X 

> i′̸∈Imax (A(X)i)

exp( cA (X)ii ′ ) ≤ N

exp( c) exp( cA (X)i1) = 1poly( N ) exp( cA (X)i1).Then, for i′ ∈ Imax (A(X)i) note that 1 

> |Imax (Ai)|

≥ softmax( cA (X)) ii ′ so that 

|softmax( cA (X)) ii ′ − hardmax( cA (X)) ii ′ | = 1

|Imax (A(X)i)| − exp( cA (X)) i1

P 

> i′

exp( cA (X)) ii ′

.Now, we can upper bound 

X

> i′

exp( cA (X)) ii ′ ≤ | Imax (A(X)i| exp( cA (X)) i1 + 1poly( N )

so that 

|softmax( cA (X)) ii ′ − hardmax( cA (X)) ii ′ | ≤ 1

|Imax (A(X)i)| 1 − 11 + 1poly( N )

!

≤ 1poly( N ) .For i′̸ ∈ Imax (A(X)i), note that hardmax( cA (X)) ii ′ = 0 so that 

|softmax( cA (X)) ii ′ − hardmax( cA (X)) ii ′ | ≤ exp( cA (X)) ii ′

|Imax | exp( cA (X)) i1

≤ 1poly( N ) .Then, for a fixed column of V (X), denoted V (X)j , we use Holder’s inequality to obtain 

f ′(X)ij − hardmax( cA (X)i) · V (X)j = (softmax( cA (X)i) − hardmax( cA (X)i)) · V (X)j

≤ ∥ softmax( cA (X)i) − hardmax( cA (X)i)∥1 V (X)j

> ∞

≤ 1poly( N ) .In particular, every entry of the output is within 1poly( N ) for an arbitrary small inverse polynomial by choosing a larger constant factor for c.34 Lemma 2.13. Let 0 < s 2 . . . , s k ≤ 1. Let A1, . . . A k ⊂ { 0, 1}d for d = Θ(log n) with |A1| = n

and |Ai| = nsi for all i > 1. Then, any algorithm computing k-OV on A1, . . . , A k requires time 

ns1+... +sk −o(1) under the k-OV Hypothesis. In particular, the Unbalanced k-OV Hypothesis holds under the k-OV Hypothesis. Proof of Lemma 2.13. Suppose we are given an instance to (balanced) k-OV of size n, with sets 

B1, . . . , B k. For 2 ≤ j ≤ k, divide Bj into tj := O(n1−sj ) sets of size nsj , denoted B(1)  

> j

, . . . , B (tj ) 

> j

.Now, for every k-tuple (i1, . . . , i k) ∈ Qkj=1 [tj ], construct an instance of unbalanced k-OV consisting of sets B1, B (i2)2 , . . . , B (ik ) 

> k

. Note that this creates O

Qkj=2 tj



instances of unbalanced k-OV . Suppose there is an algorithm computing unbalanced k-OV in time O (ns1+... +sk −ε) for some ε > 0. We can then compute all of the above instances in time 

O



> k

Y

> j=2

tj n1+ s2+··· +sk −ε

 = O



nk−ε

.Since, there is an orthogonal k-tuple in the original k-OV instance if and only if one of the unbalanced 

k-OV instances contains an orthogonal k-tuple, this violates the k-OV Hypothesis. 

A.1 Denormalized Attention 

When thinking about attention, it is frequently useful to be able to ignore the normalization required by softmax. To this end, we define denormalized attention, which replaces softmax( v) with exp( v)

for every row v of Q(X)K(X)⊤.

Definition A.1 (Denormalized Attention) . An denormalized attention head is a mapping gQ,K,V :

RN ×din → RN ×m defined by 

gQ,K,V (X) = exp( Q(X)K(X)⊤)V (X)

and parameterized by row-wise query , key , and value embeddings Q, K, V : RN ×din → RN ×m (e.g., 

Q(X) = ( Q1(X1), . . . , Q N (XN )) .We show that denormalized attention can be efficiently computed with a small transformer in the standard setting (i.e. with normalization). Unfortunately, this does not in general imply that a transformer with normalization can simulate a transformer without normalization, as the MLP is applied to the output of each layer (i.e. after summation over heads) whereas the construction below requires an MLP applied immediately to the output of each attention head. For simplicity, we describe the following transformer 

Lemma A.2. Let gQ,K,V be a denormalized attention head with input length N , input dimension 

din and embedding dimension m. There is a transformer T with input length N + 1 , embedding dimension m + din + 1 , 1 layer, 1 head, input dimension din and output dimension m such that 

T

 X

0

 

=

gQ,K,V (X)11×N V (X)



for all X ∈ RN ×din . Furthermore, all embedding and MLP maps can be computed by an arithmetic circuit of size O(N m ).

35 Proof. Consider the following embeddings: 

Q′(X) = 

Q(X) 0N ×(din +1) 

01×m 01×(din +1) 



,K′(X) = 

K(X) 0N ×(din +1) 

01×m 01×(din +1) 



,V ′(X) = 

0N ×din V (X) 0N ×1

01×din 01×m 1



where Q′(X), K ′(X), V ′(X) ∈ R(N +1) ×(m+din +1) . Then 

exp( Q′(X)K′(X)⊤) = 

exp( Q(X)K(X)⊤) 1N ×1

11×N 1



.In particular, the first N rows have row-sum Si + 1 where Si is the row-sum of the i-th row in the denormalized attention head. Note Si ≥ 0 so the denominator is always positive. Thus, in the output matrix, the first N rows consist of the input X(0) =

X

0



. Then, for i ∈ [N ], the 

(i, d in + m + 1) -th entry is 11+ Si while for j ∈ [m] the (i, d in + j)-th entry is Yi,j  

> 1+ Si

where Yij is the 

(i, j )-th entry of the denormalized attention head g. For i = N + 1 , observe the (N + 1 , d in + m + 1) -th entry is 1  

> N+1

and for j ∈ [m], the (N + 1 , j )-th entry is ∥V (X)[; j]∥1  

> N+1

. In particular, we obtain the vector 11×N V (X) scaled by 1  

> N+1

.Then, applying the output MLP ψ = ( ψ1, . . . , ψ m) : Rdin +m+1 → Rm defined by ψi : v 7 →

vdin +i/v din +m+1 , we obtain the desired output Y . Here, note that vdin +m+1 = 11+ Si > 0 since 

Si > 0. Note that every embedding map and MLP can be implemented with a size O(m) eAC. 

# B MLPs 

In standard transformer architectures, MLPs are applied after the attention layers. In our case, 

X = PHh=1 Att i(X′) is the sum of the outputs of the attention heads of the corresponding layer. MLPs are functions that are applied to input X.

GLU MLPs. In modern language models, gated linear units (GLUs) are a popular choice of acti-vation function [Sha20], for example in PaLM [CND +23], LLaMA [Tea24], and Gemma [TMH +24]. We will show that without loss of generality, transformers with GLU MLPs can simulate transform-ers with no MLPs, thus obtaining our lower bounds for transformers with GLU MLPs. We define a gated linear unit below. 

Definition B.1 (Gated Linear Unit (GLU) MLP) . Given an activation function σ : R → R, an GLU with hidden dimension dhid and activation function σ is a map ψ : Rm → Rm defined 

x 7 →    x⊤ 1 W0 ⊙ σ    x⊤ 1 W1

 1 W2

where W0, W 1 ∈ R(m+1) ×dhid , W2 ∈ R(dhid +1) ×m and σ is applied entry-wise. We show that transformers with GLU MLPs can simulate transformers with no MLPs whenever the activation function σ is not identically 0. In particular, our lower bounds for Theorem 3.1 and Theorem 5.1 hold also for transformers the GLU MLPs. 36 Proposition B.2. Suppose there exists some c ∈ R with σ(c)̸ = 0 .Let T be a transformer with input length N , embedding dimension m, L layers, H heads, input dimension din , output dimension dout with no MLPs. Then, there is a transformer T ′ with input length N , embedding dimension m, L layers, H heads, input dimension din , output dimension dout and GLU MLPs with dhid = m and activation function 

σ such that T ′(X) = T (X) for all inputs X.Furthermore, if σ can be computed by an eAC of size t, the GLU MLPs can be computed with an eAC of size O(tm ).Proof. Let σ(c) = z̸ = 0 . We construct a GLU MLP that computes the identity map. We set 

dhid = m and 

W0 =

z−1 · Im

01×m



, W1 =

 0m×m

c · 11×m



, W2 =

 Im

01×m



.Then, for any x, the GLU MLP maps 

x 7 → xz ⊙ (σ(c)1 m) = x

so that the GLU MLP ψ is the identity map. In the remainder of this section, we show that our lower bounds also hold for traditional (non-gated) MLPs. Traditionally, an MLP layer, given some input X ∈ RN ×m, computes f (XW 1)W2

where W1, W 2 ∈ Rm×dhid are learned weight matrices and f is an entry-wise activation function. In general, when we multiply by W1, W 2, we may assume that the input matrix has a column of 1’s appended to allow for a bias term to be added to the linear function computed by W1, W 2. When it is clear from context, for matrix X ∈ RN ×m, we abuse notation and let 

XW :=  X 1N ×1

 W

where W ∈ R(m+1) ×dhid .

Definition B.3 (MLP) . Given an activation function σ : R → R, an MLP with hidden dimension 

dhid and activation function σ is a map ψ : Rm → Rm defined 

x 7 →  σ    x⊤ 1 W1

 1 W2

where W1 ∈ R(m+1) ×dhid , W2 ∈ R(dhid +1) ×m and σ is applied entry-wise. In particular, we consider two standard choices of activation function: ReLU and sigmoid. 

ReLU. For the ReLU activation function x 7 → max(0 , x ), we claim that a transformer with ReLU MLP layers can simulate a transformer with no MLPs. Our lower bound then implies that there are transformers with ReLU MLP layers that cannot be computed with an eAC of LHN ω−c for any c > 0. In this case however, it is not clear how to efficiently simulate a ReLU function using an eAC, so it may be the case that an even larger eAC is necessary. 

Lemma B.4. Let T be a transformer with input length N , embedding dimension m, L layers, H

heads, input dimension din , output dimension dout with no MLPs. Then, there is a transformer T ′ with input length N , embedding dimension m, L layers, H

heads, input dimension din , output dimension dout and MLPs with dhid = 2 m and ReLU activation functions such that T ′(X) = T (X) for all inputs X.

37 Proof. Given X ∈ RN ×m, we show how to use an MLP with ReLU activation to compute the identity map. Let 

W1 =

 Im −Im

01×m 01×m



, W2 =



Im

−Im

01×m

 .Now, letting f (x) = max( x, 0) denote the ReLU function, observe 

f (XW 1)W2 =  f (X) f (−X) W2 = f (X) − f (−X).We conclude the proof by noting that f (x) −f (−x) = max(0 , x ) −max(0 , −x) = x for all x ∈ R.

Sigmoid. For the sigmoid activation function (denoted σ(x) = 1 /(1 + e−x)), we require a more intricate modification of Theorem 3.1 and Theorem 5.1. We will in fact prove the results for a more general class of activation functions. 

Definition B.5. An activation function σ : R → R is gap-preserving if the following hold: 1. σ(0) ̸ = σ(1) ,2. There exists c∗ such that for all x ≤ c∗ − 1, σ(x) ≤ σ(c∗ − 1) < σ (c∗).Note that the sigmoid is gap-preserving with c∗ = 0 . We remark that the specific choices of constants (e.g. 0, 1, −1, −.1) are not too important, and the proof can be modified for other choices of constants. We show that our lower bound in the small embedding dimension (Theorem 3.1) holds for MLPs with gap-preserving activation functions. 

Proposition B.6. Let m = Θ(log N ) and L, H = poly( N ). Any algorithm computing transformers with input length N , embedding dimension m, L layers, H heads in each layer, input dimension 

m, and output dimension 1 up to entry-wise additive error 110 N requires LHN 2−o(1) time under the 

3-OV Hypothesis. The lower bound holds even if the transformer has MLPs with hidden dimension 

O(m) and gap-preserving activation function. 

We give a high level overview of the modifications. Recall that in Theorem 3.1 we construct a transformer such that each layer’s output (1) preserves its input on m − 1 columns and (2) maintains one column to record whether an orthogonal triple has been found. On this final column, the value is 2Hℓ if ai has not participated in any orthogonal triple so far and at most 2Hℓ − 0.5

otherwise, achieving a 0.5 additive gap between the yes and no instances. We thus design an MLP that computes the identity map on m − 1 columns and maintains a gap on the final column. To compute the identity, we observe that each of the first m − 1 columns has at most 2 distinct values, thus we can design an appropriate linear function to map the outputs of the sigmoid σ back to the input. In the final column, we construct W1 to map x 7 → x − 2H and W2 to map σ(0) 7 → 0 and 

σ(−1/2) 7 → y < −0.5, maintaining a gap of at least 0.5 in the output. 

Proof. We describe the appropriate modifications to Theorem 3.1, beginning with the first layer. Let X(0) denote the input to the transformer and Zh,ℓ denote the output of h-th head in the ℓ-th layer of the hardmax transformer defined in Theorem 3.1. Recall 

X(0) =



a⊤ 

> 1

b⊤ 

> 1

1

... ... ...

a⊤ 

> N

b⊤ 

> N

10 0 2

 ∈ R(N +1) ×(m−1) , Zh,ℓ =



0 . . . 0 z1,h,ℓ 

... . . . ... ...

0 . . . 0 zN,h,ℓ 

0 . . . 0 N +2  

> N+1

 ∈ R(N +1) ×m

38 where zi,h,ℓ = 2 if ai, c h,ℓ do not participate in an orthogonal triple, and zi,h,ℓ ≤ 32 otherwise. Then, 

X

> h

Zh,ℓ =



0 . . . 0 z1,ℓ 

... . . . ... ...

0 . . . 0 zN,ℓ 

0 . . . 0 H(N +2)  

> N+1



where zi,ℓ = P 

> h

zi,h,ℓ .We construct an MLP with hidden dimension dhid = m and gap-preserving activation function. Define linear functions f1, f 2, f 3 such that f1 is an increasing linear function mapping σ(c∗) 7 → 0

and σ(c∗ − 1) 7 → − 1 and 

f1(x) = b1x + b2

f2(x) = x − σ(0) 

σ(1) − σ(0) = c1x + c2

f3(x) = 1 + f2(x) = c1x + (1 + c2)

for some constants b1, b 2, c 1, c 2 ∈ R and observe 

f1 : σ(c∗) 7 → 0, σ(c∗ − 1) 7 → − 1

f2 : σ(0) 7 → 0, σ(1) 7 → 1

f3 : σ(0) 7 → 1, σ(1) 7 → 2.Note that we can construct f1, f 2, f 3 by the first two properties of Definition B.5. Set the weight matrices as 

W1 =



I2d 02d×1 02d×1

01×2d 1 001×2d 0 201×2d −1 c∗ − 4H

 , W2 =



c1I2d 02d×1 02d×1

01×2d c1 001×2d 0 b1

(c2)1×2d 1 + c2 b2

 .Note that W1, W 2 have m + 1 rows (instead of m) since the last row encodes the bias term of the linear function W1, W 2.Since the first 2d columns of X(0) is 0 or 1, we ensure that the first 2d columns is preserved by the MLP, since W1 is the identity map and f2 maps σ(0) = 12 to 0 and σ(1) to 1.Similarly, since the (2 d + 1) -th column is 1 or 2, we ensure that this is preserved by the MLP as well. Here, observe that W1 maps 1 and 2 to 0 and 1 respectively, while f3 maps σ(0) to 1 and 

σ(1) to 2.Finally, consider an entry in the (2 d + 2) -th column. As before, if ai does not participate in an orthogonal triple with any {ch, 1} then zi, 1 = 2 H and otherwise, zi, 1 ≤ 2H − 12 . Then, 

σ(Y (1) W1)[ i, m ] = σ(c∗) in the former case and σ(x) ≤ σ(c∗ − 1) in the latter case. In particular, the MLP layer outputs X(1) [i, m ] = ( σ(Y (1) W1)W2)[ i, m ] = 0 in the former case, and is at most −1

(since f1 is increasing) in the latter case. We can proceed inductively for subsequent layers. Note that with the exception of the m-th column, the identical proof as above shows that the contents of the input matrix X(0) are preserved. It suffices to show that the m-th column allows us to distinguish a yes-instance of 3-OV from a no-instance. We use the following inductive claim: for all i, if ai participates in an orthogonal triple with ch,t for some h ∈ [H], t ≤ ℓ, then X(ℓ)[i, m ] ≤ − 1. Otherwise, X(ℓ)[i, m ] = 0 . Above we have established the base case with ℓ = 1 .39 Let Y (ℓ) denote the output of the attention layer, we have 

Y (ℓ)[i, m ] = X(ℓ−1) [i, m ] + X

> h

zi,h,ℓ .In a no-instance, we have X(ℓ−1) [i, m ] = 0 (by induction) and P 

> h

zi,h,ℓ = 2 H. Following the proof of the base case, we conclude X(ℓ)[i, m ] = 0 .We now argue that in a yes-instance, whenever ai participate in an orthogonal triple with some ch,t for t ≤ ℓ, X(ℓ)[i, m ] ≤ − 1. By induction, we either have X(ℓ−1) [i, m ] ≤ − 1 if t < ℓ 

or P 

> h

zi,h,ℓ ≤ 2H − 0.5. Since X(ℓ−1) [i, m ] ≤ 0 and P 

> h

zi,h,ℓ ≤ 2H in either case, we have 

Y (ℓ)[i, m ] ≤ 2H − 0.5. Since W1 applies an increasing function to the m-th column, we again have that the input to σ is c∗ in the no-instance and at most c∗ − 1 in the yes instance. We then conclude the proof identically as in the base case. Thus, encoding MLPs as above, it suffices to examine the m-th column of the output of the transformer to compute 3-OV . In a no instance, all the output entries should be 0. In a yes instance, at least one entry will be less than −1.We now move on to Theorem 5.1, which we prove a separate class of activation functions (which sigmoid also satisfies). 

Definition B.7. An activation function σ : R → R is eAC gradient efficient if the following hold: 1. σ(0) ̸ = σ(1) ,2. σ−1 can be implemented with an eAC of size O(1) .3. σ′(x) = h(σ(x)) for some h such that both σ, h can be implemented with an eAC of size O(1) .Note that the sigmoid function is eAC gradient efficient as σ′(x) = σ(x)(1 − σ(x)) and σ−1 is computable on the range of the sigmoid function (0 , 1) . We remark that in the event that the last layer has no MLP, the second condition is unnecessary. 

Proposition B.8. There exists a transformer T with input length N + 1 , embedding dimension 

m = 2 N + 1 , L layers, H attention heads per layer, input dimension 1 and output dimension N + 1 

satisfying the following: 1. T is computable by an eAC , and each embedding and MLP is computable by an eAC of size 

O(m).2. Any eAC computing T has size at least LHN ω−o(1) − O  LHN 2.The lower bound holds even when T has MLPs with dhid = O(m) and eAC gradient efficient acti-vation functions. Proof. We maintain the construction of Theorem 5.1, modifying only the MLPs. As in Proposi-tion B.6, each MLP weight matrix has an additional row to allow for a bias term in the linear function. We set 

W1 =



IN +1 0(N +1) ×(m′−(N +1)) 

0(m′−(N +1)) ×(N +1) Im′−(N +1) 

01×(N +1) 01×(m′−(N +1)) 

 , W 2 =



IN +1 0(N +1) ×(m′−(N +1)) 

0(m′−(N +1)) ×(N +1) c1Im′−(N +1) 

01×(N +1) −(c2)1×(m′−(N +1)) 



where c1, c 2 are constants as defined in Proposition B.6. Note that to define c1, c 2 we only require the property σ(0) ̸ = σ(1) . Thus, the MLP layer applies σ entry-wise to the first N + 1 columns and 40 maps 0, 1 to 0, 1 respectively in the last m′ − (N + 1) columns. Since σ can be computed by an eAC of O(1) size, each MLP map can be computed by an eAC of size O(m), satisfying the first property. We first argue that the outputs of the attention heads as described in Lemma 5.2 remain un-changed. Since the guarantees of Lemma 5.2 holds as long as the last m′ − (N + 1) columns are the same as the input X(0) , it suffices to argue that this is maintained after each layer, which is formalized in the following claim. 

Claim B.9. In the transformer defined in Theorem 5.1 with MLPs specified by W1, W 2 above, the output of each layer ℓ ≥ 0 satisfies X(ℓ)[i, j ] = X(0) [i, j ] for all i ∈ [N + 1] , j > N + 1 i.e., the input is preserved in the last m′ − N + 1 columns. Proof. We proceed inductively. The base case holds trivially, so we consider the inductive case. From Lemma 5.2, each attention head outputs 0 beyond the first N + 1 columns, so that the output of each attention layer is its exactly its input on these columns. Since our input to the transformer, denoted X(0) , contains only 0, 1 in these columns, the MLP computes the identity map on these values, and maintains the input beyond the first N + 1 columns, proving the inductive hypothesis. We now argue that we may recover the matrix products efficiently from an eAC that computes the described transformer. Let h be the function such that σ′(x) = h(σ(x)) . In the following, assume j ≤ N + 1 , since we only need the entries from the first N + 1 columns of the output of the transformer to recover the matrix products. As in Proposition B.6, we define 

Y (ℓ) := X(ℓ−1) + X

> h

Y (h,ℓ )

where Y (h,ℓ ) denotes the output of the h-th head in the ℓ-th layer. Recall that above we have shown 

Y (h, ℓ ) are exactly the outputs of the corresponding attention head in the transformer constructed in Theorem 5.1. In particular, if Y denotes the output of the transformer, we have for j ≤ N + 1 ,

Yi,j = σ



Y (L)

> i,j



= σ X(L−1)  

> i,j

+ X

> h

Y (h,L )

> i,j

!

and for all ℓ > 0 and j ≤ N + 1 ,

X(ℓ) 

> i,j

= σ



Y (ℓ)

> i,j



= σ X(ℓ−1)  

> i,j

+ X

> h

Y (h,ℓ )

> i,j

!

.In contrast to when there is no MLP, where Yi,j = P 

> h,ℓ

Y (h,ℓ ) 

> i,j

we now have 

Yi,j = σ X

> h

Y (h,L ) 

> i,j

+ σ X

> h

Y (h,L −1)  

> i,j

+ σ (. . . )

!! 

. (3) 41 Recall that in the proof of Theorem 5.1 we use the outputs of the transformer to compute the following two functions: 

f



{Ak, B k, C k, D k}LH k=1 



=

> N

X

> i=1

Yi, 1

g



{Ak, B k, C k, D k}LH k=1 



=

> N

X

> i=1

Yi,i +1 .We now describe how to recover LH independent matrix products AkB⊤ 

> k

, thus completing the proof. This is formalized in the following inductive lemma. 

Lemma B.10. Recall that we index k ∈ [LH ] as (h, ℓ ). For all 0 ≤ ℓ ≤ L, there is an eAC of size 

O(H(L − ℓ)N 2) that computes AkB⊤ 

> k

for k = ( h, p ) with p ≥ ℓ,

nQLp=t σ′



Y (p)

> i0,1

o 

> t∈[ℓ−1,L ],i 0

, and 

nQLp=t σ′



Y (p)

> i0,i 0+1

o 

> t∈[ℓ−1,L ],i 0

.Proof. For the base case, fix ℓ = L. Then, for fixed k = ( h0, L ) and i0, j 0 we have 

∂f ∂C ki 0j0

= ∂∂C ki 0j0

> N

X

> i=1

Yi, 1

= ∂∂C ki 0j0

Yi0,1

= σ′ 

Y (L)

> i0,1

 ∂Y (h0,L )

> i0,1

∂C ki 0j0

.In the second equality, we keep only summands depending on Cki 0j0 and in the third, we apply the chain rule and observe that only Y (h0,ℓ ) 

> i0,1

in Y (L) 

> i0,1

depends on Cki 0j0 . Since σ is eAC gradient efficient, we can compute σ′



Y (L)

> i0,1



with h



σ



Y (L)

> i0,1

 

= h(Yi0,1) using the output of the transformer. Over all h ∈ [H] and i0 ∈ [N ], we use an eAC of size O(HN ). In particular, we can compute 

∂Y (h0,L )

> i0,1

∂C ki 0j0

=

> ∂f ∂C ki 0j0

h(Yi0,1) .We make a similar argument for g as follows. 

∂g ∂D ki 0

= ∂∂D ki 0

Yi0,i 0+1 = σ′(Y (L)

> i0,i 0+1

) ∂Y (h0,L )

> i0,i 0+1

∂D ki 0

.An identical proof allows us to recover ∂Y (h0,L )

> i0,i 0+1
> ∂D ki 0

using an eAC of size O(HN ).Since in Theorem 5.1, we showed how to compute the products from ∂Y (h,ℓ )

> i01
> ∂C ki 0j0

and ∂Y (h,ℓ )

> i0,i 0+1
> ∂D ki 0

, we can use the above derivatives to recover the matrix products AkB⊤ 

> k

with an eAC of size O(N 2).Repeating the above argument for all H gives us a O(HN 2) eAC that computes all matrix products in the L-th layer. Given the resulting matrix products, we can compute all Y (h,L ) 

> i0,1

with an eAC of size O(HN 2) by summing the rows of each matrix product. Then, since 

Yi0,1 = σ X

> h

Y (h,L ) 

> i0,1

+ σ



Y (L−1) 

> i0,1

!

42 we can compute 

σ′ 

Y (L−1) 

> i0,1



= h



σ



Y (L−1) 

> i0,1

 

= h σ1 (Yi0,1) − X

> h

Y (h,L )

> i0,1

!

for all i0 with an eAC of size O(HN ). Note that this is only step where we require σ to be invertible. In particular, we have described how to compute the following quantities: 

n

σ′ 

Y (L−1) 

> i0,1



σ′ 

Y (L)

> i0,1

o 

> i0

,

n

AkB⊤

> k

o

> k=( h,L )

using an eAC of size O(HN 2), completing the base case. Now, consider the inductive case ℓ < L . We argue that all additional required values can be computed with an eAC of size O(HN 2). Applying the chain rule, for k ← (h0, ℓ ), i 0, j 0, we have 

∂f ∂C ki 0j0

= ∂∂C ki 0j0

Yi0,1

=

> L

Y

> t=ℓ

σ′ 

Y (t)

> i0,1

! ∂Y (h0,ℓ )

> i0,1

∂C ki 0j0

following similar logic to the base case. By the inductive hypothesis, we have QLt=ℓ σ′



Y (t)

> i0,1



from the previous (ℓ + 1) -th iteration. Thus, with an eAC of size O(HN ) we can compute for all i0:

∂Y (h0,ℓ )

> i0,1

∂C ki 0j0

=

> ∂f ∂C ki 0j0

QLt=ℓ σ′



Y (t)

> i0,1

 .From here, we again follow the proof of Theorem 5.1 to recover 

{AkBk}k=( h,ℓ )

and sum up matrix rows to obtain 

n

Y (ℓ)

> i0,1

o

> i0

with an eAC of size O(HN 2). Then, since 

Y (ℓ) 

> i0,1

= X

> h

Y (h,ℓ ) 

> i0,1

+ σ



Y (ℓ−1) 

> i0,1



we can compute 

σ′ 

Y (ℓ−1) 

> i0,1



= h



σ



Y (ℓ−1) 

> i0,1

 

= h Y (ℓ) 

> i0,1

− X

> h

Y (h,ℓ )

> i0,1

!

for all i0 using an eAC of size O(HN ). Using our inductive hypothesis, we compute for all i0,

> L

Y

> t=ℓ−1

σ′ 

Y (t)

> i0,1



= σ′ 

Y (ℓ−1) 

> i0,1

 LY

> t=ℓ

σ′ 

Y (t)

> i0,1



with O(N ) size eAC, completing the inductive case. A similar argument holds for the partial derivatives of g with respect to Dki 0 .The above lemma allows us to conclude the proof, computing all LH matrix products. 43 C Head Aggregation 

Consider a transformer with L layers of H heads with embedding dimension m. There are two standard ways to aggregate the outputs of the heads in the ℓ-th layer, denoted X(ℓ), given the input 

X(ℓ−1) : summation and concatenation. Summation aggregation is described in Definition 2.6. In concatenation aggregation (see e.g. [VSP +17]), each attention head maps each row of dimension m

into dimension m/H . The output is then 

X(ℓ) = Ψ ℓ



X(ℓ−1) +  Att Q1,K 1,V 1

 X(ℓ−1)  Att Q2,K 2,V 2

 X(ℓ−1)  . . . Att QH ,K H ,V H

 X(ℓ−1)  

where Ψ denotes the MLP applied after concatenation. We show such a transformer can simulate a transformer with summation aggregation and embed-ding dimension m/H , so that any lower bound against transformers with summation aggregation and embedding dimension m/H also hold against transformations with concatenation aggregation and embedding dimension m.

Proposition C.1. Assume H ≤ m divides m. Suppose T is a transformer with input length N ,embedding dimension m/H , L layers, H heads per layer, and summation aggregation. Then, there is a transformer T ′ with input length N + 1 , embedding dimension m + H, L layers, 

H heads per layer and concatenation aggregation such that there is a linear function f : Rm → Rm

such that f (T ′(X)) = T (X) for all inputs X. Furthermore, the following hold: 1. If T is computable by an eAC , so is T ′.2. If each embedding map of T is computable by an AC of size O(m/H ), then all embedding maps of T in one layer are computable by an AC of size O(m).3. If T has no MLPs, then neither does T ′.4. If T has MLPs or GLU MLPs with dhid = O(m/H ) and activation function σ, then T ′ has an MLP with dhid = O(m) and activation function σ (see Definition B.3 and Definition B.1). 

In particular, Theorem 3.1 implies that whenever m/H = N o(1) , the standard LHN 2+ o(1) time algorithm to compute transformers with concatenation aggregation is optimal, and Theorem 5.1 implies that whenever m/H = Θ( N ), the corresponding LHN ω+o(1) time algorithm is optimal. Furthermore, the reduction preserves whatever assumptions we make on the MLPs, either in the main body or Section B. 

Proof. We show that each layer can be simulated. Let Y (ℓ) be the output of the ℓ-th layer of T .In addition, let Y (ℓ,h ) be the output of the h-th attention head in the ℓ-th layer of T before the summation. By definition, Y (ℓ) = Ψ ℓ

 Y (ℓ−1) + P 

> h

Y (ℓ,h ) for ℓ ≥ 1 (in the case where T does not have MLP, Ψℓ is simply the identity map). Consider a single layer of T ′ with input X(ℓ−1) , where we view it as a concatenation of H blocks: 



X(ℓ−1) 1 X(ℓ−1) 2 . . . X(ℓ−1) 

> H



.We define the input of the transformer T ′ as X(0) 1 = Y (0) and X(ℓ−1)  

> h

:= 0 N ×(m/H ) for all h > 1. We will prove two cases separately: when T has no MLPs, and when T has MLPs with dhid = O(m)

and activation function σ.44 Case 1: T has no MLPs. We proceed with the inductive hypothesis 

> H

X

> h=1

X(ℓ−1)  

> h

= Y (ℓ−1) .That is, the sum of all the blocks is the output of the (ℓ − 1) -th layer of T . Note that the base case is satisfied by construction. Consider the h-th attention head. Fix an attention head of T with embedding maps Qh, K h, V h :

RN ×(m/H ) → RN ×(m/H ) (recall that T has embedding dimension m/H ). We design attention heads with similar embedding maps. Define the map g : Rm → Rm/H where 

g : v 7 → v



Im/H 

...

Im/H 

 := vW g

so that g(v)i = P  

> i≡i′mod ( m/H )

vi.If T has no MLPs, we design embedding maps Q′

> h

, K ′

> h

, V ′ 

> h

: RN ×m → RN ×(m/H ) as 

Q′ 

> h

= Qh ◦ g, K ′ 

> h

= Kh ◦ g, V ′ 

> h

= Vh ◦ g.Note here that if Qh : X → XW Qh can be described as a linear function, so can Q′ 

> h

: X → XW gWQh

(and analogously for Kh, V h). Under the inductive hypothesis, we have that 

Q′

> h

 

X(ℓ−1) 1 X(ℓ−1) 2 . . . X(ℓ−1) 

> H

 

= Qh

X

> h

X(ℓ−1) 

> h

!

= Qh(Y (ℓ−1) )

is exactly the embedding computed by the h-th head of the ℓ-th layer of T . A similar argument holds for K′

> h

, V ′

> h

. As a result, the concatenated attention heads outputs 

 Y (ℓ, 1) . . . Y (ℓ,H ) .The output of the layer is then 



Y (ℓ, 1) + X(ℓ−1) 1 . . . Y (ℓ,H ) + X(ℓ−1) 

> H



so that X

> h

Y (ℓ,h ) + X(ℓ−1)  

> h

= Y (ℓ−1) + X

> h

Y (ℓ,h ) = Y (ℓ)

satisfying the inductive hypothesis. At the final layer, the transformer T ′ outputs 

 Y (L, 1) Y (L, 2) . . . Y (L,H )

so that g(T ′(X)) = T (X). To conclude the proof, note that g is easily implemented by an AC (and therefore eAC) of size O(m). Since every embedding map Qh, K h, V h takes the same input (i.e. the output of g), we can implement all H embedding maps with an AC of size O(m).45 Case 2: T has MLPs or GLU MLPs with dhid = O(m/H ) and σ activation. If T has MLPs with hidden dimension dhid = O(m) and activation function σ, we instead proceed with the inductive hypothesis 

X(ℓ−1) 1 = Y (ℓ−1) and X(ℓ−1)  

> h

= 0 N ×m for h > 1.That is, X(ℓ−1) 1 is the output of the (ℓ − 1) -th layer of T and the remaining blocks are 0. The base case again holds by definition. Consider the ℓ-th layer. Define the maps π : Rm → Rm/H as 

π : v 7 → v

 Im/H 

0(m−m/H )×(m/H )



= ( v1, . . . , v m/H ).Furthermore, for a ≤ b, define i : Ra → Rb as the map i : v 7 → (v, 0b−a) as the injective map with padding 0. For every fixed attention head h, define Q′ 

> h

= Qh ◦ π and K′

> h

, V ′ 

> h

analogously. Here, observe that Q′

> h

, K ′

> h

, V ′ 

> h

are embeddings that can in fact be implemented with AC of size O(m/H ),so all embedding maps can be implemented with an AC Of size O(m).Now, for the MLP, if T has MLPs given by W1 : R(m/H )+1 → Rdhid , W 2 : Rdhid +1 → Rm, σ , we let T ′ have MLPs given by 

W ′

> 1

(X) :=  X 1

Im/H 0(m/H )×1

... ...

Im/H 0(m/H )×1

01×(m/H ) 1

 W1, W ′

> 2

(X) :=    X 1 W2 01×(m−(m/H )) 



and σ activation function. Note that both W1, W 2 can be implemented as eAC of size O(m).We claim that the construction yields the desired result. By the inductive hypothesis, the output of the attention heads (after concatenation) in the ℓ-th layer is 

Z :=  Y (ℓ, 1) Y (ℓ, 2) . . . Y (ℓ,H ) .In particular, the first linear map of the MLP W ′ 

> 1

computes 

W ′

> 1



Y (ℓ−1) + Z



= W ′

> 1

   Y (ℓ, 1) + Y (ℓ−1) Y (ℓ, 2) . . . Y (ℓ,H ) 

=  Y (ℓ−1) + P 

> h

Y (ℓ,h ) 1N ×1

 W1

is exactly the input to activation function σ computed by T . This completes the proof of the inductive case, as the W ′ 

> 2

outputs the output of the MLP into X(ℓ)1 and 0 everywhere else. A similar modification applies to GLU MLPs by modifying W0 analogously to W1.46