---
title: Multi-stage Flow Scheduling for LLM Serving
title_zh: LLM 推理服务中的多阶段流调度
authors: "Yijun Sun, Xudong Liao, Songrun Xie, Hao Chen, Han Tian, Wenxue Li, Yiming Zhang, Kai Chen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17456v1"
tags: ["query:llm"]
score: 10.0
evidence: 多阶段流调度优化大模型服务的首字延迟
tldr: 针对大语言模型（LLM）推理中严格的首字延迟（TTFT）需求，本文提出了多阶段流调度机制MFS。在解耦架构下，KV块检索、集体通信等阶段的流量竞争会导致SLO违规。MFS通过“延迟与提升”原则和反向多级队列（RMLQ）结构，在无需精确余量信息的情况下近似最小松弛度优先（LLF）策略，动态优化网络带宽分配。实验表明，MFS显著提升了TTFT SLO的达成率。
selection_source: fresh_fetch
motivation: 现有的LLM推理系统在多阶段工作流中缺乏阶段感知的流调度，导致网络竞争严重并破坏了首字延迟（TTFT）的SLO。
method: 提出MFS机制，利用反向多级队列（RMLQ）实现“延迟与提升”原则，从而在不依赖精确余量预测的情况下近似最小松弛度优先调度。
result: 在8节点GPU集群和大规模仿真中，MFS相比现有基准方案将TTFT SLO的达成率提高了1.2倍至2.4倍。
conclusion: MFS通过协同多阶段网络流量调度，有效解决了LLM服务中的网络瓶颈问题，是提升推理效率的关键技术。
---

## 摘要
满足严格的首字延迟（TTFT）要求对于大语言模型（LLM）应用至关重要。为了提高效率，现代 LLM 推理服务系统采用了具有多种并行模式的解耦架构，引入了复杂的多阶段工作流，涉及可重用的 KV 块检索、集合通信以及 P2D 传输。来自依赖阶段的流在共享瓶颈链路上跨请求或在请求内部发生重叠，使得 TTFT 极易受到网络竞争的影响，因此需要感知阶段的调度。遗憾的是，现有的大多数工作以阶段无关的方式调度流，导致了不协调的竞争，这是造成服务水平目标（SLO）违规的主要原因。在本文中，我们提出了 MFS，一种旨在最大化 TTFT SLO 达成率的整体多阶段流调度机制。其核心在于，MFS 在不需要精确掌握请求剩余松弛时间的情况下，近似实现了最小松弛度优先（LLF）调度策略。它通过一种基于反向多级队列（RMLQ）结构实现的“延迟与提升”（Defer-and-Promote）原则来达成这一目标。通过随着有效松弛度的减少动态提升任务优先级，MFS 优先处理松弛度较小的流，同时防止 SLO 较宽松的请求过早消耗网络带宽。我们将 MFS 实现为集成到 vLLM 中的可插拔模块，并在包含 8 台服务器、32 个 GPU 的测试平台以及大规模仿真中对其进行了评估。实验结果表明，MFS 有效地优于最先进的基准方案，将 TTFT SLO 达成率提高了 1.2 倍至 2.4 倍。

## Abstract
Meeting stringent Time-To-First-Token (TTFT) requirements is crucial for LLM applications. To improve efficiency, modern LLM serving systems adopt disaggregated architectures with diverse parallelisms, introducing complex multi-stage workflows involving reusable KV-block retrieval, collective communication, and P2D transfer. Flows from dependent stages overlap within and across requests on shared bottleneck links, making TTFT highly susceptible to network contention and necessitating stage-aware scheduling. Unfortunately, most existing works schedule flows in a stage-agnostic manner, leading to uncoordinated contention that constitutes a primary cause of SLO violations.   In this paper, we present MFS, a holistic multi-stage flow scheduling mechanism designed to maximize TTFT SLO attainment. At its core, MFS approximates the Least-Laxity-First (LLF) scheduling policy without requiring precise knowledge of a request's remaining slack. It achieves this through a Defer-and-Promote principle implemented through a Reverse Multi-Level Queue (RMLQ) structure. By dynamically promoting task precedence as effective laxity diminishes, MFS prioritizes flows with less laxity while preventing requests with loose SLOs from prematurely consuming network bandwidth. We implement MFS as a pluggable module integrated into vLLM, and evaluate it on a 8-server, 32-GPU testbed as well as through large-scale simulations. Our results demonstrate that MFS effectively outperforms state-of-the-art baselines, improving the TTFT SLO attainment by 1.2x--2.4x.

---

## 论文详细总结（自动生成）

这是一份关于论文《Multi-stage Flow Scheduling for LLM Serving》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在现代大语言模型（LLM）推理服务中，**首字延迟（TTFT）**是衡量用户体验和系统可靠性的核心指标。
*   **核心挑战**：现代系统采用预填充-解码解耦（Prefill-Decode Disaggregation）和并行架构，导致生成首个 Token 需要经历三个通信阶段：(1) 远程 KV 缓存检索；(2) 算子并行产生的集体通信（如 All-to-All）；(3) 预填充到解码（P2D）的状态传输。
*   **问题本质**：这三个阶段的流量在共享的网络链路上存在严重的**请求内竞争**和**请求间竞争**。现有的流调度算法（如 SJF, EDF）通常是“阶段无关”的，无法识别不同通信任务对计算任务的阻塞关系，导致网络拥塞并频繁触发 TTFT 的服务水平目标（SLO）违规。

### 2. 论文提出的方法论：MFS
MFS 的核心思想是**多阶段协同调度**，通过“**延迟与提升（Defer-and-Promote）**”原则最大化 SLO 达成率。
*   **反向多级队列（RMLQ）**：不同于传统 MLFQ 随时间降级，MFS 将流初始化在低优先级队列（延迟），随着截止日期临近或松弛度减少，动态提升其优先级（提升）。
*   **显式截止日期调度（针对 P2D 阶段）**：引入**最小链路利用率（MLU）**指标。只有当剩余带宽刚好能满足截止日期要求时才触发优先级提升，实现“准时完成”而非“过早完成”，为早期阶段腾出带宽。
*   **隐式截止日期调度（针对 KV 检索和集体通信）**：
    *   **相对层索引（RLI）**：根据当前计算层与目标通信层的距离判断紧急程度。RLI 越小（越接近当前计算需求），优先级越高。
    *   **鲁棒有效截止日期（RED）**：在请求间竞争时，通过 RED 算法过滤掉极少数极紧迫请求对整个批次的优先级干扰（防止“搭便车”效应）。
*   **可行性检查与剪枝**：在系统过载时，通过启发式算法剪掉（降级）注定无法达标的请求，保护其他可行请求的带宽。

### 3. 实验设计
*   **场景与数据集**：
    *   **模型**：涵盖多种 MoE 模型（Mixtral 8x7B/8x22B, DBRX, Grok-2, Qwen3-Coder）和长文本 Dense 模型（Llama3-8B）。
    *   **工作负载**：使用来自阿里巴巴 Qwen 的真实生产轨迹（对话类 QwenA 和智能体类 QwenB）。
*   **基准测试（Benchmarks）**：
    *   **Fair Sharing (FS)**：公平共享带宽。
    *   **Shortest Job First (SJF)**：短作业优先。
    *   **Earliest Deadline First (EDF)**：最早截止日期优先。
    *   **Karuna**：针对混合流的截止日期感知调度。
*   **对比维度**：TTFT SLO 达成率、集体通信完成时间（CCT）、请求早熟度（Earliness）。

### 4. 资源与算力
*   **物理测试平台**：使用了 8 台服务器，共计 **32 个 NVIDIA RTX 3090 GPU**，配备 100Gbps Mellanox NIC 和 NVIDIA SN2700 交换机。
*   **仿真环境**：基于 Vidur 和 flowsim 构建了大规模仿真器，模拟了 **256 台服务器**（基于 NVIDIA A100 性能剖析数据）的胖树（Fat-tree）网络拓扑。

### 5. 实验数量与充分性
*   **实验规模**：论文结合了真实硬件测试（验证可行性）和大规模仿真（验证可扩展性）。
*   **充分性**：
    *   涵盖了 5 种以上的主流模型架构。
    *   测试了不同的请求到达率（RPS）以模拟不同负载压力。
    *   进行了微观基准测试（Micro-benchmarks），深入分析了 CCT 减少和流量调节对性能的具体贡献。
*   **客观性**：对比了工业界和学术界最常用的四种基准算法，实验设计较为公平，考虑了长文本、MoE 等多种前沿场景。

### 6. 论文的主要结论与发现
*   **性能提升**：MFS 在各种负载下均显著优于基准方案，将 **TTFT SLO 达成率提升了 1.2 倍至 2.4 倍**。
*   **效率优化**：在相同 SLO 预算下，MFS 能支持比最强基准（Karuna）高出 **17% - 46% 的请求吞吐量**。
*   **关键发现**：通过延迟非紧急的 P2D 流量，可以将集体通信的延迟（CCT）降低约 **50%**，从而直接缩短预填充阶段的计算停顿时间。

### 7. 优点
*   **阶段感知（Stage-aware）**：首次系统性地分析了 LLM 推理中多阶段通信的依赖关系，并将其转化为调度策略。
*   **无需精确预测**：通过 RMLQ 结构，在不需要精确知道请求剩余执行时间的情况下，实现了对最小松弛度优先（LLF）算法的有效近似。
*   **高兼容性**：作为可插拔模块集成到 vLLM、NCCL 和 Mooncake 中，易于在现有生产环境中部署。

### 8. 不足与局限
*   **算力平台限制**：物理实验使用的是较旧的 RTX 3090 和 PCIe 3.0，虽然仿真补足了 A100 的数据，但在 H100/B200 等具有更强硬件级隔离特性（如多租户隔离）的最新硬件上的表现尚未验证。
*   **剪枝风险**：虽然“软剪枝”能保护系统，但在极端高负载下，被降级到“清除者（Scavenger）”队列的请求可能会面临极长的延迟，论文对这部分被牺牲请求的用户体验讨论较少。
*   **内部通信假设**：主要关注节点间（Scale-out）网络，对于节点内（Scale-up，如 NVLink）的复杂竞争虽有讨论但未深入实验。

（完）
