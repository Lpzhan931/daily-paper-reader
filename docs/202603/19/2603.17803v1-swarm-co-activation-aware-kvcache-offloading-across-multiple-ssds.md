---
title: "Swarm: Co-Activation Aware KVCache Offloading Across Multiple SSDs"
title_zh: Swarm：跨多 SSD 的协同激活感知 KVCache 卸载
authors: "Tuowei Wang, Liyun Chu, Ruwen Fan, Ju Ren"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17803v1"
tags: ["query:llm"]
score: 10.0
evidence: 利用协同激活相关性将KV缓存卸载到SSD
tldr: 针对大语言模型推理中KVCache带来的内存压力，本文提出Swarm系统。针对SSD带宽受限的问题，Swarm利用KVCache共同激活（Co-Activation）的特性，通过离线聚类和基于图的多SSD放置策略，将单设备访问转化为并行I/O。实验表明，Swarm显著降低了I/O耗时并提升了带宽利用率，为大规模长文本推理提供了高效且低成本的存储方案。
selection_source: fresh_fetch
motivation: 传统的KVCache卸载到SSD受限于单设备带宽和PCIe瓶颈，难以满足高性能推理需求。
method: 利用KVCache共同激活特性，通过离线聚类、图驱动的多SSD放置及选择性副本机制实现并行I/O。
result: 实验结果显示，Swarm将I/O时间缩短了2.41倍，并将有效带宽利用率提升了2.72倍。
conclusion: Swarm证明了通过感知访问模式并利用多SSD并行性，可以有效克服SSD在KVCache卸载中的带宽瓶颈。
---

## 摘要
键值（KV）缓存已成为大语言模型（LLM）推理中内存消耗的主要来源。尽管将 KVCache 从 GPU 高带宽内存（HBM）卸载到 CPU DRAM 缓解了设备内存压力，但对于大规模、持久性的工作负载，DRAM 的容量仍然有限且成本高昂。固态硬盘（SSD）提供了一种具有成本效益的替代方案，但由于 PCIe 吞吐量受限以及单设备带宽限制，朴素的基于 SSD 的分页机制在根本上受限于带宽。在本文中，我们观察到真实工作负载中的 KVCache 激活表现出强且稳定的相关性。我们将这种现象称为 KVCache 协同激活（KVCache Co-Activation），即访问一个 KV 条目通常伴随着一组稳定且重复出现的其他 KV 条目。利用这一特性，我们提出了 Swarm，一个基于 SSD 的 KVCache 卸载系统，它将受带宽限制的单设备访问转换为跨多个 SSD 的并行 I/O。具体而言，Swarm 离线聚类协同激活的 KV 条目，并使用基于图的放置策略和选择性副本机制将生成的聚类分布在多个 SSD 上，以最大化并行 I/O 带宽。在运行时，Swarm 执行负载均衡的聚类检索，并动态调整聚类和缓存决策，以在不断变化的访问模式下维持高带宽利用率。评估结果表明，Swarm 将 I/O 时间减少了 2.41 倍，并将有效带宽利用率提高了 2.72 倍。

## Abstract
The key-value (KV) cache has become the dominant contributor to memory consumption in large language model (LLM) inference. Although offloading KVCache from GPU high-bandwidth memory (HBM) to CPU DRAM alleviates device memory pressure, DRAM remains capacity-limited and costly for large, persistent workloads. Solid-state drives (SSDs) provide a cost-effective alternative, but naive SSD-based paging is fundamentally bandwidth-bound due to limited PCIe throughput and per-device bandwidth constraints.   In this paper, we observe that KVCache activations in real-world workloads exhibit strong and stable correlations. We term this phenomenon KVCache Co-Activation, where accessing a KV entry is often accompanied by a stable and recurring set of other KV entries. Leveraging this property, we present Swarm, an SSD-based KVCache offloading system that converts bandwidth-bound single-device access into parallel I/O across multiple SSDs. Specifically, Swarm clusters co-activated KV entries offline and distributes the resulting clusters across SSDs using graph-based placement with selective replication to maximize parallel I/O bandwidth. At runtime, Swarm performs load-balanced cluster retrieval and dynamically adapts clustering and caching decisions to sustain high bandwidth utilization under evolving access patterns. Evaluations show that Swarm reduces I/O time by 2.41x and improves effective bandwidth utilization by 2.72x.

---

## 论文详细总结（自动生成）

### Swarm: 跨多 SSD 的协同激活感知 KVCache 卸载论文总结

#### 1. 核心问题与背景
随着大语言模型（LLM）上下文长度的增加和模型深度的提升，**KV 缓存（KVCache）** 已成为推理过程中内存消耗的主要来源。虽然将 KVCache 从 GPU 显存（HBM）卸载到 CPU 内存（DRAM）是常用手段，但 DRAM 容量有限且成本高昂。固态硬盘（SSD）虽具备大容量和低成本优势，但其单设备带宽（受限于 PCIe x4 接口）远低于 DRAM，导致 SSD 卸载方案通常受限于 I/O 带宽瓶颈，严重拖慢推理速度。

#### 2. 方法论：核心思想与技术细节
论文提出了 **SWARM** 系统，其核心思想是利用 **KVCache 协同激活（Co-Activation）** 现象——即在推理过程中，某些 KV 条目总是倾向于被同时访问。通过将这些协同激活的条目分布在多个 SSD 上，将带宽受限的单设备访问转化为高效的并行 I/O。

*   **离线阶段（建模与放置）：**
    1.  **模式提取：** 通过分析真实工作负载，构建邻接矩阵记录 KV 条目间的协同激活频率。
    2.  **图抽象与聚类：** 将 KV 条目视为节点，协同激活强度转化为距离。采用启发式算法：先选出协同激活密度最高的条目作为**中心点（Medoid）**，再根据半径阈值 $\tau$ 贪婪扩展形成聚类。
    3.  **分层分区：** 
        *   **DRAM 层：** 存放聚类中心点（用于索引）、局部窗口条目（保持上下文连续性）和热点聚类。
        *   **SSD 层：** 将聚类内的条目通过 Round-robin 策略均匀分布在多个 SSD 上，并允许少量副本以解耦复杂的激活依赖。
*   **在线阶段（检索与更新）：**
    1.  **负载均衡调度：** 运行时根据查询激活聚类，合并重复条目，并根据 SSD 桶（Bucket）的当前负载，将带有副本的条目路由到负载最轻的设备。
    2.  **动态适配：** 
        *   **聚类维护：** 在解码过程中，根据新生成 token 在局部窗口内的表现，将其动态归类到最相关的聚类中。
        *   **缓存替换：** 基于“成本-效益”评分（结合激活频率和聚类大小）动态更新 DRAM 中的热点缓存。
    3.  **异步 I/O 与流水线：** 利用 `io_uring` 实现非阻塞读取，并通过预取技术使 SSD 访问与 GPU 计算重叠。

#### 3. 实验设计
*   **数据集/场景：** 涵盖了 WikiText（语言建模）、LongBench（长文本理解）、MMLU（多任务知识）和 GSM8K（数学推理）。
*   **Benchmark 模型：** Qwen3 (14B/32B/235B MoE)、Llama3.1 (70B)、GPT-OSS (120B)。
*   **对比方法：** 
    *   **No Cluster：** 无聚类的朴素 SSD 存储。
    *   **InfLLM：** 基于固定大小分块和块级重要性检索的方法。
    *   **PQCache：** 基于乘积量化（K-means 聚类）的 KV 检索方法。

#### 4. 资源与算力
*   **硬件配置：** 
    *   **CPU：** Intel Xeon Gold 6530 (64核)。
    *   **GPU：** 8 张 NVIDIA H20 (每张 96GB HBM)。
    *   **内存：** 1TB DDR5 DRAM。
    *   **存储：** 最多 8 个 NVMe SSD（包括高性能 Samsung PM9A3 和低延迟 Intel Optane 900P）。
*   **训练/分析时长：** 论文未明确给出离线聚类分析的具体耗时，但提到聚类是作为一次性的预处理步骤。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **端到端性能：** 在 5 种模型和 4 个数据集上进行了吞吐量、带宽和准确率测试。
*   **消融实验：** 针对离线聚类策略（中心点、副本）、SSD 放置策略、DRAM 缓存策略、在线调度算法等分别进行了对比。
*   **敏感性分析：** 测试了不同前缀长度（最高达 1M token）、SSD 数量（1-8个）、SSD 类型、稀疏率以及数据集偏移对性能的影响。
*   **客观性：** 实验覆盖了从 14B 到 235B 不同规模的模型，并对比了当前最先进的稀疏检索基准。

#### 6. 主要结论与发现
*   **性能提升：** SWARM 平均减少了 **2.41 倍** 的 I/O 时间，有效带宽利用率提升了 **2.72 倍**。
*   **可扩展性：** 随着 SSD 数量增加，吞吐量呈线性增长趋势。在 8 个 SSD 协同下，带宽可达 **37.67 GB/s**，接近 GPU HBM 与 CPU DRAM 之间的通信带宽。
*   **准确性：** 相比于其他稀疏检索方法，SWARM 的聚类策略能更精准地捕捉注意力动态，其模型输出准确率最接近全量 KVCache 的基准。

#### 7. 优点
*   **创新视角：** 首次深入探讨并利用了 KVCache 的“协同激活”特性，将存储优化从简单的“相似度”转向“访问模式相关性”。
*   **系统级优化：** 不仅有算法改进，还结合了 `io_uring`、零拷贝技术和多设备负载均衡，是一个完整的系统级解决方案。
*   **成本效益：** 证明了通过增加廉价 SSD 的数量并配合智能调度，可以达到昂贵 DRAM 的性能水平。

#### 8. 不足与局限
*   **离线开销：** 聚类过程依赖于离线分析，如果推理任务的分布与离线数据集差异极大，可能需要重新分析。
*   **存储冗余：** 为了最大化并行性，系统引入了选择性副本机制，虽然论文称开销可控，但在存储空间极端受限的情况下仍需权衡。
*   **硬件依赖：** 性能高度依赖于服务器的 PCIe 通道数量和 SSD 的并发处理能力，在消费级硬件上可能难以完全发挥 8 盘并行的优势。

（完）
