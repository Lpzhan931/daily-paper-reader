---
title: "SpecEyes: Accelerating Agentic Multimodal LLMs via Speculative Perception and Planning"
title_zh: SpecEyes：通过投机性感知与规划加速代理型多模态大语言模型
authors: "Haoyu Huang, Jinfa Huang, Zhongwei Wan, Xiawu Zheng, Rongrong Ji, Jiebo Luo"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23483v1"
tags: ["query:llm"]
score: 9.0
evidence: 智能体级投机加速框架，打破顺序执行瓶颈
tldr: 针对代理型多模态大模型在调用视觉工具时产生的巨大串行延迟问题，本文提出SpecEyes加速框架。该框架利用轻量级模型作为投机规划器，预测执行轨迹并提前终止冗余工具链。通过引入基于答案可分性的认知门控机制进行自我验证，并结合异构并行漏斗设计，SpecEyes在保持甚至提升准确率的同时，实现了1.1-3.35倍的推理加速，显著优化了系统吞吐量。
selection_source: fresh_fetch
motivation: 代理型多模态模型频繁调用视觉工具导致的串行开销（代理深度）造成了严重的推理延迟，限制了系统的并发处理能力。
method: 利用轻量级模型作为投机规划器预测执行路径，并引入基于答案可分性的认知门控机制进行自我验证，配合异构并行漏斗实现高效推理。
result: "在V* Bench等多个基准测试中，该方法实现了1.1至3.35倍的加速，且准确率最高提升了6.7%。"
conclusion: SpecEyes通过投机执行打破了代理任务的串行瓶颈，为高性能代理型多模态大模型的高效部署提供了新方案。
---

## 摘要
代理型多模态大语言模型（MLLM）（如 OpenAI o3 和 Gemini Agentic Vision）通过迭代调用视觉工具实现了卓越的推理能力。然而，级联的感知、推理和工具调用循环引入了显著的顺序开销。这种被称为“代理深度”（agentic depth）的开销导致了极高的延迟，并严重限制了系统级的并发性。为此，我们提出了 SpecEyes，这是一个代理级的投机加速框架，旨在打破这一顺序瓶颈。我们的核心见解是，轻量级、无需工具的 MLLM 可以作为投机规划器来预测执行轨迹，从而在不牺牲准确性的情况下提前终止昂贵的工具链。为了调节这种投机规划，我们引入了一种基于答案可分离性（answer separability）的认知门控机制，该机制在不需要标准标签的情况下量化模型的置信度以进行自我验证。此外，我们设计了一个异构并行漏斗，利用小模型的无状态并发性来掩盖大模型的有状态串行执行，从而最大化系统吞吐量。在 V* Bench、HR-Bench 和 POPE 上的广泛实验表明，SpecEyes 相比代理基线实现了 1.1-3.35 倍的加速，同时保持甚至提高了准确性（最高提升 6.7%），从而提升了并发负载下的服务吞吐量。

## Abstract
Agentic multimodal large language models (MLLMs) (e.g., OpenAI o3 and Gemini Agentic Vision) achieve remarkable reasoning capabilities through iterative visual tool invocation. However, the cascaded perception, reasoning, and tool-calling loops introduce significant sequential overhead. This overhead, termed agentic depth, incurs prohibitive latency and seriously limits system-level concurrency. To this end, we propose SpecEyes, an agentic-level speculative acceleration framework that breaks this sequential bottleneck. Our key insight is that a lightweight, tool-free MLLM can serve as a speculative planner to predict the execution trajectory, enabling early termination of expensive tool chains without sacrificing accuracy. To regulate this speculative planning, we introduce a cognitive gating mechanism based on answer separability, which quantifies the model's confidence for self-verification without requiring oracle labels. Furthermore, we design a heterogeneous parallel funnel that exploits the stateless concurrency of the small model to mask the stateful serial execution of the large model, maximizing system throughput. Extensive experiments on V* Bench, HR-Bench, and POPE demonstrate that SpecEyes achieves 1.1-3.35x speedup over the agentic baseline while preserving or even improving accuracy (up to +6.7%), thereby boosting serving throughput under concurrent workloads.

---

## 论文详细总结（自动生成）

### SpecEyes: 通过投机性感知与规划加速代理型多模态大模型

#### 1. 核心问题与研究动机
*   **核心问题**：代理型多模态大语言模型（Agentic MLLMs）通过迭代调用视觉工具（如缩放、裁剪、OCR）来增强推理能力，但这引入了严重的**顺序开销（Agentic Depth）**。
*   **研究动机**：
    *   **延迟爆炸**：每一步工具调用都依赖前一步的结果，导致端到端响应时间随深度线性增长。
    *   **并发崩溃**：由于工具调用链具有状态依赖性，传统的 GPU 批处理（Batching）失效，系统吞吐量极低。
    *   **现有局限**：现有的 Token 级投机采样或 Token 剪枝方法仅优化单步计算，无法打破“感知-推理-工具调用”这一宏观循环的串行瓶颈。

#### 2. 方法论：SpecEyes 框架
SpecEyes 将投机加速从 Token 级别提升到了**代理级别（Agentic Level）**，核心思想是“快思考与慢思考”的结合。
*   **四阶段流水线**：
    1.  **工具使用判断（Phase I）**：由大模型 $M_L$ 快速判断查询是否需要工具。若不需要，进入投机路径。
    2.  **投机预测（Phase II）**：利用轻量级、无状态的小模型 $M_S$ 直接根据原始图像生成答案及 Logits。
    3.  **认知门控（Phase III）**：引入**答案可分性评分（Answer Separability, $S_{sep}$）**。该指标通过计算 Top-K Logits 的决策边际（均值与标准差的标准化差异）来衡量 $M_S$ 的置信度。若评分高于阈值 $\tau$，则直接采纳 $M_S$ 的答案。
    4.  **代理回退（Phase IV）**：若 $M_S$ 置信度不足，则回退到完整的大模型 $M_L$ 代理循环中。
*   **异构并行漏斗**：利用 $M_S$ 的无状态特性进行大规模并行处理，仅将少量低置信度任务转发给有状态的串行 $M_L$，从而掩盖串行开销。

#### 3. 实验设计
*   **数据集/基准测试**：
    *   **V* Bench**：测试细粒度属性识别和空间推理。
    *   **HR-Bench (4K/8K)**：测试高分辨率图像感知。
    *   **POPE**：测试物体幻觉的鲁棒性（Adversarial, Popular, Random 分组）。
*   **对比方法**：
    *   **基线模型**：DeepEyes 和 Thyme（均为代理型 MLLM）。
    *   **对比框架**：SpecReason（一种基于语义一致性的投机推理方法）。
    *   **小模型选择**：Qwen3-VL-2B。

#### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A100 40GB GPU** 上运行。
*   **训练/校准时长**：文中未提及大规模训练（因为是推理框架），但提到门控阈值的离线校准仅需约 **5-10 分钟**。
*   **推理配置**：代理模型最大步数限制为 5 步，使用贪婪解码。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流基准测试的 7 个子集，进行了详尽的对比。
*   **消融实验**：针对**门控阈值**、**批处理大小（Batch Size）**以及 **Top-K 参数**进行了专门的消融研究。
*   **充分性评价**：实验设计较为充分，通过在两个不同的代理模型（DeepEyes 和 Thyme）上验证，证明了框架的通用性。对比了准确率与速度的 Pareto 前沿，结果客观且具有说服力。

#### 6. 主要结论与发现
*   **显著加速**：SpecEyes 实现了 **1.1x 至 3.35x** 的端到端加速。
*   **准确率提升**：在加速的同时，平均准确率提升了约 **2.87%**（最高达 **+6.7%**），这表明跳过不必要的复杂工具链反而能减少幻觉。
*   **吞吐量优化**：通过异构并行设计，系统吞吐量随批处理大小增加而显著提升，有效缓解了代理模型的并发瓶颈。

#### 7. 优点与亮点
*   **维度创新**：首次提出“代理级投机”，跳过了整个工具调用循环而非仅仅是 Token。
*   **认知门控设计**：$S_{sep}$ 指标具有**尺度不变性**，解决了 Softmax 概率容易过自信的问题，且无需标签即可自我验证。
*   **系统友好**：异构并行漏斗设计直接解决了代理模型在实际部署中难以批处理的痛点。

#### 8. 不足与局限
*   **任务依赖性**：在极度依赖高分辨率感知的任务（如 HR-Bench 8K）中，加速效果有限（接近 1.0x），因为大部分查询必须回退到 $M_L$。
*   **投机深度单一**：目前 $M_S$ 仅支持 0 深度（完全不用工具）投机。如果 $M_S$ 能支持 1-2 步轻量级工具调用，可能会进一步提升复杂任务的效率。
*   **固定开销**：对于必须使用工具的任务，运行 $M_S$ 的过程变成了纯粹的额外延迟开销。

（完）
