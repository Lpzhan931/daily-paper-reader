---
title: "Beyond Token Eviction: Mixed-Dimension Budget Allocation for Efficient KV Cache Compression"
title_zh: 超越 Token 剔除：用于高效 KV 缓存压缩的混合维度预算分配
authors: "Ruijie Miao, Zhiming Wang, Wang Li, Shiwei Wu, Shufan Liu, Yanbing Jiang, Tong Yang"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20616v1"
tags: ["query:llm"]
score: 10.0
evidence: 混合维度KV缓存压缩
tldr: 针对Transformer推理中KV缓存随长度线性增长导致的内存瓶颈，本文提出MixedDimKV。不同于传统的粗粒度Token剔除（全留或全删），该方法通过更细粒度的混合维度分配来压缩KV缓存，并进一步推出结合多头重要性的MixedDimKV-H。实验证明，该方法在长文本任务中表现优异，仅需极小比例的缓存即可维持高性能，显著提升了长文本推理效率。
selection_source: fresh_fetch
motivation: 现有的KV缓存压缩方法多采用粗粒度的Token剔除，本质上是全维或零维分配，限制了压缩效率和精度。
method: 提出MixedDimKV及其增强版MixedDimKV-H，通过在Token级别和多头级别动态分配不同维度的预算来实现更精细的KV缓存压缩。
result: "在LongBench上仅用6.25%的缓存即达到全注意力性能，并在50K长度的大海捞针测试中以0.26%的缓存保持100%准确率。"
conclusion: 细粒度的维度分配比传统的Token剔除更有效，为长文本大模型的高效部署提供了新思路。
---

## 摘要
键值 (KV) 缓存被广泛用于加速 Transformer 推理，但其内存开销随输入长度线性增长，限制了长上下文部署。现有的 Token 剔除方法通过丢弃重要性较低的 Token 来减少内存，这可以被视为一种粗粒度的降维形式，即为每个 Token 分配零维度或全维度。我们提出了 MixedDimKV，一种混合维度 KV 缓存压缩方法，它在更细粒度的层面上为 Token 分配维度；以及 MixedDimKV-H，它进一步整合了注意力头级别的显著性信息。在长上下文基准测试上的实验表明，MixedDimKV 优于此前不依赖头级别显著性分析的 KV 缓存压缩方法。在配备相同的头级别显著性信息时，MixedDimKV-H 的表现始终优于 HeadKV。值得注意的是，在 LongBench 上，我们的方法仅使用 6.25% 的 KV 缓存即可达到与全注意力相当的性能。此外，在“大海捞针”测试中，我们的方案在 50K 上下文长度下保持了 100% 的准确率，而仅使用了低至 0.26% 的缓存。

## Abstract
Key-value (KV) caching is widely used to accelerate transformer inference, but its memory cost grows linearly with input length, limiting long-context deployment. Existing token eviction methods reduce memory by discarding less important tokens, which can be viewed as a coarse form of dimensionality reduction that assigns each token either zero or full dimension. We propose MixedDimKV, a mixed-dimension KV cache compression method that allocates dimensions to tokens at a more granular level, and MixedDimKV-H, which further integrates head-level importance information. Experiments on long-context benchmarks show that MixedDimKV outperforms prior KV cache compression methods that do not rely on head-level importance profiling. When equipped with the same head-level importance information, MixedDimKV-H consistently outperforms HeadKV. Notably, our approach achieves comparable performance to full attention on LongBench with only 6.25% of the KV cache. Furthermore, in the Needle-in-a-Haystack test, our solution maintains 100% accuracy at a 50K context length while using as little as 0.26% of the cache.

---

## 论文详细总结（自动生成）

### 论文总结：超越 Token 剔除：用于高效 KV 缓存压缩的混合维度预算分配

#### 1. 核心问题与整体含义
随着大语言模型（LLM）支持的上下文长度不断增加，**KV 缓存（KV Cache）** 占用的内存随序列长度线性增长，成为制约长文本推理效率和部署规模的核心瓶颈。
*   **研究动机**：现有的“Token 剔除”（Token Eviction）方法（如 H2O, SnapKV）本质上是一种粗粒度的降维：要么保留全部维度（重要 Token），要么完全丢弃（0 维度，不重要 Token）。
*   **核心问题**：能否在 Token 级别实现更细粒度的维度分配，以在有限的内存预算下最大化保留关键信息？

#### 2. 方法论
论文提出了 **MixedDimKV** 框架，其核心思想是将 KV 缓存压缩视为一个受限优化问题，为每个 Token 动态分配不同的维度预算。

*   **关键技术细节**：
    1.  **按头压缩（Head-wise Compression）**：放弃跨头联合压缩，选择对每个注意力头独立进行主成分分析（PCA）。这样可以减少投影矩阵的内存开销，并支持不同头之间的非均匀预算分配。
    2.  **准确率损失评分（Accuracy Loss Score）**：设计了一种度量指标，量化压缩特定 Token 对注意力输出造成的偏差。该指标综合考虑了 Token 的注意力权重、Value 向量的模长以及降维带来的固有误差。
    3.  **二分搜索分配维度（Bisection Search Allocation）**：将维度分配建模为约束优化问题（最小化总损失，受限于总内存预算）。利用拉格朗日对偶性，通过高效的二分搜索寻找最优的拉格朗日乘子，从而确定每个 Token 的最优维度。
    4.  **MixedDimKV-H 变体**：进一步整合了现有的“头级别重要性”分析技术（如 HeadKV），在不同注意力头之间实现更优的资源倾斜。

#### 3. 实验设计
*   **模型**：Llama-3-8B-Instruct、Llama-3-8B-Instruct-Gradient-1048K、Mistral-7B-Instruct-v0.3。
*   **数据集/场景**：
    *   **LongBench**：涵盖单文档/多文档问答、摘要、少样本学习等综合长文本任务。
    *   **RULER**：测试模型在不同上下文长度下的真实处理能力。
    *   **Needle-in-a-Haystack (NIAH)**：大海捞针测试，验证长程信息检索准确性。
*   **对比方法**：
    *   **Token 剔除类**：H2O, SnapKV, PyramidKV, HeadKV。
    *   **降维类**：Palu（基于低秩分解）。

#### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA A100 80GB GPU** 上进行。
*   **算力细节**：论文未明确给出总的训练或测试时长，但强调了推理效率。在 128K 上下文下，该方法将每 Token 解码延迟降低至全注意力的 **55.18%**，峰值内存占用降低至 **66.44%**。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个主流模型和 3 个大型基准测试上进行了广泛评估，涵盖了从 8K 到 128K（甚至 1048K 模型）的不同上下文长度。
*   **消融实验**：对比了“按头压缩”与“联合头压缩”的效果、不同维度粒度（如 {0%, 100%} vs {0%, 12.5%, 25%, 100%}）的影响。
*   **客观性与公平性**：在对比时，确保了所有方法在相同的等效 KV 缓存预算（如 KV Size = 128/256/512）下进行，并考虑了 MixedDimKV 引入的投影矩阵额外内存开销，实验设计较为严谨客观。

#### 6. 主要结论与发现
*   **性能卓越**：MixedDimKV 在不依赖外部头重要性信息的情况下，显著优于所有传统的 Token 剔除方法。
*   **极高压缩比**：在 LongBench 上，仅需 **6.25%** 的 KV 缓存即可达到全注意力性能；在 50K 长度的大海捞针测试中，仅用 **0.26%** 的缓存即可保持 100% 准确率。
*   **维度分配规律**：实验发现，中间维度（如 12.5% 和 25%）被分配给了约 10%-15% 的 Token，这证明了细粒度分配比简单的“留或删”更能有效利用空间。
*   **效率提升**：相比全注意力，显著降低了推理延迟和显存占用，且预处理（Pre-filling）阶段的额外开销极小。

#### 7. 优点
*   **理论支撑**：通过数学证明展示了离散优化问题的对偶间隙在长文本场景下几乎可以忽略，保证了算法的近优性。
*   **灵活性**：MixedDimKV 是一个通用框架，既可以独立运行，也可以作为插件与现有的头级别优化方法（如 HeadKV）结合。
*   **动态适配**：不同于静态降维，它能根据当前输入的 Query 动态调整每个 Token 的保留精度。

#### 8. 不足与局限
*   **投影矩阵开销**：虽然按头压缩降低了开销，但仍需存储额外的投影矩阵，在极小预算下这部分占比会上升。
*   **依赖局部窗口**：算法依赖于观察最近的 $\alpha$ 个 Token 来计算评分，如果关键信息完全不在观察窗口内，评分准确性可能受影响。
*   **硬件实现复杂性**：为了实现高效推理，需要对 FlashAttention 等内核进行定制化修改以支持混合维度计算，这增加了工程实现的难度。

（完）
