---
title: "GradMem: Learning to Write Context into Memory with Test-Time Gradient Descent"
title_zh: GradMem：利用测试时梯度下降学习将上下文写入记忆
authors: "Yuri Kuratov, Matvey Kairov, Aydar Bulatov, Ivan Rodkin, Mikhail Burtsev"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13875v1"
tags: ["query:llm"]
score: 9.0
evidence: 压缩内存作为大型KV缓存的替代方案
tldr: 针对大语言模型处理长上下文时KV缓存内存开销大的问题，本文提出GradMem。该方法通过在推理时对少量前缀记忆令牌进行梯度下降优化，将上下文信息压缩写入记忆。相比传统的单向前向传播写入，GradMem利用自监督重构损失进行迭代误差修正，在保持模型权重不变的情况下，显著提升了记忆容量和检索性能，并在多项自然语言任务中表现出色。
selection_source: fresh_fetch
motivation: 传统的KV缓存机制内存开销巨大，而现有的压缩记忆方法多为单向前向写入，缺乏迭代修正能力。
method: 提出GradMem，在冻结模型权重的前提下，通过对前缀记忆令牌执行测试时梯度下降，优化自监督上下文重构损失。
result: 在键值检索任务中优于同等规模的前向写入方法，且在bAbI和SQuAD等自然语言任务上展现出极具竞争力的性能。
conclusion: GradMem证明了通过测试时优化进行损失驱动的记忆写入，能比前向方法更有效地压缩和存储长上下文信息。
---

## 摘要
许多大语言模型应用需要基于长上下文进行条件生成。Transformer 通常通过存储庞大的逐层 KV 缓存（KV-cache）来支持这一功能，但这会带来显著的内存开销。一种理想的替代方案是压缩记忆：一次性读取上下文，将其存储在紧凑的状态中，并基于该状态回答多个查询。我们在“上下文移除”设置下研究这一问题，即模型在推理时必须在无法访问原始上下文的情况下生成答案。我们提出了 GradMem，它通过逐样本的测试时优化将上下文写入记忆。给定上下文后，GradMem 在保持模型权重冻结的同时，对一小组前缀记忆标记（prefix memory tokens）执行若干步梯度下降。与仅前向传播的方法不同，GradMem 显式地优化模型级的自监督上下文重建损失，从而实现一种具备迭代误差校正能力的损失驱动写入操作。在关联键值检索任务中，GradMem 在相同记忆大小下的表现优于仅前向传播的记忆写入器，且增加梯度步数比重复前向写入能更有效地提升容量。我们进一步证明 GradMem 的效能不仅限于合成基准：配合预训练语言模型，它在包括 bAbI 和 SQuAD 变体在内的自然语言任务上取得了具有竞争力的结果，且完全依赖于记忆中编码的信息。

## Abstract
Many large language model applications require conditioning on long contexts. Transformers typically support this by storing a large per-layer KV-cache of past activations, which incurs substantial memory overhead. A desirable alternative is ompressive memory: read a context once, store it in a compact state, and answer many queries from that state. We study this in a context removal setting, where the model must generate an answer without access to the original context at inference time. We introduce GradMem, which writes context into memory via per-sample test-time optimization. Given a context, GradMem performs a few steps of gradient descent on a small set of prefix memory tokens while keeping model weights frozen. GradMem explicitly optimizes a model-level self-supervised context reconstruction loss, resulting in a loss-driven write operation with iterative error correction, unlike forward-only methods. On associative key--value retrieval, GradMem outperforms forward-only memory writers with the same memory size, and additional gradient steps scale capacity much more effectively than repeated forward writes. We further show that GradMem transfers beyond synthetic benchmarks: with pretrained language models, it attains competitive results on natural language tasks including bAbI and SQuAD variants, relying only on information encoded in memory.

---

## 论文详细总结（自动生成）

以下是对论文《GradMem: Learning to Write Context into Memory with Test-Time Gradient Descent》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大型语言模型（LLM）在处理长上下文时，传统的 KV 缓存（KV-cache）机制会产生巨大的内存开销，且难以生成可移植、可复用的上下文表示。
*   **研究动机**：现有的“压缩记忆”方法（如 RMT）多采用单向的前向传播（Forward-only）来编码上下文，这种方式缺乏反馈机制，无法在压缩过程中纠正错误。
*   **整体含义**：本文提出 **GradMem**，将上下文记忆视为一个优化问题。通过在推理阶段对一小组“记忆标记”（Memory Tokens）进行梯度下降，使模型能够以自监督的方式迭代地将上下文信息“写入”紧凑的记忆状态中，从而在不访问原始上下文的情况下回答查询。

### 2. 方法论
*   **核心思想**：将记忆建模为可学习的参数（前缀嵌入），在模型权重冻结的情况下，通过测试时训练（Test-Time Training, TTT）更新这些记忆标记。
*   **关键技术细节**：
    *   **写入阶段（WRITE）**：给定上下文 $C$，从元学习得到的初始状态 $M_0$ 开始，执行 $K$ 步梯度下降（通常 $K \le 5$）。优化目标是**自监督重构损失**（即模型在给定记忆标记时，预测上下文 $C$ 中后续标记的交叉熵损失）。
    *   **读取阶段（READ）**：在推理时移除原始上下文 $C$，仅使用优化后的记忆标记 $\hat{M}$ 和查询 $Q$ 来预测目标 $Y$。
    *   **元学习（Meta-learning）**：为了使模型在极少数梯度步数内就能完成有效写入，研究者通过反向传播穿透整个“写入”优化循环（二阶导数），共同训练模型权重 $\theta$ 和记忆初始化参数 $M_0$。
*   **算法流程**：初始化记忆 $\rightarrow$ 计算重构损失 $\rightarrow$ 梯度更新记忆标记 $\rightarrow$ 使用最终记忆回答问题。

### 3. 实验设计
*   **数据集/场景**：
    *   **关联键值检索（KV-retrieval）**：合成基准，测试在固定大小记忆中能存储多少对 Key-Value。
    *   **自然语言任务**：包括推理任务 **bAbI**（QA1-QA5）、阅读理解 **SQuAD**（短文本变体）以及 **WikiText-103** 语言建模。
*   **Benchmark 与对比方法**：
    *   **上界**：全注意力 Transformer（保留完整 KV 缓存）。
    *   **逐层记忆模型**：Mamba（状态空间模型）、ARMT（关联递归记忆）。
    *   **同等规模前向模型**：RMT（递归记忆 Transformer，仅通过前向传播更新记忆）。

### 4. 资源与算力
*   **硬件支持**：论文在附录中提到使用了 **NVIDIA A100 GPU** 进行实验。
*   **算力优化**：由于 GradMem 需要计算二阶导数（Double Backward），计算开销较大。作者实现了一种高效的注意力机制双向反向传播算法（Fast forward → autograd），在 1024 标记长度下将内存占用从 60GB 降至 30GB。
*   **训练时长**：文中未明确给出总训练小时数，但强调了通过元学习，推理时的写入步数可以压缩到 5 步以内。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成任务到真实自然语言任务的多个维度，并对记忆标记数量、梯度步数 $K$、上下文长度等进行了详细的消融实验。
*   **充分性与公平性**：实验对比了在相同记忆容量（如 8 个向量）下，梯度写入与前向前向写入的差异。结果显示，即使只进行 1 步梯度更新，效果也优于多次重复的前向扫描，证明了实验对比的客观性。

### 6. 主要结论与发现
*   **梯度写入更高效**：在相同记忆大小下，GradMem 的存储容量显著超过前向写入方法（如 RMT）。
*   **计算量与容量的可扩展性**：在推理时增加梯度步数（即使超过训练时的步数），能持续提升记忆容量和任务准确率，形成一种“推理时计算换精度”的机制。
*   **通用性**：该方法不仅适用于合成任务，在预训练模型（如 GPT-2, Pythia）上微调后，也能有效处理自然语言推理和问答。
*   **选择性记忆**：分析发现，GradMem 在优化过程中会优先重构对回答问题更有用的信息（如 KV 对中的 Value），而非死记硬背。

### 7. 优点
*   **迭代纠错**：相比单向编码，基于损失驱动的梯度更新能自动关注模型预测不准的部分，实现精准压缩。
*   **参数高效**：无需微调模型权重，仅更新极少量的记忆标记。
*   **推理灵活性**：允许在推理时根据任务难度动态调整梯度步数，以平衡计算成本和性能。

### 8. 不足与局限
*   **训练成本高**：元学习阶段需要处理二阶梯度，对显存和计算时间的要求远高于普通训练。
*   **推理延迟**：虽然读取阶段很快，但写入阶段（执行梯度下降）比单纯的前向扫描慢，只有在“单次写入、多次查询”的场景下才具有计算优势。
*   **任务目标偏差**：目前主要使用重构损失作为写入目标，这可能并不总是与所有下游任务的最优特征提取完全对齐。

（完）
