---
title: "LMetric: Simple is Better - Multiplication May Be All You Need for LLM Request Scheduling"
title_zh: LMetric：大道至简——乘法或许就是 LLM 请求调度所需的全部
authors: "Dingyan Zhang, Jinbo Han, Kaixi Zhang, Xingda Wei, Sijie Shen, Chenguang Fang, Wenyuan Yu, Jingren Zhou, Rong Chen"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15202v1"
tags: ["query:llm"]
score: 10.0
evidence: 优化LLM请求调度以提升吞吐量和KV缓存局部性
tldr: 本研究针对大语言模型请求调度中KV缓存加速与负载均衡难以兼顾的问题，提出了LMetric策略。该方法摒弃了复杂的线性组合和超参数微调，创新性地采用“新预填充Token数”与“当前批次大小”的乘积作为调度分数。实验证明，LMetric在无需调优的情况下，显著降低了首字延迟（TTFT）和Token间延迟（TPOT），在多种真实场景下表现优于现有主流调度器，为高性能分布式推理提供了简洁高效的方案。
selection_source: fresh_fetch
motivation: 现有调度方法依赖复杂的线性组合和繁琐的超参数微调，难以在KV缓存利用与负载均衡之间取得最优平衡。
method: 提出一种简单的乘法指标LMetric，通过将KV缓存感知的预填充Token数与负载感知的批次大小相乘，实现免调优的双重目标优化。
result: "在真实负载下，LMetric相比vLLM-v1将TTFT降低了高达92%，TPOT降低了21%，性能显著优于现有生产级调度器。"
conclusion: 简单的乘法运算足以高效处理复杂的LLM请求调度，证明了在分布式推理系统中“简单即更好”的设计理念。
---

## 摘要
高质量的 LLM 请求调度需要实现两个关键目标：路由到的实例是否具有 KV 缓存（KV$）以加速请求执行，以及各实例间的负载是否均衡。同时实现这两个目标具有挑战性，因为追求其中一个目标可能会损害另一个。目前的方法采用各种组合器（如线性组合）来计算结合了这两个目标指标的调度分数，这些方法非常复杂，因为它们要么需要针对特定工作负载进行大量的超参数调优，要么需要开发感知模型和硬件的模拟器，且仍可能导致次优性能。在本文中，我们展示了使用两个精心选择的指标（一个用于感知 KV 缓存，即路由到实例后的新预填充 Token 数；另一个用于感知负载均衡，即实例当前的 Batch Size）的简单乘积作为调度分数，可以在无需任何超参数调优的情况下同时很好地实现这两个目标。核心思想是，乘法分数以类似于线性组合的方式考虑了两个目标，并且具有一个优良特性：在比较过程中原始超参数被抵消了，因此我们不需要通过调优来寻找最佳参数。这两个指标是基于我们对 LLM 特性的分析而选择的。广泛的实验表明，在涵盖聊天机器人、API 调用和编程代理的真实工作负载下，与 vLLM-v1 和生产级调度器相比，这种简单的方法可以将 TTFT 降低 92% 和 52%，将 TPOT 降低 21% 和 20%。我们还从数学上推导了乘法可能失效的条件，并发现这些条件在实践中极其罕见，且可以预先检测（并缓解）。

## Abstract
High-quality LLM request scheduling requires achieving two key objectives: whether the routed instance has KV$ to accelerate the request execution and whether the workload is balanced across instances. Achieving both objectives is challenging because pursuing one objective may compromise the other. Current approaches adopt various combinators (e.g., linear combinations) to compute a scheduling score combining indicators for the two objectives, which are complex in that they either require significant workload-specific hyperparameter tuning or model-hardware-aware simulator development, and could still lead to suboptimal performance. In this paper, we show that using a simple multiplication of two carefully chosen indicators-one for KV$-aware (new prefill tokens if routed to an instance) and one for load balancing-aware (current batch size of the instance)-as the scheduling score can simultaneously achieve both objectives well without any hyperparameter tuning. The key idea is that the multiplied score considers both objectives in a manner similar to a linear combination, with a nice property that the original hyperparameters are canceled out during comparison so we don't need tuning to find the best parameters. The two indicators are chosen based on our analysis of LLM characteristics, and our extensive experiments show that this simple approach can reduce TTFT by 92% and 52%, and TPOT by 21% and 20%, compared to vLLM-v1 and a production scheduler on real-world workloads covering chatbots, API calls, and coding agents. We also mathematically derive the conditions under which multiplication may fail, and find that such conditions are extremely rare in practice and can be detected (and mitigated) beforehand.

---

## 论文详细总结（自动生成）

### LMetric 论文深度总结：LLM 请求调度的乘法法则

#### 1. 核心问题与研究动机
*   **核心问题**：在分布式 LLM 推理集群中，如何高效地将请求路由到不同的服务实例，以同时实现 **KV 缓存感知（KV$-aware）**和**负载均衡（Load Balancing）**。
*   **研究背景**：
    *   **KV 缓存**：若请求的前缀已在某实例缓存，可跳过重复计算，降低首字延迟（TTFT）。
    *   **负载均衡**：避免请求堆积在少数实例，降低 Token 间延迟（TPOT）。
*   **现有挑战**：当前主流方法（如线性加权、过滤机制或模拟器预测）要么需要针对不同工作负载进行繁琐的超参数调优，要么依赖复杂的硬件/模型模拟器，且在动态负载下表现往往不是最优。

#### 2. 方法论：LMetric 乘法调度
*   **核心思想**：摒弃复杂的加法权重公式，改用两个关键指标的**乘积**作为调度分数。
*   **关键指标选择**：
    1.  **$P\text{-token}$（新预填充 Token 数）**：请求 Prompt 长度减去该实例已缓存的匹配长度。反映了 KV 缓存的节省程度。
    2.  **$BS$（当前批次大小）**：实例正在处理及排队的请求总数。反映了实例的实时负载（尤其是 Decode 阶段的压力）。
*   **算法流程**：
    *   计算每个实例的得分：$Score = P\text{-token} \times BS$。
    *   将请求路由至得分最低的实例。
*   **数学原理**：乘法形式在比较两个实例时，其隐含的权重因子在比例关系中会被抵消，从而实现“免调优”特性，且能自然地在 KV 命中收益与负载压力之间取得平衡。
*   **失效检测**：论文推导了乘法可能失效的极端情况（如极度倾斜的 KV 热点），并设计了一个两阶段检测器，在检测到异常时回退到纯负载均衡策略。

#### 3. 实验设计与 Benchmark
*   **实验场景**：涵盖了四种真实的 LLM 应用负载：
    *   **ChatBot (Qwen)**：通用聊天机器人。
    *   **API (Qwen)**：API 调用。
    *   **Coder**：阿里巴巴内部编程助手（2025年11月数据）。
    *   **ToolAgent (Kimi)**：智能体工具调用。
*   **对比方法（Baselines）**：
    *   **vLLM-v1**：仅关注负载均衡（JSQ 变体）。
    *   **Bailian**：某大型云厂商生产环境使用的线性组合调度器。
    *   **AI-Dynamo**：NVIDIA 提出的线性加权调度器。
    *   **llm-d**：基于模拟器（Vidur）预测延迟的调度器。
*   **模型覆盖**：包括 Dense 模型（Qwen2-7B）和 MoE 模型（Qwen3-30B）。

#### 4. 资源与算力
*   **硬件环境**：
    *   **GPU**：16 台 NVIDIA H20 (96GB HBM)。
    *   **CPU**：160 核 Intel Xeon 服务器，1TB DRAM（用于运行全局调度器）。
*   **软件实现**：使用 **Rust** 编写了高性能的调度框架，以消除 Python 性能瓶颈对实验结果的干扰。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 4 种不同类型的工作负载进行了详尽测试。
    *   测试了不同的请求到达率（从低负载到接近饱和）。
    *   进行了消融实验，验证了指标选择（如 $P\text{-token}$ vs 命中率，$BS$ vs 总 Token 数）的合理性。
    *   对比了模拟器在“调优良好”与“未调优”情况下的性能差异。
*   **客观性**：所有基准测试均在统一的 Rust 框架下重新实现，确保了对比的公平性。

#### 6. 主要结论与发现
*   **性能提升**：相比 vLLM-v1，LMetric 将 TTFT 降低了 **92%**，TPOT 降低了 **21%**；相比经过精细调优的生产级调度器，TTFT 仍能降低 **52%**。
*   **免调优优势**：LMetric 在所有测试负载下均表现最优，无需针对特定任务修改任何参数。
*   **指标敏感性**：实验证明 $P\text{-token}$ 比“缓存命中率”更能反映预填充阶段的真实开销；$BS$ 比“总 Token 数”更能准确衡量 Decode 阶段的排队压力。

#### 7. 优点
*   **大道至简**：用极简的乘法逻辑解决了复杂的双目标优化问题。
*   **工程友好**：无需维护复杂的模拟器，不依赖模型架构或硬件参数，易于在生产环境部署。
*   **高性能实现**：基于 Rust 的调度器能够处理极高的并发请求，调度延迟极低。

#### 8. 不足与局限
*   **架构限制**：研究主要基于 **PD-colocation**（预填充与解码共存）架构，未深入探讨在 PD 分离（Disaggregated）架构下的表现。
*   **极端倾斜风险**：虽然论文证明了失效情况罕见，但在极端的 KV 热点攻击或极度不均的工作负载下，仍需依赖检测器进行干预。
*   **指标单一性**：目前仅考虑了 Token 数和 Batch Size，未考虑不同请求之间可能存在的计算强度差异（如复杂推理任务）。

（完）
