---
title: "DyLLM: Efficient Diffusion LLM Inference via Saliency-based Token Selection and Partial Attention"
title_zh: DyLLM：通过基于显著性的 Token 选择和部分注意力实现高效扩散大语言模型推理
authors: "Younjoo Lee, Junghoo Lee, Seungkyun Dan, Jaiyoung Park, Jung Ho Ahn"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08026v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过标记选择和部分注意力实现高效扩散LLM推理
tldr: 针对掩码扩散语言模型（MDLM）在迭代去噪过程中计算开销大的问题，本文提出 DyLLM 推理框架。该框架利用扩散步骤间 token 表示的稳定性，通过余弦相似度识别对更新贡献大的“显著 token”，仅对这些 token 进行重计算并复用其余缓存。DyLLM 无需训练，在保持精度的同时，将 LLaDA 等模型的吞吐量提升了高达 9.6 倍。
selection_source: fresh_fetch
motivation: 掩码扩散语言模型在每一步迭代中都处理全量序列，导致计算资源浪费，且观察发现多数 token 在步骤间保持稳定。
method: 提出一种基于显著性的 token 选择与部分注意力机制，通过测量相邻步骤间注意力上下文的余弦相似度来识别并仅更新关键 token。
result: 在推理和代码生成任务中，DyLLM 在基本不损失精度的情况下，实现了最高 9.6 倍的吞吐量提升。
conclusion: DyLLM 证明了利用扩散过程中的时间稀疏性可以显著优化 MDLM 的推理效率，且无需额外训练。
---

## 摘要
掩码扩散语言模型 (MDLM) 支持并行 Token 解码，为自回归生成的序列化特性提供了一种极具前景的替代方案。然而，其迭代去噪过程的计算开销仍然很大，因为它在每一步都会重复处理整个序列。我们观察到，在这些扩散步骤中，大多数 Token 的表示保持稳定；只有一小部分（我们称之为显著 Token）对下一次更新有实质性贡献。利用这种时间稀疏性，我们提出了 DyLLM，这是一个无需训练的推理框架，通过选择性地仅计算这些显著 Token 来加速解码。DyLLM 通过测量相邻去噪步骤之间注意力上下文的余弦相似度来识别显著性。它仅针对显著 Token 重新计算前馈和注意力操作，而对剩余部分则重用缓存的激活值。在多种推理和代码生成基准测试中，DyLLM 实现了高达 9.6 倍的吞吐量提升，同时在很大程度上保留了 LLaDA 和 Dream 等最先进模型的基准准确度。

## Abstract
Masked Diffusion Language Models (MDLMs) enable parallel token decoding, providing a promising alternative to the sequential nature of autoregressive generation. However, their iterative denoising process remains computationally expensive because it repeatedly processes the entire sequence at every step. We observe that across these diffusion steps, most token representations remain stable; only a small subset, which we term salient tokens, contributes meaningfully to the next update. Leveraging this temporal sparsity, we present DyLLM, a training-free inference framework that accelerates decoding by selectively computing only these salient tokens. DyLLM identifies saliency by measuring the cosine similarity of attention contexts between adjacent denoising steps. It recomputes feed-forward and attention operations only for salient tokens while reusing cached activations for the remainder. Across diverse reasoning and code-generation benchmarks, DyLLM achieves up to 9.6x higher throughput while largely preserving the baseline accuracy of state-of-the-art models like LLaDA and Dream.

---

## 论文详细总结（自动生成）

这是一份关于论文《DyLLM: Efficient Diffusion LLM Inference via Saliency-based Token Selection and Partial Attention》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：掩码扩散语言模型（MDLM，如 LLaDA、Dream）通过并行解码打破了自回归模型（ARLM）逐个生成 Token 的限制，具有极高的吞吐量潜力。
*   **核心问题**：MDLM 的推理过程是迭代去噪的，每一步都需要处理整个序列（包括 Prompt 和已生成的 Response），这导致了巨大的计算冗余。与 ARLM 可以利用 KV Cache 仅计算新 Token 不同，MDLM 的双向注意力机制使得每一步都像是在进行“重复的 Prefill”，计算开销（尤其是 FFN 层）极高。
*   **核心观察**：研究者发现，在相邻的扩散步骤之间，大部分 Token 的表示是非常稳定的，只有极少数关键 Token（显著 Token，Salient Tokens）发生了实质性的语义变化。

### 2. 方法论：核心思想与关键技术
DyLLM 是一个**无需训练（Training-free）**的推理框架，其核心思想是利用时间稀疏性，仅对发生显著变化的 Token 进行重计算。
*   **层自适应显著性机制（Layer-Adaptive Saliency）**：
    *   通过计算相邻去噪步骤 $t$ 和 $t-1$ 之间注意力上下文向量的**余弦相似度**来识别显著 Token。
    *   设定阈值 $\tau$，若相似度低于 $\tau$，则判定为显著 Token。
    *   理论证明（Proposition 3.1 & 3.2）：证明了余弦相似度可以作为 FFN 输入误差的有效界限，且具有缩放不变性。
*   **显著性感知近似注意力（Saliency-Aware Approximate Attention）**：
    *   **显著路径**：对显著 Token 进行完整的行更新（计算完整的注意力分数）。
    *   **非显著路径**：利用列稀疏性，仅根据显著 Token 的变化量（$\Delta V$）来近似更新非显著 Token 的上下文，将复杂度从 $O(N^2d)$ 降低到 $O(N \cdot |A|d)$，其中 $|A|$ 是显著 Token 的数量。
*   **仅响应步骤（Response-only Step）**：
    *   观察到显著 Token 主要集中在 Response 部分，因此在大多数步骤中仅处理 Response Token，每隔固定步数（如 4 步）才与 Prompt 进行一次全量交互。

### 3. 实验设计
*   **模型对象**：LLaDA 8B Instruct 和 Dream 7B Instruct（代表了当前最先进的开源扩散 LLM）。
*   **数据集/场景**：
    *   数学推理：GSM8K、MATH。
    *   通用知识：MMLU-pro。
    *   代码生成：MBPP。
*   **对比方法（Baselines）**：
    *   原始模型实现（Original Implementation）。
    *   **Fast-dLLM**（基于块的缓存策略）。
    *   **dLLM-Cache**（基于激活相似度的自适应缓存）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在单张 **NVIDIA H100 PCIe 80GB GPU** 上完成。
*   **训练时长**：由于该方法是**无需训练**的推理优化，因此不涉及训练时长。
*   **推理配置**：使用了 `lm-eval` 库进行确定性评估，Batch Size 设为 16，并行解码度 $nu$ 取值范围为 1 到 4。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流基准测试上进行了全面测试，涵盖了精度和吞吐量两个维度。
*   **消融实验**：
    *   测试了不同阈值 $\tau$ 对精度和速度的影响。
    *   分析了不同层中显著 Token 的分布情况。
    *   评估了并行解码度 $nu$ 增加时的扩展性。
*   **客观性与公平性**：实验对比了多种最新的加速框架，并使用了相同的硬件环境和模型权重。作者特意关闭了“置信度感知解码”等采样层面的优化，以纯粹对比框架层面的计算效率，实验设计较为严谨。

### 6. 主要结论与发现
*   **大幅提速**：在 LLaDA 上实现最高 **7.6 倍**提速，在 Dream 上实现最高 **9.6 倍**提速。
*   **精度保持**：在大幅提升吞吐量的同时，DyLLM 在多个任务上的准确率与原始模型持平，甚至在某些数学任务（如 MATH）上略有提升（可能是因为抑制了非显著 Token 的噪声）。
*   **扩展性强**：随着并行解码度 $nu$ 的增加，传统加速方法（如 Fast-dLLM）因频繁的“全量刷新”导致效率下降，而 DyLLM 依然能保持高稀疏性，表现出更好的扩展性。
*   **层间差异**：浅层 Token 更稳定，深层 Token 波动更大，验证了层自适应策略的必要性。

### 7. 优点
*   **无需训练**：直接应用于现有的预训练扩散 LLM，部署成本极低。
*   **动态自适应**：相比于 Fast-dLLM 等固定块缓存方法，DyLLM 能根据 Token 的实际语义变化动态调整计算量，更加精细。
*   **理论支撑**：为显著性指标提供了数学证明，增强了方法的可靠性。
*   **显著降低 FFN 开销**：有效解决了 MDLM 推理中 FFN 占比过高（>70%）的瓶颈问题。

### 8. 不足与局限
*   **阈值敏感性**：虽然论文给出了推荐值，但不同模型和任务可能需要微调阈值 $\tau$ 以平衡精度和速度。
*   **硬件实现复杂性**：虽然理论上减少了计算量，但在 GPU 上实现高效的稀疏算子（如处理不规则的显著 Token 索引）可能存在工程挑战，实际加速比受限于算子优化程度。
*   **应用范围**：该方法专为掩码扩散模型设计，无法直接应用于目前主流的自回归模型（如 Llama、GPT 系列）。

（完）
