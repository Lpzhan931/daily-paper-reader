---
title: "SkipOPU: An FPGA-based Overlay Processor for Large Language Models with Dynamically Allocated Computation"
title_zh: SkipOPU：一种面向动态分配计算的大语言模型 FPGA 覆盖处理器
authors: "Zicheng He, Anhao Zhao, Xiaoyu Shen, Chen Wu, Lei He"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14785v1"
tags: ["query:llm"]
score: 9.0
evidence: 基于FPGA的动态LLM计算加速
tldr: 针对大语言模型推理中动态计算分配带来的硬件适配难题，本文提出SkipOPU，一种基于FPGA的覆盖处理器。它通过轻量级路由机制实现灵活的Token和层级跳过，采用非线性模块解耦与融合技术隐藏延迟，并设计了支持浮点-定点混合执行的PE阵列。此外，利用KV缓存的不变性优化了片上存储。实验表明，SkipOPU在带宽效率上显著优于GPU和其他FPGA加速器，并有效降低了KV存储开销。
selection_source: fresh_fetch
motivation: 现有硬件平台主要针对静态执行优化，难以高效支持大语言模型中利用Token和层级不均匀贡献实现的动态计算分配模式。
method: 提出一种FPGA覆盖处理器，通过解耦非线性操作、设计混合精度PE阵列以及构建主动式片上KV历史缓冲区来优化动态推理。
result: "在AMD U280 FPGA上，SkipOPU的带宽效率比GPU和其他FPGA加速器高出1.23至3.83倍，并减少了高达25.4%的KV存储开销。"
conclusion: SkipOPU通过软硬件协同设计，成功解决了动态LLM推理中的计算冗余与访存瓶颈，为高效大模型部署提供了新方案。
---

## 摘要
大语言模型（LLM）在广泛的任务中取得了卓越的性能，但由于参数量的快速增长，其推理效率仍然是一个关键瓶颈。动态计算分配的最新进展通过利用不同 token 和层之间高度不均匀的贡献来应对这一挑战，实现了选择性执行，在保持模型准确性的同时显著减少了冗余计算。然而，现有的硬件平台和加速器主要针对统一、静态的执行进行优化，限制了它们高效支持此类动态推理模式的能力。在这项工作中，我们提出了 SkipOPU，这是一种基于 FPGA 的覆盖处理器，通过轻量级路由机制在 token 和层之间灵活地动态分配计算。首先，我们将非线性模块中的归约操作与逐元素计算解耦，并进行增量归约，从而使这两个阶段能够与相邻的线性操作（路由器或矩阵乘法）融合，实现有效的延迟隐藏。其次，受激活值和权重对数值精度敏感性不对称的启发，我们设计了一个高效支持浮点-定点混合执行的 PE 阵列。引入了一种新颖的 DSP 过载封装技术，以在最小化资源开销的同时最大化硬件利用率。最后，我们开发了一种主动式片上 KV 历史缓冲区，利用被剪枝 token 的跨层 KV 不变性，消除了解码过程中的不规则 HBM 访问，并通过高局部性的片上重用补充了片外带宽。实验结果表明，在 AMD U280 FPGA 上，SkipOPU 在动态计算分配的 LLM 推理带宽效率方面优于 GPU 和其他基于 FPGA 的加速器 1.23 倍至 3.83 倍，并且在不同序列长度下可减少高达 25.4% 的 KV 存储开销。

## Abstract
Large language models (LLMs) have achieved remarkable performance across a wide range of tasks, but their inference efficiency remains a critical bottleneck due to rapidly growing parameters. Recent advances in dynamic computation allocation address this challenge by exploiting the highly uneven contributions of different tokens and layers, enabling selective execution that significantly reduces redundant computation while preserving model accuracy. However, existing hardware platforms and accelerators are primarily optimized for uniform, static execution, limiting their ability to efficiently support such dynamic inference patterns. In this work, we propose SkipOPU, an FPGA-based overlay processor that dynamically allocates computation across tokens and layers with high flexibility through a lightweight routing mechanism. First, we decouple reduction operations from element-wise computation in nonlinear modules and perform reductions incrementally, which enables both stages to be fused with adjacent linear operations (router or matrix multiplication) for effective latency hiding. Second, motivated by asymmetric sensitivity to numerical precision between activation and weight, we design a PE array that efficiently supports float-fixed hybrid execution. A novel DSP overpacking technique is introduced to maximize hardware utilization while minimizing resource overhead. Finally, we develop a proactive on-chip KV history buffer that exploits cross-layer KV invariance of pruned tokens, eliminating irregular HBM accesses during decoding and supplementing off-chip bandwidth through high-locality on-chip reuse. Experimental results demonstrate that SkipOPU on an AMD U280 FPGA outperforms GPU and other FPGA-based accelerators by 1.23x-3.83x in bandwidth efficiency for LLMs inference with dynamic computation allocation and can reduce up to 25.4% KV storage overhead across varying sequence lengths.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SkipOPU** 的 FPGA 覆盖处理器（Overlay Processor），专门针对具有动态计算分配（如 Token 和层级跳过）的大语言模型（LLM）推理进行优化。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然动态计算分配技术（如 SkipGPT）能通过跳过不重要的 Token 或模型层来显著减少理论计算量，但现有的硬件（GPU 和传统 FPGA 加速器）主要针对静态、均匀的计算模式设计。
*   **研究动机**：
    *   **非线性操作瓶颈**：路由（Router）和非线性层（如 Softmax, RMSNorm）引入了复杂的依赖，导致流水线停顿。
    *   **硬件利用率低**：现有的 DSP 单元在处理混合精度（如 FP16 激活与 INT4 权重）时，无法充分发挥其算力。
    *   **访存不规则**：动态跳过层会导致 KV 缓存（KV-Cache）的跨层重用，产生极其不规则的 HBM（高带宽内存）访问模式，严重降低有效带宽。

### 2. 方法论
SkipOPU 提出了一套软硬件协同的设计方案：
*   **深度融合数据flow（Fused Dataflow）**：
    *   将非线性模块（如 RMSNorm 和 Softmax）的归约操作（求和、求最大值）与元素级计算解耦。
    *   利用增量计算，将归约阶段与前后的线性操作（如矩阵乘法）融合，从而在流水线中隐藏非线性层的延迟。
*   **混合精度 PE 阵列与 DSP 过载封装（Overpacking）**：
    *   **核心思想**：通过截断和符号位处理，在一个 DSP48E2 单元中封装两个 FP16 尾数乘法。
    *   **BFP 累加**：采用块浮点（Block Floating Point）累加树，将昂贵的浮点加法转为轻量级的定点加法，仅在最后进行一次浮点转换，大幅节省资源。
*   **主动式片上 KV 历史缓冲区**：
    *   **KV 不变性利用**：观察到被跳过的 Token 其 KV 值在层间保持不变。
    *   **硬件实现**：在片上 URAM 中构建缓冲区存储这些不变的 KV 条目。在解码阶段，直接从片上读取重用的 KV，而从 HBM 读取新生成的 KV，将不规则的片外访问转化为高局部性的片上重用。

### 3. 实验设计
*   **模型与数据集**：使用 Llama2-7B 和 13B 模型，应用 SkipGPT 剪枝策略（约 25% 的跳过率）。
*   **量化方案**：权重采用 4-bit 对称量化（GPTQ），激活值和 KV 缓存保持 FP16。
*   **Benchmark（对比对象）**：
    *   **GPU**：使用 vLLM 运行在 NVIDIA A100 上。
    *   **FPGA 加速器**：对比了 FlightLLM、ChatOPU、MCoreOPU、DFX 等最先进的 FPGA 推理方案。
*   **场景**：涵盖 Prefill（预填充）阶段和 Decode（解码）阶段，序列长度从 128 到 1024 不等。

### 4. 资源与算力
*   **硬件平台**：AMD Alveo U280 FPGA。
*   **频率**：核心逻辑运行在 225 MHz，HBM 接口运行在 450 MHz。
*   **资源消耗**：
    *   **LUT**：1,288,673 (98.85%) —— 资源利用率极高。
    *   **DSP**：4,618 (51.17%)。
    *   **URAM**：512 (53.33%)。
*   **算力表现**：在 Llama2-7B 上达到约 144.5 Token/s 的归一化吞吐量。

### 5. 实验数量与充分性
*   **实验维度**：论文进行了多项消融实验，包括数据流优化对比、不同 KV 映射策略的带宽测试、PE 阵列的精度与资源权衡实验。
*   **充分性**：实验覆盖了从算子级（PE 精度）到系统级（端到端吞吐量）的各个层面。对比了不同序列长度下的表现，验证了 KV 缓冲区在长文本下的有效性。实验设计客观，对比基准均为目前工业界或学术界的主流方案。

### 6. 主要结论
*   **带宽效率**：SkipOPU 的有效带宽利用率达到 89% 以上，在动态计算场景下比 GPU 和其他 FPGA 加速器高出 **1.23x 至 3.83x**。
*   **存储优化**：通过 KV 重用策略，KV 缓存的存储开销降低了高达 **25.4%**。
*   **延迟隐藏**：成功将非线性操作的延迟完全隐藏在线性计算流水线中。

### 7. 优点
*   **创新的 DSP 利用**：提出的 DSP 过载封装技术显著提升了 FPGA 处理混合精度 LLM 的算力密度。
*   **针对性解决访存瓶颈**：KV 不变性缓冲区巧妙地解决了动态模型在硬件上最难处理的“不规则访存”问题。
*   **软硬协同**：不仅有硬件架构创新，还配合了专门的编译器和数据流调度算法。

### 8. 不足与局限
*   **资源极度紧张**：LUT 利用率接近 100%（98.85%），这可能导致布线拥塞（Routing Congestion），在更高频率或更大规模设计中难以扩展。
*   **模型依赖性**：该设计高度依赖于类似 SkipGPT 的动态跳过框架，对于其他类型的动态模型（如 MoE 或非结构化稀疏）的通用性有待验证。
*   **单卡限制**：目前仅在单张 U280 上验证，对于超大规模模型（如 70B 以上）需要多卡互联的支持，论文未深入讨论多卡扩展性。

（完）
