---
title: Unified Spatio-Temporal Token Scoring for Efficient Video VLMs
title_zh: 针对高效视频视觉语言模型的统一时空 Token 评分
authors: "Jianrui Zhang, Yue Yang, Rohun Tripathi, Winson Han, Ranjay Krishna, Christopher Clark, Yong Jae Lee, Sangho Lee"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18004v1"
tags: ["query:llm"]
score: 8.0
evidence: 跨ViT和LLM的令牌剪枝以提高效率
tldr: 针对视频视觉语言模型（VLM）中时空冗余导致的计算开销问题，本文提出了一种统一的时空标记评分（STTS）模块。该模块无需文本条件或标记合并，即可在ViT和LLM中同步进行视觉标记剪枝。通过辅助损失学习时间评分并利用LLM梯度进行空间评分，STTS在保持性能基本不变的情况下，显著提升了长短视频问答任务的训练和推理效率。
selection_source: fresh_fetch
motivation: 现有的视频VLM剪枝方法通常仅局限于ViT或LLM内部，且往往依赖复杂的文本条件，难以兼顾效率与多模态任务的适配性。
method: 提出STTS模块，通过辅助损失进行时间维度评分，并结合LLM下游梯度进行空间维度评分，实现跨架构的视觉标记剪枝。
result: "在13个视频问答任务中，STTS剪枝了50%的视觉标记，使训练和推理效率提升了62%，而平均性能仅下降0.7%。"
conclusion: STTS是一种简单且有效的统一剪枝技术，能够显著降低视频VLM的计算成本，尤其适用于长视频理解任务。
---

## 摘要
Token 剪枝对于提高视觉语言模型 (VLMs) 的计算效率至关重要，特别是在时间冗余普遍存在的视频任务中。先前的方法通常要么 (1) 仅在视觉 Transformer (ViT) 内部针对动作识别和物体分割等单模态感知任务进行 Token 剪枝，而不适应下游视觉语言任务；要么 (2) 仅在大语言模型 (LLM) 内部进行剪枝，同时保持 ViT 输出完整，这通常需要复杂的文本条件 Token 选择机制。在本文中，我们引入了时空 Token 评分 (STTS)，这是一个简单且轻量级的模块，它可以在不依赖文本条件或 Token 合并的情况下，同时在 ViT 和 LLM 中剪枝视觉 Token，并且完全兼容端到端训练。通过辅助损失学习时间评分，并通过 LLM 下游梯度学习空间评分，在高效打包算法的辅助下，STTS 在整个架构中剪枝了 50% 的视觉 Token，在 13 个长短视频问答 (QA) 任务中，使训练和推理效率提高了 62%，而平均性能仅下降了 0.7%。随着每个视频采样帧数的增加，效率提升更加显著。对长视频问答应用测试时缩放 (test-time scaling) 进一步使性能比基线提高了 0.5-1%。总之，STTS 代表了一种新颖、简单且有效的技术，用于统一的、全架构范围的视觉 Token 剪枝。

## Abstract
Token pruning is essential for enhancing the computational efficiency of vision-language models (VLMs), particularly for video-based tasks where temporal redundancy is prevalent. Prior approaches typically prune tokens either (1) within the vision transformer (ViT) exclusively for unimodal perception tasks such as action recognition and object segmentation, without adapting to downstream vision-language tasks; or (2) only within the LLM while leaving the ViT output intact, often requiring complex text-conditioned token selection mechanisms. In this paper, we introduce Spatio-Temporal Token Scoring (STTS), a simple and lightweight module that prunes vision tokens across both the ViT and the LLM without text conditioning or token merging, and is fully compatible with end-to-end training. By learning how to score temporally via an auxiliary loss and spatially via LLM downstream gradients, aided by our efficient packing algorithm, STTS prunes 50% of vision tokens throughout the entire architecture, resulting in a 62% improvement in efficiency during both training and inference with only a 0.7% drop in average performance across 13 short and long video QA tasks. Efficiency gains increase with more sampled frames per video. Applying test-time scaling for long-video QA further yields performance gains of 0.5-1% compared to the baseline. Overall, STTS represents a novel, simple yet effective technique for unified, architecture-wide vision token pruning.

---

## 论文详细总结（自动生成）

### 论文总结：针对高效视频视觉语言模型的统一时空 Token 评分 (STTS)

#### 1. 核心问题与整体含义（研究动机和背景）
视频视觉语言模型（Video VLMs）在处理长视频时面临巨大的计算挑战。由于视频包含大量帧，每帧又被分解为数百个 Patch Token，导致序列长度随帧数增加呈二次方增长，严重消耗显存并增加延迟。
*   **现有局限**：目前的剪枝方法要么仅在 ViT 内部进行（针对单模态任务，不适配下游多模态推理），要么仅在 LLM 之前进行（ViT 仍需处理全量帧，计算瓶颈依然存在），且往往依赖复杂的文本条件选择机制。
*   **研究目标**：提出一种简单、轻量、无需文本条件的模块，能够同时减少 ViT 和 LLM 的计算负担，实现端到端的效率优化。

#### 2. 论文提出的方法论
STTS（Spatio-Temporal Token Scoring）是一个插入在 ViT 层间的轻量级插件，核心流程如下：
*   **双轴评分机制**：
    *   **空间维度**：通过将评分作为 **Attention Bias（注意力偏置）** 注入到后续 ViT 层中，利用 LLM 的下游任务梯度回传，使模型自动学会识别哪些空间区域对回答问题更重要。
    *   **时间维度**：引入**辅助损失（Auxiliary Loss）**，计算相邻帧对应 Patch 之间的余弦相似度。相似度越高，表示冗余度越高，评分越低。
*   **核心算法与公式**：
    *   **评分器**：由自注意力池化层和 3 层 MLP 组成。
    *   **辅助损失**：使用均方误差（MSE）最小化预测评分与“1 - 余弦相似度”之间的差异，强制模型识别时间冗余。
    *   **打包算法（Packing）**：由于剪枝后各帧保留的 Token 数量不一，论文采用了 **First-Fit Descending（首次适应递减）算法**，将稀疏的 Token 重新打包进致密的张量中，从而在硬件上实现真正的并行加速。

#### 3. 实验设计
*   **数据集与场景**：涵盖了 13 个视频问答（QA）基准测试，分为短视频（如 NextQA, MVBench）和长视频（如 VideoMME, MLVU, LVBench）。
*   **Benchmark 与对比方法**：
    *   **基线模型**：以 Molmo2 (基于 SigLIP-2 ViT 和 Qwen3-4B LLM) 为基础。
    *   **对比对象**：包括 SOTA 模型（Qwen3-VL, InternVL3.5）、随机剪枝（Random）、启发式剪枝（Heuristic）以及 Token 合并方法（ToMe）。
*   **测试时缩放（TTS）**：在推理时，利用剪枝节省的预算增加采样帧数（如从 64 帧增加到 128 帧），验证其在长视频任务中的潜力。

#### 4. 资源与算力
*   **硬件环境**：所有性能分析和训练均在单节点 **8 张 NVIDIA H100 GPU** 上完成。
*   **训练细节**：
    *   在 Molmo2 的视频字幕预训练检查点基础上微调。
    *   训练步数为 6,250 步，Batch Size 为 64（通过序列打包实现有效 Batch Size 128）。
    *   使用了 `torch.compile` 优化静态图执行。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 13 个主流视频 Benchmark 上进行了详尽测试，实验覆盖面广。
*   **消融实验**：
    *   **剪枝层深度 $l$**：测试了在第 0, 1, 2, 3 层后插入 STTS 的效果，证明过早剪枝会损害性能。
    *   **剪枝比例 $k$**：从 30% 到 90% 的梯度测试，展示了性能退化的曲线。
    *   **辅助损失的作用**：对比了有无辅助损失对时间冗余识别的影响。
*   **客观性**：论文不仅对比了性能，还详细记录了训练和推理的吞吐量（Throughput），并提供了 100% 剪枝（纯文本基准）作为参考底线，实验设计严谨、客观。

#### 6. 主要结论与发现
*   **高效性**：在剪枝 50% 视觉 Token 的情况下，训练和推理效率提升了 **62%**，而 13 个任务的平均性能仅下降 **0.7%**。
*   **长视频优势**：通过测试时缩放（TTS），在相同计算预算下处理更多帧，长视频 QA 性能可提升 **0.5-1%**。
*   **语义感知**：可视化分析表明，STTS 能够智能地保留动态前景（如游戏中的角色）并剔除静态背景，比单纯的相似度启发式方法更具语义理解能力。

#### 7. 优点：亮点与创新
*   **架构统一**：打破了以往“只管 ViT”或“只管 LLM”的局限，实现了全流程加速。
*   **无需文本条件**：不依赖 Query-based 选择，降低了模块复杂度和推理延迟，使其更具通用性。
*   **硬件友好**：通过 Packing 算法解决了剪枝后的张量稀疏问题，确保了在现代 GPU 上的实际加速效果。
*   **端到端可微**：通过 Bias 注入机制，使剪枝决策能够直接受下游任务目标驱动。

#### 8. 不足与局限
*   **训练依赖**：虽然模块轻量，但仍需要一定规模的微调才能达到最佳效果，并非完全的“即插即用”（Training-free）。
*   **层深限制**：实验发现剪枝不能太早（需在第 3 层后），这意味着 ViT 的前几层计算开销无法被削减。
*   **图像-视频平衡**：虽然附录提到图像性能未下降，但在极端剪枝比例下，模型在需要极高空间分辨率的图像任务（如 OCR）上的表现可能存在潜在风险。
*   **长视频定义**：实验主要集中在数分钟到一小时的视频，对于数小时甚至更长的超长视频，其内存管理和 Token 累积效应仍需进一步验证。

（完）
