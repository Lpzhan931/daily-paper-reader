---
title: "BEAVER: A Training-Free Hierarchical Prompt Compression Method via Structure-Aware Page Selection"
title_zh: BEAVER：一种通过结构感知页面选择实现的无需训练的分层提示词压缩方法
authors: "Zhengpei Hu, Kai Li, Dapeng Fu, Chang Zeng, Yue Li, Yuanhao Tang, Jianqiang Huang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19635v1"
tags: ["query:llm"]
score: 9.0
evidence: 分层提示压缩以降低推理延迟
tldr: 针对大语言模型长上下文推理延迟高和语义碎片化的问题，本文提出BEAVER，一种无需训练的层级化提示词压缩方法。该方法通过双路径池化将变长上下文映射为页级张量以提升并行度，并结合语义与词法双分支选择及句子平滑技术，在保持话语完整性的同时实现高效压缩。实验表明，BEAVER在保持高保真度的同时，将128k上下文的推理延迟降低了26.4倍，为长文档理解提供了高效的解决方案。
selection_source: fresh_fetch
motivation: 现有的提示词压缩方法面临高昂的训练成本或因过度剪枝导致的语义碎片化问题，限制了长上下文LLM的推理效率。
method: 提出一种无需训练的层级化框架，通过双路径池化进行页面级选择，并结合语义与词法双分支规划器及句子平滑技术来保留上下文结构。
result: 在多个基准测试中达到SOTA性能，尤其在RULER多针检索中表现优异，且在128k上下文中实现了26.4倍的延迟缩减。
conclusion: BEAVER证明了通过结构感知的页面选择可以在不牺牲模型性能的情况下，显著提升长文本处理的吞吐量和效率。
---

## 摘要
大语言模型（LLM）上下文窗口的指数级扩张解锁了长文档理解的能力，但也引入了推理延迟和信息利用方面的严重瓶颈。现有的压缩方法通常由于激进的 token 剪枝而面临高昂的训练成本或语义碎片化问题。在本文中，我们提出了 BEAVER，这是一个新颖的无需训练的框架，它将压缩从线性的 token 移除转变为结构感知的分层选择。BEAVER 通过双路径池化将变长上下文映射为稠密的页面级张量，从而最大化硬件并行性，并通过结合语义和词汇双分支选择与句子平滑的混合规划器来保持话语完整性。在四个长上下文基准测试上的广泛评估表明，BEAVER 达到了与 LongLLMLingua 等最先进（SOTA）方法相当的性能。值得注意的是，在 RULER 基准测试中，BEAVER 在基线方法性能下降的多针检索任务中保持了高保真度。在效率方面，BEAVER 在 128k 上下文中将延迟降低了 26.4 倍，为高吞吐量应用提供了一个可扩展的解决方案。我们的代码可在 https://cslikai.cn/BEAVER/ 获取。

## Abstract
The exponential expansion of context windows in LLMs has unlocked capabilities for long-document understanding but introduced severe bottlenecks in inference latency and information utilization. Existing compression methods often suffer from high training costs or semantic fragmentation due to aggressive token pruning. In this paper, we propose BEAVER, a novel training-free framework that shifts compression from linear token removal to structure-aware hierarchical selection. BEAVER maximizes hardware parallelism by mapping variable-length contexts into dense page-level tensors via dual-path pooling, and preserves discourse integrity through a hybrid planner combining semantic and lexical dual-branch selection with sentence smoothing. Extensive evaluations on four long-context benchmarks demonstrate that BEAVER achieves comparable performance to state-of-the-art (SOTA) methods like LongLLMLingua. Notably, on the RULER benchmark, BEAVER maintains high fidelity in multi-needle retrieval where baselines deteriorate. Regarding efficiency, BEAVER reduces latency by 26.4x on 128k contexts, offering a scalable solution for high-throughput applications. Our code is available at https://cslikai.cn/BEAVER/.

---

## 论文详细总结（自动生成）

以下是对论文《BEAVER: A Training-Free Hierarchical Prompt Compression Method via Structure-Aware Page Selection》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）上下文窗口扩展至百万级，长文档理解能力显著增强，但也带来了两大瓶颈：
*   **计算墙（Computation Wall）：** 自注意力机制的 $O(L^2)$ 复杂度导致推理延迟（尤其是首字延迟）随长度激增。
*   **信息利用率下降：** 模型常出现“迷失中段”（Lost in the Middle）现象，长文本并不等同于高鲁棒性。
*   **现有方法局限：** 现有的提示词压缩方法要么依赖高昂的训练成本（如 LLMLingua-2），要么因激进的 Token 级剪枝导致语义碎片化，破坏了上下文的连贯性。
**BEAVER 的目标：** 提出一种**无需训练**、**硬件友好**且**感知结构**的分层压缩框架，在保持语义完整性的同时大幅提升推理效率。

### 2. 方法论：核心思想与技术细节
BEAVER 将压缩范式从“线性 Token 移除”转变为“结构感知的分层页面选择”，主要包含三个组件：
*   **分段器（Segmenter）：** 利用自然分隔符（如换行符）将变长上下文划分为逻辑段落，并采用贪婪分页策略将其映射为 2D 页面张量（Page Tensor），以便于 GPU 并行处理并保留局部语义边界。
*   **页面编码器（PageEncoder）：** 
    *   **双路径池化：** 结合加权平均池化（捕获全局语义）和最大池化（捕获显著局部特征）。
    *   **上下文自适应 ITF 权重：** 引入“语境逆词频”（In-Context ITF）机制，降低高频无意义 Token 的权重，突出稀有关键词。
*   **查询规划器（QueryPlanner）：**
    *   **混合匹配：** 结合语义相关性（向量相似度）和词法重叠（Token 匹配）进行双分支评分。
    *   **结构化先验（Anchor-Flow-Flash）：** 模仿人类认知，保留**锚点页**（开头元数据）、**流动页**（紧邻查询的上下文）和**闪速页**（高相关性得分页）。
    *   **句子平滑（Sentence Smoothing）：** 将选中的页面扩展至最近的句子边界，确保语法完整性。

### 3. 实验设计
*   **数据集/基准测试：** 涵盖了四大长上下文基准：
    1.  **LongBench：** 综合性长文本理解。
    2.  **ZeroSCROLLS：** 零样本长文本推理。
    3.  **RULER：** 压力测试长上下文（16k-128k），包含多针检索等任务。
    4.  **L-Eval：** 跨领域（法律、金融、小说）的泛化性评估。
*   **对比方法：** 
    *   **无监督统计方法：** Selective-Context, LongLLMLingua。
    *   **有监督/专门学习方法：** LLMLingua, LLMLingua-2, SBERT, OpenAI Embeddings。
*   **下游模型：** 统一使用 `gpt-3.5-turbo-instruct` 进行评估，编码器使用 `Qwen3-8B` 的嵌入层。

### 4. 资源与算力
*   **硬件：** 实验在 **NVIDIA A100 (80GB) GPU** 上进行。
*   **训练时长：** 该方法为 **Training-Free（无需训练）**，因此没有训练算力消耗，这使其在跨模型部署时具有极高的成本优势。
*   **推理效率：** 在 128k 上下文下，BEAVER 实现了 **26.4倍** 的加速，处理时间仅需 1.20秒。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个主流基准上进行了广泛测试，涵盖了单/多文档问答、摘要、代码补全、少样本学习等多种任务。
*   **消融实验：** 针对页面大小（M）、池化方式、ITF 权重、评分分支（语义 vs 词法）、选择策略（Anchor/Flow/Flash）以及句子平滑机制均做了详细的对比实验。
*   **客观性：** 实验采用了严格的 Token 预算控制（如 2000/3000 tokens），并对比了不同参数规模（0.6B 到 32B）的模型，验证了方法的鲁棒性和公平性。

### 6. 主要结论与发现
*   **性能卓越：** BEAVER 在 LongBench 和 RULER 上达到了 SOTA 水平。特别是在 RULER 的多针检索任务中，BEAVER 几乎保持了 100% 的准确率，而其他压缩方法（如 LongLLMLingua）在相同预算下性能大幅崩溃。
*   **高效扩展：** 随着上下文长度增加，BEAVER 的延迟增长曲线远比基线平缓，证明了其分层设计的并行优势。
*   **跨模型稳定性：** 在不同规模的模型（如 Qwen3-0.6B 到 32B）上，BEAVER 的性能保持率（Retention Rate）始终在 84%-98% 之间，远超学习型方法。

### 7. 优点与亮点
*   **无需训练（Plug-and-Play）：** 作为一个即插即用的模块，不需要针对特定模型或任务进行微调，泛化能力强。
*   **结构感知：** 通过页面级选择和句子平滑，有效解决了 Token 级剪枝带来的“语义碎片化”问题。
*   **硬件友好：** 将变长序列转化为固定大小的页面张量，充分利用了 GPU 的并行计算能力。
*   **抗干扰能力：** ITF 权重和混合匹配机制使其在处理包含大量噪声的长文档时，能精准锁定关键证据。

### 8. 不足与局限
*   **粒度权衡：** 页面级（Page-level）选择虽然保护了结构，但相比 Token 级剪枝，其压缩精度略粗，可能会保留少量冗余信息。
*   **多步推理挑战：** 在需要深度多跳推理（Multi-hop reasoning）且证据与查询之间缺乏直接词法/语义重叠的场景下，检索机制可能面临挑战。
*   **超参数依赖：** 虽然无需训练，但某些超参数（如页面大小 M、融合权重 λ）在极端不同的领域之间可能仍需手动微调以达到最优效果。

（完）
