---
title: "The Residual Stream Is All You Need: On the Redundancy of the KV Cache in Transformer Inference"
title_zh: 残差流即你所需：论 Transformer 推理中 KV 缓存的冗余性
authors: "Kaleem Ullah Qasim, Jiashu Zhang, Muhammad Kafeel Shaheen, Razan Alharith, Heying Zhang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19664v1"
tags: ["query:llm"]
score: 10.0
evidence: 证明KV缓存冗余性并基于残差流重构
tldr: "本研究证明了 Transformer 推理中的 KV 缓存是完全冗余的，因为每一层的键和值都可以从残差流中位一致地重构。作者提出了 KV-Direct 方案，通过仅存储每 token 的残差向量而非完整的 KV 对，在保持 100% 输出一致性的同时，将存储开销降低了约 27 倍。实验表明，该方法在多种架构上均有效，且在特定批处理规模下，重构速度比从内存读取缓存快达 5 倍，为长文本推理提供了极高的内存效率。"
selection_source: fresh_fetch
motivation: 旨在解决 Transformer 推理过程中 KV 缓存占用过多内存且现有压缩方法会导致精度损失的问题。
method: 通过数学证明和实验验证残差流是唯一必要的状态信息，并开发了存储残差向量、按需重构 KV 的 KV-Direct 推理框架。
result: KV-Direct 在 4B 参数模型上将每 token 存储从 136 KB 降至 5 KB，且在所有测试模型上实现了与原始输出完全一致的贪婪解码结果。
conclusion: 残差流足以代表 Transformer 的推理状态，利用其冗余性可以实现比传统 KV 缓存更高效、更节省内存的推理模式。
---

## 摘要
键值（KV）缓存被广泛视为 Transformer 推理中的核心状态，大量研究致力于设计压缩、剔除或近似其条目的策略。我们证明了这一状态是完全冗余的：每一层的键和值都是残差流（residual stream）的确定性投影，通过每个 token 的单个残差向量重新计算它们，其重构误差精确为零——并非近似，而是位一致（bit-identically）。我们在来自四个架构家族的六个模型（参数量从 135M 到 4B）上验证了这一点。在每一层进行跨任务残差修补（residual patching）使得修补后的输出分布与原始分布之间的 KL 散度 D_KL = 0，这证实了残差流满足马尔可夫性质，且是唯一携带信息的状态。在所有测试模型上，完全移除缓存并从头重新计算，在贪婪解码下产生的输出与原始输出完全一致。基于这一结果，我们提出了 KV-Direct，这是一种内存受限的推理方案，它通过保存残差向量（在 Gemma 3-4B 上每个 token 仅需 5 KB）而非完整的 KV 对（136 KB）作为检查点，并根据需要重新计算键和值。在超过 20 轮对话中，KV-Direct 将峰值内存维持在 42 MB，而标准缓存则增长超过了 103 MB。与五种剔除基准（H2O、StreamingLLM、SnapKV、TOVA、仅窗口模式）相比，KV-Direct 在任何缓存预算下都能保持 100% 的 token 匹配率，而所有基准方法的匹配率均下降至 5-28%。逐操作延迟分析表明，在中等批处理大小下，重新计算的速度比读取缓存张量快达 5 倍。代码可在 https://github.com/Kaleemullahqasim/KV-Direct 获取。

## Abstract
The key-value (KV) cache is widely treated as essential state in transformer inference, and a large body of work engineers policies to compress, evict, or approximate its entries. We prove that this state is entirely redundant: keys and values at every layer are deterministic projections of the residual stream, and recomputing them from a single residual vector per token incurs exactly zero reconstruction error, not approximately, but bit-identically. We verify this across six models from four architecture families (135M to 4B parameters). Cross-task residual patching at every layer produces D_KL = 0 between patched and original output distributions, confirming that the residual stream satisfies a Markov property and is the sole information-carrying state. Removing the cache entirely and recomputing from scratch yields token-identical output under greedy decoding on all models tested. We build on this result with KV-Direct, a bounded-memory inference scheme that checkpoints residual vectors (5 KB per token on Gemma 3-4B) instead of full KV pairs (136 KB), recomputing keys and values on demand. Over 20 conversation turns, KV-Direct holds peak memory at 42 MB while the standard cache grows past 103 MB. Against five eviction baselines (H2O, StreamingLLM, SnapKV, TOVA, window-only), KV-Direct maintains 100% token match at every cache budget; all baselines degrade to 5-28%. A per-operation latency analysis shows recomputation runs up to 5x faster than reading cached tensors at moderate batch sizes. Code is available at https://github.com/Kaleemullahqasim/KV-Direct.

---

## 论文详细总结（自动生成）

这篇论文对 Transformer 推理中的 KV 缓存（Key-Value Cache）提出了颠覆性的看法，以下是详细的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在 LLM 推理中，KV 缓存是主要的内存瓶颈，随着序列变长，内存占用呈线性增长。现有的优化方法（如剔除、量化、稀疏化）大多假设 KV 缓存包含必须保留或近似的“原始信息”，且往往会引入精度损失。
*   **核心发现**：论文证明了 KV 缓存实际上是**完全冗余**的。在 Pre-norm 架构中，每一层的 K 和 V 都是残差流（Residual Stream）的确定性投影。这意味着只需存储每个 token 的一个残差向量，即可在需要时“位一致”（Bit-identical）地重构出所有层的 K 和 V。

### 2. 方法论
*   **核心思想**：利用残差流的马尔可夫性质（Markov Property），将残差流视为推理状态的唯一真值，而将 KV 缓存视为可随时丢弃并重构的计算快捷方式。
*   **关键技术细节**：
    *   **重构公式**：$V = \text{RMSNorm}(h) W_v$，$K = \text{RoPE}(\text{RMSNorm}(h) W_k, p)$。其中 $h$ 是残差流向量。
    *   **KV-Direct 方案**：这是一种有界内存推理方案。当缓存超出预算时，不再永久丢弃旧 token 的 KV，而是仅保存其残差向量（Checkpointing）。在后续注意力计算时，根据这些残差向量实时重构 K 和 V。
    *   **存储优势**：标准 KV 缓存存储 $2 \times L \times n_{kv} \times d_{head}$ 个参数，而 KV-Direct 仅需存储 $d_{hidden}$ 个参数。对于 Gemma 3-4B，每 token 存储从 136 KB 降至 5 KB（约 27 倍压缩）。

### 3. 实验设计
*   **测试模型**：涵盖 4 个架构家族的 6 个模型（135M 到 4B 参数），包括 SmolLM2、Qwen2.5、Qwen3、DeepSeek-R1-Distill 和 Gemma 3。
*   **评估场景**：
    *   **重构验证**：测量原始 KV 与重构 KV 之间的位误差。
    *   **输出一致性**：在贪婪解码下对比有无缓存的 token 匹配率。
    *   **跨任务残差修补**：通过替换残差流观察 KL 散度变化。
    *   **下游任务**：HellaSwag 准确率和 WikiText-2 困惑度（PPL）。
*   **对比基准（Benchmarks）**：对比了 H2O、StreamingLLM、SnapKV、TOVA 和仅窗口剔除（Window-only）等主流 KV 压缩/剔除方法。

### 4. 资源与算力
*   **硬件环境**：所有实验均在配备 **64 GB 统一内存的 Apple M3 Max** 芯片上运行。
*   **软件框架**：使用 MLX 框架进行推理和测量。
*   **算力说明**：文中未提及模型训练时长（因为是针对推理的研究），但详细记录了推理延迟。例如，在处理 500 个被剔除 token 时，重构速度比从内存读取缓存快 5 倍。

### 5. 实验数量与充分性
*   **实验规模**：论文在不同规模（135M-4B）、不同量化精度（bfloat16 和 4-bit）以及不同注意力机制（全局和滑动窗口）上进行了广泛测试。
*   **充分性评价**：实验设计非常严密。不仅验证了数学上的等价性，还通过 KL 散度（$D_{KL}=0$）证明了信息论意义上的等价性。对比实验涵盖了目前最先进的几种剔除策略，样本量和任务类型足以支撑其结论。

### 6. 主要结论与发现
*   **零误差重构**：在全局注意力层，重构误差为零，输出 token 100% 匹配。
*   **性能超越基准**：在任何缓存预算下，KV-Direct 均保持 100% 匹配率，而所有对比基准（如 H2O）在低预算下匹配率仅为 5-28%。
*   **计算换带宽**：当被剔除的 token 数量超过 50 个时，重新计算 KV 的延迟低于从内存读取缓存的延迟，因为现代硬件的计算能力远超内存带宽。
*   **滑动窗口边界**：发现滑动窗口注意力层是重构的边界，K 的重构需要额外的相对位置信息，而 V 的重构依然是通用的。

### 7. 优点
*   **理论优雅**：将复杂的 KV 缓存管理问题简化为残差流的确定性投影问题。
*   **无损压缩**：这是目前唯一能实现极高压缩比且保证**零精度损失**的方法。
*   **实用性强**：KV-Direct 方案直接解决了长文本推理中的内存爆炸问题，且在特定条件下能提升推理速度。

### 8. 不足与局限
*   **架构限制**：目前主要针对 Pre-norm 架构。虽然这是主流，但对于 LayerNorm 或其他变体架构的适用性尚未完全验证。
*   **模型规模**：实验最高只做到 4B 参数。虽然理论上适用于更大模型，但在超大规模模型（如 70B+）或混合专家模型（MoE）上的实际表现有待观察。
*   **滑动窗口挑战**：在像 Gemma 3 这样大量使用滑动窗口注意力的模型中，简单的重构会失效，需要更复杂的缓存管理。
*   **长序列延迟**：对于极长序列（如 100K+），如果频繁大规模重构，计算开销可能会累积。

（完）
