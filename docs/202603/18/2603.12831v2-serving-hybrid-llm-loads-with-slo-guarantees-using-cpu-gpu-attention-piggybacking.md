---
title: Serving Hybrid LLM Loads with SLO Guarantees Using CPU-GPU Attention Piggybacking
title_zh: 使用 CPU-GPU 注意力捎带机制在保证 SLO 的情况下服务混合大语言模型负载
authors: "Zizhao Mo, Junlin Chen, Huanle Xu, Chengzhong Xu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12831v2"
tags: ["query:llm"]
score: 9.0
evidence: 优化混合LLM负载的吞吐量和延迟
tldr: 针对共享集群中混合部署延迟敏感型（LS）和尽力而为型（BE）大模型服务导致的干扰和资源受限问题，本文提出OmniServe系统。该系统通过“注意力搭便车”（Attention Piggybacking）机制，将BE服务的注意力计算动态卸载至CPU，并结合异步通信和动态批处理策略，在保障LS服务SLO的同时显著提升BE吞吐量。
selection_source: fresh_fetch
motivation: 现有的共享集群资源预留方案粒度过粗，导致LS服务SLO受损且BE服务吞吐量受限。
method: 提出OmniServe系统，利用Attention Piggybacking机制将BE注意力计算卸载到CPU，并采用层级动态批处理优化Dense模块计算。
result: 实验表明，OmniServe将LS服务的SLO达成率提升了1.48倍，同时将BE服务的吞吐量提升了9.85倍。
conclusion: 通过协同利用CPU和GPU资源并优化计算卸载策略，可以有效解决混合LLM负载下的干扰问题并最大化系统效率。
---

## 摘要
如今，服务提供商通常在共享集群中部署多种类型的大语言模型（LLM）服务。虽然服务混部提高了资源利用率，但它为延迟敏感型（LS）服务带来了显著的干扰风险——这些服务对推理延迟有严格的服务水平目标（SLO）要求——并且由于可用内存有限，严重限制了尽力而为型（BE）服务的服务能力。为了应对干扰，现有系统通常依赖于预留余量来限制 BE 资源的消耗。然而，这种方法的粗粒度损害了延迟敏感型服务的 SLO 达标率，并过度限制了尽力而为型服务的生成潜力。在本文中，我们提出了 OmniServe，这是一种新型 LLM 服务系统，它高效地利用 CPU 和 GPU 资源来减轻干扰并提高吞吐量。OmniServe 的核心是注意力捎带（Attention Piggybacking）机制，该机制能实时有效地将 BE 服务的注意力计算卸载到 CPU。该机制还促进了 CPU 和 GPU 流之间的异步通信，防止 GPU 在聚合注意力结果时被阻塞。此外，OmniServe 结合了动态批处理控制策略以适应波动的请求到达，利用逐层批处理促进密集（Dense）模块的计算。实验结果表明，与现有最先进的系统相比，OmniServe 将 LS 服务的 SLO 达成率提高了多达 1.48 倍，同时将 BE 服务的吞吐量提高了多达 9.85 倍。

## Abstract
Nowadays, service providers often deploy multiple types of LLM services within shared clusters. While the service colocation improves resource utilization, it introduces significant interference risks for latency-sensitive (LS) services-which have strict SLO requirements for inference latency-and severely constrain the service capacity of best-effort (BE) services due to limited available memory. To address interference, existing systems typically rely on reserving headroom to constrain BE resource usage. However, this approach's coarse granularity compromises the SLO compliance of the latency-sensitive service and unnecessarily restricts the generation potential of the best effort service.   In this paper, we propose OmniServe, a novel LLM serving system that efficiently harnesses both CPU and GPU resources to mitigate interference and improve throughput. Central to OmniServe is the Attention Piggybacking mechanism, which effectively offloads the Attention computation of BE services to CPUs on the fly. This mechanism also facilitates asynchronous communication between CPU and GPU streams, preventing GPUs from being blocked while aggregating Attention results. Additionally, OmniServe incorporates a dynamic batching control policy to adapt to fluctuating request arrivals, facilitating Dense module computation using layer-wise batching. Experimental results show that OmniServe improves the SLO attainment rate for LS services by up to $1.48\times$ while enhancing BE serving throughput by up to $9.85\times$ compared to state-of-the-art systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Serving Hybrid LLM Loads with SLO Guarantees Using CPU-GPU Attention Piggybacking》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）服务的普及，服务商通常在共享集群中混部两类负载：**延迟敏感型（LS）**（如在线聊天，要求严格的SLO）和**尽力而为型（BE）**（如离线数据处理，无严格延迟要求）。
*   **研究动机**：现有系统（如 Llumnix）主要通过预留 GPU 显存来隔离两类服务，但存在两个核心痛点：
    1.  **干扰严重**：即使预留了显存，BE 请求仍会与 LS 请求竞争 GPU 的计算单元（SM）和内存带宽，导致 LS 服务的延迟波动。
    2.  **资源浪费**：GPU 显存有限限制了 BE 服务的并发量，而集群中大量的 CPU 核心和内存资源在推理过程中处于闲置状态。
*   **核心目标**：在确保 LS 服务 SLO 的前提下，通过协同利用 CPU 和 GPU 资源，最大化 BE 服务的吞吐量。

### 2. 方法论：核心思想与关键技术
论文提出了 **OmniServe** 系统，其核心思想是**计算解耦与异步捎带**。
*   **注意力捎带机制（Attention Piggybacking）**：
    *   **计算卸载**：将 BE 请求中计算密度相对较低但显存占用高的 Attention 模块卸载到 CPU 执行，而将计算密度极高的 Dense 模块（如 MLP、QKV 投影）保留在 GPU 上。
    *   **异步执行**：GPU 流在处理 LS 请求时，不会阻塞等待 CPU 返回 BE 的 Attention 结果。BE 的 Dense 计算会“捎带”在后续的 GPU 迭代中，利用层级批处理（Layer-wise Batching）完成。
*   **精确延迟建模**：
    *   系统对 GPU 的 Attention 和 Dense 模块进行模块化建模。例如，使用线性回归模型预测解码阶段的 Attention 延迟：$f_{DA}(c_{DA}(t), g(t)) = a_{DA} \cdot c_{DA}(t) + h_{DA} \cdot g(t) + b_{DA}$，其中 $c_{DA}$ 是计算负载，$g(t)$ 是请求数量。
    *   针对 Dense 模块，提出了一种基于插值的递归建模算法（Alg 1），以捕捉 GPU 在处理不同输入规模时的“阶梯式”延迟特性。
*   **动态调度策略**：
    *   **准入控制**：实时评估新到的 LS 请求是否会破坏现有请求的 SLO，若无法满足则提前拒绝。
    *   **分块预填充（Chunked Prefill）控制**：动态调整 BE 和 LS 的预填充 Token 数量，确保每一轮迭代的延迟都在 SLO 阈值内。

### 3. 实验设计
*   **数据集/场景**：
    *   **LS 负载**：使用 ShareGPT 数据集模拟真实对话分布，采用泊松到达模式。
    *   **BE 负载**：使用 LongBench-v2（长文本理解）和 DailyMails（摘要生成）数据集。
*   **Benchmark（对比方法）**：
    *   **Baseline A (Llumnix + vLLM-CPU)**：基于显存隔离的 GPU 混部 + 纯 CPU 推理。
    *   **Baseline B (NEO)**：一种现有的 CPU-GPU 协同推理系统，但其将所有 Attention 均放在 CPU。
    *   **Baseline C (Sarathi-Serve)**：仅使用 GPU 的 SLO 优化系统（作为 SLO 达成的上限参考）。
*   **测试模型**：Yi-34B（2路张量并行）和 Llama-2-70B（4路张量并行）。

### 4. 资源与算力
*   **GPU 资源**：1 台服务器，配备 4 张 NVIDIA A100 80GB GPU，通过 PCIe 连接。
*   **CPU 资源**：
    *   GPU 服务器自带的 Intel Xeon Gold 6342 CPU（24核）。
    *   额外 4 台纯 CPU 服务器（同型号 CPU），用于分布式 Attention 计算。
*   **网络**：100 Gbps RoCE（默认）和 10 Gbps LAN（用于带宽敏感性测试）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括不同请求到达率下的端到端性能、不同 SLO 严苛程度下的表现、突发流量（Bursty Load）测试。
*   **消融实验**：涵盖了延迟建模的准确性验证、捎带机制的额外开销分析、准入控制的有效性以及在 Prefill-Decode 分离架构下的兼容性。
*   **充分性评价**：实验设计非常充分且客观。通过对比 10Gbps 和 100Gbps 网络，证明了该方法对带宽的低依赖性；通过不同规模的模型验证了通用性。

### 6. 主要结论与发现
*   **SLO 提升**：在严苛的延迟要求下，OmniServe 将 LS 服务的 SLO 达成率比 Llumnix 提升了高达 **1.48 倍**。
*   **吞吐量提升**：在重负载场景下，BE 服务的吞吐量比现有系统提升了高达 **9.85 倍**。
*   **异步优势**：异步 CPU-GPU 交互成功隐藏了 PCIe 传输延迟，使得 LS 服务的平均每 Token 延迟（TPOT）几乎不受 BE 任务的影响。
*   **扩展性**：随着 CPU 数量增加，BE 吞吐量呈现近乎线性的增长，证明了利用闲置 CPU 资源的巨大潜力。

### 7. 优点（亮点）
*   **创新的异步架构**：打破了传统卸载方案中 GPU 必须同步等待 CPU 结果的限制，通过“捎带”机制实现了真正的计算重叠。
*   **细粒度控制**：从传统的 Token 级批处理细化到层级（Layer-wise）批处理，提供了更灵活的调度空间。
*   **实用性强**：不依赖昂贵的专用硬件（如 AMX），在普通 CPU 集群上即可实现显著收益，且兼容分块预填充和 PD 分离等前沿技术。

### 8. 不足与局限
*   **建模依赖**：系统的调度精度高度依赖于离线 Profile 的准确性，如果硬件环境发生动态剧烈变化（如 CPU 被其他非 LLM 任务占用），模型可能失效。
*   **通信开销**：虽然论文证明了中间张量传输开销较小，但在极大规模模型或极低带宽环境下，频繁的 CPU-GPU 激活值交换仍可能成为瓶颈。
*   **架构假设**：该方法主要针对标准的 Transformer 架构，对于某些非典型架构（如线性注意力机制或高度定制化的非层级模型）的适用性未做讨论。

（完）
