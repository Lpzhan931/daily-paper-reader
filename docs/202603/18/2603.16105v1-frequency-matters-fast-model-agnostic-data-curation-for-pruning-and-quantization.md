---
title: "Frequency Matters: Fast Model-Agnostic Data Curation for Pruning and Quantization"
title_zh: 频率至关重要：用于剪枝与量化的快速模型无关数据筛选
authors: "Francesco Pio Monaco, Elia Cunegatti, Flavio Vella, Giovanni Iacca"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16105v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于剪枝和量化的数据筛选策略
tldr: 本研究针对大语言模型剪枝与量化中的校准数据选择问题，提出了ZipCal。传统方法依赖模型信号且计算成本高，而ZipCal是一种模型无关的策略，通过齐夫定律（Zipfian power laws）最大化词汇多样性来筛选数据。实验表明，ZipCal在多种基准测试中优于随机采样，且在保持下游性能的同时，比基于困惑度的先进方法快约240倍，为大规模模型压缩提供了高效的数据策选方案。
selection_source: fresh_fetch
motivation: 现有的校准数据选择方法要么效果有限，要么在大规模模型和数据集上计算成本过高，亟需一种快速且模型无关的筛选策略。
method: 提出ZipCal策略，利用齐夫定律分析数据的内在词频属性，通过最大化词汇多样性来筛选高质量的校准数据集。
result: ZipCal在剪枝和量化任务中表现优异，性能媲美基于困惑度的先进方法，且处理速度平均提升了约240倍。
conclusion: 词汇多样性是提升校准数据质量的关键，ZipCal为大规模模型的高效压缩提供了一种具有线性复杂度的实用方案。
---

## 摘要
训练后模型压缩对于在保持性能的同时增强大语言模型（LLMs）的可移植性至关重要。尽管已经提出了多种压缩方法，但对于选择最合适的数据集（即所谓的“校准数据”）以确定压缩模型配置的关注较少。校准数据的选择是保持模型在任务内和任务间能力的关键步骤。在这项工作中，我们通过分析数据的内在属性而非特定于模型的信号，解决了为剪枝和量化识别高性能校准集的问题。我们引入了 ZipCal，这是一种基于齐普夫幂律（Zipfian power laws）最大化词汇多样性的模型无关数据筛选策略。实验表明，我们的方法在各种剪枝基准测试中始终优于标准的均匀随机采样。值得注意的是，在下游性能方面，它的表现与依赖模型困惑度（perplexity）的先进方法相当。后者在大规模模型和数据集上成本极高，而 ZipCal 由于其易于处理的线性复杂度，平均速度快了约 240 倍。

## Abstract
Post-training model compression is essential for enhancing the portability of Large Language Models (LLMs) while preserving their performance. While several compression approaches have been proposed, less emphasis has been placed on selecting the most suitable set of data (the so-called \emph{calibration data}) for finding the compressed model configuration. The choice of calibration data is a critical step in preserving model capabilities both intra- and inter-tasks. In this work, we address the challenge of identifying high-performance calibration sets for both pruning and quantization by analyzing intrinsic data properties rather than model-specific signals. We introduce \texttt{\textbf{ZipCal}}, a model-agnostic data curation strategy that maximizes lexical diversity based on Zipfian power laws. Experiments demonstrate that our method consistently outperforms standard uniform random sampling across various pruning benchmarks. Notably, it also performs on par, in terms of downstream performance, with a state-of-the-art method that relies on model perplexity. The latter becomes prohibitively expensive at large-scale models and datasets, while \texttt{\textbf{ZipCal}} is on average $\sim$240$\times$ faster due to its tractable linear complexity\footnote{We make the code and the experiments available at https://anonymous.4open.science/r/zipcal-71CD/.}.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ZipCal** 的新型数据筛选策略，旨在为大语言模型（LLM）的训练后压缩（剪枝与量化）提供高效、模型无关的校准数据选择方案。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在对 LLM 进行剪枝或量化时，需要一小部分“校准数据”来计算权重重要性或量化参数。现有的校准数据选择方法存在两极分化：要么是**随机采样**（效果不佳，容易忽略长尾分布中的关键信息），要么是**模型依赖型方法**（如基于困惑度 PPL 的 COLA，虽然效果好但计算成本极高，在大模型上难以扩展）。
*   **研究动机**：研究者希望找到一种既能保证压缩后模型性能，又具备高扩展性（Scalable）、模型无关性（Model-agnostic）且能处理多领域泛化（Multi-domain）的数据筛选方法。

### 2. 方法论
*   **核心思想**：基于语言学的**齐夫定律（Zipf's Law）**。自然语言中少数词频繁出现，而大多数词处于稀疏的长尾分布。ZipCal 认为，最大化校准集的**词汇多样性**（覆盖长尾词汇）能更有效地触发模型中的激活离群值（Outliers），从而为压缩算法提供更丰富、更具代表性的统计信息。
*   **关键技术细节**：
    *   **单领域采样（ZipCal-SD）**：采用贪心启发式算法。从候选池中迭代选择样本，每次选择能给当前已覆盖词汇表带来最大“边际增益”（即引入最多新唯一词汇）的样本。
    *   **多领域采样（ZipCal-MD）**：采用层次化策略。首先在各领域内运行 ZipCal 提取局部代表池，然后利用轻量级嵌入（Embedding）和 K-centers 聚类算法，从这些池中选出语义分布最广的最终样本集。
*   **算法复杂度**：具有线性复杂度 $O(nk)$（$n$ 为数据集大小，$k$ 为采样数），远优于需要模型前向传播的方法。

### 3. 实验设计
*   **实验场景**：涵盖了非结构化剪枝（Wanda）、结构化剪枝（2SSP）以及量化（GPTQ, AWQ）。
*   **数据集与基准**：
    *   **校准源**：C4, Pile, WikiText, GSM8k, Winogrande, HellaSwag 等。
    *   **评估基准**：使用 LM-Evaluation-Harness 在 MMLU（综合）、GSM8k（数学）、HellaSwag（常识）、ANLI（推理）等多个维度进行零样本或少量样本评估。
*   **对比方法**：标准的**均匀随机采样**（Random Sampling）和目前最先进的（SoTA）模型依赖型方法 **COLA**。

### 4. 资源与算力
*   **算力消耗**：论文提到完成全套实验（涉及多种模型、任务和压缩组合）大约消耗了 **1200 GPU 小时**。
*   **效率对比**：在 70B 参数模型上，COLA 筛选数据需要超过 3 小时，而 ZipCal 仅需 **9 秒**。平均而言，ZipCal 比 COLA 快约 **240 倍**。

### 5. 实验数量与充分性
*   **实验规模**：实验非常充分。测试了 Llama-3.1-8B/70B 和 Gemma-2-9B 等主流模型；涵盖了从 16 到 2048 的不同上下文长度（$w$）和样本数量（$k$）的消融实验。
*   **客观性**：通过在多个互不相关的下游任务上进行交叉验证（任务内 vs 任务间），证明了该方法的稳健性和公平性。

### 6. 主要结论与发现
*   **优于随机**：ZipCal 在所有压缩技术中均显著优于随机采样，尤其在逻辑推理任务（如 ANLI）上提升明显。
*   **媲美 SoTA**：ZipCal 在下游任务性能上与计算昂贵的 COLA 持平，甚至在某些情况下表现更好。
*   **多领域优势**：多领域版本的 ZipCal-MD 能够克服单一领域校准带来的性能偏差，提供更均衡的通用模型性能。
*   **稳定性**：ZipCal 对样本数量和上下文长度的敏感度较低，在极小样本（如 16 个）下也能保持较好性能。

### 7. 优点与亮点
*   **极速且模型无关**：不需要任何模型推理即可完成数据筛选，计算开销几乎可以忽略不计。
*   **理论支撑**：将经典的统计语言学原理（齐夫定律）成功应用于现代 LLM 压缩领域。
*   **通用性强**：一次筛选出的校准集可以复用于不同的模型架构和不同的压缩算法。

### 8. 不足与局限
*   **语言局限性**：实验主要集中在英语数据集上。虽然齐夫定律是跨语言的，但针对形态丰富的语言或非字母文字（如中文），其“清洗和分词”预处理步骤可能需要额外调整。
*   **模型覆盖**：尚未在混合专家模型（MoE）或多模态大模型（MLLM）上进行验证，这些模型可能对数据分布有更特殊的要求。
*   **预处理依赖**：算法效果依赖于对特殊标记的清洗和词汇提取的质量。

（完）
