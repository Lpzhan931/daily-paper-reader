---
title: "VQKV: High-Fidelity and High-Ratio Cache Compression via Vector-Quantization"
title_zh: VQKV：通过矢量量化实现高保真、高压缩比的缓存压缩
authors: "Yixuan Wang, Qingyu Shi, Jiayu Zhou, Dianbo Liu, Ziwei He, Zhouhan Lin"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16435v1"
tags: ["query:llm"]
score: 10.0
evidence: 通过矢量量化进行KV缓存压缩
tldr: "针对大语言模型长上下文导致的KV缓存占用过高问题，本文提出VQKV，一种无需训练的向量量化压缩方法。该方法通过将大量浮点值映射为少量整数索引，在保持高保真度的同时实现了极高的压缩率。在LLaMA3.1-8B上，VQKV实现了82.8%的压缩率，保留了98.6%的性能，并将相同显存下的生成长度提升了4.3倍。"
selection_source: fresh_fetch
motivation: 现有的KV缓存压缩技术如标量量化或低秩近似，难以在实现高压缩比的同时保证模型输出的高保真度。
method: 引入向量量化（VQ）技术，将KV缓存中的高维浮点向量转化为极少量的整数索引进行存储。
result: "在LLaMA3.1-8B上达到82.8%的压缩率，且在LongBench测试中维持了98.6%的基准性能。"
conclusion: VQKV证明了向量量化在无需训练的情况下，能有效解决长文本大模型部署中的显存瓶颈问题。
---

## 摘要
大语言模型（LLM）不断增长的上下文长度增大了键值（KV）缓存，限制了其在资源受限环境中的部署。先前的免训练 KV 缓存压缩方法通常依赖于低秩近似或标量量化，这些方法无法同时实现高压缩比和高重构保真度。我们提出了 VQKV，这是一种新颖的免训练方法，通过引入矢量量化（VQ）来获得高度压缩的 KV 表示，同时保持高模型保真度，从而仅用几个整数索引即可表示数千个浮点值。结果表明，VQKV 在 LLaMA3.1-8B 上实现了 82.8% 的压缩比，同时在 LongBench 上保留了 98.6% 的基准性能，并在相同的内存占用下实现了 4.3 倍的生成长度。

## Abstract
The growing context length of Large Language Models (LLMs) enlarges the Key-Value (KV) cache, limiting deployment in resource-limited environments. Prior training-free approaches for KV cache compression typically rely on low-rank approximation or scalar quantization, which fail to simultaneously achieve high compression ratios and high reconstruction fidelity. We propose VQKV, a novel, training-free method introducing vector quantization (VQ) to obtain highly compressed KV representations while preserving high model fidelity, allowing for the representation of thousands of floating-point values with just a few integer indices. As a result, VQKV achieves an 82.8\% compression ratio on LLaMA3.1-8B while retaining 98.6\% of the baseline performance on LongBench and enabling 4.3x longer generation length on the same memory footprint.

---

## 论文详细总结（自动生成）

这是一份关于论文《VQKV: High-Fidelity and High-Ratio Cache Compression via Vector-Quantization》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）支持的上下文长度不断增加，Key-Value (KV) 缓存的显存占用成为制约长文本推理的主要瓶颈。现有的压缩技术存在明显缺陷：
*   **Token 剔除（如 SnapKV）**：直接丢弃信息，导致不可逆的精度损失。
*   **低秩近似（如 ASVD、Palu）**：在极高压缩比下难以维持重构质量。
*   **标量量化（如 KIVI）**：独立处理每个数值，忽略了高维向量内部的结构相关性。
*   **训练依赖方法（如 MLA）**：需要昂贵的重新训练，通用性差。

**VQKV 的核心动机**是引入**向量量化（Vector Quantization）**技术，在**无需训练模型参数**的前提下，通过将高维向量映射为极少量的整数索引，同时实现极高的压缩比和极高的重构保真度。

### 2. 方法论：核心思想与技术细节
VQKV 的核心是 **RSimVQ（残差简单向量量化）**，其处理流程如下：

*   **核心思想**：将 KV 缓存中的高维浮点向量（如 128 维）视为一个整体，通过多个码本（Codebooks）进行分级逼近。
*   **关键技术细节**：
    1.  **残差量化（Residual VQ）**：使用多个码本。第一个码本寻找最接近的向量并计算残差，第二个码本对残差进行量化，以此类推。这种设计能有效处理 RoPE（旋转位置编码）带来的维度异构性。
    2.  **码本学习**：在推理前，使用约 1000 万个 Token 的数据预先训练码本（仅训练码本，不改动 LLM 参数）。
    3.  **预填充阶段（Prefilling）**：将生成的 KV 向量映射为码本索引（整数），仅存储这些索引。
    4.  **解码阶段（Decoding）**：利用自定义的 **FlashAttention 算子**，在计算注意力时按需从码本中查找并重构 KV 向量。
    5.  **分块量化策略**：为了降低计算开销，采用块状计算和批处理量化，每隔一定步数（$L_{local}$）进行一次集中压缩。

### 3. 实验设计
*   **基础模型**：LLaMA3.1-8B 和 LLaMA3.2-3B。
*   **数据集/Benchmark**：
    *   **LongBench**：评估多任务长文本理解能力。
    *   **Needle-In-A-Haystack (NIAH)**：评估长序列中的信息检索精度。
    *   **RULER**：在 4K 到 32K 不同长度下测试长上下文能力。
*   **对比方法**：包括 SnapKV（Token 剔除）、ASVD/Palu（低秩近似）、KIVI（标量量化）以及全缓存基准（Full-cache）。

### 4. 资源与算力
*   **硬件环境**：实验评估主要在 **NVIDIA A800** GPU 上进行（FP16 精度）。
*   **显存测试**：在单张 **NVIDIA RTX 4090 (48 GB)** 上测试了最大生成长度（注：原文标注为 48GB，可能指代特定专业卡或双卡环境）。
*   **训练开销**：码本训练使用了 OpenWebText 的 0.1% 数据（约 10M tokens），学习率为 0.001，Batch Size 为 65536。由于只需训练码本，该过程非常迅速且开销远小于模型微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流模型系列，并在三个权威长文本 Benchmark 上进行了全面测试。
*   **消融实验**：详细探讨了**码本数量（$N$）**和**码本大小（$S$）**对性能与压缩比的影响，分别针对 Key 和 Value 缓存做了独立分析。
*   **客观性**：通过在相同压缩率（如 80% 左右）下对比多种不同原理的方法，证明了 VQKV 在高压缩比下的领先地位。实验设计较为严谨，对比维度包括精度、压缩率和实际显存占用。

### 6. 主要结论与发现
*   **高性能保留**：在 LLaMA3.1-8B 上实现 **82.8%** 的压缩比，LongBench 性能保留率高达 **98.6%**。
*   **长文本优势**：在 NIAH 测试中保持了 100 分的完美得分，显著优于 ASVD 和 Palu。
*   **显存效率**：在相同硬件条件下，VQKV 支持的生成长度是原始模型的 **4.3 倍**（从 190k 提升至 824k tokens）。
*   **KV 差异性**：实验发现 Value 缓存对码本配置更敏感，而 Key 缓存相对鲁棒，这为差异化压缩提供了依据。

### 7. 优点与亮点
*   **免训练（Training-free）**：无需修改 LLM 权重，即插即用，适配不同 Checkpoint。
*   **高保真度**：相比标量量化，向量量化能更好地捕捉特征间的相关性，重构误差极小。
*   **系统级优化**：开发了自定义 Triton 内核，将 VQ 重构与 FlashAttention 融合，提升了实际运行效率。
*   **突破极限**：在极高压缩比（>80%）下，性能衰减远小于现有的低秩近似方法。

### 8. 不足与局限
*   **解码延迟**：尽管有算子优化，但向量量化的查表和残差求和操作仍会引入额外的计算开销，相比原始模型，推理速度（吞吐量）可能会有一定程度的下降。
*   **码本依赖**：码本需要在代表性数据集上预训练，如果下游任务的数据分布与预训练数据差异极大，可能会影响重构质量。
*   **硬件适配**：目前高度依赖自定义的 Triton/CUDA 内核优化，在非 NVIDIA 硬件上的移植和优化难度较大。

（完）
