---
title: "ForestPrune: High-ratio Visual Token Compression for Video Multimodal Large Language Models via Spatial-Temporal Forest Modeling"
title_zh: ForestPrune：通过时空森林建模实现视频多模态大语言模型的高比例视觉 Token 压缩
authors: "Shaobo Ju, Baiyang Song, Tao Chen, Jiapeng Zhang, Qiong Wu, Chao Chang, HuaiXi Wang, Yiyi Zhou, Rongrong Ji"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22911v1"
tags: ["query:llm"]
score: 8.0
evidence: 针对视频多模态大模型的免训练Token剪枝方法
tldr: "针对视频多模态大模型（MLLM）计算开销大的挑战，本文提出 ForestPrune，一种无需训练的高比例视频令牌剪枝方法。该方法通过构建跨帧的时空森林模型，结合语义、空间和时间约束对令牌进行全局建模，并依据树深度和节点角色进行重要性评估。实验证明，该方法在减少 90% 令牌的情况下仍能保持极高精度，且在性能和效率上均显著优于现有压缩技术。"
selection_source: fresh_fetch
motivation: 现有视频令牌压缩方法因缺乏对视频时空连续性的充分建模，难以在保持性能的同时实现高比例的压缩。
method: 通过构建时空令牌森林并利用树深度与节点角色评估全局重要性，实现了一种无需训练的高效剪枝策略。
result: "在 LLaVA-OneVision 上减少 90% 令牌仍保留 95.8% 的精度，且剪枝速度比 FrameFusion 快 81.4%。"
conclusion: ForestPrune 为视频 MLLM 提供了一种高效、高压缩比且无需重新训练的视觉令牌管理方案。
---

## 摘要
由于能大幅节省计算和内存开销，Token 压缩已成为多模态大语言模型（MLLMs）的研究热点，并在图像-语言任务中取得了显著进展。然而，对于视频而言，现有方法在实现高比例 Token 压缩方面仍力有不逮。我们将这一缺陷归因于对时序和连续视频内容建模的不足，并为视频 MLLMs 提出了一种名为 ForestPrune 的新型免训练 Token 剪枝方法，该方法通过时空森林建模（Spatial-temporal Forest Modeling）实现了有效且高比例的剪枝。在实践中，ForestPrune 根据语义、空间和时间约束在视频帧之间构建 Token 森林，从而实现对视频的全面理解。随后，ForestPrune 根据树深度和节点角色评估 Token 树及节点的重要性，从而获得全局最优的剪枝决策。为了验证 ForestPrune，我们将其应用于两个代表性的视频 MLLMs，即 LLaVA-Video 和 LLaVA-OneVision，并在多个视频基准测试上进行了广泛实验。实验结果不仅展示了其对视频 MLLMs 的巨大效能，例如在 LLaVA-OneVision 上减少 90% Token 的同时保留了 95.8% 的平均准确率，还展示了其优于对比 Token 压缩方法的性能和效率，例如在 LLaVA-Video 上，相比 FrameFusion，其在 MLVU 上的准确率提升了 10.1%，且剪枝时间缩短了 81.4%。

## Abstract
Due to the great saving of computation and memory overhead, token compression has become a research hot-spot for MLLMs and achieved remarkable progress in image-language tasks. However, for the video, existing methods still fall short of high-ratio token compression. We attribute this shortcoming to the insufficient modeling of temporal and continual video content, and propose a novel and training-free token pruning method for video MLLMs, termed ForestPrune, which achieves effective and high-ratio pruning via Spatial-temporal Forest Modeling. In practice, ForestPrune construct token forests across video frames based on the semantic, spatial and temporal constraints, making an overall comprehension of videos. Afterwards, ForestPrune evaluates the importance of token trees and nodes based on tree depth and node roles, thereby obtaining a globally optimal pruning decision. To validate ForestPrune, we apply it to two representative video MLLMs, namely LLaVA-Video and LLaVA-OneVision, and conduct extensive experiments on a bunch of video benchmarks. The experimental results not only show the great effectiveness for video MLLMs, e.g., retaining 95.8% average accuracy while reducing 90% tokens for LLaVA-OneVision, but also show its superior performance and efficiency than the compared token compression methods, e.g., +10.1% accuracy on MLVU and -81.4% pruning time than FrameFusion on LLaVA-Video.

---

## 论文详细总结（自动生成）

以下是对论文《ForestPrune: High-ratio Visual Token Compression for Video Multimodal Large Language Models via Spatial-Temporal Forest Modeling》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着视频多模态大语言模型（Video MLLMs）的发展，处理数十甚至数百帧视频带来的视觉 Token 激增，导致了巨大的计算开销和内存压力。现有的 Token 压缩方法（如剪枝或合并）大多源于图像任务，虽然在低压缩比下表现尚可，但在**高比例压缩（如 90% 以上）**时性能会剧烈下降。
*   **核心痛点**：现有方法缺乏对视频时空连续性的全局建模。仅关注单帧内的重要性，会导致相邻帧之间保留大量语义重复的 Token，造成严重的全局冗余。
*   **研究目标**：提出一种无需训练的剪枝方法，通过建模跨帧的“时空森林”来识别并移除全局冗余信息。

### 2. 方法论：ForestPrune 核心思想与技术细节
ForestPrune 的核心是将视频 Token 视为森林中的节点，通过时空约束构建树结构，并根据树的拓扑属性进行剪枝。
*   **候选节点选择**：首先从每帧中提取代表性 Token 作为潜在的树节点（可利用现有方法或随机采样）。
*   **时空森林构建**：
    *   **语义约束**：计算节点间的余弦相似度，仅连接相似度高于阈值 $\tau_s$ 的节点。
    *   **空间约束**：计算节点在原始图像中的欧几里得距离，仅连接距离小于阈值 $\tau_p$ 的节点，确保局部一致性。
    *   **时间约束**：节点只能连接到后续帧的相似节点，形成具有时间指向性的树。
*   **重要性评估与剪枝**：
    *   **树深度（Tree Depth）**：深度越大的树代表该视觉特征在视频中持续时间越长、越稳定，其根节点和主干节点更重要。
    *   **节点角色**：优先保留根节点（Root）和主干节点（Trunk），优先剪除叶子节点（Leaf）和尾部节点（Tail）。
*   **全局决策**：根据预设的压缩预算，在全视频范围内按优先级递归剪枝，直至达到目标 Token 数量。

### 3. 实验设计
*   **基础模型**：LLaVA-Video-7B 和 LLaVA-OneVision-7B。
*   **数据集/基准测试**：涵盖了 5 个极具竞争力的视频理解榜单：
    *   **NExT-QA**（短视频问答）、**MVBench**（多能力评测）、**VideoMME**（全方位综合评测）、**MLVU**（长视频理解）、**LongVideoBench**（长上下文理解）。
*   **对比方法**：
    *   **图像类方法**：FastV, VisionZip, G-Prune。
    *   **视频类方法**：STTM, FrameFusion。

### 4. 资源与算力
*   **训练开销**：该方法是**免训练（Training-free）**的，不需要额外的 GPU 训练时长。
*   **推理效率**：论文对比了 TFLOPS、GPU 显存峰值和预填充（Pre-filling）时间。
    *   在 LLaVA-Video 上，ForestPrune 的剪枝耗时比 FrameFusion 缩短了 **81.4%**。
    *   在 90% 压缩率下，显著降低了显存占用（从约 35G 降至约 23G）和计算量。
*   **硬件环境**：文中未明确提及具体使用的 GPU 型号数量，但通过效率对比图表展示了其在标准推理环境下的优越性。

### 5. 实验数量与充分性
*   **实验规模**：在 2 个主流模型上针对 5 个数据集进行了 70%、80%、90% 三种压缩梯度的全面测试。
*   **消融实验**：详细探讨了候选节点选择方式（随机 vs 剪枝）、保留比例、以及语义阈值 $\tau_s$ 和空间阈值 $\tau_p$ 对性能的影响。
*   **扩展实验**：验证了在固定 Token 预算下，利用 ForestPrune 增加输入帧数（从 64 帧扩展到 512 帧）能否提升模型性能。
*   **评价**：实验设计客观、公平，对比了多种 SOTA 方法，覆盖了从短视频到超长视频的多种场景，数据支撑充分。

### 6. 主要结论与发现
*   **高保真压缩**：在 LLaVA-OneVision 上，剪掉 90% 的 Token 后仍能保留 **95.8%** 的原始平均准确率。
*   **性能领先**：在 90% 高压缩比下，ForestPrune 在 MLVU 上的表现比 FrameFusion 高出 **10.1%**。
*   **帧数扩展红利**：通过 ForestPrune 压缩 Token，使得模型能够处理更多输入帧，从而在 MLVU 上达到了 **72.5** 的 SOTA 级别表现。
*   **时空建模的必要性**：可视化结果证明，ForestPrune 能有效捕捉场景切换，在画面剧烈变化时自动保留更多 Token，在静态画面中大幅压缩。

### 7. 优点
*   **全局最优性**：不同于逐帧剪枝，它通过森林模型实现了跨帧的全局冗余评估。
*   **即插即用**：无需微调模型，适配性强，计算开销极低。
*   **鲁棒性强**：在极端压缩比例下，性能下降曲线远比现有方法平缓。
*   **效率极高**：预处理速度快，适合实时或长视频推理场景。

### 8. 不足与局限
*   **超参数依赖**：语义和空间阈值（$\tau_s, \tau_p$）对不同模型和视频类型的敏感度可能不同，虽然文中给出了默认值，但在极端多变的视频流中可能需要动态调整。
*   **细粒度损失风险**：虽然保留了全局重要特征，但在极高压缩比下，可能会丢失某些仅在单帧出现的微小、关键视觉细节（如快速闪过的文字）。
*   **应用限制**：目前主要针对基于 Transformer 架构的 MLLMs，对于其他非注意力机制驱动的视觉编码器适配性有待验证。

（完）
