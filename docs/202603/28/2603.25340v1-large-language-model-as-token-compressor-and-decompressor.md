---
title: Large Language Model as Token Compressor and Decompressor
title_zh: 大语言模型作为 Token 压缩器与解压器
authors: "Wenbing Li, Zikai Song, Jielei Zhang, Tianhao Zhao, Junkai Lin, Yiran Wang, Wei Yang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25340v1"
tags: ["query:llm"]
score: 8.0
evidence: 通过Token压缩降低推理成本
tldr: 本研究发现现成的预训练大语言模型（LLM）可作为高效的Token压缩器和解压器。作者提出了一个自表达自编码学习框架，通过轻量化LoRA适配器将长文本转换为紧凑的离散变长潜码（Z-tokens），并能实现精确重构。该方法具有内容自适应性，在保持重构保真度和下游任务性能的同时，在多个数据集上实现了高达18倍的Token压缩率，为长上下文推理提供了高效路径。
selection_source: fresh_fetch
motivation: 旨在利用预训练LLM自身的能力来解决长文本处理中的Token冗余问题，提高计算和存储效率。
method: 提出一种基于LoRA适配器的自编码框架，将文本映射为内容自适应的离散变长潜码（Z-tokens）并进行重构。
result: 在Wikipedia和CNN/DailyMail等多个数据集上实现了高达18倍的Token压缩率，且能保持极高的重构保真度。
conclusion: 该研究证明了LLM作为Token压缩器的巨大潜力，为高效的长上下文推理和Prompt压缩提供了创新的技术方案。
---

## 摘要
在本文中，我们提出了一个新颖的见解，即现成的大语言模型（LLM）可以作为出色的 Token 压缩器和解压器。为了证明这一点，我们设计了一个自表达自编码学习框架，该框架微调预训练的 LLM，将长文本翻译成一种由离散、变长潜码组成的紧凑内部语言（称为 Z-tokens），并从中精确地重建原始文本。由此产生的表示是内容自适应的：通过轻量级的基于 LoRA 的适配器头，语义密集的片段会获得更多的 Z-tokens，而冗余或可预测的区域则会被大幅压缩。实验表明，我们的方法在 Wikipedia、CNN/DailyMail、HotpotQA 和 Qulac 风格的长查询数据集上实现了高达 18 倍的 Token 缩减，同时保持了重建保真度和下游性能。这种简单而有效的设计支持包括提示词压缩和直接在 Z-token 空间中进行自回归生成在内的应用，为实现 Token 高效的长上下文推理提供了一条潜在路径。

## Abstract
In this paper, we establish the novel insight that an off-the-shelf LLM can function as an excellent token compressor and decompressor. To demonstrate, we design a self-expressive autoencoding learning framework fine-tunes a pretrained LLM to translate long texts into a compact internal language of discrete, variable-length latent codes, termed Z-tokens, and to reconstruct the original text exactly from them. The resulting representation is content-adaptive: semantically dense segments receive more Z-tokens, while redundant or predictable regions are aggressively compressed, via lightweight LoRA-based adapter heads. Empirically, our method achieves up to 18 times token reduction on Wikipedia, CNN/DailyMail, HotpotQA, and Qulac-style long-query datasets, while preserving reconstruction fidelity and downstream performance. This simple yet effective design supports applications including prompt compression and autoregressive generation directly in the Z-token space, offering a potential pathway toward token-efficient long-context reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Large Language Model as Token Compressor and Decompressor》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）在处理长上下文时面临注意力机制平方级复杂度的挑战，导致计算和内存开销巨大。
*   **核心问题**：现有的 Token 压缩方法（如启发式裁剪或固定长度嵌入）往往会丢失细粒度信息、缺乏解释性，且无法根据内容密度动态调整压缩率。
*   **整体含义**：本文提出 LLM 本身可以作为高效的压缩器和解压器。通过将长文本“翻译”成一种离散的、变长的内部语言（Z-tokens），模型能在极高的压缩比下保持语义完整性，并支持直接在压缩空间进行推理。

### 2. 方法论
*   **核心思想**：利用预训练 LLM 的语义抽象能力，构建一个自表达的自编码框架。将压缩视为“自然语言到内部抽象语言”的翻译，将解压视为逆向过程。
*   **关键技术细节**：
    *   **Z-tokens 压缩器**：扩展原有的词表，引入专门的 Z-vocabulary。压缩器以自回归方式生成变长的 Z-tokens，能够根据内容复杂度动态分配 Token（简单内容少给，复杂内容多给）。
    *   **离散化训练**：使用 **Gumbel-Softmax** 估计器和直通估计器（Pass-through Estimator）解决离散 Token 采样过程中的梯度回传问题。
    *   **解压器双模式**：
        1.  **作为推理器（Inferencer）**：直接从 Z-tokens 生成下游任务答案（如 QA、摘要）。
        2.  **作为重构器（Reconstructor）**：将 Z-tokens 还原为原始文本，支持在 Z-token 空间进行抽象推理后再还原。
    *   **滑动窗口压缩**：针对超长文本，采用带重叠的滑动窗口，并通过一致性约束（Loverlap）确保语义连贯。
    *   **训练策略**：采用 **LoRA** 进行轻量化微调，并引入**计划采样（Scheduled Sampling）**来缓解自回归生成中的曝光偏差（Exposure Bias）。

### 3. 实验设计
*   **数据集**：
    *   **重构与续写**：Wikipedia。
    *   **长文本问答（Long-text QA）**：HotpotQA、QuALITY、NarrativeQA、QASPER。
    *   **摘要生成**：CNN/DailyMail。
*   **Benchmark 与对比方法**：
    *   对比了 **ICAE**（上下文自编码器）、**Gist-Token**（提示词压缩）、**AutoCompressor** 和 **LLOCO** 等主流学习型压缩方法。
    *   基座模型使用了 Qwen3 系列（0.4B, 1.7B, 4B）。

### 4. 资源与算力
*   **硬件环境**：使用了 6 台 NVIDIA GPU，具体包括 **2 张 A800-80G** 和 **4 张 V100-32G**。
*   **计算配置**：配备 Intel Xeon Gold 6348 CPU。
*   **训练细节**：虽然未明确给出总训练时长，但提到使用了 LoRA 微调（秩 r=128），并在 8k Z-token 规模下进行实验。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 6 个大型数据集，进行了重构、续写、摘要、长文本 QA 四大类任务。
*   **消融实验**：
    *   测试了不同模型规模（0.6B 到 4B）的影响。
    *   研究了 Z-token 数量（1k 到 10k）对性能的饱和效应。
    *   验证了计划采样对自由解码（Free Decoding）鲁棒性的提升。
    *   分析了推理速度与显存占用的实际收益。
*   **充分性评价**：实验设计较为全面，不仅关注准确率（BLEU, F1, ROUGE），还深入探讨了 Z-tokens 的语义一致性和多义性，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **高压缩比**：在 Wikipedia 等数据集上实现了高达 **18 倍**的 Token 缩减，且重构保真度极高（4x 压缩下 BLEU-4 超过 99）。
*   **性能保持**：在长文本 QA 任务中，压缩后的表现接近甚至在某些指标上超过了原始全量 Token 的表现。
*   **效率提升**：推理速度最高提升了 **4.6 倍**（摘要任务中约 2 倍），显存占用显著降低。
*   **Z-token 特性**：发现 Z-tokens 具有“多义性”且受上下文驱动，类似于人类语言中的抽象概念，能够高效编码复杂的语义结构。

### 7. 优点与亮点
*   **变长压缩**：不同于以往固定长度的压缩方法，本文的方法能根据语义密度自适应调整，更符合语言本质。
*   **自表达性**：直接利用 LLM 自身的词表扩展和语义先验，无需引入复杂的外部编码器。
*   **可解释性**：通过语义一致性分析证明了 Z-tokens 是有意义的符号，而非混乱的潜变量。
*   **端到端推理**：支持直接在压缩空间进行“思考”，为解决长上下文推理提供了新范式。

### 8. 不足与局限
*   **微调成本**：虽然使用了 LoRA，但仍需要针对特定模型进行微调训练，无法完全“开箱即用”。
*   **全局协调**：在滑动窗口模式下，如何更好地处理跨窗口的超长程全局依赖仍有待进一步优化。
*   **任务感知**：目前的压缩过程是通用的，未来若能针对特定任务（如只保留事实或只保留逻辑）进行任务感知压缩，效果可能更佳。
*   **模型偏差**：压缩过程可能强化预训练模型原有的偏见或忽略极少数低频但关键的信息。

（完）
