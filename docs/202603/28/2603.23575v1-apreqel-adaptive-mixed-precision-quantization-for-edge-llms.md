---
title: "APreQEL: Adaptive Mixed Precision Quantization For Edge LLMs"
title_zh: APreQEL：面向边缘大语言模型的自适应混合精度量化
authors: "Meriem Bouzouad, Yuan-Hao Chang, Jalil Boukhobza"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23575v1"
tags: ["query:llm"]
score: 9.0
evidence: 自适应混合精度量化，用于高效的边缘LLM部署
tldr: 针对大语言模型在边缘设备部署时面临的高计算和内存挑战，本文提出APreQEL框架。该框架摒弃了传统的统一量化方法，通过分析各层对模型性能的贡献以及在目标硬件上的表现，实现自适应混合精度量化。它能根据用户定义的优先级平衡内存、延迟和准确性，为资源受限环境下的LLM部署提供了更优的配置方案。
selection_source: fresh_fetch
motivation: 传统的统一量化忽略了不同模型层对精度的敏感度差异，且内存消耗与计算吞吐量在硬件上往往并不对等。
method: 通过分析层级贡献和推断不同量化类型在特定硬件上的行为，为每一层分配最合适的量化精度以满足用户定义的优先级。
result: 该方法实现了统一量化无法达到的新配置设计，在资源受限的设备上显著扩展了LLM部署的有效方案空间。
conclusion: APreQEL通过整合层重要性与硬件性能权衡，为在边缘设备上高效部署大语言模型提供了一种灵活且高效的自适应量化机制。
---

## 摘要
如今，大语言模型在推理、代码生成和复杂问题解决等各种任务中展现出了强大的实力。然而，这种进步伴随着高昂的计算成本和内存需求，使得在边缘设备上部署这些模型以确保实时响应和数据隐私变得具有挑战性。量化是减少内存使用的一种常用方法，但大多数方法在所有层中统一应用量化。这忽略了不同层对精度降低的反应可能不同这一事实。重要的是，内存消耗和计算吞吐量并不一定同步，这进一步增加了部署决策的复杂性。本文提出了一种自适应混合精度量化机制，在用户定义的优先级下，平衡边缘部署中的内存、延迟和准确性。通过分析逐层贡献并推断不同量化类型在目标硬件平台上的表现，该机制为每一层分配最合适的量化类型。这种集成确保了设计中能够同时兼顾层重要性和整体性能的权衡。我们的工作开启了统一量化无法实现的新配置设计，扩展了在资源受限设备上高效部署大语言模型的解空间。

## Abstract
Today, large language models have demonstrated their strengths in various tasks ranging from reasoning, code generation, and complex problem solving. However, this advancement comes with a high computational cost and memory requirements, making it challenging to deploy these models on edge devices to ensure real-time responses and data privacy. Quantization is one common approach to reducing memory use, but most methods apply it uniformly across all layers. This does not account for the fact that different layers may respond differently to reduced precision. Importantly, memory consumption and computational throughput are not necessarily aligned, further complicating deployment decisions. This paper proposes an adaptive mixed precision quantization mechanism that balances memory, latency, and accuracy in edge deployment under user-defined priorities. This is achieved by analyzing the layer-wise contribution and by inferring how different quantization types behave across the target hardware platform in order to assign the most suitable quantization type to each layer. This integration ensures that layer importance and the overall performance trade-offs are jointly respected in this design. Our work unlocks new configuration designs that uniform quantization cannot achieve, expanding the solution space to efficiently deploy the LLMs on resource-constrained devices.

---

## 论文详细总结（自动生成）

以下是对论文《APreQEL: Adaptive Mixed Precision Quantization For Edge LLMs》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限的边缘设备（如嵌入式系统）上高效部署大语言模型（LLM），并平衡内存占用、推理延迟和模型准确性这三个相互冲突的指标。
*   **研究动机**：
    *   **层重要性差异**：传统方法多采用“统一量化”（所有层使用相同位宽），忽略了不同层对模型输出贡献不同的事实。
    *   **延迟与大小的非线性关系**：实验发现，更激进的量化（如3-bit）并不总是带来更低的延迟，因为反量化（De-quantization）开销可能抵消内存带宽带来的收益。
    *   **用户需求多样化**：不同应用场景对内存、速度和精度的优先级要求不同，需要一种可定制的量化方案。

### 2. 方法论
APreQEL 框架由三个核心模块组成：
*   **层级贡献分析模块**：利用余弦相似度测量连续层之间隐藏状态（Embeddings）的变化。变化大的层被认为贡献更高。采用“奖励-惩罚”机制：若相似度低于阈值（0.9），则增加该层的贡献得分。
*   **量化类型分布模块**：
    *   **指标估算**：基于统一量化的实测数据，通过加权组合估算混合精度配置下的内存、延迟和困惑度（Perplexity）。
    *   **多准则决策（MCDA）**：引入 **TOPSIS 算法**，根据用户为三个指标设置的权重（如延迟优先或内存优先），从数以万计的可能组合中筛选出最优的层级量化比例分布。
*   **层级量化分配模块**：将筛选出的量化类型按“重要层用高精度，次要层用低精度”的原则，映射到具体的模型层上。

### 3. 实验设计
*   **模型对象**：Llama-3.1-8B、Phi-3.5-mini、Qwen2.5-3B（文中记为Qwen3-4B）。
*   **数据集**：使用 WikiText-2 评估困惑度（PPL），使用 WebInstructSub-prometheus 评估层间相似度。
*   **Benchmark（基准）**：与 `llama.cpp` 提供的统一 K-quant 量化（3, 4, 5, 6, 8-bit）进行对比。
*   **实验场景**：设计了 28 种不同的权重配置，分为四类：公平竞争（指标均衡）、延迟优先、准确性优先、内存优先。

### 4. 资源与算力
*   **硬件平台**：NVIDIA Jetson Orin-AGX 嵌入式开发板（12核 ARM CPU，Ampere 架构 GPU，64GB LPDDR5 内存）。
*   **算力细节**：实验在边缘端真实硬件上运行，使用 GPU 加速推理。论文未提及具体的训练时长，因为该方法属于后量化（PTQ）范畴，主要开销在于推理测试和指标估算，而非大规模重训练。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 个主流模型架构，每种模型测试了 28 种混合精度配置，并与 5 种统一量化基准对比。
*   **充分性评价**：实验涵盖了不同规模和架构的模型，使用了超体积（Hypervolume）指标评估帕累托前沿（Pareto-front）的优化情况。实验设计较为客观，考虑了硬件真实的延迟表现而非理论值，具有较强的实践指导意义。

### 6. 主要结论与发现
*   **性能提升**：APreQEL 在超体积指标上比统一量化平均提升了约 **9%**，这意味着它能找到统一量化无法触达的更优权衡点。
*   **硬件敏感性**：证实了反量化开销在边缘硬件上不可忽视，混合精度能有效规避某些量化类型在特定硬件上的性能陷阱。
*   **鲁棒性**：该方法能准确响应用户的优先级设置，例如在“内存优先”模式下，生成的模型确实达到了最低的内存占用。

### 7. 优点
*   **硬件感知**：不仅考虑模型精度，还实测了特定硬件上的推理延迟，解决了理论加速与实际表现脱节的问题。
*   **高度灵活**：通过简单的权重设置即可适配实时响应、低功耗或高精度等多种边缘应用场景。
*   **搜索高效**：利用估算模型和 TOPSIS 算法，避免了对海量混合精度组合进行穷举测试（文中提到穷举可能需要 42 年）。

### 8. 不足与局限
*   **指标代理**：使用困惑度（Perplexity）作为准确性的唯一代理指标，虽然常用，但可能无法完全反映模型在特定下游任务上的表现。
*   **量化库依赖**：目前主要基于 `llama.cpp` 的 K-quant 算法，未测试其他量化框架（如 AutoGPTQ 或 AWQ）的混合效果。
*   **阈值启发式**：层贡献分析中的阈值（0.9）是启发式设置的，可能需要针对不同模型进行微调。
*   **环境单一**：实验仅在 Jetson 平台上完成，未在 NPU 或其他架构的边缘芯片上验证通用性。

（完）
