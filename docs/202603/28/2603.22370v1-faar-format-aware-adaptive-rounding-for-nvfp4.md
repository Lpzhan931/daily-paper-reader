---
title: "FAAR: Format-Aware Adaptive Rounding for NVFP4"
title_zh: FAAR：针对 NVFP4 的格式感知自适应舍入
authors: "Hanglin Li, Shuchang Tian, Chen Lin, Zhiyong Zhao, Kun Zhan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22370v1"
tags: ["query:llm"]
score: 8.0
evidence: 低比特量化加速计算
tldr: 针对边缘设备上大语言模型的超低比特量化需求，本文提出了面向NVFP4格式的可学习舍入策略FAAR。该方法通过将NVFP4的非均匀数值网格引入优化过程，并结合两阶段格式对齐（2FA）微调方案，有效降低了量化误差。实验表明，FAAR在极低训练开销下显著提升了模型在WikiText-2及下游任务上的性能，优于现有主流方法。
selection_source: fresh_fetch
motivation: 传统的量化舍入策略未考虑NVFP4非均匀数值网格的特性，导致在超低比特量化时产生较大的精度损失。
method: 提出了FAAR可学习舍入策略，通过损失梯度自适应调整舍入决策，并配合两阶段格式对齐（2FA）方案进行逐层参数优化。
result: 在Llama3-1B上将WikiText-2困惑度从14.28降至12.60，且仅需4个GPU小时的极低训练开销。
conclusion: FAAR为NVFP4格式下的LLM量化提供了一种高效且高性能的解决方案，显著缩小了超低比特量化与原始模型之间的性能差距。
---

## 摘要
在边缘设备上部署大语言模型（LLM）需要极低比特量化。诸如 NVFP4 之类的超低精度格式为减少内存占用和加速计算提供了一种极具前景的解决方案。然而，现有的量化方法通常依赖于传统的舍入策略，未能考虑到 NVFP4 数值网格的非均匀性，导致舍入决策并非最优并放大了量化误差。为了解决这一问题，我们提出了格式感知自适应舍入（FAAR），这是一种专为 NVFP4 格式定制的可学习舍入策略。与传统的量化范式不同，FAAR 显式地将非均匀的 NVFP4 网格纳入优化过程。通过在损失梯度的指导下自适应地调整舍入决策，我们的方法有效地逼近了理论上的最优量化。作为 FAAR 的补充，我们引入了一种两阶段格式对齐（2FA）微调方案，该方案将 LLM 参数逐层对齐到 NVFP4 数值空间，从而进一步缩小性能差距。值得注意的是，这种可学习的优化在 Llama3-1B 上仅产生 4 个 GPU 小时的极小训练开销。广泛的实验证明了我们方法的有效性。与最近邻舍入（RTN）相比，我们的方法在 WikiText-2 数据集上将 Llama3-1B 的困惑度从 14.28 降低到 12.60，将 Qwen3-1.7B 的困惑度从 23.06 降低到 21.27。此外，我们的方法在各种零样本下游任务中始终优于最先进的方法。

## Abstract
Deploying large language models (LLMs) on edge devices requires extremely low-bit quantization. Ultra-low precision formats such as NVFP4 offer a promising solution for reducing memory footprint and accelerating computation. However, existing quantization methods typically rely on conventional rounding strategies and fail to account for the non-uniformity of the NVFP4 numerical grid, resulting in suboptimal rounding decisions and amplified quantization errors. To address this, we propose Format-Aware Adaptive Rounding (FAAR), a learnable rounding strategy tailored for the NVFP4 format. Unlike conventional quantization paradigms, FAAR explicitly incorporates the non-uniform NVFP4 grid into the optimization process. By adaptively adjusting rounding decisions guided by loss gradients, our method effectively approximates the theoretically optimal quantization. To complement FAAR, we introduce a 2-stages Format Alignment (2FA) fine-tuning scheme that aligns LLM parameters layer-by-layer to the NVFP4 numerical space, further narrowing the performance gap. Remarkably, this learnable optimization incurs a minimal training overhead of only 4 GPU hours on Llama3-1B. Extensive experiments demonstrate the effectiveness of our approach. Compared with Round-to-Nearest (RTN), our method reduces perplexity on WikiText-2 from 14.28 to 12.60 on Llama3-1B and from 23.06 to 21.27 on Qwen3-1.7B. Additionally, our method consistently outperforms state-of-the-art approaches across various zero-shot downstream tasks.

---

## 论文详细总结（自动生成）

以下是对论文《FAAR: Format-Aware Adaptive Rounding for NVFP4》的结构化深度总结：

### 1. 核心问题与研究动机
*   **背景**：在边缘设备部署大语言模型（LLM）需要极低比特量化。NVIDIA 推出的 **NVFP4** 格式（E2M1 结合块缩放）在 Blackwell 架构上可实现 4-6 倍的推理加速，是极具前景的方案。
*   **核心问题**：NVFP4 的数值网格是**非均匀分布**的（数值在 0 附近密集，在大数值处稀疏）。
*   **研究动机**：现有的量化方法（如 AdaRound、BRECQ）多针对均匀的整数网格设计，或依赖简单的最近邻舍入（RTN）。在非均匀网格下，固定阈值的舍入决策会导致严重的量化误差放大，限制了 NVFP4 在超低精度下的性能表现。

### 2. 方法论：FAAR 与 2FA
论文提出了一个结合可学习舍入与全局对齐的框架：
*   **FAAR (格式感知自适应舍入)**：
    *   **核心思想**：将离散的舍入决策松弛为连续的可优化问题。
    *   **技术细节**：为每个权重引入一个可学习变量 $v \in [0, 1]$。量化值通过公式 $w_q = \text{sign}(w) \cdot [w_{lower} + h_\beta(v) \cdot (w_{upper} - w_{lower})]$ 计算，其中 $h_\beta(v)$ 是带温度系数的 Sigmoid 函数。
    *   **格式感知**：公式显式包含了 NVFP4 的区间跨度 $(w_{upper} - w_{lower})$，使得梯度能够根据非均匀网格的间距自适应调整。
*   **2FA (两阶段格式对齐)**：
    *   **第一阶段（逐层优化）**：固定输入激活值，最小化量化层与 BF16 层之间的输出 MSE 误差，并加入舍入正则项。
    *   **第二阶段（全模型对齐）**：组装全模型，通过 KL 散度（输出概率分布）和最后隐藏层 MSE 损失进行端到端微调，以消除层间误差累积。
    *   **硬化（Hardening）**：训练结束后，将连续变量 $v$ 确定性地转换为 0 或 1 的离散决策。

### 3. 实验设计
*   **数据集**：WikiText-2、C4（评估困惑度 PPL）；BoolQ、Arc-Easy、Arc-Challenge、HellaSwag（评估零样本下游任务准确率）。
*   **Benchmark 模型**：Llama3 (1B, 8B) 和 Qwen3 (1.7B, 8B)。
*   **对比方法**：RTN（最近邻）、GPTQ、MR-GPTQ、4/6 方法（针对 NVFP4 的缩放优化）以及 GPTQ+4/6 组合。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA H200 GPU**。
*   **时长**：对于 Llama3-1B 模型，整个优化过程仅需约 **4 个 GPU 小时**。
*   **效率**：相比于全量微调，该方法极其高效，适合快速部署。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流模型家族（Llama 和 Qwen）、两种规模（1B 级和 8B 级）、多种任务类型（语言建模与下游推理）。
*   **消融实验**：详细分析了 FAAR 组件、2FA 阶段、优化步数（Steps）以及学习率（LR）对结果的影响。
*   **客观性**：实验对比了多种最先进的（SOTA）量化方案，并提供了余弦相似度分析来验证特征空间的保持情况，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **性能提升**：在 Llama3-1B 上，WikiText-2 的 PPL 从 RTN 的 14.28 降至 **12.60**；在 Qwen3-1.7B 上从 23.06 降至 **21.27**。
*   **下游任务**：在零样本准确率上，FAAR 显著优于 GPTQ 等方法，平均准确率接近 BF16 基准（差距仅约 0.7-1.3%）。
*   **特征保持**：FAAR 显著提高了量化模型与原模型隐藏状态的余弦相似度（最高达 99.13%），证明了其对原始特征空间的极佳保留。

### 7. 优点与亮点
*   **针对性强**：首次针对 NVFP4 的非均匀特性提出了“格式感知”的舍入优化，填补了该领域的空白。
*   **高效性**：在极短的时间内（4 小时）实现了显著的精度回升，具有极高的工程实用价值。
*   **两阶段策略**：通过局部优化初始化，再进行全局对齐，有效解决了量化误差在深层网络中的累积问题。

### 8. 不足与局限
*   **模型规模限制**：实验最高仅验证到 8B 参数模型，尚未在 70B 或更大规模的模型上验证其扩展性。
*   **应用范围**：虽然逻辑可推广，但本文方法高度绑定 NVFP4 格式，对于其他新兴的微缩放格式（如 MXFP4）未做直接对比。
*   **微调依赖**：虽然开销极小，但它仍属于需要少量数据进行微调的范畴，而非完全的“数据无关”量化（Data-free Quantization）。

（完）
