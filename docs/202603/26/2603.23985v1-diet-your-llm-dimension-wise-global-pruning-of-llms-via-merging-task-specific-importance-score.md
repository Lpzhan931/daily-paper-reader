---
title: "Diet Your LLM: Dimension-wise Global Pruning of LLMs via Merging Task-specific Importance Score"
title_zh: Diet Your LLM：通过合并任务特定重要性得分实现大语言模型的维度级全局剪枝
authors: "Jimyung Hong, Jaehyung Kim"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23985v1"
tags: ["query:llm"]
score: 10.0
evidence: LLM的无需训练的结构化剪枝方法
tldr: 针对大语言模型部署难题，本文提出 DIET，一种无需训练的结构化剪枝方法。它通过分析少量样本在不同任务中的激活强度，利用多数投票机制融合任务特定的重要性得分，构建全局掩码。在 Gemma-2 模型上的实验表明，DIET 在保持低计算成本的同时，显著优于现有 SOTA 方法，为高效模型压缩提供了新方案。
selection_source: fresh_fetch
motivation: 现有的结构化剪枝方法在任务适应性与计算成本之间存在权衡，缺乏既能感知任务又无需昂贵训练的方案。
method: DIET 通过分析各任务少量样本的激活幅度，并采用多数投票机制融合任务重要性得分，从而生成全局维度的剪枝掩码。
result: "在 Gemma-2 2B 模型上，20% 稀疏度下的平均准确率比现有最先进方法提高了近 10%，且优势在不同规模和稀疏度下均保持稳定。"
conclusion: DIET 证明了通过简单的维度级重要性融合，可以在不进行重训练的情况下实现高效且鲁棒的 LLM 结构化剪枝。
---

## 摘要
大语言模型（LLMs）展现了卓越的能力，但其庞大的规模给实际部署带来了重大挑战。结构化剪枝通过移除整个维度或层提供了一种极具前景的解决方案，但现有方法面临关键的权衡：任务无关（task-agnostic）方法无法适应特定任务的需求，而任务感知（task-aware）方法则需要昂贵的训练来学习任务适应性。我们提出了 DIET（Dimension-wise global pruning of LLMs via merging Task-wise importance scores），这是一种无需训练的结构化剪枝方法，结合了维度级粒度与任务感知选择。DIET 仅使用每个任务 100 个样本来分析跨任务的激活幅度，然后应用多数投票法构建单一的全局掩码。DIET 不需要高昂的预计算或训练成本。在 Gemma-2 2B 和 9B 模型上进行的七个零样本基准测试实验证明了 DIET 的有效性；例如，在 Gemma-2 2B 的 20% 稀疏度下，与之前的最先进结构化剪枝方法相比，DIET 实现了近 10% 的平均准确率提升。这种优势在各种稀疏水平和模型规模下均保持一致，使 DIET 成为结构化 LLM 剪枝的一种实用且稳健的选择。

## Abstract
Large language models (LLMs) have demonstrated remarkable capabilities, but their massive scale poses significant challenges for practical deployment. Structured pruning offers a promising solution by removing entire dimensions or layers, yet existing methods face critical trade-offs: task-agnostic approaches cannot adapt to task-specific requirements, while task-aware methods require costly training to learn task adaptability. We propose DIET (Dimension-wise global pruning of LLMs via merging Task-wise importance scores), a training-free structured pruning method that combines dimension-level granularity with task-aware selection. DIET profiles activation magnitudes across tasks using only 100 samples per task, then applies majority voting to construct a single global mask. DIET does not require large costs from pre-computation or training. Experiments on seven zero-shot benchmarks using Gemma-2 2B and 9B models demonstrate the effectiveness of DIET; for example, at 20% sparsity on Gemma-2 2B, DIET achieves near 10% average accuracy improvement, compared to previous state-of-the-art structured pruning methods. This advantage persists across various sparsity levels and model scales, positioning DIET as a practical and robust choice for structured LLM pruning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Diet Your LLM: Dimension-wise Global Pruning of LLMs via Merging Task-specific Importance Score》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模的不断扩大，其在资源受限设备上的部署面临巨大挑战。**结构化剪枝**（如移除整个维度或层）比非结构化剪枝更具实践意义，因为后者与现有的硬件加速器不兼容。
然而，现有结构化剪枝方法存在两难境地：
*   **任务无关方法**（如 SliceGPT）：虽然无需针对特定任务，但无法利用任务特性，且计算开销（如 PCA 运算）较大。
*   **任务感知方法**（如 PuDDing）：虽然性能好，但通常需要昂贵的重训练或训练额外的路由模块（Router）。
**核心动机**：开发一种**无需训练（Training-free）**、**维度级（Dimension-wise）**且能**融合多任务信息**的全局结构化剪枝框架。

### 2. 方法论：DIET 核心思想与技术细节
DIET 的核心逻辑分为三个阶段：
*   **任务特定重要性评分（Task-wise Scoring）**：
    *   针对每个任务，仅抽取极少量样本（如 100 个）。
    *   通过前向传播记录每个 Transformer 块中 MLP 层的输出激活值。
    *   计算每个维度的平均绝对激活强度，作为该维度对该任务重要性的代理指标。
*   **多数投票机制（Majority Voting）**：
    *   对于每个任务，将得分最低的维度标记为“可剪枝”候选，生成二值掩码。
    *   将所有任务的掩码进行堆叠，统计每个维度被投“剪枝票”的次数。
    *   选择得票数最高的维度（即大多数任务都认为不重要的维度）构建最终的**全局剪枝掩码**。
*   **全局应用与硬剪枝（Hard Pruning）**：
    *   将该掩码统一应用于嵌入层、注意力投影、MLP 投影及 LM Head。
    *   **方差修正（Variance Correction）**：在物理移除维度后，为了弥补维度减少导致的激活方差下降，引入缩放因子 $\alpha = \sqrt{d/d'}$ 对权重进行重缩放，以保持表征的稳定性。

### 3. 实验设计
*   **模型对象**：Gemma-2 2B 和 9B。
*   **数据集/基准测试**：
    *   7 个零样本（Zero-shot）NLP 基准：BoolQ, RTE, HellaSwag, WinoGrande, ARC-Easy, ARC-Challenge, OpenBookQA。
    *   扩展测试：XNLI（多语言能力）和 IFEval（指令遵循能力）。
*   **对比方法（Baselines）**：
    *   **Magnitude-Dim**：基于权重的维度剪枝。
    *   **SliceGPT**：基于 PCA 的维度压缩方法。
    *   **PuDDing**：基于训练路由器的块级（深度）剪枝方法。

### 4. 资源与算力
*   **样本需求**：每个任务仅需 100 个样本进行激活分析，极大地降低了数据需求。
*   **算力说明**：论文强调 DIET 是“无需训练”的，仅涉及前向推理统计。
*   **具体硬件**：文中未明确列出具体的 GPU 型号和总耗时，但提到该方法旨在加速推理并减少内存占用（实验中测量了延迟和峰值内存）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种模型规模（2B, 9B）、三种稀疏度（10%, 20%, 30%）以及 7 个核心任务。
*   **消融与分析**：
    *   对比了全局掩码与单任务掩码的效果。
    *   对比了二值投票与连续得分合并的效果。
    *   分析了样本量（10 到全量数据）对性能的影响。
    *   验证了在 Qwen2.5 和 Phi-4 上的泛化性。
*   **评价**：实验设计较为全面，通过跨模型、跨任务、跨稀疏度的对比，客观地展示了 DIET 在结构化剪枝领域的竞争力。

### 6. 主要结论与发现
*   **性能卓越**：在 10% 稀疏度下，DIET 在 Gemma-2 2B 上的平均准确率比 SliceGPT 和 PuDDing 高出约 20%。
*   **鲁棒性强**：随着模型规模从 2B 增加到 9B，DIET 的优势依然存在。
*   **硬剪枝有效**：通过方差修正，物理移除维度后的性能与零掩码（Zero-masking）性能几乎持平，且能带来实际的推理加速和内存节省。
*   **共识性**：实验发现约 10% 的维度在几乎所有任务中都被视为不重要，这验证了全局投票机制的合理性。

### 7. 优点（亮点）
*   **极低成本**：无需微调，无需训练路由器，仅需少量样本的一次性分析。
*   **简单高效**：采用多数投票这种直观的逻辑，解决了任务感知与全局通用性之间的矛盾。
*   **部署友好**：生成的全局掩码在所有层中是一致的，非常适合硬件加速。
*   **方差修正**：提出的权重缩放策略有效缓解了硬剪枝带来的分布偏移问题。

### 8. 不足与局限
*   **高稀疏度挑战**：当稀疏度达到 30% 或更高时，所有方法（包括 DIET）的性能都会出现剧烈下降，显示出维度剪枝的极限。
*   **架构敏感性**：在非 Gemma 架构（如 Qwen 或 Phi）上，高稀疏度下的性能退化比 Gemma 更明显，可能需要更精细的层间分配策略。
*   **任务覆盖**：主要集中在英语常识推理任务，对于长文本、代码生成或多模态任务的有效性尚待验证。

（完）
