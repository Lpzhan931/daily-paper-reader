---
title: Self-Distillation for Multi-Token Prediction
title_zh: 面向多标记预测的自蒸馏
authors: "Guoliang Zhao, Ruobing Xie, An Wang, Shuaipeng Li, Huaibing Xie, Xingwu Sun"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23911v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于推理加速的多标记预测
tldr: 针对大语言模型推理效率瓶颈，本文提出了一种名为 MTP-D 的自蒸馏方法，旨在优化多 Token 预测（MTP）架构。该方法在极低训练成本下显著提升了 MTP 头的接受率，并引入循环扩展策略以实现更高效的推理加速。实验证明，MTP-D 在保持主模型性能的同时，大幅提高了推理速度和扩展性，为 MTP 的实际应用提供了有效方案。
selection_source: fresh_fetch
motivation: 现有的多 Token 预测方法面临 MTP 头接受率低以及多个预测头联合训练困难的挑战。
method: 提出 MTP-D 自蒸馏方法及循环扩展策略，通过蒸馏主模型知识来增强 MTP 头的预测能力并实现经济的头扩展。
result: "MTP-D 将 MTP 头接受率提升了 7.5%，且循环扩展策略相比单头 MTP 实现了 220.4% 的推理加速。"
conclusion: MTP-D 及其扩展策略有效提升了 MTP 头的性能与推理效率，为大语言模型的实际应用提供了有力支持。
---

## 摘要
随着大语言模型（LLM）规模的扩大，推理效率已成为一个关键瓶颈。多标记预测（MTP）可以通过并行预测多个未来标记来加速 LLM 推理。然而，现有的 MTP 方法仍面临两个挑战：MTP 头的接受率有限，以及联合训练多个 MTP 头的困难。因此，我们提出了 MTP-D，这是一种简单且有效的自蒸馏方法，其训练成本极低，在最大程度保留主头性能的同时，提升了 MTP 头的接受率（+7.5%）。我们还为 MTP-D 引入了一种循环扩展策略，实现了高效且经济的 MTP 头扩展，并相对于单头 MTP 实现了进一步显著的推理加速（+220.4%）。此外，我们通过在七个基准测试上的广泛实验，系统地探索并验证了关于蒸馏策略和 MTP 潜在可扩展性的关键见解。这些结果表明，我们的 MTP-D 和循环扩展策略有效地增强了 MTP 头的性能和推理效率，促进了 MTP 在 LLM 中的实际应用。

## Abstract
As Large Language Models (LLMs) scale up, inference efficiency becomes a critical bottleneck. Multi-Token Prediction (MTP) could accelerate LLM inference by predicting multiple future tokens in parallel. However, existing MTP approaches still face two challenges: limited acceptance rates of MTP heads, and difficulties in jointly training multiple MTP heads. Therefore, we propose MTP-D, a simple yet effective self-distillation method with minimal additional training cost, which boosts MTP head acceptance rates (+7.5\%) while maximumly preserving main-head performance. We also introduce a looped extension strategy for MTP-D, enabling effective and economical MTP head extension and further significant inference speedup to 1-head MTP (+220.4\%). Moreover, we systematically explore and validate key insights on the distillation strategies and the potential scalability of MTP through extensive experiments on seven benchmarks. These results demonstrate that our MTP-D and looped extension strategy effectively enhance MTP-head performance and inference efficiency, facilitating the practical usage of MTP in LLMs.

---

## 论文详细总结（自动生成）

以下是对论文《Self-Distillation for Multi-Token Prediction》（面向多标记预测的自蒸馏）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）推理效率低下。虽然多标记预测（MTP）通过并行预测未来多个 Token 来加速推理，但现有方法面临两个痛点：
    1.  **接受率有限**：MTP 预测头与主预测头之间存在性能差距，导致预测 Token 的接受率随预测长度增加而指数级下降。
    2.  **联合训练困难**：同时训练多个 MTP 头和主头会产生“跷跷板效应”，即提升 MTP 头性能往往会损害主头的表现。
*   **研究背景**：MTP 已被 DeepSeek-V3 等工业界模型采用，但如何高效扩展 MTP 头的数量并提升其准确性仍是待解决的难题。

### 2. 方法论
论文提出了 **MTP-D** 框架，包含两个核心组件：
*   **自蒸馏预训练（Self-Distillation）**：
    *   **核心思想**：将主预测头作为“教师”，MTP 头作为“学生”，通过蒸馏损失函数对齐两者的 Logits 分布。
    *   **梯度截断（Gradient-detached）**：在计算蒸馏损失时，对主头的 Logits 执行 `stop-gradient` 操作，确保蒸馏信号只更新 MTP 头，从而避免干扰主头的优化。
    *   **Top-N Logits 选择**：由于词表巨大且分布长尾，仅选择主头概率最高的 Top-N（如 10,000）个 Token 进行蒸馏，以降低计算开销并提高数值稳定性。
*   **循环扩展策略（Looped Extension Strategy）**：
    *   **核心思想**：利用 MTP 架构的结构一致性，将已训练好的 $m$ 个 MTP 头作为初始化权重，通过“循环”方式扩展到更多预测头。
    *   **经济化训练**：在持续预训练阶段，冻结主模型和已有的 MTP 头，仅更新新扩展的预测头，大幅降低了扩展成本。

### 3. 实验设计
*   **数据集**：FineWeb-Edu-350BT（从 FineWeb-Edu 中随机抽取的 3500 亿 Token 子集）。
*   **模型架构**：
    *   2B Dense（稠密模型）
    *   N10BA1B MoE（混合专家模型）
*   **Benchmark**：涵盖 7 个主流基准测试，包括 AGIEval-en（综合）、GSM8K/MATH（数学）、NaturalQuestions/TriviaQA（知识问答）、SimpleQA（事实性）和 SuperGPQA（研究生水平推理）。
*   **对比方法**：以 DeepSeek 风格的标准 MTP 架构作为主要 Baseline。

### 4. 资源与算力
*   **硬件设备**：使用了 **256 张 NVIDIA H20 GPU**（每张显存 98GB）。
*   **训练时长**：主要实验运行了约 **30 天**。
*   **训练规模**：MTP-D 预训练使用了 350B Token，循环扩展的持续预训练使用了 70B Token。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度实验，包括不同模型架构（Dense vs MoE）、不同预测头数量（1 到 16 个头）的对比。
*   **消融实验**：针对梯度截断、Top-N 取值、KL 散度类型（前向/后向/混合）、蒸馏系数 $\beta_k$ 等关键超参数做了详尽的消融研究。
*   **评价指标**：不仅关注主头准确率，还深入分析了接受率（AR）、累积接受率（CAR）和实际推理加速比（Speedup Ratio）。
*   **客观性**：实验覆盖了多种任务类型，且在相同算力和数据条件下进行对比，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：MTP-D 在不损害主头性能的前提下，将 MTP 头的接受率提升了约 **7.5%**。
*   **推理加速**：4 头配置下相比标准 MTP 提速 22.9%；通过循环扩展至更多头，相比单头 MTP 实现了最高 **220.4%** 的推理加速。
*   **可扩展性**：MTP-D 显著增强了 MTP 头的扩展潜力。在无训练直接扩展实验中，标准 MTP 在第 3 个头时性能就已崩溃，而 MTP-D 仍能保持较高的累积接受率。
*   **数据效率**：循环扩展策略仅需少量数据（70B Token）即可达到与大规模数据训练相当的效果。

### 7. 优点与亮点
*   **设计巧妙**：通过简单的梯度截断解决了主头与 MTP 头训练冲突的顽疾。
*   **实用性强**：Top-N 选择和循环扩展策略极大地降低了 MTP 的训练和扩展门槛，适合工业界大规模部署。
*   **分析深入**：论文揭示了级联 MTP 架构天然具有结构一致性，为未来的架构优化提供了理论见解。

### 8. 不足与局限
*   **场景覆盖**：研究主要集中在预训练阶段，尚未充分探讨 MTP-D 在微调（SFT）和强化学习（RLHF）阶段的表现。
*   **模型规模限制**：实验最高仅在 2B/10B 规模模型上验证，尚未在千亿级（100B+）超大模型上进行测试。
*   **超参数依赖**：最优的蒸馏系数 $\beta_k$ 与预测头数量 $K$ 之间的理论关系仍需进一步探索。

（完）
