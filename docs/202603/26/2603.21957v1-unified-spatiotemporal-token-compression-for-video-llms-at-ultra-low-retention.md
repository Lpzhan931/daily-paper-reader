---
title: Unified Spatiotemporal Token Compression for Video-LLMs at Ultra-Low Retention
title_zh: 极低保留率下视频大语言模型的统一时空 Token 压缩
authors: "Junhao Du, Jialong Xue, Anqi Li, Jincheng Dai, Guo Lu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21957v1"
tags: ["query:llm"]
score: 8.0
evidence: 视频LLM的时空Token压缩与剪枝
tldr: "针对视频大语言模型（Video-LLMs）因视觉标记过多导致计算成本高昂的问题，本文提出了一种统一的时空标记压缩策略。该方法摒弃了传统的两阶段分离压缩，将压缩重新定义为全局标记分配任务，结合注意力权重和语义相似度筛选关键标记，并通过聚类合并保留冗余信息。此外，引入文本感知合并进一步优化查询相关性。该方法无需重训，在仅保留约2%标记的情况下仍能保持90.1%的基准性能，显著降低了推理延迟和内存消耗。"
selection_source: fresh_fetch
motivation: 现有的两阶段时空压缩方法在极低保留率下容易导致视觉证据丢失和分配不均，难以平衡计算效率与理解精度。
method: 提出一种统一的选择机制，结合注意力与语义相似度进行全局标记筛选，并利用聚类合并未选标记及文本感知合并技术实现高效压缩。
result: "在多个基准测试中，仅需保留约2%的视觉标记即可维持90.1%的原始性能，同时将计算量（FLOPs）降至2.6%。"
conclusion: 该统一时空压缩策略在超低标记保留率下实现了视频理解的最优性能，为高效Video-LLM推理提供了即插即用的解决方案。
---

## 摘要
视频大语言模型（Video-LLMs）由于大量的视觉 Token 而面临高昂的计算成本。现有的 Token 压缩方法通常采用两阶段时空压缩策略，依赖于特定阶段的指标以及时空可分离性的隐式假设。然而，在极低的保留率下，此类方法往往会导致分配不均，并丢失对问答至关重要的视觉证据。我们将 Token 压缩重新表述为全局 Token 保留池中的时空分配任务。我们提出了一种统一的选择机制，结合注意力权重和语义相似度，在全局范围内选择高贡献且低冗余的 Token。未被选中的 Token 通过聚类进行合并并重新填充，从而保持信息的完整性。在 LLM 内部，我们进一步引入了文本感知合并，根据查询相关性进行二次压缩。该方法无需重新训练，可作为即插即用的模块与现有的 Video-LLMs 兼容。实验表明，仅保留约 2% 的视觉 Token 即可在多个基准测试中保持 90.1% 的基准性能，同时将 FLOPs 降低至约 2.6%。这些优势可推广至多种骨干网络，降低了端到端推理延迟和内存消耗。我们的统一时空 Token 压缩策略在极低 Token 保留率下的视频理解领域达到了当前最先进的水平。

## Abstract
Video large language models (Video-LLMs) face high computational costs due to large volumes of visual tokens. Existing token compression methods typically adopt a two-stage spatiotemporal compression strategy, relying on stage-specific metrics and an implicit assumption of spatiotemporal separability. Under extremely low retention ratios, however, such approaches often result in unbalanced allocation and loss of visual evidence essential for question answering. We reformulate token compression as a spatiotemporal allocation task within a global token retention pool. We propose a unified selection mechanism that integrates attention weights and semantic similarity to globally select tokens with high contribution and low redundancy. Unselected tokens are merged via clustering and refilled, preserving information integrity. Inside the LLM, we further introduce text-aware merging to perform secondary compression based on query relevance. Without requiring retraining, our method serves as a plug-and-play module compatible with existing Video-LLMs. Experiments show that retaining only about 2% of visual tokens preserves 90.1% of baseline performance across multiple benchmarks, while reducing FLOPs to roughly 2.6%. These benefits generalize across diverse backbones, decreasing end-to-end inference latency and memory consumption. Our unified spatiotemporal token compression strategy establishes the state-of-the-art in video understanding under ultra-low token retention.

---

## 论文详细总结（自动生成）

以下是对论文《Unified Spatiotemporal Token Compression for Video-LLMs at Ultra-Low Retention》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
视频大语言模型（Video-LLMs）在处理长视频时面临巨大的计算开销，主要源于视觉 Token 数量庞大（如 32 帧视频可产生超过 6000 个 Token）。
*   **现有挑战**：目前的 Token 压缩方法多采用“先空间、后时间”的两阶段分离策略，隐式假设时空冗余是可分离的。但在**极低保留率（如 <5%）**下，这种分离策略会导致时空资源分配不均，容易丢失关键视觉证据，导致问答性能剧降。
*   **核心动机**：将 Token 压缩重新定义为全局池化下的时空分配任务，旨在超低保留率下通过统一的时空筛选和文本感知机制，平衡计算效率与理解精度。

### 2. 方法论：核心思想与关键技术
论文提出了一种**统一时空 Token 压缩框架**，包含 LLM 外部的全局压缩和 LLM 内部的文本感知合并：

*   **统一时空压缩（外部）**：
    *   **时空剪枝（Spatiotemporal Pruning）**：建立全局保留池。利用视觉编码器的注意力分数（衡量贡献度）和余弦相似度（衡量冗余度）进行筛选。只有高贡献且与已选 Token 相似度低于阈值 $\tau$ 的 Token 才能进入保留池。
    *   **时空聚类（Spatiotemporal Clustering）**：未被选中的 Token 进入回收池，使用 DPC-KNN（基于密度峰值的 K 近邻聚类）算法进行语义聚合，提取聚类中心作为补充信息重新填入保留池，确保语义完整性。
*   **文本感知合并（内部）**：
    *   在 LLM 推理过程中，结合**文本-视觉注意力分数**和**文本-视觉语义相似度**，计算每个视觉 Token 的决策分数。
    *   将低分 Token 合并到与其最相似的高分 Token 中，而非直接丢弃，从而在减少计算量的同时保留查询相关的上下文。
*   **特性**：该方法为**无需训练（Training-free）**的即插即用模块。

### 3. 实验设计
*   **数据集/场景**：涵盖了 6 个主流视频理解基准测试：
    *   **短视频/通用**：MVBench, ActivityNet-QA。
    *   **长视频/复杂推理**：EgoSchema, MLVU, LongVideoBench, VideoMME。
*   **基准模型（Backbones）**：主要基于 LLaVA-OneVision-7B，并推广至 LLaVA-Video-7B、LLaVA-OneVision-0.5B 和 Qwen2.5-VL-7B。
*   **对比方法**：对比了 5 种最先进的训练前/推理时压缩方法：FastV, VisionZip, LLaVA-Scissor, FastVID, 以及最新的 HoliTom。

### 4. 资源与算力
*   **硬件环境**：实验在单张 NVIDIA RTX 4090 或 A100 GPU 上完成。
*   **训练时长**：由于该方法是**训练无关（Training-free）**的，因此不存在训练阶段，仅涉及推理评估。
*   **效率提升**：在 2% 保留率下，FLOPs 降至原始模型的 2.6%，吞吐量显著提升。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的大规模实验，包括：
    *   在 4 种不同架构的视觉语言模型上验证通用性。
    *   在 6 个不同侧重点的数据集上进行性能对比。
    *   针对不同采样帧数（最高 128 帧）的扩展性测试。
*   **消融实验**：针对注意力筛选、相似度剪枝、聚类合并、文本感知权重 $\lambda$、剪枝层数 $K$ 等关键超参数进行了详尽的消融研究。
*   **客观性**：通过 FLOPs、TTFT（首字延迟）和吞吐量等硬指标量化效率，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能卓越**：在仅保留 **2%** 视觉 Token 的极端情况下，该方法仍能保持基准模型 **90.1%** 的性能，远超 FastVID 和 HoliTom 等方法。
*   **效率极高**：计算量（FLOPs）可缩减至约 1/40，显著降低了推理延迟和显存占用。
*   **长视频优势**：通过超低保留率，模型能够处理更多帧数（如 128 帧），在长视频基准 VideoMME 上表现甚至优于未压缩但帧数较少的原始模型。
*   **文本引导的重要性**：实验证明，结合文本查询信息进行内部合并能有效缓解注意力机制的局部位置偏差（RoPE 引起）。

### 7. 优点与亮点
*   **全局优化**：打破了时空分离压缩的局限，通过全局池化实现了更合理的 Token 分配。
*   **信息无损化处理**：通过“回收池+聚类”机制，避免了直接剪枝带来的信息彻底丢失。
*   **即插即用**：无需任何微调即可适配多种主流模型，部署成本极低。
*   **文本感知**：内部合并机制考虑了用户查询，使压缩过程具有动态的“任务导向性”。

### 8. 不足与局限
*   **实时性限制**：目前主要针对离线视频处理，对于实时流式视频的 Token 压缩支持尚不明确。
*   **采样策略依赖**：虽然压缩效果好，但前端仍依赖均匀采样，可能在极短或极长视频中存在关键帧遗漏的风险。
*   **超参数敏感性**：虽然提供了推荐值，但在不同任务下，相似度阈值 $\tau$ 和剪枝层数 $K$ 可能仍需微调以达到最优平衡。

（完）
