---
title: "Energy Efficient Software Hardware CoDesign for Machine Learning: From TinyML to Large Language Models"
title_zh: 机器学习中的高能效软硬件协同设计：从 TinyML 到大语言模型
authors: "Mohammad Saleh Vahdatpour, Yanqing Zhang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23668v1"
tags: ["query:llm"]
score: 9.0
evidence: LLM服务能效软硬件协同设计综述
tldr: 本文综述了从毫瓦级TinyML到大规模语言模型（LLM）的机器学习能效软硬件协同设计方法。针对数据传输而非算力成为能效瓶颈的现状，文章系统梳理了加速器架构（如ASIC、存内计算）及系统级优化技术（如量化、调度）。通过总结设计权衡与现有挑战，提出了一种层次化分解视角，为构建低碳可持续的AI系统提供了实践指南。
selection_source: fresh_fetch
motivation: 随着机器学习应用规模跨度剧增，数据传输和存储系统已取代算力成为制约AI可持续发展的核心能效瓶颈。
method: 综述了涵盖硬件加速器架构、存内计算以及量化、分区、调度等系统级软硬件协同设计方法，并提出了层次化分解的优化视角。
result: 总结了不同规模下的设计杠杆与权衡，并指出了跨平台泛化难、协同设计搜索空间巨大及基准测试不一致等关键挑战。
conclusion: 层次化分解框架能有效将优化策略映射至计算角色，为开发能效高且具备碳意识的机器学习系统提供了指导路径。
---

## 摘要
机器学习在从毫瓦级 TinyML 设备到大语言模型的各类平台上的快速部署，使得能效成为可持续人工智能的主要约束。在这些尺度上，性能和能耗日益受到数据移动和存储系统行为的限制，而不仅仅取决于算术吞吐量。本文综述了涵盖从边缘推理与训练到数据中心级 LLM 服务的能效软硬件协同设计方法，涉及加速器架构（如 ASIC/FPGA 数据流、存内处理/存内计算设计）以及系统级技术（如分区、量化、调度和运行时自适应）。我们提炼了常见的设计杠杆和权衡，并强调了反复出现的差距，包括有限的跨平台泛化能力、庞大且昂贵的协同设计搜索空间，以及不同工作负载和部署设置下不一致的基准测试。最后，我们概述了一种分层分解视角，将优化策略映射到计算角色并支持增量自适应，为构建能源和碳感知的机器学习系统提供了实用指南。

## Abstract
The rapid deployment of machine learning across platforms from milliwatt-class TinyML devices to large language models has made energy efficiency a primary constraint for sustainable AI. Across these scales, performance and energy are increasingly limited by data movement and memory-system behavior rather than by arithmetic throughput alone. This work reviews energy efficient software hardware codesign methods spanning edge inference and training to datacenter-scale LLM serving, covering accelerator architectures (e.g., ASIC/FPGA dataflows, processing-/compute-in-memory designs) and system-level techniques (e.g., partitioning, quantization, scheduling, and runtime adaptation). We distill common design levers and trade-offs, and highlight recurring gaps including limited cross-platform generalization, large and costly co-design search spaces, and inconsistent benchmarking across workloads and deployment settings. Finally, we outline a hierarchical decomposition perspective that maps optimization strategies to computational roles and supports incremental adaptation, offering practical guidance for building energy and carbon aware ML systems.

---

## 论文详细总结（自动生成）

这篇论文对机器学习（ML）领域中能效导效的软硬件协同设计进行了系统性的综述和前瞻性分析。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战：** 随着 AI 从毫瓦级的 TinyML 到兆瓦级的超大规模语言模型（LLM）全面部署，**能效**已成为可持续 AI 的首要约束。
*   **瓶颈转移：** 性能和能耗的瓶颈已从单纯的“算力（算术吞吐量）”转向了**“存储墙（Memory Wall）”**。数据在计算单元与存储器之间的移动消耗了绝大部分能量（例如，DRAM 访问能耗比算术运算高出 200 倍）。
*   **研究意义：** 传统的独立优化（仅软件或仅硬件）已无法满足需求。本文旨在探讨如何通过软硬件协同设计（Co-Design），在不同尺度下解决数据移动带来的能效挑战。

### 2. 论文提出的方法论
论文总结了跨层优化的核心思想和关键技术：
*   **算法层：** 采用量化（Quantization）、剪枝（Pruning）、知识蒸馏（Knowledge Distillation）以及 N:M 稀疏模式来减少模型参数和计算量。
*   **架构层：** 
    *   **数据流优化：** 如权重驻留（Weight-stationary）或输出驻留数据流，以最大化数据复用。
    *   **存内计算（CiM）：** 在 SRAM 或 DRAM 内部直接进行算术运算，消除数据搬运。
    *   **专用加速器：** 利用 FPGA 和 ASIC 实现定制化流水线（如零缓冲区数据流）。
*   **系统层：** 动态电压频率调节（DVFS）、边缘-云分割计算（Split Computing）、连续批处理和 KV 缓存管理。
*   **创新视角（层次化分解）：** 提出将网络分为两层：底层负责稳定的特征提取（通过 FPGA 直接求解方程，无需反向传播），顶层负责自适应推理（增量学习），从而实现跨尺度的能效平衡。

### 3. 实验设计与 Benchmark
由于这是一篇综述性论文，它汇总并对比了多项代表性研究的实验结果：
*   **数据集/模型：** 涵盖了卷积神经网络（VGG16, MobileNetV2, ResNet）、Transformer 模型（BERT, DistilBERT）以及大语言模型（LLM，如 LLaMA, Falcon）。
*   **对比场景：** 
    *   **TinyML：** 对比了基于 RISC-V 的加速器与传统软件实现的能效。
    *   **边缘-云：** 使用 Raspberry Pi 4 结合云端，测试不同分割点对能效的影响。
    *   **LLM 规模：** 对比了 NVIDIA A40 GPU 与存内计算（CiM）架构、FPGA 空间加速器的吞吐量与能效（TOPS/W）。

### 4. 资源与算力
文中提到了多种硬件平台作为基准或实验对象：
*   **GPU：** NVIDIA A40（用于 LLM 推理对比）。
*   **边缘设备：** Raspberry Pi 4（4GB 内存）、FPGA（如 KV260, 28nm/32nm/65nm 工艺）。
*   **超大规模：** Cerebras WSE-2（晶圆级系统）。
*   **注意：** 论文本身作为综述，未提供单一的训练时长数据，而是引用了各研究中的能效指标（如 HASTILY 架构在 CiM 上比 GPU 能效高 16-36 倍）。

### 5. 实验数量与充分性
*   **覆盖范围：** 论文引用了从 2016 年到 2025 年（注：文中时间线包含 2025 年预测/近期工作）的数十项关键研究。
*   **充分性：** 实验数据涵盖了从亚毫瓦级到兆瓦级的全频谱分析，对比了速度提升（Speedup）、功耗降低（Power Reduction）和能效比（TOPS/W）等多个维度。
*   **客观性：** 论文不仅展示了协同设计的优势，也客观指出了 FPGA 在绝对吞吐量上仍逊于大批次 GPU 推理的局限性。

### 6. 主要结论与发现
*   **存储优化是核心：** 无论在什么尺度，减少数据移动都是提升能效的关键（TinyML 零缓冲设计可减少 87% 的数据移动）。
*   **协同设计优势巨大：** 相比软件基准，FPGA 和 CiM 架构在特定任务中可实现 16-59 倍的能效提升。
*   **系统级优化不可忽视：** 仅通过 DVFS 和推理服务优化（如 vLLM），在不改变模型语义的情况下即可降低 30-73% 的能耗。
*   **非线性关系：** 能效并不随模型规模线性变化，边缘-云分割的收益高度依赖于模型架构和网络带宽。

### 7. 优点
*   **全频谱覆盖：** 罕见地将 TinyML、边缘计算和大语言模型（LLM）放在统一的软硬件协同框架下讨论。
*   **前瞻性视角：** 提出了层次化分解（Hierarchical Decomposition）的原创性观点，为未来绿色 AI 提供了理论路径。
*   **多维度分析：** 不仅关注能耗，还讨论了碳足迹、水资源消耗等环境可持续性指标。

### 8. 不足与局限
*   **跨平台泛化难：** 针对 CNN 优化的协同设计技术往往难以直接迁移到具有不规则访问模式的 Transformer 模型。
*   **搜索空间巨大：** 软硬件协同设计的参数空间（量化、数据流、并行度等）极其庞大，目前仍缺乏高效的自动化工具。
*   **基准测试不一致：** 许多研究依赖软件模拟而非实测硬件功耗，可能导致对实际能耗的低估。
*   **安全风险：** 激进的能效优化（如低精度运算、定制数据流）可能增加侧信道攻击或对抗性攻击的风险。

（完）
