---
title: Demystifying When Pruning Works via Representation Hierarchies
title_zh: 通过表示层级揭秘剪枝何时有效
authors: "Shwai He, Guoheng Sun, Haichao Zhang, Yun Fu, Ang Li"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24652v1"
tags: ["query:llm"]
score: 9.0
evidence: 生成式语言任务中的网络剪枝分析
tldr: 本研究探讨了网络剪枝在不同语言任务中表现差异的根源。通过将模型计算划分为嵌入、Logit和概率三个空间，研究发现剪枝引起的微小扰动会在Logit到概率的非线性转换中被放大，并在生成任务中随时间步累积导致性能崩溃；而在非生成任务中，由于嵌入空间和特定子空间的鲁棒性，剪枝仍能保持高效。该分析为剪枝技术的适用场景提供了理论解释和实践指导。
selection_source: fresh_fetch
motivation: 探究为何剪枝后的模型在非生成任务中表现稳健，但在生成式任务中性能却显著下降。
method: 从表示层级视角出发，将模型内部计算分解为嵌入、Logit和概率三个连续空间来量化剪枝的影响。
result: 嵌入和Logit空间对剪枝具有鲁棒性，但非线性的概率转换会放大偏差并在生成过程中产生累积效应。
conclusion: 剪枝的成功与否取决于任务对概率空间波动的敏感程度，这解释了其在检索与生成任务中的表现差异。
---

## 摘要
网络剪枝通过移除不重要的参数或架构，通常被期望在保持性能的同时提高效率。然而，这种期望在不同的语言任务中并不总是一致：剪枝后的模型在非生成式任务中表现良好，但在生成式场景中经常失败。为了理解这一差异，我们从表示层级的角度分析了网络剪枝，将语言模型的内部计算分解为三个顺序空间：嵌入空间（隐藏表示）、Logit 空间（Softmax 前的输出）和概率空间（Softmax 后的分布）。我们发现，嵌入空间和 Logit 空间中的表示对剪枝引起的扰动具有很强的鲁棒性。然而，从 Logit 到概率的非线性变换放大了这些偏差，这些偏差在时间步长中不断累积，导致生成过程中的性能大幅下降。相比之下，类别标记概率子空间的稳定性以及嵌入空间的鲁棒性，支持了剪枝在检索和多项选择等非生成式任务中的有效性。我们的分析解耦了剪枝在不同任务中的影响，并为其应用提供了实践指导。代码可在 https://github.com/CASE-Lab-UMD/Pruning-on-Representations 获取。

## Abstract
Network pruning, which removes less important parameters or architectures, is often expected to improve efficiency while preserving performance. However, this expectation does not consistently hold across language tasks: pruned models can perform well on non-generative tasks but frequently fail in generative settings. To understand this discrepancy, we analyze network pruning from a representation-hierarchy perspective, decomposing the internal computation of language models into three sequential spaces: embedding (hidden representations), logit (pre-softmax outputs), and probability (post-softmax distributions). We find that representations in the embedding and logit spaces are largely robust to pruning-induced perturbations. However, the nonlinear transformation from logits to probabilities amplifies these deviations, which accumulate across time steps and lead to substantial degradation during generation. In contrast, the stability of the categorical-token probability subspace, together with the robustness of the embedding space, supports the effectiveness of pruning for non-generative tasks such as retrieval and multiple-choice selection. Our analysis disentangles the effects of pruning across tasks and provides practical guidance for its application. Code is available at https://github.com/CASE-Lab-UMD/Pruning-on-Representations

---

## 论文详细总结（自动生成）

以下是对论文《Demystifying When Pruning Works via Representation Hierarchies》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：为什么大语言模型（LLM）在经过网络剪枝（Pruning）后，在非生成式任务（如选择题、检索）上表现稳健，但在生成式任务（如数学推理、代码生成）中性能却经常发生崩溃？
*   **研究动机**：尽管剪枝是提高模型效率的重要手段，但其效果在不同任务间存在显著的不一致性。作者试图通过分析模型内部的“表示层级（Representation Hierarchies）”来揭示这一现象背后的根本原因。

### 2. 方法论：表示层级分析框架
作者将 LLM 的推理过程分解为三个连续的数学空间，并利用泰勒展开（Taylor Expansion）进行局部偏差分析：
*   **嵌入空间（Embedding Space, $h$）**：隐藏层表示。研究发现该空间对剪枝引起的扰动（$\Delta h$）具有天然的鲁棒性。
*   **Logit 空间（Logit Space, $z$）**：Softmax 前的输出。通过 **定理 1** 证明，线性投影层（LM Head）倾向于保持相似性，甚至能进一步衰减嵌入空间的扰动。
*   **概率空间（Probability Space, $p$）**：Softmax 后的分布。
    *   **核心发现**：通过 **定理 2 和 3** 证明，Softmax 这一非线性变换会显著放大 Logit 空间的微小偏差。
    *   **关键公式**：KL 散度近似于 Logit 扰动在概率加权下的方差（$KL(p\|q) \approx \frac{1}{2T^2} Var_{i \sim p}(\Delta z_i)$）。
*   **误差传播分析**：在自回归生成中，早期的概率偏差会导致采样到错误的 Token，这些错误会通过 KV Cache 随时间步累积，最终导致生成崩溃。

### 3. 实验设计
*   **模型对象**：Mistral-7B, Llama-3-8B, Qwen-2.5-7B, Qwen-3-4B 等主流开源模型。
*   **剪枝方法**：
    *   **层间剪枝（Inter-layer）**：移除整个 Transformer 块或特定的 Attention/MLP 层（如 ShortGPT, Layer Drop）。
    *   **层内剪枝（Intra-layer）**：非结构化或半结构化稀疏（如 Wanda, SparseGPT）。
*   **评测基准（Benchmark）**：
    *   **非生成式**：MMLU, HellaSwag, BoolQ（多轮选择）；E5-Mistral 在 Arguana 等 13 个检索数据集上的表现。
    *   **生成式**：GSM8K（数学）, HumanEval, MBPP（代码）, NarrativeQA（长文本生成）。
*   **对比维度**：对比了原始模型与不同剪枝比例/策略下的模型在三个空间（$h, z, p$）的余弦相似度和 KL 散度。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **原因分析**：由于本研究主要侧重于**无需训练的剪枝（Training-free Pruning）**和推理阶段的表示分析，其计算开销主要集中在模型推理和指标统计上，而非大规模反向传播训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 8B 参数规模的多个模型家族，测试了多种剪枝算法。
*   **充分性**：
    *   作者不仅提供了宏观的 Benchmark 分数，还深入到了**逐层（Layer-wise）**和**逐时间步（Step-wise）**的相似度分析。
    *   针对温度系数（Temperature）进行了消融实验，验证了理论推导的普适性。
    *   实验设计客观，通过对比“全词表概率”与“特定选项子空间概率”，合理解释了多选题为何更鲁棒。

### 6. 主要结论与发现
*   **层级差异**：嵌入空间和 Logit 空间相对稳定，而概率空间对剪枝极度敏感。
*   **非线性放大**：Softmax 是偏差放大的“元凶”，它将 Logit 空间的微小波动转化为概率分布的剧烈跳变。
*   **时间累积效应**：生成任务的失败源于误差在自回归循环中的持续传播与放大；而非生成任务仅需单次推理或关注受限的子空间（如 A/B/C/D 选项），因此受影响较小。
*   **子空间鲁棒性**：在多选题中，尽管全词表分布变化很大，但特定选项 Token 之间的相对顺序往往能保持不变。

### 7. 优点与亮点
*   **理论与实证结合**：不仅观察到了现象，还通过泰勒展开给出了严谨的数学解释（定理 1-3）。
*   **视角新颖**：从表示空间的几何特性出发，解耦了任务类型与剪枝效果的关系，为后续开发“生成友好型”剪枝算法指明了方向。
*   **实践指导性强**：解释了为什么 MMLU 等指标不能完全代表剪枝后模型的真实生成能力。

### 8. 不足与局限
*   **剪枝方法局限**：研究主要集中在无需训练的剪枝方法上，未深入探讨通过微调（Fine-tuning）或恢复训练是否能有效抑制概率空间的偏差放大。
*   **架构单一性**：实验主要基于标准的 Transformer 架构，对于新兴的架构（如线性注意力机制或状态空间模型 SSM）是否适用尚待验证。
*   **应用限制**：论文更多是“诊断性”的分析，并未直接提出一种能够完美解决生成任务崩溃的新型剪枝算法。

（完）
