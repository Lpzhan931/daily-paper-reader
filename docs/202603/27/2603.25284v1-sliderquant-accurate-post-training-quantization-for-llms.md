---
title: "SliderQuant: Accurate Post-Training Quantization for LLMs"
title_zh: SliderQuant：大语言模型的高精度训练后量化
authors: "Shigeng Wang, Chao Li, Yangyuxuan Kang, Jiawei Fan, Zhonghong Ou, Anbang Yao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25284v1"
tags: ["query:llm"]
score: 8.0
evidence: 用于大模型推理加速的训练后量化
tldr: 针对大语言模型（LLM）训练后量化（PTQ）中各层敏感度不一的问题，本文提出 SliderQuant 框架。研究发现浅层和深层（尤其是首尾层）对量化更敏感。SliderQuant 通过层间滑动量化（针对不同层设计三种窗口）和层内滑动量化（增量策略）来减少量化误差。实验证明该方法在多种模型和任务上优于现有 PTQ 技术，显著提升了低比特量化精度。
selection_source: fresh_fetch
motivation: 发现 LLM 的浅层和深层对量化误差比中间层更敏感，而现有方法往往平等对待所有层，导致量化效果不佳。
method: 提出 SliderQuant 框架，利用层间滑动量化窗口应对不同层的敏感度，并结合层内增量滑动策略进一步降低误差。
result: 在 Llama、Qwen、DeepSeek-R1 等多种模型及数学、代码任务上，该方法在权重量化和权重-激活量化方面均优于现有最先进技术。
conclusion: SliderQuant 通过自适应的滑动量化机制，有效解决了 LLM 不同层量化敏感度差异的问题，实现了更高精度的模型压缩。
---

## 摘要
在本文中，我们从一个被忽视的角度探讨了大语言模型（LLM）的训练后量化（PTQ）：给定一个预训练的高精度 LLM，主流的顺序量化框架对不同层一视同仁，但在具有挑战性的位宽设置下，这可能并非最优选择。我们通过实证研究了不同层对模型准确率的量化影响，并观察到：（1）浅层和深层通常比中间层对量化更敏感；（2）在浅层/深层中，最敏感的是第一层和最后一层，它们表现出的量化误差显著高于其他层。这些实证观察表明，LLM 不同层的量化设计需要多层次的方法，而非所有层共享单一层次。受此启发，我们提出了一种名为滑动层量化（SliderQuant）的新型 PTQ 框架，该框架依赖于一种简单的自适应滑动量化概念，并由少量可学习参数辅助。SliderQuant 的基础组件被称为层间滑动量化，它包含三种新型滑动窗口设计，专门用于解决浅层、中间层和深层各异的量化敏感性。另一个组件被称为层内滑动量化，它利用增量策略来量化每个窗口。因此，SliderQuant 具有强大的跨层降低量化误差的能力。在基础语言生成、零样本常识推理以及具有挑战性的数学和代码任务上，针对包括 Llama/Llama2/Llama3/Qwen2.5 系列、DeepSeek-R1 蒸馏模型和大型 MoE 模型在内的多种 LLM 进行的广泛实验表明，我们的方法在仅权重量化和权重-激活量化方面均优于现有的 PTQ 方法（包括使用旋转变换的最新 PTQ 方法）。

## Abstract
In this paper, we address post-training quantization (PTQ) for large language models (LLMs) from an overlooked perspective: given a pre-trained high-precision LLM, the predominant sequential quantization framework treats different layers equally, but this may be not optimal in challenging bit-width settings. We empirically study the quantization impact of different layers on model accuracy, and observe that: (1) shallow/deep layers are usually more sensitive to quantization than intermediate layers; (2) among shallow/deep layers, the most sensitive one is the first/last layer, which exhibits significantly larger quantization error than others. These empirical observations imply that the quantization design for different layers of LLMs is required on multiple levels instead of a single level shared to all layers. Motivated by this, we propose a new PTQ framework termed Sliding-layer Quantization (SliderQuant) that relies on a simple adaptive sliding quantization concept facilitated by few learnable parameters. The base component of SliderQuant is called inter-layer sliding quantization, which incorporates three types of novel sliding window designs tailored for addressing the varying quantization sensitivity of shallow, intermediate and deep layers. The other component is called intra-layer sliding quantization that leverages an incremental strategy to quantize each window. As a result, SliderQuant has a strong ability to reduce quantization errors across layers. Extensive experiments on basic language generation, zero-shot commonsense reasoning and challenging math and code tasks with various LLMs, including Llama/Llama2/Llama3/Qwen2.5 model families, DeepSeek-R1 distilled models and large MoE models, show that our method outperforms existing PTQ methods (including the latest PTQ methods using rotation transformations) for both weight-only quantization and weight-activation quantization.

---

## 论文详细总结（自动生成）

### SliderQuant: 大语言模型高精度训练后量化论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是大语言模型（LLM）的**训练后量化（PTQ）**技术。研究动机源于一个被长期忽视的观察：现有的顺序量化框架（如 GPTQ、OmniQuant 等）通常对模型的所有层“一视同仁”，采用统一的量化策略。
然而，作者通过实证研究发现，LLM 的不同层对量化的敏感度存在显著差异：
*   **浅层和深层**比中间层更敏感。
*   **第一层和最后一层**是整个模型中最脆弱的环节，量化误差对整体精度的影响远超其他层。
基于此，论文旨在设计一种能够根据层敏感度自适应调整的量化框架，以在极低比特（如 W4A4, W2A16）下保持模型性能。

#### 2. 方法论：核心思想与技术细节
论文提出了 **SliderQuant** 框架，其核心思想是通过“滑动窗口”建立跨层的量化协同效应。主要包含以下技术点：
*   **层间滑动量化（Inter-layer Sliding Quantization）**：
    *   **浅层**：采用“渐进扩张窗口（PESW）”，从第一层开始，窗口逐层扩大，确保第一层在多次优化中作为锚点被反复精炼。
    *   **中间层**：采用“固定大小滑动窗口（FSSW）”，保持稳定的量化频率。
    *   **深层**：采用“渐进收缩窗口（PCSW）”，窗口逐渐缩小至最后一层，强化对输出层特征抽象能力的保护。
*   **层内滑动量化（Intra-layer Sliding Quantization）**：在单个窗口内部，不一次性量化所有参数，而是采用增量策略（如先量化 50% 的权重/激活，再量化整体），通过局部到全局的参数协同降低误差。
*   **优化目标与参数**：结合了**通道缩放（Channel Scaling）**和**低秩适配（LoRA）**。通过学习少量的缩放因子和低秩矩阵来补偿量化损失，这些参数在推理前可被吸收进原始权重，实现**零额外推理开销**。

#### 3. 实验设计
*   **数据集与场景**：
    *   **语言生成**：WikiText2, C4（评估困惑度 PPL）。
    *   **常识推理**：PIQA, ARC, HellaSwag, Winogrande, BoolQ, MMLU。
    *   **复杂推理**：针对 DeepSeek-R1 蒸馏模型，测试了 MATH-500, AIME-2024, GSM8K（数学）及 HumanEval+, MBPP+（代码）。
*   **Benchmark 与对比方法**：
    *   **无额外开销方法**：RTN, GPTQ, AWQ, SmoothQuant, OmniQuant, AffineQuant, CBQ。
    *   **带额外开销（如旋转变换）方法**：QuaRot, SpinQuant, FlatQuant, DuQuant 等。
*   **模型覆盖**：Llama 1/2/3 全系列（7B 到 70B）、Qwen 2.5/3 系列、DeepSeek-R1 蒸馏版（14B/32B）以及 MoE 模型（Qwen3-30B-A3B）。

#### 4. 资源与算力
论文详细记录了实验环境：
*   **GPU 型号**：主要使用 **NVIDIA RTX A6000 (48GB)**。
*   **算力分配**：32B 以下模型单卡运行，65B/70B 模型使用双卡。
*   **训练时长**：以 Llama2-7B 为例，默认设置（20 epochs）在 A6000 上约需 **6.14 小时**。若追求极致效率，10 epochs（约 3.24 小时）即可达到优于 OmniQuant 的效果。
*   **部署测试**：在 RTX 4090 和 A100 上使用 `llama.cpp` 测试了推理速度和显存占用。

#### 5. 实验数量与充分性
实验设计非常充分且具有高度的客观性：
*   **多维度对比**：涵盖了权重量化（W4/W3/W2）和权重-激活联合量化（W4A4）。
*   **消融实验**：针对层间/层内滑动组件、窗口大小（$L_s, L_d$）、增量比例（$\gamma$）、校准样本数等做了详尽测试。
*   **模型多样性**：不仅测试了 Dense 模型，还验证了 MoE 架构和最新的 Reasoning 模型（DeepSeek-R1），证明了方法的普适性。
*   **公平性**：对比时区分了“有无推理额外开销”两种赛道，确保了对比的严谨性。

#### 6. 主要结论与发现
*   **性能领先**：SliderQuant 在所有测试配置下均优于现有 SOTA 方法。特别是在 W4A4 极低比特下，其 PPL 显著低于 OmniQuant 和 CBQ。
*   **层敏感度验证**：实验证实了保护首尾层对维持模型逻辑推理能力至关重要。
*   **DeepSeek-R1 适配性**：在 4-bit 权重量化下，SliderQuant 几乎实现了无损量化，保留了强大的思维链（CoT）能力。
*   **效率平衡**：虽然涉及迭代优化，但通过合理的窗口设计，其收敛速度快，在较短时间内即可获得高质量量化模型。

#### 7. 优点：亮点总结
*   **视角独特**：敏锐捕捉到 LLM 物理结构中不同层的“地位”差异，打破了 PTQ 中“层平等”的思维定式。
*   **架构优雅**：滑动窗口机制既建立了层间联系，又通过自适应设计解决了敏感度问题，且最终模型不增加推理延迟。
*   **泛化性强**：在数学、代码等高精度要求的任务上表现惊人，证明了该方法能有效保留模型的深层认知能力。

#### 8. 不足与局限
*   **量化耗时**：虽然比部分迭代方法快，但相比 GPTQ 这种基于闭式解的非迭代方法，SliderQuant 仍需要数小时的 GPU 优化时间，在大规模模型量化时存在时间成本。
*   **超参数依赖**：虽然论文给出了默认配置，但针对不同架构（如超深模型或特殊 MoE），可能仍需微调窗口大小（$L_s, L_d$）以达到最优效果。
*   **硬件适配**：虽然推理无额外开销，但 W4A4 等格式仍依赖底层算子库（如 `llama.cpp` 或 `TensorRT-LLM`）的支持才能发挥实际加速效果。

（完）
