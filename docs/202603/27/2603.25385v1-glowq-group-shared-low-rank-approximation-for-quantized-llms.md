---
title: "GlowQ: Group-Shared LOw-Rank Approximation for Quantized LLMs"
title_zh: GlowQ：针对量化大语言模型的组共享低秩近似
authors: "Selim An, Il hong Suh, Yeseong Kim"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25385v1"
tags: ["query:llm"]
score: 8.0
evidence: 量化大模型的低秩近似以降低延迟
tldr: 针对低比特量化大语言模型精度下降及现有低秩修正方法带来的高延迟和内存开销问题，本文提出 GlowQ。该方法通过在输入共享组内共享低秩修正模块的右因子，并仅对高收益层进行选择性恢复，显著降低了计算和存储开销。实验表明，GlowQ 在提升模型精度的同时，有效减少了首字延迟并提高了推理吞吐量，为高效部署量化 LLM 提供了新方案。
selection_source: fresh_fetch
motivation: 现有的低秩修正方法在每个解码器块中插入冗余模块，导致量化模型推理时的延迟和内存开销显著增加。
method: 提出组共享低秩近似技术，在共享输入的模块组内复用投影矩阵，并利用选择性恢复策略仅优化高收益层。
result: "GlowQ 在提升下游任务准确率的同时，平均降低了 5.6% 的首字延迟并提升了 9.6% 的吞吐量，其变体 GlowQ-S 性能提升更显著。"
conclusion: 通过组共享和选择性低秩修正，可以在保持量化模型精度的前提下，大幅优化大语言模型的推理效率和资源占用。
---

## 摘要
量化技术（如 BitsAndBytes、AWQ 和 GPTQ）被广泛用作部署大语言模型的标准方法，但在使用低比特表示（例如 4 比特）时通常会降低准确率。低秩修正方法（如 LQER、QERA、ASER）已被提出以缓解这一问题，然而，它们会恢复所有层并在每个解码器块中插入误差修正模块，这增加了延迟和内存开销。为了解决这一局限性，我们提出了 GlowQ，一种针对量化大语言模型的组共享低秩近似方法，它为每个输入共享组缓存一个共享的右因子，并仅恢复能产生最高准确率收益的组或层。GlowQ 为每个输入共享组计算一次高精度投影，并在其模块间重用，从而减少了参数和内存开销，同时保留了层特定修正的表达能力。我们还提出了一种选择性变体 GlowQ-S，它仅在能提供最大收益的地方应用缓存的共享模块。与强基准模型相比，我们的方法平均将首字延迟（TTFB）降低了 5.6%，吞吐量提高了 9.6%，同时将 WikiText-2 上的困惑度降低了 0.17%，并将下游任务准确率提高了 0.42 个百分点。选择性模型 GlowQ-S 进一步降低了延迟，将 TTFB 削减了 23.4%，吞吐量提高了 37.4%，同时平均准确率保持在 0.2 个百分点以内。

## Abstract
Quantization techniques such as BitsAndBytes, AWQ, and GPTQ are widely used as a standard method in deploying large language models but often degrades accuracy when using low-bit representations, e.g., 4 bits. Low-rank correction methods (e.g., LQER, QERA, ASER) has been proposed to mitigate this issue, however, they restore all layers and insert error-correction modules into every decoder block, which increases latency and memory overhead. To address this limitation, we propose GlowQ, a group-shared low-rank approximation for quantized LLMs that caches a single shared right factor per input-sharing group and restores only the groups or layers that yield the highest accuracy benefit. GlowQ computes the high-precision projection once per input-sharing group and reuses it across its modules, reducing parameter and memory overhead, and retaining the expressivity of layer-specific corrections. We also propose a selective variant, GlowQ-S, that applies the cached shared module only where it provides the largest benefit. Compared with strong baselines, our approach reduces TTFB by (5.6%) and increases throughput by (9.6%) on average, while reducing perplexity on WikiText-2 by (0.17%) and increasing downstream accuracy by 0.42 percentage points. The selective model GlowQ-S further reduces latency, cutting TTFB by (23.4%) and increasing throughput by (37.4%), while maintaining accuracy within 0.2 percentage points on average.

---

## 论文详细总结（自动生成）

以下是对论文《GlowQ: Group-Shared LOw-Rank Approximation for Quantized LLMs》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）部署面临内存和带宽瓶颈。虽然 4-bit 量化（PTQ）能缓解压力，但会导致精度下降。
*   **现有痛点**：目前的低秩修正方法（如 LQER、QERA）为每一层独立添加修正模块（$W \approx W_q + AB$）。这导致：
    1.  **计算冗余**：对共享相同输入张量的模块（如 Attention 中的 Q/K/V）重复进行高精度投影计算。
    2.  **开销巨大**：在每一层插入模块会显著增加推理延迟（TTFB）和显存占用。
    3.  **忽视各向异性**：现有方法往往忽略了激活值的强各向异性（Anisotropy），导致低秩空间分配效率低下。

### 2. 方法论
*   **核心思想**：提出**组共享低秩近似（Group-Shared Low-Rank Approximation）**。将共享输入的模块划分为组，在组内共享一个右因子（Right Factor）$B$，而保留模块特有的左因子 $A_i$。
*   **关键技术细节**：
    *   **组级共享 B**：对于共享输入 $X$ 的模块组（如 Q/K/V 组、MLP 的 Gate/Up 组），仅计算一次 $R = B_{shared}X$ 并缓存，各模块通过 $A_i R$ 进行修正。
    *   **数据感知协方差对齐**：利用输入激活值的协方差矩阵 $\Sigma_x$ 进行“白化”处理，使学习到的共享子空间向数据偏好的方向对齐，解决激活值分布不均的问题。
    *   **QR 缩减的随机 SVD（QR-reduced RSVD）**：为了处理大规模矩阵，先通过 QR 分解将高瘦的误差矩阵压缩为 $d \times d$ 的核心矩阵，再应用随机 SVD 快速提取主成分，提高计算效率和数值稳定性。
    *   **选择性恢复（GlowQ-S）**：基于能量捕获分数或归一化误差比率对各层/组进行重要性排序，仅在收益最高的层应用修正，以进一步降低延迟。

### 3. 实验设计
*   **数据集**：WikiText-2、C4（困惑度测试）；ARC、PIQA、HellaSwag、WinoGrande、BoolQ、LAMBADA（零样本准确率）；LongBench（长文本能力）。
*   **模型覆盖**：LLaMA 2/3/3.1/3.2、Qwen 2.5/3/1.5-MoE、OPT、Mistral 等，参数量涵盖 1.3B 到 30B。
*   **Benchmark 与对比方法**：
    *   **量化基准**：BitsAndBytes (NF4)、AWQ、GPTQ。
    *   **修正基准**：ZeroQuant-V2、QERA、L2QER（层独立修正方法）。
*   **场景**：主要针对 W4A16（4位权重，16位激活），也测试了 W4A4 和 W4A8。

### 4. 资源与算力
*   **硬件使用**：
    *   **离线校准/分解**：使用 NVIDIA A100 (80GB) GPU。
    *   **推理测试**：在 NVIDIA RTX 4090 GPU 上测量延迟和吞吐量。
*   **算力开销**：文中提到对于 30B 模型，校准过程（计算协方差和误差张量）大约需要几个 GPU 小时，而 SVD 分解步骤由于采用了 QR 缩减技术，耗时极短（每层平均仅需 0.09 秒）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，涵盖了 10 余种模型变体、多个下游任务、长文本评估以及 MoE 架构。
*   **消融实验**：针对秩（Rank）的大小、校准样本数、协方差收缩因子、RSVD 参数（过采样和幂迭代次数）以及不同的选择性恢复指标进行了深入分析。
*   **客观性**：对比实验在相同的校准数据和量化配置下进行，且涵盖了多种主流开源模型家族，实验结果具有较强的说服力和公平性。

### 6. 主要结论与发现
*   **性能提升**：GlowQ 在 WikiText-2 上平均降低困惑度 0.17%，下游任务准确率提升 0.42%。
*   **效率增益**：相比层独立修正，GlowQ 平均降低 TTFB 5.6%，提升吞吐量 9.6%。
*   **极致加速**：GlowQ-S（选择性恢复）在保持精度损失极小（<0.2%）的前提下，将 TTFB 降低 23.4%，吞吐量提升 37.4%。
*   **MoE 兼容性**：在 MoE 模型上，GlowQ 能以极小的精度代价减少约 63% 的修正参数显存占用。

### 7. 优点
*   **算法创新**：通过数学证明确认了共享右因子在最小二乘意义下的最优性，并结合协方差白化解决了数据分布不均的问题。
*   **工程实用**：缓存 $BX$ 的设计直接减少了推理时的冗余矩阵乘法，对实际部署非常友好。
*   **高度通用**：不依赖特定的量化器，可作为插件应用于 GPTQ、AWQ 等多种 PTQ 流程，且支持多种数据类型（INT, MXFP, NVFP）。

### 8. 不足与局限
*   **校准依赖**：仍需要少量的离线校准数据（如 64 条序列）来估计协方差，无法做到完全的数据无关。
*   **超参数敏感性**：GlowQ-S 的效果依赖于针对不同模型家族选择合适的重要性指标和恢复比例，这可能需要一定的先验经验。
*   **硬件优化限制**：虽然算法减少了计算量，但实际加速效果高度依赖于底层 CUDA Kernel 对低秩路径的优化程度。

（完）
