---
title: "How Pruning Reshapes Features: Sparse Autoencoder Analysis of Weight-Pruned Language Models"
title_zh: 剪枝如何重塑特征：权重剪枝语言模型的稀疏自编码器分析
authors: "Hector Borobia, Elies Seguí-Mas, Guillermina Tormo-Carbó"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25325v1"
tags: ["query:llm"]
score: 9.0
evidence: 非结构化权重剪枝语言模型的分析
tldr: 本研究利用稀疏自编码器（SAEs）首次系统分析了非结构化权重剪枝如何重塑语言模型的特征几何。通过对Gemma和Llama系列模型在不同剪枝方法（幅值和Wanda）及稀疏度下的实验，发现了一个反直觉现象：低频稀疏特征比高频特征更易存活。研究还证明了Wanda剪枝的优越性，并揭示了特征几何存活与因果重要性之间的脱节，为压缩模型的解释性提供了新见解。
selection_source: fresh_fetch
motivation: 尽管权重剪枝被广泛用于模型压缩，但其对模型内部学习到的特征表示及几何结构的影响仍缺乏深入理解。
method: 采用稀疏自编码器（SAEs）作为探测工具，在多种模型家族和剪枝算法下，从特征存活、迁移性和因果相关性等维度进行系统评估。
result: 实验表明稀有特征在剪枝后存活率极高，且Wanda剪枝在维持特征结构上比幅值剪枝效果好高达3.7倍。
conclusion: 剪枝表现出隐式的特征选择倾向，优先保留专业化稀有特征，且特征的几何稳定性并不等同于其对模型输出的因果贡献。
---

## 摘要
权重剪枝是压缩大型语言模型的标准技术，但其对学习到的内部表示的影响仍不为人所知。我们利用稀疏自编码器（SAEs）作为可解释性探针，首次系统地研究了非结构化剪枝如何重塑语言模型的特征几何。在三个模型系列（Gemma 3 1B、Gemma 2 2B、Llama 3.2 1B）、两种剪枝方法（量级剪枝和 Wanda）以及六个稀疏度水平（0-60%）中，我们调查了涵盖种子稳定性、特征存活、SAE 可迁移性、特征脆弱性和因果相关性的五个研究问题。我们最引人注目的发现是，稀有的 SAE 特征（即激活率较低的特征）在剪枝后的存活情况远好于频繁特征，在 17 个实验条件中的 11 个中，条件内 Spearman 相关系数 rho = -1.0。这一违反直觉的结果表明，剪枝起到了隐式特征选择的作用，优先破坏高频通用特征，同时保留专门的稀有特征。我们进一步表明，Wanda 剪枝在保留特征结构方面比量级剪枝好 3.7 倍，预训练的 SAE 在稀疏度高达 50% 的 Wanda 剪枝模型上仍然有效，且几何特征存活并不能预测因果重要性——这种解耦对压缩下的可解释性具有重要意义。

## Abstract
Weight pruning is a standard technique for compressing large language models, yet its effect on learned internal representations remains poorly understood. We present the first systematic study of how unstructured pruning reshapes the feature geometry of language models, using Sparse Autoencoders (SAEs) as interpretability probes. Across three model families (Gemma 3 1B, Gemma 2 2B, Llama 3.2 1B), two pruning methods (magnitude and Wanda), and six sparsity levels (0--60%), we investigate five research questions spanning seed stability, feature survival, SAE transferability, feature fragility, and causal relevance. Our most striking finding is that rare SAE features--those with low firing rates--survive pruning far better than frequent ones, with within-condition Spearman correlations of rho = -1.0 in 11 of 17 experimental conditions. This counter-intuitive result suggests that pruning acts as implicit feature selection, preferentially destroying high-frequency generic features while preserving specialized rare ones. We further show that Wanda pruning preserves feature structure up to 3.7x better than magnitude pruning, that pre-trained SAEs remain viable on Wanda-pruned models up to 50% sparsity, and that geometric feature survival does not predict causal importance--a dissociation with implications for interpretability under compression.

---

## 论文详细总结（自动生成）

这篇论文《How Pruning Reshapes Features: Sparse Autoencoder Analysis of Weight-Pruned Language Models》对权重剪枝如何影响大语言模型（LLM）内部特征表示进行了首次系统性研究。以下是详细的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：权重剪枝是压缩 LLM 的标准手段，但其对模型内部学习到的“特征几何”（Feature Geometry）的影响尚不明确。
*   **核心问题**：当模型被剪枝时，其内部特征是消失了、改变了还是保持不变？现有的可解释性工具（如 SAE）在剪枝模型上是否依然有效？
*   **研究意义**：通过理解剪枝对特征的影响，可以为开发更具解释性的压缩算法提供指导，并帮助开发者判断在部署压缩模型时是否需要重新训练解释性工具。

### 2. 方法论
*   **核心思想**：利用**稀疏自编码器（SAE）**作为探针，将模型的稠密激活向量分解为稀疏且可解释的特征方向，通过对比稠密模型和剪枝模型提取出的特征字典来量化特征的变化。
*   **关键技术细节**：
    *   **SAE 架构**：采用 **TopK SAE**，通过保留前 $k$ 个最大的激活值来实现稀疏性，避免了传统 L1 正则化的收缩偏差。
    *   **特征匹配指标**：使用**互近邻（MNN）率**作为主要指标，即两个字典中的特征互为对方在余弦相似度下的最邻近，且相似度需超过阈值 $\tau$（通常设为 0.7）。
    *   **剪枝方法**：对比了**全局幅值剪枝（Magnitude Pruning）**和**Wanda 剪枝**（考虑权重大小与输入激活范数的乘积）。
    *   **因果消融协议**：通过将特定特征的激活值置零，观察模型输出分布的 KL 散度变化，以评估特征的因果重要性。

### 3. 实验设计
*   **模型与数据集**：
    *   **模型家族**：Gemma 3 1B、Gemma 2 2B、Llama 3.2 1B。
    *   **数据集**：使用 FineWeb-Edu 进行 SAE 训练和剪枝校准，使用 WikiText-2 评估模型困惑度（PPL）。
*   **实验场景**：设置了 0% 到 60% 的六个稀疏度梯度。
*   **对比基准**：以未剪枝的稠密模型（Dense Model）作为基准，对比不同剪枝算法（Magnitude vs. Wanda）在相同稀疏度下的表现。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量。
*   **训练规模**：共完成了 22 组成功的实验运行，每组运行包含 3-5 个不同的随机种子，总计训练了约 80 个 SAE。每个 SAE 训练 20,000 步，批大小为 2,048–4,096。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个模型家族、2 种剪枝方法、6 个稀疏度水平，并进行了跨层（浅层、中层、深层）的验证。
*   **充分性与客观性**：实验设计非常充分。通过多模型验证确保了结论的普适性；通过多随机种子分析了 SAE 的种子稳定性（Seed Stability），证明了尽管单个特征具有随机性，但特征群体的统计行为是稳定的。

### 6. 主要结论与发现
*   **稀有特征更易存活（核心发现）**：激活频率较低的“稀有特征”在剪枝后的存活率远高于高频特征。在多数条件下，激活率与存活率呈完美的负相关（Spearman $\rho = -1.0$）。这表明剪枝具有隐式的特征选择作用，优先破坏通用特征而保留专业化特征。
*   **Wanda 优于幅值剪枝**：在相同稀疏度下，Wanda 保留特征结构的能力比幅值剪枝高出 3.7 倍。
*   **SAE 的可迁移性**：在 Wanda 剪枝模型上，即使稀疏度达到 50%，直接使用为稠密模型训练的官方 SAE 依然能保持极低的重构误差（FVU）。
*   **几何与因果的脱节**：在几何上保持稳定的特征（Robust features）与被破坏的特征（Fragile features）在因果重要性上没有显著差异。

### 7. 优点
*   **视角新颖**：首次将 SAE 引入剪枝研究，从机械解释性角度揭示了模型压缩的内在逻辑。
*   **反直觉的发现**：揭示了稀有特征的鲁棒性，挑战了“高频特征更重要”的直觉假设。
*   **实用性强**：提出了基于激活率的剪枝前诊断工具（逻辑回归模型），预测特征存活率的 AUC 超过 0.79。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 1B-2B 参数的模型上，尚不确定相关结论是否能外推至 70B 以上的超大规模模型。
*   **上下文长度**：实验使用的上下文长度较短（256 tokens），可能影响了对长程依赖特征的捕捉。
*   **剪枝方式单一**：仅研究了一次性（One-shot）剪枝，未涉及剪枝后的微调（Fine-tuning）或迭代剪枝，后者可能会导致特征的重新组织。
*   **数值稳定性**：在深层网络（如 Gemma 3 的第 18 层）出现了 float16 溢出导致实验失败的问题。

（完）
