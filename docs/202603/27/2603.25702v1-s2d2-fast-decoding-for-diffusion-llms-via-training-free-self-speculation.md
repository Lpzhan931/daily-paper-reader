---
title: "S2D2: Fast Decoding for Diffusion LLMs via Training-Free Self-Speculation"
title_zh: S2D2：通过免训练自投机实现扩散大语言模型的快速解码
authors: "Ligong Han, Hao Wang, Han Gao, Kai Xu, Akash Srivastava"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25702v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对块扩散大语言模型的免训练自投机解码
tldr: 针对块扩散语言模型在少步数解码时质量与速度难以兼得的问题，本文提出S2D2框架。该方法利用预训练模型在块大小为1时退化为自回归模型的特性，实现无需额外训练的自投机解码。通过扩散并行生成与自回归序列校验的结合，S2D2在保持或提升生成质量的同时，显著提升了解码速度，为高效非自回归生成提供了新方案。
selection_source: fresh_fetch
motivation: 现有的块扩散模型在加速解码时，其置信度阈值机制难以平衡生成质量与效率，且现有改进方案往往需要额外的训练成本。
method: 利用同一预训练模型在不同块大小下的特性，将其同时作为扩散生成器和自回归校验器，通过自投机校验和轻量级路由策略优化解码过程。
result: 在多个模型族上，S2D2相比自回归解码实现了高达4.7倍的加速，且在提升速度的同时改善了生成准确率。
conclusion: S2D2证明了无需额外训练的自投机机制能有效优化扩散语言模型的解码权衡，是提升非自回归模型实用性的重要进展。
---

## 摘要
块扩散语言模型通过结合块级自回归解码与块内并行去噪，为实现快于自回归生成的路径提供了前景。然而，在实际加速所需的少步数（few-step）机制下，标准的置信度阈值解码通常较为脆弱：激进的阈值会损害质量，而保守的阈值则需要不必要的去噪步骤。解决这一问题的现有方法要么需要额外的训练，要么会产生额外的测试时计算开销。我们提出了 S2D2，一种用于块扩散语言模型的免训练自投机解码框架。我们的核心观察是，当块大小减小为 1 时，块扩散模型会变为自回归模型，从而允许同一个预训练模型同时充当起草者（drafter）和验证者（verifier）。S2D2 在标准的块扩散解码中插入了一个投机验证步骤，并使用轻量级路由策略来决定何时验证是值得其成本的。这产生了一种混合解码轨迹，其中扩散模式并行地提出 token，而自回归模式则充当局部序列级评论者（critic）。在三个主流的块扩散模型系列中，S2D2 在准确率与速度的权衡上始终优于强置信度阈值基准。在 SDAR 上，我们观察到相比自回归解码最高达 4.7 倍的加速，相比调优后的动态解码基准最高达 1.57 倍的加速，同时准确率提升了高达 4.5 个百分点。在 LLaDA2.1-Mini 上，S2D2 与内置的自纠正功能保持互补，包括在一种保守设置下，其速度比静态基准快 4.4 倍，且准确率略高。

## Abstract
Block-diffusion language models offer a promising path toward faster-than-autoregressive generation by combining block-wise autoregressive decoding with within-block parallel denoising. However, in the few-step regime needed for practical acceleration, standard confidence-thresholded decoding is often brittle: aggressive thresholds hurt quality, while conservative thresholds require unnecessary denoising steps. Existing approaches that address this issue either require additional training or incur extra test-time compute. We present S2D2, a training-free self-speculative decoding framework for block-diffusion language models. Our key observation is that a block-diffusion model becomes autoregressive when the block size is reduced to one, allowing the same pretrained model to act as both drafter and verifier. S2D2 inserts a speculative verification step into standard block-diffusion decoding and uses lightweight routing policies to decide when verification is worth its cost. This yields a hybrid decoding trajectory in which diffusion proposes tokens in parallel, while the autoregressive mode acts as a local sequence-level critic. Across three mainstream block-diffusion families, S2D2 consistently improves the accuracy-speed tradeoff over strong confidence-thresholding baselines. On SDAR, we observe up to $4.7\times$ speedup over autoregressive decoding, and up to $1.57\times$ over a tuned dynamic decoding baseline while improving accuracy by up to $4.5$ points. On LLaDA2.1-Mini, S2D2 remains complementary to built-in self-correction, including a conservative setting where it is $4.4\times$ faster than the static baseline with slightly higher accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：S2D2：通过免训练自投机实现扩散大语言模型的快速解码

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：扩散大语言模型（Diffusion LLMs）通过块级并行去噪提供了超越自回归（AR）生成的潜力。然而，在实际应用所需的“少步数”解码场景下，传统的置信度阈值解码机制表现不稳定：阈值过高会导致解码步数过多，阈值过低则会严重损害生成质量。
*   **核心问题**：如何在不进行额外模型训练、不引入辅助模型且不显著增加计算开销的前提下，提升块扩散模型的解码速度与准确率？
*   **整体含义**：本文提出了 **S2D2** 框架，利用块扩散模型在块大小为1时等效于自回归模型的特性，实现“自投机”解码，即用同一个模型同时担任“起草者”和“验证者”。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：将标准块扩散解码作为**起草者（Drafter）**并行提出多个 token，随后将同一模型切换至块大小为1的自回归模式作为**验证者（Verifier）**，对提出的 token 进行序列级校验。
*   **关键技术细节**：
    *   **自验证模式（Self-Verification Mode）**：对于位置对齐的模型（如 LLaDA），使用“2L trick”构造特殊的注意力掩码，在单次前向传播中并行计算出所有待验证 token 的自回归概率。
    *   **拒绝采样（Rejection Sampling）**：利用验证者概率与起草者概率的比值进行拒绝采样，确保局部序列的逻辑连贯性，并在拒绝处进行残差重采样。
    *   **轻量级路由策略（Verification Routing Policies）**：由于验证步骤会增加一次前向传播开销，S2D2 引入了多种路由策略（如最小跨度策略、得分阈值策略、迟滞策略等），仅在预期收益（接受 token 数）较高时才激活验证。
    *   **AR 引导的能量修正**：从理论上将该机制解释为一种随机贪婪的局部能量修正，优化了扩散生成的轨迹。

#### 3. 实验设计
*   **实验对象**：涵盖三个主流块扩散模型家族的五款模型：**SDAR** (1.7B/4B/8B)、**Fast-dLLM v2**、**LLaDA 2.1-Mini**。
*   **数据集/场景**：
    *   数学推理：**GSM8K**。
    *   代码生成：**MBPP**、**HumanEval**。
    *   指令遵循：**IFEval**。
*   **Benchmark 与对比方法**：
    *   基准：标准的自回归解码（Block Size = 1）。
    *   对比方法：标准的块扩散解码（BD3），包括静态置信度阈值和动态置信度阈值（Dynamic Thresholding）方案。

#### 4. 资源与算力
*   **训练算力**：由于该方法是**免训练（Training-Free）**的，因此不需要额外的训练算力。
*   **推理算力**：论文未明确说明具体的 GPU 型号和数量，但通过“墙钟时间（Wall Clock Time）”和加速比（Speedup）来衡量效率。实验重点在于展示 S2D2 如何在相同的硬件条件下通过减少解码步数来节省总计算量。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同规模和架构的模型上进行了测试，覆盖了数学、代码和通用指令三大任务。
*   **消融实验**：非常充分。作者对 token 接受率估计器、四种不同的路由策略、拒绝采样比例的平滑因子等进行了详细的消融研究。
*   **客观性与公平性**：通过对比不同块大小（B）和去噪步数（S）下的准确率-速度曲线（Pareto Frontier），证明了 S2D2 在多种配置下均优于强基准线，实验设计较为客观。

#### 6. 主要结论与发现
*   **性能提升**：在 SDAR-1.7B 上，相比自回归解码实现了 **4.7倍** 的加速；相比调优后的动态解码基准，在提速 **1.57倍** 的同时，准确率提升了 **4.5个百分点**。
*   **互补性**：在 LLaDA 2.1 上，S2D2 与模型内置的“标记编辑（Token Editing）”自纠正机制互补，在保守设置下比静态基准快 **4.4倍**。
*   **稳定性**：S2D2 在大块尺寸（Large Block Size）下表现尤为出色，有效缓解了标准扩散解码在长块时准确率急剧下降的问题。

#### 7. 优点
*   **免训练且即插即用**：无需微调或蒸馏，直接适用于现有的预训练块扩散模型。
*   **架构通用性**：同时支持位置对齐（Position-aligned）和右移（Right-shifted）两种扩散模型架构。
*   **效率权衡优化**：通过路由策略巧妙地平衡了“验证开销”与“接受增益”，实现了更优的准确率-速度边界。

#### 8. 不足与局限
*   **局部验证限制**：目前的实现仅验证“第一个连续的掩码跨度”，对于块内非连续的预测 token 缺乏全局校验。
*   **验证开销**：尽管有路由策略，但每次验证仍需额外的一次前向传播，在某些极短序列或模型置信度极高的情况下，加速效果可能受限。
*   **任务依赖性**：实验显示 AR-ness（自回归特性）在不同任务（如数学 vs 代码）中表现不同，这意味着路由策略的超参数可能需要针对特定任务进行微调。

（完）
