---
title: Robust Batch-Level Query Routing for Large Language Models under Cost and Capacity Constraints
title_zh: 成本与容量约束下大语言模型的鲁棒批次级查询路由
authors: "Jelena Markovic-Voronov, Kayhan Behdin, Yuanda Xu, Zhengze Zhou, Zhipeng Wang, Rahul Mazumder"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.26796v1"
tags: ["query:llm"]
score: 9.0
evidence: 容量限制下的批处理级查询路由
tldr: "本研究针对大语言模型在成本、GPU资源和并发限制下的查询路由问题，提出了一种批处理级别（batch-level）且具备资源感知能力的路由框架。该框架通过联合优化每批次的模型分配，并引入鲁棒性机制来应对性能预测的不确定性，同时结合离线实例分配策略。实验证明，该方法在对抗性批处理下比单查询路由准确率提升高达24%，且能严格控制成本和资源消耗。"
selection_source: fresh_fetch
motivation: 现有的单查询路由方法在非均匀或对抗性批处理情况下，难以有效控制批处理级别的成本和模型容量限制。
method: 提出一种联合优化模型分配的批处理级路由框架，包含应对性能预测不确定性的鲁棒变体及离线实例分配程序。
result: "在多任务基准测试中，鲁棒路由使准确率提升1-14%，批处理级路由在对抗环境下优于单查询方法达24%。"
conclusion: 该框架在严格遵守成本和GPU资源约束的同时，显著提升了多模型系统的路由效率和预测准确性。
---

## 摘要
我们研究了在成本、GPU 资源和并发约束下，将查询路由到大语言模型（LLM）的问题。先前的逐查询路由方法通常无法控制批次级成本，特别是在非均匀或对抗性批处理的情况下。为解决这一问题，我们提出了一种批次级、资源感知的路由框架，该框架在遵守成本和模型容量限制的同时，联合优化每个批次的模型分配。我们进一步引入了一种鲁棒变体，以考虑预测的 LLM 性能中的不确定性，并提供了一种离线实例分配程序，用于平衡多个模型之间的质量和吞吐量。在两个多任务 LLM 基准测试上的实验表明，鲁棒性相比非鲁棒对应方法将准确率提高了 1-14%（取决于性能估计器），在对抗性批处理下，批次级路由的性能优于逐查询方法高达 24%，且优化后的实例分配与非优化分配相比产生了高达 3% 的额外收益，同时所有方法都严格控制了成本和 GPU 资源约束。

## Abstract
We study the problem of routing queries to large language models (LLMs) under cost, GPU resources, and concurrency constraints. Prior per-query routing methods often fail to control batch-level cost, especially under non-uniform or adversarial batching. To address this, we propose a batch-level, resource-aware routing framework that jointly optimizes model assignment for each batch while respecting cost and model capacity limits. We further introduce a robust variant that accounts for uncertainty in predicted LLM performance, along with an offline instance allocation procedure that balances quality and throughput across multiple models. Experiments on two multi-task LLM benchmarks show that robustness improves accuracy by 1-14% over non-robust counterparts (depending on the performance estimator), batch-level routing outperforms per-query methods by up to 24% under adversarial batching, and optimized instance allocation yields additional gains of up to 3% compared to a non-optimized allocation, all while strictly controlling cost and GPU resource constraints.

---

## 论文详细总结（自动生成）

以下是对论文《Robust Batch-Level Query Routing for Large Language Models under Cost and Capacity Constraints》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型（LLM）的查询路由（Query Routing）**问题。
*   **研究动机**：现有的路由方法大多是“逐查询（Per-query）”的，即独立决定每个请求去往哪个模型。但在工业界实际部署中，这种方式存在三大缺陷：
    1.  **成本失控**：无法严格遵守批次级别（Batch-level）的预算，容易在难任务集中时出现成本激增。
    2.  **资源忽视**：忽略了硬件（如 GPU 显存、并发数）的物理限制，可能导致某些模型过载。
    3.  **预测不确定性**：路由决策依赖于对模型表现的预测，但预测往往存在误差，逐查询方法容易因“过度自信”导致路由失败。
*   **核心目标**：在满足成本、GPU 资源和并发限制的前提下，通过批次级优化和鲁棒性建模，最大化整体响应质量。

### 2. 方法论
论文提出了一套完整的**资源感知型批次路由框架**：
*   **核心思想**：将路由问题建模为**整数线性规划（ILP）**。不再单独处理请求，而是将一批请求（Batch）作为一个整体进行优化分配。
*   **关键技术细节**：
    1.  **在线批次优化**：目标函数是最大化批次内所有查询的平均预测性能。约束条件包括：总成本不超过预算、每个模型的请求数不超过其“实例数 × 单实例并发上限”。
    2.  **鲁棒路由变体**：为了应对性能预测的不确定性，不直接使用预测均值，而是使用**自助法（Bootstrap Resampling）**计算预测区间的下界（如 10% 分位数）。这种“悲观”策略能确保在最坏情况下系统仍有稳定表现。
    3.  **离线实例分配**：在推理前，通过历史数据优化不同模型（如本地开源模型 vs 云端闭源模型）的 GPU 实例分配数量，平衡质量与吞吐量。
*   **算法流程**：首先通过 XGBoost 或 MIRT 等估计器预测性能，然后使用 SCIP 求解器在毫秒级时间内解出 ILP 最优分配方案。

### 3. 实验设计
*   **数据集**：
    1.  **Dataset 1**：来自 Song et al. (2025) 的基准，包含 20 个 LLM 在 MMLU、MATH 等 8 个任务上的表现。
    2.  **Dataset 2**：来自 RouterBench (2024)，包含 11 个 LLM 在 GSM8K、MT-Bench 等任务上的表现。
*   **Benchmark 与对比方法**：
    *   **性能估计器**：对比了 MIRT（SOTA）、kNN、XGBoost 及其鲁棒版本。
    *   **路由策略**：对比了传统的逐查询路由（Per-query）与本文提出的批次级路由（Batch-level）。
    *   **分配策略**：对比了固定实例分配与优化后的实例分配。

### 4. 资源与算力
*   **推理算力建模**：论文详细设定了不同模型的 GPU 需求（如 405B 模型需 8 张 GPU，8B 模型需 1 张），并设定了总 GPU 预算（如 50-800 张不等）。
*   **求解效率**：使用了 **SCIP 优化套件**。实验显示，对于 50-400 大小的批次，求解时间仅需 **0.09 到 0.4 秒**，证明了该方法在实时系统中的可行性。
*   **训练算力**：文中未详细说明训练性能估计器（如 XGBoost）的具体 GPU 型号和时长，但提到这些轻量级模型的训练和推理开销极低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个大型多任务基准测试，涉及 30 多个不同的 LLM。
*   **维度覆盖**：
    *   测试了不同的批次大小（50, 100, 200, 400）。
    *   对比了随机批次与**对抗性批次**（故意将高难度/高成本请求聚在一起）。
    *   进行了消融实验，验证了鲁棒性分位数（Q5-Q30）的影响。
*   **评价**：实验设计非常充分且具有针对性，特别是引入“对抗性批次”模拟极端流量波动，增强了结论的客观性和说服力。

### 6. 主要结论与发现
*   **鲁棒性至关重要**：引入鲁棒性估计后，准确率比非鲁棒方法提升了 **1-14%**，有效降低了预测误差带来的风险。
*   **批次优化优势明显**：在对抗性流量下，批次级路由比逐查询路由的准确率高出多达 **24%**，且能完美控制成本不超标。
*   **资源分配收益**：通过离线优化 GPU 实例分配，在相同硬件条件下可额外获得约 **3%** 的性能提升。
*   **超越单一模型**：在相同成本/资源约束下，该路由框架的表现优于任何单一的最强模型（如 GPT-4o 或 DeepSeek-V3）。

### 7. 优点（亮点）
*   **极具工程实践价值**：不同于纯学术研究，该论文直接解决了工业界 LLM 部署中“钱不够”和“显存不够”的痛点。
*   **数学严谨性**：利用 ILP 和鲁棒优化理论，将复杂的路由决策转化为成熟的数学优化问题。
*   **模型无关性**：框架不绑定特定的 LLM 或预测器，具有很强的通用性。

### 8. 不足与局限
*   **静态批次假设**：实验基于固定大小的批次，而现实系统是动态流式请求（Dynamic Streaming），可能需要更复杂的滑动窗口优化。
*   **分布偏移风险**：如果线上请求分布与训练路由器的历史数据发生剧烈偏移，性能估计器的准确性会下降。
*   **多轮对话开销**：未深入探讨多轮对话中的上下文切换开销（Context-switching），在频繁切换模型时可能会增加延迟。
*   **静态分配限制**：GPU 实例分配是离线确定的，无法根据瞬时流量自动弹性伸缩。

（完）
