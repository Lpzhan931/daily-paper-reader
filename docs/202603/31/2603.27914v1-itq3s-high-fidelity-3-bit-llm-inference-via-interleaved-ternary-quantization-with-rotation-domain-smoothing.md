---
title: "ITQ3_S: High-Fidelity 3-bit LLM Inference via Interleaved Ternary Quantization with Rotation-Domain Smoothing"
title_zh: ITQ3_S：通过旋转域平滑的交织三值量化实现高保真 3-bit 大语言模型推理
authors: Edward J. Yoon
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27914v1"
tags: ["query:llm"]
score: 9.0
evidence: 大语言模型推理的3比特权重量化
tldr: ITQ3_S 是一种针对大语言模型的新型 3-bit 权重量化方法，旨在解决传统低比特量化中因权重重尾分布和离群值导致的精度损失。该方法通过快速沃尔什-阿达玛变换（FWHT）在旋转域平滑权重分布，使其接近高斯分布，并结合 CUDA 算子优化实现了高效的反量化。实验表明，ITQ3_S 在保持接近 FP16 精度水平的同时，在消费级显卡上显著提升了推理吞吐量。
selection_source: fresh_fetch
motivation: 传统 3-bit 量化方法难以处理权重分布中的重尾现象和通道离群值，导致模型推理精度出现灾难性下降。
method: 采用基于 FWHT 的旋转域自适应量化策略平滑权重，并设计了融合 256 点逆变换的 CUDA 共享内存加载机制以确保高保真反量化。
result: 在 NVIDIA RTX 5090 上，ITQ3_S 的困惑度表现接近 FP16 基准，且推理吞吐量超过 4-bit 量化方案的 1.5 倍。
conclusion: ITQ3_S 为在消费级硬件上实现高保真、高性能的大语言模型部署提供了一种数学严谨且实践可行的 3-bit 量化解决方案。
---

## 摘要
我们提出了 ITQ3_S（Interleaved Ternary Quantization -- Specialized），这是一种针对大语言模型（LLM）的新型 3-bit 权重量化格式，它集成了 TurboQuant (TQ)——一种基于快速沃尔什-阿达玛变换（FWHT）的旋转域自适应量化策略。传统的 3-bit 量化方法由于重尾权重分布和通道间离群值的影响，往往会遭受灾难性的精度损失。ITQ3_S 通过在量化前利用 FWHT 对权重空间进行预旋转，有效地将离群值能量分散到整个向量中，并诱导出一种适用于均匀三值编码的近高斯分布，从而解决了这一根本限制。关键在于，我们推导出了一个数学上严谨的反量化过程，该过程使用融合到 CUDA 共享内存加载阶段的 256 点逆沃尔什-阿达玛变换来精确反转 FWHT，确保了离线量化与在线推理之间的零误差往返保真度。我们证明，对于由我们的流水线处理的任何权重向量 $\mathbf{w} \in \mathbb{R}^{256}$，其重构满足 $\|\hat{\mathbf{w}} - \mathbf{w}\|_2 \leq ε_q$，其中 $ε_q$ 仅由三值量化网格决定，并且在相同的位预算约束下严格小于任何均匀 3-bit 基准。实验表明，在 NVIDIA RTX 5090（Blackwell 架构）上，得益于交织内存布局中优化的 DP4A 和 Tensor Core 调度，ITQ3_S 在实现与 FP16 基准相当的困惑度的同时，其吞吐量超过了 4-bit 替代方案的 1.5 倍。我们的研究结果证明 ITQ3_S 是一种在消费级硬件上实现高保真 LLM 部署的实用且具有数学依据的解决方案。

## Abstract
We present \textbf{ITQ3\_S} (Interleaved Ternary Quantization -- Specialized), a novel 3-bit weight quantization format for large language models (LLMs) that integrates \textbf{TurboQuant (TQ)}, a rotation-domain adaptive quantization strategy based on the Fast Walsh-Hadamard Transform (FWHT). Conventional 3-bit quantization methods suffer from catastrophic precision loss caused by heavy-tailed weight distributions and inter-channel outliers. ITQ3\_S addresses this fundamental limitation by pre-rotating the weight space via FWHT prior to quantization, effectively spreading outlier energy across the entire vector and inducing a near-Gaussian distribution amenable to uniform ternary coding.   Critically, we derive a mathematically rigorous dequantization procedure that inverts the FWHT exactly using a 256-point Inverse Walsh-Hadamard Transform fused into the CUDA shared-memory loading stage, ensuring zero-error round-trip fidelity between offline quantization and online inference. We prove that for any weight vector $\mathbf{w} \in \mathbb{R}^{256}$ processed by our pipeline, the reconstruction satisfies $\|\hat{\mathbf{w}} - \mathbf{w}\|_2 \leq ε_q$, where $ε_q$ is determined solely by the ternary quantization grid and is strictly smaller than any uniform 3-bit baseline under equal bit-budget constraints.   Empirically, on the NVIDIA RTX 5090 (Blackwell architecture), ITQ3\_S achieves perplexity competitive with FP16 baselines while delivering throughput exceeding 1.5$\times$ that of 4-bit alternatives, owing to optimized DP4A and Tensor Core scheduling in the interleaved memory layout. Our results establish ITQ3\_S as a practical, mathematically grounded solution for high-fidelity LLM deployment on consumer-grade hardware.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ITQ3_S** 的新型 3-bit 权重量化方案，旨在解决大语言模型（LLM）在极低比特（sub-4-bit）压缩下的精度崩塌问题，并实现在消费级显卡上的高效推理。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 的参数量巨大（如 70B 模型），在 FP16 精度下超出了消费级 GPU 的显存容量。虽然量化可以减小体积，但 **3-bit 被认为是 LLM 逻辑能力的“崩溃点”**。
*   **研究动机**：传统的 3-bit 量化难以处理权重分布中的“重尾（Heavy-tailed）”现象和“通道间离群值（Outliers）”。这些极少数的大值强迫量化器扩大范围，导致大部分权重的精度被稀释。
*   **整体含义**：通过数学变换（旋转域平滑）将异常值能量分散，使权重分布趋于高斯分布，从而利用三值编码（Ternary Coding）实现高保真的 3-bit 推理。

### 2. 核心方法论
ITQ3_S 的核心由三部分组成：
*   **旋转域平滑（FWHT 预处理）**：在量化前，对权重向量应用**快速沃尔什-阿达玛变换（FWHT）**。根据中心极限定理，这种确定性的正交变换能将结构化的离群值能量均匀分散到整个向量中，使变换后的权重呈现近高斯分布，极大地降低了量化误差的上界。
*   **交织三值编码（Interleaved Ternary Coding）**：
    *   将权重限制在 $\{-1, 0, 1\}$ 的三值网格上。
    *   通过交织方式将 3-bit 数据打包进 32-bit 字中，优化了 NVIDIA GPU 的 DP4A 指令吞吐量。
    *   每个 256 元素的块共享缩放因子（Scale）和零点（Zero-point），实际位宽约为 3.125 bits/weight。
*   **TurboQuant 融合算子**：设计了一个高效的 CUDA 内核，将 **256 点逆 FWHT（IFWHT）** 直接融合进共享内存加载阶段。这意味着反量化过程在 GPU 内部完成，不增加额外的显存访问开销。

### 3. 实验设计
*   **模型覆盖**：LLaMA-3 (8B, 70B), Mistral 7B v0.3, Qwen2.5 32B。
*   **数据集**：WikiText-2 和 C4 困惑度（Perplexity）评估。
*   **基准测试（Benchmark）**：
    *   **精度对比**：FP16（基准）、Q8_0、Q4_K_M (GGUF)、IQ3_S (llama.cpp)、QuIP#-3bit。
    *   **性能对比**：推理吞吐量（tokens/sec），包括 Prefill（预填充）和 Decode（解码）阶段。
*   **硬件平台**：NVIDIA RTX 5090 (Blackwell 架构，32GB GDDR7)。

### 4. 资源与算力
*   **硬件使用**：明确提到了使用 **NVIDIA RTX 5090**。
*   **算力细节**：论文侧重于推理优化，未详细说明离线量化过程的具体训练时长，但指出 FWHT 是 $O(n \log n)$ 复杂度，量化过程是确定性的，不需要昂贵的二阶 Hessian 计算（如 GPTQ 所需）。
*   **显存占用**：展示了 70B 模型在 3.125-bit 下仅需约 27.3 GiB 显存，可单卡运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 7B 到 70B 不同规模的主流开源模型。
    *   进行了 **消融实验**，测试了 FWHT 块大小（32 到 512）对精度和开销的影响，确定 256 为最优平衡点。
*   **充分性评价**：实验设计较为充分，对比了当前最先进的旋转量化方案（如 QuIP#）和主流推理框架（llama.cpp）的实现。对比维度涵盖了精度、速度和显存，具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **精度突破**：ITQ3_S 在 3-bit 下的困惑度显著优于 IQ3_S，将与 FP16 的差距缩小了 57%。
*   **性能优异**：在 RTX 5090 上，ITQ3_S 的解码速度达到 960 tok/s，预填充速度甚至超过了 4-bit 方案，证明了融合算子的有效性。
*   **单卡运行 70B**：首次展示了 70B 级别模型在单块消费级 GPU 上以全吞吐量运行，且保留了较高的逻辑推理能力。

### 7. 优点与亮点
*   **数学严谨性**：利用 FWHT 的等距性质（Isometry）证明了重构误差界限，理论基础扎实。
*   **硬件感知设计**：算子针对 Blackwell 架构的共享内存和 Tensor Core 进行了深度优化，而非纯理论研究。
*   **确定性**：相比 QuIP# 使用随机旋转，ITQ3_S 使用确定性变换，无需存储随机种子，降低了推理延迟。

### 8. 不足与局限
*   **激活量化缺失**：目前仅针对权重量化，未处理激活值量化，在长文本推理时 KV Cache 仍可能成为瓶颈。
*   **架构限制**：FWHT 要求块大小为 2 的幂次方，对于隐藏层维度不是 256 倍数的特殊模型，需要额外的填充（Padding）策略。
*   **训练感知缺失**：目前属于后训练量化（PTQ），如果能结合量化感知训练（QAT），精度可能进一步提升。

（完）
