---
title: Low-Latency Edge LLM Handover via Joint KV Cache Transfer and Token Prefill
title_zh: 通过联合 KV 缓存传输与 Token 预填充实现低延迟边缘大语言模型切换
authors: "Seunghun Lee, Jihong Park, Ce Zheng, Hyuncheol Park"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28018v1"
tags: ["query:llm"]
score: 10.0
evidence: 联合 KV 缓存传输与 Token 预填充以实现低延迟
tldr: 针对边缘大语言模型在用户跨基站切换时产生的服务中断问题，本文提出了一种联合KV缓存传输与Token预填充的低延迟切换方案。该方案通过协同优化回传网络中的KV缓存传输调度与目标服务器的预填充计算，旨在最小化多用户场景下的最大切换延迟。研究提供了具有显式可行性条件的逐步求解算法，为移动边缘LLM令牌流服务提供了实用的优化准则。
selection_source: fresh_fetch
motivation: 移动用户在基站间切换时，边缘服务器需快速恢复LLM上下文状态以避免显著的服务中断延迟。
method: 提出一种统一的切换框架，通过联合调度回传链路的KV缓存传输与目标侧的Token预填充长度来优化性能。
result: 仿真证明该方法在多种回传容量、预填充速度和上下文规模下均能显著降低切换延迟，优于现有基准方案。
conclusion: 联合优化传输与计算资源是实现低延迟边缘LLM移动性管理的关键，为未来边缘智能服务提供了理论指导。
---

## 摘要
大语言模型（LLMs）的边缘部署可以降低交互式服务的延迟，但当用户设备（UE）在基站（BSs）之间切换时，移动性会引入服务中断。为了及时恢复解码，目标侧边缘服务器必须恢复 UE 的上下文状态，这可以通过 Token 转发后进行预填充计算，或者通过回传网络直接传输键值（KV）缓存来实现。本文提出了一种统一的切换（HO）设计，通过联合选择预填充长度并调度回传 KV 缓存传输，以最小化多个 UE 中最差用户的 LLM 切换延迟。所得方案包含一个具有显式可行性条件和构造性速率调度策略的易处理分步解决方案。仿真结果表明，该方法在各种回传容量、预填充速度和上下文大小下均优于基准方法，为感知移动性的边缘 LLM Token 流式传输提供了实用指南。

## Abstract
Edge deployment of large language models (LLMs) can reduce latency for interactive services, but mobility introduces service interruptions when an user equipment (UE) hands over between base stations (BSs). To promptly resume decoding, the target-side edge server must recover the UE context state, which can be provisioned either by token forwarding followed by prefill computation or by direct key-value (KV) cache transmission over backhaul. This paper proposes a unified handover (HO) design that jointly selects the prefill length and schedules backhaul KV cache delivery to minimize the worst-user LLM HO delay for multiple UEs. The resulting scheme admits a tractable step-wise solution with explicit feasibility conditions and a constructive rate-scheduling policy. Simulations show that the proposed method consistently outperforms baselines across a wide range of backhaul capacities, prefill speeds, and context sizes, providing practical guidelines for mobility-aware Edge LLM token streaming.

---

## 论文详细总结（自动生成）

以下是对论文《Low-Latency Edge LLM Handover via Joint KV Cache Transfer and Token Prefill》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在边缘计算环境下，移动用户在基站（BS）间切换（Handover, HO）时，如何最小化大语言模型（LLM）服务的延迟。
*   **研究动机**：LLM 推理具有自回归特性，依赖于历史 Token 的键值缓存（KV Cache）。当用户切换到新基站时，目标服务器缺乏上下文。
    *   **现有方案的缺陷**：
        1.  **基于 Token 的切换 (tHO)**：传输历史 Token 并重新进行预填充（Prefill），计算开销大，导致首字延迟（TTFT）高。
        2.  **基于缓存的切换 (cHO)**：直接通过回传链路（Backhaul）传输巨大的 KV Cache，受限于带宽，在多用户并发切换时容易拥塞。
*   **整体含义**：本文旨在通过联合优化“计算（预填充）”与“通信（缓存传输）”资源，解决边缘 LLM 移动性管理中的服务中断问题。

### 2. 方法论
*   **核心思想**：提出 **ctHO（联合缓存-Token 切换）** 框架。目标基站通过接收部分 Token 进行批量预填充（Batch Prefill）来重建部分缓存，同时通过回传链路接收剩余的 KV Cache，两者并行执行。
*   **关键技术细节**：
    *   **联合优化模型**：将问题建模为最小化“最差用户切换延迟”的优化问题。变量包括预填充的 Token 长度 $L$ 和回传带宽分配策略 $r_i(t)$。
    *   **分步求解算法**：
        1.  **第一步**：在给定预填充长度 $L$ 的情况下，通过一种构造性速率调度策略（优先服务目标完成时间最早的用户）来优化回传带宽分配。
        2.  **第二步**：寻找最优的 $L$，使得预填充延迟与缓存传输延迟达到平衡（通常是两者相等时最优）。
    *   **数学证明**：论文证明了这种分步解法具有全局最优性。

### 3. 实验设计
*   **场景设定**：一维线性网络模型，两个基站相距 300 米，4 名用户以 20m/s 的速度移动并触发切换。
*   **LLM 配置**：采用 **Qwen2.5-7B-Instruct** 模型参数（28层，4个KV头，128维头维度，16位精度），计算出每 Token 的 KV Cache 大小约为 458.752 bits。
*   **对比基准 (Benchmarks)**：
    *   **tHO**：仅依赖 Token 转发和预填充。
    *   **cHO**：仅依赖回传链路传输 KV Cache。
    *   **no HO**：不进行切换，用户继续连接信号逐渐衰减的源基站（用于评估总流式传输延迟）。
*   **变量分析**：对比了不同回传带宽（2-12 Gbps）、预填充速度、最大上下文长度（2k-12k Tokens）以及用户数量（2-12人）对延迟的影响。

### 4. 资源与算力
*   **算力说明**：文中未明确提及具体的 GPU 型号或训练时长。
*   **实现方式**：论文主要基于数学建模和数值仿真。预填充延迟采用了线性模型 $p(L) = aL + b$，其中的参数 $a, b$ 是基于经验数据设定的（例如 $a = 9.42 \times 10^{-5}$）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了 5 组主要实验，分别探讨了回传速率、计算能力、缓存大小、用户密度对切换延迟的影响，以及切换与不切换在总延迟上的对比。
*   **充分性评价**：实验覆盖了边缘计算的核心变量（带宽、算力、负载），对比路径清晰。虽然是在简化的 1D 场景下进行，但对于验证其提出的联合优化理论是充分且客观的。

### 6. 主要结论与发现
*   **性能提升**：ctHO 显著优于单一方案。在特定条件下，其延迟比 cHO 降低了 **3.1倍**，比 tHO 降低了 **3.7倍**。
*   **资源互补**：当预填充速度慢（算力弱）时，系统自动倾向于更多地利用回传链路；反之则增加预填充长度。
*   **切换必要性**：实验证明，尽管切换会带来瞬时延迟，但在长距离移动场景下，切换到近端基站带来的信号增益能显著降低后续的 Token 流式传输延迟，总延迟远低于不切换的情况。

### 7. 优点
*   **理论完备**：提供了全局最优解的数学证明，而非简单的启发式算法。
*   **协同设计**：打破了通信与计算的孤岛，通过“以计算换通信”或“以通信换计算”实现了灵活的资源调度。
*   **实用性强**：为未来 6G 或边缘智能网络中的移动性管理提供了具体的参数选择指南。

### 8. 不足与局限
*   **场景简化**：采用 1D 移动模型，未考虑复杂的城市多径环境或非线性移动轨迹。
*   **同构假设**：假设源基站和目标基站部署完全相同的 LLM 模型，未讨论异构模型间的上下文迁移。
*   **开销忽略**：假设 Token 传输延迟相对于 KV Cache 传输可忽略不计，在极极端网络条件下可能不成立。
*   **硬切换限制**：目前关注的是“硬切换”（断开再连），未深入探讨更复杂的“软切换”或预热机制（虽然文中提到这是未来工作）。

（完）
