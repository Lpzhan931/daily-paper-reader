---
title: A Switch-Centric In-Network Architecture for Accelerating LLM Inference in Shared-Memory Network
title_zh: 一种用于加速共享内存网络中大语言模型推理的以交换机为中心的在网架构
authors: "Aojie Jiang, Kang Zhu, Zhiheng Zhang, Zhengxu Su, Juntao Liu, Yuan Du, Li Du"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28239v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于加速大模型推理的网络内架构
tldr: 针对大模型推理中的通信瓶颈，本文提出SCIN，一种面向共享内存网络的首个以交换机为中心的网络内计算架构。SCIN通过引入交换机内加速器（ISA）和协同设计的通信结构，消除了NVLS等传统架构中的冗余数据移动，并支持网络内量化（INQ）技术。该架构显著降低了All-Reduce延迟并提升了带宽利用率，在LLaMA-2模型上实现了显著的推理加速。
selection_source: fresh_fetch
motivation: 现有的以加速器为中心的网络内计算架构存在冗余数据传输开销，且无法支持量化等非内存语义算子的卸载。
method: 提出SCIN架构，通过在交换机中集成可主动发起内存操作的加速器并结合网络内量化技术，优化All-Reduce操作。
result: 实验显示SCIN将All-Reduce速度提升高达8.7倍，并使LLaMA-2模型的首字延迟和吞吐性能分别提升1.74倍和1.34倍。
conclusion: SCIN通过以交换机为中心的设计有效解决了共享内存网络中的通信瓶颈，是加速大规模语言模型推理的高效方案。
---

## 摘要
以 NVLink Sharp (NVLS) 为代表的网络内计算技术，通过将 All-Reduce 等集合操作卸载到交换机，为解决大语言模型（LLM）推理中的通信瓶颈提供了一种极具前景的方法。然而，NVLS 以加速器为中心的架构存在两个根本局限性：1) 它依赖 GPU 的 load 指令来触发归约操作，这意味着在交换机中归约后的数据必须额外传输回发起请求的 GPU，而不能直接广播，从而引入了不必要的通信开销；2) 受限于架构约束，NVLS 无法卸载不能分解为内存语义指令的算子，例如本研究提出的网络内量化（INQ）。因此，NVLS 中的 All-Reduce 必须以 FP16/BF16 精度运行，导致了大量的带宽浪费。为了解决这些局限性，我们提出了 SCIN，这是首个针对 AI 加速器共享内存网络的以交换机为中心的在网架构，能够实现低延迟和高带宽的 All-Reduce。具体而言，我们引入了一种交换机内加速器（ISA），它能够发起用于网络内处理的内存语义操作，并配合协同设计的通信结构，产生的协议开销微乎其微。通过消除冗余的数据移动，SCIN 提供了比 NVLS 更低的 All-Reduce 延迟。此外，通过在 ISA 中集成量化模块，SCIN 实现了 All-Reduce 的网络内量化（INQ），将其精度降低至 8 位，在精度损失可忽略不计的情况下使带宽几乎翻倍。我们还在多 FPGA 系统上展示了 SCIN 的原型，以证明其可行性和有效性。实验结果表明，我们的设计在处理小消息和大消息时，分别将 All-Reduce 加速了高达 8.7 倍和 3.8 倍，在 LLaMA-2 模型上实现了高达 1.74 倍的首字生成时间（TTFT）加速和 1.34 倍的逐字生成时间（TPOT）加速。

## Abstract
In-network computing techniques, exemplified by NVLink Sharp (NVLS), offer a promising approach to addressing the communication bottlenecks in LLM inference by offloading collective operations, such as All-Reduce, to switches. However, the accelerator-centric architecture of NVLS suffers from two fundamental limitations: 1) it relies on GPU load instructions to trigger reduction operations, which means that the data reduced in the switch must be additionally transferred back to the initiating GPU rather than being broadcast directly, thereby introducing unnecessary communication overhead; 2) due to its architectural constraints, NVLS cannot offload operators that are not decomposable into memory-semantic instructions, such as the in-network quantization (INQ) proposed in this work. As a result, All-Reduce in NVLS must operate at FP16/BF16 precision, leading to substantial bandwidth waste.To address these limitations, we propose SCIN, the first switch-centric in-network architecture for shared-memory networks of AI accelerators, enabling both low-latency and high-bandwidth All-Reduce. Specifically, we introduce an in-switch accelerator (ISA) capable of initiating memory-semantic operations for in-network processing, together with a co-designed communication fabric that incurs negligible protocol overhead. By eliminating redundant data movement, SCIN delivers lower All-Reduce latency than NVLS. Moreover, by integrating a quantization module into the ISA, SCIN enables INQ for All-Reduce, reducing its precision to 8 bits and nearly doubling bandwidth with negligible accuracy loss. We also present a prototype of SCIN on a multi-FPGA system to demonstrate its feasibility and effectiveness. Experimental results show that our design accelerates All-Reduce by up to 8.7x for small messages and 3.8x for large messages, leading up to 1.74x faster TTFT and 1.34x faster TPOT on LLaMA-2 models.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Switch-Centric In-Network Architecture for Accelerating LLM Inference in Shared-Memory Network》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在大语言模型（LLM）的张量并行（TP）推理中，**All-Reduce 通信操作**已成为严重的性能瓶颈（占比可达端到端延迟的 60%）。
*   **研究动机**：现有的网络内计算技术（如 NVIDIA 的 NVLS）采用“以加速器为中心”的架构，存在两大缺陷：
    1.  **冗余传输**：归约后的数据必须先返回发起请求的 GPU，再由该 GPU 广播，增加了网络跳数和延迟。
    2.  **功能受限**：仅支持简单的内存语义指令，无法支持如**网络内量化（INQ）**等复杂操作，导致通信必须维持高精度（FP16/BF16），浪费带宽。
*   **整体含义**：本文提出 **SCIN** 架构，通过将交换机从被动的数据处理器转变为主动的内存访问发起者，彻底消除冗余路径并引入量化压缩，从而加速 LLM 推理。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：建立**以交换机为中心（Switch-Centric）**的在网计算范式。交换机内的加速器（ISA）可以直接读写 AI 加速器的内存。
*   **关键技术细节**：
    *   **交换机内加速器 (ISA)**：ISA 能够主动发起内存语义请求（Pull/Push），直接从参与计算的所有 GPU 抓取数据，在交换机内完成归约后直接写回各 GPU 内存。
    *   **网络协议扩展**：在数据包头引入 1-bit 的 **INC（网络内计算）标志位**，用于区分 ISA 发起的事务与普通加速器间的事务，协议开销极低。
    *   **网络内量化 (INQ)**：在 ISA 流水线中集成量化/反量化模块。采用**块量化（Block-wise Quantization）**策略（每 64 个元素一个缩放因子），将通信精度降至 8 位，使有效带宽翻倍。
    *   **轻量级同步机制**：将同步路径从两跳缩减为一跳，降低了 Decode 阶段小消息通信的敏感延迟。
    *   **波次调节 (Wave Regulation)**：通过在 ISA 中维护多个“波次（Waves）”来重叠通信与同步开销，确保大消息传输能跑满线速。

### 3. 实验设计
*   **实验场景**：
    1.  **硬件原型**：使用 5 个 AMD FPGA（1 个作为交换机，4 个模拟加速器）搭建真实硬件系统。
    2.  **系统仿真**：基于 BookSim2 开发了周期级网络仿真器，并使用硬件原型数据进行了校准。
*   **数据集/模型**：
    *   **模型**：LLaMA-2 (7B, 13B, 70B), Mistral-7B, Mixtral-8x7B。
    *   **精度评估数据集**：WikiText-2 (困惑度), MMLU, Common Sense QA (WinoGrande, PIQA 等)。
*   **Benchmark 与对比方法**：
    *   **软件基准**：Ring All-Reduce 算法。
    *   **硬件对比**：NVIDIA NVLink SHARP (NVLS) 的公开性能数据。

### 4. 资源与算力
*   **硬件原型**：使用了 5 块 **AMD xcvu13p FPGA**。单链路提供 128 Gbps 双向带宽。
*   **GPU 模拟**：仿真环境模拟了类似 **NVIDIA DGX-H200** 的 8-GPU 系统，单链路带宽 900 GB/s。
*   **软件工具**：使用 TensorRT-LLM 进行算子性能分析，SmoothQuant 用于量化模拟。
*   **训练时长**：文中未提及模型训练，主要关注推理阶段的通信加速。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 4 节点（硬件原型）到 8 节点、16 节点（仿真）的扩展性分析。
    *   针对不同 Batch Size (1-128) 和序列长度 (128-4096) 进行了详尽的测试。
    *   进行了**消融实验**，验证了波次数量、缓冲区大小对带宽利用率的影响。
*   **充分性评价**：实验设计较为充分。作者不仅验证了底层通信性能（延迟、带宽），还深入评估了 INQ 对 LLM 最终任务准确率的影响，证明了该方法在实际应用中的可行性。

### 6. 主要结论与发现
*   **通信性能**：相比软件 Ring 算法，SCIN 在小消息上实现 **8.7x** 加速，大消息实现 **3.8x** 加速。
*   **推理加速**：在 LLaMA-2 模型上，**TTFT（首字生成时间）提升高达 1.74x**，**TPOT（逐字生成时间）提升高达 1.34x**。
*   **量化效果**：8 位 INQ 在几乎不损失精度（困惑度增量 < 0.003）的情况下，将通信数据量压缩了近 2 倍。
*   **同步优化**：通过将同步路径减半，显著提升了对延迟敏感的 Decode 阶段的效率。

### 7. 优点（亮点）
*   **架构范式转移**：从“被动响应”转为“主动发起”，解决了 NVLS 架构中固有的冗余回传问题。
*   **软硬协同优化**：不仅是硬件架构创新，还结合了 LLM 特有的量化需求，实现了跨层优化。
*   **硬件验证**：提供了 FPGA 原型实现，证明了该架构在现有共享内存协议（如 UALink 趋势）下的落地潜力。

### 8. 不足与局限
*   **协议依赖性**：该架构高度依赖于支持共享内存语义（如 NVLink, UALink）的紧耦合网络，在标准以太网或 InfiniBand 上的适用性未被讨论。
*   **硬件开销**：在交换机内集成复杂的量化流水线和多波次缓冲区会增加交换机芯片的面积和功耗成本。
*   **扩展性边界**：虽然仿真到了 16 节点，但在更大规模（如千卡集群）下的多级交换机拓扑中，该架构的同步复杂度和冲突管理尚未深入探讨。

（完）
