---
title: "ScoutAttention: Efficient KV Cache Offloading via Layer-Ahead CPU Pre-computation for LLM Inference"
title_zh: ScoutAttention：通过提前一层 CPU 预计算实现大语言模型推理的高效 KV 缓存卸载
authors: "Qiuyang Zhang, Kai Zhou, Ding Tang, Kai Lu, Cheng Li, Zhenyu Yang, Peng Xu, Jiguang Wan"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27138v1"
tags: ["query:llm"]
score: 10.0
evidence: KV缓存卸载与GPU-CPU协同注意力
tldr: 针对大语言模型长文本推理中KV缓存导致的显存瓶颈，本文提出ScoutAttention框架。该框架通过GPU-CPU协作的块稀疏注意力机制减少计算负载，并创新性地引入“提前一层”的CPU预计算算法与异步周期性召回机制，有效解决了传统卸载方案中的I/O和计算延迟问题，在保持高精度的同时显著提升了推理速度。
selection_source: fresh_fetch
motivation: 现有的KV缓存卸载到DRAM的方案受限于频繁的数据传输或沉重的CPU计算负担，导致GPU利用率低下。
method: 提出一种GPU-CPU协作的块稀疏注意力机制，并结合提前一层的CPU预计算算法和异步周期性召回机制来优化计算流程。
result: "实验表明，ScoutAttention在保持精度损失低于2.4%的前提下，比现有卸载方法实现了2.1倍的加速。"
conclusion: ScoutAttention通过创新的层间预计算和稀疏化策略，为长文本LLM推理提供了一种高效的显存扩展与加速方案。
---

## 摘要
大语言模型在长上下文推理过程中面临严重的 GPU 显存容量限制，其中 KV 缓存的内存消耗严重限制了解码批处理大小。虽然现有研究探索了将 KV 缓存卸载到 DRAM，但这些方法要么需要频繁的 GPU-CPU 数据传输，要么对 CPU 计算有极高要求，导致系统在等待 I/O 操作或 CPU 处理完成时 GPU 利用率低下。我们提出了 ScoutAttention，这是一种新型的 KV 缓存卸载框架，通过 GPU-CPU 协同注意力计算加速大语言模型推理。为了防止 CPU 计算成为系统瓶颈，ScoutAttention 引入了 GPU-CPU 协同的分块稀疏注意力，显著降低了 CPU 负载。与传统的并行计算方法不同，我们的框架采用了一种新颖的提前一层 CPU 预计算算法，使 CPU 能够提前一层启动注意力计算，并辅以异步周期性召回机制，以保持极低的 CPU 计算负载。实验结果表明，ScoutAttention 在保持准确率与基准线差距在 2.4% 以内的同时，与现有的卸载方法相比实现了 2.1 倍的加速。

## Abstract
Large language models encounter critical GPU memory capacity constraints during long-context inference, where KV cache memory consumption severely limits decode batch sizes. While existing research has explored offloading KV cache to DRAM, these approaches either demand frequent GPU-CPU data transfers or impose extensive CPU computation requirements, resulting in poor GPU utilization as the system waits for I/O operations or CPU processing to complete.   We propose ScoutAttention, a novel KV cache offloading framework that accelerates LLM inference through collaborative GPU-CPU attention computation. To prevent CPU computation from bottlenecking the system, ScoutAttention introduces GPU-CPU collaborative block-wise sparse attention that significantly reduces CPU load. Unlike conventional parallel computing approaches, our framework features a novel layer-ahead CPU pre-computation algorithm, enabling the CPU to initiate attention computation one layer in advance, complemented by asynchronous periodic recall mechanisms to maintain minimal CPU compute load. Experimental results demonstrate that ScoutAttention maintains accuracy within 2.4% of baseline while achieving 2.1x speedup compared to existing offloading methods.

---

## 论文详细总结（自动生成）

以下是对论文《ScoutAttention: Efficient KV Cache Offloading via Layer-Ahead CPU Pre-computation for LLM Inference》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）向长文本（如 RAG、思维链推理）演进，KV 缓存（KV Cache）消耗了大量 GPU 显存，严重限制了推理时的批处理大小（Batch Size）和吞吐量。
*   **核心问题**：现有的 KV 缓存卸载（Offloading）方案存在瓶颈：
    *   **召回型方案（如 InfiniGen）**：受限于 PCIe 带宽，GPU 经常因等待数据传输而闲置（约 61% 的时间）。
    *   **协同计算型方案（如 HGCA）**：虽然减少了 I/O，但 CPU 计算速度远慢于 GPU，导致 GPU 需等待 CPU 计算结果（约 57% 的时间）。
*   **论文目标**：提出 ScoutAttention 框架，通过 GPU-CPU 协同计算和创新的流水线设计，消除 I/O 和计算瓶颈，实现高效的长文本推理。

### 2. 方法论
ScoutAttention 的核心思想是**“空间换时间”与“异步流水线”**，包含三个关键技术：
*   **GPU-CPU 协同块稀疏注意力**：
    *   将 KV 缓存分块，仅在 GPU 显存中保留块摘要（Digests）和少量关键块，其余卸载至 DRAM。
    *   利用注意力权重的**时间局部性**（相邻 Token 关注的块高度重合），GPU 仅需将不在显存中的 Top-k 块交给 CPU 计算，大幅降低 CPU 负载。
*   **提前一层 CPU 预计算（Layer-Ahead Pre-computation）**：
    *   **核心创新**：在 GPU 计算第 $i$ 层时，利用残差连接的特性，通过第 $i$ 层的输入和第 $i+1$ 层的权重预估第 $i+1$ 层的 Query。
    *   **流程**：提前触发 CPU 对第 $i+1$ 层缺失块的计算，使 CPU 计算与 GPU 的整层执行（包括 FFN、投影等）重叠，从而隐藏 CPU 的计算延迟。
*   **异步周期性 KV 缓存召回**：
    *   针对解码过程中重要块发生偏移（Drift）的问题，系统会定期将 DRAM 中的重要块异步拉回 GPU。
    *   这种召回操作在层计算完成后触发，利用 Token 解码之间的时间窗，不占用推理关键路径。

### 3. 实验设计
*   **数据集/场景**：使用 **LongBench** 基准测试，涵盖单文档 QA、多文档 QA、摘要和信息检索等 8 个数据集，上下文长度最高达 64k。
*   **对比方法（Baselines）**：
    *   **FullKV**：标准全量 KV 缓存推理。
    *   **InfiniGen**：基于预测和提前召回的卸载方案。
    *   **HGCA**：基于滑动窗口和 GPU-CPU 并行计算的方案。
*   **测试模型**：主要使用 Qwen 3 (8B/14B/32B)，同时也验证了 Llama 3.1、Mistral、GLM 4 等模型。

### 4. 资源与算力
*   **硬件环境**：配备 80GB HBM 显存的 GPU（通过 PCIe 4.0 x16 连接）和 36 核 CPU。
*   **软件框架**：基于 **SGLang** 推理引擎实现，使用了 FlashInfer 加速算子和 Intel IPEX 优化 CPU 计算。
*   **训练时长**：该论文关注推理优化，不涉及模型训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 8 个长文本数据集上进行了准确率验证。
    *   在不同输入长度（8k 到 100k）、不同 Batch Size（16 到 64）以及不同块大小下进行了吞吐量测试。
    *   进行了详尽的**消融实验**，分别验证了预计算（PC）和周期性召回（PR）的增益。
*   **充分性评价**：实验设计较为全面，涵盖了准确率、吞吐量、延迟分解等多个维度，对比了当前最前沿的卸载方案，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：在 64k 上下文下，ScoutAttention 的吞吐量是 FullKV 的 **5.1 倍**，是现有卸载方案（InfiniGen/HGCA）的 **2.1 倍**。
*   **延迟优化**：成功将 GPU 的空闲时间（Idle Time）从 60% 左右降低至 **6%**。
*   **准确率保持**：在 2048 的稀疏预算下，平均准确率损失仅为 **2.1%**，证明了预估 Query 的有效性。
*   **可扩展性**：随着 Batch Size 增加，ScoutAttention 表现出比对比方法更好的线性增长趋势。

### 7. 优点
*   **流水线创新**：打破了传统的层内并行思路，提出“提前一层”预计算，巧妙利用了 Transformer 的结构特性隐藏了 CPU 延迟。
*   **协同效率高**：结合了块稀疏和协同计算，既规避了 PCIe 带宽瓶颈，又解决了 CPU 算力不足的问题。
*   **实用性强**：直接集成在主流推理框架 SGLang 中，具有较高的工程参考价值。

### 8. 不足与局限
*   **架构依赖性**：预计算依赖于相邻层输入的高度相似性（残差连接），对于某些非标准 Transformer 架构（如没有残差连接或层间差异极大的模型）可能效果受限。
*   **超参数敏感**：周期性召回的阈值 $\beta$ 和召回间隔需要根据硬件（CPU/GPU 算力比）进行离线分析和手动调整，缺乏完全自动化的自适应机制。
*   **模型覆盖**：虽然测试了多个模型，但主要集中在 Dense 模型，未探讨在 MoE（混合专家模型）上的表现。

（完）
