---
title: "Adaptive Depth Sparse Framework: Similarity-Driven Resource Allocation for Pre-Trained LLMs"
title_zh: 自适应深度稀疏框架：预训练大语言模型的相似性驱动资源分配
authors: "Yidu Wu, Xiang Wang, Kejie Zhao, Zhangchi Wang, Qinghai Guo, Xiaoying Tang"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21291v1"
tags: ["query:llm"]
score: 8.0
evidence: 提出无需完全重训练的自适应深度稀疏框架用于大语言模型推理加速
tldr: 大语言模型推理成本高，现有加速方法需全量重训练或微调，跨任务泛化受限。本文提出自适应深度稀疏框架AdaDSF，利用层间余弦相似度统计为每层分配token保留比率，用轻量路由器选择信息性token，并引入特征对齐目标匹配稀疏与稠密模型表征。在GPT-NeoX和Qwen2.5上，推理FLOPs大幅降低，语言建模和常识推理性能接近稠密模型。同等稀疏度下，精度损失显著小于MoD、D-LLM和DLO等基线。该方法无需全量重训练即可实现高效推理，具有良好通用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21291v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1168, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21291v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 950, \"height\": 565, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21291v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1148, \"height\": 429, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1236, \"height\": 509, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 663, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 721, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 468, \"height\": 140, \"label\": \"Table\"}]"
motivation: 现有LLM加速方法依赖特定任务微调或从头训练，跨任务泛化受限且适应成本高。
method: 基于层间输入输出余弦相似度统计分配token保留比率，轻量路由器选择token，特征对齐目标匹配稀疏与稠密模型表征。
result: 在GPT-NeoX和Qwen2.5上大幅降低推理FLOPs，语言建模和常识推理性能接近稠密模型，同等稀疏度下优于MoD、D-LLM和DLO。
conclusion: AdaDSF无需全量重训练即可将预训练LLM转为深度稀疏模型，显著降低推理开销并保持性能，具有良好的跨任务通用性。
---

## 摘要
大语言模型（LLMs）在生成和推理任务上表现出色，但Transformer架构带来了高昂的推理成本。现有的加速方法通常依赖于任务特定的微调或从头训练，增加了适配成本并限制了跨任务可用性。我们提出了一种自适应深度稀疏框架（AdaDSF），它能够将现成的预训练大语言模型转换为深度稀疏模型，而无需完全重新训练。我们的关键洞察是，各层对表示变换的贡献是不均等的，这可以通过层输入和输出隐藏状态之间的余弦相似度来表征。基于此，AdaDSF从相似度统计中分配逐层的令牌保留比率，使用轻量级路由器选择每层的信息性令牌，并引入特征保留对齐目标，以匹配稀疏模型和稠密模型之间的中间表示和最终表示。在GPT-NeoX和Qwen2.5上的语言建模和常识推理任务中，AdaDSF大幅减少了推理FLOPs，同时保持了接近稠密模型的性能。在可比稀疏度下，与包括MoD、D-LLM和DLO在内的强基线相比，AdaDSF始终产生更小的精度下降。

## Abstract
Large language models (LLMs) achieve strong generation and reasoning performance, but the Transformer architecture incurs high inference cost. Existing acceleration methods often rely on task-specific fine-tuning or training from scratch, increasing adaptation cost and limiting cross-task usability. We present an Adaptive Depth Sparse Framework (AdaDSF) that converts off-the-shelf pre-trained LLMs into depth-sparse models without full retraining. Our key insight is that layers contribute unequally to representation transformation, characterized by the cosine similarity between layer input and output hidden states. Based on this, AdaDSF assigns layer-wise token retention ratios from similarity statistics, uses a lightweight router to select informative tokens at each layer, and introduces a feature-preserving alignment objective to match intermediate and final representations between sparse and dense models. On GPT-NeoX and Qwen2.5 over language modeling and commonsense reasoning, AdaDSF substantially reduces inference FLOPs while preserving performance close to dense counterparts. Under comparable sparsity, AdaDSF consistently yields smaller accuracy degradation than strong baselines including MoD, D-LLM, and DLO.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）推理成本高，现有加速方法（如量化、蒸馏、架构重设计）往往需要针对特定任务进行微调或从头训练，跨任务泛化受限，且增加了适配成本。特别是深度稀疏方法（如Mixture-of-Depths、D-LLM、DLO）虽能减少计算，但仍存在固定/启发式令牌保留策略导致计算分配不合理、依赖专用训练管道、架构改动大等局限。
- **整体含义**：提出一种无需完全重训练、对模型架构改动极小的自适应深度稀疏框架AdaDSF，将预训练稠密LLM高效转化为深度稀疏变体，在保持性能的同时显著降低推理开销，提升部署灵活性和跨任务可用性。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：利用层输入与输出隐藏状态之间的余弦相似度来量化各层对表示变换的贡献，据此为每层分配不同的令牌保留比率；通过轻量路由器动态选择每层信息性令牌；并通过特征对齐目标匹配稀疏与稠密模型的中间和最终表示，以补偿稀疏化带来的表示偏移。

**关键技术细节（算法流程）**：
1. **相似性驱动的层间令牌保留**：
   - 对第i层稠密模型，计算输入与输出余弦相似度 \( s_i \)。
   - 使用校准集得到所有层的相似度向量 \( s = [s_1, ..., s_L] \)。
   - 步骤①：温度归一化加权 → 步骤②：偏差缩放 → 步骤③：Sigmoid映射到0.05~0.95范围 → 步骤④：全局预算校正，使得平均保留比率为目标t。
2. **稀疏模块与轻量路由器**：每层包含一个MLP路由器和一个Transformer层。路由器预测令牌重要性分数，按分配比率 \( K_i = \lfloor r_i \cdot s \rfloor \) 选择Top-Ki令牌；选中的令牌经过完整Transformer块，未选中的通过残差路径绕过。
3. **特征保留对齐训练**：
   - 隐藏状态对齐损失：每层的稀疏表示与稠密表示（softmax后）的L2距离。
   - 输出分布对齐损失：KL散度对齐最终logits。
   - 总损失 = 输出对齐损失 + 平均隐藏状态对齐损失。

**公式**：如式(4)-(11)，具体为余弦相似度定义、温度归一化、偏差缩放、Sigmoid映射、预算校正、对齐损失函数。

### 3. 实验设计

- **模型**：GPT-NeoX-130M、Qwen2.5-0.5B、Qwen2.5-1.5B。
- **数据集与场景**：
  - 语言建模：WikiText103测试集。
  - 指令微调场景：GenQA、InfinityInstruct、OpenHermes2.5（用于Qwen2.5训练）。
  - 常识推理评估：ARC-Challenge、ARC-Easy、HellaSwag、PIQA、WinoGrande、OpenBookQA。
- **基准对比方法**：MoD（Mixture-of-Depths）、D-LLM、DLO（动态层操作）。
- **评估指标**：令牌保留比率、归一化FLOPs（稀疏/稠密）、困惑度（PPL）、推理准确率（Avg和相对于稠密模型的Diff）。

### 4. 资源与算力

论文中未明确说明使用的GPU型号、数量及训练时长等具体算力信息，仅描述了模型参数量级（130M～1.5B），未提供硬件配置或训练时间。

### 5. 实验数量与充分性

- **实验组数**：包含主要性能对比表2张（表1a/b对应Qwen2.5，表2对应GPT-NeoX）、消融实验表2张（表3：相似性驱动保留比率分配的消融；表4：中间层对齐损失的消融）、以及图2展示层间保留比率分配示例。
- **充分性评价**：
  - 覆盖了不同模型规模（130M、0.5B、1.5B）和不同任务类型（语言建模、六项常识推理）。
  - 对比了三个强基线方法，并在多个保留比率下（90%、80%、70%）进行对比，实验设计较为全面。
  - 消融实验验证了核心模块（相似性分配、对齐损失）的有效性。
  - 客观性：指标清晰，对比公平（按相同稀疏度比较FLOPs和准确率）。
  - 不足：缺乏更大模型（7B+）的验证，也缺少与更多基线（如量化方法）的对比；未提供统计显著性检验。

### 6. 论文的主要结论与发现

- AdaDSF能在无需完全重训练、仅需轻量校准和微调的情况下，将预训练稠密LLM高效转化为深度稀疏模型。
- 在GPT-NeoX-130M和Qwen2.5（0.5B/1.5B）上，AdaDSF在保持相近性能（PPL或平均准确率）的同时，大幅降低推理FLOPs（70%～90%保留率下FLOPs降至0.68～0.90）。
- 与MoD、D-LLM、DLO相比，在相同稀疏度下，AdaDSF的精度损失更小（例如Qwen2.5-0.5B平均准确率Diff为-2.6% vs MoD -7.3% vs DLO -3.4%）。
- 相似性驱动的保留比率分配和中间层对齐损失是关键模块，缺失任一都会导致性能显著下降（PPL从18.91升至19.69或20.14）。

### 7. 优点

- **方法创新**：首次利用层间相似度统计自适应分配计算资源，而非固定或启发式预算，更符合各层变换非均匀性。
- **架构改动小**：仅添加轻量MLP路由器和微调训练，无需修改注意力或前馈网络内部结构，可直接应用于现成预训练模型。
- **训练高效**：无需全量预训练，仅需少量校准数据和对齐训练，适应成本低。
- **性能稳定**：在多个模型和任务上表现一致，且优于现有深度稀疏方法。
- **实践价值**：直接降低了LLM在资源受限场景下的部署门槛。

### 8. 不足与局限

- **实验覆盖有限**：仅验证了最大1.5B参数模型，未在更大模型（7B、13B等）上验证，可扩展性尚待确认。
- **缺乏硬件效率实测**：仅用FLOPs作为计算代价指标，未报告实际推理延迟或吞吐量，可能无法完全反映部署收益（如路由器引入的额外开销）。
- **未与其他类型加速方法比较**：如量化（GPTQ）、蒸馏、剪枝等，无法全面衡量AdaDSF在整体效率谱系中的位置。
- **训练细节缺失**：未说明训练超参数、优化器、学习率、训练时长等可复现性关键信息。
- **校准集选择影响**：相似度统计依赖校准子集，但对校准集规模和分布鲁棒性未做敏感性分析。
- **偏差风险**：仅在英文数据集（WikiText、常识推理）上评估，未涉及多语言或更长上下文任务。

（完）
