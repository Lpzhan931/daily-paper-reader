---
title: When Is a Draft Accepted? A Theory of Acceptance in Speculative Decoding
title_zh: 何时草案被接受？推测解码中的接受理论
authors: Aaryam Sharma
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30265v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 投机解码中接受理论，涵盖宽松验证和贪婪解码设置
tldr: 投机解码中，实际系统常采用贪心解码、宽松接受规则或树形候选集，但现有理论仅分析分布保持的随机设置。本文将这些接受准则的拒绝区域表征为目标分布的低水平集，推导了严格贪心解码、加性和乘性宽松接受等的精确KL散度要求与尖刻边界，并扩展至树形解码。在Qwen3模型上的实验表明，宽松和树形准则大幅扩大了认证接受区域，尤其在目标模型分布边界较小时。该工作为实际推理系统中的确定性局部接受事件提供了理论证书。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30265v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1691, \"height\": 678}, {\"url\": \"assets/figures/arxiv/2606.30265v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1665, \"height\": 657}, {\"url\": \"assets/figures/arxiv/2606.30265v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1655, \"height\": 703}, {\"url\": \"assets/figures/arxiv/2606.30265v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1624, \"height\": 676}, {\"url\": \"assets/figures/arxiv/2606.30265v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1684, \"height\": 722}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30265v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 710}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1106, \"height\": 357}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1487, \"height\": 517}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 465, \"height\": 378}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1240, \"height\": 318}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 449, \"height\": 384}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1305, \"height\": 535}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1302, \"height\": 535}]"
motivation: 现有投机解码理论仅关注分布保持的随机采样，缺乏对实际系统常用的贪心解码、宽松接受和树形候选集的理论分析。
method: 将接受准则的拒绝区域表征为目标分布的低水平集，推导KL散度要求与尖刻边界，并扩展至树形解码的精确与仅边界证书。
result: 在Qwen3模型上，宽松和树形准则下认证接受区域显著扩大，尤其在目标模型分布边界较小时。
conclusion: 为实际推理系统中常见的确定性局部接受事件提供了理论证书，补充了现有分布保持的分析。
---

## 摘要
推测解码通过使用快速起草器提出候选词元，然后由更大的目标模型进行验证，从而加速语言模型推理。现有理论主要研究随机、分布保持的设置，其目标是精确地从目标分布中采样。相比之下，许多实际系统使用贪婪解码、宽松的接受规则或基于树的候选集，其成功取决于局部排序和阈值事件，而非精确的分布相等性。我们为这些情况开发了一个理论。我们识别出许多常见的接受标准具有拒绝区域，这些区域可以表征为目标分布的低水平集。对于这些标准，我们描述了拒绝所需的确切KL散度，从而得到了严格贪婪解码、加性和乘性宽松接受、top-(m)宽松准则以及熵阈值接受的精确证书和基于边界的锐界。然后，我们将该框架扩展到贪婪树解码，推导出当目标贪婪词元仍被起草器的top-(m)候选覆盖时的精确证书和仅基于边界的证书。最后，我们在Qwen3模型上评估所得证书，表明宽松和基于树的准则显著扩大了认证接受区域，尤其是在目标模型分布边界较低的解码步骤上。这些结果通过刻画实际推理系统中常见的确定性局部接受事件，补充了现有关于推测解码的分布保持分析。

## Abstract
Speculative decoding accelerates language model inference by using a fast drafter to propose candidate tokens that are then verified by a larger target model. Existing theory largely studies the stochastic, distribution-preserving setting, where the goal is to exactly sample from the target distribution. In contrast, many practical systems use greedy decoding, relaxed acceptance rules, or tree-based candidate sets, where success is governed by local ranking and threshold events rather than exact distributional equality. We develop a theory for these regimes. We identify that many common acceptance criteria have rejection regions that can be characterized as lower level sets of the target distribution. For these, we characterize the exact KL divergence required for rejection yielding exact certificates and sharp margin-based bounds for strict greedy decoding, additive and multiplicative relaxed acceptance, top-(m) relaxed criteria, and entropy-thresholded acceptance. We then extend the framework to greedy tree decoding, deriving exact and margin-only certificates for when the target greedy token remains covered by the drafter's top-(m) candidates. Finally, we evaluate the resulting certificates on Qwen3 models, showing that relaxed and tree-based criteria substantially enlarge the region of certified acceptance, especially on decoding steps with low target model distribution margin. These results complement existing distribution-preserving analyses of speculative decoding by characterizing the deterministic local acceptance events common in practical inference systems.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，以中文、Markdown 形式，进行结构化、深入且客观的总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

-   **核心问题**：推测解码（Speculative Decoding）技术通过快速“起草”（draft）模型生成候选词元，再由大型“目标”（target）模型并行验证，以加速LLM推理。然而，现有理论主要聚焦于**分布保持**的随机采样场景，其目标是保证输出分布与目标模型完全一致。但在实际部署中，大量系统采用**贪婪解码**、**宽松接受规则**或**树形候选集**等方法，其成功与否取决于局部的排序和阈值事件，而非精确的分布匹配。这类广泛使用的确定性、局部性接受机制缺乏系统的理论分析。
-   **整体含义**：本文旨在填补这一理论空白，为实际推测解码系统中常见的、非分布保持的接受准则提供严格的理论基础。通过分析接受与拒绝的局部条件，论文揭示了 **KL散度**（衡量目标与起草模型分布差异的关键指标）与接受稳定性之间的精确关系，为设计和评估实用化的推测解码方法提供了理论证书。

### 2. 论文提出的方法论：核心思想、关键技术细节

-   **核心思想**：论文的核心洞察是，许多常见接受准则（如严格贪婪、加性/乘性宽松、top-m、熵阈值）的**拒绝区域都可以被统一表征为目标模型分布的低水平集**。因此，接受问题被转化为一个关于“需要多大的KL散度才能导致拒绝”的问题。

-   **关键技术细节与框架**：
    1.  **单令牌接受统一框架**：
        -   **定义**：给定目标分布 \(p\)，拒绝区域 \(R\) 是一个不包括顶级词元的集合 \(x_0\)。如果起草模型的最优词元落在 \(R\) 中，则拒绝。
        -   **核心定理**：论文证明，拒绝所需的最小KL散度 \(R_p(R)\)（即证书，certificate）可以精确计算。公式为：
            \[
            R_p(R) = s \cdot KL\left( \frac{p|A}{s} \middle\| \text{Unif}(A) \right)
            \]
            其中，\(x^*\) 是 \(R\) 中概率最高的词元，\(A\) 和 \(s\) 是由 \(x^*\) 确定的“活跃集合”及其总概率。
        -   **边界推导**：
            -   **下界**：通过松弛为两词元问题，推导出基于目标分布边界（margin）的通用下界 \(G(p(x_0), p(x^*))\)。
            -   **上界**：得出一个普遍的 **\(\log(2)\)** 上界，意味着任何单令牌接受准则都无法容忍大于 \(\log(2)\) 的KL散度。

    2.  **具体准则的推论**：
        -   **严格贪婪解码 (Greedy)**：证书 \(M^*(p)\) 由 \(p(x_0)\) 和 \(p(x_1)\) 决定。得出一个简单的充分条件：如果目标分布的边界 \(\gamma_p > \sqrt{2\epsilon}\)，则当 \(KL(p\parallel q) < \epsilon\) 时，贪婪草案必然被接受。此 \(\sqrt{2\epsilon}\) 的率是最优的。
        -   **加性宽松 (Additive)**：通过容忍度 \(t\)，接受条件变为 \(p(y) > p(x_0) - t\)。其证书下界为 \(g(t)\)。这将对目标模型边界 \(\gamma_p\) 的依赖，转换为用户可控的参数 \(t\)。
        -   **乘性宽松 (Multiplicative)**：通过因子 \(\alpha\)，接受条件变为 \(p(y) > \alpha \cdot p(x_0)\)。其证书下界依赖于 \(p(x_0)\) 和 \(\alpha\)，对置信度高的模型更宽松。
        -   **Top-m 与熵阈值**：这些准则的证书也都是统一框架的特例。

    3.  **树形解码扩展**：
        -   **场景**：起草器每一步提出 top-m 个词元，形成一个树。如果目标最优词元 \(x_0\) 在该树中，则接受。
        -   **定理**：推导出树形解码的精确证书 \(R^{tree}_p(m)\) 及其基于树形边界 \(\gamma_m\) 的下界 \(B_m(\gamma_m)\)。
        -   **关键结论**：树形解码将单个词元的KL容忍度上界从 \(\log(2)\) 提升到了 \(\log(m+1)\)，并将所需边界从 \(\sqrt{2\epsilon}\) 降低到 \(\sqrt{4\epsilon/(m+1)}\)，带来了显著的改进。

### 3. 实验设计：数据集、场景与对比方法

-   **数据集**：采用 **UltraChat 200k** 数据集。
-   **场景**：使用 **Qwen3-1.7B** 和 **Qwen3-4B** 两个模型进行生成，分析了128步解码过程中的目标模型分布。
-   **Benchmark 与对比方法**：实验并非传统的性能竞赛，而是对论文推导的 **理论证书** 进行实证评估。
    -   **对比标准**：对比了不同接受准则（严格贪婪、加性/乘性宽松、Medusa式熵阈值）的 **KL证书大小（p5, p25, 中位数, 平均值）**。
    -   **对比参数**：对于加性宽松，对比了不同的 \(t\) 值（0.01, 0.1, 0.3等）；对于乘性宽松，对比了不同的 \(\alpha\) 值（0.9, 0.5, 0.1等）；对于树形解码，对比了不同的 \(m\) 值（2, 4, 8）。
    -   **性能指标**：主要指标是 **可保证接受长度**（Guaranteed Acceptance Lengths），即在给定KL上界 \(\epsilon\) 下，有多少个连续的解码步骤的证书值都大于 \(\epsilon\)。

### 4. 资源与算力

-   **未明确说明**：论文原文中**没有明确提及**所使用的 GPU 型号、数量、训练时长或推理的具体硬件配置。可以推测其实验的算力消耗处于常规水平（因为仅涉及在 Qwen3-1.7B 和 4B 模型上的推理和分布分析，而非从头训练模型）。

### 5. 实验数量与充分性

-   **实验数量**：论文在单模型（每个模型1次数据集）上进行了深入分析。
    -   分析了超过 **63,000个** 解码步骤的分布特性（表5、6的样本量）。
    -   讨论了不同参数（\(t, \alpha, m\)）组合下的证书分布（表3、4）。
    -   计算了不同KL界值 \(\epsilon\) 下的可保证接受长度（表5、6）。
-   **充分性评估**：
    -   **充分之处**：实验设计精巧，直接验证了理论推导的核心结论（如 \(\log(2)\) 上界、不同宽松准则对尾部分布的提升、树形解码的 \(\log(m+1)\) 上界等）。实验规模足以展示不同准则的相对优劣和理论预测的准确性。
    -   **不足之处**：
        1.  **模型范围局限**：仅使用了 Qwen3 家族的两个模型，未在其他架构（如LLaMA, Mistral）上验证结果，可能存在偏差。
        2.  **场景单一**：仅评估了单个数据集的生成过程，未在翻译、代码生成、摘要等多样化任务上评价。
        3.  **缺乏实际比较**：实验并未将理论证书与实际模型的端到端加速比（如延迟、吞吐量）直接挂钩，也未与其它具体的推测解码算法（如EAGLE-2, SpecInfer）进行对比，理论结果与实际性能之间的桥梁尚未完全建立。

### 6. 论文的主要结论与发现

1.  **统一理论框架**：成功建立了一个统一的框架，该框架将多种实用接受准则（贪婪、宽松、树形）的拒绝区域视为目标分布的低水平集，并由此推导出精确的KL证书。
2.  **证书上界**：所有**单令牌接受准则**的KL证书上界均为 \(\log(2)\)，而**树形解码**将此上界提升至 \(\log(m+1)\)。
3.  **宽松准则的优势**：加性和乘性宽松接受准则能显著拓宽认证接受区域，尤其是在目标模型分布边界 \(\gamma_p\) 很小（即模型不自信）的“长尾”步骤上，其效果远超严格贪婪解码。
4.  **树形解码的效用**：树形解码是提高接受稳定性的有效方法，能大幅降低对目标模型原始边界的要求（从 \(\sqrt{2\epsilon}\) 到 \(\sqrt{4\epsilon/(m+1)}\)），从而在相同的KL界下实现更长的可保证接受长度。
5.  **实证验证**：在Qwen3模型上的实验证实，理论推导的证书与模型的实际分布特征高度吻合，例如观察到在解码过程的低频、低置信度步骤上，宽松和树形准则的确能带来巨大的益处。

### 7. 优点：方法或实验设计上的亮点

-   **理论创新性**：论文在研究视角上具有开创性，首次系统地分析了实际推理系统中普遍存在的、但被理论界忽视的确定性局部接受机制。
-   **框架的简洁与普适性**：将不同准则统一到“低水平集”的概念下，建立了一个既美观又强大的分析框架，使得推导和结论具有高度的普适性。
-   **界限的精确与锐度**：不仅给出了精确的证书表达式，还推导了基于边界的、易于理解的下界和通用的上界，并证明了某些情况下的最优性（如贪婪解码中 \(\sqrt{2\epsilon}\) 率的最优性）。
-   **理论与实证的紧密结合**：通过精心设计的实验，完美地验证了理论的核心预测（如不同准则证书的分布差异、 \(\log(2)\) / \(\log(m+1)\) 的界限等），使理论不流于空谈。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

-   **理论假设**：理论分析是**局部**的（单步验证），假设了每一步的KL界是独立的。在实际解码中，多步草案的生成和验证是一个复杂的**动态过程**，前一步的接受/拒绝会影响后续的状态分布。论文本身也承认，这是一个值得研究的未来方向。
-   **实验覆盖不足**：如第5点所述，实验仅在单一模型家族和数据集上进行，缺乏在不同架构、规模、任务上的广泛验证，其结论的普适性有待进一步确认。
-   **未考虑质量与计算代价**：论文的理论证书只关注“是否能接受”，但未分析：
    -   **文本质量影响**：宽松接受准则虽然提高了接受率，但会改变最终生成的文本轨迹，这可能对文本质量（如事实性、创造性）带来负面影响，论文未探讨此权衡。
    -   **计算代价**：树形解码虽然理论证书更好，但验证的成本（验证树中所有节点）也更高，论文未对此进行系统分析。
-   **实证与性能的鸿沟**：论文并未将理论证书与端到端的速度提升或推理延迟等最终性能指标直接联系起来，也缺乏与其它主流推测解码算法（如EAGLE-2、SpecInfer）在相同基准上的横向对比。

（完）
