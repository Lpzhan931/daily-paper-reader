---
title: "DSpark: Confidence-Scheduled Speculative Decoding with Semi-Autoregressive Generation"
title_zh: "DSpark: 基于置信度调度的半自回归推测解码"
authors: "Xin Cheng, Xingkai Yu, Chenze Shao, Jiashi Li, Yunfan Xiong, Yi Qian, Jiaqi Zhu, Shirong Ma, Xiaokang Zhang, Jiasheng Ye, Qinyu Chen, Chengqi Deng, Jiping Yu, Damai Dai, Zhengyan Zhang, Yixuan Wei, Yixuan Tan, Wenkai Yang, Runxin Xu, Yu Wu, Zhean Xu, Xuanyu Wang, Muyang Chen, Rui Tian, Xiao Bi, Zhewen Hao, Shaoyuan Chen, Huanqi Cao, Wentao Zhang, Anyi Xu, Huishuai Zhang, Dongyan Zhao, Wenfeng Liang"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05147v1"
tags: ["query:llm"]
score: 9.0
evidence: 投机解码框架加速LLM推理
tldr: "推测解码通过解耦草稿生成和目标验证加速LLM推理，但并行草稿因缺乏token间依赖导致接受率快速下降，且盲目验证长块浪费批量容量。DSpark统一高吞吐并行生成与自适应负载验证：采用半自回归架构（并行骨干+轻量顺序模块）引入块内依赖缓解后缀衰减，并利用置信度调度基于前缀生存概率动态定制验证长度。在多样离线基准上，接受长度显著优于现有自回归与并行草稿方法。部署于DeepSeek-V4生产系统后，在匹配吞吐水平下用户生成速度提升60-85%，并避免交互约束下的严重吞吐退化，实现了之前无法达到的性能层级，推动了服务系统的Pareto前沿。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05147v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1490, \"height\": 967}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1563, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1476, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1573, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1569, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1570, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1643, \"height\": 623}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1480, \"height\": 895}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05147v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1635, \"height\": 641}]"
motivation: 并行草稿生成缺乏token间依赖导致接受率快速下降，且盲目验证长块浪费高并发服务的批量容量。
method: 提出DSpark框架，采用半自回归架构（并行骨干+轻量顺序模块）引入块内依赖，并基于置信度调度动态调整验证长度以适应负载。
result: "离线基准上接受长度显著提升；在DeepSeek-V4生产系统中用户速度提升60-85%，避免吞吐退化，实现更优性能。"
conclusion: DSpark通过结合半自回归生成与自适应验证，有效提升了推测解码的吞吐和接受率，推动了服务系统的Pareto前沿。
---

## 摘要
推测解码通过将草稿生成与目标验证解耦来加速大语言模型（LLM）推理。虽然最近的并行草稿模型能够单次前向传递高效生成长令牌序列，但由于缺乏令牌间依赖关系，其接受率会迅速下降。此外，不加区分地验证这些扩展块会浪费关键批处理容量在具有高拒绝风险的令牌上，严重降低高并发服务系统的吞吐量。我们提出DSpark，一个统一的推测解码框架，将高吞吐并行生成与自适应、负载感知验证相结合。为保持草稿质量，DSpark采用半自回归架构，将并行骨干与轻量级顺序模块耦合，引入块内依赖建模以缓解后缀衰减。为优化系统效率，DSpark采用基于置信度调度的验证，根据估计的前缀存活概率和引擎特定的吞吐量特征，动态调整每个请求的验证长度。在跨领域的离线基准测试中，DSpark在最先进的自回归和并行草稿模型基础上显著提升了接受长度。当部署在DeepSeek-V4服务系统中并在真实用户流量下运行时，DSpark成功减少了验证浪费。与现有生产基线（MTP-1）相比，在相同吞吐量水平下，DSpark将每用户生成速度提升了60%至85%。更重要的是，通过在严格的交互性约束下防止严重吞吐量下降，它实现了以前无法达到的性能层级，从而推动了服务系统的帕累托前沿。

## Abstract
Speculative decoding accelerates Large Language Model (LLM) inference by decoupling draft generation from target verification. While recent parallel drafters efficiently propose long token sequences in a single forward pass, they suffer from rapid acceptance decay due to a lack of inter-token dependencies. Furthermore, indiscriminately verifying these extended blocks wastes critical batch capacity on tokens with high rejection risks, severely degrading throughput in high-concurrency serving systems. We introduce DSpark, a speculative decoding framework that unifies high-throughput parallel generation with adaptive, load-aware verification. To maintain draft quality, DSpark utilizes a semi-autoregressive architecture, coupling a parallel backbone with a lightweight sequential module, to introduce intra-block dependency modeling and mitigate suffix decay. To optimize system efficiency, DSpark employs confidence-scheduled verification, dynamically tailoring the verification length for each request based on estimated prefix survival probabilities and engine-specific throughput profiles. On offline benchmarks across diverse domains, DSpark substantially improves the accepted length over state-of-the-art autoregressive and parallel drafters. When deployed within the DeepSeek-V4 serving system under live user traffic, DSpark successfully mitigates verification waste. Compared to the established production baseline (MTP-1), DSpark accelerates per-user generation speeds by 60 to 85 percent at matched throughput levels. More importantly, by preventing severe throughput degradation under strict interactivity constraints, it enables performance tiers that were previously unattainable, shifting the Pareto frontier of our serving system.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）推理中，推测解码通过轻量草稿模型生成候选token再由目标模型验证来加速生成。然而，现有并行草稿模型（如DFlash）虽能单次前向传递生成长序列，但由于各位置独立预测，缺乏token间依赖，导致后续token接受率快速衰减（即后缀衰减）。同时，无差别地验证所有长候选token会占用关键批处理容量，尤其在高并发环境下严重降低系统吞吐量。
- **研究动机**：在保持并行草稿高吞吐优势的同时，引入轻量的token间依赖建模来提升接受率；并针对动态系统负载自适应地决定验证长度，从而提升端到端服务效率。
- **整体含义**：DSpark是首个将**半自回归生成**与**负载感知的验证长度调度**结合的统一框架，在离线基准和真实生产部署中均大幅优于此前方法，推动了LLM推理的帕累托前沿。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：DSpark由两大组件构成：
  1. **半自回归草稿生成**：采用“并行骨干 + 轻量顺序头”架构。并行骨干（基于DFlash）一次性输出所有位置的隐藏状态和基础logit；轻量顺序头（Markov头或RNN头）依据已采样token逐位添加依赖性偏置，从而在几乎不增加草稿延迟的前提下缓解后缀衰减。
  2. **置信度调度验证**：通过置信度头预测每个位置的**条件接受概率**（即前缀存活概率），再结合硬件吞吐曲线（SPS(batch size)），以最大化系统总吞吐为目标，贪心地选择每个请求的最优验证长度。

- **关键技术细节**：
  - **半自回归生成**：
    - 并行骨干输出基础logit \(U_k\)。
    - Markov头：利用低秩因子分解（\(W_1 \in \mathbb{R}^{V \times r}, W_2 \in \mathbb{R}^{r \times V}\)）计算过渡偏置 \(B(x_{k-1}, x_k) = W_1[x_{k-1}] W_2\)。
    - RNN头：维护状态\(s_k\)，通过门控更新累积前缀信息，再生成偏置。
    - 最终概率为：\(p_k(v) \propto \exp(U_k(v) + B_k(\cdot))\)，采样时从左到右依次进行。
  - **置信度头**：轻量线性投影+sigmoid，输入为隐藏状态和前一token的Markov嵌入，输出条件接受概率\(c_k\)。通过分析接受率（\(c_k^* = 1 - \frac{1}{2}\|p_k^d - p_k^t\|_1\)）进行监督。
  - **顺序温度缩放（STS）**：在验证集上从左到右逐步校准累积接受概率，解决过度自信问题。
  - **硬件感知前缀调度器（Algorithm 1）**：
    - 计算每个请求每个位置的前缀存活概率\(a_{r,j} = \prod_{i\le j} c_{r,i}\)。
    - 按\(a_{r,j}\)降序全局排序候选token。
    - 贪心地逐个添加token到验证集合，更新当前吞吐量\(\Theta = \tau \cdot \mathrm{SPS}(B)\)（\(\tau\)为期望接受数，\(B\)为验证批大小）。
    - 当吞吐量停止增加时停止，保证因果性（不泄露未来token信息）。
  - **训练目标**：交叉熵损失、分布匹配损失（TV距离）、置信度损失三项加权，位置权重指数衰减。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **离线基准测试**：
  - **目标模型**：Qwen3-{4B,8B,14B}, Gemma4-12B。
  - **数据集**：分为三类领域：
    - **数学**: GSM8K, MATH500, AIME25。
    - **代码**: MBPP, HumanEval, LiveCodeBench。
    - **日常对话**: MT-Bench, Alpaca, Arena-Hard。
  - **对比方法**：Eagle3（自回归草稿，基于TTT）、DFlash（并行草稿）。所有方法在相同框架和训练数据上重新训练，确保公平比较。DSpark默认使用Markov头。
- **在线生产部署**：
  - **场景**：DeepSeek-V4-Flash和DeepSeek-V4-Pro的预览服务，在真实用户流量下运行。
  - **基线**：MTP-1（单token预测头，此前生产基线）。
  - **评估指标**：聚合吞吐量 (token/s/GPU) vs. 每用户生成速度 (token/s/user) 的帕累托曲线，以及不同并发度下的吞吐量和平均验证预算。

### 4. 资源与算力：GPU 型号、数量、训练时长等

- **论文未明确给出**GPU型号、数量或具体训练时长。仅提到：
  - 使用内部训练框架HAI-LLM。
  - 训练数据为Open-PerfectBlend（约130万样本），每个草稿模型训练10个epoch以确保收敛。
  - 在线部署部分未说明具体服务器配置。
- **结论**：文中未披露算力细节，无法量化。

### 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、客观、公平

- **实验数量**：
  - **主结果（表1）**：4个目标模型 × 3个领域（每个领域3个数据集） × 3种方法 = 36组结果。
  - **消融与分析**：
    - 位置条件接受率（图2）：Qwen3-4B上对3种方法在3个领域共9条曲线。
    - 草稿深度（图3）：DSpark深度1/2/5层 vs DFlash 5层，9个数据集。
    - 提议长度影响（图4）：4/8/12/16长度下DSpark vs DFlash，3个领域 + 延迟开销。
    - 置信度阈值扫描（图5）：0~0.8阈值下接受率变化。
    - 校准可靠性图（图6）：Alpaca数据集上4个位置的校准前后对比。
    - 在线系统吞吐量（图7-8）：两种引擎（Flash/Pro）下不同并发度的吞吐和验证预算曲线。
- **充分性与公平性**：
  - 离线实验覆盖多个模型规模、多种数据类型，统计指标清晰（接受长度/条件接受率）。
  - 所有方法在同一框架、同一数据上重训练，超参数对齐（如Eagle3的TTT horizon与块大小一致），对比公平。
  - 在线实验基于真实用户流量，与生产基线直接比较，具有生态效度。
  - 消融实验系统验证了关键设计选择（深度、长度、置信头、校准等）。
- **客观性**：实验设计合理，结果呈现完整，包括正面和负面（如延迟开销极低），未发现选择性报告。

### 6. 论文的主要结论与发现

- DSpark在离线基准上**稳定大幅优于**自回归（Eagle3）和并行（DFlash）草稿模型，宏平均接受长度提升16%–31%。
- 半自回归结构成功结合了并行模型高初始接受率和自回归模型高后缀一致性，缓解了后缀衰减。
- 仅需2层草稿的DSpark即可超越5层DFlash，证明轻量顺序头的高效性。
- 置信度头能有效预测接受概率，经STS校准后误差降至约1%，可实现精确调度。
- 在DeepSeek-V4生产部署中，DSpark在相同吞吐量下将每用户生成速度提升60%–85%（Flash）和57%–78%（Pro）；在严格交互性约束下（如120 TPS）避免了基线的大幅吞吐下降，拓宽了可行工作区。
- 负载自适应调度器在高并发下自动缩短验证预算，保护批处理容量；在轻负载下充分利用闲置计算。

### 7. 优点：方法或实验设计上有哪些亮点

- **方法创新**：
  - 半自回归生成：仅需极小额外延迟（<1.3%）即可大幅提升接受率，参数效率高。
  - 置信度调度：将系统级吞吐最大化问题映射为贪心选择，并理论证明损失无损性。
  - 顺序温度缩放：创新地将传统模型校准扩展到顺序概率积场景，提升调度可靠性。
- **实验设计**：
  - 离线+在线双验证：离线控制变量测算法潜力，在线生产环境测实际效益。
  - 全面的消融：深度、长度、阈值、校准等维度均有实验支撑。
  - 与生产基线直接对比，且公开模型权重和训练代码（DeepSpec仓库），可复现性强。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验覆盖**：
  - 离线仅使用非思考模式（non-thinking mode），未覆盖推理链思考场景（如CoT）。
  - 在线部署仅针对DeepSeek-V4，未在不同模型架构（如LLaMA系列）或不同硬件上验证通用性。
  - 未评估长上下文（>4K tokens）下的表现；文中虽声称上下文长度影响可忽略，但仍需验证。
- **偏差风险**：
  - 置信度头依赖Markov特征，若前一token采样质量差，后续预估可能偏差，但调度因果性已通过早停保证。
  - 异步调度（用两步前的吞吐预估当前验证预算）可能在小波动场景引入亚优性，但实际效果良好。
- **应用限制**：
  - 固定草稿侧开销：对于接受率极低的复杂查询，生成完整草稿块的计算可能无法收回。
  - 训练依赖目标模型分布：需要目标模型在线提供logits或隐藏状态，增加训练复杂度（论文已通过隐藏状态通信和anchor packing优化）。
  - 硬件调度需预先profiling SPS曲线，且在非smooth硬件上需异步修改，增加工程实现难度。

（完）
