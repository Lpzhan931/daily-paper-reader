---
title: "DepthWeave-KV: Token-Adaptive Cross-Layer Residual Factorization for Long-Context KV Cache Compression"
title_zh: DepthWeave-KV：面向长上下文KV缓存压缩的令牌自适应跨层残差分解
authors: "Anna Cordoba, Adam Puente Tercero, Nerea Angulo Hijo, Mar Linares Tercero, Julia Barrientos, Ainhoa Miranda, Jesus Olivera"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06523v1"
tags: ["query:llm"]
score: 8.0
evidence: 用于高效LLM推理的KV缓存压缩
tldr: 长上下文语言模型推理受限于KV缓存内存瓶颈，现有压缩方法对词元和层采用统一预算，导致检索关键信息丢失。DepthWeave-KV通过跨层残差分解共享低秩通道基，并利用词元条件路由器为指令承载和检索关键词元分配更高重建秩，结合在线误差跟踪无需重训即可自适应调整压缩。在LongBench等基准上，以8.3倍KV内存压缩实现接近全缓存的准确率，64K上下文中速度达72.8 tok/s，显著优于此前压缩缓存，平衡了内存效率与任务质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06523v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 859, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2607.06523v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 860, \"height\": 481}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06523v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 909, \"height\": 545}, {\"url\": \"assets/tables/arxiv/2607.06523v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1115, \"height\": 1033}]"
motivation: 解决现有KV缓存压缩方法统一预算导致检索关键信息在词汇和语义需求不同时丢失的问题。
method: 跨层残差分解共享低秩基，词元自适应分配重建秩，使用注意力输出探针在线跟踪误差动态调整压缩。
result: 8.3倍KV内存压缩，64K上下文每秒72.8词元，多个长上下文基准接近全缓存性能。
conclusion: DepthWeave-KV实现高效自适应KV缓存压缩，显著降低内存占用同时保持检索准确性和任务质量。
---

## 摘要
长上下文语言模型推理日益受到存储键值缓存所需内存带宽和容量的限制，然而现有压缩方法通常对各层或各令牌应用统一预算，并在词汇线索与语义状态需要不同保留时降低检索质量。我们提出DepthWeave-KV，一种令牌自适应缓存压缩方法，该方法利用共享低秩通道基对相邻Transformer层的键值状态进行分解，同时在注意力行为敏感处保留轻量令牌特定残差。DepthWeave-KV结合跨深度残差分解与令牌条件深度路由器，为指令承载和检索关键的令牌分配更高重建秩，并使用来自注意力输出探针的无校准在线误差跟踪，在生成过程中自适应压缩而无需重新训练基础模型。融合的CUDA实现联合执行基查找、残差反量化和注意力投影，以减少解码时的内存流量。在LongBench、大海捞针、L-Eval以及长形式问答和摘要基准测试中，DepthWeave-KV以显著更低的内存使用达到接近全缓存的任务质量，在实现8.3倍KV内存减少和64K上下文下每秒72.8个令牌的同时，相较于先前压缩缓存提高了平均得分和检索准确率。

## Abstract
Long-context language model inference is increasingly limited by the memory bandwidth and capacity required to store key-value caches, yet existing compression methods often apply uniform budgets across layers or tokens and degrade retrieval when lexical cues and semantic states require different preservation. We introduce DepthWeave-KV, a token-adaptive cache compression method that factorizes key and value states across neighboring transformer layers using shared low-rank channel bases while retaining lightweight token-specific residuals where attention behavior is sensitive. DepthWeave-KV combines cross-depth residual factorization with a token-conditional depth router that allocates higher reconstruction rank to instruction-bearing and retrieval-critical tokens, and uses calibration-free online error tracking from attention-output probes to adapt compression during generation without retraining the base model. A fused CUDA implementation jointly performs basis lookup, residual dequantization, and attention projection to reduce decode-time memory traffic. Across LongBench, Needle-in-a-Haystack, L-Eval, and long-form QA and summarization benchmarks, DepthWeave-KV achieves near-full-cache task quality with substantially lower memory use, improving average score and retrieval accuracy over prior compressed caches while reaching 8.3x KV memory reduction and 72.8 tokens per second at 64K context.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：长上下文语言模型推理的**KV缓存**成为主要内存带宽和容量瓶颈。现有压缩方法（如令牌驱逐、量化、低秩近似、层共享）通常对所有层和所有令牌施加**统一压缩预算**，忽略了不同层（浅层词汇锚点 vs 深层语义抽象）和不同令牌（指令分隔符、实体提及、检索关键跨度 vs 流畅续写令牌）对缓存保真度的**异质性需求**。
- **动机**：这种“一刀切”的压缩策略会导致**检索退化**——即便平均困惑度或短上下文基准表现良好，在需要精确召回的长上下文任务（如“大海捞针”、多跳QA）中仍会出现脆性失败。论文因此提出一种**同时具有深度感知、令牌自适应和在线误差跟踪能力**的压缩机制。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想：跨层残差分解 + 令牌条件深度路由
- 将相邻Transformer层（深度窗口）的KV缓存分解为**共享低秩通道基**和**令牌特定残差**。大部分令牌仅需共享基表示，检索关键令牌则保留高保真残差。
- 利用**注意力输出探针**在线跟踪重建误差，无校准地自适应调整压缩比，无需重训基础模型。

### 2.2 关键技术细节
- **跨深度残差分解**：对每个注意力头，在重叠深度窗口 \(W_m\) 内维护共享低秩基 \(B_{m,h}^K, B_{m,h}^V \in \mathbb{R}^{r_b \times d}\)。对于令牌 \(t\) 在层 \(\ell\) 的键/值状态，重建公式为：
  \[
  \hat{X}_{\ell,h,t} = A_{\ell,h,t} B_{m,h}^Z + g_{\ell,h,t} R_{\ell,h,t}^Z, \quad Z \in \{K,V\}
  \]
  其中 \(A\) 是紧凑系数向量（8-bit量化），\(R\) 是稀疏残差（4-bit量化），\(g\) 是轻量残差门控。
- **令牌条件深度路由器**：根据四个在线特征（累积注意力质量、最大注意力尖峰、定界符/指令指示符、注意力输出重建误差）计算路由分数，分配三个残差秩等级（ρ=0/2/4/8）。非关键续写令牌可被分配零残差，仅用共享基表示。
- **无校准在线误差跟踪**：每隔 \(p\) 步解码，对少数头部和查询位置进行两次注意力计算（分别使用压缩缓存和临时高保真缓存），计算注意力输出相对误差 \(e_{\ell,h}\)。若误差超阈值则降低路由阈值，反之提高阈值。
- **融合CUDA实现**：联合执行基查找、残差反量化、系数乘法和注意力投影，避免重建后的KV张量写回全局内存，减少解码时内存流量。

## 3. 实验设计

### 3.1 数据集/场景
- **检索/QA类**：Needle-in-a-Haystack（16K-128K上下文，0%-100%深度）、Qasper、HotpotQA、MultiFieldQA-en、TriviaQA
- **长文档摘要类**：GovReport、QMSum
- **综合长上下文基准**：LongBench、L-Eval、NarrativeQA
- 所有实验在 **64K令牌预填长度**、贪婪解码512个生成令牌下进行。

### 3.2 对比方法
- **无压缩**: Full KV Cache
- **令牌驱逐/保留类**: StreamingLLM、H2O、SnapKV、PyramidKV
- **深度共享类**: MiniCache、KVSharer
- **语义分块/混合策略**: ChunkKV、TailorKV
- **低秩空间压缩**: Eigen Attention
- **本文方法**: DepthWeave-KV

### 3.3 评估指标
- 平均任务得分、Needle准确率、KV内存缩减倍数、解码吞吐量（tok/s）、首令牌延迟（TTFT）、困惑度增量、注意力输出重建误差。

## 4. 资源与算力
- **未明确说明**：文中未提及使用的GPU型号、数量、训练时长。仅提到学习组件（残差门和路由器参数）在无标签长上下文文本上一次性训练，使用**冻结教师缓存目标**，但未提供具体算力配置或训练成本数据。

## 5. 实验数量与充分性
- **总体实验组数**：主表1包含11种方法对比，覆盖7个关键指标。消融研究表2包含6个变体实验。此外，论文按任务家族（提取式/检索密集型 vs 摘要型）进行了分解分析。
- **充分性评估**：
  - **优点**：覆盖了多种长上下文任务类型（检索、QA、摘要），对比了多种主流压缩范式，消融实验系统地验证了核心组件（跨层基共享、令牌路由、在线误差跟踪、残差门、融合内核）的贡献。
  - **局限性**：
    1. 未报告实验的**统计显著性**或**多次重复的方差**，难以判断性能差异是否稳定。
    2. 仅评估64K上下文，未在更长上下文（如128K+）下测试缩放行为。
    3. 未提供不同压缩预算（如4×、6×、10×）下的质量-内存权衡曲线，仅展示单一目标（≥4×压缩下的最佳质量）。
    4. 缺乏对**多轮对话**或**批处理推理**场景的评估。
  - 总体实验设计**客观，但可更全面**。

## 6. 主要结论与发现
- DepthWeave-KV在**8.3倍内存压缩**下达到**平均任务得分62.9%**（接近全缓存63.8%），Needle准确率**96.1%**（全缓存98.7%），显著优于所有压缩基线（最佳基线TailorKV：61.4%得分、92.6%准确率）。
- 解码吞吐量达**72.8 tok/s**，比TailorKV高12%，比MiniCache高15.2%。首令牌延迟降至4.89s（全缓存6.84s）。
- 消融实验表明：令牌自适应路由和残差门是检索鲁棒性的关键（去除后Needle准确率降至91.8%）；跨层基共享提供主要内存缩减；在线误差跟踪进一步减少重建误差。

## 7. 优点（亮点）
1. **设计协同性**：将深度冗余利用（层间共享）与令牌异质性（自适应残差分配）有机融合，而非简单的层共享或令牌驱逐。
2. **无校准在线适应**：利用注意力输出探针而非固定压缩预算，无需下游校准数据，能动态响应生成过程中的检索需求变化。
3. **系统效率**：融合CUDA内核避免了重建KV张量的全局内存写入，直接服务于注意力计算，显著提升吞吐、降低延迟。
4. **轻量化学习**：仅学习残差门和路由器参数（而非重训整个模型），部署开销低。
5. **广泛基准覆盖**：在多种长上下文任务上验证，并展示了检索密集型任务相较于摘要类任务的更大改进。

## 8. 不足与局限
1. **深度窗口假设**：假设相邻层缓存冗余最高，但某些模型可能存在非连续层共享模式或异构层角色，固定窗口可能次优。
2. **路由器目标的局限性**：当前优化注意力输出重建误差，但检索失败可能源于罕见的后期重要令牌，缺乏更直接的下游对齐（如预期风险最小化）。
3. **系统集成未解决**：未考虑与批处理、前缀复用、解耦预填充/解码、多租户缓存池等生产系统特性的协同。
4. **缺乏长上下文缩放分析**：仅在64K下验证，未展示在128K+下内存/质量缩放行为，也未与其他方法在同等预算下进行多级压缩比的全曲线对比。
5. **资源信息缺失**：未报告训练/推理所需GPU算力，不利于可复现性和成本评估。
6. **未报告统计变异**：无法判断性能改进的显著性，可能受单次运行随机性影响。

（完）
