---
title: "DSpark: Confidence-Scheduled Speculative Decoding with Semi-Autoregressive Generation"
title_zh: DSpark：基于置信度调度的半自回归推测解码
authors: "Xin Cheng, Xingkai Yu, Chenze Shao, Jiashi Li, Yunfan Xiong, Yi Qian, Jiaqi Zhu, Shirong Ma, Xiaokang Zhang, Jiasheng Ye, Qinyu Chen, Chengqi Deng, Jiping Yu, Damai Dai, Zhengyan Zhang, Yixuan Wei, Yixuan Tan, Wenkai Yang, Runxin Xu, Yu Wu, Zhean Xu, Xuanyu Wang, Muyang Chen, Rui Tian, Xiao Bi, Zhewen Hao, Shaoyuan Chen, Huanqi Cao, Wentao Zhang, Anyi Xu, Huishuai Zhang, Dongyan Zhao, Wenfeng Liang"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05147v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 半自回归生成与自适应验证的投机解码
tldr: "针对大型语言模型推理加速，现有推测解码中平行草稿缺乏词间依赖导致接受率快速下降，且无差别验证浪费批处理容量。DSpark提出半自回归架构，在平行骨干后加轻量顺序模块建模块内依赖，抑制后缀衰减；同时采用置信度调度验证，根据估计的存活概率和引擎吞吐动态调整验证长度。在离线基准上显著提升接受长度，在DeepSeek-V4生产系统中，同等吞吐下每用户生成速度提升60-85%，并扩展了Pareto前沿，实现了之前不可达的性能层级。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05147v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1490, \"height\": 967}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1563, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1476, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1573, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1569, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1570, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1643, \"height\": 623}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1480, \"height\": 895}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05147v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1635, \"height\": 641}]"
motivation: 现有平行草稿因缺乏词间依赖导致接受率快速下降，且无差别验证浪费批处理容量，限制了高并发系统吞吐。
method: 采用半自回归架构（平行骨干+轻量顺序模块）建模块内依赖，并结合置信度调度动态调整验证长度。
result: "离线基准上接受长度显著提升；在DeepSeek-V4生产系统中，同等吞吐下每用户生成速度提升60-85%。"
conclusion: DSpark通过自适应验证消除了验证浪费，扩展了系统Pareto前沿，实现了先前不可达的性能层级。
---

## 摘要
推测解码通过将草稿生成与目标验证解耦来加速大语言模型推理。尽管最近的并行草稿模型能够通过单次前向传递高效生成较长的令牌序列，但由于缺乏令牌间依赖关系，其接受率会迅速衰减。此外，不加区分地验证这些扩展块会浪费关键的批处理容量在高拒绝风险的令牌上，严重降低高并发服务系统的吞吐量。我们提出了DSpark，一个将高吞吐量并行生成与自适应、负载感知验证相统一的推测解码框架。为了保持草稿质量，DSpark采用半自回归架构，将并行主干与轻量级序列模块耦合，以引入块内依赖建模并缓解后缀衰减。为了优化系统效率，DSpark使用置信度调度验证，根据估计的前缀生存概率和引擎特定的吞吐量配置文件，动态调整每个请求的验证长度。在跨多个领域的离线基准测试中，DSpark在接受的令牌长度上显著优于最先进的自回归和并行草稿模型。当在真实用户流量下部署于DeepSeek-V4服务系统时，DSpark成功减轻了验证浪费。与已建立的生产基线（MTP-1）相比，DSpark在匹配的吞吐量水平下将每用户生成速度提高了60%至85%。更重要的是，通过防止在严格交互性约束下吞吐量的严重下降，它实现了以前无法达到的性能等级，改变了我们服务系统的帕累托前沿。

## Abstract
Speculative decoding accelerates Large Language Model (LLM) inference by decoupling draft generation from target verification. While recent parallel drafters efficiently propose long token sequences in a single forward pass, they suffer from rapid acceptance decay due to a lack of inter-token dependencies. Furthermore, indiscriminately verifying these extended blocks wastes critical batch capacity on tokens with high rejection risks, severely degrading throughput in high-concurrency serving systems. We introduce DSpark, a speculative decoding framework that unifies high-throughput parallel generation with adaptive, load-aware verification. To maintain draft quality, DSpark utilizes a semi-autoregressive architecture, coupling a parallel backbone with a lightweight sequential module, to introduce intra-block dependency modeling and mitigate suffix decay. To optimize system efficiency, DSpark employs confidence-scheduled verification, dynamically tailoring the verification length for each request based on estimated prefix survival probabilities and engine-specific throughput profiles. On offline benchmarks across diverse domains, DSpark substantially improves the accepted length over state-of-the-art autoregressive and parallel drafters. When deployed within the DeepSeek-V4 serving system under live user traffic, DSpark successfully mitigates verification waste. Compared to the established production baseline (MTP-1), DSpark accelerates per-user generation speeds by 60 to 85 percent at matched throughput levels. More importantly, by preventing severe throughput degradation under strict interactivity constraints, it enables performance tiers that were previously unattainable, shifting the Pareto frontier of our serving system.

---

## 论文详细总结（自动生成）

# DSpark 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：大型语言模型（LLM）自回归推理延迟与输出长度成正比，GPU利用率低。推测解码（Speculative Decoding）通过轻量草稿模型提出候选令牌、目标模型并行验证来加速推理，但面临两大瓶颈：
  - **草稿质量**：并行草稿模型（如DFlash）虽单次前向生成长块，但各位置独立预测，缺乏块内令牌间依赖，导致后缀接受率快速衰减（多模态冲突）。
  - **系统效率**：无差别验证所有草稿令牌，在高并发场景下浪费关键批处理容量于高拒绝风险的令牌，降低整体吞吐。
- **整体含义**：本文旨在统一高吞吐并行生成与自适应、负载感知的验证，在保证理论无损的前提下，同时提升草稿质量和系统吞吐。

## 2. 论文提出的方法论

### 2.1 核心思想
- **半自回归生成**：保留并行骨干（基于DFlash，高效产生隐藏状态和基础logits），叠加轻量级顺序模块（Markov头或RNN头）注入块内依赖，使每位置能条件于之前已采样令牌，缓解后缀衰减。
- **置信度调度验证**：训练置信度头预测每位置条件接受概率，经顺序温度缩放（STS）校准后，通过硬件感知前缀调度器（Algorithm 1）在全局吞吐最大化目标下动态决定每个请求的验证长度。

### 2.2 关键技术细节
- **半自回归架构**（公式4）：
  - 并行骨干：单次前向生成基础logits \(U_k\)。
  - 顺序模块：
    - Markov头：低秩双线性变换 \(B(x_{k-1}, x_k) = W_1[x_{k-1}]W_2\)，仅依赖前一个令牌。
    - RNN头：维持循环状态 \(s_k\)，累积块内所有历史信息，生成依赖更长的偏差。
- **置信度头**（公式7）：输入骨干隐藏状态和前一个令牌的Markov嵌入，经线性层+sigmoid输出 \(c_k\)，监督信号为分析接受率 \(c_k^* = 1 - \frac{1}{2}\|p_k^d - p_k^t\|_1\)。
- **校准**：顺序温度缩放（STS），从左到右逐位置最小化累积乘积的期望校准误差（ECE），保持排序不变。
- **硬件感知前缀调度**（Algorithm 1）：
  - 计算每个请求的累积生存概率 \(a_{r,j} = \prod_{i\le j} c_{r,i}\)。
  - 全局贪心搜索：将所有候选扩展按 \(a_{r,j}\) 降序排列，逐步添加，检验当前批大小下的系统吞吐 \(\Theta = \tau \cdot \text{SPS}(B)\)，一旦吞吐下降则停止（保证因果性、无损性）。
  - 工程适配：生产环境中采用异步调度（基于两步前的置信度估计当前容量），避免GPU流水线停滞，并通过全局搜索（不早停）结合异步隔离确保无损。

### 2.3 训练目标
- 联合优化三项损失（公式12）：交叉熵 \(\mathcal{L}_\text{ce}\)、分布匹配 \(\mathcal{L}_\text{tv}\)（最小化总变差距离，等价于最大化接受率）、置信度二元交叉熵 \(\mathcal{L}_\text{conf}\)，位置加权 \(w_k = \exp(-(k-1)/\gamma)\)。
- 目标模型冻结，共享嵌入层和LM头，仅更新骨干、顺序模块和置信度头。

## 3. 实验设计

- **数据集/场景**：
  - **离线基准**：三个领域共9个数据集。
    - 数学推理：GSM8K、MATH500、AIME25。
    - 代码生成：MBPP、HumanEval、LiveCodeBench。
    - 日常对话：MT-Bench、Alpaca、Arena-Hard。
  - **在线部署**：DeepSeek-V4-Flash（preview）和DeepSeek-V4-Pro（preview）生产服务系统，在真实用户流量下评估。
- **基准方法**：
  - 自回归草稿器：Eagle3（基于TTT，块大小7，1层）。
  - 并行草稿器：DFlash（5层，块大小7）。
  - DSpark（默认Markov头，5层，块大小7）。
- **目标模型**：Qwen3-4B/8B/14B、Gemma4-12B。
- **评估指标**：离线报告每轮接受长度 \(\tau\)（含bonus token）；在线报告系统吞吐（tokens/s/GPU）和每用户生成速度（tok/s/user）。

## 4. 资源与算力

论文未明确说明训练所使用的GPU型号、数量或训练总时长。仅提及训练数据为Open-PerfectBlend（130万样本），每个草稿器训练10个epoch以保证收敛。对于在线部署，由DeepSeek-V4的MoE架构和mHC等优化支撑，但具体算力规模未公开。

## 5. 实验数量与充分性

- **数量**：离线实验覆盖4个目标模型 × 9个数据集 = 36个主要结果（表1）；此外还包含：
  - 位置逐条件接受率分析（图2）。
  - 草稿器深度消融（1/2/5层，图3）。
  - 提案长度消融（4/8/12/16，图4）。
  - 延迟开销测量（图4右）。
  - 置信度阈值扫描（图5）和可靠性图（图6）。
  - 在线部署：V4-Flash和V4-Pro两个引擎，不同SLA锚点（80/120 tok/s/user等），吞吐-互动机曲线（图7）、并发量-吞吐/验证预算图（图8）。
- **充分性与公平性**：
  - 对比方法均在同一框架、同一训练数据下重训，超参数对齐（块大小、特征层、层数等）。
  - 离线评估采用标准推测解码（温度1.0），禁用调度器以隔离草稿质量。
  - 在线部署与生产基线MTP-1直接对比，数据来自真实用户流量（散点+拟合曲线）。
  - 实验设计较为全面，能客观反映算法各部分贡献和系统级收益。

## 6. 论文的主要结论与发现

1. **草稿质量显著提升**：DSpark在离线基准上宏观平均接受长度比Eagle3提高约30%，比DFlash提高约18%（Qwen3系列）。数学/代码任务提升更大，对话任务也有显著改善（表1）。
2. **半自回归设计有效**：位置分析（图2）显示DSpark继承了并行模型的初始高接受率（因深层骨干），同时通过轻量顺序模块缓解了后缀衰减，使接受率更平稳。
3. **置信度调度减少验证浪费**：阈值扫描（图5）证明置信度头可有效筛选出大概率被拒绝的后缀令牌，且经STS校准后概率估计可靠（ECE降至~1%，图6）。
4. **生产部署扩展帕累托前沿**：
   - 在DeepSeek-V4-Flash和Pro中，DSpark在匹配吞吐下每用户速度提升60-85%（图7）。
   - 在严格交互性约束（高SLA）下，DSpark避免了MTP-1的吞吐崩溃，实现了之前不可达的性能层级。
   - 硬件感知调度器自动根据负载调节验证长度（图8），低负载时分配更多预算，高负载时主动裁剪，稳定系统吞吐。

## 7. 优点

- **方法论亮点**：
  - 半自回归架构巧妙平衡并行效率与序列质量，仅增加极小延迟（约1%）。
  - 将验证长度选择建模为全局吞吐最大化问题，并给出可解的贪心算法，理论保证无损（通过早停保持因果性）。
  - 置信度头经顺序温度缩放校准，提供可靠的概率估计，是调度器运行的基础。
- **实验设计亮点**：
  - 离线+在线双重验证，覆盖学术基准和生产真实流量，结果可信度高。
  - 消融实验（深度、长度、阈值、校准）充分剖析各组件贡献。
  - 在线部署中不仅报告平均提升，还通过帕累托曲线展示系统能力的扩展。
- **工程实践亮点**：
  - 生产中采用异步调度、可变长度执行（解耦逻辑与物理执行），解决与CUDA图重放和零开销调度的冲突。
  - 开源检查点和训练代码库（DeepSpec），促进社区发展。

## 8. 不足与局限

- **方案局限**：
  - 即使调度器减少验证浪费，草稿生成（并行骨干）仍需固定开销，对于接受率极低的请求，这些计算不可恢复。
  - 置信度头依赖Markov特征（前一个令牌），在长跨度依赖场景下可能不够准确；RNN头能缓解但实现更复杂。
  - 调度器假设吞吐曲线SPS(B)单调/平滑，实际中通过异步近似处理，可能引入微小次优性。
- **实验局限**：
  - 离线实验的目标模型仅限于Qwen3系列和Gemma4-12B，未在更大模型（如数百B）或不同架构（如纯稠密模型）上验证。
  - 训练数据仅为Open-PerfectBlend，虽然领域多样，但未在更多样化的指令数据上验证泛化性。
  - 未报告置信度头在不同领域上的校准误差细节（仅展示Alpaca的可靠性图）。
  - 在线部署的硬件配置未说明，结果可能依赖特定GPU和架构优化。
- **研究局限**：
  - 对比方法中未包括最新并发工作（如Domino、DFlare等，仅在相关工作中提及但未实验）。
  - 未探讨草稿长度\(\gamma\)的自适应设置（论文固定为7或5），仅通过调度器动态截断。

（完）
