---
title: "DSpark: Confidence-Scheduled Speculative Decoding with Semi-Autoregressive Generation"
title_zh: "DSpark: 基于置信度调度的半自回归推测解码"
authors: "Xin Cheng, Xingkai Yu, Chenze Shao, Jiashi Li, Yunfan Xiong, Yi Qian, Jiaqi Zhu, Shirong Ma, Xiaokang Zhang, Jiasheng Ye, Qinyu Chen, Chengqi Deng, Jiping Yu, Damai Dai, Zhengyan Zhang, Yixuan Wei, Yixuan Tan, Wenkai Yang, Runxin Xu, Yu Wu, Zhean Xu, Xuanyu Wang, Muyang Chen, Rui Tian, Xiao Bi, Zhewen Hao, Shaoyuan Chen, Huanqi Cao, Wentao Zhang, Anyi Xu, Huishuai Zhang, Dongyan Zhao, Wenfeng Liang"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05147v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 基于置信度调度的半自回归生成推测解码
tldr: "推测解码中并行生成因缺乏依赖导致接受率下降，无差别验证浪费容量。DSpark框架采用半自回归架构，结合并行骨干与轻量顺序模块引入块内依赖，并通过置信度调度基于前缀存活概率和引擎特性动态调整验证长度。离线测试中接受长度显著提升，在线系统中用户速度提升60-85%，避免吞吐下降，推动性能前沿。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05147v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1490, \"height\": 967}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1563, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1476, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1573, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1569, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1570, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1643, \"height\": 623}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1480, \"height\": 895}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05147v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1635, \"height\": 641}]"
motivation: 并行草案生成缺乏依赖导致接受率快速衰减，无差别验证浪费批量容量，降低高并发吞吐。
method: 半自回归架构耦合并行骨干与轻量顺序模块，引入块内依赖；基于前缀生存概率和引擎吞吐动态调整验证长度。
result: "离线基准上接受长度大幅提升；在线系统中用户生成速度提升60-85%，避免严格交互约束下的吞吐下降。"
conclusion: DSpark统一高效并行生成与自适应验证，在保证质量的同时提升系统吞吐，为推测解码部署提供新方案。
---

## 摘要
推测解码通过将草稿生成与目标验证解耦，加速大型语言模型（LLM）推理。尽管最近的并行草稿生成器能够单次前向传播高效地提出长令牌序列，但由于缺乏令牌间依赖关系，它们会遭遇快速接受衰减的问题。此外，不加区分地验证这些扩展块会在高拒绝风险的令牌上浪费关键批处理容量，严重降低高并发服务系统的吞吐量。我们引入了DSpark，一种将高吞吐量并行生成与自适应、负载感知验证相统一的推测解码框架。为了保持草稿质量，DSpark采用半自回归架构，将并行主干与轻量级顺序模块相结合，引入块内依赖建模并缓解后缀衰减。为了优化系统效率，DSpark采用基于置信度调度的验证，根据估计的前缀生存概率和引擎特定的吞吐量特征，动态调整每个请求的验证长度。在跨多个领域的离线基准测试中，DSpark相较于最先进的自回归和并行草稿生成器显著提高了接受长度。当部署在DeepSeek-V4服务系统中并承受实时用户流量时，DSpark成功减少了验证浪费。与已建立的生产基线（MTP-1）相比，DSpark在匹配的吞吐量水平下将每用户生成速度提升了60%至85%。更重要的是，通过在严格的交互性约束下防止严重的吞吐量下降，它实现了以前无法达到的性能层级，从而移动了我们服务系统的帕累托前沿。

## Abstract
Speculative decoding accelerates Large Language Model (LLM) inference by decoupling draft generation from target verification. While recent parallel drafters efficiently propose long token sequences in a single forward pass, they suffer from rapid acceptance decay due to a lack of inter-token dependencies. Furthermore, indiscriminately verifying these extended blocks wastes critical batch capacity on tokens with high rejection risks, severely degrading throughput in high-concurrency serving systems. We introduce DSpark, a speculative decoding framework that unifies high-throughput parallel generation with adaptive, load-aware verification. To maintain draft quality, DSpark utilizes a semi-autoregressive architecture, coupling a parallel backbone with a lightweight sequential module, to introduce intra-block dependency modeling and mitigate suffix decay. To optimize system efficiency, DSpark employs confidence-scheduled verification, dynamically tailoring the verification length for each request based on estimated prefix survival probabilities and engine-specific throughput profiles. On offline benchmarks across diverse domains, DSpark substantially improves the accepted length over state-of-the-art autoregressive and parallel drafters. When deployed within the DeepSeek-V4 serving system under live user traffic, DSpark successfully mitigates verification waste. Compared to the established production baseline (MTP-1), DSpark accelerates per-user generation speeds by 60 to 85 percent at matched throughput levels. More importantly, by preventing severe throughput degradation under strict interactivity constraints, it enables performance tiers that were previously unattainable, shifting the Pareto frontier of our serving system.

---

## 论文详细总结（自动生成）

好的，遵照您的要求，以下是根据给定论文内容整理的中文总结，严格遵循您指定的结构和要点。

---

### **DSpark: 基于置信度调度的半自回归推测解码 (论文总结)**

#### **1. 论文的核心问题与整体含义 (研究动机和背景)**
*   **核心问题：** 在大语言模型 (LLM) 的推测解码 (Speculative Decoding) 中，存在两个关键瓶颈：
    1.  **生成质量瓶颈：** 现有的高效**并行**草案生成器（如 DFlash）虽然单次前向传播即可生成长序列，但由于缺乏令牌间依赖，导致后续位置接受率快速衰减（suffix decay）。
    2.  **系统效率瓶颈：** 不加区分地验证所有提议令牌在高并发场景下浪费了宝贵的批处理容量，尤其是对于拒绝风险高的令牌，导致整体系统吞吐量下降。
*   **整体含义：** 本工作旨在统一高吞吐的并行生成与自适应的、感知负载的验证机制，以解决上述双重瓶颈，从而在保持生成质量的同时，最大化生产环境下 LLM 服务的吞吐量和响应速度。

#### **2. 论文提出的方法论：核心思想、关键技术**
*   **核心思想：** DSpark 是一种推测解码框架，通过两个互补的机制解决核心瓶颈：
    1.  **半自回归架构**：结合并行骨干网络的速度和自回归模型的依赖建模能力。
    2.  **基于置信度调度的验证**：动态调整每个请求的验证长度，以匹配当前系统负载和令牌质量。
*   **关键技术细节：**
    *   **半自回归生成 (Semi-Autoregressive Generation)：**
        *   **并行阶段 (Parallel Stage)：** 使用一个重的并行骨干网络（实例化自 DFlash）进行一次前向传播，生成所有草案令牌的隐藏状态和基础 logits。
        *   **顺序阶段 (Sequential Stage)：** 在基础 logits 上添加一个轻量级的顺序模块，引入前缀依赖。具体实现了两种变体：
            *   **马尔可夫头 (Markov Head)：** 仅依赖前一个令牌，通过低秩因子分解实现高效的一阶转移偏置。
            *   **循环神经网络头 (RNN Head)：** 通过维护循环状态来捕捉更长的前缀依赖。
    *   **基于置信度调度的验证 (Confidence-Scheduled Verification)：**
        *   **置信度头 (Confidence Head)：** 一个轻量级线性投影 + Sigmoid 模块，为每个草案位置输出一个标量 `c_k`，预测该位置的令牌在给定之前令牌都被接受的条件生存概率。使用每个位置的分析接受率（基于总变差距离）作为监督信号。
        *   **校准 (Post-hoc Calibration)：** 引入**顺序温度缩放 (Sequential Temperature Scaling, STS)** 对累积概率进行校准，以获得可靠的概率估计，避免因过度自信导致的调度次优。
        *   **硬件感知前缀调度器 (Hardware-Aware Prefix Scheduler)：** 将验证长度选择形式化为一个全局吞吐量最大化问题。算法（Algorithm 1）通过贪婪的方式，根据每个令牌的估计生存概率（`a_{r,j}`）进行全局排序，并结合预配置的引擎吞吐量曲线 (SPS(B)) 动态决定为每个请求验证多少令牌。该调度器通过早停机制保证无损解码。

#### **3. 实验设计**
*   **数据集/场景：**
    *   **离线评估：** 涵盖数学推理 (GSM8K, MATH500, AIME25)、代码生成 (MBPP, HumanEval, LiveCodeBench) 和日常聊天 (MT-Bench, Alpaca, Arena-Hard) 三大领域。
    *   **在线部署：** 在 DeepSeek-V4 服务系统的真实用户流量下进行评估，包括 V4-Flash (预览版) 和 V4-Pro (预览版) 两个引擎。
*   **Benchmark：** 离线评估主要指标为每解码轮次接受的令牌长度 (Accepted Length, τ)；在线评估衡量吞吐量-交互性帕累托前沿，即聚合吞吐量 vs. 每用户生成速度。
*   **对比方法：**
    *   **Eagle3 (自回归/AR)：** 最先进的自回归草案生成器。
    *   **DFlash (并行/Parallel)：** 最先进的并行草案生成器，也是 DSpark 的并行骨干。
    *   **MTP-1：** DeepSeek-V4 之前的官方生产基线（单令牌草案）。
*   **目标模型：** Qwen3-4B、8B、14B 和 Gemma4-12B (离线)；DeepSeek-V4-Flash/Pro (在线)。

#### **4. 资源与算力**
*   论文未明确说明训练所使用的具体 GPU 型号、数量或时长。文中提及使用了 **HAI-LLM** 内部训练框架，并采用了隐蔽状态通信和锚点界限序列打包等优化来降低内存和通信开销。训练数据为 **Open-PerfectBlend**（130 万个样本），草案模型训练了 **10 个 epoch**。

#### **5. 实验数量与充分性**
*   **实验数量：** 非常充分。进行了：
    *   跨 4 个目标模型、3 大领域、9 个数据集的离线主实验（Table 1）。
    *   草案模型深度（1-5层）和提议长度（γ=4, 8, 12, 16）的系统消融实验。
    *   置信度头有效性的诊断实验（阈值扫描、校准图）和延迟开销测量。
    *   DeepSeek-V4 在线系统下的完整吞吐量-交互性曲线对比，包括并发数、SLA 等维度。
*   **充分性与公平性：**
    *   **充分：** 实验设计完整，覆盖了算法性能、架构选择、系统效率等多个维度。
    *   **公平：** 为了进行公平比较，作者**重新训练了所有基线模型（包括 Eagle3 和 DFlash）**，并确保它们在相同的训练框架、数据和模型架构设定下进行。这极大地增加了实验结果的可靠性。

#### **6. 论文的主要结论与发现**
*   **离线性能：** DSpark 在所有评估的目标模型和领域上，其接受的令牌长度 (τ) 均显著优于 Eagle3 和 DFlash。例如，在 Qwen3 系列上，宏平均接受长度相比 Eagle3 提升约 27%-31%，相比 DFlash 提升约 16%-18%。
*   **在线性能：** 在 DeepSeek-V4 生产系统中，DSpark 相比 MTP-1 基线**将每用户生成速度提升了 60%~85%** (V4-Flash) 和 **57%~78%** (V4-Pro)，同时保持了相当的聚合吞吐量。
*   **帕累托前沿移动：** DSpark 成功**移动了 LLM 服务的帕累托前沿**。在严格的交互性约束下（如高 TPS 要求），MTP-1 的性能急剧下降，而 DSpark 能够维持高吞吐，实现了以前无法达到的性能层级。
*   **根本机制：** 半自回归架构有效结合了并行模型在初始位置的高容量和自回归模型在后缀位置的依赖建模能力；基于置信度的调度器能够根据系统负载动态分配验证预算，避免浪费。

#### **7. 优点**
*   **方法创新性强：** 巧妙地将半自回归生成与负载感知的置信度调度结合，同时解决了并行生成的质量问题和系统效率问题。
*   **实验设计严谨公平：** 统一重训所有基线，保证了对比的公平性；同时进行了详尽的离线分析和真实生产环境部署验证，说服力强。
*   **系统层面落地性好：** 详细描述了如何将调度器适配到生产环境（如异步化、解耦物理与逻辑执行），并开源了模型和训练仓库 (DeepSpec)，对社区有实际价值。
*   **结果突出：** 不仅学术指标优秀，更在实际生产系统中取得了显著的吞吐量和延迟收益，并突破了系统性能瓶颈。

#### **8. 不足与局限**
*   **固定草案生成成本：** 论文指出，即使置信度调度器会裁剪低分令牌，但草案生成（通过并行骨干）的固定计算成本依然存在。对于接受率极低的复杂查询，这部分计算是净开销。
*   **标定依赖：** 调度算法的有效性高度依赖于后处理校准 (STS) 的质量和离线预配置的引擎吞吐量曲线 (SPS) 的准确性。在实践中，环境动态变化可能导致 SPS 曲线不准确。
*   **工程复杂性：** 实现自适应验证长度需要修改推理引擎的低层内核（如稀疏注意力），并处理异步调度等复杂问题，工程实现门槛较高。
*   **泛化性验证有限：** 在线实验主要基于 DeepSeek-V4 系列模型 (MoE 架构)。虽然在离线实验中验证了 Qwen 和 Gemma 系列，但在其他架构（如纯 Dense 模型）或不同规模下的在线性能尚需进一步验证。
*   **未讨论内存开销：** 论文未详细比较 DSpark 与其他基线在训练和推理时的显存占用（如置信度头和顺序模块带来的额外参数），这可能影响在小模型或低显存设备上的部署。

（完）
