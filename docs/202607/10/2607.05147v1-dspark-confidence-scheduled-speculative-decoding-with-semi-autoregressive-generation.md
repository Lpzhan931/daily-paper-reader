---
title: "DSpark: Confidence-Scheduled Speculative Decoding with Semi-Autoregressive Generation"
title_zh: DSpark：基于置信度调度的半自回归投机解码
authors: "Xin Cheng, Xingkai Yu, Chenze Shao, Jiashi Li, Yunfan Xiong, Yi Qian, Jiaqi Zhu, Shirong Ma, Xiaokang Zhang, Jiasheng Ye, Qinyu Chen, Chengqi Deng, Jiping Yu, Damai Dai, Zhengyan Zhang, Yixuan Wei, Yixuan Tan, Wenkai Yang, Runxin Xu, Yu Wu, Zhean Xu, Xuanyu Wang, Muyang Chen, Rui Tian, Xiao Bi, Zhewen Hao, Shaoyuan Chen, Huanqi Cao, Wentao Zhang, Anyi Xu, Huishuai Zhang, Dongyan Zhao, Wenfeng Liang"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05147v1"
tags: ["query:llm"]
score: 8.0
evidence: 带置信度调度的半自回归投机解码用于LLM推理加速
tldr: "大语言模型推理中，并行草稿生成缺乏token间依赖导致接受率快速衰减，而盲目验证长块浪费批量容量。DSpark提出半自回归架构，结合并行主干与轻量级顺序模块以建模块内依赖，缓解后缀衰减；并采用置信度调度验证，基于前缀存活概率动态调整验证长度，优化高并发系统吞吐。在离线基准上，DSpark相比最先进的草稿方法显著提升接受长度。在DeepSeek-V4生产系统中部署后，用户生成速度提升60-85%，交互约束下吞吐不降，拓展了服务系统的帕累托前沿。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05147v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1490, \"height\": 967}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1563, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1476, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1573, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1569, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1570, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1643, \"height\": 623}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1480, \"height\": 895}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05147v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1635, \"height\": 641}]"
motivation: 并行草稿生成因缺乏token间依赖导致接受快速衰减，且盲目验证长块浪费系统容量，制约高并发推理吞吐。
method: DSpark采用半自回归架构（并行主干+轻量顺序模块）建模块内依赖，并基于置信度调度动态调整验证长度以适应负载。
result: "离线基准上接受长度显著优于现有方法；在线系统下用户生成速度提升60-85%，吞吐瓶颈被突破。"
conclusion: DSpark统一了高吞吐并行生成与自适应验证，有效提升服务效率，拓展了交互应用的性能边界。
---

## 摘要
投机解码通过将草稿生成与目标验证解耦，加速了大语言模型（LLM）的推理。尽管最近的并行草稿模型能够在单次前向传播中高效地生成长令牌序列，但由于缺乏令牌间的依赖关系，它们会遭受快速接受衰减。此外，不加区分地验证这些扩展块会在高拒绝风险的令牌上浪费关键的批处理容量，严重降低高并发服务系统的吞吐量。我们提出了DSpark，一个统一的投机解码框架，结合了高吞吐量的并行生成与自适应、负载感知的验证。为了保持草稿质量，DSpark采用了半自回归架构，将并行主干与轻量级顺序模块相结合，引入块内依赖建模以缓解后缀衰减。为了优化系统效率，DSpark采用了基于置信度调度的验证，根据估计的前缀生存概率和引擎特定的吞吐量特征，动态地为每个请求定制验证长度。在跨多个领域的离线基准测试中，DSpark相比于最先进的自回归和并行草稿模型显著提升了接受长度。当部署在DeepSeek-V4服务系统中并受真实用户流量影响时，DSpark成功减轻了验证浪费。与已建立的生产基线（MTP-1）相比，DSpark在匹配的吞吐量水平下将每个用户的生成速度提高了60%到85%。更重要的是，通过在严格的交互性约束下防止严重的吞吐量下降，它实现了以前无法达到的性能层级，改变了我们服务系统的帕累托前沿。

## Abstract
Speculative decoding accelerates Large Language Model (LLM) inference by decoupling draft generation from target verification. While recent parallel drafters efficiently propose long token sequences in a single forward pass, they suffer from rapid acceptance decay due to a lack of inter-token dependencies. Furthermore, indiscriminately verifying these extended blocks wastes critical batch capacity on tokens with high rejection risks, severely degrading throughput in high-concurrency serving systems. We introduce DSpark, a speculative decoding framework that unifies high-throughput parallel generation with adaptive, load-aware verification. To maintain draft quality, DSpark utilizes a semi-autoregressive architecture, coupling a parallel backbone with a lightweight sequential module, to introduce intra-block dependency modeling and mitigate suffix decay. To optimize system efficiency, DSpark employs confidence-scheduled verification, dynamically tailoring the verification length for each request based on estimated prefix survival probabilities and engine-specific throughput profiles. On offline benchmarks across diverse domains, DSpark substantially improves the accepted length over state-of-the-art autoregressive and parallel drafters. When deployed within the DeepSeek-V4 serving system under live user traffic, DSpark successfully mitigates verification waste. Compared to the established production baseline (MTP-1), DSpark accelerates per-user generation speeds by 60 to 85 percent at matched throughput levels. More importantly, by preventing severe throughput degradation under strict interactivity constraints, it enables performance tiers that were previously unattainable, shifting the Pareto frontier of our serving system.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：大型语言模型（LLM）推理中，投机解码通过轻量草稿模型生成候选令牌，再由目标模型并行验证，加速生成。并行草稿模型（如DFlash）能单步生成长块，但每个位置独立预测，缺乏块内令牌间依赖，导致多模态碰撞和后续位置接受率快速衰减（后缀衰减）。此外，不加区分地验证整个长块会浪费目标模型的批处理容量，尤其是在高并发服务系统中，低置信度的后缀令牌占用关键计算资源，降低系统吞吐。
- **核心问题**：如何既保持并行草稿的高吞吐能力，又建模令牌依赖以提升接受率，并动态优化验证长度以适应系统负载，从而最大化端到端性能。

## 2. 方法论：核心思想、关键技术细节

### 2.1 半自回归生成（Semi-Autoregressive Generation）

- **并行骨干（Parallel Backbone）**：采用DFlash架构，单次前向传播产生所有位置的隐藏状态和基础logits。将锚定令牌作为第一个预测位置，减少计算。
- **轻量级顺序模块（Sequential Head）**：在并行输出上施加因果依赖，修正基础logits。两种实现：
  - **Markov Head**：仅依赖前一个令牌，使用低秩分解（\( B = W_1 W_2 \)）实现一阶转移偏置。
  - **RNN Head**：维护循环状态，累积块内整个前缀历史，进一步利用更长的上下文。
- **优势**：并行骨干保持 \( T_{\text{draft}} \) 与块大小近似无关；顺序模块极小增加延迟，但显著改善后缀接受率。

### 2.2 置信度调度验证（Confidence-Scheduled Verification）

- **置信度头（Confidence Head）**：对每个草稿位置输出标量 \( c_k \in (0,1) \)，建模给定前缀已被接受下的条件接受概率。基于并行骨干的隐藏状态和前一令牌的Markov嵌入，通过sigmoid输出。监督信号为理论接受率 \( c_k^* = 1 - \frac{1}{2} \|p_k^d - p_k^t\|_1 \)。
- **后处理校准（Sequential Temperature Scaling, STS）**：由于神经网络通常过度自信，STS在留存验证集上从左到右依次为每个位置的累积乘积校准温度，降低期望校准误差（ECE），保持排序不变性。
- **硬件感知前缀调度器（Hardware-Aware Prefix Scheduler）**：将验证长度选择建模为全局吞吐量最大化问题。对每个请求 \( r \)，设计前缀生存概率 \( a_{r,j} = \prod_{i \le j} c_{r,i} \)。调度器利用预配置的引擎吞吐量曲线 SPS(\( B \))（每步吞吐 vs. 批大小），按 \( a_{r,j} \) 降序贪心采纳候选令牌，直到吞吐量不再提升（早期停止），确保因果关系和无损性。实际部署中采用异步版本以避免流水线停顿。

### 2.3 训练目标

- 总损失：\( \mathcal{L} = \alpha_{\text{ce}} \mathcal{L}_{\text{ce}} + \alpha_{\text{tv}} \mathcal{L}_{\text{tv}} + \alpha_{\text{conf}} \mathcal{L}_{\text{conf}} \)，各损失位置加权（指数衰减权重 \( w_k = \exp(-(k-1)/\gamma) \)）。
  - 交叉熵损失 \( \mathcal{L}_{\text{ce}} \)：预测真实下一令牌。
  - 分布匹配损失 \( \mathcal{L}_{\text{tv}} \)：最小化草稿分布与目标分布的总变差距离（直接优化接受率）。
  - 置信度损失 \( \mathcal{L}_{\text{conf}} \)：二值交叉熵，拟合软接受标签。

## 3. 实验设计

- **目标模型**：Qwen3-{4B, 8B, 14B}, Gemma4-12B。
- **草稿模型对比**：Eagle3（自回归，TTT-based）、DFlash（并行）。所有方法在相同框架和训练数据上重训练，对齐块大小、特征层和层数。
- **训练数据**：Open-PerfectBlend（1.3M样本，含聊天17.6%、数学39.4%、代码38.9%、指令遵循4.1%）。响应由目标模型重新生成。
- **评估协议**：标准投机解码，温度1.0，链式草稿。主要指标为每轮接受长度 \( \tau \)（包含奖励令牌）。
- **离线 Benchmark**：
  - 数学推理：GSM8K, MATH500, AIME25
  - 代码生成：MBPP, HumanEval, LiveCodeBench
  - 日常聊天：MT-Bench, Alpaca, Arena-Hard
- **在线部署**：在DeepSeek-V4-Flash（预览）和DeepSeek-V4-Pro（预览）生产系统中，与MTP-1（之前的生产基线）对比。测量聚合输出吞吐量 vs. 单用户生成速度（tok/s/user）。

## 4. 资源与算力

- **未明确说明**：论文未提供使用的GPU型号、数量或训练时长。仅提及训练在内部框架HAI-LLM上进行，并采用了隐藏状态通信和锚定序列打包优化以降低内存和通信开销。

## 5. 实验数量与充分性

- **主实验**：Table 1覆盖4种目标模型×3个领域×每领域3个数据集（共9个benchmark），对比Eagle3和DFlash，结果一致表明DSpark最优。
- **消融与分析实验**：
  - 位置条件接受率分析（图2）：展示不同架构的行为差异，验证半自回归设计的有效性。
  - 草稿深度实验（图3）：1–5层对比，证明2层DSpark即超越5层DFlash。
  - 提议长度实验（图4）：块大小4–16，Markov vs. RNN head，并测量延迟开销。
  - 置信阈值扫描（图5）：展示置信度头可有效修剪低置信令牌。
  - 校准分析（图6）：可靠性图显示校准后ECE降至约1%。
- **在线部署**：图7展示Pareto前沿，图8展示动态验证预算随并发变化。评估了两种引擎下多个SLA锚点。
- **公平性**：所有草稿模型在相同框架和数据上重训练，超参数对齐，对比严格。实验设计充分覆盖不同规模和领域。

## 6. 主要结论与发现

- **离线性能**：DSpark在Qwen3-{4B,8B,14B}上宏平均接受长度较Eagle3提升30.9%/26.7%/30.0%，较DFlash提升16.3%/18.4%/18.3%。在Gemma4-12B上同样领先。
- **位置分析**：并行草稿器（DFlash）首位置容量高但后缀衰减严重；自回归草稿器（Eagle3）后缀稳定但首位置弱；DSpark结合两者优势：高初始容量+稳定后缀。
- **领域差异**：结构化任务（数学、代码）接受率显著高于开放聊天，验证了动态调度必要性。
- **在线部署**：在DeepSeek-V4中，DSpark在匹配吞吐水平下，用户生成速度提升60–85%（Flash）和57–78%（Pro）。在严格交互性约束下（如120 TPS for Flash），DSpark避免吞吐崩溃，拓展了系统的可行区域，实现了之前无法达到的性能层级。

## 7. 优点

- **创新架构**：半自回归设计等效地结合了并行高速和高初始容量与顺序依赖建模，参数效率高（2层超越5层并行）。
- **系统感知调度**：将验证长度选择形式化为吞吐量最大化问题，利用校准的置信度头和实时引擎曲线，动态适应负载，避免固定阈值带来的次优。
- **部署兼容性**：解决了异步流水线、CUDA Graph replay、Zero-Overhead Scheduling等工程难题，设计了异步调度器，既保持无损性又兼容现有系统。
- **开源贡献**：开源了DSpark检查点以及DeepSpec训练仓库（含Eagle3、DFlash、DSpark），促进社区研究。

## 8. 不足与局限

- **固定草稿计算成本**：即使接受率低，并行骨干仍需生成整个块，不可恢复的计算被浪费。
- **异步调度近似**：在线部署中基于两步前的历史预测确定截断长度，虽可兼容系统，但可能偏离理论最优。
- **实验覆盖**：离线评估未包含极端长上下文或特定领域（如长文档、多语言）；在线部署仅在DeepSeek-V4架构下验证，通用性有待进一步测试。
- **超参数与敏感性**：未报告训练中α权重、块大小γ、顺序头选择等超参数的详细敏感度分析。
- **资源信息缺失**：算力细节未给出，不利于复现和成本估计。
- **校准依赖**：STS需要留存验证集，且校准质量影响调度效果；极端分布下校准可能失败。

（完）
