---
title: "DSpark: Confidence-Scheduled Speculative Decoding with Semi-Autoregressive Generation"
title_zh: DSpark：基于置信度调度的半自回归推测解码
authors: "Xin Cheng, Xingkai Yu, Chenze Shao, Jiashi Li, Yunfan Xiong, Yi Qian, Jiaqi Zhu, Shirong Ma, Xiaokang Zhang, Jiasheng Ye, Qinyu Chen, Chengqi Deng, Jiping Yu, Damai Dai, Zhengyan Zhang, Yixuan Wei, Yixuan Tan, Wenkai Yang, Runxin Xu, Yu Wu, Zhean Xu, Xuanyu Wang, Muyang Chen, Rui Tian, Xiao Bi, Zhewen Hao, Shaoyuan Chen, Huanqi Cao, Wentao Zhang, Anyi Xu, Huishuai Zhang, Dongyan Zhao, Wenfeng Liang"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05147v1"
tags: ["query:llm-sd"]
score: 8.0
evidence: 基于置信度调度的半自回归推测解码方法
tldr: "大模型推理中，现有并行草稿机制因缺乏token间依赖导致接受率快速衰减，且无差别验证浪费批次容量。DSpark提出半自回归架构，在并行骨干后附加轻量序列模块以建模块内依赖，缓解后缀衰减；同时采用置信度调度验证，基于前缀存活概率和引擎吞吐轮廓动态调整每个请求的验证长度。离线基准测试中，接受长度显著优于最先进的自动回归和并行草稿方法；在DeepSeek-V4生产系统下，用户生成速度提升60-85%，并避免了高并发下的吞吐量退化，实现了此前无法达到的性能层次，推动了服务系统的帕累托前沿。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05147v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1490, \"height\": 967}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1563, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1476, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1573, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1569, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1570, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1643, \"height\": 623}, {\"url\": \"assets/figures/arxiv/2607.05147v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1480, \"height\": 895}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05147v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1635, \"height\": 641}]"
motivation: 解决并行草稿生成中接受率快速下降和验证资源浪费问题，提升高并发场景下大模型推理效率。
method: 半自回归架构耦合并行骨干与轻量序列模块，及基于置信度的动态验证长度调度。
result: "离线接受长度显著提升；在线系统用户速度提升60-85%，实现新性能层次。"
conclusion: 统一高吞吐并行生成与自适应验证，优化系统吞吐与延迟权衡。
---

## 摘要
推测解码通过将草稿生成与目标验证解耦来加速大语言模型（LLM）推理。尽管近期并行草稿生成器能够在单次前向传播中高效生成长令牌序列，但由于缺乏跨令牌依赖，其接受率迅速衰减。此外，不加区分地验证这些扩展块会将关键的批处理容量浪费在高拒绝风险的令牌上，严重降低高并发服务系统的吞吐量。我们提出DSpark，一种统一高吞吐量并行生成与自适应、负载感知验证的推测解码框架。为保持草稿质量，DSpark采用半自回归架构，将并行主干与轻量级顺序模块相结合，引入块内依赖建模以缓解后缀衰减。为优化系统效率，DSpark使用置信度调度验证，基于估计的前缀存活概率和引擎特定的吞吐量特征，动态调整每个请求的验证长度。在跨多个领域的离线基准测试中，DSpark在接受的令牌长度上较最先进的自回归和并行草稿生成器有显著提升。当部署于DeepSeek-V4服务系统并面对真实用户流量时，DSpark成功减少了验证浪费。与成熟的生产基线（MTP-1）相比，在匹配的吞吐量水平下，DSpark将每位用户的生成速度提升60%至85%。更重要的是，通过防止在严格交互性约束下吞吐量的严重下降，它实现了先前无法达到的性能层级，推动了服务系统帕累托前沿的移动。

## Abstract
Speculative decoding accelerates Large Language Model (LLM) inference by decoupling draft generation from target verification. While recent parallel drafters efficiently propose long token sequences in a single forward pass, they suffer from rapid acceptance decay due to a lack of inter-token dependencies. Furthermore, indiscriminately verifying these extended blocks wastes critical batch capacity on tokens with high rejection risks, severely degrading throughput in high-concurrency serving systems. We introduce DSpark, a speculative decoding framework that unifies high-throughput parallel generation with adaptive, load-aware verification. To maintain draft quality, DSpark utilizes a semi-autoregressive architecture, coupling a parallel backbone with a lightweight sequential module, to introduce intra-block dependency modeling and mitigate suffix decay. To optimize system efficiency, DSpark employs confidence-scheduled verification, dynamically tailoring the verification length for each request based on estimated prefix survival probabilities and engine-specific throughput profiles. On offline benchmarks across diverse domains, DSpark substantially improves the accepted length over state-of-the-art autoregressive and parallel drafters. When deployed within the DeepSeek-V4 serving system under live user traffic, DSpark successfully mitigates verification waste. Compared to the established production baseline (MTP-1), DSpark accelerates per-user generation speeds by 60 to 85 percent at matched throughput levels. More importantly, by preventing severe throughput degradation under strict interactivity constraints, it enables performance tiers that were previously unattainable, shifting the Pareto frontier of our serving system.

---

## 论文详细总结（自动生成）

# DSpark: 基于置信度调度的半自回归推测解码——论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）自回归推理速度慢，GPU利用率低，用户感知延迟高。推测解码（Speculative Decoding）通过轻量草稿模型生成候选令牌、再由目标模型并行验证来加速推理，但现有方法存在两大瓶颈：
  - **生成质量瓶颈**：并行草稿生成器（如DFlash）虽能一次前向生成多令牌，但各位置独立预测，缺乏块内令牌间依赖，导致多模态冲突和后缀接受率快速衰减（suffix decay）。
  - **系统效率瓶颈**：固定长度的验证策略在高并发场景下浪费关键批处理容量——低置信度的后缀令牌大概率被拒绝，却仍占用验证计算，降低整体吞吐量。
- **整体含义**：本文提出DSpark框架，通过**半自回归生成架构**改善草稿质量，同时通过**置信度调度验证机制**实现动态、负载感知的验证长度选择，从而统一高吞吐并行生成与自适应验证，提升LLM推理的端到端效率。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在并行草稿生成后附加轻量级顺序模块以引入块内依赖，并利用置信度预测和系统吞吐量轮廓动态裁剪验证长度，最大化全局吞吐量。

### 2.1 半自回归生成（Semi-autoregressive Generation）

- **并行骨干（Parallel Backbone）**：基于DFlash架构，单次前向传播生成所有位置的隐藏状态和基础logits。输入为锚点令牌（anchor token）加γ-1个掩码令牌，输出γ个草稿logits。骨干保持并行，计算开销近似常数。
- **顺序头（Sequential Head）**：在并行骨干后附加轻量级序列模块，注入前缀依赖：
  - **Markov头**：仅依赖前一令牌，使用低秩分解（秩r=256）表示转移矩阵，计算高效。
  - **RNN头**：维护循环状态，捕获更长的前缀历史，但实现复杂、收益边际。
- 推理时从左到右按条件分布采样，顺序头计算量远小于并行骨干，整体草稿延迟仍由并行骨干主导。

### 2.2 置信度调度验证（Confidence-scheduled Verification）

- **置信度头（Confidence Head）**：对每个草稿位置输出标量置信度c_k∈(0,1)，建模“给定之前令牌均被接受时当前令牌通过验证”的条件概率。使用轻量线性投影加sigmoid，监督信号为理论接受率c*_k = 1 - ½∥p^d_k - p^t_k∥₁。
- **顺序温度缩放（Sequential Temperature Scaling, STS）**：后处理校准，从左到右逐位置调整温度最小化期望校准误差（ECE），使累积概率预测与实际接受率匹配。
- **硬件感知前缀调度器（Hardware-aware Prefix Scheduler）**：
  - 将验证长度选择形式化为**全局吞吐量最大化问题**：Θ = τ · SPS(B)，其中τ为期望接受令牌数，B为总批大小（含锚点），SPS(B)为发动机在批大小B下的每秒步数（预分析成本表）。
  - 利用生存概率a_{r,j}=∏_{i≤j}c_{r,i}的单调非增性质，设计**贪心算法**：全局排序所有候选前缀扩展（按a_{r,j}降序），逐步添加，并用早停机制确保因果性（不泄露未来令牌信息），严格满足无损推测解码的非预测性要求。
  - 在生产环境中，为兼容CUDA graph replay和零开销调度，采用**异步近似**：用两步前的置信度预测来估计当前步的批容量限制K，保持选择机制按置信度排序，近似最优。

## 3. 实验设计

### 3.1 数据集与评测场景

- **离线基准测试**：涵盖三个领域共9个数据集：
  - **数学推理**：GSM8K、MATH500、AIME25
  - **代码生成**：MBPP、HumanEval、LiveCodeBench
  - **日常对话**：MT-Bench、Alpaca、Arena-Hard
- 使用**标准推测解码**，采样温度1.0，报告每解码轮次接受长度（τ，含奖励令牌）。
- **在线部署**：在DeepSeek-V4-Flash和DeepSeek-V4-Pro预览版生产引擎中评测，对比MTP-1基线。

### 3.2 对比方法

- **Eagle3**：基于TTT的自回归草稿生成器（块大小=7，1层草稿模型）
- **DFlash**：并行草稿生成器（5层草稿模型，块大小=7）
- **DSpark**：本文方法（默认Markov头，5层并行骨干，块大小=7）
- 所有草稿器使用相同框架、相同数据重新训练，确保公平。

### 3.3 训练数据

- **Open-PerfectBlend**（PerfectBlend的开源版），130万样本，含聊天(17.6%)、数学(39.4%)、代码(38.9%)、指令遵循(4.1%)。
- 仅使用提示，由目标模型以推荐采样参数重新生成响应。每个草稿器训练10个epoch确保收敛。

### 3.4 目标模型

- Qwen3-{4B, 8B, 14B}（非思考模式）
- Gemma4-12B

## 4. 资源与算力

- **未明确说明具体GPU型号和数量**：论文提到使用了训练框架HAI-LLM实现隐藏状态通信和锚点边界序列打包优化，但未给出训练耗时、GPU卡数等定量信息。
- **仅能从架构推断**：目标模型为4B~14B规模，草稿模型为5层Transformer/3层MoE，训练数据130万条、10个epoch，所需算力中等。

## 5. 实验数量与充分性

- **主实验（表1）**：在4种目标模型×3个领域共9个数据集上进行，对比3种方法，共4×9×3=108个数据点，覆盖全面。
- **位置级条件接受率分析**（图2）：揭示不同架构的行为差异，验证了半自回归的优势。
- **深度消融**（图3）：固定块大小，比较1~5层草稿模型，证明2层DSpark即可超越5层DFlash。
- **块大小消融**（图4）：比较γ=4/8/12/16，且同时对比Markov头和RNN头，并测量延迟开销。
- **置信度头有效性**（图5）：离线阈值扫描，展示剪枝效果；图6展示校准前后的可靠性图。
- **在线部署**（图7、8）：在真实用户流量下，从吞吐量-交互性帕累托前沿、动态验证预算随并发变化两个角度评估。
- **附录A**：提供反例证明早停的必要性。
- **总体评估**：实验设计充分，覆盖离线算法质量、系统在线性能、消融分析，对比基线公平，结论可靠。唯一不足是缺少更大规模模型（如70B+）的实验。

## 6. 论文的主要结论与发现

1. **离线性能**：DSpark在所有目标模型和领域上均一致优于Eagle3和DFlash。在Qwen3-{4B/8B/14B}上，宏平均接受长度相对Eagle3提升30.9%/26.7%/30.0%，相对DFlash提升16.3%/18.4%/18.3%。
2. **架构优势**：并行草稿器因深度架构在大位第一令牌上具有高接受率，但后缀衰减严重；自回归草稿器后缀稳定但初始容量不足。半自回归设计结合两者优点，既保持高初始接受又缓解衰减。
3. **在线部署**：在DeepSeek-V4生产系统中，DSpark在匹配吞吐量水平下，用户生成速度提升60%-85%（Flash）和57%-78%（Pro）；在严格交互性约束下避免了MTP-1的吞吐量崩塌，扩展了可操作域，推动了帕累托前沿。
4. **负载自适应**：硬件感知调度器在低并发时分配较长验证预算，高并发时自动收缩，稳定系统吞吐量。

## 7. 优点

- **方法新颖**：首次将半自回归思想用于推测解码草稿，以极小延迟开销显著提升草稿质量。
- **系统级优化**：将验证长度选择建模为全局吞吐量最大化问题，并设计硬件感知贪心调度器，兼顾理论无损与工程实现（异步近似、CUDA graph兼容）。
- **校准机制**：提出顺序温度缩放，使置信度预测准确，支撑可靠的调度决策。
- **工业落地验证**：在DeepSeek-V4真实生产系统中取得显著加速，论文详细描述了从训练到推理的系统优化（隐藏状态通信、锚点序列打包、零开销调度、可变长查询物理执行）。
- **开源贡献**：开源DSpark检查点和DeepSpec算法驱动训练仓库，促进社区研究。

## 8. 不足与局限

- **实验覆盖**：未在更大规模模型（如70B/130B）上验证，仅限4B~14B和Gemma4-12B。在线实验也仅在DeepSeek-V4上，未给出其他模型家族结果。
- **算力信息缺失**：未提供训练所需GPU数量、时间，不利于社区复现和评估成本。
- **生产调度近似可导致次优**：异步近似使用两步前置信度决定批容量，虽保证因果性，但可能在高负载波动时引入时滞，理论上非最优决策。
- **固定草稿开销**：即便为低接受率请求，DSpark仍需执行完整并行骨干生成，无法提前退出。作者也承认这是未来优化方向。
- **领域泛化需验证**：在线实验仅报告了总体吞吐量和用户速度，未按请求领域（如代码vs聊天）分解调度行为，无法确认置信度剪枝在不同负载下的实际效果。
- **对比基线有限**：未与自适应验证长度的其他方法（如动态停止）直接比较，仅与固定MTP-1和离线草稿器对比。

（完）
