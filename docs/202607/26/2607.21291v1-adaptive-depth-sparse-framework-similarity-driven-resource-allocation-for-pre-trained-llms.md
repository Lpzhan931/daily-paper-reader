---
title: "Adaptive Depth Sparse Framework: Similarity-Driven Resource Allocation for Pre-Trained LLMs"
title_zh: 自适应深度稀疏框架：基于相似性的预训练大语言模型资源分配
authors: "Yidu Wu, Xiang Wang, Kejie Zhao, Zhangchi Wang, Qinghai Guo, Xiaoying Tang"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21291v1"
tags: ["query:llm"]
score: 8.0
evidence: 深度稀疏化用于LLM推理加速
tldr: 大型语言模型推理成本高，现有加速方法依赖任务微调或从头训练，适应性差。AdaDSF无需完整重训练，将预训练LLM转化为深度稀疏模型，基于层间余弦相似度分配token保留比例，轻量路由器选择关键token，并通过特征对齐目标保持表示质量。在GPT-NeoX和Qwen2.5上，显著降低FLOPs，性能接近密集模型，精度退化小于MoD等基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21291v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1168, \"height\": 678}, {\"url\": \"assets/figures/arxiv/2607.21291v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 950, \"height\": 565}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21291v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1148, \"height\": 429}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1236, \"height\": 509}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 663}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 721, \"height\": 223}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 468, \"height\": 140}]"
motivation: 现有加速方法需任务微调或重新训练，跨任务适应性差且成本高。
method: 利用层输入输出余弦相似度分配token保留比例，轻量路由器选择信息token，特征对齐目标匹配稀疏与密集模型表示。
result: 在GPT-NeoX和Qwen2.5上，推理FLOPs显著降低，性能接近密集模型；同等稀疏度下精度退化小于MoD、D-LLM、DLO。
conclusion: AdaDSF无需重训练即可转换预训练模型为深度稀疏模型，实现高效推理。
---

## 摘要
大语言模型（LLMs）在生成和推理任务上表现出色，但Transformer架构带来了高昂的推理成本。现有的加速方法通常依赖于任务特定的微调或从头训练，增加了适配成本并限制了跨任务可用性。我们提出自适应深度稀疏框架（AdaDSF），无需完全重训练即可将现成的预训练LLMs转化为深度稀疏模型。我们的关键洞见是：不同层级对表示变换的贡献不均，这可以通过层输入和输出隐藏状态之间的余弦相似度来刻画。基于此，AdaDSF根据相似度统计量为每层分配令牌保留比率，使用轻量级路由器选择每层的信息性令牌，并引入特征保留对齐目标以匹配稀疏模型与稠密模型之间的中间和最终表示。在GPT-NeoX和Qwen2.5上，针对语言建模和常识推理任务，AdaDSF在保持接近稠密模型性能的同时显著降低了推理FLOPs。在相当的稀疏度下，与包括MoD、D-LLM和DLO在内的强基线相比，AdaDSF始终产生更小的精度下降。

## Abstract
Large language models (LLMs) achieve strong generation and reasoning performance, but the Transformer architecture incurs high inference cost. Existing acceleration methods often rely on task-specific fine-tuning or training from scratch, increasing adaptation cost and limiting cross-task usability. We present an Adaptive Depth Sparse Framework (AdaDSF) that converts off-the-shelf pre-trained LLMs into depth-sparse models without full retraining. Our key insight is that layers contribute unequally to representation transformation, characterized by the cosine similarity between layer input and output hidden states. Based on this, AdaDSF assigns layer-wise token retention ratios from similarity statistics, uses a lightweight router to select informative tokens at each layer, and introduces a feature-preserving alignment objective to match intermediate and final representations between sparse and dense models. On GPT-NeoX and Qwen2.5 over language modeling and commonsense reasoning, AdaDSF substantially reduces inference FLOPs while preserving performance close to dense counterparts. Under comparable sparsity, AdaDSF consistently yields smaller accuracy degradation than strong baselines including MoD, D-LLM, and DLO.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）推理成本高，尽管Transformer架构性能卓越，但推理时FLOPs随着层数线性增长、随序列长度平方增长，限制了实际部署。
- **现有方法的不足**：现有加速方法（如量化、蒸馏、轻量注意力）往往依赖任务特定的微调或从头训练，导致跨任务适配成本高、可移植性差；而深度稀疏方法（如Mixture-of-Depths、D-LLM、DLO）虽可减少计算，但多采用固定或启发式的令牌保留策略，未考虑不同层对表示变换的不均衡贡献，且部分方法需要对模型进行较大架构修改。
- **研究动机**：利用预训练LLM本身固有的层间表示变换差异（通过余弦相似度刻画），设计一种无需完全重训练、只需轻量调整的深度稀疏化框架，在保持性能的同时显著降低推理FLOPs，且易于部署到资源受限场景。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：层输入与输出隐藏状态之间的余弦相似度可反映该层对表示变换的贡献程度；低相似度意味着层进行了大幅变换，应分配更多计算资源（保留更多令牌）；高相似度意味着层变换较小，可允许更多令牌跳过计算。
- **关键技术细节**：
    1. **相似性驱动的逐层令牌保留比率分配**：
        - 对每一层 \(i\)，计算输入与输出的余弦相似度 \(s_i\)。
        - 使用温度归一化权重 \(w_i\) 并引入偏差缩放（\(z_i = \beta(1/L \sum w_j - w_i)\)），再通过Sigmoid映射到 [0.05, 0.95] 区间，得到初步比率 \(r'_i\)。
        - 最后通过全局预算校正（给定目标平均保留率 \(t\)），缩放使各层比率之和满足 \( \sum r_i = tL\)，从而获得层间差异化分配。
    2. **轻量级令牌路由器**：每个稀疏层包含一个MLP路由器（参数极少）和原始Transformer层。路由器预测每令牌重要性分数，根据该层保留比率 \(K_i = \lfloor r_i \cdot s \rfloor\) 选择Top-K个令牌，被选中的令牌进入完整计算，其余通过残差跳过。训练时保持路由微分。
    3. **特征保持对齐训练**：
        - 隐藏状态对齐：对每层稀疏模型的隐藏状态（经Softmax后）与稠密模型对应层做均方误差损失 \(L^{(l)}_{hid}\)。
        - 输出分布对齐：计算稀疏模型与稠密模型输出分布的KL散度 \(L_{align}\)。
        - 总损失：\(L = L_{align} + \frac{1}{L}\sum_l L^{(l)}_{hid}\)。
    - 该方法无需修改原始Transformer结构，仅需在每层前插入路由器和额外对齐损失，适合直接转换预训练检查点。

## 3. 实验设计：数据集、场景、对比方法

- **模型与数据集**：
    - GPT-NeoX-130M：在Wikitext103上进行语言建模任务（模拟预训练场景）。
    - Qwen2.5-0.5B和Qwen2.5-1.5B：在指令微调数据（GenQA、InfinityInstruct、OpenHermes2.5）上训练，然后评估常识推理。
- **评估任务**：
    - 语言建模：Wikitext103测试集上的困惑度（PPL）。
    - 常识推理：六个基准（ARC-Challenge、ARC-Easy、HellaSwag、PIQA、WinoGrande、OpenBookQA），报告平均准确率（Avg）及相对于稠密模型的退化（Diff）。
- **对比方法**：
    - MoD（Mixture-of-Depths）
    - D-LLM
    - DLO
    - 稠密基线（100%保留率）
- **评估指标**：令牌保留比率（70%、80%、90%）、归一化FLOPs（稀疏/稠密）、准确率/困惑度。
- **实验分组**：
    - 主要性能对比（表1a、表1b、表2）：不同模型、不同稀疏度下与基线的全面对比。
    - 消融研究（表3、表4）：
        - 相似性驱动的比率分配策略对比（固定分配 vs 不同温度τ）。
        - 对齐损失设计对比（仅有因果损失 vs 加入中间层对齐损失）。

## 4. 资源与算力

- 论文**未明确说明**所使用的GPU型号、数量及训练时长。仅在实验设置中提及使用了GPT-NeoX-130M、Qwen2.5-0.5B和1.5B模型，并进行了指令微调和稀疏化训练。但未提供具体的硬件配置和时间消耗细节。

## 5. 实验数量与充分性

- **实验数量**：包含三类主要实验：① 不同模型（3个）在不同稀疏度（3种比例）下的性能对比；② 消融研究：比率分配策略（5种配置）、损失函数设计（2种）；③ 针对Qwen2.5-0.5B进行了多比率扫描（3种比率各3种方法）。总计在多个数据集上进行了数十次实验。
- **充分性与公平性**：
    - 充分：覆盖了常见稀疏度、多个模型规模、两种任务类型（语言建模和常识推理），并进行了必要的消融。
    - 客观公平：与三个强基线方法（MoD、D-LLM、DLO）在相同稀疏度下比较，并报告了归一化FLOPs及性能退化。但测试集样本已从训练数据中排除，保证了评估公正。
    - 潜在不足：仅评估了最大1.5B参数的模型，未验证7B以上大模型；常识推理任务共6个数据集，但均为单一类型（多项选择推理），任务多样性一般。

## 6. 论文的主要结论与发现

- AdaDSF在保持与稠密模型非常接近的性能（PPL或准确率退化最小）的同时，显著降低了推理FLOPs（在70%保留率时约节省32% FLOPs）。
- 在相同的稀疏度下，AdaDSF在所有对比方法中表现出最小的精度下降，优于MoD、D-LLM和DLO。
- 相似性驱动的逐层比率分配是关键：对比固定分配，PPL最多降低0.78（19.69→18.91）。
- 特征保持对齐损失（中间层对齐+输出分布对齐）对性能有显著提升：PPL从20.14降至18.91。
- 该方法无需修改内部架构，易于集成到现有Transformer pipeline中，实际部署价值高。

## 7. 优点

- **创新性**：首次将层间余弦相似度作为计算资源分配的指标，数据驱动地分配令牌保留比率，而非手工设定。
- **轻量高效**：仅需额外训练一个极小的MLP路由器和对齐损失，无需改动Transformer核心结构，训练成本低且易于泛化。
- **通用性好**：可在多个预训练模型（GPT-NeoX、Qwen2.5）上直接应用，无需重新训练完整模型。
- **实验严谨**：消融研究充分分离了各组件贡献，与多个强基线对比，报告了归一化FLOPs，确保公平比较。
- **实际价值**：面向资源受限部署场景，提供了一种简单可行的加速方案。

## 8. 不足与局限

- **模型规模有限**：实验最大仅到1.5B参数，未验证在7B、13B等更大模型上的表现，论文也承认这是未来工作。
- **任务覆盖单一**：常识推理任务均为选项类，未涉及文本生成、多轮对话、代码生成等，可能掩盖稀疏化对生成质量的影响。
- **资源信息缺失**：未提供训练所需的GPU型号、数量及时间，难以评估训练成本的实用性。
- **潜在偏差风险**：相似度分配依赖于校准子集的选择，若校准集分布与下游任务差异过大，可能影响比率分配的有效性。
- **路由器开销未明确**：虽称路由器“轻量”，但未量化其引入的额外计算或延迟，尤其在高批量推理时可能影响收益。
- **对齐训练的适用性**：对齐损失需要访问稠密模型中间表示，对于极大型模型（如70B+）可能带来显著内存和通信开销。

（完）
