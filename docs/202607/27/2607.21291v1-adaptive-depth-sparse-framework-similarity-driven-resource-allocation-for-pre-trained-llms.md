---
title: "Adaptive Depth Sparse Framework: Similarity-Driven Resource Allocation for Pre-Trained LLMs"
title_zh: 自适应深度稀疏框架：面向预训练大语言模型的相似性驱动资源分配
authors: "Yidu Wu, Xiang Wang, Kejie Zhao, Zhangchi Wang, Qinghai Guo, Xiaoying Tang"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21291v1"
tags: ["query:llm"]
score: 8.0
evidence: 自适应深度稀疏框架，无需全重训练即可加速LLM推理
tldr: 大语言模型Transformer推理成本高，现有加速方法需微调或重训练。本文提出AdaDSF，基于层输入输出隐藏状态余弦相似度统计，为每层自适应分配令牌保留比率，并通过轻量路由器选择信息令牌，同时引入特征保持对齐目标匹配稀疏与密集模型表示。在GPT-NeoX和Qwen2.5上，该方法大幅降低推理FLOPs且性能接近密集模型，优于MoD等基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21291v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1168, \"height\": 678}, {\"url\": \"assets/figures/arxiv/2607.21291v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 950, \"height\": 565}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21291v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1148, \"height\": 429}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1236, \"height\": 509}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 663}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 721, \"height\": 223}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 468, \"height\": 140}]"
motivation: 现有LLM加速方法依赖任务微调或重训练，跨任务通用性差且适应成本高。
method: 基于层间余弦相似度分配令牌保留比率，用轻量路由器选择每层信息令牌，并通过特征对齐目标训练。
result: 在GPT-NeoX和Qwen2.5上，推理FLOPs显著降低，性能接近密集模型，优于MoD、D-LLM和DLO。
conclusion: AdaDSF实现无需重训练即可将预训练LLM转化为深度稀疏模型，兼顾效率与性能。
---

## 摘要
大语言模型（LLMs）在生成和推理任务中表现出色，但Transformer架构带来了高昂的推理成本。现有加速方法通常依赖特定任务的微调或从头训练，增加了适配成本并限制了跨任务可用性。我们提出自适应深度稀疏框架（AdaDSF），该框架无需完整重新训练即可将现成的预训练LLMs转换为深度稀疏模型。我们的关键见解是：各层对表示变换的贡献不相等，这一特征可通过层输入和输出隐藏状态之间的余弦相似度来刻画。基于此，AdaDSF根据相似性统计分配逐层的令牌保留比率，使用轻量级路由器选择每层的信息性令牌，并引入特征保持对齐目标，以匹配稀疏模型与密集模型的中间和最终表示。在语言建模和常识推理任务上对GPT-NeoX和Qwen2.5进行的实验表明，AdaDSF在显著减少推理FLOPs的同时，保持了与密集模型相近的性能。在可比稀疏度下，与包括MoD、D-LLM和DLO在内的强基线相比，AdaDSF始终产生更小的精度下降。

## Abstract
Large language models (LLMs) achieve strong generation and reasoning performance, but the Transformer architecture incurs high inference cost. Existing acceleration methods often rely on task-specific fine-tuning or training from scratch, increasing adaptation cost and limiting cross-task usability. We present an Adaptive Depth Sparse Framework (AdaDSF) that converts off-the-shelf pre-trained LLMs into depth-sparse models without full retraining. Our key insight is that layers contribute unequally to representation transformation, characterized by the cosine similarity between layer input and output hidden states. Based on this, AdaDSF assigns layer-wise token retention ratios from similarity statistics, uses a lightweight router to select informative tokens at each layer, and introduces a feature-preserving alignment objective to match intermediate and final representations between sparse and dense models. On GPT-NeoX and Qwen2.5 over language modeling and commonsense reasoning, AdaDSF substantially reduces inference FLOPs while preserving performance close to dense counterparts. Under comparable sparsity, AdaDSF consistently yields smaller accuracy degradation than strong baselines including MoD, D-LLM, and DLO.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：大语言模型（LLMs）基于Transformer架构，推理时计算量随深度线性增长、随序列长度二次增长，导致高推理成本。现有加速方法（如量化、蒸馏、轻量注意力、深度稀疏方法等）大多需要任务特定微调或从零训练，跨任务通用性差，且实现复杂。
- **研究动机**：提出一种无需全重训练、仅需最小架构修改即可将现成预训练LLMs转化为深度稀疏模型的方法，在保持性能的同时大幅降低推理FLOPs，以适应资源受限部署。
- **整体含义**：利用层间表示变换的不均匀性（通过余弦相似度刻画），实现数据驱动的自适应计算分配，从而在稀疏推理中更好地保留密集模型行为。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：不同Transformer层对表示变换的贡献不同，通过层输入输出隐藏状态的余弦相似度量化这一差异；相似度低的层表示变换大，应分配更多计算资源（更高的令牌保留比率），反之则分配更少。
- **关键技术细节**：
  1. **相似度驱动的层间令牌保留比率分配**：
     - 对第i层计算余弦相似度 s_i = CosSim(x_in, x_out)。
     - 通过温度归一化加权、偏差缩放、Sigmoid映射到[0.05, 0.95]区间，最后进行全局预算校正（确保平均保留比率等于目标t）。
     - 保留比率高 → 表示变换大的层（相似度低）。
  2. **稀疏模块与轻量路由器**：
     - 每个稀疏层包含一个MLP路由器和原Transformer层。
     - 路由器根据令牌重要性分数选择Top-K_i个令牌（K_i = r_i * 序列长度），其余令牌跳过全计算，走残差路径。
     - 路由器在训练中保持可微。
  3. **特征保持对齐训练**：
     - 隐藏状态对齐：对每一层稀疏与密集输出的softmax表示计算L2损失。
     - 输出分布对齐：KL散度损失（L_align）。
     - 总损失 = L_align + (1/L) * Σ L_hid^{(l)}。
     - 目标：保持稀疏模型中间和最终表示与密集模型一致，减少跳级引起的表示偏移。

## 3. 实验设计：数据集、场景、基准与方法对比
- **模型**：GPT-NeoX-130M、Qwen2.5-0.5B、Qwen2.5-1.5B。
- **数据集**：
  - 预训练场景：Wikitext103（用于GPT-NeoX）。
  - 指令微调场景：GenQA、InfinityInstruct、OpenHermes2.5（用于Qwen2.5模型）。
- **评估任务**：
  - 语言建模：Wikitext103测试集困惑度（PPL）。
  - 常识推理：ARC-Challenge、ARC-Easy、HellaSwag、PIQA、WinoGrande、OpenBookQA（共6个，报告平均准确率和相对于密集模型的下降）。
- **对比方法**：MoD（Mixture-of-Depths）、D-LLM、DLO。注意D-LLM在Qwen2.5上不适用（未报告）。
- **主要指标**：令牌保留比率、归一化FLOPs（稀疏/密集）、准确率、PPL。

## 4. 资源与算力
- **论文未明确说明**使用的GPU型号、数量、训练时长。仅提到实验在GPT-NeoX-130M、Qwen2.5-0.5B和1.5B上进行，但未报告具体硬件配置和训练时间。这属于信息缺失。

## 5. 实验数量与充分性
- **主要实验组**：
  - 表1(a)：Qwen2.5-0.5B和1.5B在6个推理任务上的性能对比（MoD、DLO、AdaDSF）。
  - 表1(b)：Qwen2.5-0.5B在三种令牌保留比率（90%、80%、70%）下的性能对比。
  - 表2：GPT-NeoX-130M在Wikitext103上三种比率下的PPL和FLOPs对比（含MoD、D-LLM、DLO）。
  - 表3：对相似度驱动分配策略的消融（不同温度τ）。
  - 表4：对特征保持对齐损失函数的消融（只有因果损失 vs. 完整损失）。
- **充分性与公平性**：
  - 实验覆盖两个模型系列（GPT-NeoX、Qwen2.5）和两种任务（语言建模、常识推理），具有较强的代表性。
  - 与当前主流深度稀疏方法在同一设置下比较，指标全面（PPL、准确率、FLOPs）。
  - 消融实验针对关键组件（分配策略、损失函数），验证了各自的有效性。
  - 但论文未进行更大规模模型（如7B+）的实验，也未涉及生成类任务（如对话、摘要）的评估，实验充分性存在一定局限。

## 6. 主要结论与发现
- AdaDSF在保持推理FLOPs显著降低的同时，性能下降幅度最小，优于MoD、D-LLM、DLO。
- 相似度驱动的分配策略比固定或启发式策略更优：合理的温度τ（小τ带来更高方差）能提升性能。
- 特征保持对齐损失（同时对齐中间和输出表示）比仅用因果损失更有效。
- 方法无需内部架构修改，可推广到不同预训练模型，且训练成本低。

## 7. 优点
- **创新性**：首次将层间余弦相似度作为计算分配的直接依据，数据驱动且无需额外标注。
- **轻量性**：仅增加一个MLP路由器（几M参数），不改变Transformer骨干，方便集成。
- **对齐训练**：同时对齐中间和最终表示，有效缓解稀疏化导致的表示偏移。
- **实用性**：无需重新训练整个模型，只需少量对齐训练（可使用小规模校准数据），适用于现有预训练检查点。
- **实验设计全面**：多模型、多任务、多比率、多基线，消融实验清晰。

## 8. 不足与局限
- **实验规模有限**：最大模型仅1.5B参数，未验证7B及以上大模型的可扩展性。
- **任务覆盖不全**：未测试生成类任务（如摘要、翻译、对话），仅聚焦语言建模和分类式推理。
- **资源信息缺失**：未提供训练所需的GPU型号、数量、时长，不利于复现评估。
- **潜在偏差风险**：相似度分配策略依赖于校准数据，可能对校准数据分布敏感，但论文未讨论其鲁棒性。
- **D-LLM在Qwen2.5上不可用**，导致部分对比不完整。

（完）
