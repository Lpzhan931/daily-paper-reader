---
title: "AngelSpec: Towards Real-World High Performance Inference with Speculative Decoding"
title_zh: AngelSpec：面向现实世界高性能推理的推测解码方法
authors: "Hong Liu, Rui Cen, Junhan Shi, Guangshuo Qin, Jiebin Zhang, Tianyu Liu, Runzhi Fan, Guoliang Zhao, Ruobing Xie, Kai Zhang, Song Liu, Guanghua Yu, Jianchen Zhu"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25852v2"
tags: ["query:llm"]
score: 9.0
evidence: 提出AngelSpec，一个统一训练框架，支持多令牌预测和块并行投机解码
tldr: "推测解码中，自回归多令牌预测与块并行扩散草案在不同负载下性能差异显著。AngelSpec通过训练级结构与数据共特化、架构级DFly混合框架、推理级自适应验证深度，统一优化异构草案。在Hy3-A21B上平均接受长度提升约30%，吞吐量达自回归解码的1.98-2.40倍，并开源支持扩展。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25852v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1522, \"height\": 687, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25852v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1402, \"height\": 743, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25852v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1572, \"height\": 604, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25852v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1551, \"height\": 677, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25852v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1573, \"height\": 1184, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25852v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1560, \"height\": 600, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25852v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1579, \"height\": 342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25852v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1580, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25852v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1586, \"height\": 425, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25852v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1584, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25852v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1584, \"height\": 290, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25852v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1584, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25852v2/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1583, \"height\": 799, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25852v2/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1586, \"height\": 542, \"label\": \"Table\"}]"
motivation: 真实工作负载输出分布多样，单一草案结构无法始终最优，需统一框架适配异构场景。
method: 训练级分结构与数据特化，架构级DFly组合混合目标条件与自回归头，推理级批级验证资源重分配。
result: "在Hy3-A21B上平均接受长度提升30%，吞吐量比自回归解码快1.98-2.40倍，优于DFlash 10.5-11.8%。"
conclusion: AngelSpec统一训练框架有效融合MTP与块扩散，实现高性能推测解码，开源促进扩展。
---

## 摘要
推测解码在不改变目标分布的情况下加速大型语言模型推理，但没有任何单一的草稿结构在所有真实工作负载中表现最佳。自回归多令牌预测（MTP）是一种轻量级、稳定的提议机制，而块并行扩散则将草稿延迟摊销到更长的候选序列上；两者的更优选择强烈依赖于输出分布。我们提出AngelSpec，一个统一的训练框架，用于MTP和块并行推测解码，从三个层面应对这种异质性。在训练层面，我们并非让一个通用草稿模型适配统一的数据混合，而是协同特化结构与数据：MTP草稿模型在多样化对话数据上训练，用于高熵开放聊天；块扩散草稿模型在代码和数学数据上训练，用于更长的可预测续写。在架构层面，我们提出DFly，一个结合混合目标条件主干与前驱条件自回归头的块扩散框架，在保持并行生成的同时，改进了目标特征利用和块内依赖建模。在推理层面，接受长度和验证成本均随领域、请求、在线负载和硬件而变化，因此DFly将验证视为共享的批次级资源：它在请求间将计算重新分配给高置信度的前缀，并结合预期效用与基于分析的成本模型，在线调整验证深度。在Hy3系列中，DFly在Hy3-A21B上将平均接受长度提高了约30%，并在从4到64的每个测试并发数下均达到最高平均吞吐量，相对于自回归解码实现1.98-2.40倍加速，吞吐量比DFlash高10.5-11.8%。我们发布AngelSpec以支持这些方法的训练与扩展。

## Abstract
Speculative decoding accelerates large language model inference without changing the target distribution, but no single drafting structure performs best across real-world workloads. Autoregressive multi-token prediction (MTP) is a lightweight, stable proposal mechanism, whereas block-parallel diffusion amortizes drafting latency over much longer candidate sequences; the better choice depends strongly on the output distribution. We present AngelSpec, a unified training framework for MTP and block-parallel speculative decoding that addresses this heterogeneity at three levels. At the training level, rather than fitting one universal drafter to a uniform data mixture, we co-specialize structure and data: the MTP drafter is trained on diverse conversational data for high-entropy open-ended chat, and the block-diffusion drafter on code and mathematics data for longer predictable continuations. At the architecture level, we propose DFly, a block-diffusion framework combining a hybrid target-conditioning backbone with a predecessor-conditioned autoregressive head, improving target-feature utilization and intra-block dependency modeling while keeping generation parallel. At the inference level, both acceptance length and verification cost vary with domain, request, online load, and hardware, so DFly treats verification as a shared batch-level resource: it reallocates compute toward high-confidence prefixes across requests and combines expected utility with a profiled cost model to adapt verification depth online. Across the Hy3 series, DFly raises the average accepted length on Hy3-A21B by roughly 30% and attains the highest average throughput at every tested concurrency from 4 to 64, a 1.98-2.40x speedup over autoregressive decoding and 10.5-11.8% higher throughput than DFlash. We release AngelSpec to support training and extending these methods.

---

## 论文详细总结（自动生成）

好的，根据您提供的论文内容，以下是对论文《AngelSpec: Towards Real-World High Performance Inference with Speculative Decoding》的详细结构化总结。

---

### 1. 论文的核心问题与整体含义

- **研究动机与背景**：大型语言模型（LLM）的自回归解码过程缓慢且昂贵。推测解码（Speculative Decoding）是一种有前景的加速技术，它通过一个轻量级的草稿模型（Drafter）快速生成候选令牌，再由目标LLM并行验证，从而在不改变输出分布的情况下加速推理。
- **核心问题**：
    1.  **工作负载异构性**：现有研究通常寻求一个通用的草稿模型，但真实世界的在线请求（如对话、代码生成、数学推理）具有截然不同的输出分布和可预测性。没有单一草稿结构能在所有场景下最优。例如，高熵的对话场景适合短期的自回归多令牌预测（MTP），而结构化的代码/数学场景则更适合能生成长序列的块并行扩散（Block-diffusion）。
    2.  **训练与推理的落差**：训练时使用的“教师强制”方法与推理时的自回归生成轨迹不一致，导致草稿质量下降。
    3.  **推理资源的浪费**：固定的验证深度（即每次验证的令牌数）无法适应动态变化的接受率和部署条件，导致在高负载时浪费批次容量，或在负载低时无法充分利用可预测的草稿。

### 2. 论文提出的方法论：核心思想与关键技术

论文提出**AngelSpec**，一个从**训练**、**架构**和**推理**三个层面系统性优化推测解码的统一框架。

- **训练层面：工作负载特化**
    - **核心思想**：不再使用单一草稿模型或统一数据，而是针对不同工作负载，对模型结构和训练数据进行联合特化。
    - **MTP草稿模型**：针对高熵、开放的对话场景，使用多样化、长上下文的对话数据训练，侧重于短而稳定的提议。
    - **DFly草稿模型**：针对结构性强、可预测性高的代码和数学场景，使用代码和数学强化数据训练，侧重于长序列的并行生成。

- **架构层面：DFly（块扩散框架）**
    - 提出DFly，一个结合了**混合目标条件主干**和**前驱条件自回归头**的块并行草稿框架。
    - **混合目标条件主干**：结合了DFlash的全局非线性变换和DFlare的深度特异性加权和，为不同深度的草稿层提供了更丰富且具有区分度的目标模型特征。
    - **前驱条件自回归头**：在并行的草稿主干之后，增加一个轻量级的序列头（如Hidden-correction头），将前一个预测令牌的信息注入当前位置的预测，从而将边缘分布转换为条件分布，改善了块内的依赖建模，且保持了主干的并行效率。

- **推理层面：D-cut（自适应验证预算）**
    - **核心思想**：将目标模型的验证视为一个共享的批次级资源。
    - **技术细节**：
        1.  **估计每请求增益**：利用草稿模型的令牌级置信度，评估不同深度的预期接受收益。
        2.  **选择运行时感知预算**：服务器启动时，对不同的批次大小和预算比例（如25%, 50%, 75%, 100%）进行性能分析，得到一个运行时成本模型。
        3.  **动态验证**：在每个解码步，根据当前请求的置信度排序，选择总收益与运行时成本之比最优的预算，并将高置信度的前缀打包成一个密集的验证批，动态地分配计算资源。

- **其他关键技术细节**：
    - **训练时测试（TTT）**：对于MTP草稿模型，在训练时使用自回归展开，而不是教师强制，让模型学会从自身错误中恢复，以解决训练-推理落差。
    - **接受对齐的损失函数**：使用直接与推测采样接受率相关的**总变差距离（TV）** 和**负对数重叠（negative log-overlap）** 损失，替代传统的交叉熵或KL散度，更高效地优化草稿质量。
    - **损失函数两阶段训练**：首先使用D-PACE加权的混合LK损失进行冷启动，然后再切换到端到端的TV损失进行精细调优。

### 3. 实验设计

- **使用的数据集与场景**：
    - **数学**：GSM8K, MATH500
    - **代码**：HumanEval, MBPP, LiveCodeBench
    - **聊天**：AlpacaEval, MT-Bench
    - **训练数据**：Open-PerfectBlend (通用数据)，并从OpenCodeInstruct, OpenCodeReasoning, Big-Math中筛选出的代码和数学领域特定数据。
    - **部署测试**：复现腾讯混元（Hunyuan）的生产流量。

- **Benchmark**：主要采用**平均接受长度（Mean Accepted Length, MAL）** 和**吞吐量（Tokens/s, Throughput）** 作为评价指标，对比方法包括自回归解码（AR）和多种推测解码方法。
- **对比的方法**：
    - **MTP基线**：基础MTP模型。
    - **DFlash**：代表性的全并行块草稿模型。
    - **DSpark**：半自回归的并行草稿模型。
- **其他对比**：包括不同的训练损失函数（CE, KL, LK Loss, TV Loss）、不同的架构组件（主干、自回归头）、不同的模型深度和推理块大小、不同的思考模式（高思考/无思考）、以及在有D-cut和没有D-cut情况下的端到端部署性能。

### 4. 资源与算力

- 文中没有透露AngelSpec框架的总训练GPU数量和时长。
- 在**实验和性能分析**部分，论文明确提到实验是在一个配备**8张NVIDIA H20 GPU（每张96GB内存）** 的单节点（single node）上进行的。
- 采用了**张量并行（Tensor Parallelism, TP=8）**，将模型执行分布在全部8张GPU上。

### 5. 实验数量与充分性

- **实验数量丰富，覆盖多个维度**：
    1.  **损失函数消融实验**：对比了5种不同损失函数（CE, KL, LK, TV, e2e TV with LK cold start）在7个基准上的效果（表1）。
    2.  **MTP训练效果对比**：在温度和有无TTT+Rollout配置下，对比了7个基准上的接受率和平均接受长度（表2）。
    3.  **草稿模型质量对比**：在两个目标模型（Qwen3-8B, Hy3-A21B）上，对比了MTP, DFlash, DSpark和DFly的6个基准上的平均接受长度（表3）。
    4.  **架构组件消融实验**：在Hy3模型上，逐步添加和替换主干、自回归头、数据等，对比了6个基准上的平均接受长度（表4）。
    5.  **模型深度与块大小消融实验**：探究了不同层数和块大小对性能的影响（表5）。
    6.  **思考模式消融实验**：对比了不同思考模式下的训练和测试效果（表6）。
    7.  **端到端部署性实验**：在6个基准和5个不同并发级别下，对比了AR, MTP, DFlash, DFly的吞吐量和加速比（表7）。
    8.  **D-cut部署实验**：在真实生产流量下，对比了有无D-cut的吞吐量、每用户解码速度和接受长度（图4）。
    9.  **组件性能分析**：对草稿生成和模型执行阶段的延迟进行了详细分析（图5、表8）。

- **实验的充分性与公平性分析**：
    - **充分性**：实验覆盖了从模型架构、训练策略、推理优化到端到端部署的全流程，消融实验设计严谨，能够明确每个组件的贡献。在不同并发度、不同温度、不同思考模式下的性能测试，验证了方法的鲁棒性。
    - **客观性**：论文对比了多个公认的强基线（DFlash, DSpark），并分析了自身方法的优劣（如MTP更优聊天的特点）。消融实验设计合理，能够清晰地回答“是什么带来了改进”的问题。
    - **公平性**：为了确保对比公平，论文在训练DFlash和其他方法时，使用了相同的学习率调度和训练轮次，并在相同的数据上进行。对于Hy3-A21B，他们直接使用最佳的MTP模型，而非重新训练，这可能带来一定的不公，但论文也指出了这一点。论文也指出，D-cut的实验设置（部分CUDA图）可能导致其性能被低估，这体现了实验的诚实性。

### 6. 论文的主要结论与发现

1.  **工作负载异构性是核心挑战**：对话场景和代码/数学场景需要不同结构（MTP vs. 块扩散）和训练数据（多样化 vs. 专业化）的草稿模型。
2.  **DFly显著优于现有方案**：提出的DFly草稿模型在Hy3-A21B上，将平均接受长度提高了约30%，在所有测试的并发度下，吞吐量均高于自回归解码和DFlash。
3.  **D-cut有效缓解验证瓶颈**：在高并发场景下，D-cut通过动态裁剪低价值草稿令牌，将有限的验证资源集中于高置信度前缀，从而比DFly基线实现高达15.7%的吞吐量提升，同时保持理想的每用户延迟。
4.  **训练与推理对齐至关重要**：采用“训练时测试（TTT）”和接受对齐的损失函数（如端到端TV损失）显著改善了第二代、第三代草稿令牌的接受率，弥补了训练-推理落差。
5.  **开源框架价值**：AngelSpec提供了一个统一、可扩展的开源框架，支持MTP和块并行推测解码的训练、评估与扩展，为社区研究提供了便利。

### 7. 优点

1.  **问题导向明确**：论文精准识别了真实世界部署中的核心问题——工作负载异构性，并以此为核心出发点进行设计，具有很强的实用价值。
2.  **系统性解决方案**：不是提出单一的技巧，而是从训练（数据+策略）、模型架构、推理优化三个层面构建了一个端到端的解决方案，各部分紧密耦合，协同工作。
3.  **创新的架构设计**：提出的DFly巧妙地融合了不同方法的优点（如DFlash的共享投影和DFlare的深度特定视图），并通过轻量级自回归头弥补了并行生成的缺陷，设计优雅且高效。
4.  **推理层面的适应性设计**：D-cut将验证视作共享资源，并根据运行时成本进行自适应调整，这在处理动态负载和异构请求方面非常关键，也是论文区别于其他自适应方法（如DSpark）的亮点。
5.  **充分的实验与坦诚的分析**：论文进行了大量消融实验，覆盖了多个维度。在性能分析部分，坦率地指出了D-cut可能被低估（部分CUDA图）和增加开销的情况，並未回避方法的局限性，体现了严谨的学术态度。

### 8. 不足与局限

1.  **实验数据未完全披露**：论文未提供AngelSpec框架训练所用的总GPU数量和时长，这在一定程度上影响了方法在算力成本上的可评估性。
2.  **领域偏差风险**：论文明确提出DFly模型针对代码和数学进行特化。虽然这对于特定场景效果更好，但会导致其在通用聊天任务上可能不如MTP。论文的实验数据也证明了这一点（如MT-Bench上DSpark略优于DFly）。这种方法需要部署方根据自身流量特征进行选择和组合，增加了部署的复杂度。
3.  **对比基线的局限性**：与DFlash等基线相比，AngelSpec (DFly) 的改进部分可能源于其使用的更高质的数据和更深入的调参（如DPACE优化、两阶段训练）。虽然论文进行了消融实验，但要完全排除这些因素对最终结果的贡献仍有难度。
4.  **部署复杂性**：AngelSpec要求根据工作负载部署不同的草稿模型（MTP和DFly），并且D-cut机制需要在服务器启动时进行性能分析（profile），这比简单的固定参数推测解码方案更复杂，部署成本更高。
5.  **性能分析的潜在偏差**：组件性能分析中的“解码步延迟代理”是基于中位数计算的，可能无法完全反映极端情况（如长尾延迟）对用户体验的影响。
6.  **开源框架的可复现性**：虽然论文声称开源了AngelSpec框架，但未在正文中提供指向具体实现细节或文档的链接，也未讨论其与主流推理框架（如vLLM）的集成复杂度，这对于社区复现其核心方法构成了障碍。

（完）
