---
title: Unified Static-Dynamic Pruning for Efficient LLM Inference
title_zh: 统一静态-动态剪枝实现高效LLM推理
authors: "Jinhyeok Kim, Yejoon Lee, Jaeyoung Do"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.21985v1"
tags: ["query:llm"]
score: 8.0
evidence: 提出统一静态-动态剪枝框架SPDP，用于高效大语言模型推理
tldr: "LLM自回归推理存在计算和内存瓶颈，现有静态剪枝缺乏自适应性、动态剪枝引入不规则性。本文提出统一静态-动态剪枝框架SPDP，设计Tiled-CBC格式及互补GPU核，协同利用静态和动态稀疏性。在推理GPU上平均加速1.24x-1.37x（最高2.51x），稀疏度提升25%且困惑度匹配。该工作推进了推理效率-质量帕累托前沿。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21985v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 842, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 813, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 845, \"height\": 294, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1755, \"height\": 507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1774, \"height\": 321, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1797, \"height\": 321, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 759, \"height\": 436, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 882, \"height\": 970, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 850, \"height\": 219, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 883, \"height\": 279, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1671, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 812, \"height\": 272, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21985v1/fig-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 878, \"height\": 284, \"label\": \"Figure\"}]"
motivation: 现有静态剪枝缺乏输入自适应性，动态剪枝虽自适应但带来运行时不规则性，两者均无法兼顾效率与灵活性。
method: 提出SPDP框架，包含Tiled-CBC格式、CUDA核（HAD-SMBD实现激活跳过）和Tensor-Core核，统一静态与动态稀疏性。
result: "在推理GPU上比SpInfer加速1.24x-1.37x（最高2.51x），稀疏度提高25%时困惑度不变。"
conclusion: 统一静-动剪枝可显著提升LLM推理吞吐与能效，拓展了效率-质量帕累托边界。
---

## 摘要
大语言模型(LLM)的日益部署加剧了自回归解码的计算和内存瓶颈，其中低计算强度和带宽受限的内核主导了推理成本。权重剪枝提供了一种有前景的补救措施，但现有方法仍局限于静态剪枝（SP，永久移除冗余权重但缺乏自适应性）或动态剪枝（DP，自适应输入稀疏性但引入运行时不规则性）。本文提出SPDP，一个统一的稀疏推理框架，将非结构化SP与输入自适应DP相结合，用于GPU上的高效LLM推理。SPDP协同设计了新的平铺列式位图压缩（Tiled-CBC）格式和两个互补的GPU内核：（1）基于CUDA核心的spMspV内核，采用混合激活感知动态共享内存位图解码（HAD-SMBD）实现细粒度、运行时激活跳过；（2）针对预填充计算优化的Tensor-Core SpMM内核。这种联合格式-内核设计协调了静态和动态稀疏性，在LLM推理的两个阶段保持带宽高效的内存访问和高计算强度。在推理优化GPU上的全面评估表明，SPDP相比最先进的稀疏框架（如SpInfer）实现了1.24倍至1.37倍的平均加速（最高2.51倍），同时在高25%的稀疏度下匹配困惑度。SPDP推进了推理效率-质量帕累托前沿，表明统一的静态-动态剪枝可以在大规模LLM服务中带来显著的吞吐量和每瓦性能提升。

## Abstract
The increasing deployment of large language models (LLMs) has magnified the computational and memory bottlenecks of autoregressive decoding, where low compute intensity and bandwidth-bound kernels dominate inference cost. Weight pruning offers a promising remedy,   but existing methods remain confined to either static pruning (SP), which permanently removes redundant weights but lacks adaptivity, or dynamic pruning (DP), which adapts to input sparsity but introduces runtime irregularity. This paper presents SPDP, a unified   sparse-inference framework that integrates unstructured SP with input-adaptive DP for efficient LLM inference on GPUs. SPDP co-designs a new Tiled-Column-wise Bitmap Compressed (Tiled-CBC) format and two complementary GPU kernels: (1) a CUDA-core spMspV kernel   featuring Hybrid Activation-aware Dynamic Shared-Memory Bitmap Decoding (HAD-SMBD) for fine-grained, runtime activation skipping, and (2) a Tensor-Core SpMM kernel optimized for prefill computation. This joint format-kernel design harmonizes static and dynamic   sparsity, maintaining bandwidth-efficient memory access and high compute intensity under both phases of LLM inference. Comprehensive evaluations on inference-optimized GPUs demonstrate that SPDP achieves 1.24x-1.37x average speedup (up to 2.51x) over state-of-the-   art sparse frameworks such as SpInfer, while matching. perplexity with up to 25% higher sparsity. SPDP advances the inference efficiency-quality Pareto frontier, showing that unified static-dynamic pruning can deliver substantial throughput and performance-per-watt   improvements in large-scale LLM serving

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）自回归解码阶段存在严重的计算和内存瓶颈，尤其是小批量场景下，内存带宽成为主要限制。权重剪枝是一种有前景的加速方法，但现有方法分为两类：**静态剪枝（SP）** 永久移除冗余权重，缺乏输入自适应性，可能导致质量下降；**动态剪枝（DP）** 根据输入自适应跳过不重要的激活，但引入运行时不规则性，且难以利用硬件高效执行。两者各自独立使用时均无法实现最优的推理效率-质量权衡。
- **整体含义**：本文认为静态剪枝和动态剪枝本质上是互补的（SP沿行剪枝权重，DP沿列跳过激活），联合利用可以产生乘法有效稀疏度，从而大幅提升内存受限解码阶段的性能。论文提出**SPDP**框架，首次在GPU上统一非结构化静态剪枝与输入自适应动态剪枝，通过联合格式-内核协同设计，突破现有系统的性能瓶颈，推进LLM推理的效率-质量帕累托前沿。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过联合设计稀疏数据格式和GPU内核，使静态压缩（减少内存占用）和动态激活跳过（减少计算量）能够协同工作，同时解决两者结合时产生的布局不匹配、Tensor Core利用率低、元数据开销等系统级挑战。
- **关键技术细节**：
  - **Tiled-CBC（平铺列式位图压缩）格式**：将权重矩阵分层划分为平铺（GTile），每个GTile按列存储非零值（列微块大小256×1）。使用五个数组：TileOffset、Values、Bitmap、ColInfo（每列偏移和非零个数）、对齐元数据。该格式支持按列快速定位稀疏片段，便于动态剪枝时只加载激活相关的列，同时保持低索引开销。
  - **CUDA-core spMspV内核（解码阶段）**：采用混合激活感知动态共享内存位图解码（HAD-SMBD），在解码过程中结合动态激活掩码，跳过非活跃列的权重加载与解码。设计了三级异步流水线：ColInfo预加载、XTile加载、GTile加载与解码，通过双缓冲和预取重叠内存传输与计算，实现近乎零额外延迟的动态剪枝执行。
  - **Tensor-Core SpMM内核（预填充阶段）**：复用Tiled-CBC格式，增加布局对齐阶段，将列主序的解码后权重重新排列为Tensor Core兼容的行主序布局，避免bank冲突，保持高计算吞吐。
  - **公式**：压缩率 CR = 存储量_稠密 / 存储量_压缩；计算强度 CI = FLOPs / 传输字节数 = (M×N) / (M/CR×(1-s) + N)，其中s为动态跳过率。联合静态和动态稀疏性可显著提升CI。

## 3. 实验设计：数据集、场景、对比方法

- **模型与剪枝方法**：主要使用Llama-2-7B-hf；部分实验（HumanEval）使用Qwen3-32B。静态剪枝采用Wanda，动态剪枝采用TEAL。校准集：C4（128序列，SP）和Alpaca（300样本，DP）。
- **评估任务**：
  - 模型质量：WikiText（困惑度）、HumanEval（pass@1）、GSM8K（精确匹配）、CoQA（F1）、MMLU（准确率）。
  - 系统性能：kernel级吞吐（TFLOP/s）、端到端每输出令牌时间（TPOT）、能量效率。
- **对比方法**：
  - 稠密基线：cuBLAS（Tensor Core）。
  - 稀疏基线：cuSPARSE、Sputnik、SparTA、Flash-LLM、SpInfer（最先进）。
  - 消融：仅静态（SpInfer/Wanda）、仅动态（TEAL）。
- **GPU平台**：NVIDIA A10G、L4、L40S（推理优化型GPU）。

## 4. 资源与算力

- 论文未明确说明训练或校准所需的GPU数量、运行时长或总计算量。所有实验均为推理阶段评估，在单GPU（A10G / L4 / L40S）上完成。校准过程（Wanda、TEAL）使用少量样本（128-300序列），计算开销较小，未报告具体时间。
- 算力信息核心在于推理加速效果，而非训练成本。

## 5. 实验数量与充分性

- **实验数量**：非常丰富。
  - kernel级性能：在多种LLM投影矩阵形状（来自OPT、LLaMA、Qwen、Mixtral等）上对比6个基线，报告平均和最高加速。
  - 稀疏度扫描：从0%到90%静态稀疏度联合30%动态稀疏度，展示性能曲线。
  - 端到端评估：A10G、L4、L40S三块GPU上，对比TPOT和困惑度，分析不同总稀疏度（30%-80%）。
  - 硬件性能剖析：SM busy、带宽利用率、bank冲突等指标。
  - 量化兼容性：4-bit RTN量化后困惑度。
  - 跨领域稀疏矩阵：SuiteSparse集合（验证专用性）。
- **充分性与公平性**：
  - 覆盖了主流LLM架构和多种下游任务，对比了当前最先进的稀疏推理系统SpInfer，且包括稠密基线。
  - 消融实验清楚分离了SP和DP各自的贡献。
  - 所有实验均多次运行取平均，使用Nsight工具测量，排除CPU开销。
  - 弱点：动态剪枝方法仅使用TEAL，未验证与其他DP方法（如DejaVu、CATS）的结合；预填充阶段未在更大数据集上充分展示优势。

## 6. 论文的主要结论与发现

- **性能提升**：SPDP在A10G和L4上，kernel级平均加速分别达1.88×和2.11×（vs cuBLAS），1.24×和1.37×（vs SpInfer）；最高加速达2.51×和3.52×。端到端TPOT同样显著优于SpInfer。
- **质量保持**：在相同困惑度下，SPDP可实现比SpInfer高达25%的额外稀疏度（通过动态剪枝）。在下游任务中，SPDP在中等稀疏度下匹配甚至优于仅静态或仅动态方法。
- **硬件效率**：联合稀疏提高了SM占用率和内存-计算重叠，减少bank冲突。
- **量化兼容性**：与4-bit量化结合后困惑度趋势稳定，可进一步压缩模型。
- **局限性**：SPDP专为LLM解码的适度稀疏度（30-50%）设计，不适用于极端稀疏（>95%）的通用科学计算矩阵。

## 7. 优点

- **方法创新**：首次提出统一的静态-动态剪枝GPU推理框架，通过Tiled-CBC格式和HAD-SMBD解码，弥合了两种剪枝范式的系统级鸿沟。
- **系统实现**：异步流水线设计、双缓冲、元数据预取等技术使动态剪枝几乎零开销，同时保持Tensor Core支持预填充。具有实际部署价值。
- **实验全面**：覆盖多种GPU、模型、任务、稀疏度组合，包括详细的硬件剖析，展示了效率-质量帕累托改善。
- **开源可用**：提供代码仓库（GitHub），便于复现。

## 8. 不足与局限

- **适用范围**：主要针对解码阶段的内存受限场景，预填充阶段加速有限（甚至不如稠密）。在预填充-解码分离部署中需额外设计。
- **动态剪枝方法单一**：仅测试TEAL，未验证与其他动态剪枝方法（如DejaVu、CATS）的兼容性，泛化性未充分证明。
- **量化组合效率**：低精度量化下元数据（Bitmap、ColInfo）占比增大，SP和量化不提供乘法压缩收益，需要更高稀疏度才能体现优势。
- **通用性有限**：在SuiteSparse集合上性能不如cuSPARSE/Sputnik，说明不适用于极端稀疏或非LLM工作负载。
- **硬件依赖**：关键优化（cp.async、Tensor Core）依赖NVIDIA Ampere及以上架构，无法直接迁移到其他硬件（如Intel CPU/AMD GPU）。
- **未考虑批处理动态剪枝**：当前实现针对单token解码，未扩展到batch DP或连续批处理场景。
- **调参成本**：动态剪枝阈值需要校准（ECDF），可能增加部署复杂度。

（完）
