---
title: "BlockPilot: Instance-Adaptive Policy Learning for Diffusion-based Speculative Decoding"
title_zh: BlockPilot：面向扩散式推测解码的实例自适应策略学习
authors: "Hao Zhang, Yiming Hu, Yong Wang, Mingqiao Mo, Xin Xiao, Xiangxiang Chu"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31315v1"
tags: ["query:llm"]
score: 9.0
evidence: 直接研究基于扩散的块级投机解码方法
tldr: 推测解码通过草稿模型并行生成候选token来加速推理，扩散推测解码进一步利用块级扩散并行生成多个token。现有方法固定块大小和统一策略，但最优块大小因样本而异且具有局部结构。本文提出BlockPilot，从预填充表示中预测最优块大小，实现实例自适应策略。实验在Qwen3-4B上取得5.92接受长度和4.20倍加速，即插即用且开销极低。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31315v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 678, \"height\": 283, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31315v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1385, \"height\": 540, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31315v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1439, \"height\": 361, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31315v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 425, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31315v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 679, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31315v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1462, \"height\": 990, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31315v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 680, \"height\": 434, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31315v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 706, \"height\": 248, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31315v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 706, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31315v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1461, \"height\": 989, \"label\": \"Table\"}]"
motivation: 现有扩散推测解码采用固定块大小和统一策略，但最优块大小因样本而异，导致效率损失。
method: BlockPilot从预填充表示中通过轻量策略学习预测每个样本的最优块大小，实现实例自适应决策。
result: 在Qwen3-4B上接受长度达5.92，获得4.20倍加速（温度T=1）。
conclusion: BlockPilot即插即用，开销极低，显著提升扩散推测解码效率，验证了自适应块大小策略的有效性。
---

## 摘要
推测解码通过使用轻量级草稿模型并行生成候选令牌，再由目标模型验证，从而实现无损加速。近年来，基于扩散的推测解码通过块级扩散在每次前向传播中生成多个令牌，进一步提升了并行性，达到了最先进的性能。然而，现有方法采用固定的推理块大小，并假设所有输入都采用统一的最优解码策略。本文表明，这一假设并非最优，因为最优块大小在不同样本间存在差异，并且对推测解码性能至关重要。此外，这些最优值呈现出清晰的局部结构，集中在训练块大小附近，从而将问题简化为低维且结构化的决策空间。基于这些发现，我们提出了BlockPilot，一种样本自适应策略，通过预填充表示预测最优块大小。具体而言，我们将块大小选择形式化为一个轻量级策略学习问题，并提出一种实例自适应决策机制，根据预填充阶段的表示来预测最优块大小。该预测仅在预填充后执行一次，便于无缝集成。大量实验表明，我们的方法即插即用，开销极小，并能持续提升效率，在温度T=1时，Qwen3-4B上的接受长度达到5.92，加速比达到4.20倍。

## Abstract
Speculative decoding accelerates inference by using a lightweight draft model to generate candidate tokens in parallel, and are then verified by the target model, enabling lossless acceleration. Recently, diffusion-based speculative decoding further improves parallelism by generating multiple tokens per forward pass via block-level diffusion, achieving state-of-the-art (SOTA) performance. However, existing methods adopt a fixed inference block size and assume a uniform optimal decoding strategy across all inputs. In this paper, we show that this assumption is suboptimal, as the optimal block size varies across samples and plays a critical role in speculative decoding performance. Moreover, these values exhibit a clear local structure, concentrating around the training block size, which reduces the problem to a low-dimensional and structured decision space. Based on these insights, we propose BlockPilot, a sample-adaptive policy that predicts the optimal block size from the prefilling representation. Specifically, we formulate block size selection as a lightweight policy learning problem and propose an instance-adaptive decision mechanism that predicts the optimal block size based on the representation of the prefilling stage. The prediction is performed only once after prefilling, allowing for seamless integration. Extensive experiments demonstrate that our method is plug-and-play, introduces minimal overhead, and consistently improves efficiency, achieving an acceptance length of 5.92 and a 4.20$\times$ speedup on Qwen3-4B under temperature $T=1$.

---

## 论文详细总结（自动生成）

# BlockPilot：面向扩散式推测解码的实例自适应策略学习 —— 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：大语言模型（LLM）推理受限于逐词自回归解码的低并行性。推测解码通过轻量级草稿模型并行生成候选词，再由目标模型验证，可实现无损加速。基于扩散的推测解码（如DFlash）进一步通过块级扩散在一次前向中生成多个词，达到SOTA性能。
- **现有局限**：现有方法均采用固定的推理块大小（与训练块大小一致），假设对所有输入都适用统一最优解码策略。作者指出这一假设不成立：最优块大小因样本而异（受上下文可预测性影响），且固定策略无法充分利用并行潜力。
- **关键发现**：最优块大小虽然变化，但其分布呈现强**局部结构**——绝大多数样本的最优块大小集中在训练块大小附近（±3以内），将问题从全局搜索简化为**局部离散分类**。
- **整体含义**：本文提出BlockPilot，将块大小选择视作可学习策略，实现样本自适应的并行度选择，以进一步提升扩散推测解码效率。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想

- 利用目标模型预填充阶段**最后 token 的预测概率分布**作为解码状态表示，学习一个轻量级预测器，从**局部候选集** {B-k, ..., B+k}（k=2）中预测每个样本的最优块大小。

### 2.2 关键技术细节

1. **问题形式化**：
   - 平均每词生成延迟 L(B) = (T_draft(B) + T_verify(B)) / τ(B)，其中 τ(B) 是期望接受长度。
   - 目标是找到最大化 τ(B) 的样本自适应块大小 B*。

2. **数据构建**：
   - 对每个训练样本 x，枚举局部候选集内所有块大小 b，执行完整推测解码得到接受长度 τ(b; x)，取最大值对应块大小为监督标签。
   - 候选集小（2k+1=5），枚举可并行，成本可接受。

3. **预测器设计**：
   - 输入：预填充阶段最后 token 的预测概率分布 p(x)（完整分布，非仅 top-k，避免过拟合）。
   - 模型：**2层MLP**，隐藏层维度2048，输出logits经softmax得到各类别概率。
   - 训练目标：交叉熵损失。
   - 推理时：预测仅执行一次（预填充后），选择得分最高的块大小贯穿整个解码过程。

4. **引入模块的代价**：预测器仅0.32B参数，延迟约7.34ms，远小于骨干模型（如Qwen3-4B延迟183.48ms），内存占用可忽略。

## 3. 实验设计：数据集、基准测试与对比方法

### 3.1 数据集与场景
| 类别 | 数据集 |
|------|--------|
| 数学推理 | GSM8K, MATH-500, AIME24 |
| 代码生成 | HumanEval, MBPP, SWE-Bench |
| 对话生成 | MT-Bench |

涵盖数学、代码、聊天三大典型场景。

### 3.2 基准模型
- 目标模型：Qwen3-4B, Qwen3-8B, Llama-3.1-8B-Instruct, Qwen3-Coder-30B-A3B（覆盖不同规模和领域）。
- 对比方法：
  - 标准自回归解码（baseline）
  - EAGLE-3（经典自回归草稿模型推测解码）
  - DFlash（SOTA扩散推测解码），使用不同固定块大小：4, 8, 16, 32。

### 3.3 评估指标
- **平均接受长度 τ**：每个推测-验证周期内被接受的草稿词数。
- **加速比**：标准自回归解码时间 / 推测解码时间（无损，输出分布不变）。

## 4. 资源与算力

- **硬件**：NVIDIA H100 80GB GPU（未说明具体数量）。
- **训练数据**：ShareGPT、WSC、COPA 三个数据集（未给出具体大小）。
- **训练配置**：100 epoch，Adam优化器，学习率 1e-5。
- **数据构建开销**：每个样本需枚举5个块大小（k=2），在32B模型上，每个样本约需25秒（5秒×5），但仅在离线进行一次。
- **未明确说明**：GPU数量、总训练时间、数据量大小。

## 5. 实验数量与充分性

论文进行了多组实验：

- **主实验**（表2）：在Qwen3-4B/8B上，温度=0和温度=1两种设置，共7个数据集×2种温度×2种模型，报告加速比和τ，对比EAGLE-3和DFlash所有变体。
- **跨模型实验**（表6）：在Llama-3.1-8B-Instruct和Qwen3-Coder-30B-A3B上重复上述实验。
- **消融实验**（表3-5）：
  - 预测器配置（隐藏层大小、层数）
  - 候选区间半径k（1,2,3）
  - 输入预处理方式（softmax、归一化 vs 原始分布）
- 每个消融选取3个代表性数据集（GSM8K、HumanEval、MT-Bench）进行验证。
- **未做**：与其他自适应方法（如动态树结构）的对比；更大模型规模（如70B+）的验证。

**评价**：实验覆盖多个模型、多种温度、多类任务，消融全面，对比基线SOTA且公平（与最强固定块DFlash变体对比），实验设计较为充分。

## 6. 主要结论与发现

1. **固定块大小策略次优**：仅部分样本在训练块大小下达到最优，大量样本偏好不同推理块大小。
2. **局部区间性质**：最优块大小几乎均落在[B-3, B+3]内，将问题简化为小规模分类。
3. **预测器有效性**：轻量MLP利用预填充最后token分布，可有效预测最优块大小，且即插即用，开销极低。
4. **性能提升**：在Qwen3-4B上，温度=1时达到τ=5.92，加速比4.20×，全面超越EAGLE-3和所有固定块DFlash。在更大模型上同样一致提升。
5. **稳定性**：在随机采样（温度=1）下依然有效，表明该信号在更不可预测的生成中也具有鲁棒性。

## 7. 优点

- **问题新颖**：首次将解码策略（块大小）作为可学习组件，识别到固定策略的局限性。
- **方法简洁高效**：仅需一次预填充后预测，无需修改目标/草稿模型，兼容现有框架。
- **理论支撑**：附录A给出了基于前缀生存概率和二次保留函数的理论分析，解释局部性。
- **实验扎实**：跨模型、跨任务、多温度，消融实验覆盖关键设计维度，对比基线为SOTA方法。
- **开销极低**：预测器0.32B参数、7ms延迟，几乎不影响整体推理。

## 8. 不足与局限

- **数据构建成本**：虽然离线，但对大型模型（如32B）每个样本需约25秒，若需扩展至更大模型或更大训练集，成本可能显著增加。
- **未与动态树/自适应草稿结构方法对比**：例如EAGLE-2/3可动态调整草稿结构，本文未比较其与自适应块大小结合的潜力。
- **候选区间固定**：假设所有模型和任务中局部范围k=2是最优，但可能随模型或数据集变化，缺乏跨迁移验证。
- **训练数据规模未明确**：ShareGPT、WSC、COPA的具体大小未知，可能影响泛化性讨论。
- **未评测大模型（70B+）**：实验最大为30B，更大模型上的行为未验证，但方法论应可扩展。
- **实际部署考虑**：预测器依赖目标模型的预填充表示，若预填充本身耗时较长，则引入预测器可能略有额外延迟，但文中已表明可忽略。

（完）
