---
title: "Depth-Attention: Cross-Layer Value Mixing for Language Models"
title_zh: 深度注意力：语言模型的跨层值混合
authors: "Boyi Zeng, Yiqin Hao, Zitong Wang, Shixiang Song, He Li, Feichen Song, Yifan Liu, Ziwei He, Xinbing Wang, Zhouhan Lin"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05014v1"
tags: ["query:llm"]
score: 8.0
evidence: 在注意力机制中进行跨层值混合以提升大语言模型推理效率，与 KV 缓存优化相关
tldr: "标准Transformer跨层信息仅通过残差相加，限制了浅层表示的选择性利用。Depth-Attention在注意力内部引入跨层值混合：当前层查询关注前层键并加权融合其值，复用标准KV缓存而不增加参数或推理状态。在Qwen3-style 1.5B和3B模型上，该方法取得最低困惑度和最高下游准确率，相比基线提升2.3个点，额外计算量小于0.01%，且无额外缓存开销。该机制从360M到3B参数及循环Transformer中均显著有效，实现了高效且无代价的跨层信息选择。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05014v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1693, \"height\": 950}, {\"url\": \"assets/figures/arxiv/2606.05014v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 830, \"height\": 661}, {\"url\": \"assets/figures/arxiv/2606.05014v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 693, \"height\": 607}, {\"url\": \"assets/figures/arxiv/2606.05014v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 494}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05014v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1256}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1726, \"height\": 345}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 876, \"height\": 376}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 740, \"height\": 306}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 980, \"height\": 296}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 978, \"height\": 301}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1649, \"height\": 641}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1726, \"height\": 376}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1647, \"height\": 724}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1665, \"height\": 1061}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1655, \"height\": 659}]"
motivation: 现有Transformer跨层仅通过残差相加，无法选择性重用浅层表示；近期跨层方法虽改进但增加推理状态和缓存开销。
method: 在注意力模块内，当前层查询关注之前同一位置的键，将对应值加权混合后作为本层值，复用标准KV缓存，无额外参数。
result: "在Qwen3-style 1.5B和3B上困惑度最低，下游准确率最高提升2.3点，计算量增加<0.01%，KV缓存不变。"
conclusion: Depth-Attention实现了无额外代价的跨层信息选择，在多种规模模型上稳定提升性能，并适用于循环Transformer。
---

## 摘要
自注意力机制可以在序列中自由选择信息，但在深度维度上，Transformer只是将每一层的输出添加到残差流中，因此后续层无法选择性地重用早期层的表示。最近的跨层方法改进了这一流程，但在注意力之外的隐藏状态上操作，增加了推理时键值缓存之外的状态——随着现代LLM使用分组查询和多头潜在注意力压缩缓存，这一成本日益显著。我们引入了深度注意力，它在注意力模块内部执行这种选择：在一层对序列进行注意力计算之前，其查询会关注同一 token 位置上的早期层的键，并将它们的值混合到自注意力随后读取的值中。由于深度注意力重用了标准的注意力查询、键和值缓存槽，用深度混合值替换原始值，它不增加参数，也不引入标准键值缓存之外的持久推理状态——缓存大小与普通解码器相同，且小于基于隐藏状态的跨层方法。在1.5B和3B参数的Qwen3风格解码器上，深度注意力实现了最低的困惑度和最高的平均下游准确率，与普通Transformer相比，准确率提升高达2.3个百分点，并在困惑度和平均准确率上超越了强大的跨层基线，同时仅增加不到0.01%的额外算术FLOPs，且没有额外的持久推理状态。这些优势在360M到3B参数规模上均成立，并扩展到循环Transformer。

## Abstract
Self-attention selects information freely across the sequence, but across depth, Transformers merely add each layer's output to the residual stream, so later layers cannot selectively reuse earlier-layer representations. Recent cross-layer methods improve this flow but operate on hidden states outside attention, adding state beyond the key-value cache at inference--a cost that becomes increasingly salient as modern LLMs compress the cache with grouped-query and multi-head latent attention. We introduce Depth-Attention, which performs this selection inside the attention module itself: before a layer attends over the sequence, its query attends over the keys of earlier layers at the same token position and mixes their values into the value that self-attention then reads. Because Depth-Attention reuses the standard attention queries, keys, and value-cache slots, storing depth-mixed values in place of the original values, it adds no parameters and introduces no persistent inference state beyond the standard key-value cache--the same cache size as a vanilla decoder and less than hidden-state-based cross-layer methods. On Qwen3-style decoders at 1.5B and 3B parameters, Depth-Attention attains the lowest perplexity and the highest average downstream accuracy, improving over the vanilla Transformer by up to 2.3 accuracy points and surpassing strong cross-layer baselines in perplexity and average accuracy, while adding under 0.01% extra arithmetic FLOPs and no additional persistent inference state. The gains hold from 360M to 3B parameters and extend to looped Transformers.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：标准Transformer解码器通过残差连接传递跨层信息，但残差将所有先前层的表示压缩成一个单一隐藏状态，导致后续层无法选择性地重用任意特定早期层的表示。现有的跨层方法（如DenseFormer、Attention Residuals、mHC）虽然改进了信息流，但这些方法操作在隐藏状态层面上，需要在标准键值缓存（KV cache）之外额外维护或计算隐藏状态，从而增加推理时的内存和计算开销。随着现代LLM采用分组查询注意力（GQA）或多头潜在注意力（MLA）来压缩KV缓存，这种额外开销变得相对更加显著。
- **研究动机**：能否在注意力模块内部设计一种机制，复用已有的键和值来携带跨层信息，从而在不增加任何推理内存的前提下实现选择性的跨层信息重用？

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出深度注意力（Depth-Attention），在注意力模块内部执行跨层值混合。具体来说，在每个Transformer层进行标准因果自注意力之前，当前层的查询（query）会沿着深度维度（即层维度）关注同一token位置上当前层及之前层的键（key），然后使用得到的深度注意力权重加权混合对应的值（value），产生一个深度混合值状态（depth-mixed value）。随后，该层执行标准因果自注意力，但读取的是这个深度混合值，而查询、键和因果掩码保持不变。
- **关键技术细节**：
  - **值混合公式**：对于第ℓ层位置t的token，深度注意力权重 α_t^{ℓ,j} = softmax_{j≤ℓ} ( q_t^ℓ · k_t^j / √d )，然后深度混合值 e_v_t^ℓ = α_t^{ℓ,ℓ} v_t^ℓ + Σ_{j<ℓ} α_t^{ℓ,j} e_v_t^j。注意早期层使用的也是自己的深度混合值，从而实现信息向上传播。
  - **无额外参数与状态**：深度混合值直接存储在原本的V cache槽中，代替原始值。因此不增加任何参数，持久推理状态与标准KV cache完全一致（大小不变）。
  - **稀疏深度源**：为避免深度源数量随层数线性增长，采用步长s来稀疏选取早期层（例如只考虑ℓ-s, ℓ-2s, ...）。默认步长s = L/2（总层数的一半），实验证明该设置最优。
  - **适配GQA**：在分组查询注意力下，每个查询组先平均得到一个公共查询，然后在该查询组对应的单个KV头上执行深度注意力，进一步减少计算和内存。

### 3. 实验设计
- **数据集与场景**：
  - 预训练数据集：The Pile（800GB多样化文本）。
  - 下游评估任务：LAMBADA（完形填空）、PIQA（物理常识）、WinoGrande（代词消歧）、SciQ（科学问答）、HellaSwag（句子完成）、ARC-Easy / ARC-Challenge（推理问答）、RACE（阅读理解）。评估设置：零样本和五样本。
- **Benchmark**：
  - 主要对比的基线：标准Transformer解码器（vanilla）、Manifold Hyper-Connections (mHC)、Attention Residuals、DenseFormer。
  - 消融实验中的变体：不同步长、均匀混合 vs. 自适应软注意力混合、仅值混合 vs. 键混合或键值混合。
- **模型架构**：基于Qwen3-style解码器，规模为1.5B和3B参数（主实验），此外还测试了360M、500M、710M和循环Transformer（500M 3-loop）。

### 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量和训练时长。只提到模型在32B tokens上训练，使用FP16精度、AdamW优化器、cosine学习率衰减、全局batch size 1024、序列长度2048。训练步数约15625步（32B tokens / 1024*2048）。训练在“有限计算资源”下完成，可能使用了若干张GPU（但具体型号与数量未披露）。

### 5. 实验数量与充分性
- **实验数量**：包含了多个维度的对比实验：
  - 主实验：在1.5B和3B规模上与三种强基线对比，报告了困惑度和8个下游任务的零样本/五样本准确率。
  - 效率分析：理论FLOPs、I/O、内存对比，以及实际训练/推理耗时、峰值内存测量。
  - 规模扩展实验：从360M到3B共6个模型规模，验证趋势。
  - 循环Transformer实验：在500M 3-loop模型上验证。
  - 消融实验（500M模型）：包括步长选择、混合规则（均匀 vs. softmax）、混合对象（键 vs. 值 vs. 键值）。
- **充分性与公平性**：实验设计较为全面，覆盖了方法的主要设计选择和不同规模。基线按照原论文推荐配置设置（如mHC使用4流、Attention Residuals使用8个块等），并且共享相同的训练框架和超参数。但所有方法均未使用自定义融合内核，统一在参考PyTorch实现下比较，避免了实现偏差，但可能对需大量额外I/O的方法不利（但论文指出这公平地反映了方法论开销）。结论有一定局限性（见局限部分）。

### 6. 论文的主要结论与发现
- **主要结论**：Depth-Attention在1.5B和3B Qwen3-style模型上取得了最低的验证困惑度和最高的平均下游准确率（零样本提升2.3个点，五样本提升1.5个点），超越了vanilla Transformer和所有跨层基线。
- **效率优势**：理论额外FLOPs仅0.004%（相对于2N），额外持久推理内存为0 GiB；实际训练开销（+9%~+11%）和推理开销（+1.18%）均显著低于其他跨层方法。
- **规模稳定性**：从360M到3B参数，Depth-Attention始终优于vanilla，且收益稳定。
- **循环Transformer适用性**：在循环Transformer中同样有效，验证损失从2.208降至2.194。
- **消融洞察**：半深度步长最优；自适应软注意力混合优于均匀混合；仅值混合优于键混合或键值混合；可视化表明模型确实学习到了非平凡的跨层分配权重。

### 7. 优点
- **方法简洁高效**：不增加参数和持久缓存，仅修改值状态，与现有KV cache机制完全兼容。
- **理论开销极低**：通过稀疏步长将深度注意力复杂度限制在O(T L / s)，相对于序列自注意力O(T²)可忽略。
- **实验验证全面**：不仅考察了预训练困惑度和下游任务，还剖析了效率、规模行为、循环场景和设计选择，可信度高。
- **适配主流架构**：自然适配GQA，无需额外设计；代码已开源。

### 8. 不足与局限
- **实现未融合**：当前PyTorch版本未进行内核融合，导致实际训练开销（+11%）高于理论值，作者承认可通过自定义内核大幅缩小差距。
- **规模与数据有限**：实验仅验证到3B参数和32B训练tokens，更大规模（如7B/13B）和更多训练数据（如1T tokens）下的表现未知，结论可能受限于资源。
- **固定稀疏策略**：使用固定步长选择深度源，未尝试学习或自适应选择，可能不是最优。
- **仅值混合**：只混合了值，未探索键或其他状态的可能收益，虽然消融表明值混合最好，但空间有限。
- **单一架构/数据集**：仅在Qwen3风格架构和Pile数据集上测试，通用性尚需在其他架构（如LLaMA、GPT）和领域数据上验证。
- **潜在偏差**：所有基线均未经过同等力度调优（如步长、块数等仅按原文推荐），可能未达到基线最佳性能；此外，训练和评估使用的随机性和数据顺序未做充分控制。

（完）
