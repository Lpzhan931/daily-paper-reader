---
title: Express Language Modeling
title_zh: Express语言建模
authors: "Albert Gong, Annabelle Michael Carrell, Raaz Dwivedi, Lester Mackey"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10944v1"
tags: ["query:llm"]
score: 8.0
evidence: Express工具实现KV缓存压缩和长上下文解码加速，性能优于FlashAttention 2
tldr: "语言模型处理长序列时，因果注意力计算存在严重资源瓶颈。本文提出Express工具，能将非因果注意力近似高效转换为因果近似，并保持原有精度保证。结合先进Thinformer方法，实现仅O(s)内存和O(s^2 log^2(n))压缩开销下达到log^{3/2}(n)/s近似误差。基于Triton的I/O优化实现，速度超越FlashAttention 2，有效克服长上下文预填充、KV缓存压缩、长序列解码等四大瓶颈。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10944v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 573, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2606.10944v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 1280}, {\"url\": \"assets/figures/arxiv/2606.10944v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1403, \"height\": 621}, {\"url\": \"assets/figures/arxiv/2606.10944v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1399, \"height\": 483}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10944v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1471, \"height\": 359}, {\"url\": \"assets/tables/arxiv/2606.10944v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 671, \"height\": 189}, {\"url\": \"assets/tables/arxiv/2606.10944v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1148, \"height\": 257}, {\"url\": \"assets/tables/arxiv/2606.10944v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1163, \"height\": 256}, {\"url\": \"assets/tables/arxiv/2606.10944v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 966, \"height\": 268}, {\"url\": \"assets/tables/arxiv/2606.10944v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1224, \"height\": 487}]"
motivation: 因果注意力近似在精度与效率间难以平衡，缺乏统一转换框架。
method: 设计Express将非因果注意力近似适配为因果形式，保持近似保证，结合Thinformer实现高效压缩。
result: "序列长度n下达到log^{3/2}(n)/s近似误差，内存O(s)，压缩开销O(s^2 log^2(n))，并实现I/O高效加速。"
conclusion: Express成功解决语言模型推理中四个关键资源瓶颈，提升长序列处理效率。
---

## 摘要
我们介绍了一个新工具Express，用于将非因果注意力近似转换为具有匹配近似保证的因果近似。当与最先进的Thinformer近似结合时，Express改进了已知的最佳因果注意力保证，对于长度为n的序列，仅需O(s)内存和O(s² log²(n))压缩开销，即可实现log^{3/2}(n)/s的近似误差。我们将这些进展与高效的I/O感知Triton实现相结合，展示了相比FlashAttention 2的显著加速，并使用Express克服了语言建模流程中的四个资源瓶颈：长上下文预填充、KV缓存压缩、长形式内存受限解码以及长形式计算受限解码。

## Abstract
We introduce a new tool, Express, for converting a non-causal attention approximation into a causal approximation with matching approximation guarantees. When combined with the state-of-the-art Thinformer approximation, Express improves upon the best known causal attention guarantees, delivering $\log^{3/2}(n)/s$ approximation error with only $O(s)$ memory and $O(s^2 \log^2(n))$ compression overhead for a sequence of length $n$. We pair these developments with an efficient I/O-aware Triton implementation, demonstrate substantial speedups over FlashAttention 2, and use Express to overcome four resource bottlenecks in the language modeling pipeline: long-context prefill, KV cache compression, long-form memory-constrained decoding, and long-form compute-constrained decoding.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在语言模型中，因果注意力（causal attention）的计算复杂度随序列长度 n 呈二次增长，导致长上下文预填充、KV 缓存压缩、长形式解码等环节出现严重的资源瓶颈。现有的高效注意力近似方法（如 Thinformer）主要针对非因果（无掩码）注意力设计，缺少能高效转换为因果掩码场景且保持近似保证的通用方法。
- **整体含义**：本文旨在填补这一空白，提出一种元过程 Express，能够将任意基于子选择（thinning）的非因果注意力近似转换为等价的、可流式更新的因果近似，从而在不牺牲精度与效率的前提下突破语言模型推理中的四个关键资源瓶颈。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（文字说明）

- **核心思想**：
  - 设计 **Express** 元算法，以增量的方式维护一个加权核心集（weighted coreset），将静态的离线 thinning 算法适配为动态、流式的因果注意力近似。
  - 将 Express 与当前最先进的非因果 thinning 方法 **Thinformer**（基于核减半算法 KH(δ)）结合，得到 **Thinformer Express**。
- **关键技术细节**：
  - **Express 算法流程**（算法 1）：
    1. **精确阶段**：前 nout 个 token 直接存入核心集。
    2. **瘦身阶段**：后续 token 按批次（大小 2m nout）先均匀子采样（Subsample），再用 **Compress2**（对几何递增大小的组逐级 KH 减半）压缩至 nout 个点。
    3. **减半阶段**：当核心集大小达到 4nout 时，连续调用两次 KH 将其减至 nout，并增大 m。
  - **Thinformer Express**（算法 4）：
    - 使用核减半算法 KH(δ) 和注意力核 κ_att（公式 3），初始化一个容量为 2h 的 Express 加权缓存。
    - 对每个新 token，先通过 **WtdAttention**（算法 5）仅对当前 token 和缓存中的键值对计算加权注意力输出，然后将新键值对加入缓存。
- **理论保证**（定理 1–3）：
  - Express 的加权缓存大小恒定为 O(6nout)，空间复杂度 O(nout d)，运行时间对 n 仅对数依赖。
  - Thinformer Express 能达到 O(log^{3/2}(n)/s) 的近似误差（s 为缓存大小），优于现有最佳保证（如 HyperAttention 和 BalanceKV），且值矩阵依赖从 ∥V∥op 或 ∥V∥F 改进为 ∥V∥2,∞。

### 3. 实验设计：使用的数据集/场景、benchmark、对比方法

- **实验场景**：
  1. **加速长上下文预填充**：使用 ChatGLM2-6B-32K 模型，在 LongBench-E 任务上评估。
  2. **加速 KV 缓存压缩**：使用 Llama 3.1 8B Instruct 模型，在 TREC-E、TriviaQA-E、HotpotQA-E 三个 LongBench-E 子任务上评估。
  3. **内存高效长形式解码**：使用 DeepSeek-R1-Distill-Llama-8B 模型，在 MATH-500 竞赛级数学推理数据集上评估。
  4. **加速长形式解码**：同样使用 MATH-500 和 DeepSeek-R1-Distill-Llama-8B 模型。
- **Benchmark**：精确注意力（FlashAttention 2）作为标准参考；预填充实验采用 HyperAttention 作为对比；缓存压缩和长形式解码实验采用 SnapKV、StreamingLLM、PyramidKV、ExpectedAttention、KeyDiff、Knorm 等五种主流方法。
- **对比方法**：
  - 预填充：HyperAttention（官方 Triton 实现）。
  - 缓存压缩：SnapKV、StreamingLLM、PyramidKV 的原始版本及其与 Express 结合的版本。
  - 长形式解码：StreamingLLM、SnapKV、ExpectedAttention、KeyDiff、Knorm，均采用公开实现和默认参数。

### 4. 资源与算力

- **硬件平台**：单台服务器，配备 4 × Intel Xeon Gold 5218 CPU（2.30 GHz）、64 GB RAM、单张 NVIDIA RTX A6000 GPU（48 GB 显存，CUDA 12.8，驱动 570.124.06）。
- **软件**：Ubuntu 24.04.4 LTS，Python 3.12~3.13，PyTorch 2.8~2.10，Triton 编译器。
- **训练与推理**：所有实验均为推理（inference）场景，未涉及模型训练；文中未报告 GPU 使用时长或总能耗。

### 5. 实验数量与充分性

- **实验数量**：本文围绕四个场景共设计了 6 组主要实验（图 1a–c、图 2、图 3 左和右），每组实验包含多个配置（不同 h 值、不同缓存大小、不同层数替换等）。图 3 中每个方法在 7 个缓存大小上评估，图 2 中每种压缩方法在不同缓存大小下生成 trade-off 曲线。
- **充分性**：
  - 覆盖了语言模型推理的四个关键瓶颈，场景全面。
  - 对比了 5–6 种主流基线方法，且面向每个场景使用了对应领域的最佳实践（如 HyperAttention 用于预填充，KV 压缩方法用于解码）。
  - 所有实验报告了误差带（±1 标准误），并进行了多次重复（warm-up + 计时），保证了统计学可靠性。
  - 不足：仅涉及英文和中文语言模型及数学推理任务，未涵盖其他语种或领域（如图像生成、多模态）；未进行消融实验以单独验证 Express 中每个组件（如 Subsample vs. Compress2）的贡献。

### 6. 论文的主要结论与发现

- Thinformer Express 在所有四个场景中均优于或匹配现有方法，且能显著降低内存和计算开销：
  - **长上下文预填充**：在 512K token 长度下，Thinformer Express 实现了 **82×** 加速比（FlashAttention 2 为基准），远超 HyperAttention（后者在 512K 时 OOM）。在 LongBench-E 上，Thinformer Express（h=10）同时提升了困惑度和加速比。
  - **加速 KV 缓存压缩**：与 SnapKV、StreamingLLM、PyramidKV 结合后，总注意力运行时间减少，且精度无显著损失。
  - **内存高效长形式解码**：在 MATH-500 上，Thinformer Express 在几乎每个缓存大小下取得最高准确率，仅用 **61%** 缓存即可达到精确注意力的准确率。
  - **加速长形式解码**：在 MATH-500 上，Thinformer Express 仅用 **56%** 的计算时间即达到精确注意力的准确率，且维护 Express 缓存的压缩开销远低于查询开销（470 ms vs. 35000 ms）。
- 理论分析表明，Thinformer Express 的误差界随缓存大小 nout 衰减更快（O(n^{-a}) vs. O(n^{-a/6})），且对值矩阵的依赖更优。

### 7. 优点

- **方法论创新**：提出统一的 Express 框架，将离线 thinning 方法在线化，理论保证简洁且几乎无损。
- **理论贡献**：给出了迄今最紧的因果注意力近似误差界（O(log^{3/2}(n)/s)），改进了 HyperAttention 和 BalanceKV 的经典结果。
- **工程实现**：基于 Triton 的 I/O 感知实现，利用分块并行和消除冗余数据传输，显著快于原始 PyTorch 编译版本。
- **实验全面性**：覆盖四个独立且实际重要的推理瓶颈，每个场景均使用多个数据集和基线，结果稳健。
- **开源友好**：代码已公开（GitHub），便于复现与二次开发。

### 8. 不足与局限

- **语言与领域覆盖**：实验仅涉及英文（Llama 3.1）和中文（ChatGLM2）语言模型以及数学推理，未在其他语言（如法语、阿拉伯语）或多模态任务中验证。
- **硬件适配**：Triton 实现未利用 warp specialization、Tensor Memory Accelerator、FP8 等新特性，实际加速潜力可能更大；作者承认这些优化可直接应用于未来版本。
- **基算法依赖**：核心基于 KH(δ) 算法，但 Express 本身兼容任意 halving 算法；论文未对比其他 base halving 算法的表现，仅理论兼容。
- **可能存在的风险**：效率提升可能引发杰文斯悖论（Jevons paradox），导致总体使用量上升而抵消环境收益。

（完）
