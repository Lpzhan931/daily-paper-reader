---
title: Cost-Aware Diffusion Draft Trees for Speculative Decoding
title_zh: 面向推测解码的成本感知扩散草稿树
authors: "Shuai Zhang, Huachuan Qiu, Hongliang He, Yong Dai"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01813v1"
tags: ["query:llm"]
score: 9.0
evidence: 面向投机解码的成本感知扩散草案树
tldr: 现有推测解码的块扩散草稿方法（如DDTree）在固定节点预算下最大化期望接受长度，但接受长度随预算非递减，缺乏基于验证成本的预算选择原则。本文提出CaDDTree，通过联合选择树结构和节点预算直接优化令牌吞吐量，并证明在凸验证成本下吞吐量函数是单峰的，从而支持高效贪心停止规则。实验表明，CaDDTree在多个基准上匹配或超越使用最优预算的DDTree。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01813v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1644, \"height\": 555}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 781, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 792, \"height\": 524}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1625, \"height\": 601}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1649, \"height\": 619}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01813v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1663, \"height\": 826}, {\"url\": \"assets/tables/arxiv/2606.01813v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 746, \"height\": 179}, {\"url\": \"assets/tables/arxiv/2606.01813v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 811, \"height\": 143}, {\"url\": \"assets/tables/arxiv/2606.01813v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1664, \"height\": 829}]"
motivation: 现有块扩散草稿树方法缺乏基于验证成本的预算选择原则，导致吞吐量优化不充分。
method: 提出CaDDTree，显式建模草稿和验证延迟，联合搜索树结构与节点预算，利用吞吐量分解和凸性实现单峰搜索。
result: 在Qwen3-4B和Qwen3-8B上，CaDDTree在推理、编码等任务中匹配或超越最优预算DDTree。
conclusion: CaDDTree自适应调整预算，无需离线搜索，在多种任务上有效提升推测解码吞吐量。
---

## 摘要
推测解码通过让轻量级起草器生成候选词并由目标语言模型并行验证来加速推理。诸如DFlash之类的块扩散起草器一次生成整个草稿块，产生每个位置上的边缘分布；DDTree利用这些分布构建候选树，在固定节点预算下最大化期望接受长度。然而，我们观察到接受长度随预算非递减：无论验证成本如何，它总是倾向于更大的树，这为预算选择提供了无原则的依据。我们提出了一种直接优化令牌吞吐量（单位时间内预期生成的令牌数）的方法——	extbf{CaDDTree}（成本感知扩散草稿树），该方法联合选择树结构和节点预算。我们显式建模起草和验证延迟，证明吞吐量目标分解为每轮对预算的一维搜索，并证明在凸验证成本下吞吐量函数是	extit{单峰的}，从而实现了高效的贪心停止规则。CaDDTree不需要离线预算搜索，而是每轮根据当前的位置分布和验证成本自适应调整预算。在Qwen3-4B和Qwen3-8B上的实验涵盖推理、编码和指令遵循等八个基准测试，结果表明CaDDTree在几乎所有任务上匹配或超越了使用最优预算选择的DDTree。

## Abstract
Speculative decoding accelerates inference by having a lightweight drafter propose tokens verified in parallel by the target language model. Block diffusion drafters such as DFlash generate an entire draft block in one pass, yielding per-position marginals; DDTree uses these to build a candidate tree that maximizes expected acceptance length under a fixed node budget. We observe, however, that acceptance length is non-decreasing in budget: it always favors larger trees regardless of verification cost, offering no principled basis for budget selection. We introduce \textbf{CaDDTree} (Cost-aware Diffusion Draft Tree), a method that directly optimizes token throughput (expected tokens generated per unit time) by jointly selecting the tree structure and node budget. We model draft and verification latencies explicitly, show that the throughput objective decomposes into a per-round one-dimensional search over the budget, and prove that under a convex verification cost the throughput function is \emph{unimodal}, enabling an efficient greedy stopping rule. CaDDTree requires no offline budget search, adapting the budget each round from the current per-position distributions and verification cost. Experiments on Qwen3-4B and Qwen3-8B across eight benchmarks spanning reasoning, coding, and instruction-following tasks show that \caDDTree{} matches or surpasses DDTree with oracle budget selection on nearly all tasks.

---

## 论文详细总结（自动生成）

# 中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **背景**：推测解码通过轻量草稿器提议令牌，目标模型并行验证，以加速自回归生成。树形验证（如SpecInfer）允许一次验证多个候选路径，但树的大小（节点预算）直接影响验证成本和接受长度。
- **现有问题**：基于块扩散的草稿树方法（如DDTree）在**固定节点预算**下最大化期望接受长度。然而，接受长度随预算**单调非递减**，缺乏考虑验证成本的原则来指导预算选择，导致吞吐量（每秒生成的令牌数）次优。
- **动机**：需要一种能够**直接优化吞吐量**，并**自适应调整每轮预算**的方法，以避免离线网格搜索和固定预算导致的效率损失。

## 2. 方法论
### 核心思想
- 将吞吐量（单位时间生成的令牌数）作为直接优化目标，而非期望接受长度。
- 显式建模草稿成本 \(C_d\) 和验证成本 \(C_v(n; \ell)\)（\(n\) 为节点数，\(\ell\) 为上下文长度）。
- **关键洞察**：在凸验证成本假设下，代理吞吐量函数 \(\hat{\theta}^*(n; \ell) = \frac{1 + \Phi^*(n)}{C_d + C_v(n; \ell)}\) 是**单峰的**，从而可以通过贪心停止规则高效找到最优预算 \(n^*\)。

### 关键技术细节
1. **成本建模**：
   - 草稿成本 \(C_d\) 为一次草稿器前向传播的固定延迟。
   - 验证成本 \(C_v(n; \ell)\) 通过离线 profiling 测量：在多个上下文长度和节点数网格上记录中位数延迟，然后通过**凸回归**拟合（保证单调非递减且凸性）。
   - 推理时通过线性插值获得任意 \(\ell\) 下的 \(C_v(n; \ell)\)。

2. **吞吐量优化目标分解**：
   - 利用 DDTree 中已有的最优树结构（基于前缀概率最大堆），只需搜索节点数 \(n\)。
   - 代理吞吐量 \(\hat{\theta}^*(n; \ell)\) 为单变量函数。

3. **贪心停止规则**（算法 1）：
   - 使用最大堆按前缀概率由大到小弹出节点。
   - 每加入一个节点 \(n\)，计算 \(\hat{\theta}_{\text{new}}\)；若 \(\hat{\theta}_{\text{new}} \leq \hat{\theta}_{\text{prev}}\) 则停止。
   - 由于存在单峰性，第一个下降点即为全局最优。

4. **理论保证**：
   - **引理 2**：\(\hat{\theta}^*(n+1)\) 可表示为 \(\hat{\theta}^*(n)\) 与边际效益-成本比 \(\rho_{n+1}\) 的加权平均，从而得出单调性条件。
   - **定理 3**：若边际效益-成本比 \(\rho_n\) 非增（凸成本下成立），则 \(\hat{\theta}^*\) 单峰。
   - **推论 4**：贪心停止规则找到全局最优。
   - **推论 5**：成本凸性 ⇒ 单调性条件成立。

## 3. 实验设计
### 数据集 / 场景
- **推理**：MATH-500、GSM8K、AIME 2025
- **编码**：HumanEval、MBPP、LiveCodeBench
- **指令遵循**：MT-Bench、Alpaca
- **温度**：0.0（贪婪解码）和 1.0（采样）

### Benchmark 与对比方法
- **目标模型**：Qwen3-4B、Qwen3-8B
- **草稿器**：DFlash（块扩散）
- **对比基线**：
  - AR：标准自回归（无推测解码）
  - DFlash：单序列草稿
  - DDTree-oracle：DDTree 配合网格搜索得到的最优固定预算（每数据集/模型/温度最佳值）
- **评价指标**：每令牌生成时间（ms/tok，越低越好）和平均接受长度 \(\tau\)

### 实验数量与充分性
- **主实验**：表 1（温度 0.0）和附录 C（温度 1.0），覆盖 8 个数据集 × 2 个模型 × 2 个温度，共 32 个设置。
- **消融实验**：
  - 成本模型对比：恒定成本（忽略节点数） vs. 真实凸回归成本（表 2）。
  - 成本参数敏感性（表 3）：将 \(C_d\) 和 \(C_v\) 分别乘以 0.5 和 2.0，观察延迟变化。
- **分析实验**：
  - 自适应预算分布图（图 4）：展示每轮 \(n^*\) 的分布。
  - 最优树可视化（图 5）：展示不同置信度下的树结构。
  - 验证成本曲线与凸回归拟合（图 3）。
- **公平性**：DDTree-oracle 使用网格搜索（16, 32, 64, 128, 256, 512, 1024）选最优预算，属于强基线。CaDDTree 无需任何离线调参，直接与 oracle 比较。

## 4. 资源与算力
- **硬件**：8× A800 GPU（未明确具体显存型号，通常为 80GB）。
- **精度**：bfloat16。
- **块大小**：\(L=16\)。
- **推理配置**：最大 2048 个新 token。
- **成本 profiling**：仅需一次离线运行，在网格（上下文长度 0~8k，节点数 1~1024）上测量验证延迟，每次测量 20 次取中位数，草稿成本测量 500 轮。**未明确训练时长**（因为 CaDDTree 无需额外训练，仅 profiling）。

## 5. 实验数量与充分性评估
- **覆盖度**：8 个差异化的基准涵盖数学推理、代码生成、对话指令遵循，能反映方法的泛化能力。
- **消融充分**：成本模型消融（表 2）证实节点数的重要性；参数敏感性（表 3）显示对成本估计不敏感（变化 < 0.07 ms/tok），说明方法鲁棒。
- **可视化分析**：自适应预算分布（图 4）和树结构（图 5）直观解释了为何自适应优于固定预算。
- **局限性**：仅测试了 Qwen3 系列模型（4B/8B），未在更大模型（如 70B）或其他架构（LLaMA、Mistral）上验证；草稿器固定为 DFlash（块扩散），未验证自回归草稿器（如 EAGLE）。

## 6. 主要结论与发现
- **主要结论**：通过直接优化吞吐量并自适应每轮预算，CaDDTree 在几乎所有基准上**匹配或超越**使用最优固定预算的 DDTree，同时**消除了离线预算搜索**。
- **关键发现**：
  - 接受长度单调增长，但吞吐量在中等预算处达到峰值（图 2），说明存在最优预算。
  - 最优预算在不同轮次差异巨大（图 4、5），低置信度轮次需要更大的树，高置信度轮次用窄链即可。
  - 验证成本在长上下文下显著增加，CaDDTree 自动收缩预算。
  - 成本凸性假设在实验中得到支持（图 3，\(R^2 \ge 0.979\)）。

## 7. 方法亮点
- **理论贡献**：证明在凸成本假设下代理吞吐量函数的**单峰性**，为贪心停止规则提供了严格保证。
- **实用性强**：无需超参数调优，仅需一次性离线 profiling，且对成本估计不敏感。
- **直观易懂**：算法 1 基于标准的最大堆扩展，停止条件简单。
- **实验全面**：包含消融、可视化、敏感性分析，论证充分。

## 8. 不足与局限
- **实验范围受限**：仅测试 Qwen3 系列和 DFlash 草稿器，未验证其他语言模型或自回归草稿器（如 EAGLE）。
- **成本模型假设**：验证成本凸性假设在极端情况（如 batch 处理或系统级优化）下可能不成立，需要进一步验证。
- **额外开销**：离线 profiling 仍需要针对每对（模型，硬件）进行一次，虽然是一次性的，但增加了部署成本。
- **搜索空间**：仅考虑节点数，未联合优化树形状（虽然最优形状已由前缀概率排序隐含给出，但未验证是否有更优的非贪心树结构）。
- **长上下文验证**：只测试了最多 8k 上下文，更长上下文（如 32k、128k）下的性能未知。

（完）
