---
title: Teaching Diffusion to Speculate Left-to-Right
title_zh: 教扩散模型从左到右推测
authors: "Lexington Whalen, Yuki Ito, Ryo Sakamoto"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11552v1"
tags: ["query:llm-sd"]
score: 8.0
evidence: 使用扩散语言模型作为草稿模型进行LLM投机解码
tldr: "自回归语言模型推理慢，推测解码用草稿模型并行生成tokens加速。扩散语言模型可并行生成块，但块内双向生成与自回归左到右验证存在不对称。本文提出位置加权、首错焦点损失和链损失三种训练干预，缩小该差距。在多个模型和基准上，接受草稿长度提升21-76%，且不改变推理流程。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11552v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1659, \"height\": 627, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11552v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 726, \"height\": 439, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11552v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 658, \"height\": 420, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11552v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 859, \"height\": 360, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11552v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1552, \"height\": 692, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11552v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1614, \"height\": 728, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11552v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 777, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11552v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 428, \"height\": 245, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11552v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 883, \"height\": 252, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11552v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 805, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11552v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 801, \"height\": 197, \"label\": \"Table\"}]"
motivation: 区块扩散草稿模型训练时双向生成，验证时自回归左到右评估，不对称导致性能次优。
method: 三种训练干预：token位置加权、针对块内首个错误的位置焦点损失、用可微代理替代期望接受长度的链损失。
result: "四个目标模型、六个基准上，接受草稿长度相对位置均匀基线提升21-76%，无额外前向传播。"
conclusion: 三种干预正交可组合，缩小训练验证差距，且与测试时对齐机制兼容。
---

## 摘要
大型语言模型（LLMs）在广泛的任务中表现出色，但其自回归解码过程由于本质上顺序生成令牌而产生了大量推理成本。推测解码通过使用轻量级草稿模型提出多个未来令牌，然后由较大的目标模型并行验证，从而解决了这一瓶颈。最近的研究表明，扩散语言模型非常适合这种设置，因为它们可以并行生成整个草稿令牌块，从而减轻了自回归草稿的顺序约束。这种方式的一个微妙之处在于，块扩散草稿器在块内双向生成令牌，而验证则由自回归目标模型以严格的从左到右方式评估令牌，这导致了对称的训练时目标与非对称的验证时奖励之间存在差距。在这项工作中，我们提供了三种训练时干预措施的实证分析，以缩小这一差距：令牌位置加权、针对每个块内破坏接受前缀的第一个错误位置的焦点损失，以及用可微替代项替代期望接受长度的链损失。这三种干预措施沿正交轴（位置、块条件首错误、联合前缀）起作用，并且可以加性组合；它们同样与测试时对齐机制（如多草稿自选择）正交，原则上可以与它们结合。在四个目标模型和六个推理、代码和对话基准测试中，这三种干预措施在位置均匀基线上将接受的草稿长度提高了每个基准测试的21-76%，而无需增加额外的前向传递，也无需改变推理流程或拒绝采样的精确性契约。

## Abstract
Large language models (LLMs) achieve remarkable performance across a wide range of tasks, but their autoregressive decoding process incurs substantial inference costs due to inherently sequential token generation. Speculative decoding addresses this bottleneck by employing a lightweight draft model to propose multiple future tokens that are subsequently verified in parallel by a larger target model. Recent work has demonstrated that diffusion language models are well suited for this setting, as they can generate entire blocks of draft tokens in parallel and thereby alleviate the sequential constraints of autoregressive drafting. A subtlety of this regime is that block-diffusion drafters generate tokens bidirectionally within a block, whereas verification is performed by an autoregressive target model that evaluates tokens in a strictly left-to-right manner, leaving a gap between the symmetric training-time objective and the asymmetric verification-time reward. In this work, we offer an empirical analysis of three training-time interventions that narrow this gap: token positional weighting, a first-error focal loss that targets the position that breaks the accepted prefix within each block, and a chain loss term that substitutes a differentiable surrogate for the expected accepted length. The three interventions act along orthogonal axes (position, block-conditional first error, joint prefix) and compose additively; they are likewise orthogonal to test-time alignment mechanisms such as multi-draft self-selection, with which they can in principle be combined. Across four target models and six reasoning, code, and dialogue benchmarks, the three interventions raise accepted draft length by 21-76% per benchmark over a position-uniform baseline, without adding additional forward passes and without changing the inference pipeline or the rejection-sampling exactness contract.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

大型语言模型（LLM）的自回归解码因顺序生成令牌而存在严重的推理延迟和成本瓶颈。推测解码通过轻量级草稿模型并行生成候选项，再由目标模型验证，从而加速推理。近期工作（如DFlash）采用**块扩散语言模型**作为草稿器，可一次性并行生成整个K令牌块，理论上比自回归草稿器有更高加速上限。然而，扩散草稿器在块内使用**双向自注意力**训练（所有位置对称），但验证时目标模型采用**严格从左到右**的因果接受规则：一旦早期位置被拒绝，后续所有令牌无论质量如何均被丢弃。这种**训练-验证不对称性**导致草稿器将容量浪费在深层位置上，因为均匀交叉熵损失无法反映位置的因果重要性。本文的核心动机是：通过修改训练损失来弥合这一不对称性，提高草稿器在猜测解码中的实际接受长度。

### 2. 方法论：核心思想、关键技术细节、公式或算法流程

论文提出三种**正交可组合**的训练干预，均在不改变推理流程和精确性契约的前提下作用于损失函数：

- **位置加权（Loss Decay）**：对每个块内位置k（k=1,...,K-1）赋予指数衰减权重 \( w_k(\gamma) = \exp(-\frac{k-1}{\gamma}) \)，使得早期位置获得更大梯度信号，近似匹配验证时几何衰减的接受贡献。这是一种固定先验调度。
  
- **首错焦点损失（First-Error Focal Loss）**：针对每个块中**草稿器argmax解码首个错误的位置**（即链断裂点）添加额外的交叉熵项。该损失仅在块内有错误时激活，且聚焦于单个关键位置，能够自适应地随着草稿器改进而将目标推向更深位置。公式为：
  \[
  L_{fe} = \frac{\sum_{n:E_n\neq\emptyset} w_{k_n^*}(\gamma) \left[-\log q_\psi(x_{n,k_n^*} | H_{<t_n})\right]}{\sum_{n:E_n\neq\emptyset} w_{k_n^*}(\gamma) + \varepsilon}
  \]
  与标准交叉熵相加，权重为 \(\alpha_f\)。

- **链奖励（Chain Reward）**：引入一个可微的期望接受长度代理项 \( R_{chain}(\psi) = \frac{1}{K-1}\sum_{k=1}^{K-1} \exp\left(\sum_{j=1}^k \log p_j\right) \)，其中 \(p_j\) 是草稿器对真实令牌的边际概率。该项直接优化**联合前缀概率**，使每个位置的梯度正比于其前缀接受概率，因而早期位置获得更强梯度。它仅需对已计算的log概率进行累积求和与指数运算，无额外前向传播。

最终训练目标为：\( \mathcal{L} = \mathcal{L}_{CE}^{(\gamma)} + \alpha_f \mathcal{L}_{fe} - \alpha_c R_{chain} \)。三种干预分别沿位置轴、块条件首错误轴、联合前缀轴起作用，可加性组合。

### 3. 实验设计：数据集/场景、基准、对比方法

- **目标模型**：4个指令调优变体：Llama-3.2-3B-Instruct、Meta-Llama-3-8B-Instruct、Qwen3-4B-Instruct、Qwen3-8B-Instruct。
- **草稿器架构**：4层DFlash去噪Transformer，隐藏尺寸4096，GQA配置与目标匹配，块大小K=16（除消融实验外）。
- **训练数据**：
  - 主实验：ShareGPT多轮对话，未目标蒸馏。
  - 附加实验：目标对齐数据（NVIDIA Nemotron V2 + CodeAlpaca，用目标模型重新生成响应）。
- **评估基准**（6个）：GSM8K（数学推理）、MT-Bench（开放对话）、HumanEval（代码生成）、AIME（竞赛数学）、MBPP（代码生成）、LiveCodeBench（代码生成）。主要指标：平均接受长度 \(\bar{\tau}\)。
- **对比方法**：
  - 基线：位置均匀的DFlash（即标准DFlash训练）。
  - 消融：每种干预单独应用、组合应用。
  - 与DDTree（测试时树形验证）结合验证正交性。
  - 与SpecDiff-2（streak-distillation）结合验证兼容性。

### 4. 资源与算力

- **硬件**：8块NVIDIA H100 80GB GPU，单节点，1路张量并行，纯数据并行。
- **优化**：AdamW优化器，余弦学习率调度，预热1.5%，有效批大小32（每个设备微批1，梯度累积4）。
- **训练时长**：训练3个epoch，部分实验报告75,000步检查点。未明确给出总训练时间（小时数），但提及使用SpecForge框架。

### 5. 实验数量与充分性

实验数量丰富且系统，覆盖：

- **学习率消融**（3个水平：1e-4, 1e-3, 1e-2）确定最优LR=1e-3。
- **每种干预单独**：位置加权（γ=7/10/20）、焦点损失（αf=0.3/0.5/1.0）、链奖励（αc=5/10/20/30/40/50）的扫描。
- **组合堆叠**：从均匀基线→+γ→+αf→+αc逐步累积，共5个核心配置（表2）。
- **跨目标模型泛化**：在4个不同模型上重复4个参考配置（表3）。
- **训练数据影响**：在目标对齐数据上重做4个配置（表5）。
- **块大小影响**：在训练块大小16上评估K=2/4/8/16/32（表6）。
- **与DDTree结合**：每个配置与DDTree交叉（表7）。
- **与SpecDiff-2结合**：在SpecDiff-2基线上堆叠三种干预（表8）。
- **每种技术单独对基线**（表4）验证独立性。

实验设计公平客观：控制优化器、批大小、评估协议一致，所有消融均在同一基线上逐步添加。实验覆盖了多个模型族、多种任务类型、不同训练数据分布，充分性较高。

### 6. 主要结论与发现

1. **三种干预各自均为正收益**：单独使用时，链奖励效果最好（τ从2.376提升至2.919），焦点损失次之（2.736），位置加权最弱（2.583），符合它们与验证规则耦合紧密程度的分析。
2. **堆叠效果显著**：完全堆叠配置（γ=10, αf=0.3, αc=40）在Llama-3-8B上达到平均τ=3.420，较均匀基线提升43.9%；在推理和代码基准上提升更大（AIME上+75.9%，HumanEval上+54.7%）。
3. **跨目标模型鲁棒**：四种目标模型上完全堆叠均优于基线，提升幅度20.9%~43.9%。
4. **与训练数据正交**：目标对齐数据本身带来巨大提升（τ从2.376→3.914），但干预仍可叠加提升（达到4.985）。
5. **与测试时机制正交**：DDTree和SpecDiff-2均可与干预组合，获得更大增益（DDTree组合下τ=4.609，TPS提升132.5%）。
6. **链奖励在K=16训练时最有效**：超出训练块大小（K=32）性能下降，表明干预需匹配训练块大小。

### 7. 优点

- **无额外推理开销**：三种干预仅在训练时修改损失，不增加前向传播，不改变推理pipeline，保持精确性契约。
- **正交可组合**：沿不同轴作用，可加性堆叠，且与现有测试时对齐机制（DDTree、SpecDiff-2）完全兼容。
- **方法简单实用**：仅需调整损失计算（加权、焦点、累积求和），无需修改模型架构或增加训练计算量。
- **充分消融**：系统扫描超参数，验证贡献来源，跨模型、数据、块大小、测试策略验证泛化性。
- **针对本质不对称性**：直接优化与验证目标一致的接受长度代理，而非间接启发式。

### 8. 不足与局限

- **实验覆盖有限**：仅测试了DFlash作为实例草稿器，未验证其他块扩散架构（如BLD、TiDAR）；仅使用指令调优模型，未涵盖基座模型或更大规模（>8B）模型。
- **评估仅基于贪心解码**：论文中所有测量均使用argmax（greedy）解码，未评估采样（temperature>0）下的接受长度，而猜测解码通常用于采样设置。
- **训练数据规模较小**：ShareGPT数据集可能多样性不足；目标对齐数据仅约800K样本，且只来自特定领域（代码+对话）。
- **超参数敏感性**：最佳γ、αf、αc需针对每对模型-数据集调优，论文未提供自动化调参指南。
- **未分析长序列场景**：假设块大小固定16，未测试更大块（如64）或极长推理链（如思维链）下的性能。
- **实验客观性良好，但缺乏与更多现有对齐方法（如LK Losses, D-PACE）的直接比较**：论文仅与SpecDiff-2和DDTree进行了结合，未在相同设置下与它们独立对比。

（完）
