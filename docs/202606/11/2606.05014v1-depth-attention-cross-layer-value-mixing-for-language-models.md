---
title: "Depth-Attention: Cross-Layer Value Mixing for Language Models"
title_zh: 深度注意力：语言模型的跨层值混合
authors: "Boyi Zeng, Yiqin Hao, Zitong Wang, Shixiang Song, He Li, Feichen Song, Yifan Liu, Ziwei He, Xinbing Wang, Zhouhan Lin"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05014v1"
tags: ["query:llm"]
score: 9.0
evidence: 注意力中的跨层值混合用于高效LLM推理
tldr: 标准Transformer的残差连接使后期层无法灵活选择早期层表示，现有跨层方法引入额外推理状态。提出Depth-Attention，在注意力模块内用当前层查询关注同位置早期层的键并混合值，复用标准缓存。在1.5B和3B模型上，困惑度最低，下游准确率最高提升2.3点，且无额外推理状态与参数。实现了高效跨层信息选择，不增加缓存大小。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05014v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1693, \"height\": 950}, {\"url\": \"assets/figures/arxiv/2606.05014v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 830, \"height\": 661}, {\"url\": \"assets/figures/arxiv/2606.05014v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 693, \"height\": 607}, {\"url\": \"assets/figures/arxiv/2606.05014v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 494}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05014v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1256}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1726, \"height\": 345}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 876, \"height\": 376}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 740, \"height\": 306}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 980, \"height\": 296}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 978, \"height\": 301}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1649, \"height\": 641}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1726, \"height\": 376}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1647, \"height\": 724}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1665, \"height\": 1061}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1655, \"height\": 659}]"
motivation: 标准Transformer的残差连接使后期层无法选择性重用早期层表示；现有跨层方法引入额外推理状态，成本随现代LLM缓存压缩而增加。
method: 在注意力模块内，当前层查询关注同位置早期层的键，混合其值替换原始值，复用标准注意力查询、键和值缓存槽，无额外参数和持久状态。
result: "在Qwen3风格360M至3B参数解码器上，Depth-Attention达到最低困惑度，下游平均准确率最高提升2.3点，计算量增加不足0.01%，且无额外推理状态。"
conclusion: Depth-Attention通过注意力内跨层值混合，零额外成本提升语言模型性能，适用于多种架构。
---

## 摘要
自注意力在序列中自由选择信息，但在深度上，Transformer仅仅将每一层的输出添加到残差流中，因此后面的层不能选择性地重用前面层的表示。最近的跨层方法改善了这种流动，但在注意力之外对隐藏状态进行操作，在推理时增加了键值缓存之外的状态——随着现代LLM使用分组查询和多头潜在注意力压缩缓存，这种成本变得越来越显著。我们引入了深度注意力（Depth-Attention），它在注意力模块内部执行这种选择：在一层对序列进行注意力之前，它的查询对同一标记位置的前面层的键进行注意力，并将它们的值混合到自注意力随后读取的值中。由于深度注意力重用了标准的注意力查询、键和值缓存槽，将深度混合的值存储在原始值的位置，它不增加参数，也不引入超出标准键值缓存的持久推理状态——与普通解码器相同的缓存大小，且少于基于隐藏状态的跨层方法。在1.5B和3B参数的Qwen3风格解码器上，深度注意力获得了最低的困惑度和最高的平均下游准确度，相比普通Transformer最高提升了2.3个准确点，并在困惑度和平均准确度上超越了强大的跨层基线，同时仅增加不到0.01%的额外算术FLOPs，且没有额外的持久推理状态。这些优势在360M到3B参数范围内成立，并扩展到循环Transformer。

## Abstract
Self-attention selects information freely across the sequence, but across depth, Transformers merely add each layer's output to the residual stream, so later layers cannot selectively reuse earlier-layer representations. Recent cross-layer methods improve this flow but operate on hidden states outside attention, adding state beyond the key-value cache at inference--a cost that becomes increasingly salient as modern LLMs compress the cache with grouped-query and multi-head latent attention. We introduce Depth-Attention, which performs this selection inside the attention module itself: before a layer attends over the sequence, its query attends over the keys of earlier layers at the same token position and mixes their values into the value that self-attention then reads. Because Depth-Attention reuses the standard attention queries, keys, and value-cache slots, storing depth-mixed values in place of the original values, it adds no parameters and introduces no persistent inference state beyond the standard key-value cache--the same cache size as a vanilla decoder and less than hidden-state-based cross-layer methods. On Qwen3-style decoders at 1.5B and 3B parameters, Depth-Attention attains the lowest perplexity and the highest average downstream accuracy, improving over the vanilla Transformer by up to 2.3 accuracy points and surpassing strong cross-layer baselines in perplexity and average accuracy, while adding under 0.01% extra arithmetic FLOPs and no additional persistent inference state. The gains hold from 360M to 3B parameters and extend to looped Transformers.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：标准Transformer的自注意力机制允许在序列维度上灵活选择信息，但在深度维度（跨层）上，信息流动缺乏选择性。残差连接将早期层的输出压缩为单个隐藏状态，导致深层无法直接重用特定早期层的表示。现有跨层方法（如DenseFormer、Attention Residuals、mHC）虽然改善了跨层信息流，但都在隐藏状态空间操作，推理时需要额外存储或重计算隐藏状态，增加了键值缓存之外的内存开销。随着现代LLM通过分组查询注意力（GQA）或多头潜在注意力（MLA）压缩缓存，这种额外状态的相对成本愈发显著。

- **研究动机**：能否在不增加任何推理内存的前提下，让当前层利用已有的键和值来携带跨层信息？论文提出Depth-Attention，将跨层选择操作嵌入注意力模块内部，利用现有查询、键和值缓存槽，实现零额外参数和零持久推理状态的跨层值混合。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在每一层的标准因果自注意力之前，引入一个额外的深度注意力（Depth-Attention）步骤。该步骤在同一token位置上，用当前层的查询去关注当前层及先前层的键，获得深度注意力权重，然后用这些权重混合当前层值和先前层的深度混合值，生成一个全新的深度混合值（depth-mixed value）。这个深度混合值替换原始值缓存，后续的标准自注意力则读取这个混合值，而查询和键保持不变。

- **关键技术细节**：
  - **形式化**：对于第ℓ层、位置t，深度注意力权重为  
    αₜ^{ℓ,j} = softmax_{j≤ℓ} (qₜ^ℓ · kₜ^j / √d)  
    深度混合值：ẽvₜ^ℓ = αₜ^{ℓ,ℓ} vₜ^ℓ + Σ_{j<ℓ} αₜ^{ℓ,j} ẽvₜ^j  
    其中早期层使用的是其自身的深度混合值，从而向上传递累积信息。
  - **实现方式**：深度混合后的值以相同形状替换原始V缓存，不改变缓存大小；查询和键复用自注意力已经计算出的值，不增加参数。
  - **稀疏化策略**：为了避免深度源集随层数增长，采用步长（stride）s，使每层只关注当前层及间隔s层的先前层（如ℓ, ℓ-s, ℓ-2s,…）。默认步长s = L/2（L为总层数）。
  - **GQA适配**：在分组查询注意力中，将每组内的多个查询平均为一个查询，使得深度注意力在键值头分辨率上运行，进一步降低计算和内存开销。
  - **计算效率**：深度注意力复杂度为O((ℓ/s+1)·d_kv)，远小于序列自注意力的O(T²)；增加的总FLOPs不足0.01%。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：使用Pile数据集（约800GB多样文本）进行从头预训练（32B tokens）。下游评估采用8个任务：LAMBADA、PIQA、WinoGrande、SciQ、HellaSwag、ARC-Easy、ARC-Challenge、RACE，评估zero-shot和5-shot准确率。
- **基准架构**：Qwen3风格解码器，规模包括360M、500M、710M、1.5B和3B参数。
- **对比方法**：
  - 基础模型：普通Transformer解码器（vanilla）
  - 跨层基线：manifold hyper-connections (mHC)、Attention Residuals、DenseFormer
  - 消融变体：均匀混合、键混合、不同步长等
- **评估指标**：Pile验证集困惑度（PPL）、下游任务平均准确率、效率指标（FLOPs、训练/推理时间、内存）。

### 4. 资源与算力

- **明确提及**：所有模型在Pile上预训练32B tokens，使用FP16精度，全局batch size 1024，序列长度2048。但论文**未明确说明GPU型号、数量、训练总时长**。仅报告了相对训练速度（如vanilla 3B模型每步4.63秒，Depth-Attention 5.15秒）。硬件信息缺失，可能受限于计算资源（提及“有限的计算资源”）。

### 5. 实验数量与充分性

- **实验分组**：
  - **主要实验**（表1）：1.5B和3B规模，对比5种方法（vanilla、mHC、Attention Residuals、DenseFormer、Depth-Attention），每个规模下zero-shot和5-shot共16个下游任务结果。
  - **效率分析**（表2-4）：理论FLOPs/I/O/额外内存对比，实际训练每步时间，推理总时间和内存。
  - **缩放行为**（图2）：从360M到3B共5个模型大小，比较Depth-Attention与vanilla的验证损失。
  - **循环Transformer实验**（第4.4节）：500M looped Transformer，3个循环，比较加入Depth-Attention前后的验证损失。
  - **注意力权重可视化**（图3）：展示3B模型中层间混合权重分布。
  - **消融实验**（图4、表5、表6）：在500M模型上分别消融步长（首层、半深度、四分之一深度、全源）、混合规则（均匀混合 vs 自适应softmax）、混合对象（仅值、仅键、键值混合）。
- **充分性与公平性**：
  - 基线使用了推荐/常用配置（如DenseFormer用full 1×1，Attention Residuals用Block AttnRes，mHC用4流），且所有方法使用相同基础架构、数据、训练超参数。
  - 消融实验控制了单一变量，证明设计选择合理。
  - 缺点：所有方法均使用相同的非融合PyTorch实现，未使用自定义CUDA核，因此绝对效率低于最优实现，但相对比较公平。

### 6. 论文的主要结论与发现

- **性能**：Depth-Attention在所有规模（360M~3B）下均取得最低PPL和最高平均下游准确率；在1.5B和3B上相比vanilla最高提升2.3个准确点，并全面超越所有跨层基线。
- **效率**：额外FLOPs不足0.01%，推理时间仅增加1.18%，预fill峰值内存不变（30.6 GB），不增加任何持久推理状态；训练每步开销也比其他基线小得多。
- **适应性**：效果在循环Transformer（参数共享）中同样成立。
- **消融发现**：半深度步长最优；自适应softmax混合优于均匀混合；仅混合值是最佳选择，混合键会损害性能。

### 7. 优点：方法或实验设计上的亮点

- **零额外推理状态**：深度混合值直接替换标准V缓存，不增加KV缓存大小，与压缩缓存技术（GQA、MLA）天然兼容。
- **极低计算开销**：深度注意力复杂度与层数线性相关，但步长策略使其远小于序列自注意力；即使非融合实现，相对开销也远低于其他跨层方法。
- **无参数增加**：复用现有Q/K/V，不引入可学习参数，易于集成到现有Transformer中。
- **实验设计严谨**：包含多种规模、多种基线、详细消融、循环Transformer验证，且尽可能公平对比（相同训练协议、基线推荐配置）。
- **效率分析详尽**：不仅给出理论FLOPs，还测量实际训练/推理时间和内存，并解释理论与实际差异原因。

### 8. 不足与局限

- **实现未融合**：当前Depth-Attention未集成到注意力核中，导致实测训练开销（+9%~11%）大于理论预期（<0.01%）；若定制CUDA核可进一步缩小差距。
- **规模验证有限**：仅验证到3B参数和32B训练tokens，更大规模（如70B+）和更多训练数据下的表现尚未确认（受计算资源限制）。
- **固定步长源选择**：深度源集使用固定步长，未学习源选择；虽然消融表明半深度最好，但可能存在更优的自适应选择策略。
- **仅混合值**：当前仅更新值状态，未探索键或更多跨层交互形式（消融表明键混合有害，但还有其他可能性）。
- **内存开销分析简化**：理论上额外内存为0，但实际中因非融合实现仍有少量临时激活内存；但结论中强调“无额外持久推理状态”是准确的。
- **实验报告缺失硬件细节**：未说明GPU型号和数量，影响可复现性。

（完）
