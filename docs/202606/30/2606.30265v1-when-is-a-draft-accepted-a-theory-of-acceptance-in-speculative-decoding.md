---
title: When Is a Draft Accepted? A Theory of Acceptance in Speculative Decoding
title_zh: 草案何时被接受？投机解码中的接受理论
authors: Aaryam Sharma
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30265v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 投机解码接受理论涵盖宽松验证
tldr: 投机解码常用贪心解码与放松接受规则，但缺乏理论分析。本文将拒绝区域建模为目标分布低水平集，导出精确KL散度条件与贪心、放松、top-m等准则的边界界，并扩展至树形解码。在Qwen3上验证，放松和树形准则显著扩大认证接受区域，尤其低边际步骤。该工作为实际推理系统提供了确定性接受事件的严格证书。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30265v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1691, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30265v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1665, \"height\": 657, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30265v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1655, \"height\": 703, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30265v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1624, \"height\": 676, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30265v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1684, \"height\": 722, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30265v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 710, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1106, \"height\": 357, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1487, \"height\": 517, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 465, \"height\": 378, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1240, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 449, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1305, \"height\": 535, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30265v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1302, \"height\": 535, \"label\": \"Table\"}]"
motivation: 现有投机解码理论多针对分布保持采样，缺乏对贪心解码与放松接受规则的精确分析。
method: 将拒绝区域表征为目标分布的低水平集，推导KL散度条件及多种接受准则的边界界，并扩展至树形解码。
result: 在Qwen3模型上，放松和树形接受准则大幅提升认证接受率，尤其低目标分布边际的步骤。
conclusion: 本文从确定性视角补充了投机解码理论，为实际系统提供可证明的接受保证。
---

## 摘要
投机解码通过使用快速起草者提出候选令牌，再由更大的目标模型验证，从而加速语言模型推理。现有理论主要研究随机、分布保持的设置，其目标是从目标分布中精确采样。相比之下，许多实际系统使用贪婪解码、宽松的接受规则或基于树的候选集，其成功由局部排序和阈值事件决定，而非精确的分布等价性。我们为这些场景发展了一套理论。我们识别出许多常见接受标准的拒绝区域可被刻画为目标分布的低水平集。对于这些，我们刻画了拒绝所需的精确KL散度，从而为严格贪婪解码、加性和乘性宽松接受、top-(m)宽松准则以及熵阈值接受提供精确证书和基于边际的尖锐界。然后我们将该框架扩展到贪婪树解码，推导出目标贪婪令牌仍被起草者top-(m)候选覆盖的精确和仅边际证书。最后，我们在Qwen3模型上评估所得证书，表明宽松和基于树的标准显著扩大了认证接受区域，尤其在目标模型分布边际较低的解码步骤中。这些结果通过刻画实际推理系统中常见的确定性局部接受事件，补充了投机解码的现有分布保持分析。

## Abstract
Speculative decoding accelerates language model inference by using a fast drafter to propose candidate tokens that are then verified by a larger target model. Existing theory largely studies the stochastic, distribution-preserving setting, where the goal is to exactly sample from the target distribution. In contrast, many practical systems use greedy decoding, relaxed acceptance rules, or tree-based candidate sets, where success is governed by local ranking and threshold events rather than exact distributional equality. We develop a theory for these regimes. We identify that many common acceptance criteria have rejection regions that can be characterized as lower level sets of the target distribution. For these, we characterize the exact KL divergence required for rejection yielding exact certificates and sharp margin-based bounds for strict greedy decoding, additive and multiplicative relaxed acceptance, top-(m) relaxed criteria, and entropy-thresholded acceptance. We then extend the framework to greedy tree decoding, deriving exact and margin-only certificates for when the target greedy token remains covered by the drafter's top-(m) candidates. Finally, we evaluate the resulting certificates on Qwen3 models, showing that relaxed and tree-based criteria substantially enlarge the region of certified acceptance, especially on decoding steps with low target model distribution margin. These results complement existing distribution-preserving analyses of speculative decoding by characterizing the deterministic local acceptance events common in practical inference systems.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：投机解码通过一个快速起草模型提出候选令牌，再由大型目标模型并行验证，从而加速自回归语言模型推理。现有理论主要关注**分布保持的随机采样设置**（如投机采样），即精确地从目标分布中采样。然而，实际系统大量使用**贪婪解码、松弛接受规则（如加性/乘性容差）以及树形候选集**，这些场景的成功依赖于局部排序和阈值事件，而非精确的分布等价性。目前缺乏对这些确定性、局部接受事件的严格理论分析。
- **核心问题**：对于贪婪解码、松弛贪婪解码、Medusa风格的熵阈值接受、以及树形贪婪解码变体，**在起草分布与目标分布之间的KL散度下，保证被接受的条件是什么？** 即，需要多大的KL散度才能使起草的令牌被拒绝？本文旨在给出精确的KL证书和可解释的边界。
- **整体含义**：将投机解码中的接受问题转化为**目标分布低水平集的稳定性问题**，为实际推理系统提供可证明的局部接受保证，补充了现有以随机采样为中心的理论。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  1. **拒绝区域是目标分布的低水平集**：许多常见接受准则（贪婪、加性/乘性松弛、top-m、熵阈值）的拒绝区域均可表示为 $R = \{v \in V \setminus \{x_0\} : p(v) \le c\}$，其中 $c$ 是一个依赖于 $p$ 的阈值。
  2. **单令牌接受问题的精确KL证书**：对于给定的拒绝区域 $R$，最小KL散度使得存在某个 $q$ 的众数落在 $R$ 内（即可能被拒绝）定义为 $R_p(R) = \inf\{KL(p\|q): \arg\max_v q(v) \cap R \ne \emptyset\}$。通过KKT条件，作者推导出 $R_p(R) = s \cdot KL(p|_A / s \,\|\, \text{Unif}(A))$，其中 $A$ 是“活动集”，$s$是 $A$ 上的概率和。如果 $KL(p\|q) < R_p(R)$，则保证接受。
  3. **两令牌放松下界**：证明 $R_p(R) \ge G(p(x_0), p(x^*)) = (p(x_0)+p(x^*))g\big(\frac{p(x_0)-p(x^*)}{p(x_0)+p(x^*)}\big)$，其中 $x^*$ 是拒绝区域中最可能的令牌。这给出了一个仅依赖于目标模型边际 $\gamma_p = p(x_0)-p(x_1)$ 或松弛参数 $t$、$\alpha$ 的简洁下界。
  4. **通用上界**：任何单令牌准则的证书 $R_p(R) \le \frac{2}{k}\log 2 \le \log 2$，其中 $k\ge2$ 为活动集大小；树形解码的证书上界为 $\log(m+1)$。
  5. **树形贪婪解码**：考虑每层提出 top-m 令牌，目标令牌 $x_0$ 需被覆盖。导出精确证书 $R_p^{\text{tree}}(m) = s\cdot KL(p|_A/s\|\text{Unif}(A))$，并用 Donsker-Varadhan 变分表示得到基于树边际 $\gamma_m = p(x_0)-p(x_{(m)})$ 的下界 $B_m(\gamma_m)$。当 $\gamma\to0$，$B_m(\gamma) \approx \frac{m+1}{4}\gamma^2$，相比贪婪的 $\frac{1}{2}\gamma^2$ 有 $\sqrt{(m+1)/2}$ 倍改进。

- **关键技术细节**：
  - **KKT条件**：用于求解极小化 $KL(p\|q)$ 受约束众数在 $R$ 内的凸优化问题，得到 $q^*$ 的封闭形式。
  - **引理4.3（交换引理）**：$M(x_1)$ 随 $p(x_1)$ 非增，因此 $R_p(R)$ 由 $R$ 中最可能令牌 $x^*$ 决定。
  - **两令牌下界**：放松除 $x_0$ 与 $x^*$ 外的所有众数约束，得到下界 $G(p(x_0),p(x^*))$。
  - **Donsker-Varadhan 表示**：用于树形解码下界的推导，通过构造特定函数 $h$ 并优化参数 $\tau$。
  - **通用上界**：利用活动集概率分布的凸性，最大化 $\log k - H(\nu)$ 在约束多面体 Ω_k 上，得到 $\frac{2}{k}\log2$。

- **算法/流程**：作者主要给出的是接受准则的数学刻画，而非具体算法。文中展示了标准贪婪解码（算法4）、投机采样（算法3）的流程，作为背景。方法论核心是分析这些准则的拒绝区域并计算KL下界。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集**：UltraChat 200k 数据集（Ding et al., 2023）。
- **模型**：Qwen3-1.7B 和 Qwen3-4B（Yang et al., 2025）。
- **实验设置**：从数据集中随机选择 500 个至少 64 个 prompt 令牌的提示，每个提示生成 128 步，存储并分析每一步的目标模型分布。总计超过 63000 个解码步骤。
- **对比的接受准则**：
  - 严格贪婪解码
  - 加性松弛（t = 0.1, 0.3 等）
  - 乘性松弛（α = 0.5, 0.1 等）
  - Medusa 熵阈值接受（ε₀=0.1, δ₀=0.09）
  - 树形贪婪解码（m = 2,4,8）
  - 混合 top-m 加性松弛（m=2,4,6,8 与 t=0.01~0.20）
- **评测指标**：
  1. **KL 证书分布**：每个准则在各解码步骤的精确证书值的均值、中位数、5%和25%分位数。
  2. **证书累积分布函数（CDF）**：展示证书大于给定 ε 的步骤比例。
  3. **可认证接受长度**：假定每一步 $KL(p,q) < \epsilon$，计算连续被接受步骤的平均长度（模拟实际推理中的接受长度）。

## 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

- **未明确说明**：论文是理论驱动型工作，实验部分仅涉及在预训练好的 Qwen3 模型上运行推理以收集分布统计数据，**没有提及任何训练过程或具体的 GPU 型号、数量、训练时长**。推测实验可在单张或少量 GPU 上完成，但作者未给出硬件细节。

## 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

- **实验数量**：
  - 证书分布统计（表2，图1-5）：覆盖了10种以上的接受准则变体，计算了多个分位数和CDF曲线。
  - 可认证接受长度（表5，表6）：针对两个模型，8种准则，8个ε值（0.01至0.69），共128个平均长度值。
  - 阈值敏感性分析（表3，表4）：对加性 t 从0.01到0.5、乘性 α 从0.9到0.1、混合 top-m 加性 t 进行了 5th 分位数统计。
- **充分性评价**：
  - **优点**：覆盖了论文推导的所有主要准则，数据量较大（>63000步），统计可信。对两个不同规模的模型做了验证，增强泛化性。
  - **不足**：
    1. **仅使用一种数据集（UltraChat）**，未测试其他领域（如代码、数学、长文档）的分布特性。
    2. **仅使用 Qwen3 系列**，未在其他架构（如 LLaMA、Mistral、GPT）上验证，可能存在模型特异性。
    3. **无真实起草者参与**：实验是统计目标模型本身的分布性质，假设 KL 界已知，未实际训练或运行一个起草模型来验证证书在真实推理中的预测能力。
    4. **无比较基线**：没有对比文献中其他理论（如 SpecTr、Yin et al. 的马尔可夫链分析）的预测结果。
  - **公平性**：统计方法客观，报告了分位数，避免了均值偏差。但对真实接受长度假设每一步 $KL(p,q) < \epsilon$ 是强假设，实际起草者难以同时满足，因此结果可视为**理想化上界**。

## 6. 论文的主要结论与发现

1. **统一证书框架**：任何拒绝区域为目标分布低水平集的接受准则，其精确 KL 证书可由一个活动集上的 KL 散度闭式给出。
2. **两令牌下界的普适性**：证书的下界主要取决于目标模型顶级令牌之间的边际（单令牌时）或树形边际（树形）以及松弛参数，具有 $O(\gamma^2)$ 或 $O(\gamma_m^2)$ 的渐近率。
3. **放松准则的有效性**：加性松弛用可控参数 $t$ 替代不可控的目标边际，乘性松弛根据模型置信度自适应；两者均显著提升低边际步骤的证书值。
4. **树形解码的收益**：分支因子 $m$ 将边际要求从 $\sqrt{2\epsilon}$ 降至 $\sqrt{4\epsilon/(m+1)}$，上界从 $\log 2$ 升至 $\log(m+1)$，实验显示 $m=8$ 的中位证书超过 2.0（近乎贪婪的4倍）。
5. **实证验证**：Qwen3 模型在多数步骤具有高置信度（中位 top-1 概率>0.97），但存在低边际长尾；放松和树形准则在该尾部大幅提升认证接受率，可认证接受长度在 $KL<0.1$ 时可达 5~40+ 步（取决于准则）。

## 7. 优点：方法或实验设计上有哪些亮点

- **理论贡献**：
  - 首次系统建立**确定性局部接受事件的 KL 证书理论**，填补了分布保持分析之外的空白。
  - **精确证书 + 可解释下界 + 紧上界**相结合，既给出闭式公式，又给出简洁的边际/参数函数，便于实践应用。
  - 使用 **KKT 条件**和 **Donsker-Varadhan 变分表示**等工具，证明严谨，构造的反例证明边界紧性。
- **实验设计**：
  - 统计了**大规模真实分布**（>63000步），而非合成数据，结果可靠。
  - **多层次展示**：证书分布、CDF、可认证接受长度，直观展示不同准则的差异。
  - **敏感性分析**：系统变化松弛参数，揭示其对证书影响的非线性规律。
  - **树形解码分析**：从单令牌自然扩展到多令牌树，提供统一视图。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **理论局限**：
  - 假设目标分布**严格正**（$p(v)>0$），虽然可扩展至零支持情形，但未显式处理。
  - 接受准则是**确定的**（如 argmax 取法），实际系统中可能存在**随机重采样或近似 argmax**，未覆盖。
  - 证书是**局部的**（单步前缀），未建模多步自回归中起草-验证交互如何改变后续分布（作者在结论中承认这是个开放方向）。
- **实验局限**：
  - **无实际起草者验证**：证书仅在目标分布上统计，未训练/使用任何具体起草模型（如 n-gram、EAGLE、Medusa）来检验 KL 界约束下真实接受率是否与预测一致。
  - **单一数据集、单一模型系列**：可能无法代表所有 LLM 推理场景。
  - **可认证接受长度假设每一步 $KL<\epsilon$** 过于理想，真实场景中 KL 随上下文变化，起草者未必能持续满足该条件。
- **偏差风险**：论文未讨论放松准则对**文本质量**的影响（如改变输出分布），仅关注接受率。过大的松弛可能导致生成退化。

**（完）**
