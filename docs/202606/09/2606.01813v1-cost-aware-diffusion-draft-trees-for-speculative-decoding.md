---
title: Cost-Aware Diffusion Draft Trees for Speculative Decoding
title_zh: 用于推测解码的成本感知扩散草稿树
authors: "Shuai Zhang, Huachuan Qiu, Hongliang He, Yong Dai"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01813v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 面向LLM的代价感知扩散草案树投机解码
tldr: 投机解码中，草稿模型生成候选令牌由目标模型并行验证以加速推理。块扩散草稿器如DFlash生成完整草稿块，DDTree构建候选树最大化预期接受长度，但接受长度随节点预算单调不减，未考虑验证成本。本文提出CaDDTree，联合选择树结构和节点预算，直接优化令牌吞吐量。通过建模草案和验证延迟，将吞吐量目标分解为对预算的一维搜索，并证明在凸验证成本下吞吐量函数是单峰的，从而设计高效贪心停止规则。实验表明CaDDTree在多个任务上匹配或超越带最优预算的DDTree。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01813v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1644, \"height\": 555}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 781, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 792, \"height\": 524}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1625, \"height\": 601}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1649, \"height\": 619}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01813v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1663, \"height\": 826}, {\"url\": \"assets/tables/arxiv/2606.01813v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 746, \"height\": 179}, {\"url\": \"assets/tables/arxiv/2606.01813v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 811, \"height\": 143}, {\"url\": \"assets/tables/arxiv/2606.01813v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1664, \"height\": 829}]"
motivation: 现有方法DDTree在固定节点预算下最大化预期接受长度，但接受长度随预算增加不减，无法合理选择预算以优化吞吐量。
method: CaDDTree通过联合优化树结构和节点预算，直接最大化预期令牌吞吐量，利用凸验证成本下吞吐量的单峰性实现贪心预算停止。
result: 在Qwen3-4B和Qwen3-8B上的八项基准测试中，CaDDTree在几乎所有任务上匹配或超越使用oracle预算选择的DDTree。
conclusion: CaDDTree无需离线预算搜索，每轮自适应调整预算，更高效地提升推理速度。
---

## 摘要
推测解码通过让轻量级草稿模型生成候选词，并由目标语言模型并行验证，从而加速推理。诸如DFlash等块扩散草稿模型一次性生成整个草稿块，得到每个位置边缘分布；DDTree利用这些分布构建候选树，在固定节点预算下最大化期望接受长度。然而我们观察到，接受长度随预算增加而非递减：它总是偏好更大的树，而不考虑验证成本，这为预算选择提供了无原则的依据。我们提出CaDDTree（成本感知扩散草稿树），该方法通过联合选择树结构和节点预算，直接优化令牌吞吐量（单位时间内期望生成的令牌数）。我们显式建模草稿和验证延迟，证明吞吐量目标可分解为每轮预算上的一维搜索，并证明在凸验证成本下，吞吐量函数是单峰的，从而能够采用高效的贪心停止规则。CaDDTree无需离线预算搜索，每轮根据当前逐位置分布和验证成本自适应调整预算。在Qwen3-4B和Qwen3-8B上的实验覆盖了推理、编程和指令遵循等八项基准测试，结果表明，在几乎所有任务上，CaDDTree在匹配或超过使用最优预算选择的DDTree方面表现优异。

## Abstract
Speculative decoding accelerates inference by having a lightweight drafter propose tokens verified in parallel by the target language model. Block diffusion drafters such as DFlash generate an entire draft block in one pass, yielding per-position marginals; DDTree uses these to build a candidate tree that maximizes expected acceptance length under a fixed node budget. We observe, however, that acceptance length is non-decreasing in budget: it always favors larger trees regardless of verification cost, offering no principled basis for budget selection. We introduce \textbf{CaDDTree} (Cost-aware Diffusion Draft Tree), a method that directly optimizes token throughput (expected tokens generated per unit time) by jointly selecting the tree structure and node budget. We model draft and verification latencies explicitly, show that the throughput objective decomposes into a per-round one-dimensional search over the budget, and prove that under a convex verification cost the throughput function is \emph{unimodal}, enabling an efficient greedy stopping rule. CaDDTree requires no offline budget search, adapting the budget each round from the current per-position distributions and verification cost. Experiments on Qwen3-4B and Qwen3-8B across eight benchmarks spanning reasoning, coding, and instruction-following tasks show that \caDDTree{} matches or surpasses DDTree with oracle budget selection on nearly all tasks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义
- **背景**：推测解码通过轻量级草稿模型生成候选词，由目标语言模型并行验证，以加速推理。块扩散草稿器（如 DFlash）可一次性生成整个草稿块，得到每个位置边缘分布。DDTree 利用这些分布构建候选树，在固定节点预算下最大化期望接受长度。
- **问题**：接受长度随节点预算增加而单调不减，总是偏好更大的树，而不考虑验证成本，因此无法为预算选择提供合理原则。现有方法固定预算离线，无法适应每轮解码的变化。
- **意义**：本文提出以吞吐量（单位时间生成令牌数）为优化目标，直接建模草稿和验证延迟，联合选择树结构和预算，实现自适应预算选择，无需离线调参。

## 2. 方法论
### 核心思想
- 替代固定预算下的接受长度最大化，直接优化令牌吞吐量 `ˆθ(T; ℓ) = (1+Φ(T)) / (Cd + Cv(|T|; ℓ))`，其中 `Cd` 为草稿成本，`Cv` 为验证成本，`Φ(T)` 为期望接受令牌数（基于草稿模型的因子化边缘分布）。
- 通过 **结构-预算分解**（Proposition 1）：吞吐量最优的树是给定预算下贪婪最优树（按前缀概率排序选前 n 个节点），因此问题简化为搜索最优预算 `n*`。
- **单峰性**（Theorem 3）：在验证成本随节点数凸增假设下，吞吐量函数 `ˆθ*(n)` 是单峰的，因此可采用贪心停止规则：遍历节点（按前缀概率降序），一旦加入下一个节点导致吞吐量下降则停止，此时得到全局最优预算和树结构。
- **验证成本建模**：通过离线在不同上下文长度和节点数上测量验证延迟，拟合凸回归函数，在线通过线性插值估计当前上下文长度的验证成本。

### 算法流程（Algorithm 1）
- 输入：逐位置 top-K 分布、当前上下文长度、草稿成本、验证成本函数。
- 初始化空树、堆为空、当前吞吐量阈值。
- 维护一个最大堆（按 log 前缀概率），每次弹出概率最大的节点。
- 检查加入该节点后吞吐量是否增大，若否，则停止；否则加入树，更新指数，并压入下一个兄弟节点和第一个子节点。
- 输出：吞吐量最优的树（大小自适应）。

## 3. 实验设计
### 数据集与场景
- 八项基准测试：**MATH-500、GSM8K、AIME 2025**（数学推理）、**HumanEval、MBPP、LiveCodeBench**（代码生成）、**MT-Bench、Alpaca**（指令跟随）。
- 温度设置：0.0（贪心）和 1.0（采样）。

### 对比方法
- **AR**：标准自回归生成（无推测解码）。
- **DFlash**：块扩散推测解码单序列草稿。
- **DDTree-oracle**：DDTree 使用每（数据集、模型、温度）的最佳预算（通过网格搜索 {16,32,64,128,256,512,1024} 确定）。
- **CaDDTree**（本文方法）。

### 硬件与参数
- 8 张 A800 GPU，块大小 L=16，bfloat16 精度，最大新令牌数 2048。
- 成本参数基于同一硬件离线测量。

## 4. 资源与算力
- 文中明确说明：所有实验运行在 **8×A800 GPU** 上。
- 训练时长：未提及具体训练时间，但方法无需额外训练，仅需一次离线成本剖析（profiling），在 9 个上下文长度 × 最多 1024 个节点上的测量，每次 20 次试运行取中位数，总时间在可接受范围内（秒级求解凸回归）。
- 推理基准测试：每个方法在每个数据集上运行 128 个示例（除 AIME 2025 30 个、HumanEval 164 个、MT-Bench 160 个），使用经过一次预热运行后的时间。

## 5. 实验数量与充分性
- **主要实验结果**（表 1）：两个模型（Qwen3-4B, Qwen3-8B）× 温度 0.0 × 8 个数据集 = 16 组对比。每组报告了每令牌生成时间和平均接受长度。
- **温度 1.0 结果**（附录 C）：同样 16 组扩展，共 32 组主要结果，对比稳定。
- **消融实验**：
  - **成本模型消融**（表 2）：对比常数验证成本模型，显示凸回归的必要性。
  - **成本参数敏感性**（表 3）：对草稿成本精度不敏感，证明方法鲁棒。
  - **自适应预算分析**（图 4、图 5）：展示每轮预算分布和树结构示例，直观验证适应性。
- **验证成本建模**（图 3）：九个上下文长度下的验证延迟与节点数的关系，拟合凸函数，R²≥0.979。
- **充分性**：实验覆盖多种任务类型和模型大小，结果统计可靠。对比 baseline 使用了 oracle 最佳预算（DDTree-oracle），公平性高。
- **客观性**：采用了标准 benchmark，每个方法按相同 token 数或时间测量，有预热、中位数等避免偏差。

## 6. 主要结论与发现
- CaDDTree 在几乎所有任务上匹配或超越使用 oracle 预算的 DDTree，而无需离线调参。
- 吞吐量最优预算通常远小于接受长度最大化预算（图 2 证实），验证了直接优化吞吐量的必要性。
- 每轮最优预算随草稿模型置信度大幅变化（图 1、图 4），固定预算无法自适应，CaDDTree 可自动调整。
- 验证成本凸性假设在真实硬件上成立（图 3），使得贪心停止规则有效。
- 方法对成本参数扰动不敏感（表 3），实用性强。

## 7. 优点
- **理论贡献**：证明了吞吐量函数在凸验证成本下具有单峰性，从而设计出高效贪心算法，无需网格搜索或额外训练。
- **实用性**：一次离线成本剖析即可适用所有任务和模型；运行时仅需一次前向计算和堆操作，计算开销低。
- **自适应性**：每轮根据草稿模型置信度和上下文长度自动调整树大小，优于固定预算方法。
- **实验充分**：覆盖多个任务、两种温度、两个模型，并做了消融和敏感性分析。

## 8. 不足与局限
- **实验覆盖**：仅测试了 Qwen3-4B/8B 与 DFlash，未推广到其他草稿模型（如 EAGLE）或更大目标模型（如 70B+），也未说明在非 GPU 架构（如 TPU）上的效果。
- **验证成本假设**：凸性假设虽在测量中成立，但可能因注意力实现（如 FlashAttention 支持变化）而改变，方法对特定硬件配置敏感。
- **应用限制**：方法依赖于块扩散草稿器提供边缘分布，不适用于自回归草稿器（除非改造）。同时仅适用于贪心或拒绝采样验证，不覆盖采样式推测解码的复杂情况。
- **内存与延迟**：大规模树（如 1024 节点）可能增加 KV 缓存和显存压力，文中未详细分析显存消耗。
- **论文未提及偏差风险**：数据集并非全部平衡，但这是通用推理模型常见问题。

（完）
