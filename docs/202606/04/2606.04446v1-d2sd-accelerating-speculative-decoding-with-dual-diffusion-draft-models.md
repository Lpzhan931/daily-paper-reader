---
title: "D^2SD: Accelerating Speculative Decoding with Dual Diffusion Draft Models"
title_zh: "D^2SD: 利用双重扩散草稿模型加速推测解码"
authors: "Liyuan Zhang, Jiarui Zhang, Jinwei Yao, Ran Yan, Yuchen Yang, Jiahao Zhang, Tongkai Yang, Yi Wu, Binhang Yuan"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04446v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 双扩散草稿模型加速投机解码
tldr: 针对投机解码中扩散草稿模型仅生成单一序列导致接受率低的问题，提出D^2SD框架。第一扩散模型生成令牌块并输出置信度，据此构建前缀树并识别最可能拒绝边界，选取前K个前缀范围；第二扩散模型以这些前缀为锚批量生成替代续写，通过级联注意力联合验证共享前缀候选。实验显示，D^2SD在多个基准上显著优于原扩散法和强自回归投机解码基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04446v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1502, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04446v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1719, \"height\": 407, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04446v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 813, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 639, \"height\": 237, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1721, \"height\": 588, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1721, \"height\": 586, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 907, \"height\": 345, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 903, \"height\": 321, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 671, \"height\": 341, \"label\": \"Table\"}]"
motivation: 现有扩散草稿模型每验证步仅生成一个候选序列，首个令牌被拒后后续全部丢弃，接受率受限。
method: 采用双扩散草稿框架：首先生成令牌块及置信度，构建前缀树并选取可能拒绝的前缀范围；再以这些前缀为锚批量生成替代续写，通过级联注意力联合验证。
result: 在多个基准任务上，D^2SD相比原扩散方法和强自回归投机解码基线均有显著提升。
conclusion: D^2SD通过双扩散草稿模型和级联验证机制有效提高了投机解码的接受率和效率。
---

## 摘要
推测解码通过草拟多个token并在单个目标模型前向传播中验证它们，从而加速自回归大语言模型推理。近期基于扩散的草稿模型并行生成整个token块，但通常每次验证只提交一个草稿序列：一旦发生首次不匹配，所有后续草稿token即被丢弃，导致接受率有限。简单地对更多草稿候选序列进行批处理只会带来边际改善，因为冗余或位置不当的分支增加了草拟和验证的成本，而未成比例地增加接受的token数量。我们提出D^2SD，一种双重扩散草稿推测解码框架，它将候选序列组织成置信度引导的前缀树：第一个扩散草稿模型生成一个块及其逐位置置信度分数，用于识别最可能的拒绝边界并选择前K个前缀范围进行恢复；第二个可变前缀扩散草稿模型在每个选定前缀处重新锚定，并在一次批处理传递中提出替代续写；由此产生的共享前缀候选通过级联注意力进行联合验证。实验表明，D^2SD在底层扩散方法和强自回归推测解码基线上均有明显改进。

## Abstract
Speculative decoding accelerates autoregressive large language model inference by drafting multiple tokens and verifying them in a single target-model forward pass. Recent diffusion-based drafters generate an entire block of tokens in parallel but usually commit to a single draft sequence per verification: once the first mismatch occurs, all subsequent draft tokens are discarded, resulting in a limited acceptance rate. Naively batching more draft candidate sequences only introduces a marginal improvement, as redundant or poorly placed branches increase the cost of drafting and verification without proportionally increasing the number of accepted tokens.   We propose D^2SD, a dual diffusion draft speculative decoding framework that organizes candidates into a confidence-guided prefix tree, where the first diffusion drafter generates a block along with per-position confidence scores that are used to identify the most likely rejection boundary and select the top-K prefix ranges for recovery; the second variable-prefix diffusion drafter re-anchors at each selected prefix and proposes alternative continuations in one batched pass; the resulting shared-prefix candidates are jointly verified via cascade attention. Empirically, D^2SD shows clear improvements over both the underlying diffusion approach and strong autoregressive speculative decoding baselines.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：推测解码（Speculative Decoding）中，现有扩散草稿模型（如DFlash）每验证步仅生成一个候选序列，采用“最长正确前缀”规则——首个不匹配便丢弃所有后续token，导致接受率受限。简单增加块大小（γ）或对同一草稿模型进行多次采样（K个分支）效果有限，前者出现“扩展墙”（diminishing returns），后者因误差同质性（error homogeneity）仅带来边际改进。

- **整体含义**：需要一种更智能的草稿组织方式，在不显著增加推理开销的前提下，提高每个验证周期的接受长度（α）和端到端加速比。论文提出D^2SD框架，通过双扩散草稿模型构建置信度引导的前缀树，在早期错误位置进行定向恢复，从而突破单一序列的局限。

## 2. 论文提出的方法论

### 核心思想
利用第一个扩散草稿模型（DFlash）生成token块的同时输出逐位置置信度，据此估计最可能的拒绝边界，选取前K个前缀长度作为“重新锚定”点；第二个可变前缀扩散草稿模型（VP-Drafter）在这些前缀处批量生成替代续写，形成共享前缀候选集；最后通过级联注意力（cascade attention）由目标模型联合验证所有候选，提交最长接受前缀。

### 关键技术细节
1. **第一阶段：DFlash首轮草稿**  
   - 输入包含锚定token（上一轮验证的奖励token）和γ-1个[MASK]位置，在目标模型隐藏特征注入下并行解码所有掩码位置，得到候选token序列及逐位置置信度ck = max_v p_k(v)。

2. **拒绝边界估计**  
   - 假设条件独立，计算边界后验概率：  
     r(i) = (∏_{k=1}^{i} c_k) × (1 - c_{i+1})，i = 0,...,γ-2。  
   - 选择Top-K个最大r(i)对应的前缀长度i。

3. **第二阶段：VP-Drafter批量重草稿**  
   - 对每个选定的前缀i，保留锚定及前i个DFlash token，其余位置重新掩码；K个分支组成一个batch，由VP-Drafter并行生成续写。  
   - VP-Drafter架构与DFlash相同，但训练时使用可变的可见前缀长度（按截断几何分布采样），并采用指数衰减的交叉熵损失（权重wk = exp(-(k-l-1)/τ)），强调靠近锚定的错误。

4. **联合验证**  
   - 目标模型对K+1个候选（原始DFlash + K个重草稿分支）执行级联注意力验证，通过共享前缀KV缓存避免重复计算，最终提交所有候选中的最长接受前缀，并将目标模型在拒绝位置的预测作为下一轮锚定。

### 公式/算法流程（文字说明）
- 每解码周期：  
  (i) 第一阶段草稿 → (ii) 计算r(i)并选Top-K → (iii) 构建可变前缀batch → (iv) VP-Drafter批量生成 → (v) 目标模型联合验证，接受最长前缀。

## 3. 实验设计

### 数据集/场景
- **数学**：GSM8K、MATH、AIME25  
- **代码**：HumanEval、MBPP、LiveCodeBench  
- **聊天**：MT-Bench、Alpaca  
共8个任务，覆盖三类主流场景。

### Benchmark / 对比方法
- 目标模型：Qwen3-8B 和 GPT-OSS-20B  
- 对比基线：  
  - DFlash（单扩散草稿基线）  
  - EAGLE-3（强自回归推测解码基线）  
- 默认配置：γ=16，K=4，贪心解码（T=0）和采样解码（T=1）均报告。

### 指标
- 平均接受长度α（每个解码周期接受的token数）  
- 端到端加速比（相对自回归解码的加速倍数）

## 4. 资源与算力

- 论文明确说明：所有实验在NVIDIA H200 GPU上进行（未说明数量）。  
- 使用BF16精度、FlashAttention-2、flashinfer内核及预热CUDA图。  
- 训练数据：使用PerfectBlend 1数据集（含多种指令-响应对），所有训练响应由目标模型自己生成。  
- 论文未详细披露训练时长、GPU数量等具体算力信息。

## 5. 实验数量与充分性

### 实验数量
- **端到端测试**：在Qwen3-8B和GPT-OSS-20B两个目标模型上，8个数据集、两种解码模式（T=0和T=1）共16组主结果。  
- **消融实验**：3组完整消融：  
  1. 对比“DFlash + K次重采样” vs D^2SD（验证分支多样性来源）  
  2. 对比“DFlash作为第二草稿” vs D^2SD（验证可变前缀训练的必要性）  
  3. 对比“三级级联” vs 两级（验证深层级联的边际效益）  
- 其他：表1展示块大小γ对DFlash接受长度的影响（4种γ值），表2展示GSM8K上的初步对比。

### 充分性与公平性
- **充分性**：覆盖多种任务类型（数学、代码、聊天）和两种目标模型大小（8B、20B），消融实验针对核心设计点，实验设计较全面。  
- **公平性**：所有方法在同一硬件、同一解码超参数下测量；使用公共模型检查点；对比EAGLE-3和DFlash时报告了速度与接受长度两个维度。但未报告多次运行的标准差，可能受单次运行噪声影响。

## 6. 论文的主要结论与发现

1. **D^2SD显著提升接受长度和加速比**：在Qwen3-8B上，贪心解码平均加速比从DFlash的4.16×提升到4.98×，接受长度从5.31提高到7.05；在GPT-OSS-20B上提升更明显（3.53× → 6.15×）。  
2. **简单重采样同一草稿模型效果微弱**：在GSM8K上仅提升0.37 token，而D^2SD提升2.25 token，说明需要不同的归纳偏置。  
3. **可变前缀训练是关键**：复用DFlash作为第二草稿，虽因分支位置优化有所提升（α 6.53），但仍低于完整D^2SD（α 7.62），表明针对任意前缀长度的训练至关重要。  
4. **三级级联边际效益递减**：增加第三级草稿使接受长度增加8%，但速度反而下降4%，表明两级是收益-成本平衡点。

## 7. 优点

1. **方法创新**：首次将扩散草稿模型的置信度用于指导分支位置选择，而非均匀或启发式分支。  
2. **并行效率**：双扩散草稿都采用并行生成，避免了自回归草稿的序列瓶颈；联合验证利用共享前缀KV缓存，有效控制验证开销。  
3. **设计合理**：通过指数衰减损失强调近锚错误，使第二草稿聚焦于最易导致拒绝的位置。  
4. **实验全面**：覆盖多个任务、两个模型规模、两种解码模式，消融实验验证了每个设计组件的必要性。

## 8. 不足与局限

1. **计算资源细节缺失**：未说明GPU数量、训练时长、内存占用等，影响复现和成本评估。  
2. **统计不确定性未报告**：实验结果未提供多次运行的标准差或置信区间，可能因随机性（如采样模式）导致结果波动。  
3. **应用限制**：方法依赖于目标模型的隐藏特征（KV注入），要求目标模型暴露中间表示，对黑盒模型不可用。  
4. **消融实验覆盖面有限**：未探索K和γ不同组合的敏感性，仅使用默认值（γ=16, K=4）。  
5. **与其他树结构方法（如Sequoia、SpecInfer）的直接对比缺失**：主要对比了自回归的EAGLE-3和单扩散的DFlash，未与同类树结构推测解码方法（如Sequoia、EAGLE-2）进行比较。  
6. **实时适应性未验证**：论文在固定数据集上评估，未考虑在线分布偏移或长序列生成场景。

（完）
