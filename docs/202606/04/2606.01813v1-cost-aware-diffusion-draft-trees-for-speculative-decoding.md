---
title: Cost-Aware Diffusion Draft Trees for Speculative Decoding
title_zh: 面向推测解码的成本感知扩散草稿树
authors: "Shuai Zhang, Huachuan Qiu, Hongliang He, Yong Dai"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01813v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 面向LLM吞吐量优化的成本感知投机解码树结构
tldr: 投机解码通过轻量级草稿模型生成候选token再由目标模型并行验证，可加速推理。现有块扩散草稿器如DDTree在固定节点预算下构建候选树最大化期望接受长度，但接受长度随预算单调不减，未考虑验证成本，缺乏预算选择依据。本文提出CaDDTree，直接优化token吞吐量，联合选择树结构和节点预算，将目标分解为单轮一维搜索，并利用凸验证成本下吞吐量的单峰性实现高效贪心停止。在Qwen3-4B/8B的八个推理、编码和指令遵循基准上，CaDDTree匹配或超越带最优预算的DDTree，无需离线预算搜索，每轮自适应调整，显著提升实际加速效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01813v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1644, \"height\": 555}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 781, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 792, \"height\": 524}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1625, \"height\": 601}, {\"url\": \"assets/figures/arxiv/2606.01813v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1649, \"height\": 619}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01813v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1663, \"height\": 826}, {\"url\": \"assets/tables/arxiv/2606.01813v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 746, \"height\": 179}, {\"url\": \"assets/tables/arxiv/2606.01813v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 811, \"height\": 143}, {\"url\": \"assets/tables/arxiv/2606.01813v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1664, \"height\": 829}]"
motivation: DDTree基于固定预算最大化期望接受长度，但忽略验证成本，预算单调不减导致无法选择最优，难以最大化实际吞吐量。
method: 提出CaDDTree，显式建模草稿和验证延迟，以token吞吐量为目标联合优化树结构和预算，分解为一维搜索并利用凸成本下的单峰性实现贪心停止。
result: 在Qwen3-4B/8B的八个基准（推理、编码、指令遵循）上，CaDDTree匹配或超越带最优预算的DDTree，无需预算搜索。
conclusion: CaDDTree通过自适应预算调整有效提升投机解码吞吐量，方法简洁且易于集成到现有框架。
---

## 摘要
推测解码通过让轻量级草稿模型生成候选词，并由目标语言模型并行验证来加速推理。诸如DFlash等块扩散草稿模型可一次性生成整个草稿块，给出每个位置的边际分布；DDTree利用这些分布构建一棵候选树，在固定节点预算下最大化预期接受长度。然而，我们注意到接受长度随预算非递减：它总是倾向于更大的树，而不考虑验证成本，因此缺乏预算选择的原则性依据。我们提出CaDDTree（成本感知扩散草稿树），该方法通过联合选择树结构和节点预算来直接优化令牌吞吐量（单位时间内预期生成的令牌数）。我们显式地建模草稿生成和验证延迟，证明吞吐量目标可分解为对预算的逐轮一维搜索，并证明在凸验证成本下吞吐量函数是单峰的，从而能够采用高效的贪心停止规则。CaDDTree无需离线预算搜索，每轮根据当前各位置分布和验证成本自适应调整预算。在Qwen3-4B和Qwen3-8B上的实验涵盖推理、编码和指令遵循等八个基准测试，结果表明，CaDDTree在几乎所有任务上均匹配或超越了使用预言机预算选择的DDTree。

## Abstract
Speculative decoding accelerates inference by having a lightweight drafter propose tokens verified in parallel by the target language model. Block diffusion drafters such as DFlash generate an entire draft block in one pass, yielding per-position marginals; DDTree uses these to build a candidate tree that maximizes expected acceptance length under a fixed node budget. We observe, however, that acceptance length is non-decreasing in budget: it always favors larger trees regardless of verification cost, offering no principled basis for budget selection. We introduce \textbf{CaDDTree} (Cost-aware Diffusion Draft Tree), a method that directly optimizes token throughput (expected tokens generated per unit time) by jointly selecting the tree structure and node budget. We model draft and verification latencies explicitly, show that the throughput objective decomposes into a per-round one-dimensional search over the budget, and prove that under a convex verification cost the throughput function is \emph{unimodal}, enabling an efficient greedy stopping rule. CaDDTree requires no offline budget search, adapting the budget each round from the current per-position distributions and verification cost. Experiments on Qwen3-4B and Qwen3-8B across eight benchmarks spanning reasoning, coding, and instruction-following tasks show that \caDDTree{} matches or surpasses DDTree with oracle budget selection on nearly all tasks.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：在推测解码中，现有树构建方法（如DDTree）在**固定节点预算**下最大化期望接受长度，但忽略验证成本随树大小增加这一事实。接受长度随预算单调递增，导致始终偏好更大树，缺乏预算选择的依据，无法直接优化**吞吐量**（单位时间生成token数）。
- **研究背景**：推测解码通过轻量级草稿模型生成候选，目标模型并行验证来加速推理。块扩散草稿器（如DFlash）可一次生成整个草稿块，提供各位置边际分布；DDTree利用这些分布构建最优树，但预算需人工设定。本文旨在**直接优化吞吐量**，并**自适应选择每轮预算**，消除离线调参。

### 论文提出的方法论

- **核心思想**：将吞吐量（每秒生成token数）作为优化目标，显式建模草稿延迟 \(C_d\) 和验证延迟 \(C_v(n; \ell)\)（\(n\)为节点数，\(\ell\)为上下文长度），定义每轮吞吐量：
  \[
  \hat{\theta}(T; \ell) = \frac{1 + \Phi(T)}{C_d + C_v(|T|; \ell)},
  \]
  其中 \(\Phi(T) = \sum_{s \in T} \pi_s\) 为树上所有节点的前缀概率之和（可计算代理）。
- **技术细节**：
  - **结构-预算分解**（Proposition 1）：最优吞吐量的树必然是在固定预算下最大化 \(\Phi\) 的树（即DDTree构建的树），因此问题简化为搜索最优预算 \(n^*\)。
  - **单峰性保证**（Theorem 3）：若验证成本为凸函数（边际成本非递减），则边际收益-成本比 \(\rho_n = \delta_n / c_n\) 单调不增，吞吐量函数 \(\hat{\theta}^*(n)\) 是**单峰**的（先增后减）。这允许贪心停止：当添加下一个节点导致吞吐量下降时停止。
  - **算法流程（Algorithm 1）**：维护最大堆，按前缀概率降序弹出节点，计算加入前后的吞吐量，若不再增加则停止，同时保证树结构有效（前缀封闭）。
- **成本模型**：离线在不同上下文长度和节点数上测量验证延迟，拟合凸非降回归函数，推理时线性插值。

### 实验设计

- **目标模型与草稿器**：Qwen3-4B、Qwen3-8B，均搭配DFlash块扩散草稿器。
- **数据集/基准**：8个基准，覆盖推理（MATH-500, GSM8K, AIME 2025）、编码（HumanEval, MBPP, LiveCodeBench）、指令遵循（MT-Bench, Alpaca）。
- **对比方法**：
  - **AR**：标准自回归生成。
  - **DFlash**：单序列块扩散推测解码。
  - **DDTree-oracle**：DDTree使用网格搜索（预算{16,32,...,1024}）选出的最优预算作为强基线。
- **评估指标**：每token生成时间（ms/tok，越低越好）、平均接受长度 \(\tau\)。
- **温度设置**：0.0（贪心）和 1.0（采样），主表为温度0.0，1.0结果在附录。

### 资源与算力

- **硬件**：8× A800 GPU，显存未提及。
- **精度**：bfloat16。
- **最大生成长度**：2048新token。
- **草稿块大小**：L=16。
- **成本配置时间**：一次离线测量（每个（上下文长度，节点数）组合20次重复，热身后取中位数）。
- **未提及训练时长**：CaDDTree无需额外训练，仅需离线成本分析。

### 实验数量与充分性

- **主要结果**（Table 1 & Table 4）：两个温度下，两个模型 × 8个数据集，共16个实验场景，对比三种基线（AR, DFlash, DDTree-oracle）。
- **消融实验**：
  - **成本模型消融**（Table 2）：将 \(C_v(n;\ell)\) 设为常数，吞吐量显著下降，验证验证成本非平凡。
  - **成本参数敏感性**（Table 3）：将草稿/验证成本缩放0.5x/2x，吞吐量变化<0.07 ms/tok，表明算法对近似成本鲁棒。
- **自适应预算分析**（Figure 4, 5）：展示每轮预算分布，说明按需调整的必要性。
- **实验充分性**：覆盖多种任务类型、两个模型规模、两种温度；与带最优预算的强基线对比；消融验证关键设计；敏感性分析证明鲁棒性；公正性良好（DDTree-oracle使用网格搜索最优）。

### 论文主要结论与发现

1. **吞吐量函数单峰**：实际验证成本为凸，导致吞吐量存在唯一峰值，贪心停止可找到最优预算。
2. **预算需求因轮次而异**：高置信度轮次只需少量节点，低置信度需更多；固定预算无法兼顾。
3. **CaDDTree匹配或超越DDTree-oracle**：在几乎所有任务上达到同等或更低延迟，且无需离线预算搜索。
4. **自适应预算自动匹配成本**：上下文越长、验证越昂贵时，预算自动缩小。

### 优点

- **直接优化吞吐量**：取代次优的接受长度代理，更符合实际加速目标。
- **理论保证**：证明吞吐量单峰性，贪心算法全局最优。
- **完全自适应**：每轮根据边际分布和当前上下文长度自动选择预算，无需人工调参。
- **成本模型简单有效**：离线凸回归 + 推理时线性插值，对参数误差鲁棒。
- **实验广泛且公平**：涵盖多种任务和两个模型规模，与带最优预算的基线对比，验证了算法有效性。
- **代码开源**。

### 不足与局限

- **局限于块扩散草稿器**：方法依赖DFlash等一次生成边际分布的模型；对自回归草稿器（如EAGLE）不直接适用，但思路可扩展。
- **验证成本凸性假设**：虽然实验验证成立，但理论依赖凸性；若成本非凸，单峰性可能不保证。
- **仅测试两个模型**：结论泛化性需更多模型验证（尤其是更大模型或不同架构）。
- **未与RL优化方法对比**：如Zhang et al.(2026)的基于RL的吞吐量优化，虽然声称无需训练但缺乏直接比较。
- **温度1.0结果放在附录**：未在正文展示采样下的完整性能分析。
- **成本配置需一次硬件测量**：跨平台需重复测量，但敏感度低表明近似即可。
- **没有实际端到端延迟测量**：仅报告每token时间，未比较总生成延迟分布。

（完）
