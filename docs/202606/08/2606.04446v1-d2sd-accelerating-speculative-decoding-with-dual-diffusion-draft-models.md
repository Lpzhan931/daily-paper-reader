---
title: "D^2SD: Accelerating Speculative Decoding with Dual Diffusion Draft Models"
title_zh: "D^2SD: 利用双扩散草稿模型加速推测解码"
authors: "Liyuan Zhang, Jiarui Zhang, Jinwei Yao, Ran Yan, Yuchen Yang, Jiahao Zhang, Tongkai Yang, Yi Wu, Binhang Yuan"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04446v1"
tags: ["query:llm"]
score: 9.0
evidence: 利用双重扩散草稿模型加速LLM投机解码
tldr: 现有扩散式投机解码草稿器仅生成单条草稿序列，一旦首个token不匹配即丢弃，接受率低；简单多候选批处理效果有限。提出D^2SD框架，利用双扩散草稿模型：首模型生成文本块并输出逐位置置信度，据此构建前缀树并挑选top-K候选前缀；次模型以这些前缀为起点批量生成替代延续，最后通过级联注意力联合验证。实验表明D^2SD在多个任务上显著超越基础扩散方法及强自回归投机解码基线。为投机解码提供了高效利用多候选机制的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04446v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1502, \"height\": 513}, {\"url\": \"assets/figures/arxiv/2606.04446v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1719, \"height\": 407}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04446v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 813, \"height\": 279}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 639, \"height\": 237}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1721, \"height\": 588}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1721, \"height\": 586}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 907, \"height\": 345}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 903, \"height\": 321}, {\"url\": \"assets/tables/arxiv/2606.04446v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 671, \"height\": 341}]"
motivation: 现有扩散草稿器单序列提交导致低接受率，多候选批处理效率低下，需更智能的候选组织与验证策略。
method: 构建置信度引导的前缀树，首模型预测拒绝边界并选top-K前缀；次模型批量生成替代延续，级联注意力联合验证。
result: 在多个任务上超越基础扩散方法和强自回归投机解码基线，实现更高加速比。
conclusion: D^2SD通过双扩散草稿模型与共享前缀验证，有效提升投机解码的接受率与效率。
---

## 摘要
推测解码通过草拟多个token并在单个目标模型前向传递中验证它们来加速自回归大语言模型的推理。最近的基于扩散的草稿模型并行生成整个token块，但通常每次验证只承诺一个草稿序列：一旦出现第一个不匹配，所有后续草稿token都被丢弃，导致接受率有限。简单地对更多草稿候选序列进行批处理只会带来微小的改进，因为冗余或位置不当的分支增加了草拟和验证的成本，而没有成比例地增加接受的token数量。我们提出了D^2SD，一种双扩散草稿推测解码框架，它将候选序列组织成一个置信度引导的前缀树，其中第一个扩散草稿模型生成一个块以及每个位置的置信度分数，这些分数用于识别最可能的拒绝边界并选择前K个前缀范围进行恢复；第二个可变前缀扩散草稿模型在每个选定的前缀处重新定位，并在一次批处理传递中提出替代延续；由此产生的共享前缀候选序列通过级联注意力进行联合验证。实验表明，D^2SD在基础扩散方法和强自回归推测解码基线上均有明显改进。

## Abstract
Speculative decoding accelerates autoregressive large language model inference by drafting multiple tokens and verifying them in a single target-model forward pass. Recent diffusion-based drafters generate an entire block of tokens in parallel but usually commit to a single draft sequence per verification: once the first mismatch occurs, all subsequent draft tokens are discarded, resulting in a limited acceptance rate. Naively batching more draft candidate sequences only introduces a marginal improvement, as redundant or poorly placed branches increase the cost of drafting and verification without proportionally increasing the number of accepted tokens.   We propose D^2SD, a dual diffusion draft speculative decoding framework that organizes candidates into a confidence-guided prefix tree, where the first diffusion drafter generates a block along with per-position confidence scores that are used to identify the most likely rejection boundary and select the top-K prefix ranges for recovery; the second variable-prefix diffusion drafter re-anchors at each selected prefix and proposes alternative continuations in one batched pass; the resulting shared-prefix candidates are jointly verified via cascade attention. Empirically, D^2SD shows clear improvements over both the underlying diffusion approach and strong autoregressive speculative decoding baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

推测解码（Speculative Decoding）通过一个轻量级草稿模型生成多个候选 token，再由目标模型并行验证，以加速大语言模型的自回归推理。然而，现有基于扩散的草稿模型（如 DFlash）每次验证只提交一条草稿序列：一旦早期位置出现一个错误 token，后续所有 token 都被丢弃，导致接受长度受限。单纯增加草稿块大小（γ）或简单批处理多个重复采样的候选序列，收益甚微且容易遭遇“缩放墙”（scaling wall）：当 γ ≥ 24 时，接受长度甚至下降。核心矛盾在于扩散草稿模型的均匀计算分配与验证规则的“最长正确前缀”特性不匹配，早期错误的惩罚远大于后期错误。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：提出双扩散草稿模型（D²SD）框架，将单个扩散草稿转化为一个**置信度引导的前缀树**：第一个扩散草稿器生成完整块并输出逐位置置信度，据此预测最可能的拒绝边界，并选择 top‑K 前缀范围；第二个可变前缀扩散草稿器（VP‑Drafter）在这些前缀处重新锚定，并在一次批处理中生成替代延续；最后通过级联注意力联合验证 K+1 个共享前缀的候选序列。

- **关键技术细节**：
  1. **第一阶段（DFlash 草稿）**：标准 DFlash 生成 γ 个 token 的位置置信度 \(c_k = \max_v p_k(v)\)。
  2. **拒绝边界估计**：基于条件独立性假设，计算长度为 i 的被接受概率 \(r(i) = \left(\prod_{k=1}^{i} c_k \right) \cdot (1 - c_{i+1})\)，选择 top‑K 前缀长度 \(S = \text{Top-K}_{i} r(i)\)。
  3. **第二阶段（VP‑Drafter）**：对每个选中的前缀长度 i，保留前 i 个 token 作为可见前缀，将后续位置重新掩码，堆叠 K 个分支为一个 batch，由 VP‑Drafter 并行生成替代延续。
  4. **联合验证**：目标模型通过级联注意力（cascade attention）一次性验证 K+1 个候选，接受最长的正确前缀。
  5. **VP‑Drafter 训练**：采用截断几何先验 \(Pr(l=j) \propto \beta^j\) 采样前缀长度，损失函数为指数衰减的交叉熵：\(L_{VP} = -\frac{\sum_{k=l+1}^{\gamma-1} w_k \log p_k(t_k^\star)}{\sum w_k}\)，其中 \(w_k = \exp(-\frac{k-l-1}{\tau})\)，强调靠近锚点的错误。

### 3. 实验设计

- **数据集**：涵盖数学（GSM8K、MATH、AIME25）、代码（HumanEval、MBPP、LiveCodeBench）、对话（MT-Bench、Alpaca）共 8 个任务。
- **基准方法**：标准自回归解码（基线）、DFlash、EAGLE‑3（强自回归推测解码基线）。
- **对比设置**：目标模型为 Qwen3‑8B 和 GPT‑OSS‑20B，默认配置 γ=16, K=4，评估贪婪解码（T=0）和采样解码（T=1）下的**平均接受长度 α** 和**端到端加速比**。
- **训练数据**：所有草稿模型（EAGLE‑3, DFlash, VP‑Drafter）均在 PerfectBlend 数据集上训练，且训练响应由目标模型自身生成以保证分布对齐。

### 4. 资源与算力

文中提及所有实验在 **NVIDIA H200 GPU** 上进行，但未明确说明使用 GPU 的数量、训练时长或具体算力消耗。训练数据 PerfectBlend 是一个公开的多领域指令数据集的完整版本，但训练的具体硬件配置（如单卡/多卡、训练轮次）未给出。

### 5. 实验数量与充分性

论文进行了以下实验：
- **主实验**：在 8 个数据集上对比 D²SD 与 DFlash、EAGLE‑3，报告两种解码模式下的接受长度和加速比（共 2 个目标模型 × 2 种温度 × 8 数据集 ≈ 32 组对比）。
- **消融实验**：
  1. **Ablation 1**：用 DFlash 自身做 K=4 重采样 vs. D²SD（验证分支来源多样性）。
  2. **Ablation 2**：用 DFlash 作为第二阶段草稿器 vs. VP‑Drafter（验证可变前缀训练的作用）。
  3. **Ablation 3**：堆叠第三级草稿 vs. 双级设计（验证级联深度的影响）。
- **其他分析**：在 4 个代表性数据集（GSM8K、MATH、MBPP、MT‑Bench）上报告消融结果，并给出了 DFlash 置信度校准曲线和接受概率分布的图示分析。

实验覆盖了多个领域、多种基线，并且消融实验系统性地验证了每个设计组件的作用，充分且客观。对比方法均使用相同硬件和解码超参数，公平性有保障。

### 6. 论文的主要结论与发现

- D²SD 在所有测试任务上显著优于 DFlash 和 EAGLE‑3，尤其在数学和代码任务上加速比提升最大（例如 Qwen3‑8B 下平均加速比从 DFlash 的 4.16× 提升至 4.98×，平均接受长度从 5.31 提升至 7.05）。
- 简单增加采样候选（K 重采样）只能带来微小改进，因为所有分支共享相同概率分布，无法突破“误差同质性”。
- 置信度引导的重新锚定比均匀重采样或块大小扩展更有效。
- 堆叠更多级联层（第三级）带来的接受长度提升不足以弥补额外的草稿和验证成本，双级设计是最优权衡。

### 7. 优点

- **方法论创新**：首次将扩散草稿模型的内部置信度用于指导拒绝边界预测，并引入**可变前缀扩散草稿器**专门训练以支持任意前缀长度，使第二个草稿器具有与第一个不同的归纳偏置。
- **高效验证**：利用共享前缀结构，通过级联注意力一次验证多个候选，避免了自回归树推测解码的串行草稿开销。
- **实验设计严谨**：包含两个不同规模的目标模型、两种解码模式、消融实验覆盖每个设计选择，对比方法包括当前最先进的扩散式（DFlash）和自回归式（EAGLE‑3）基线。
- **损失函数设计**：指数衰减的交叉熵权重自然匹配验证规则（靠近锚点的错误成本更高），训练稳定且有效。

### 8. 不足与局限

- **算力与开销未量化**：文中未报告 D²SD 相对于 DFlash 或 EAGLE‑3 的具体额外训练时间或推理时的模型参数增加量（VP‑Drafter 的参数量和模型大小未提及），可能影响实际部署的成本评估。
- **实验覆盖有局限**：仅在 Qwen3‑8B 和 GPT‑OSS‑20B 两个目标模型上测试，是否适用于其他架构（如 DeepSeek、LLaMA）尚未验证；此外，所有实验基于 NVIDIA H200 单一硬件，未在更广泛硬件（如 A100、CPU）上评估。
- **任务偏差**：在开放域对话任务（MT-Bench、Alpaca）上的提升幅度不如数学/代码任务明显，可能因为对话的拒绝边界分布更分散，置信度估计的可靠性降低。
- **仅评估贪婪和采样两种温度**：未涉及多模态或长上下文场景，也未讨论对 batch 服务和延迟约束的影响。
- **第三级消融显示收益递减**：但未探索在不同 γ 或 K 下最佳级联数是否变化，优化空间可能未被完全挖掘。

（完）
