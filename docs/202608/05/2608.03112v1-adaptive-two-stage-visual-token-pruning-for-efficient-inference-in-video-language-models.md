---
title: Adaptive Two-Stage Visual Token Pruning for Efficient Inference in Video-Language Models
title_zh: 自适应两阶段视觉令牌剪枝用于视频语言模型的高效推理
authors: "Paribesh Regmi, Qingshuang Chen, Chi Zhang, Heba Aly, Yelin Kim, Hongda Mao"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03112v1"
tags: ["query:vlm-spec"]
score: 9.0
evidence: 针对视频语言模型的自适应两阶段视觉Token剪枝，降低推理延迟
tldr: "视觉语言模型处理视频时因token过多导致高延迟，现有token剪枝方法多针对单图像且采用固定比例，难以适应视频，同时多帧处理加剧计算压力。为此提出两阶段自适应剪枝：先剔除冗余帧，再基于token嵌入相关性自适应决定保留帧的剪枝率，从而动态匹配视频内容冗余度。在视频字幕基准上，仅保留10% token时准确率提升7%，计算量降低95%。该后验方法无需训练即可应用，为视频语言模型高效推理提供了即插即用方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.03112v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1783, \"height\": 725, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03112v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1776, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03112v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1793, \"height\": 546, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03112v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 772, \"height\": 543, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03112v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1796, \"height\": 994, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03112v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1655, \"height\": 768, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03112v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 817, \"height\": 792, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03112v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 809, \"height\": 508, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03112v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 855, \"height\": 403, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03112v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1427, \"height\": 715, \"label\": \"Table\"}]"
motivation: 视频语言模型推理延迟高，现有token剪枝仅针对单图像且采用固定比例，未利用视频时空冗余，导致效率低且易丢失关键信息。
method: 提出无需训练的后验两阶段剪枝：先剔除冗余帧，再通过分析token嵌入相关性自适应决定剩余帧的token剪枝率。
result: "在视频字幕基准上，仅保留原有token的10%时，准确率提升7%，同时计算量减少95%，性能显著优于固定比例剪枝。"
conclusion: 内容自适应的两阶段剪枝能大幅降低计算量并提升性能，为视频语言模型高效推理提供了即插即用方案。
---

## 摘要
视觉语言模型在图像和视频理解方面表现出色，但由于需要处理每张图像数千个令牌而导致推理延迟高，限制了它们在资源受限的边缘设备和实时监控应用中的部署。在视频处理中，这一挑战更加突出，因为必须同时分析多个帧。现有的令牌缩减技术主要针对单图像输入开发，因此未能考虑视频序列中存在的时间性和帧间冗余。此外，这些方法通常依赖于对所有输入应用固定的统一剪枝比率，这是次优的，因为不同视频之间的冗余程度可能有显著差异，需要内容相关的剪枝水平以保留关键信息。为了解决这些限制，我们提出了一种专门为视频处理设计的两阶段自适应令牌剪枝策略。在第一阶段，我们剪除冗余帧；在第二阶段，对保留帧内进行令牌级剪枝。关键在于，第二阶段的剪枝比率是根据每个视频的内容自适应确定的。这通过分析令牌嵌入的相关结构来量化冗余，从而确定比率。重要的是，我们的方法完全是事后性的，不需要额外训练或微调，同时取得了显著的实证收益；值得注意的是，在令牌保留率为10%的情况下，它在视频字幕基准上将准确率提高了+7\%，同时将计算TFLOPs减少了95\%。

## Abstract
Vision-language models excel at image and video understanding but suffer from high inference latency due to the need to process thousands of tokens per image, limiting their deployment on resource-constrained edge devices and in real-time surveillance applications. This challenge is further amplified in video processing, where multiple frames must be analyzed simultaneously. Existing token reduction techniques are largely developed for single-image inputs and therefore fail to account for the temporal and inter-frame redundancies present in video sequences. In addition, these methods generally rely on a fixed, uniform pruning ratio applied across all inputs, which is suboptimal because the degree of redundancy can vary significantly between different videos, necessitating content-dependent pruning levels to preserve critical information. To address these limitations, we propose a two-stage adaptive token pruning strategy specifically designed for video processing. In the first stage, we prune out the redundant frames, and in the second stage, token-level pruning is applied within the retained frames. Crucially, the pruning ratio in the second stage is determined adaptively based on the content of each video. This is achieved by analyzing the correlation structure of token embeddings to quantify redundancy, which is used to determine the ratio. Importantly, our method is entirely post-hoc and requires no additional training or fine-tuning, while achieving strong empirical gains; notably, it improves accuracy by +7\% on a video captioning benchmark at 10\% token retention, while reducing computation TFLOPs by 95\%.

---

## 论文详细总结（自动生成）

好的，我将根据您提供的论文内容，生成一份详细的中文总结。

## 论文总结：自适应两阶段视觉令牌剪枝用于视频语言模型的高效推理

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉语言模型（VLMs）虽然在图像和视频理解任务中性能卓越，但其需要处理海量的视觉令牌（tokens），导致推理延迟极高。尤其是在视频处理中，多帧图像带来的令牌数量激增，使得计算成本呈二次方增长，严重阻碍了模型在资源受限的边缘设备和实时监控等场景中的部署。
- **现有方法的两大缺陷**：
    1.  **缺乏时间冗余处理**：大多数现有的令牌缩减技术主要针对单张图像设计，直接应用于视频时无法有效利用帧之间的时间冗余，导致计算资源浪费。
    2.  **固定剪枝比例**：现有方法通常对所有输入使用统一的固定剪枝比率。然而，不同视频的内容冗余度差异巨大，固定比例无法根据内容动态调整，容易在简单视频上过度计算或在复杂视频上丢失关键信息。
- **研究意义**：为了解决上述问题，本文提出了一种**无需额外训练**的、内容自适应的两阶段令牌剪枝策略，旨在高效且无损地减少视频处理的计算量，推动VLMs在更多实际场景中的部署。

### 2. 论文提出的方法论：核心思想、关键技术细节

论文提出了一种**两阶段自适应剪枝框架**，核心思想是渐进式地消除视频中的时间（帧级）和空间（令牌级）冗余。

- **第一阶段：帧级剪枝（Temporal Pruning）**
    - **目标**：减少时间冗余。
    - **方法**：使用基于多样性的选择算法（源自DivPrune），在所有视频帧中挑选出一个信息最丰富且彼此差异最大的子集（保留率γ）。此步骤可以丢弃大量视觉上相似、对理解视频内容贡献有限的冗余帧。

- **第二阶段：令牌级剪枝（Spatial Pruning）**
    - **目标**：消除保留帧内部的空间冗余。
    - **自适应剪枝比率（β）的核心机制**：
        1.  计算保留帧中所有令牌嵌入（token embeddings）的相关性矩阵。
        2.  对相关矩阵进行特征值分解，得到特征值谱（λ1 ≥ λ2 ≥ ...）。
        3.  **用指数衰减函数拟合该特征值谱**：λn = Ae^(-αn)，其中α为衰减速率。
        4.  **核心洞察**：α值越大，表示特征值衰减越快，令牌间的相关性越强，冗余度越高，因此只需保留少量令牌即可；反之，α值越小，表示内容越多样，需要保留更多令牌。
        5.  **确定保留比率**：最终的第二阶段令牌保留比率被定义为与衰减速率成反比：**β = κ / α**，其中κ是控制整体剪枝强度的超参数。

- **算法流程**：
    1.  输入令牌嵌入，利用**低秩SVD**（如迭代幂法）计算前32个最大特征值。
    2.  在(log λn, n)坐标系中拟合直线，其斜率即为-α。
    3.  根据公式β = κ / α计算自适应保留率。
    4.  在第二阶段，使用基于多样性的选择算法，根据β比率从保留帧中选择最终的代表性令牌。

### 3. 实验设计

- **评估基准与数据集**：实验在**三个主流VLM**上，覆盖了**五大视频理解基准**，涵盖三类任务：
    - **视频字幕生成**：VideoDetailCaption (VideoDC)
    - **开放式问答（Open-ended QA）**：VideoChatGPT
    - **多项选择问答（Multiple-choice QA）**：NextQA, PerceptionTest, Video-MME
- **对比基线**：选取了五种具有代表性的免训练令牌剪枝方法进行对比，包括：
    - **PruMerge**（基于注意力）
    - **FastV**（基于注意力）
    - **AvgPool**（动态池化，来自Dynamic-VLM）
    - **DivPrune**（基于嵌入多样性，最优基线之一）
    - **LLaVA-Scissor**（基于图的语义连通组件合并）
- **实验设置**：在不同令牌保留率（30%、15%、10%）下进行对比。对于本文方法，通过调整超参数κ使得其平均保留率与基线一致，确保公平比较。

### 4. 资源与算力

- **硬件平台**：论文在实验部分明确指出，所有实验均使用 **NVIDIA L40S GPU**（配备**48GB内存**）完成。
- **未明确信息**：论文**未提及**使用的GPU数量、具体训练/推理总时长、功耗等关键算力指标。由于方法本身是免训练的（post-hoc），其额外开销主要在于SVD分解，但这部分计算成本的具体数值也未详细报告（仅提供了每token生成时间）。

### 5. 实验数量与充分性

- **实验数量**：实验非常丰富，具体包括：
    - **主实验（Table 1）**：在LLaVA-Video模型上，对所有5个数据集、3种保留率进行全量对比。
    - **跨模型泛化实验（Table 2）**：将最佳方法和基线扩展到InternVL3和Qwen2.5VL，在3个数据集上进行验证。
    - **跨参数规模实验（Table 3）**：在InternVL-1B和2B模型上验证方法的通用性。
    - **消融实验（Table 4）**：分别验证两阶段剪枝和自适应保留率两个组件的贡献。
    - **消融实验（Table 5）**：比较了线性、二次和指数三种特征值谱拟合函数的性能。
- **充分性评估**：
    - **充分性**：实验设计较为全面，从模型、数据集、任务类型、参数规模等多个维度证明了方法的有效性和泛化性。消融实验清晰地证明了每个设计模块的必要性。
    - **客观性**：评估指标不仅包含准确率，还使用了LLM（Claude 3.7）打分的方式，较为客观。对比实验中的平均保留率保持一致，体现了公平性。
    - **潜在不足**：实验环境较为单一（仅NVIDIA L40S），未提供更广泛硬件上的性能表现。此外，论文未提及多次运行的标准差/置信区间，无法判断结果的稳定性。

### 6. 论文的主要结论与发现

- **性能显著提升**：论文提出的方法在**所有保留率、所有模型和几乎所有数据集上均取得了最优性能**。尤其是在10%的极低保留率下，在VideoDC字幕任务上，相比最佳基线DivPrune准确率提升了8%以上（76.75% vs 68.47%）。
- **计算开销大幅降低**：在30%、15%和10%保留率下，LLM部分的计算量（TFLOPs）分别减少了约83%、93%和95.5%，证明了该方法的效率优势。
- **两阶段优势明确**：消融实验证实，无论是单独使用两阶段剪枝（对比单阶段DivPrune），还是在此基础上增加自适应保留率，都能带来稳定的性能提升，证明了各组件均有实际贡献。
- **指数衰减拟合最佳**：在三种拟合函数中，使用指数函数来刻画特征值谱并确定保留率的效果最好。

### 7. 优点

- **即插即用，无需训练**：方法完全基于已有模型的嵌入，无需额外训练或微调，可直接应用于任何预训练的VLM，实用性强。
- **内容自适应，科学有据**：提出用特征值衰减速率来量化令牌冗余性，并据此动态决定剪枝率，克服了传统固定比例剪枝的缺陷，方法具有坚实的理论基础。
- **兼顾时间与空间冗余**：两阶段设计分别处理了视频的帧级和令牌级冗余，比仅处理单一层面冗余的方法更贴合视频数据的特性。
- **计算高效**：使用低秩SVD仅计算少量特征值，避免了完整的特征分解，显著降低了方法本身的额外计算开销。

### 8. 不足与局限

- **实验环境与细节披露不足**：论文未详细说明GPU数量和总计算时间，也未在更廉价的消费级硬件上验证，阻碍了对实际部署成本的全面评估。
- **未进行与训练方法的直接对比**：虽然与免训练方法进行了对比，但没有与需要训练的压缩方法（如Video-XL-Pro）进行比较，无法定论哪一类方法在性能-成本权衡上更优。
- **超参数引入新的“不适应性”**：虽然β是自适应的，但引入了新的超参数κ来控制整体剪枝强度。如何根据不同模型和应用场景设置最优的κ，论文未给出明确的确定准则，需人为手动调整。
- **模型大小覆盖有限**：论文验证的最大模型为8B（InternVL3），未在更大的模型（如30B、70B）上验证，其方法在大规模模型上的有效性未知。
- **对低保留率下任务差异的解释不足**：实验结果显示在多项选择QA（如NextQA）上性能提升很小（通常不到1%），而在字幕生成任务上提升巨大。论文对这一现象缺乏深入分析和解释。

（完）
