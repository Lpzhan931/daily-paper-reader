---
title: "DeltaLog: Deferred Materialization of Recurrent States for Linear Attention Decoding"
title_zh: DeltaLog：线性注意力解码中循环状态的延迟物化
authors: "Junqing Lin, Jingwei Sun, Guangzhong Sun"
date: 2026-08-16
pdf: "https://arxiv.org/pdf/2608.15533v1"
tags: ["query:llm"]
score: 8.0
evidence: 线性注意力模型的高效解码，减少循环状态物化开销
tldr: 线性注意力解码需频繁写回完整循环状态，内存流量大。DeltaLog将状态表示为稠密基态加紧凑更新日志，多数解码步骤仅追加轻量更新，周期合并回基态，语义不变。在GDN、KDA、RWKV6上，更新核加速至1.86倍，写流量减少至7.83倍，端到端提速1.05-1.20倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.15533v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 759, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 871, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 816, \"height\": 538}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1640, \"height\": 639}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1648, \"height\": 581}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1799, \"height\": 589}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1799, \"height\": 589}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 878, \"height\": 378}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 877, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 879, \"height\": 372}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 658, \"height\": 494}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 878, \"height\": 445}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.15533v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 443}, {\"url\": \"assets/tables/arxiv/2608.15533v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 879, \"height\": 220}, {\"url\": \"assets/tables/arxiv/2608.15533v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 843, \"height\": 262}, {\"url\": \"assets/tables/arxiv/2608.15533v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 829, \"height\": 411}]"
motivation: 现有线性注意力解码反复物化并写回完整循环状态，造成大量内存流量，制约性能。
method: 采用延迟物化，以稠密基态和紧凑更新日志表示状态，多数步骤仅追加，定期合并。
result: 在三种模型上更新核加速1.86倍，写流量降低7.83倍，端到端提速1.05-1.20倍。
conclusion: 提出通用循环状态解码方案，无需改变模型语义即可显著降低内存开销，提升服务性能。
---

## 摘要
线性注意力模型通过用循环状态更新替代成对标记交互，消除了softmax注意力的二次前缀计算和不断增长的KV缓存。然而，现有的解码实现通常在生成每个标记后物化并写回完整的循环状态，使得状态维护成为内存流量的主要来源，尤其是对于具有大状态和多头数的模型。本文提出了DeltaLog，一种循环状态解码方案，在不改变模型语义的情况下减少这种开销。具体来说，DeltaLog将循环状态表示为稠密基础状态加上近期紧凑更新的有界日志。大多数解码步骤仅将紧凑更新因子追加到该日志中，而周期性合并步骤则将累积的更新合并回稠密基础状态。因此，模型观察到与急切解码相同的稠密状态，但大多数完整状态写回被轻量级追加操作所取代。我们为GDN、KDA和RWKV6实现了DeltaLog，并将其集成到原型服务栈中。在这些模型上，DeltaLog将循环状态更新内核加速高达1.86倍，将分析得到的循环状态写流量减少高达7.83倍，并相对于稠密循环基线实现了1.05–1.20倍的端到端服务加速。

## Abstract
Linear attention models eliminate the quadratic prefix computation and context-growing KV cache of softmax attention by replacing pairwise token interactions with recurrent state updates. However, existing decoding implementations often materialize and write back the full recurrent state after every generated token, making state maintenance a major source of memory traffic, especially for models with large states and many heads. This paper presents DeltaLog, a recurrent-state decoding scheme that reduces this overhead without changing the model semantics. Specifically, DeltaLog represents the recurrent state as a dense base state together with a bounded log of recent compact updates. Most decode steps append only compact update factors to this log, while periodic merge steps fold the accumulated updates back into the dense base state. Thus, the model observes the same dense state as in eager decoding, but most full-state write-backs are replaced by lightweight append operations. We implement DeltaLog for GDN, KDA, and RWKV6 and integrate it into a prototype serving stack. Across these models, DeltaLog accelerates the recurrent-state update kernel by up to $1.86\times$, reduces profiled recurrent-state write traffic by up to $7.83\times$, and achieves $1.05$--$1.20\times$ end-to-end serving speedups over dense recurrent baselines.

---

## 论文详细总结（自动生成）

# DeltaLog 论文详细中文总结

## 1. 论文的核心问题与整体含义

**研究背景与动机：**
- 线性注意力模型（Linear Attention）通过循环状态（recurrent state）更新替代 softmax 注意力中的两两 token 交互，消除了二次方前缀计算和不断增长的 KV cache。
- 然而，现有解码实现（如 FLA）在每生成一个 token 后都会将完整的稠密循环状态物化并写回 HBM，导致巨大的内存流量开销。
- 论文将此问题命名为 **"state-update tax"（状态更新税）**：新 token 只贡献 $O(BH(d_k + d_v))$ 个紧凑因子，但急切物化却要读写 $O(BH d_k d_v)$ 个状态元素，存在严重的写放大问题。
- 通过 Roofline 分析，论文证明该操作算术强度仅约 0.5–1.0 FLOPs/byte，远低于 H200 等 GPU 的机器平衡点（约 208 FLOPs/byte），属于强内存受限操作。

**整体含义：**
- 传统线性注意力的状态维护策略需要重新设计，物理状态的物化调度与逻辑状态本身可以解耦，从而在保持模型语义不变的前提下降低内存带宽开销。

## 2. 论文提出的方法论

**核心思想：延迟物化（Deferred Materialization）**

DeltaLog 将循环状态分解为两部分：
- **稠密基础状态（dense base state）** $\bar{S}$：存储较早历史，FP32 精度；
- **有界更新日志（bounded update log）**：存储最近的紧凑更新因子（key 侧向量、value 侧向量、log-decay 快照）。

**关键技术细节：**

1. **追加–合并循环（Append–Merge Cycle）**
   - 设合并间隔为 $M$，日志容量 $C = M - 1$。
   - **追加阶段**（多数解码步，$L < C$）：只记录紧凑更新 $(\bm{a}_t, \bm{b}_t, \bar{\ell})$ 到日志中，不动稠密基态。
   - **合并阶段**（每 $M$ 步一次，$L = C$）：将日志中所有延迟更新合并回稠密基态，并重置计数器和日志。
   - 公式 7（状态不变量）保证逻辑状态与急切解码完全一致：
     $$S_t = D(\bar{\ell})\bar{S} + \sum_{i < L} D(\bar{\ell} - \Lambda_{\text{log}}[i]) K_{\text{log}}[i] U_{\text{log}}[i]^\top$$

3. **闭环性证明**：论文严格推导了追加和合并操作均保持式（7）状态不变量，从而在精确算术下与急切稠密解码代数等价。

4. **流量分析**：给出了 DeltaLog 的平均每步流量模型（式 10–11），阐明避免的稠密写流量需超过额外日志流量与衰减元数据流量之和，合并间隔 $M$ 存在最优值。

5. **分块内核分解（Tiled Kernel Decomposition）**
   - 沿 value 维将每个 head 状态划分为 $N_V$ 个 value 瓦片；
   - 每个瓦片拥有稠密基态切片和局部 value 侧日志；
   - key 侧日志、log-decay 快照和长度元数据在瓦片间共享，由单一 writer 负责提交；
   - 三阶段流水：ReplayPrep（共享重放系数计算）→ AppendDecode（并行瓦片解码）→ Finalize（元数据提交）。

6. **三算子实例化**：
   - **GDN**：head-wise 标量衰减 + Delta 规则修正，key 用 BF16、value 用 FP32，$M=8$；
   - **KDA**：逐 key 向量衰减 + Delta 规则修正，key 用 BF16、value 用 FP32，$M=4$；
   - **RWKV6**：逐 key 向量衰减，无修正路径，key/value 均用 BF16，$M=4$。

7. **端到端系统实现**：基于 vLLM 搭建服务原型，使用双 CUDA 图（append 图与 merge 图）阶段化调度，维护独立的 append/merge 队列以兼容 continuous batching。

## 3. 实验设计

**评估场景与算子：**
- 核级实验：覆盖 GDN（Gated DeltaNet）、KDA（Kimi Delta Attention）、RWKV6 三种线性注意力算子，形状为 32 heads / $d_k = d_v = 128$。
- 端到端服务实验：Qwen3.6-35B-A3B（GDN）和 Kimi-Linear-48B-A3B-Instruct（KDA）。

**对比基线：**
- **FLA**（Fast Linear Attention）：强化的稠密循环状态 Triton 内核，为主要基线；
- **torch.compile**：PyTorch 编译优化实现；
- **vLLM**：服务栈内的稠密循环内核（仅用于 GDN 和 KDA）。

**衡量指标：**
- 核级：单 token 解码时延（TPOT）及相对 FLA 的加速比；
- 流量：Nsight Compute 硬件计数器采集的 HBM DRAM 读写流量；
- 端到端：完整模型解码 TPOT（ms/token）。

## 4. 资源与算力

- **GPU 平台**：
  - RTX 4090：24 GB 显存，1008 GB/s 带宽，165.2 TFLOPS（FP16/BF16），代表消费级平台；
  - H200 NVL：141 GB 显存，4.8 TB/s 带宽，1671 TFLOPS，代表数据中心平台。
- 端到端实验仅在 H200 上运行。
- 论文未明确提及 GPU 数量、训练时长或能耗数据——这主要是因为该工作是推理优化而非模型训练研究，不涉及训练过程，因此算力总投入未作说明。

## 5. 实验数量与充分性

**主要实验组（共 6 类）：**

1. **核级解码性能**（图 6、7）：3 算子 × 4 批大小（64/128/256/512）× 2 GPU，共约 24 组核心对比，均报告相对 FLA 加速比。

2. **机制验证**（图 8）：对 GDN 在 B=128、M=8 下分解延迟与 DRAM 流量，直接验证写流量降低。

3. **Split-Replay 消融**（图 9）：3 算子 × 2 合并间隔（M=4/8）× 3 批大小，比较融合与拆分实现。

4. **合并间隔灵敏度扫描**（图 10）：M ∈ {2,4,8,16,32,64}，3 个算子各 6 档。

5. **状态规模扩展**（图 11）：总状态规模从 128 MiB 至 1024 MiB，3 算子。

6. **数值行为验证**（图 12）：128 token rollout 下输出与状态最大相对误差，覆盖 B ∈ {64,128,256}，3 算子。

7. **端到端服务**（表 4）：2 模型 × 3 批大小 = 6 组配置，每配置生成 128 个解码 token。

**充分性评价：**
- 优点：实验覆盖了从核级到系统级的完整链路，包含延迟、流量、消融、灵敏度、数值正确性和端到端收益，多维证据链完整。
- 不足：端到端实验仅覆盖 GDN 和 KDA 两个算子（未含 RWKV6）、仅一种 GPU（H200）、仅 3 个批大小，且生成长度仅为 128 token；论文也承认未报告延迟分位数（P99）等服务水平指标。

## 6. 主要结论与发现

1. **核级加速显著**：H200 上相对 FLA 加速 1.19–1.69 倍，RTX 4090 上加速 1.30–1.86 倍。GDN 收益最大，KDA 因 per-key 衰减元数据较重而收益较小。

2. **写流量大幅降低**：GDN 在 B=128、M=8 时，平均每 token 写流量从 0.230 GiB 降至 0.0294 GiB，减少 7.83 倍；总 DRAM 流量减少 1.64 倍。

3. **端到端收益**：全模型服务下 Qwen/GDN 加速 1.08–1.20 倍、Kimi/KDA 加速 1.05–1.12 倍，符合 Amdahl 定律预期（只优化部分路径）。

4. **状态规模依赖**：状态越大收益越明显，从 128 MiB 的接近持平（KDA 1.02 倍）到 1024 MiB 的 1.58 倍，证明该方法主要针对写回代价主导的场景。

5. **数值安全性**：在混合精度和不同累积顺序下，输出相对误差低于 1% 阈值（最大 0.00450），状态误差极低（GDN/RWKV6 在 $10^{-7}$ 量级），与稠密参考实现容差等价。

## 7. 优点

- **问题识别精准**：清晰定义了"状态更新税"这一概念，并通过 Roofline 模型和性能剖析量化其影响，动机扎实。
- **方法优雅且语义无损**：只改变物理调度而非逻辑语义，具有严格的代数等价性证明，可广泛适用于各类循环线性注意力模型。
- **工程实现完备**：分块瓦片分解、共享重放系数预计算、单写者元数据提交等设计充分考虑 GPU 执行模型，兼顾并行度与开销。
- **实验证据链完整**：从硬件计数器到核级延迟再到端到端服务，多层面交叉验证，且包含数值正确性验证，结论可信度高。

## 8. 不足与局限

- **端到端收益有限**：全模型加速仅 1.05–1.20 倍，低于核级加速（最高 1.86 倍），主要受限于 MLP、归一化、调度等未优化部分，符合 Amdahl 定律但实际收益不够突出。

- **适用场景受限**：仅在小批或小状态规模下收益较小甚至可能回退（KDA 在 128 MiB 时仅 1.02 倍）；需要状态写回占据主导才具有明显优势。

- **实验覆盖不足**：
  - 端到端实验未覆盖 RWKV6；
  - 未涉及多 GPU 张量并行/专家并行配置；
  - 生成长度仅 128 token，未测试长序列连续解码下的稳定性；
  - 未报告吞吐量（tokens/s）和服务质量（延迟分位数、SLO 满足率）指标。

- **部署层面的实际挑战**：append/merge 双队列引入调度复杂性，merge 步骤存在延迟尖峰（merge 步延迟高于普通步），对延迟敏感型服务可能不利；连续批处理下请求迁入迁出队列也增加了工程复杂度。

- **论文未讨论的潜在偏差风险**：
  - FLA 基线是否为该硬件上最优配置无法确认；
  - 未报告多次运行的标准差或置信区间，性能差异的统计显著性不明；
  - 内部实现细节（如 Triton 内核、CUDA 图捕获方式）的微小差异可能影响对比公平性。

**（完）**
