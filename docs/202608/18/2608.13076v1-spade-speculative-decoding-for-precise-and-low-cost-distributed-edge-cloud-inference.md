---
title: "SPADE: Speculative Decoding for Precise and Low Cost Distributed Edge Cloud Inference"
title_zh: SPADE：面向精确低成本的分布式边缘云推理的投机解码
authors: "Divya Jyoti Bajpai, Kishan Kumar Upadhyay, Manjesh Kumar Hanawal"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13076v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 面向LLM的边缘云联合投机解码框架
tldr: "大语言模型部署受限于高计算需求，边缘端小模型精度不足，云端大模型则产生昂贵的逐token计算成本。为此提出SPADE分布式推理框架，集成推测解码：边缘紧凑草稿模型快速生成候选token，云端大模型并行校验，接受则保留，拒绝才纠正，有效减少云查询次数。该即插即用设计将大部分计算转移至边缘，降低推理时延与云成本，同时保持大模型精度且无需重训练。实验表明，在SpecBench和CNN/Dailymail上云模型调用减少76%，零精度损失。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13076v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1538, \"height\": 579}, {\"url\": \"assets/figures/arxiv/2608.13076v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 660, \"height\": 486}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13076v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1322, \"height\": 610}, {\"url\": \"assets/tables/arxiv/2608.13076v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1321, \"height\": 462}]"
motivation: 边缘小模型精度不足，云端大模型逐token计算成本高昂，亟需兼顾精度与开销的推理方案。
method: 结合推测解码，边缘草稿模型生成候选token，云端验证模型并行校验，仅拒绝时纠正。
result: "在SpecBench和CNN/Dailymail两个数据集上的实验表明，本方法使云模型调用减少76%，且精度零损失。"
conclusion: SPADE即插即用、无需重训练，为低成本高精度边缘-云协同LLM推理提供了一条可行路径。
---

## 摘要
大型语言模型（LLM）在自然语言理解和生成方面取得了显著成功，但其部署受到高计算需求的制约。直接在边缘部署较小的LLM可以规避这一问题，但会降低准确性。在云端部署较小的强大LLM可以保持性能，但代价是昂贵的每令牌计算。我们提出了一种分布式推理框架\our{}，将投机解码（SD）集成到边缘和云中。部署在边缘的紧凑草稿模型快速生成候选令牌，而云上的大型验证模型并行验证这些令牌。接受的令牌被保留，只有拒绝时才触发验证器纠正，从而大幅减少云查询次数。我们的即插即用设计将大部分计算转移到边缘，显著降低了推理时间和云成本，并在无需任何重新训练的情况下保持了大型模型的准确性。我们的方法展示了在现实环境中实现可扩展、成本高效且准确的LLM部署的实用路径。使用SpecBench和CNN/Dailymail数据集进行的多个自然语言处理任务的实验结果表明，与完整模型相比，\our{}将云模型调用减少了76\%，且准确性零损失。

## Abstract
Large Language Models (LLMs) have achieved remarkable success in natural language understanding and generation, but their deployment is constrained by high computational demands. Deploying smaller LLMs directly on the edge can circumvent this, but with degraded accuracy. Deploying smaller cloud-based big LLMs preserves performance, but at the cost of expensive per-token computation. We present a distributed inference framework, \our{}, that integrates speculative decoding (SD) across edge and cloud. A compact draft model deployed on the edge generates candidate tokens rapidly, and a large verifier model on the cloud validates these tokens in parallel. Accepted tokens are retained, while only rejections trigger verifier correction, substantially reducing the number of cloud queries. Our plug-and-play design shifts the bulk of computation to the edge, significantly lowers inference time and cloud cost, and preserves the accuracy of the big model without any retraining requirement. Our approach demonstrates a practical path toward scalable, cost-efficient, and accurate deployment of LLMs in real-world environments. Experimental results across multiple Natural Language Processing tasks using SpecBench and CNN/Dailymail datasets demonstrate that \our{} reduces the cloud model calls by $76\%$ with zero loss in accuracy as compared to the full model.

---

## 论文详细总结（自动生成）

# SPADE：面向精确低成本的分布式边缘云推理的投机解码

## 一、核心问题与研究动机

### 背景与矛盾
- **LLM 部署的两难困境**：大语言模型（LLM）推理性能卓越，但计算资源需求极高。
- **纯边缘部署**：部署小模型在边缘设备上虽然可行，但精度明显下降，无法捕捉复杂语言模式。
- **纯云端部署**：大型模型在云端虽能保证最优质输出，但自回归生成中每个 token 都需要一次完整的前向传播，导致推理时延高、按 token 计费的经济成本昂贵。
- **核心问题**：如何在保持云端大模型精度的同时，大幅减少昂贵的云模型调用数量与推理成本？

### 研究意义
- 本文所解决的问题是 **LLM 在实际环境中规模化、低成本部署**的关键瓶颈，具有重要的系统层面应用价值。
- 提出了一条兼顾精度与效率的中间路线：**利用边缘设备承担主要生成计算，云端仅做选择性验证**。

## 二、方法论

### 核心思想
SPADE 是一种**分布式投机解码（Distributed Speculative Decoding）框架**，核心思路为：
- **边缘端**部署小型草稿模型（Draft Model）`M_q`，快速自回归生成候选 token 序列；
- **云端**部署大型验证模型（Verifier/Target Model）`M_p`，对整段候选 token 做并行一次性验证；
- 被验证接受的 token 直接保留；首个被拒绝的 token 由云端纠正后，草稿模型基于新上下文继续生成。

### 关键技术细节

**1. 验证接受准则（基于 Levyathan et al., 2023）**
- 设 `p(x)` 为目标模型 `M_p` 的分布，`q(x)` 为草稿模型 `M_q` 的分布；
- 候选 token `x ~ q(x)` 被接受的概率为：
  - **α(x) = min(1, p(x)/q(x))**
- 若被拒绝，则从修正分布中重新采样替代 token：
  - **p′(x) = norm(max(0, p(x) − q(x)))**
- 此机制保证最终输出与仅使用验证模型解码的分布**统计等价**，即零精度损失。

**2. 算法流程（Algorithm 1）**
1. 输入用户 prompt；
2. 边缘草稿模型自回归生成 `d` 个候选 token；
3. 将候选 block 发送至云端；
4. 云验证模型在单次前向传播中并行计算所有候选位置的分布；
5. 按接受准则逐 token 验证；
6. 接受最长连续前缀，拒绝首个 token 并从修正分布采样纠正；
7. 更新上下文，循环执行直到生成 `<eos>`。

**3. 草稿长度 d 的权衡**
- `d` 是关键的**超参数**，控制边缘计算与云端通信的平衡；
- `d` 过小 → 验证频率高、同步开销大，系统受限于通信；
- `d` 过大 → 拒绝概率升高、边缘计算浪费，收益递减；
- 实验中选择方式：在少量验证子集（约 10 个样本）上监控接受率，经验性选取最大化吞吐量的 `d` 值。

## 三、实验设计

### 数据集与 Benchmark
| 数据集 | 用途 | 说明 |
|---|---|---|
| **Spec-Bench** | 综合基准 | 包含 6 个子任务：多轮对话、摘要、翻译、检索增强生成、问答、数学推理；使用 LLM-as-a-judge（Gemini-2.5-Flash-Lite）在 1–5 Likert 量表上打分 |
| **CNN/DailyMail** | 文本摘要 | 使用 BLEU-1/4、ROUGE-1/L、CIDEr-D 等标准指标评估摘要质量 |

### 对比方法
| 方法 | 角色 |
|---|---|
| **Target Model**（完整云端大模型，上限基线） | LLaMA-3.2-1B 为草稿模型，LLaMA-3.1-8B 为目标模型 |
| **Draft Model**（仅边缘小模型，低精度基线） | 同上 |
| **SPADE（本文方法）** | 混合方案 |

### 效率指标
- 平均云端模型调用次数（Mean Target Model Calls）
- 平均吞吐量（tokens/s）
- 云端运行时间缩减倍数（Cloud runtime）

## 四、资源与算力

- **硬件配置**：文中有明确说明——
  - 边缘设备：NVIDIA RTX 3080 GPU（12 GB VRAM），代表高端边缘硬件；
  - 云端设备：NVIDIA RTX A6000 GPU（48 GB VRAM），代表工业级云基础设施；
- **训练时长**：**未提及**。由于 SPADE 是即插即用方案、无需训练/微调，因此也不涉及训练算力开销。

## 五、实验数量与充分性分析

### 实验规模
- **两大 benchmark 场景**：Spec-Bench（6 个 NLP 子任务）+ CNN/DailyMail 摘要任务；
- **对比了 3 种方法**：目标模型、草稿模型、SPADE；
- **效率三指标 + 质量多指标**：覆盖系统效率与输出质量两个维度；
- **d 值消融分析**：图 2 展示了草稿 token 数量 `d` 对目标模型调用次数的影响。

### 充分性评价
- **优点**：覆盖了多种 NLP 任务类型和独立数据集，实验结果呈现一致趋势，验证了方法的通用性；
- **不足**：
  1. 未能覆盖更广泛的模型规模组合（如不同规模的草稿/目标模型搭配）；
  2. 仅有 `d` 值一个超参数的分析，缺乏通信带宽、边缘算力差异等系统参数的影响实验；
  3. 数据集偏学术，未涉及真实生产环境的长序列、高并发场景；
  4. 未报告多次运行的标准差或置信区间。

## 六、主要结论与发现

### 核心结果
| 指标 | Spec-Bench | CNN/DailyMail |
|---|---|---|
| 云模型调用减少 | **77.4%** | **76%** |
| 总体性能 | 4.38 vs Target 4.45（差距极小） | CIDEr 甚至超过目标模型 |
| 云端运行时间 | 降至 0.23× | 降至 0.24× |
| 精度损失 | **零（统计等价）** | **几乎无损失** |

### 主要发现
1. **SPADE 在减少约 76% 云调用的情况下，实现了与完整云端模型接近零差距的性能**；
2. **两者精度差异仅为 0.07 分（Spec-Bench 5 分制），远优于纯边缘草稿模型（3.39）**；
3. **d 值增大一致性地降低目标模型调用次数**，且对齐良好的模型对放大该优势；
4. 框架**无需重训练**，可适用于现有任意草稿/验证模型对，即插即用。

## 七、方法优点

- **精度保证**：基于投机解码理论，保证输出分布与云端完整模型统计等价，零精度损失；
- **成本效益显著**：76% 云调用减少带来了直接的经济成本下降；
- **即插即用、通用性强**：无需训练、无需为特定数据集设计启发式路由规则，可泛化到不同任务；
- **架构合理**：将"多数计算在边缘、少数验证在云端"的负载分配方式直观且实用；
- **理论支撑**：分布式投机解码的分布保真性有严格的数学证明（引用 Levyathan 等的理论）。
- **兼顾性能与效率**：在性能和成本之间提供了灵活的权衡控制（通过参数 `d` 调节）。

## 八、不足与局限

### 实验层面的不足
1. **模型组合单一**：仅采用 LLaMA-3.2-1B ↔ LLaMA-3.1-8B 一种搭配，未验证不同精度差距的模型对/不同架构的通用性；
2. **硬件代表性有限**：RTX 3080 属于高端边缘设备，未评估常规移动设备（如手机、树莓派）上的表现；
3. **缺乏系统性消融**：仅分析了 `d` 值影响，未分析通信延迟、带宽限制等实际网络条件变化的影响；
4. **统计严谨性不足**：未报告多次重复实验的方差或置信区间，无法判断结果稳定性；
5. **实验指标覆盖面有限**：未评估端到端时延（含网络传输）、能耗等系统关键指标。

### 应用层面的局限
1. **依赖边缘-云端连接质量**：框架的有效性高度依赖边缘与云之间的通信开销；高延迟网络环境可能抵消验证次数减少的收益；
2. **草稿模型能力受限**：若草稿模型与目标模型分布差异过大，接受率低会导致大量纠正请求，效率优势可能消失；
3. **无流式保证**：框架假设边界生成的批式验证场景，未讨论流式交互场景的延迟敏感性；
4. **推理成本节省不等于总体成本最优**：长期运行时，边缘设备的能耗与维护成本未被纳入分析。

---

（完）
