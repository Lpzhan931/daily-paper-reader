---
title: "SPADE: Speculative Decoding for Precise and Low Cost Distributed Edge Cloud Inference"
title_zh: SPADE：用于精确且低成本的分布式边缘云推理的投机解码
authors: "Divya Jyoti Bajpai, Kishan Kumar Upadhyay, Manjesh Kumar Hanawal"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13076v1"
tags: ["query:llm"]
score: 9.0
evidence: 面向大语言模型高效推理的边云协同投机解码框架
tldr: "大语言模型(LLM)在边缘设备上部署受限于资源导致精度不足，而基于云的大模型则面临高额逐token计算开销，因此需要兼顾精度与成本。提出SPADE分布式推理框架，结合投机解码，在边缘运行紧凑草稿模型快速生成候选token流，云端大模型并行验证这些token，仅当被拒绝时才进行纠正，从而极大减少云查询次数。在多个NLP任务（如SpecBench、CNN/Dailymail）上，SPADE将云模型调用降低76%，并实现零精度损失。作为即插即用方案，无需任何重训练，为可扩展、经济高效的LLM边缘-云协同推理提供了实用路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13076v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1538, \"height\": 579, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13076v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 660, \"height\": 486, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13076v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1322, \"height\": 610, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13076v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1321, \"height\": 462, \"label\": \"Table\"}]"
motivation: 大型语言模型在边缘部署精度不足，云端部署成本高昂，需要兼顾精度与开销的解决方案。
method: 提出SPADE框架，利用投机解码，边缘草稿模型快速生成候选token，云端大模型并行验证，仅拒绝时纠正。
result: "在自然语言处理基准数据集上，相比完整模型，云模型调用减少76%，且精度零损失。"
conclusion: 该框架即插即用，无需重训练，为低成本、高精度的边缘云推理提供实用方案。
---

## 摘要
大型语言模型（LLM）在自然语言理解和生成方面取得了显著成功，但其部署受到高计算需求的制约。直接在边缘部署较小的LLM可以规避这一问题，但会降低准确性。在云端部署较小的基于云的大型模型可保持性能，但代价是昂贵的逐token计算。我们提出了一种分布式推理框架\our{}，它在边缘和云端集成了投机解码（SD）。部署在边缘的紧凑草稿模型快速生成候选token，而云端的大型验证模型并行验证这些token。被接受的token被保留，只有被拒绝的token才触发验证器修正，从而大幅减少云端查询次数。我们的即插即用设计将大部分计算转移到边缘，显著降低了推理时间和云端成本，并在无需任何重新训练的情况下保持了大型模型的准确性。我们的方法展示了一条在真实环境中实现可扩展、成本效益高且准确部署LLM的实用路径。使用SpecBench和CNN/Dailymail数据集进行的多项自然语言处理任务的实验结果表明，与完整模型相比，\our{}将云端模型调用减少了76\%，且准确性零损失。

## Abstract
Large Language Models (LLMs) have achieved remarkable success in natural language understanding and generation, but their deployment is constrained by high computational demands. Deploying smaller LLMs directly on the edge can circumvent this, but with degraded accuracy. Deploying smaller cloud-based big LLMs preserves performance, but at the cost of expensive per-token computation. We present a distributed inference framework, \our{}, that integrates speculative decoding (SD) across edge and cloud. A compact draft model deployed on the edge generates candidate tokens rapidly, and a large verifier model on the cloud validates these tokens in parallel. Accepted tokens are retained, while only rejections trigger verifier correction, substantially reducing the number of cloud queries. Our plug-and-play design shifts the bulk of computation to the edge, significantly lowers inference time and cloud cost, and preserves the accuracy of the big model without any retraining requirement. Our approach demonstrates a practical path toward scalable, cost-efficient, and accurate deployment of LLMs in real-world environments. Experimental results across multiple Natural Language Processing tasks using SpecBench and CNN/Dailymail datasets demonstrate that \our{} reduces the cloud model calls by $76\%$ with zero loss in accuracy as compared to the full model.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机与背景）

- **核心矛盾**：大型语言模型（LLM）性能优异，但计算和内存开销巨大。  
  - 在边缘设备直接部署小模型：成本低、延迟低，但输出质量明显下降。  
  - 在云端部署大规模模型：精度高，但逐 token 的自回归生成需要持续消耗昂贵算力，推理时间和成本居高不下。
- **关键问题**：如何设计一种分布式推理机制，既能保持云端大模型的输出准确性，又能大幅降低云端计算负担和成本？
- **本文思路**：将**投机解码（Speculative Decoding, SD）** 引入边缘–云协同推理，让边缘小模型负责大部分生成计算，云端大模型仅做并行验证和必要修正，从而在精度和成本之间取得良好平衡。

## 2. 提出的方法论

### 2.1 核心思想
- SPADE 采用“草稿模型 + 验证模型”的双模型结构：
  - **边缘端（草稿模型 \(M_q\)）**：快速生成一段候选 token 序列。
  - **云端（验证模型 \(M_p\)）**：对候选序列并行验证，接受正确 token，拒绝并纠正错误 token。
- 大部分计算负载转移到边缘，云端只需在必要时介入，从而减少昂贵的大模型调用次数。

### 2.2 关键技术细节与流程
- **草稿生成**：边缘模型 \(M_q\) 自回归生成 \(d\) 个候选 token \([y_{t+1}, \dots, y_{t+d}]\)。
- **并行验证**：云端模型 \(M_p\) 在单次前向传播中并行评估这些 token，并得到对应的概率分布 \(p(\cdot)\)。
- **接受/拒绝判据**：对于草稿分布 \(q(x)\) 采样的 token \(x\)，接受概率为  
  \[
  \alpha(x) = \min\left(1, \frac{p(x)}{q(x)}\right)
  \]
- **拒绝修正**：若 token 被拒绝，则从调整分布  
  \[
  p'(x) = \text{norm}\left(\max(0, p(x) - q(x))\right)
  \]  
  中采样一个替代 token，以保证最终输出分布与完整云端模型一致。
- **迭代推进**：接受最长前缀 token，修正第一个拒绝 token，更新上下文，继续下一轮草稿生成，直到生成 `<eos>`。
- **理论保证**：该方法保证最终生成序列与仅使用云端大模型解码在统计上等价，且每轮至少新增一个 token，确保生成进程不中断。

### 2.3 超参数 \(d\)
- \(d\) 表示每轮边缘生成的草稿 token 数，控制计算与通信的平衡：
  - 过小：验证频繁、同步开销大，系统受通信限制；
  - 过大：草稿计算成本增加，拒绝率可能升高。
- 论文建议将 \(d\) 视为超参数，在小规模验证集上通过观测接受率来经验性选取。

## 3. 实验设计

### 3.1 数据集与基准
- **Spec-Bench**：包含 6 个子任务（多轮对话、翻译、摘要、问答、数学推理、检索增强生成）。
- **CNN/DailyMail**：用于摘要生成的经典数据集。

### 3.2 对比方法
- **目标模型（Target Model）**：完整云端大模型（仅云部署），视为质量上界。
- **草稿模型（Draft Model）**：轻量边缘模型（仅边缘部署），视为效率上界。
- **SPADE（本文方法）**：边缘草稿 + 云端验证的混合推理方案。

### 3.3 评估指标
- Spec-Bench：使用 Gemini-2.5-Flash-Lite 作为自动裁判，从正确性、指令遵循、完整性、清晰连贯性、简洁性、事实性六个维度评分（1–5 分）。
- CNN/DailyMail：BLEU-1、BLEU-4、ROUGE-1、ROUGE-L、CIDEr-D。
- 效率指标：平均云模型调用次数、平均吞吐量（tokens/s）、云运行时缩减比例。

## 4. 资源与算力

- **边缘设备**：NVIDIA RTX 3080（12 GB 显存），代表高端边缘硬件。
- **云端设备**：NVIDIA RTX A6000（48 GB 显存），代表典型工业级推理基础设施。
- **模型配置**：
  - 草稿模型：LLaMA-3.2-1B（边缘）；
  - 验证模型：LLaMA-3.1-8B（云端）。
- **训练成本**：论文明确表示 **无需任何重新训练**，SPADE 是即插即用方案，因此未报告训练时长或算力。

## 5. 实验数量与充分性

- **实验组数**：
  - 一个综合基准（Spec-Bench）含 6 个子任务；
  - 一个摘要数据集（CNN/DailyMail）；
  - 一个针对草稿长度 \(d\) 的参数分析（图 2）。
  - 总体对比了 3 种方法（目标模型、草稿模型、SPADE）。
- **充分性评价**：
  - **优点**：任务覆盖较广，涵盖生成、理解、推理、摘要等典型 NLP 场景；指标既有自动质量评测，也有 LLM-as-judge 评测，能较全面反映效果。
  - **不足**：
    - 仅使用一对模型组合（1B 草稿 + 8B 验证），缺乏不同模型规模下的泛化验证；
    - 未与其他分布式推理方法（如 Neurosurgeon、SplitEE 等）直接对比；
    - 未考虑真实网络传输延迟、带宽波动等实际分布式环境因素；
    - \(d\) 的消融实验仅在一个数据集上展示，未给出最佳 d 的选择细节或多种任务下的表现；
    - LLM-as-judge 本身可能存在偏见，且未提供人工评测一致性验证。

## 6. 主要结论与发现

- **效率大幅提升**：
  - Spec-Bench：云模型调用次数从 133.25 降至 30.16（减少约 77.4%），云运行时缩减至 0.23×；
  - CNN/DailyMail：云调用次数从 127.30 降至 30.79（减少约 76%），云运行时缩减至 0.24×。
- **质量几乎无损**：
  - Spec-Bench 总体评分：目标模型 4.45，SPADE 4.38，仅下降约 1.6%；
  - CNN/DailyMail 上各指标（BLEU、ROUGE 等）均与目标模型接近，且 CIDEr-D 甚至略高于目标模型。
- **结论**：SPADE 能以极小性能折损换取显著的云端成本降低，实现高精度、低开销的边缘–云协同 LLM 推理。

## 7. 优点

- **即插即用**：无需重新训练或微调任何模型，可直接部署。
- **精度有理论保障**：基于投机解码的接受/拒绝机制，保证输出分布与完整云端模型统计一致。
- **成本可量化降低**：大幅减少云模型调用次数，直接降低按调用计费的云服务成本。
- **架构清晰实用**：将计算下沉到边缘，兼顾了精度、延迟和成本三方面需求。
- **实验评价较全面**：使用多任务基准和经典摘要数据集，既有自动指标也有 LLM 裁判评价。

## 8. 不足与局限

- **模型组合单一**：仅验证了 1B/8B 一种组合，未测试更大或更小模型对，缺乏对方法通用性的证明。
- **缺少真实网络模拟**：实验未考虑边缘与云之间的通信延迟、带宽限制或丢包，实际部署中同步成本可能显著影响收益。
- **对比基线不足**：未与已有的分布式推理工作（层切分、早退、动态路由等）进行对比，难以判断相对优势。
- **超参数分析有限**：对草稿长度 \(d\) 的分析仅一个数据集，且未分析不同任务下最佳 \(d\) 的变化规律；过于依赖接受率这一经验指标。
- **评测偏差风险**：LLM-as-judge 可能无法完全等价于人类偏好，缺少人工评估或与人类标注的一致性检验。
- **应用范围局限**：当前实验集中于文本生成与摘要，未扩展到代码生成、多模态任务等更复杂场景。

（完）
