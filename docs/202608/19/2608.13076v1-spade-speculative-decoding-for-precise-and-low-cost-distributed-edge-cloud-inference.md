---
title: "SPADE: Speculative Decoding for Precise and Low Cost Distributed Edge Cloud Inference"
title_zh: SPADE：面向边缘云分布式推理的精确低成本投机解码
authors: "Divya Jyoti Bajpai, Kishan Kumar Upadhyay, Manjesh Kumar Hanawal"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13076v1"
tags: ["query:llm"]
score: 9.0
evidence: 面向大语言模型的投机解码以加速推理
tldr: "大型语言模型（LLM）在自然语言理解与生成上表现卓越，但其计算需求高，部署受限。直接在边缘运行小模型会降低精度，而云端大模型Per-token计算昂贵。我们提出SPADE：一种跨边缘与云的分布式推理框架，在边缘部署紧凑草稿模型快速生成候选token，云端大模型并行验证，仅当token被拒绝时才触发修正，从而大幅减少云查询次数。在SpecBench和CNN/Dailymail多个NLP任务中，SPADE减少76%云端模型调用，精度与完整大模型完全一致，零损失。该方案即插即用、无需重训练，将主要计算负载转移到边缘，有效降低推理时延和云成本，为LLM规模化低成本准确部署提供了实用路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13076v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1538, \"height\": 579}, {\"url\": \"assets/figures/arxiv/2608.13076v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 660, \"height\": 486}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13076v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1322, \"height\": 610}, {\"url\": \"assets/tables/arxiv/2608.13076v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1321, \"height\": 462}]"
motivation: 边缘小模型精度不足，云端大模型推理成本高，需一种兼顾精度与成本的分布式推理方法。
method: 在边缘部署紧凑草稿模型生成候选token，云端大模型并行验证，仅拒绝时纠正，减少云查询。
result: "在SpecBench和CNN/Dailymail上云模型调用降低76%，精度与大模型持平，零损失。"
conclusion: SPADE无需重训练即插即用，显著降低推理时间与云成本，实现可扩展的高效准确部署。
---

## 摘要
大型语言模型（LLMs）在自然语言理解和生成方面取得了显著成功，但其部署受到高计算需求的制约。直接在边缘部署较小的LLM可以规避这一问题，但会降低准确性。在云端部署较小的大型模型可以保持性能，但代价是昂贵的逐令牌计算。我们提出了一种分布式推理框架\our{}，它在边缘和云之间集成了投机解码（SD）。部署在边缘的紧凑草稿模型快速生成候选令牌，而云端的大型验证模型并行验证这些令牌。接受的令牌被保留，只有被拒绝的令牌触发验证器修正，从而大幅减少云端查询次数。我们的即插即用设计将大部分计算转移到边缘，显著降低了推理时间和云成本，并在无需任何重训练的情况下保持了大模型的准确性。我们的方法展示了在真实环境中实现可扩展、高性价比且准确部署LLM的实用路径。在多个自然语言处理任务上使用SpecBench和CNN/Dailymail数据集的实验结果表明，与完整模型相比，\our{}在零精度损失的情况下将云模型调用减少了76%。

## Abstract
Large Language Models (LLMs) have achieved remarkable success in natural language understanding and generation, but their deployment is constrained by high computational demands. Deploying smaller LLMs directly on the edge can circumvent this, but with degraded accuracy. Deploying smaller cloud-based big LLMs preserves performance, but at the cost of expensive per-token computation. We present a distributed inference framework, \our{}, that integrates speculative decoding (SD) across edge and cloud. A compact draft model deployed on the edge generates candidate tokens rapidly, and a large verifier model on the cloud validates these tokens in parallel. Accepted tokens are retained, while only rejections trigger verifier correction, substantially reducing the number of cloud queries. Our plug-and-play design shifts the bulk of computation to the edge, significantly lowers inference time and cloud cost, and preserves the accuracy of the big model without any retraining requirement. Our approach demonstrates a practical path toward scalable, cost-efficient, and accurate deployment of LLMs in real-world environments. Experimental results across multiple Natural Language Processing tasks using SpecBench and CNN/Dailymail datasets demonstrate that \our{} reduces the cloud model calls by $76\%$ with zero loss in accuracy as compared to the full model.

---

## 论文详细总结（自动生成）

# SPADE：面向边缘云分布式推理的精确低成本投机解码

## 1. 论文的核心问题与整体含义

- **研究动机**：大型语言模型（LLM）性能优异但计算开销巨大，在资源受限的边缘设备上难以直接部署。若直接使用边缘小模型，精度会显著下降；若完全依赖云端大模型，则每生成一个 token 都需要昂贵的云端计算，导致推理成本高昂。
- **核心矛盾**：如何在保持大模型精度的前提下，尽可能减少云端计算调用、降低推理时延和成本？
- **整体意义**：论文提出了一种基于**投机解码（Speculative Decoding, SD）**的分布式边缘–云推理框架 SPADE，让边缘设备承担大部分生成计算，云端仅做并行验证与必要修正，从而在**零精度损失（相对云上完整模型）**的前提下大幅降低云成本，为 LLM 的规模化、低成本部署提供了实用路径。

## 2. 论文提出的方法论

### 核心思想
- 采用**双模型结构**：
  - **边缘草稿模型（Draft Model）**：轻量化 LLM，在边缘设备上快速、自回归地生成候选 token 序列。
  - **云端验证模型（Verifier / Target Model）**：大型高精度 LLM，在云端单次前向传播中并行验证边缘生成的候选 tokens。
- 过程分为**草稿生成**与**云端验证**两个阶段：
  1. 边缘模型生成 `d` 个候选 token；
  2. 将候选序列发送至云端，云模型一次性并行计算所有候选位置的预测分布；
  3. 按投机解码接受准则保留被接受的 token，若遇到首个被拒绝 token，则从修正分布中采样替换，更新上下文后继续下一轮。

### 关键技术细节
- **接受准则**：设草稿模型分布为 \( q(x) \)，云模型分布为 \( p(x) \)，则候选 token 以概率
  \[
  \alpha(x) = \min\left(1, \frac{p(x)}{q(x)}\right)
  \]
  被接受。
- **拒绝修正**：若被拒绝，则从修正分布
  \[
  p'(x) = \operatorname{norm}\left(\max(0, p(x) - q(x))\right)
  \]
  中采样替换 token，保证最终生成序列与云模型单独解码在分布上**统计等价**（理论保证来源于 Leviathan et al.）。
- **算法流程（Algorithm 1）**：
  1. 输入提示词，初始化输出序列；
  2. 循环直到生成 `<eos>`：
     - 边缘模型生成 `d` 个草稿 token；
     - 发送到云端，云端在单个 forward pass 中并行验证 `d+1` 个位置；
     - 接受最长连续前缀，拒绝处用修正分布采样纠正；
     - 将接受与纠正的 tokens 拼接到输出，更新上下文；
  3. 返回最终生成序列。
- **超参数 `d`**：草稿 token 数量。过小会增加同步开销，过大则可能降低接受率、浪费边缘计算。论文将其视为超参数，通过经验验证（约 10 个样本）选择一个在吞吐量与接受率之间平衡的值。

## 3. 实验设计

### 数据集 / Benchmark
- **Spec-Bench**：专为投机解码设计的基准，包含 6 个子任务：
  - 多轮对话
  - 翻译
  - 摘要
  - 问答
  - 数学推理
  - 检索增强生成（RAG）
- **CNN/DailyMail**：新闻摘要数据集，用于评估摘要生成质量。

### 模型配置
- **边缘草稿模型**：LLaMA-3.2-1B
- **云端验证模型**：LLaMA-3.1-8B
- **基线**：
  - **Target Model**：云端完整模型（性能上界）
  - **Draft Model**：边缘轻量模型（效率上界）
  - **SPADE**：论文提出的方法

### 评估指标
- **Spec-Bench**：使用 Gemini-2.5-Flash-Lite 作为自动评判器，对生成结果按 1–5 分打分，从正确性、指令遵循、完整性、清晰连贯性、简洁性、事实性六个维度评估。
- **CNN/DailyMail**：标准文本生成指标：
  - BLEU-1 / BLEU-4（n-gram 精度）
  - ROUGE-1 / ROUGE-L（召回与序列重叠）
  - CIDEr-D（TF-IDF 加权语义相关性）
- **效率指标**：
  - 平均云模型调用次数（越低越好）
  - 平均吞吐量（tokens/s，越高越好）
  - 云端运行时间相对倍数（越低越好）

## 4. 资源与算力

- 论文中给出了推理阶段的硬件配置：
  - **边缘设备**：NVIDIA RTX 3080 GPU，12 GB 显存
  - **云端设备**：NVIDIA RTX A6000 GPU，48 GB 显存
- **未说明**：
  - 未报告训练时长（本方法无需训练，属于即插即用）；
  - 未报告具体实验次数、GPU 数量、总运行时间等详细算力数据；
  - 未涉及通信带宽、延迟的实际测量。

## 5. 实验数量与充分性

### 实验数量
- 共两大组实验：
  1. Spec-Bench 上的多功能评测（6 个子任务）
  2. CNN/DailyMail 上的摘要质量评测
- 每组实验均对比了三种模型（Target、Draft、SPADE），并报告了性能与效率指标。
- 针对超参数 `d`，有一个额外分析（Fig. 2），展示 `d` 与云模型调用次数的关系。

### 充分性与客观性评估
- **优点**：覆盖多种 NLP 任务，评测指标多样，效率指标有意义；采用与投机解码理论匹配的 Spec-Bench，具有一定针对性。
- **不足**：
  - 没有与其他同类分布式推理方法（如 Neurosurgeon、SplitEE、DIMEE 等）进行对比，无法体现 SPADE 在已有方法中的相对优势；
  - 缺少消融实验（如不同边缘/云模型组合、不同 `d` 值对精度与效率的完整影响）；
  - 仅使用 LLaMA 系列一种模型组合，泛化性未验证；
  - 评测依赖 LLM-as-a-judge，可能有自动评测偏差；
  - 没有考虑真实网络传输延迟，实际端–云场景下的通信成本可能对收益有影响。

## 6. 论文的主要结论与发现

- **性能保持**：SPADE 在 Spec-Bench 上整体得分为 4.38，接近完整目标模型的 4.45，而边缘草稿模型仅 3.39。
- **成本大幅降低**：
  - Spec-Bench：平均云模型调用次数从 133.25 降至 30.16，减少约 **77.4%**；
  - CNN/DailyMail：平均云模型调用次数从 127.30 降至 30.79，减少约 **76%**；
  - 云端运行时间分别降至原来的 **0.23×** 和 **0.24×**。
- **摘要质量**：CNN/DailyMail 上各项指标（BLEU、ROUGE、CIDEr）与完整模型接近，显著优于边缘草稿模型；CIDEr 甚至超过完整模型。
- **`d` 的影响**：增加草稿 token 数 `d` 会减少云调用次数，前提是草稿与目标模型分布较为对齐；若对齐较差，则接受率下降，云调用会增加。
- **总体结论**：SPADE 能在保持接近云端完整模型性能的同时，显著降低云端计算成本，且无需重训练，是实用、可扩展的分布式推理方案。

## 7. 优点

- **理论保证**：基于投机解码，最终输出分布与云端完整模型等价，不存在精度妥协。
- **即插即用**：无需重新训练或微调，可直接替换现有部署。
- **成本效益显著**：平均减少约 76% 的云调用，同时吞吐量高于完整云模型。
- **通用性好**：不依赖数据集特定启发式，适用于多种 NLP 任务。
- **架构清晰**：算法流程明确，接受/拒绝准则公式化，易于实现和扩展。
- **平衡设计**：将大量计算卸载到边缘，保留了边缘设备的利用率，适合资源受限场景。

## 8. 不足与局限

- **对比基线有限**：未与其他分布式推理方法（如分层拆分、早退、复杂度路由等）进行对比，无法证明其相对于现有方法的优越性。
- **模型规模单一**：仅使用 1B/8B 的 LLaMA 组合，未验证更大的目标模型或不同架构下的有效性。
- **未考虑通信开销**：实验未模拟真实网络延迟、带宽限制，实际云边通信可能削弱收益。
- **超参数 `d` 选择依赖经验**：文中只是简单提到通过验证集选择，未给出稳定、自动化的确定方法。
- **评测偏差风险**：使用 LLM 自动评判（Gemini-2.5）可能存在主观偏差，且未与人类评价一致性进行验证。
- **实验规模较小**：缺乏多数据集、多任务的大规模测试，也缺少对失败案例或分布不匹配情况的分析。
- **资源信息不完整**：未报告训练/推理耗时、能耗、具体 GPU 数量等，成本估算不够全面。

（完）
