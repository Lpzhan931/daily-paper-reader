---
title: How Lossless Is Lossless Speculative Decoding? The Role of Numerical Precision in Orthrus
title_zh: 无损投机解码究竟有多无损？——数值精度在 Orthrus 中的作用
authors: "Ilya Koziev, Leonid Sinev, Ivan Oseledets"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15504v1"
tags: ["query:llm-sd"]
score: 8.0
evidence: 检验无损投机解码主张与数值精度
tldr: "Orthrus通过冻结自回归主干并行生成多token来加速推理，并声称其模型内共识机制可实现无损投机解码。本文独立复现并在不同数值精度下检验该主张。BF16下作者检查点在1190个提示中仅45%轨迹完全匹配，自训模型为43%，且匹配概率与参考模型响应条件困惑度强相关；FP32下则全部匹配。结果表明其实际无损性依赖数值精度，轨迹等价性应与下游任务表现分开评估。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.15504v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1411, \"height\": 635, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1061, \"height\": 530, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1420, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 628, \"height\": 527, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1098, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1124, \"height\": 170, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1410, \"height\": 353, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1406, \"height\": 207, \"label\": \"Table\"}]"
motivation: Orthrus声称无损投机解码，但该主张是否在不同数值精度下成立尚缺乏独立验证。
method: 独立复现Orthrus，在BF16与FP32下对作者检查点及自训模型，用12领域1190个提示评估轨迹完全匹配，并关联参考模型困惑度。
result: "BF16下作者检查点与自训模型匹配率仅45%和43%，匹配概率与参考模型困惑度相关；FP32下所有提示均完全匹配，下游基准未见系统性退化。"
conclusion: Orthrus的实际无损性取决于数值精度，轨迹完全等价与下游任务性能需分别评估。
---

## 摘要
Orthrus 是一种混合自回归-扩散架构，它通过并行生成多个 token 来加速自回归语言模型推理，同时使用一个冻结的自回归骨干网络。其核心主张是，一种模型内共识机制能够实现无损投机解码，产生与自回归模型相同的输出序列。我们独立复现了 Orthrus，并在不同数值精度下检验这一主张。在 BF16 推理下，对于作者的检查点和我们独立训练的模型，在来自 12 个领域的 1,190 个提示上，精确轨迹匹配分别仅出现在 45% 和 43% 的情况下。精确匹配的概率也与参考模型的响应条件困惑度密切相关。尽管存在这种轨迹偏差，Orthrus 在下游 lm-eval-harness 基准上并未表现出系统性退化。相反，使用 FP32 重复轨迹评估时，所有被评估提示均实现了精确轨迹匹配。这些结果表明，Orthrus 的实际无损性取决于数值精度，并且精确轨迹等价性应与下游任务性能分开评估。

## Abstract
Orthrus is a hybrid autoregressive-diffusion architecture that accelerates autoregressive language-model inference by generating multiple tokens in parallel while using a frozen autoregressive backbone. Its central claim is that an intra-model consensus mechanism enables lossless speculative decoding, producing the same output sequence as the autoregressive model.   We independently reproduce Orthrus and examine this claim under different numerical precisions. Under BF16 inference, exact trajectory matching occurs in only 45% of cases for the authors' checkpoint and 43% for our independently trained model across 1,190 prompts from 12 domains. The probability of exact matching is also strongly associated with the response-conditional perplexity of the reference model. Despite this trajectory divergence, Orthrus does not show systematic degradation on downstream lm-eval-harness benchmarks. In contrast, repeating the trajectory evaluation with FP32 yields exact trajectory matching on all evaluated prompts.   These results show that the practical losslessness of Orthrus depends on numerical precision and that exact trajectory equivalence should be evaluated separately from downstream task performance.

---

## 论文详细总结（自动生成）

# 论文总结：How Lossless Is Lossless Speculative Decoding? The Role of Numerical Precision in Orthrus

## 1. 核心问题与整体含义
- **研究动机**：自回归（AR）语言模型解码本质上是串行的，生成 N 个 token 通常需要 N 次前向计算，KV Cache 反复访问，限制硬件利用率和推理效率。
- **Orthrus 背景**：Orthrus 是一种混合自回归-扩散架构，保留冻结的 AR 骨干网络，同时加入轻量扩散视图，并行预测多个未来 token，再通过模型内共识机制验证。作者声称该过程是**严格无损**的，即输出序列与原始 AR 模型完全一致。
- **核心问题**：在有限精度数值计算的实际实现中，Orthrus 的“无损”是否真的成立？数值精度（BF16 vs FP32）是否会影响轨迹完全等价性？
- **整体含义**：论文并非否定 Orthrus 的加速价值，而是指出“无损”声明需要更精确的操作定义：算法上可能保持预期 AR 计算，但有限精度下可能产生不同离散输出。轨迹等价性应与下游任务表现分开评估。

## 2. 方法论：核心思想与关键技术细节
- **独立复现**：作者独立实现 Orthrus 的架构、训练和推理流程，并开发可配置训练框架，用于系统研究训练目标、数据分布和超参数。
- **训练数据构造**：
  - 使用公开 HuggingFace 数据集的 prompt。
  - 用冻结的 `Qwen/Qwen3-1.7B` 对每个 prompt 做贪心解码，生成响应，形成 teacher-generated、on-policy 蒸馏语料。
  - 只保留 prompt 长度在 50–1,000 字符之间的样本。
  - 最终数据集包含 **4,113,358** 条 prompt–response 样本。
- **训练参数**：
  - 8 张 NVIDIA H100 GPU；CUDA 13.3.73、PyTorch 2.13.0、Transformers 5.8.0。
  - 1 epoch；初始学习率 \(2\times10^{-4}\)；batch size 10；交叉熵损失。
  - block size 8；number of blocks 32；最大序列长度 3,072 tokens。
- **推理与评估设置**：
  - 评估环境：Python 3.10.12、PyTorch 2.8.0+cu128、CUDA 12.8、Transformers 5.8.1。
  - 单张 NVIDIA GeForce RTX 3090（23 GB，compute capability 8.6）。
  - 使用 BF16 与 FP32 两种精度；eager attention。
  - 生成参数：`max_new_tokens=128`、`do_sample=False`、`temperature=0.0`，即全部贪心解码。
- **关键度量**：
  - **轨迹完全匹配率**：Orthrus 生成序列是否与参考 AR 模型逐 token 完全一致。
  - **Tokens Per Forward（TPF）**：衡量并行 token 生成效率。
  - **响应条件困惑度**：
    \[
    \mathrm{PPL}_{Qwen}(y|x)=\exp\left(-\frac{1}{|y|}\sum_{t=1}^{|y|}\log p_{Qwen}(y_t|x,y_{<t})\right)
    \]
  - **Logistic 回归**：检验 PPL 与精确匹配的关系，控制响应长度和领域：
    \[
    \mathrm{logit}\,P(Y=1)=\beta_0+\beta_1\log \mathrm{PPL}(y|x)+\beta_2 L+\gamma_D
    \]
    其中 \(Y=1\) 表示轨迹完全匹配。

## 3. 实验设计：数据集、Benchmark 与对比方法
- **轨迹评估数据集**：12 个文本领域，每个领域 100 个 prompt，`gec-en` 为 90 个，总计 **1,190** 个 prompt。
  - 领域包括：`code`、`code-ru`、`creative-en`、`gec-en`、`gec-ru`、`math`、`math-ru`、`poetry-en`、`poetry-ru`、`qa`、`qa-ru`、`wmt ru-en`。
  - 覆盖代码生成、故事生成、语法纠错、数学、诗歌、问答、机器翻译等，含英语和俄语。
- **对比模型**：
  - 作者发布的检查点：`chiennv/Orthrus-Qwen3-1.7B`。
  - 作者独立训练的模型：`Orthrus-1.7B-final`。
  - 参考自回归模型：`Qwen/Qwen3-1.7B`。
- **评估内容**：
  - BF16 下的轨迹匹配率，按总体和领域分解。
  - FP32 下的轨迹匹配率，作为精度对照。
  - TPF 对比，跨 12 个领域。
  - 匹配与分歧轨迹的响应条件 PPL 对比。
  - Logistic 回归分析 PPL 与精确匹配的关联。
  - 下游基准：GSM8K、HumanEval、IFEval，使用 `lm-eval-harness`。
- **主要对比维度**：
  - 作者检查点 vs 自训模型。
  - Orthrus vs 原始 Qwen3 AR 基线。
  - BF16 vs FP32。
  - 轨迹级等价性 vs 下游任务分数。

## 4. 资源与算力
- **训练算力**：
  - 使用 **8 张 NVIDIA H100 GPU**。
  - 软件：CUDA 13.3.73、PyTorch 2.13.0、Transformers 5.8.0。
  - 训练配置：1 epoch、batch size 10、最大序列长度 3,072 tokens。
  - 训练数据规模：4,113,358 条 prompt–response 样本。
- **评估算力**：
  - 使用 **1 张 NVIDIA GeForce RTX 3090（23 GB）**。
  - 软件：Python 3.10.12、PyTorch 2.8.0+cu128、CUDA 12.8、Transformers 5.8.1。
- **未明确说明**：
  - 论文未给出具体训练时长、总 GPU 小时、能耗或训练总计算量。
  - 也未详细说明数据生成阶段使用多少算力或耗时。

## 5. 实验数量与充分性
- **实验规模**：
  - 训练了 1 个独立 Orthrus 模型。
  - 轨迹评估覆盖 12 个领域、1,190 个 prompt。
  - 对 2 个 Orthrus 模型（作者检查点、自训模型）分别进行 BF16 与 FP32 评估。
  - TPF 对比覆盖 12 个领域。
  - 下游评估覆盖 3 个 benchmark：GSM8K、HumanEval、IFEval。
  - 进行了总体与分领域匹配率统计、PPL 分组对比、Logistic 回归。
- **充分性优点**：
  - 独立复现并训练模型，降低仅依赖作者检查点的偏差。
  - 跨 12 个领域，包含多语言任务，覆盖较广。
  - BF16 与 FP32 对照直接支撑核心结论。
  - Logistic 回归控制响应长度和领域，统计上较严谨。
- **局限**：
  - 基础模型仅为 Qwen3-1.7B，规模较小，结论能否推广到更大模型未知。
  - 每个领域仅 90–100 个 prompt，置信区间较宽。
  - 未系统做多种子、多硬件、多精度（FP16、TF32、量化）消融。
  - 未定位导致数值偏差的具体层或算子。
  - 下游 benchmark 点估计差异未达统计显著，不能证明“无退化”。
  - 作者检查点的原始训练细节未知，公平对比存在一定限制。

## 6. 主要结论与发现
- **BF16 下并非严格无损**：
  - 作者检查点在 1,190 个 prompt 中轨迹完全匹配率为 **45%**，分歧率 55%。
  - 自训模型匹配率为 **43%**，分歧率 57%。
- **FP32 下轨迹完全匹配**：
  - 两个 Orthrus 模型在全部 1,190 个 prompt 上均实现 **100%** 精确轨迹匹配。
  - 说明 BF16 下的轨迹偏差主要来自有限精度数值效应。
