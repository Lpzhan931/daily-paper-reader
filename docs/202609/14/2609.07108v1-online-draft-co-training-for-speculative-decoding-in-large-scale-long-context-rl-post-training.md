---
title: "Online Draft Co-Training for Speculative Decoding in Large-Scale, Long-Context RL Post-Training"
title_zh: 大规模长上下文 RL 后训练中面向投机解码的在线草稿协同训练
authors: "Zili Wang, Zhaopeng Qiu, Yuekai Zhang, Shuang Yu, Junjie Lai"
date: 2026-09-07
pdf: "https://arxiv.org/pdf/2609.07108v1"
tags: ["query:llm"]
score: 8.0
evidence: 面向投机解码的在线草稿协同训练系统，加速大模型生成
tldr: 面向大规模长上下文 RL 后训练，rollout 生成是主要开销，在线草稿协同训练可提升推测解码草稿准确率，却受 CP 分支注意力和 PP 目标特征跨阶段传输限制。本文提出端到端系统：CP 侧扩展 packed 负载均衡 zigzag ring attention，融合分支注意力与因果主序列注意力；PP 侧用 TapChannel 独立通道跨阶段传输目标特征且不改变流水线调度。实验在最高 122B 模型上让协同草稿紧跟策略基线，带来显著 rollout 与端到端加速，并在 256K token 下实现强扩展和显著显存节省。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.07108v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1714, \"height\": 571}, {\"url\": \"assets/figures/arxiv/2609.07108v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1493, \"height\": 532}, {\"url\": \"assets/figures/arxiv/2609.07108v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1752, \"height\": 794}, {\"url\": \"assets/figures/arxiv/2609.07108v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1761, \"height\": 451}, {\"url\": \"assets/figures/arxiv/2609.07108v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 861, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2609.07108v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 864, \"height\": 486}, {\"url\": \"assets/figures/arxiv/2609.07108v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 862, \"height\": 273}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.07108v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1781, \"height\": 384}, {\"url\": \"assets/tables/arxiv/2609.07108v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 845, \"height\": 253}]"
motivation: 推测解码可加速 RL 后训练中占主导的 rollout，但在大模型长上下文下，在线草稿协同训练受 CP 不支持分支注意力和 PP 目标特征跨阶段限制。
method: CP 侧扩展负载均衡 zigzag ring attention，融合分支注意力与因果主序列注意力；PP 侧用 TapChannel 独立通道传输中间目标特征，不改变流水线调度。
result: 在最高 122B 模型上，协同草稿紧跟策略基线，实现显著 rollout 和端到端加速；CP 在 256K token 下强扩展并节省显存，PP 传输开销适中。
conclusion: 该工作为大规模长上下文 RL 后训练的在线草稿协同训练提供了可行端到端方案，兼顾并行扩展、显存与流水线效率。
---

## 摘要
投机解码可加速 rollout 生成，而 rollout 生成在强化学习（RL）后训练成本中占主导地位。在线协同训练可进一步提升草稿模型的准确率，从而带来更大的加速。然而，将该方法扩展到长上下文大模型上的协同训练面临两个障碍：(1) 标准因果上下文并行（CP）实现不支持分支注意力；(2) 目标特征跨越流水线并行（PP）阶段。我们通过一个面向大规模在线草稿协同训练的端到端系统解决这两个问题。针对 CP，我们扩展了打包式、负载均衡的 zigzag ring attention，将 rank 局部的分支注意力与因果主序列注意力合并。针对 PP，TapChannel 通过独立路径跨阶段传输中间目标特征，且不影响流水线调度。实验表明，协同训练的草稿模型能够紧密跟踪策略基线，同时在高达 122B 的模型规模上带来显著的 rollout 和端到端加速。我们的 CP 设计在 256K token 下实现了强扩展，并相比先前工作显著节省内存，而我们的 PP 传输仅带来适度开销。代码见：https://github.com/NVIDIA-NeMo/RL/issues/3698。

## Abstract
Speculative decoding accelerates rollout generation, which dominates the cost of reinforcement learning (RL) post-training. Online co-training can further increase the draft's accuracy, yielding greater speedups. However, scaling this approach to co-training on large models with long contexts poses two obstacles: (1) branch attention is unsupported by standard causal context-parallel (CP) implementations, and (2) target features span across pipeline-parallel (PP) stages. We address both with an end-to-end system for large-scale online draft co-training. For CP, we extend packed, load-balanced zigzag ring attention by merging rank-local branch attention with causal main-sequence attention. For PP, TapChannel transports intermediate target features across stages via a separate path, leaving the pipeline schedule unaffected. Experiments demonstrate that co-trained drafts closely track the policy baseline while delivering substantial rollout and end-to-end speedups across model scales up to 122B. Our CP design achieves strong scaling at 256K tokens with significant memory savings over prior work, and our PP transport incurs modest overhead. Code can be found at https://github.com/NVIDIA-NeMo/RL/issues/3698.

---

## 论文详细总结（自动生成）

# 论文总结：大规模长上下文 RL 后训练中面向投机解码的在线草稿协同训练

## 1. 核心问题与整体含义

- **研究背景**：在 LLM 的强化学习（RL）后训练中，rollout 生成通常占据主要墙钟时间。投机解码（Speculative Decoding, SD）通过“小草稿模型并行提议多个 token + 目标策略并行验证”来加速自回归生成，且不改变目标模型输出分布。
- **核心动机**：固定草稿模型会随 RL 策略演化而逐渐失配，在线草稿协同训练可提升草稿接受长度与 rollout 加速比。但将该方法扩展到**大模型、长上下文 RL 后训练**时面临两个系统障碍：
  - 标准因果上下文并行（CP）不支持 EAGLE-3、DFlash、DSpark 等先进草稿架构所需的**分支注意力**。
  - 在流水线并行（PP）下，草稿所需的目标模型中间特征可能位于远端 PP 阶段，标准相邻阶段通信无法直接传递。
- **整体含义**：论文提出一套端到端系统，在不改变目标策略原有 CP/PP 并行拓扑的前提下，使在线草稿协同训练可用于大规模、长上下文 RL 后训练。系统集成进 NeMo-RL，支持最高 122B 规模模型，并在 256K token 长上下文下验证 CP 扩展性和 PP 低开销。

## 2. 方法论

### 2.1 在线草稿协同训练总体框架

- 设策略参数为 \(\theta\)，草稿参数为 \(\phi\)。草稿在策略 rollout 生成的同一批 token 上训练，目标特征 \(H_\theta(x)\) 使用 stop-gradient。
- 联合目标为：
  \[
  L(\theta,\phi)=L_{RL}(\theta)+\lambda L_{draft}(\phi;x,\mathrm{sg}(H_\theta(x)))
  \]
- 草稿模型作为策略最后一个 PP 阶段的子模块，与策略模型联合更新。
- 核心设计原则：**不修改目标策略的并行布局，而是让系统适配草稿训练需求**。

### 2.2 CP 下的分支注意力：packed zigzag-ring attention

- 标准因果 CP 不支持分支结构。论文将每个分支 query 的注意力拆成两个 key 集合：
  - **主序列因果前缀**：按 CP rank 分片，使用 packed、负载均衡的 zigzag ring attention 计算。
  - **分支局部 key**：保留在拥有该分支 anchor 的 rank 上，本地计算，无需通信。
- 两部分独立计算后，通过在线 softmax 归并：
  \[
  \ell=\log(e^{\ell_m}+e^{\ell_b}),\quad
  O=e^{\ell_m-\ell}O_m+e^{\ell_b-\ell}O_b
  \]
  其中 \((O_m,\ell_m)\) 为主序列部分，\((O_b,\ell_b)\) 为分支局部部分。
- 该机制统一支持：
  - **EAGLE-3**：Training-Time Test，多个 TTT 分支，每步关注不断增长的草稿上下文。
  - **DFlash**：块扩散式并行生成整块 token。
  - **DSpark**：在 DFlash 上增加轻量 Markov head，恢复块内因果依赖。
- 通信与重叠：
  - 每个 rank 前向环通信量约为
    \[
    V^{CP}_{fwd}=2(C-1)\frac{N}{C}d_{kv}b
    \]
    其中 \(C\) 为 CP 度，\(N\) 为主序列 token 数，\(d_{kv}\) 为 K/V 宽度，\(b\) 为每元素字节数。
  - 分支局部 K/V 不参与环通信，因此通信成本与分支数量、深度无关。
  - 反向复用同一 K/V ring，无额外分支相关流量。
  - 通信与注意力按 ring step 粒度重叠，暴露开销为
    \[
    E(r)_{CP}=\max\left(\frac{t^{(r)}_{comm}}{t^{(r)}_{
