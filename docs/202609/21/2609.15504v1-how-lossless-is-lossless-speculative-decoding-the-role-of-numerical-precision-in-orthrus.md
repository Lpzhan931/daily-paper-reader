---
title: How Lossless Is Lossless Speculative Decoding? The Role of Numerical Precision in Orthrus
title_zh: 无损投机解码究竟有多无损？数值精度在 Orthrus 中的作用
authors: "Ilya Koziev, Leonid Sinev, Ivan Oseledets"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15504v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 检验投机解码的无损声明与验证接受行为
tldr: "Orthrus以冻结自回归骨干并行生成多token，加速语言模型推理，并声称模型内共识可实现无损投机解码。本文独立复现并在1,190条、12领域提示上比较数值精度影响：BF16下作者检查点仅45%、自训模型仅43%轨迹精确匹配，且匹配率与参考模型响应条件困惑度强相关；FP32下则全部匹配。尽管轨迹分歧，下游lm-eval-harness基准未见系统退化。结果表明实际无损性依赖数值精度，精确轨迹等价应与下游性能分开评估。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.15504v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1411, \"height\": 635, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1061, \"height\": 530, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1420, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 628, \"height\": 527, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1098, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1124, \"height\": 170, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1410, \"height\": 353, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1406, \"height\": 207, \"label\": \"Table\"}]"
motivation: Orthrus宣称模型内共识可实现无损投机解码，但其无损性在不同数值精度下是否成立尚缺独立验证。
method: "独立复现Orthrus，在1,190条、12领域提示上比较BF16与FP32推理的轨迹精确匹配率，并对照下游lm-eval-harness基准。"
result: "BF16下作者检查点仅45%、自训模型仅43%轨迹精确匹配，且匹配率与参考模型响应条件困惑度强相关；FP32下全部提示均精确匹配，下游基准无系统退化。"
conclusion: Orthrus的实际无损性依赖数值精度，精确轨迹等价应独立于下游任务性能评估。
---

## 摘要
Orthrus 是一种混合自回归-扩散架构，它通过并行生成多个 token，同时使用冻结的自回归主干来加速自回归语言模型推理。其核心主张是，一种模型内共识机制能够实现无损投机解码，产生与自回归模型相同的输出序列。我们独立复现了 Orthrus，并在不同数值精度下检验了这一主张。在 BF16 推理下，对于作者提供的检查点和我们独立训练的模型，在来自 12 个领域的 1,190 个提示上，精确轨迹匹配分别仅出现在 45% 和 43% 的情况下。精确匹配的概率也与参考模型的响应条件困惑度密切相关。尽管存在这种轨迹偏差，Orthrus 在下游 lm-eval-harness 基准上并未表现出系统性退化。相反，使用 FP32 重复轨迹评估时，在所有评估提示上都获得了精确轨迹匹配。这些结果表明，Orthrus 的实际无损性取决于数值精度，并且精确轨迹等价性应与下游任务性能分开评估。

## Abstract
Orthrus is a hybrid autoregressive-diffusion architecture that accelerates autoregressive language-model inference by generating multiple tokens in parallel while using a frozen autoregressive backbone. Its central claim is that an intra-model consensus mechanism enables lossless speculative decoding, producing the same output sequence as the autoregressive model.   We independently reproduce Orthrus and examine this claim under different numerical precisions. Under BF16 inference, exact trajectory matching occurs in only 45% of cases for the authors' checkpoint and 43% for our independently trained model across 1,190 prompts from 12 domains. The probability of exact matching is also strongly associated with the response-conditional perplexity of the reference model. Despite this trajectory divergence, Orthrus does not show systematic degradation on downstream lm-eval-harness benchmarks. In contrast, repeating the trajectory evaluation with FP32 yields exact trajectory matching on all evaluated prompts.   These results show that the practical losslessness of Orthrus depends on numerical precision and that exact trajectory equivalence should be evaluated separately from downstream task performance.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **核心问题**：Orthrus 是一种混合自回归-扩散架构，利用冻结的自回归（AR）骨干作为上下文表示，并用轻量扩散视图并行预测多个未来 token，再通过模型内共识机制验证，从而加速推理。作者声称该过程是“严格无损”的投机解码，即输出序列与原始 AR 模型完全一致。
- **论文动机**：作者独立复现 Orthrus，检验“无损”声明在有限精度实现中是否真正成立。关键背景是：即使算法在理想算术下等价，BF16/FP32 等有限精度计算也可能改变下一个 token 的选择，而生成是离散过程，微小数值差异可能导致轨迹分叉。
- **整体含义**：论文并不否定 Orthrus 的加速价值，而是指出“无损”需要更精确的操作定义。实际无损性可能依赖数值精度，且**轨迹级等价**应与**下游任务性能**分开评估。

## 2. 方法论：核心思想与关键技术细节

- **独立复现 Orthrus**：作者实现 Orthrus 架构与训练流程，并构建可配置训练框架，用于研究训练目标、数据分布和超参数。
- **训练数据构造**：
  - 使用冻结的 `Qwen/Qwen3-1.7B` 作为教师模型。
  - 从 HuggingFace 公开数据集中取 prompt，用 Qwen3-1.7B 贪心解码生成 response。
  - 仅保留长度在 50–1,000 字符之间的 prompt。
  - 最终得到 **4,113,358 条 prompt–response 样本**。
  - 动机是：扩散组件在 teacher forcing 下训练，若使用通用人类续写数据，可能无法复现冻结 AR 模型自身生成时遇到的状态；因此采用教师生成、on-policy 风格的蒸馏语料。
- **训练配置**：
  - 1 个 epoch；
  - 初始学习率 \(2 \times 10^{-4}\)；
  - batch size 10；
  - 交叉熵损失；
  - block size 8；
  - number of blocks 32；
  - 最大序列长度 3,072 token。
- **评估指标**：
  - **Tokens Per Forward, TPF**：衡量并行 token 生成效率。
  - **轨迹精确匹配率**：比较 Orthrus 与参考 AR 模型在贪心解码下的完整 token 序列是否一致。
  - **响应条件困惑度**：
    \[
    \mathrm{PPL}_{\mathrm{Qwen}}(y|x)=\exp\left(-\frac{1}{|y|}\sum_{t=1}^{|y|}\log p_{\mathrm{Qwen}}(y_t|x,y_{<t})\right)
    \]
  - **逻辑回归**：
    \[
    \mathrm{logit}\,P(Y=1)=\beta_0+\beta_1\log \mathrm{PPL}(y|x)+\beta_2 L+\gamma_D
    \]
    其中 \(Y=1\) 表示精确轨迹匹配，\(L\) 为响应长度，\(\gamma_D\) 为领域效应，用于检验困惑度与精确匹配的关联是否在控制长度和领域后仍显著。

## 3. 实验设计

- **评估提示集**：
  - 共 **1,190 条 prompt**，来自 **12 个文本领域**。
  - 每个领域通常 100 条 prompt，`gec-en` 为 90 条。
  - 领域包括：`code`、`code-ru`、`creative-en`、`gec-en`、`gec-ru`、`math`、`math-ru`、`poetry-en`、`poetry-ru`、`qa`、`qa-ru`、`wmt ru-en`。
  - 数据源包括 HuggingFace 数据集、MERA、LORuGEC、WMT19 等。
- **对比模型**：
  - 参考自回归模型：`Qwen3-1.7B`。
  - 作者发布检查点：`chiennv/Orthrus-Qwen3-1.7B`。
  - 作者独立训练模型：`Orthrus-1.7B-final`。
- **生成设置**：
  - BF16 推理，`eager` attention；
  - `max_new_tokens=128`；
  - `do_sample=False`；
  - `temperature=0.0`，即贪心解码。
- **主要实验类型**：
  - TPF 对比：作者检查点 vs 自训模型，覆盖 12 个领域。
  - BF16 轨迹匹配：总体匹配率、分域匹配率、分歧轨迹率。
  - 响应条件困惑度分析：匹配轨迹 vs 分歧轨迹的 PPL 对比。
  - 逻辑回归：控制长度和领域后，检验 PPL 与精确匹配的关联。
  - 下游 benchmark：使用 `lm-eval-harness` 评估 GSM8K、HumanEval、IFEval。
  - FP32 重复轨迹评估：与 BF16 形成对照。

## 4. 资源与算力

- **训练资源**：
  - 使用 **8 张 NVIDIA H100 GPU**。
  - 软件环境：CUDA 13.3.73、PyTorch 2.13.0、Transformers 5.8.0。
  - 论文未明确说明训练时长、总 GPU 小时数或能耗。
- **评估资源**：
  - 使用 **NVIDIA GeForce RTX 3090，23 GB 显存，compute capability 8.6**。
  - 软件环境：Python 3.10.12、PyTorch 2.8.0+cu128、CUDA 12.8、Transformers 5.8.1。
- **总体**：算力信息部分明确，但训练时长和完整成本未披露。

## 5. 实验数量与充分性

- **实验规模**：
  - 12 个领域 × 约 100 条 prompt，共 1,190 条轨迹。
  - 两个 Orthrus 模型在 BF16 和 FP32 下分别评估。
  - TPF 表覆盖 12 个领域。
  - 分域轨迹匹配表覆盖 12 个领域 × 2 个模型。
  - 逻辑回归对两个模型分别进行。
  - 下游 benchmark 覆盖 3 个任务 × 3 个模型。
- **充分性**：
  - 优点在于同时使用作者检查点和独立训练模型，并对比 BF16/FP32，增强了结论稳健性。
  - 使用相同 prompt、相同贪心解码参数，并控制响应长度和领域，统计设计较合理。
  - 但实验主要围绕 Qwen3-1.7B 单一规模，未覆盖更大模型、其他架构或不同精度格式。
  - 下游 benchmark 的提升或下降均未达到统计显著，因此不能据此判断真实能力变化。
  - 作者检查点与自训模型在训练数据和超参数上差异较大，TPF 对比不是严格消融，更多是补充验证。

## 6. 主要结论与发现

- **BF16 下并非严格无损**：
  - 作者检查点精确轨迹匹配率仅 **45%**。
  - 自训模型精确轨迹匹配率仅 **43%**。
  - 分歧轨迹率分别为 **55%** 和 **57%**。
- **匹配率具有领域差异**：
  - 例如 `gec-en` 匹配率较高，约 88%；
  - `creative-en`、`poetry-en`、`poetry-ru` 等生成自由度较高的领域匹配率较低。
- **困惑度与精确匹配强相关**：
  - 分歧轨迹的响应条件 PPL 更高。
  - 逻辑回归中，`log PPL` 系数显著为负：
    - 作者检查点：\(\beta_1=-8.10\)，95% CI \([-10.50,-5.71]\)，\(p=3\times10^{-11}\)；
    - 自训模型：\(\beta_1=-10.92\)，95% CI \([-13.60,-8.24]\)，\(p=1.3\times10^{-15}\)。
  - 说明参考模型认为更“意外”或更复杂的响应，更容易出现轨迹分歧。
- **下游 benchmark 未系统退化**：
  - 在 GSM8K、HumanEval、IFEval 上，Orthrus 未表现出一致性下降。
  - 自训模型在三个 benchmark 上点估计甚至高于 Qwen3 基线，但差异不显著。
  - 因此，benchmark 分数相同或更高不能证明 token 级轨迹等价。
- **FP32 下完全匹配**：
  - 在全部 1,190 条 prompt 上，两个 Orthrus 模型均实现 **100% 精确轨迹匹配**，分歧率为 0。
  - 表明 BF16 下的轨迹分歧主要来自有限精度数值效应，而非共识机制在算法层面失效。
- **核心结论**：
  - Orthrus 的“实际无损性”依赖数值精度。
  - 对“无损”语言模型加速的声明，应同时说明**操作等价标准**和**数值精度**。
  - 精确轨迹等价应独立于下游任务性能进行评估。

## 7. 优点

- **独立复现与双模型验证**：不仅评估作者发布的检查点，还独立训练同容量模型，降低单一实现偶然性。
- **轨迹级评估**：直接比较 token 序列，而非仅看困惑度或 benchmark 分数，能更严格检验“无损”声明。
- **数值精度对照清晰**：BF16 与 FP32 形成强对照，FP32 下 100% 匹配有力支持数值精度解释。
- **统计分析较规范**：使用置信区间、分域分析和逻辑回归，并控制响应长度和领域。
- **区分两个层面**：将“轨迹精确等价”与“下游任务性能”明确分开，指出 benchmark 不能替代逐 token 等价验证。
- **对实践
