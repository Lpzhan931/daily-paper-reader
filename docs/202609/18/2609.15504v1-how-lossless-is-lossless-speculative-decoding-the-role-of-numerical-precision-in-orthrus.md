---
title: How Lossless Is Lossless Speculative Decoding? The Role of Numerical Precision in Orthrus
title_zh: 无损投机解码究竟有多无损？数值精度在Orthrus中的作用
authors: "Ilya Koziev, Leonid Sinev, Ivan Oseledets"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15504v1"
tags: ["query:llm-sd"]
score: 8.0
evidence: 复现无损投机解码声明，检验验证精确性
tldr: "Orthrus 宣称通过模型内共识实现无损推测解码，能与自回归模型生成完全相同的输出序列。作者独立复现该架构，并在 BF16 与 FP32 精度下对比轨迹匹配率。在 12 个领域 1190 条提示上，BF16 仅约 43%–45% 轨迹完全匹配，且与参考模型困惑度相关；FP32 则全部匹配。结果表明其无损性依赖数值精度，轨迹等价性应与下游任务性能分开评估。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.15504v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1411, \"height\": 635, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1061, \"height\": 530, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1420, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 628, \"height\": 527, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1098, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1124, \"height\": 170, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1410, \"height\": 353, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15504v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1406, \"height\": 207, \"label\": \"Table\"}]"
motivation: Orthrus 声称其共识机制可无损加速自回归解码，但该无损性在有限数值精度下是否成立尚缺独立验证。
method: 独立复现 Orthrus，在 BF16 与 FP32 下用 12 领域 1190 条提示比较生成轨迹，并关联参考模型困惑度与下游基准。
result: "BF16 下作者检查点仅 45%、自训模型 43% 轨迹完全匹配；FP32 下全部匹配，且下游 lm-eval 基准无系统性下降。"
conclusion: Orthrus 的实际无损性依赖数值精度，轨迹等价性应与下游任务性能分开评估。
---

## 摘要
Orthrus是一种混合自回归-扩散架构，它通过并行生成多个token来加速自回归语言模型推理，同时使用冻结的自回归主干。其核心主张是，一种模型内共识机制能够实现无损投机解码，产生与自回归模型相同的输出序列。我们独立复现了Orthrus，并在不同数值精度下检验这一主张。在BF16推理下，对于作者的检查点和我们独立训练的模型，在来自12个领域的1,190个提示上，精确轨迹匹配分别仅发生在45%和43%的情况下。精确匹配的概率也与参考模型的响应条件困惑度密切相关。尽管存在这种轨迹偏差，Orthrus在lm-eval-harness下游基准上并未表现出系统性退化。相比之下，使用FP32重复轨迹评估时，所有被评估提示均实现了精确轨迹匹配。这些结果表明，Orthrus的实际无损性取决于数值精度，并且精确轨迹等价性应与下游任务性能分开评估。

## Abstract
Orthrus is a hybrid autoregressive-diffusion architecture that accelerates autoregressive language-model inference by generating multiple tokens in parallel while using a frozen autoregressive backbone. Its central claim is that an intra-model consensus mechanism enables lossless speculative decoding, producing the same output sequence as the autoregressive model.   We independently reproduce Orthrus and examine this claim under different numerical precisions. Under BF16 inference, exact trajectory matching occurs in only 45% of cases for the authors' checkpoint and 43% for our independently trained model across 1,190 prompts from 12 domains. The probability of exact matching is also strongly associated with the response-conditional perplexity of the reference model. Despite this trajectory divergence, Orthrus does not show systematic degradation on downstream lm-eval-harness benchmarks. In contrast, repeating the trajectory evaluation with FP32 yields exact trajectory matching on all evaluated prompts.   These results show that the practical losslessness of Orthrus depends on numerical precision and that exact trajectory equivalence should be evaluated separately from downstream task performance.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：自回归（AR）语言模型解码本质上是顺序的，生成 N 个 token 通常需要 N 次前向计算，KV Cache 反复访问导致推理效率受限。扩散式并行解码可同时预测多个未来 token，但可能偏离原 AR 分布。
- **背景对象**：Orthrus 是一种混合自回归-扩散架构，保留冻结的 AR 主干，并增加轻量扩散视图；推理时 AR 构建上下文，扩散组件并行预测多个 token，再通过“模型内共识机制”验证。作者原论文声称该过程是**严格无损投机解码**，即生成序列与原始 AR 模型完全一致。
- **核心问题**：这种“无损”声明在有限数值精度实现中是否成立？论文独立复现 Orthrus，并在 BF16 与 FP32 下直接比较其生成轨迹与参考 AR 模型的 token 序列。
- **整体含义**：论文表明 Orthrus 的“实际无损性”高度依赖数值精度：BF16 下轨迹大量分歧，FP32 下则完全匹配。因此，“无损”需要更精确的操作定义，且**轨迹等价性应与下游任务性能分开评估**。

## 2. 论文提出的方法论

- **核心思想**：不直接信任 Orthrus 的“无损”声明，而是通过独立实现训练与推理，在相同提示、相同贪心解码设置下，逐 token 比较 Orthrus 与冻结 AR 参考模型 Qwen3-1.7B 的输出轨迹。
- **独立训练流程**：
  - 使用 HuggingFace 公开数据集的 prompt，过滤为 50–1,000 字符。
  - 用冻结的 Qwen3-1.7B 对每个 prompt 进行贪心解码，记录其生成响应，形成**教师生成、on-policy 的蒸馏语料**，共 4,113,358 条 prompt–response 样本。
  - 训练扩散视图，损失为交叉熵；关键超参：1 epoch、初始学习率 2×10⁻⁴、batch size 10、block size 8、block 数 32、最大序列长度 3,072。
- **推理与评估流程**：
  - 使用贪心解码：`max_new_tokens=128`、`do_sample=False`、`temperature=0.0`。
  - BF16 下使用 `attn_implementation="eager"`；FP32 下重复相同评估。
  - 定义“精确轨迹匹配”为 Orthrus 生成的 token 序列与 Qwen3-1.7B 参考序列完全一致。
- **统计建模**：
  - 计算参考模型的响应条件困惑度：
    \[
    \text{PPL}_{\text{Qwen}}(y|x)=\exp\left(-\frac{1}{|y|}\sum_{t=1}^{|y|}\log p_{\text{Qwen}}(y_t|x,y_{<t})\right)
    \]
  - 拟合逻辑回归：
    \[
    \text{logit }P(Y=1)=\beta_0+\beta_1\log\text{PPL}(y|x)+\beta_2 L+\gamma_D
    \]
    其中 \(Y=1\) 表示精确轨迹匹配，\(L\) 为响应长度，\(\gamma_D\) 为领域效应；负 \(\beta_1\) 表示困惑度越高，越不容易精确匹配。

## 3. 实验设计

- **评估数据/场景**：
  - 12 个文本领域，每域 100 条提示，`gec-en` 为 90 条，共 **1,190 条提示**。
  - 领域包括：代码生成（英/俄）、创意写作、语法纠错（英/俄）、数学（英/俄）、诗歌（英/俄）、问答（英/俄）、俄英机器翻译。
- **Benchmark 与指标**：
  - **Tokens Per Forward（TPF）**：衡量并行 token 生成效率。
  - **轨迹匹配率 / 分歧率**：直接比较生成序列与 Qwen3-1.7B 的 token 轨迹。
  - **响应条件困惑度 PPL**：分析轨迹分歧与参考模型不确定性的关系。
  - **下游任务**：使用 lm-eval-harness 评估 GSM8K、HumanEval、IFEval。
- **对比方法/模型**：
  - 参考模型：`Qwen/Qwen3-1.7B` 自回归模型。
  - 作者发布的检查点：`chiennv/Orthrus-Qwen3-1.7B`。
  - 论文独立训练的模型：`Orthrus-1.7B-final`。
- **精度设置**：
  - BF16 推理下进行主轨迹评估。
  - FP32 推理下重复同一轨迹评估。
- **主要实验组**：
  - TPF 跨 12 域比较（表 2）。
  - BF16 总体轨迹匹配率（表 3）与分域匹配率（表 4）。
  - 匹配/分歧轨迹的 PPL 比较（表 5）。
  - 逻辑回归分析（表 6）。
  - 下游 lm-eval 基准比较（表 7）。
  - FP32 轨迹匹配统计（表 8）。

## 4. 资源与算力

- **训练算力**：
  - 使用 **8 张 NVIDIA H100 GPU**。
  - 软件环境：CUDA 13.3.73、
