---
title: "Multi-Access Speculative Inference: Uplink or Downlink?"
title_zh: 多接入推测推理：上行还是下行？
authors: "Chang Cai, Kaibin Huang"
date: 2026-08-30
pdf: "https://arxiv.org/pdf/2608.29618v1"
tags: ["query:llm"]
score: 9.0
evidence: 面向边缘网络的LLM投机推理，涉及小模型草稿与大模型验证，是LLM高效推理加速的核心主题。
tldr: 多接入投机推理中，设备端草稿token被服务端大模型拒绝时需传输全词表分布，现有服务器端纠正导致上行瓶颈。本文提出通信模式选择新维度，让设备自适应切换上行或下行纠正模式，以平衡上行限制与共享下行资源。文中揭示模式选择的简单最优结构，并设计贪心算法联合调整草稿长度与功率。在Qwen2.5和DeepSeek-R1模型对上，该框架显著提升token有效吞吐量，缓解了边缘协同生成的通信压力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.29618v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 326}, {\"url\": \"assets/figures/arxiv/2608.29618v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 925, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2608.29618v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 625, \"height\": 592}, {\"url\": \"assets/figures/arxiv/2608.29618v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 655, \"height\": 1507}, {\"url\": \"assets/figures/arxiv/2608.29618v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 794, \"height\": 524}, {\"url\": \"assets/figures/arxiv/2608.29618v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 674, \"height\": 522}, {\"url\": \"assets/figures/arxiv/2608.29618v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 876, \"height\": 378}, {\"url\": \"assets/figures/arxiv/2608.29618v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 877, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2608.29618v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 892, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2608.29618v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 892, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2608.29618v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 889, \"height\": 376}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.29618v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 916, \"height\": 540}, {\"url\": \"assets/tables/arxiv/2608.29618v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 666, \"height\": 229}]"
motivation: 现有Multi-SPIN在服务器端纠正被拒token，需上传全词表草案分布，造成上行链路瓶颈，影响多设备协作效率。
method: 提出上下行通信模式选择，设备可下载目标分布本地纠正，并联合优化模式选择、草稿长度与功率分配。
result: 在Qwen2.5和DeepSeek-R1模型对上，该框架显著提高token goodput，优于固定上行模式方案。
conclusion: 将通信模式选择纳入多接入投机推理设计，可有效缓解上行瓶颈，为边缘协同生成提供新优化维度。
---

## 摘要
多接入推测推理（Multi-SPIN）将SPIN扩展到多设备边缘网络，以加速协作式令牌生成。它允许设备端的小语言模型（SLM）为各个生成任务自回归地草拟多个令牌，而边缘服务器上的大语言模型（LLM）则并行验证这些令牌。主要的通信开销出现在草拟令牌被服务器拒绝的情况下，此时采样修正令牌需要同时访问SLM输出的草拟分布和LLM输出的目标分布在完整令牌词汇表上的信息。现有设计通常通过上传草拟分布来在服务器端执行修正，但传输词汇表范围内的分布会造成关键的上行（UL）瓶颈。另一种方案是，利用下行（DL）链路可用的高传输速率，通过下载目标分布在设备端执行修正。基于这一见解，我们将通信模式选择引入Multi-SPIN作为新的设计维度。具体而言，每个设备可以在UL和DL模式之间自适应切换，以平衡上行瓶颈与共享下行资源约束，从而减轻整体通信负担。我们构建了一个联合考虑模式选择、草拟长度控制和功率分配的令牌总吞吐量最大化问题。对于模式选择，我们揭示了一个简单的优化结构，能够高效地搜索上行设备数量，并相应地优化其发射功率。对于草拟长度控制，我们开发了一种贪心搜索算法，使设备特定的草拟长度适应异构的计算与通信能力。在Qwen2.5和DeepSeek-R1模型对上的实验结果表明，所提出的框架显著提高了令牌吞吐量。

## Abstract
Multi-access speculative inference (Multi-SPIN) extends SPIN to multi-device edge networks to accelerate cooperative token generation. It allows on-device small language models (SLMs) to autoregressively draft multiple tokens for individual generation tasks, while an edge-server large language model (LLM) verifies them in parallel. The major communication overhead arises when a drafted token is rejected by the server, in which case sampling the correction token requires access to both the SLM-output draft distribution and the LLM-output target distribution over the full token vocabulary. Existing designs typically perform correction at the server by uploading the draft distribution, but transmitting a vocabulary-wide distribution creates a critical uplink (UL) bottleneck. Alternatively, the correction can be performed at the device by downloading the target distribution, leveraging the high transmission rates available on the downlink (DL). Motivated by this insight, we introduce communication-mode selection as a new design dimension for Multi-SPIN. Specifically, each device can adaptively switch between the UL and DL modes to balance the UL bottleneck against the shared DL resource constraint, thereby relieving the overall communication burden. We formulate a sum-token-goodput maximization problem that jointly accounts for mode selection, draft-length control, and power allocation. For mode selection, we reveal a simple optimal structure that enables efficient search over the number of UL devices, with the corresponding transmit powers optimized accordingly. For draft-length control, we develop a greedy-search algorithm that adapts device-specific draft lengths to heterogeneous computation and communication capabilities. Experimental results on Qwen2.5 and DeepSeek-R1 model pairs demonstrate that the proposed framework significantly improves token goodput.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：将大语言模型（LLM）部署在无线网络边缘，可实现低时延、隐私保护的 AI 服务，但设备端算力有限而服务器端集中式推理面临瓶颈。推测推理（Speculative Inference, SPIN）通过在设备端部署轻量级小语言模型（SLM）进行本地草稿生成，再让服务器端 LLM 并行验证草稿块，将串行令牌生成转化为并行验证，显著降低服务器计算开销。
- **核心问题**：SPIN 引入了一个新的通信瓶颈——当草稿令牌被拒绝时，修正令牌的采样需要同时访问 SLM 的草稿分布和 LLM 的目标分布在完整词汇表上的信息。现代 LLM 词汇表规模庞大（如 Qwen2.5 有 152,064 条），传输这些高维分布会造成严重的通信开销（称为"令牌修正开销"）。
- **现有方案的局限**：已有研究主要采用上行（UL）方式在服务器端执行修正，即设备上传草稿分布到服务器，但上行速率受限会形成瓶颈。虽有压缩、量化等手段，但仍可能主导端到端延迟。另一种下行（DL）方式——下载目标分布到设备端修正——在多设备系统中可能造成共享下行资源过载。
- **核心研究问题**：在多设备边缘网络中的 Multi-SPIN（多接入推测推理）中，如何在 UL 模式的有限上行速率与 DL 模式的共享下行资源约束之间做出自适应权衡？这正是论文题目"Uplink or Downlink?"所指向的根本问题。
- **整体含义**：论文首次将**通信模式选择**（UL/DL mode selection）作为 Multi-SPIN 的新设计维度，将资源利用的理念从计算域扩展到通信域，使系统能够自适应地利用上下行资源，缓解共享下行瓶颈。

### 2. 论文提出的方法论

**核心思想**：
- 提出基于模式选择的 Multi-SPIN 协议。每个设备在令牌生成轮次开始时由服务器确定草稿长度，设备本地 SLM 自回归生成草稿并上传令牌–概率对，服务器并行验证。验证后分两类情况处理：
  - **全部接受**：服务器采样奖励令牌发送给设备，通信开销可忽略。
  - **存在拒绝**：根据通信模式选择，设备可选择 UL 模式（上传草稿分布至服务器端修正）或 DL 模式（下载目标分布至设备端修正）。
- 整体建模为**两阶段随机优化问题**：前摄的草稿长度决策（验证结果揭晓前）+ 反应的模式选择与功率分配（验证结果揭晓后），目标为最大化期望和令牌有效吞吐量（expected sum token goodput）。

**关键技术细节**：

- **问题分解**：利用序贯决策结构，无损地将联合问题分解为两个子问题：
  - **子问题一（反应式模式选择与功率分配）**：对每个验证结果，最小化通信延迟。
  - **子问题二（前摄式草稿长度控制）**：将已优化的反应式决策纳入所有可能的验证结果中权衡。

- **最优功率分配（引理 1）**：
  - UL 模式设备以最大功率发射使延迟最小；
  - DL 模式设备在服务器总功率约束下，通过**最大延迟均衡**求得闭式解：\( p_k^\star = \frac{N_{0,k}^{dev}B_k}{|h_k|^2}\left(2^{\frac{V_{eff}(\lceil\log_2 V\rceil+Q)}{B_k \tau^{dl}}} - 1\right) \)，其中 \(\tau^{dl}\) 为等式 \( \sum_{k\in D} \frac{N_{0,k}^{dev}B_k}{|h_k|^2}\left(2^{\frac{V_{eff}(\lceil\log_2 V\rceil+Q)}{B_k \tau^{dl}}} - 1\right) = P^{dl} \) 的唯一解（可通过二分搜索求得）。

- **最优模式选择的前缀结构（引理 2 与命题 1）**：
  - 按最小 UL 延迟对拒绝设备排序后，最优模式分配具有**前缀结构**：UL 延迟较小的设备分配 UL 模式，其余设备分配 DL 模式。
  - 由此将组合优化问题化简为一维二分搜索：计算 DL–UL 延迟差 \(\Delta\tau(n) = \tau^{dl}(n) - \tau^{ul}(n)\)，找到首个 \(\Delta\tau(n)\le 0\) 的位置 \(\bar{n}\)，最优分割点只需在 \(\{\bar{n}-1, \bar{n}\}\) 中比较即可。

- **前摄草稿长度控制算法**：
  - 设计贪心搜索算法（Algorithm 1）分两阶段：**同步全一更新**（所有设备草稿长度同时加 1）→ **坐标贪心细化**（逐个设备增加草稿长度，找出最大提升）。
  - 期望通信延迟采用**样本平均近似（SAA）**，抽取 \(N_{SAA}=1000\) 个验证结果样本近似计算，避免对所有 \(2^K\) 种验证结果进行穷举。

### 3. 实验设计

- **模型对**：两组草稿–目标模型架构配对的组合：(1) Qwen2.5-0.5B 作草稿模型 + Qwen2.5-14B 作目标模型；(2) DeepSeek-R1-Distill-Qwen-1.5B + DeepSeek-R1-Distill-Qwen-32B。均共享同一 tokenizer（V=151,665）。
- **数据集/任务**：每个设备独立分配四种代表性生成任务之一（均匀抽取）：HumanEval（代码生成）、GSM8K（数学推理）、MT-Bench（多轮对话）、IFEval（指令遵循）。
- **Benchmark/对照组**：
  - **模式选择方面**：All UL mode（全部上行）、All DL mode（全部下行）、Random mode selection（随机模式选择）；
  - **草稿长度控制方面**：Optimal uniform draft length（最优统一草稿长度，穷举搜索）、Computation-only draft-length control（仅考虑计算延迟、忽略通信延迟的草稿长度控制）。
- **通信场景参数**：OFDMA 系统，20 个设备（默认），总带宽 10 MHz 平均分配，有效词汇量 1024，16 bit 量化，Rayleigh 衰落，距离 100–500 m，路径损耗指数 3.5。

### 4. 资源与算力

- 文中仅提及边缘服务器配备 **NVIDIA A100 GPU**，用于执行目标模型批量验证，以及在同一 GPU 上测量草稿模型的生成延迟。未明确说明使用的 GPU 数量、训练时长或相关能耗等信息。实验属于推理任务而非训练任务，但对 A100 的具体规格（如显存大小、数量）也未做说明。
- 代码、模型实现细节等也未公开（论文未附带可复现代码）。

### 5. 实验数量与充分性

- **实验组数**：从论文展示的结果来看，主要包括：
  - 两个模型对（Qwen2.5 对与 DeepSeek-R1 对）下的草稿长度与模式选择行为的分析；
  - 在 \(P^{dl}/P^{ul}=4\) 和 \(P^{dl}/P^{ul}=16\) 两种不同 DL/UL 功率预算比下，不同设备数量（K 变化）的对比实验；
  - 两种模型对在不同 DL/UL 功率比下的性能对比（图 11）。
  - 实验数量方面，虽然没有单独对每个数据集逐一呈现性能对比的详细表格，但四个数据集（HumanEval、GSM8K、MT-Bench、IFEval）的接受率估计覆盖了多元任务类型，实验场景覆盖了不同的功率预算比、不同设备数量以及不同模型规模，范围较为全面。
- **充分性**：总的来说比较充分。由于每个实验均在与最优模式选择/功率分配策略相同的反应策略下对比不同的草稿长度控制方案，并与相同的草稿长度控制下对比不同的模式策略，能够较好地隔离不同设计维度的贡献。基准对比较为全面（三类模式基准 + 两类草稿长度基准）。
- **局限性**：未提供统计方差/置信区间、单设备任务的详细分解结果、不同通道信噪比条件下的大规模系统级仿真、不同词汇规模（V_eff 变化）敏感性分析，以及实际无线信道下协议运行的端到端实验验证。

### 6. 主要结论与发现

- **自适应模式选择是 Multi-SPIN 中提升令牌吞吐量的关键机制**：固定 UL 或固定 DL 策略均非最优。当 DL 功率相对有限时，全 DL 模式共享功率竞争严重；全 UL 模式受限于每设备发射功率预算。自适应模式选择可按需将合适的设备子集分配给 DL 模式，实现最优平衡。
- **最优模式选择具有前缀结构**：按最小 UL 延迟排序后，UL 延迟较小的设备在 UL 模式、其余在 DL 模式即为最优。由此实现高效的组合优化求解。
- **草稿长度应随 DL 功率预算增大而增加**：当 DL 功率增大时，拒绝带来的通信惩罚减小，草稿长度的增加可以更充分地利用并行验证增益，系统瓶颈逐渐从通信转向计算。
- **草稿长度控制需与通信条件联合设计**：仅考虑计算延迟会忽略通信延迟的影响，导致次优草稿长度选择；统一草稿长度则无法适应设备间的异构计算与通信能力。
- **所提出框架一致优于各类基准**，在两个模型对、多种配置下均显著提升令牌有效吞吐量。

### 7. 优点

- **问题视角新颖**：将通信模式选择（UL/DL）作为 Multi-SPIN 系统设计中的新自由度，从根本上一改传统通信系统中信息传输方向预先确定的假设，让传输方向随系统条件自适应，具有重要的理论意义和实用价值。
- **理论研究严谨深入**：揭示了模式选择的最优前缀结构（引理 2），将指数复杂度组合问题化简为线性/二分搜索，理论证明简明而有力；DL 功率分配获得了闭式解（引理 1），便于实际部署。
- **两阶段优化框架清晰**：将随机优化问题分解为前摄+反应两个子阶段，每个阶段具备明确的目标和约束，既利于理论分析和计算求解，又与实际系统时序结构吻合。
- **草稿长度控制的算法设计合理**：采用"同步全一更新 + 坐标贪心细化"的双阶段贪心策略，配合 SAA 估计，大幅降低了在巨大推断空间中进行优化的计算开销。
- **实验验证全面**：覆盖两个模型系列、四类代表性任务、多种通信配置（功率比、设备数量等），并与多个基准对比，能够清楚地分离模式选择和草稿长度控制各自贡献的增益。

### 8. 不足与局限

- **模型的假设简化**：令牌接受率假设为 i.i.d.（独立同分布），与实际自回归生成中令牌间的相关性有偏差；带宽分配被当成给定条件而非优化变量；功率分配和模式选择考虑的是单天线场景，未考虑多天线/波束成形等更复杂的通信系统。
- **结果对经验接受率的依赖性**：不同数据集的接受率来自经验估计并取平均，而接受率估计方式是离线进行的，这会忽略实际交互过程中对话上下文和任务难度带来的动态变化。
- **实验覆盖不够全面**：缺乏对以下方面的深入实验：不同词汇有效支持大小（V_eff）的影响、量化位数（Q）变化的敏感性；未提供端到端真实无线信道的实验验证；不同系统规模（如 K 更大或更小）下的性能表现覆盖不足但趋势展示有限；缺少与最新 SPIN 变体方法（如稀疏化、量化压缩）在通信效率方面的直接对比。
- **缺乏统计显著性与单独消融验证**：实验部分未提供多次独立运行的均值/方差、置信区间等统计指标；论文没有对各模式选择策略中 UL/DL 模式的分配比例进行细粒度的单独消融实验（仅在系统级性能上对比整个设计）。
- **算力信息不够透明**：对于使用的 A100 GPU 数量、单次实验的运行时间、内存占用等未做描述，复现成本不明确。
- **未考虑更细粒度/多模态的信息交换设计**：论文的选择模式仅限于 UL/DL 二值选择，未扩展到内容表示粒度（完整、截断、量化、压缩分布）的联合优化，这些在论文末尾也被列为未来工作方向。

（完）
