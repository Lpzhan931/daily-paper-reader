Title: Vision Is Not Overhead: One-Pass Block Drafting for Lossless Speculative Decoding in Vision-Language Models

URL Source: https://arxiv.org/pdf/2609.00355v1

Published Time: Wed, 02 Sep 2026 00:18:44 GMT

Number of Pages: 18

Markdown Content:
# Vision Is Not Overhead: One-Pass Block Drafting for Lossless Speculative Decoding in Vision-Language Models 

## Jungseob Lee 1, Seongtae Hong 1, Dongyub Jude Lee 2, Chanjun Park 3,Jaehyung Seo 4, Sugyeong Eo 5∗, Heuiseok Lim 1∗

> 1

Korea University 2Zoom Communications 3Soongsil University 

> 4

Konkuk University 5Yonsei University {omanma1928, ghdchlwls123, limhseok}@korea.ac.kr, jude.lee@zoom.us, chanjun.park@ssu.ac.kr, seojae777@konkuk.ac.kr, s.eo@yonsei.ac.kr 

Abstract 

Speculative decoding accelerates generation without chang-ing its output, yet on vision-language models (VLMs) it has been caught in a self-defeating cycle. The drafter stays autore-gressive, so it must stay small. A small drafter cannot afford the image at every step, so vision is compressed, pruned, or hidden. A drafter cut off from the image is then least reliable exactly where the image makes text predictable. We present GLANCE, the first one-pass block drafter that is lossless on an unmodified VLM target, and it breaks the cycle at both ends. A block-diffusion head reads the target’s already-fused vision-language state, so vision costs the drafter nothing, and fills a whole block in one forward pass, so depth costs no sequential steps. A wide candidate tree is verified in one target pass, and every audited prompt reproduces greedy decoding exactly. Grounded workloads reward this most, entering a verbatim-copy regime whose long runs cost an autoregressive drafter a pass for every token and a block drafter one in total. Under one engine and one round budget, GLANCE decodes up to 2.93x faster than autoregression, from one draft pass a round where the production EAGLE3-VL head takes eight, and ac-cepts 2.7x longer blocks than an EAGLE-3 head trained on the same corpus. One law organizes these results. Accepted length is set by the target’s next-token entropy, with a fitted slope that steepens with grounding across all five tasks. The law transfers across targets and modalities and names its own boundary, since free-running text still favors a chain. Our code is available at https://github.com/js-lee-AI/GLANCE. 

## 1 Introduction 

Vision-language models (VLMs) increasingly serve work-loads whose output is a long stretch of text about an im-age, describing a photograph, reading a scanned document, pulling numbers off a chart (Qwen Team 2025; Bai et al. 2025). Generation stays autoregressive, so every output to-ken needs a full forward pass of the target, and at small batch sizes that loop is bound by memory bandwidth rather than compute. The visual encoder runs once, at prefill, while the loop runs on every token, so serving grounded generation faster means attacking the loop rather than the encoder. Speculative decoding removes part of that cost without changing the output. A cheap drafter proposes several fu-ture tokens and the target verifies them in one forward pass, 

> ∗

Corresponding author. Low                   

> accepted High
> Document reading
> Open captioning ENTROPY High
> ENTROPY Low
> Qtotal amount of other expenses?
> Qdescribe the image.
> The
> plane floating over the ocean at sunset
> total amount of other expenses is $975.00
> A
> the same pass commits only a short prefix
> one draft pass commits the entire block
> Next-token entropy
> rejected

Figure 1: Grounded generation is the most draftable. Top: on a document page the image pins the answer, entropy is low, and one draft pass commits the entire block. Bottom: open captioning admits many continuations, entropy is high, and the same pass commits only a short prefix. committing the longest correct prefix (Leviathan, Kalman, and Matias 2023; Chen et al. 2023). The modern form drafts from the target’s own hidden features (Cai et al. 2024; Li et al. 2024b,a) over a tree of candidates (Miao et al. 2024; Chen et al. 2024b), and EAGLE-3 (Li et al. 2025b) is the pro-duction standard whose heads vision-language stacks now ship. Work that adapts drafting to VLMs is caught in a cycle of its own premises. The drafter stays autoregressive, so a candidate k tokens deep costs k sequential draft passes, and the drafter must stay small for those passes to be cheap (AQ-MedAI 2025). A small drafter cannot afford the image at every step, so image tokens are compressed, pruned, or hid-den from it (Gagrani et al. 2024; Kang et al. 2025; Huang et al. 2025; Xie et al. 2025). Yet depth is what a drafter has to arXiv:2609.00355v1 [cs.AI] 31 Aug 2026 sell, and a drafter cut off from the image is least reliable about exactly the text the image already fixes, so each premise en-forces the next, and the cycle prices out the drafts that would pay the most. We ask the opposite question. On grounded workloads, is vision a cost to hide from the drafter, or the reason drafting works at all? When a VLM answers a question over a docu-ment, much of what it generates already exists in the image, so the next token is often near-deterministic, frequently an exact copy off the page, as Figure 1 shows. Predictability is what a drafter converts into speed, and we make it quan-titative. Accepted block length is governed by the target’s next-token entropy through one law that fits every task we test, and grounded tasks sit at the low-entropy end, so reading documents and charts accepts the longest blocks and open description the shortest. A mismatched-image probe shows the image acts through that entropy channel rather than be-side it, and swapping the drafter’s fused visual state for a text-only LLM’s costs the most acceptance exactly where drafting is wide and deep. Low entropy pays off only when long, wide candidate sets can be proposed cheaply, so we draft in one pass. We present GLANCE, for grounded block drafting with one-pass candidate expansion, and it exits the cycle at both ends, inheriting vision and filling depth at once. A block-diffusion head (Chen, Liang, and Liu 2026; Arriola et al. 2025) reads the frozen target’s own fused vision-language state and draws a distribution over every position of a whole block from a single draft pass. The highest-ranked prefixes form a wide candidate tree, and one target pass verifies all of them. We prove the committed path is exactly the target’s own greedy decoding, and we gate every run on that equality rather than assume it. Because one draft pass has already paid for the whole block, width becomes the cheap axis, bought inside the verify rather than with more draft passes (Ringel and Romano 2026; Zhang et al. 2026b). The result is a drafter that turns grounding into speed. On document, infographic and chart question answering GLANCE decodes up to 2.93 × faster than autoregression, from one draft pass a round where the production vision-language head takes eight, and every committed token is the target’s own. Trained on the same corpus and schedule as an EAGLE-3 head, it accepts 2.7× longer blocks. The law behind the gains transfers to a second vision-language target and to speech, code and chat. Our contributions are: 

• We rediagnose why drafting has underperformed on VLMs. An autoregressive drafter and stripped-down vi-sion access enforce each other, and together they discard the workload’s best asset, grounded generation’s long ver-batim runs, which only a one-pass drafter harvests whole. 

• We build the first one-pass block drafter that is lossless on an unmodified vision-language target, combining a block-diffusion head over fused vision-language states, wide-tree verification in a single target pass, and measured bitwise equality with greedy decoding. 

• We make the mechanism quantitative. Accepted length is governed by the target’s next-token entropy, amismatched-image probe shows the image acts through that channel, and at the grounded floor a verbatim-copy analysis lifts measured acceptance above the law’s own cap on every grounded task. OCR-targeted retraining lifts acceptance where the law predicts headroom. 

• We settle the deployment claim inside one production engine on five task families, and map the law’s scope across drafters, targets and modalities. 

## 2 Related Work 

Draft heads and tree verification. Speculative decoding uses a rejection-sampling accept rule that provably preserves the target distribution (Leviathan, Kalman, and Matias 2023; Chen et al. 2023). The practical line drafts from the target’s own hidden features with a lightweight head: independent offset-wise heads (Cai et al. 2024), sequentially dependent heads (Ankner et al. 2024), and feature-level autoregres-sion with dynamic candidate trees (Li et al. 2024b,a), which EAGLE-3 extends with training-time test and multi-layer feature fusion (Li et al. 2025b). Many candidates are veri-fied at once by packing a prefix tree into a single ancestor-masked target pass (Miao et al. 2024), with later work on tree shape (Chen et al. 2024b; Wang et al. 2025a) and on theory and benchmarking (Yin et al. 2024; Huang, Guo, and Wang 2024; Xia et al. 2024). 

Speculative decoding for VLMs. The first study of spec-ulative decoding for multimodal LLMs found a text-only drafter a strong baseline (Gagrani et al. 2024). Subsequent work takes one of two routes. One shrinks what the drafter must read, by compressing image tokens into adaptor embed-dings (Kang et al. 2025), pruning visual tokens (Huang et al. 2025), or hiding them outright (Xie et al. 2025). The other gives a small drafter its own cheap vision path through distil-lation and target-feature fusion (Ganesan et al. 2025; Hu et al. 2025). Semi-autoregressive multimodal drafting relaxes one token per pass without an exactness guarantee (Wang et al. 2025b), and the setting has a benchmark (Shen et al. 2026). Both routes keep the drafter autoregressive and engineer the drafter’s vision access rather than inheriting it. We inherit it in one pass. 

Block drafting and diffusion drafters. A parallel line removes the sequential bottleneck inside the drafter it-self: lookahead embeddings (Monea, Joulin, and Grave 2023), Jacobi decoding (Fu et al. 2024), block-parallel discrete-diffusion language models (Nie et al. 2025; Ar-riola et al. 2025), diffusion drafters with an autoregressive verifier (Christopher et al. 2025; Li et al. 2025a; Cheng et al. 2025), and a block-diffusion head on the frozen tar-get’s hidden state (Chen, Liang, and Liu 2026). One-pass drafting changes the economics of tree width, settled for text-only block drafting (Ringel and Romano 2026; Zhang et al. 2026b) and reused here unchanged. Block drafting has reached vision-language targets only by converting the tar-get itself into a self-speculating diffusion model (Wu et al. 2026; Zhang et al. 2026a), which forfeits the original model’s output. We keep the target frozen and its output exact. Qwen3-VL-8B                    

> 1917 25 33
> 36 decoder layers fused hidden states
> never raw visual tokens
> accepted not accepted
> Model Input
> One-pass block head (1.05B)
> 1 draft pass
> PROMPT
> total amount of other expenses?
> 1Frozen target 2
> Block draft 3Wide tree 4Verify and commit 5
> One decoding round target greedy output (lossless)
> The total amount of other
> one pass fills the whole block prefix-closed, budget N one ancestor-masked target pass
> b
> The A
> value total sum total
> b
> The
> total
> The
> total
> bonus
> committed

Figure 2: One-pass block speculative decoding for VLMs. Top: the frozen Qwen3-VL-8B target and its input. The block head reads the fused vision-language hidden states at five kept layers, never the raw visual tokens. Bottom, one decoding round left to right: one draft pass fills the whole block at every offset at once, the highest-scoring prefixes form a prefix-closed candidate tree, and a single ancestor-masked target pass verifies all of them, committing the longest path the target itself would have taken. 

## 3 Preliminaries 

3.1 Speculative Decoding and Acceptance 

Let p(· | x) be the frozen target VLM’s next-token distri-bution given the multimodal prefix x (text tokens together with the encoded image). Autoregressive greedy decoding emits arg max w p(w | x), one token for each target forward. Speculative decoding instead proceeds in rounds (Leviathan, Kalman, and Matias 2023; Chen et al. 2023). Given the committed prefix and a pending root token b, a drafter proposes candidate continuations and the target commits the longest prefix consistent with itself. At temperature 0

that accept rule is exact prefix matching against the target’s own greedy continuation Y = ( Y1, Y 2, . . . ) after b, where 

Yk = arg max w p(w | x ◦ b ◦ Y1: k−1). Let a be the number of accepted nonroot tokens in a round. Each round commits 

a + 1 tokens, the accepted prefix plus one token read off the last verified distribution. The central quantity of this paper is the average acceptance length , the number of tokens committed in one verification round, 

τ = E[a + 1] = E[a] + 1 , (1) so plain autoregressive decoding has τ =1 . Systems that in-stead log the bonus-excluded count E[a] (Kang et al. 2025) are raised by the committed token so every row sits on one scale. A higher τ means fewer target passes for each emit-ted token, and the speedup is τ discounted by drafting and verification overhead. 

3.2 One-Pass Block Drafting 

Our drafter follows the block-diffusion interface (Chen, Liang, and Liu 2026; Ringel and Romano 2026), stated as an assumption since everything downstream depends only on it. 

Assumption 1 (One-pass marginal ranking interface) . Con-ditioned on the cached prefix x and the pending root b,the drafter returns one marginal qj (· | x, b ) at every off-set j = 1 , . . . , L of the block in a single forward pass, before any within-block token is committed, and a candidate prefix is ranked by its plug-in score ˆπ(y1: ℓ) = Q 

> j≤ℓ

qj (yj | x, b ).

Definition 1 (Candidate tree and accept rule) . A candidate tree S is a prefix-closed set of nonempty strings after the root 

b, of depth at most L and budget N = |S|. Packed into one ancestor-masked target pass, the row of a depth-d node y1: d

yields exactly p(· | x ◦ b ◦ y1: d). With Y the target’s greedy continuation, the accepted length is A(S) = max {k ≥ 0 : 

Y1: j ∈ S for all j ≤ k}. The walk commits Y1: A(S) and emits the next target-greedy token from the last verified row as the new root. Drafting therefore costs one pass for the block, indepen-dent of tree width . Growing N adds verifier FLOPs inside that single pass but no sequential draft steps. An autoregres-sive drafter instead pays depth in sequential passes, five a round for the deployed EAGLE3-VL default and eight where that head runs fastest. 

## 4 Method 

We call the resulting decoder GLANCE . Figure 2 draws a decoding round in two steps. First, a block head drafts a whole block of future tokens in one forward pass (stage 3). Then the target verifies a wide tree of those candidates in one forward pass and commits its own greedy prefix (stages 4

and 5). The head is trained and the target is frozen. 

4.1 Drafting on Fused Vision-Language States 

The drafter is a 5-layer block head in the DFlash fam-ily (Chen, Liang, and Liu 2026) with block size B=16 and 

1.05 B parameters, drawn in stage 3 of Figure 2, attached to a frozen Qwen3-VL-8B target. It never reads raw visual tokens. Its cross-attention keys and values come from the tar-get’s fused hidden states at five kept layers {1, 9, 17 , 25 , 33 },following EAGLE-3’s multi-layer feature fusion (Li et al. 2025b). By the time these states exist the target has al-ready merged the image into its text representation, so the drafter inherits the visual grounding at no extra encoding cost. Given the cached prefix and the pending root token, one forward pass fills the masked block and emits the offset-wise marginals q1, . . . , q 15 of Assumption 1. 

4.2 Wide-Tree Verification and Losslessness 

The marginals feed the candidate tree of stage 4. The builder keeps the prefixes of highest plug-in score ˆπ (Assumption 1), which forms a prefix-closed tree. The tree is packed into a single ancestor-masked target pass (Miao et al. 2024). Each node’s row reproduces the exact autoregressive conditional at that node (Definition 1). The decoder then commits the longest target-greedy path, plus one bonus token read off the last verified row. Since drafting already paid for the whole block, the builder reuses the published tree-economics machinery un-changed (Ringel and Romano 2026; Zhang et al. 2026b). The budget is a small fixed constant, and Section 5.4 shows returns flatten once the width is moderate. 

Theorem 1 (Lossless greedy equivalence, informal) . As-sume the target’s argmax is unique at every visited prefix and its top-2 logit gap there exceeds the packed-versus-unpacked logit perturbation. Then for any candidate tree the greedy walk of Definition 1 commits exactly the frozen target’s greedy autoregressive sequence, and in fp32 arithmetic the equality is bitwise. Full statement and proof in Technical Appendix B. The guarantee is a property of the verifier, not the drafter. A weak head can only shorten accepted prefixes, never change the output. Production bf16 kernels can reorder exact ties, so a tie-aware gate defers to the target’s own token there, as Technical Appendix B details. 

4.3 Training 

Only the block head trains, and the vision tower and target decoder stay frozen. The training data are the target’s own greedy generations on 8000 COCO-Caption2017 and 8000 

TextVQA prompts, 16 K teacher-forced rows in total. The objective is offset-wise top-1 coverage of the target’s own token at each block offset, with a reveal trick that exposes a random prefix so the head learns every reveal length. The head is warm-started from the released text-only Qwen3-8B DFlash block head and trained for one epoch on a single GPU, with the full training card in Technical Appendix D. The recipe contains neither ChartQA nor DocVQA data, yet those end up the two most draftable tasks, the phenomenon Section 6 explains. 

## 5 Experiments 

5.1 Setup 

Models and tasks. The primary target is Qwen3-VL-8B-Instruct (Qwen Team 2025), kept frozen. Qwen3-VL-4B and Llama-3.2-11B-Vision enter only the trans-fer study. Tasks span five families ordered from open-ended to grounded: COCO captioning (Lin et al. 2014), TextVQA (Singh et al. 2019), InfographicVQA (Mathew et al. 2022), DocVQA (Mathew, Karatzas, and Jawahar 2021), and ChartQA (Masry et al. 2022). The rest of the standard suite is multiple-choice or single-word, leaving no decode loop to amortize, and ScienceQA stands in for that regime. 

Baselines. Following the EAGLE series (Li et al. 2024b,a, 2025b) we take the members of its suite that preserve the target’s outputs: the n-gram lookup floor, classic two-model speculation at three draft sizes, the production EAGLE3-VL head (AQ-MedAI 2025), and ViSpec (Kang et al. 2025) with its Medusa baseline. ViSpec carries EAGLE-2’s dynamic-tree drafting into the VLM setting. We train the last two ourselves, since no published VLM drafter releases a head for this target. 

Metrics and protocol. Every table below inherits one pro-tocol, and a caption states only what departs from it. Decod-ing is batch one, decode-only, capped at 256 new tokens, and greedy unless a temperature is marked. Acceptance length τ

of Equation (1) counts tokens, not time, so it does not depend on the engine and compares systems directly. Speedup is the ratio of the mean decode time of one token, autoregressive over speculative, each system against its own baseline on one device. The n-gram and EAGLE3-VL rows of Table 3 are the serving engine’s end-to-end times, which pull a ratio toward 

1.0. Table 1 holds the round budget at 32 draft tokens for both drafters, while every other table runs each system at its own operating point, ours a 63 -node tree, so τ is not one number across tables. Every greedy run is gated at fp32 on reproduc-ing the target’s own decoding exactly, an equivalence prior VLM work asserts but does not measure. Prompt counts and the remaining details are in Technical Appendix D. 

5.2 Head-to-Head in a Production Engine 

Both drafters run inside SGLang 0.5.6 on one card, so with engine, card and round budget fixed, the only structural dif-ference left is that EAGLE3-VL produces its 32 draft tokens with eight sequential passes and GLANCE with one. On the three tasks whose answer is anchored in the image, GLANCE decodes up to 2.93 × faster than autoregression and up to 

7.6% faster than the production head, every paired bootstrap interval excluding zero. Where the output is free-running text the eight-pass chain leads instead, and the split is sharp rather than noisy, at one prompt in 101 on captioning and on TextVQA against at least 

70 on the grounded tasks. Assumption 1 names the mecha-nism, since our head ranks candidates by a product of offset-wise marginals, exact only where the block’s tokens are con-ditionally independent given the image. Near-determinism delivers that and free-running text does not, so our deep can-didates drift where a chain stays coherent by construction. Our accepted length is ordered by grounding across all five tasks, 2.91 < 3.32 < 3.68 < 3.93 < 4.62 , while the pro-duction head’s is not, placing TextVQA above both DocVQA and InfographicVQA. And the two tasks we lose are the only EAGLE3-VL, eight draft passes a round GLANCE, one draft pass a round task AR ms/tok τ ↑ ms/tok ↓ speedup ↑ τ ↑ ms/tok ↓ speedup ↑ GLANCE faster by                                         

> Free-running text
> captioning 24 .25 3.50 11 .21 2.16 ×2.91 13 .38 1.81 ×−16 .2%
> TextVQA 24 .31 4.18 9.75 2.49 ×3.32 12 .04 2.02 ×−19 .1%
> Answer anchored in the image
> InfographicVQA 24 .37 3.57 11 .50 2.12 ×3.68 10 .69 2.28 ×+7.6%
> DocVQA 24 .47 3.68 11 .51 2.13 ×3.93 10 .92 2.24 ×+5.4%
> ChartQA 24 .23 4.52 8.75 2.77 ×4.62 8.26 2.93 ×+6.0%

Table 1: GLANCE against the production EAGLE3-VL head with engine, card and round budget held fixed. The three arms of a task run back to back on one card, both speculative arms CUDA-graph captured. Both drafters verify 32 draft tokens a round and differ only in how those tokens are produced. GLANCE faster by is the difference in mean decode time. two in the training set of Section 4.3, while all three we win are unseen, so the wins are not a data-selection effect. 

5.3 Acceptance Against Deployed Systems 

Matched training. Both drafter architectures are trained from scratch on one corpus with the same frozen target, global batch, epochs, and framework (SGLang Team 2026), then scored on the same held-out prompts. The matched-training block of Table 2 reports the outcome. GLANCE accepts 2.7× longer blocks pooled over the five domains and 

4.0× on ChartQA. The gap replicates on a second training corpus, 2.04 against 1.29 , and the block’s ‡ speedups show it carrying through to wall-clock. The probe isolates training data, schedule, and framework, not the draft budget, which panel (b) of Figure 3 controls instead with the identical head as a width-1 chain. 

Deployed systems. Table 2 compares every system shipped for these targets on the metric that survives a change of engine. Acceptance rises with grounding, and Section 5.5 traces the gain to the wide tree rather than to the head. Loss-lessness we gate rather than assume. GLANCE reproduces greedy decoding on every audited prompt, while ViSpec and Medusa reproduce none under any tree setting, placing their divergence in the relaxed acceptance rule. The audit is read at fp32 because in bf16 the target reproduces itself on fewer than half the prompts with no drafter at all, so bf16 disagree-ment measures the arithmetic and not the drafter. 

ViSpec. We place the strongest published VLM drafter in both available frames. On its own Qwen2.5-VL-7B target GLANCE matches or exceeds its accepted length on every task, and is the one system gated for exact reproduction. On our target, with its head trained by the authors’ official pipeline, no output in any tree configuration matches the target’s greedy text, since the typical-acceptance rule trades fidelity for length, so we report length-controlled speedups. 

5.4 Wall-Clock and Generality 

Pushing the same matched pair onto longer prompts, full-resolution InfographicVQA at two context lengths, locates where the margin lives. At a 2K context GLANCE is a paired 

+5 .8% faster than the production head, 95% CI [+2 .2, +9 .4] ,and it gets there while matching that head’s accepted length from one draft forward pass a round against its eight. Graph capture is what converts the saved passes into saved time, a one-pass draft being launch-bound, and with graphs off the cell is parity. At ∼6.7K the two are level, our accepted length having fallen further than theirs and the round-cost advantage absorbing the difference. Technical Appendix E lists every comparison with its sample size. Classic two-model speculation leads acceptance on four of the five tasks in Table 2 yet is a net slowdown on all five, because wall-clock is decided by what each accepted token costs to draft, not by how much a drafter accepts. And speedup saturates past a moderate budget on every task, drawn in panel (d) of Figure 3 and tabulated in Technical Appendix E. Width pays most exactly where we win. Two regimes bound the method. Speculation pays only where there is a decode loop to amortize. On ScienceQA (Lu et al. 2022), whose answer is a single letter, our loop falls below parity, so this is a method for generation, not classifi-cation. Under sampling, reported in Table 3, the committed token is drawn from the target’s own verified row, so the dis-tribution is the target’s by construction and byte-identity is undefined. 

5.5 Ablations 

What the drafter reads. Zeroing the fused conditioning state collapses acceptance to 1.11 , so the head genuinely drafts from the target’s context. Swapping those states for a text-only LLM’s costs almost nothing on captioning, which recovers 97% of full acceptance, and more as the task be-comes grounded, to a minimum of 80% on DocVQA. The same swap takes a larger share of acceptance from the budget-

31 tree than from the identical width-1 chain on all five tasks, so losing vision costs most exactly when drafting wide and deep. 

The round budget, not head size, is the source. 

GLANCE’s head carries 2.6× the parameters of EAGLE3-VL’s, yet as a width-1 chain the same head gives up at least 

1.45 × of its accepted length on every task, panel (b) of Fig-ure 3. In the forward-level harness, where the two arms are the same head, the tree decodes 1.36 × the chain. The tree and not the head earns the margin. Across Table 1 the two systems spend within 4% of the same time in a verify round, so the larger head run once costs no more than the small head acceptance length τ ↑ speedup method (draft passes a round) params caption TextVQA InfoVQA DocVQA ChartQA geomean ↑ lossless                                                                                                  

> Our target Qwen3-VL-8B: training-free floor, two-model drafts, and trained heads
> n-gram lookup (PLD, 0)01.29 2.49 2.53 3.30 2.57 0.92 ×✓
> Classic SD (Qwen3-VL-4B, 8)4.4B3.53 3.79 3.95 4.39 4.47 0.71 ×✓
> Classic SD (Qwen3-1.7B, text-only, 8)2.0B1.49 1.42 1.90 1.68 2.12 0.42 ×✓
> EAGLE3-VL (production, 5)0.40 B2.13 2.66 2.48 2.56 2.92 2.06 ×✓
> ViSpec (official recipe, 3)0.23 B1.72 2.00 1.39 1.86 1.61 1.05 ××
> Medusa (same codebase, 1)0.08 B1.66 1.55 1.65 1.46 1.54 1.07 ××
> GLANCE ( 1)1.05 B3.04 3.29 3.63 3.78 5.16 2.49 ×✓†
> Matched training: both heads from scratch on one corpus, same target, batch, epochs, framework. Held-out acceptance probe
> EAGLE3 head, depth-3chain ( 3)0.40 B1.62 1.59 1.55 1.58 1.87 1.15 ×‡✓
> GLANCE, budget-63 tree ( 1)1.05 B4.05 3.97 3.90 4.13 7.44 2.48 ×‡✓
> ViSpec’s home target Qwen2.5-VL-7B: released head against ours trained on that target
> ViSpec (released head, 3)0.35 B3.34 3.26 3.21 3.04 3.59 1.76 ××
> GLANCE ( 1)1.23 B4.17 3.28 3.47 3.12 4.72 2.10 ×✓†

Table 2: Greedy decoding against everything shipped for these targets. A speedup is a within-system ratio against that system’s own autoregressive baseline, and τ is engine-independent. The matched-training block is a held-out probe, its ‡ speedups timed in one HF harness and read only against each other. Params is the drafter’s own weights, excluding the frozen target and any bundled copies of its embeddings. Lossless: ✓ exact, × relaxed, † audited bitwise identical to greedy decoding. 

Figure 3: Systems results on Qwen3-VL-8B. (a) Accepted length against measured next-token entropy, by decile, for the three main-run tasks. The curves separate by grounding, and DocVQA’s near-certain end clears the pooled ceiling (Theorem 2). (b) The wide tree against the identical head run as a width-1 chain. (c) Margin against the production head at two context lengths. (d) Speedup against the verifier budget N on five tasks. run eight times, and widening our tree 4.2× moves a round by only a few percent. 

Targeted data lifts grounded tasks selectively. Retrain-ing the head on OCR-heavy data lifts DocVQA at both ver-ifier budgets while captioning stays flat, so the gain concen-trates where the law predicts headroom. 

## 6 Why Grounded Is More Draftable 

Every acceptance result above is organized by one scalar: the target’s next-token entropy H at the round root. Entropy has gated drafting before (Agrawal, Jeon, and Lee 2024; Tong et al. 2026; Mahmoud 2026). We give it a closed form and test it. Modeling offset-wise matches as a survival process with a slowly varying hazard, under two approximations of Technical Appendix B, gives a truncated-geometric mean, and an affine model of the match logit in H closes the form: 

logit pm(H) = b0 − b1H, E[a | H] ≈ pm

1 − pm

, (2) truncated at the block horizon. Here logit u = ln( u/ (1 −u)) ,

pm(H) ∈ (0 , 1) is the probability that the drafter’s proposal at a round root of entropy H matches the target’s greedy token, and b0, b 1 > 0 are the zero-entropy intercept and the entropy slope, fitted for each task on its round log (values in Technical Appendix F). A Fano argument grounds the monotone part, and the affine logit is a surrogate under a temperature family. The law fits every task with a positive slope that steepens with ground-ing. Figure 3(a) draws the decile curves for the three main-run tasks, and Figure 4 places all five fits inside two brackets. Fit-ted over 7751 rounds the pooled law has intercept b0=0 .96 

and slope b1=0 .63 . The per-task slopes climb monotoni-cally with grounding, 0.46 on captioning, 0.58 on TextVQA, Figure 4: Task-level law fits inside their two brackets, for all five tasks, ChartQA under its templated prompts. Axes: root entropy 

H in nats against expected accepted length E[a | H], log scale. The fit of Equation (2) stays above the AdaEDL lower bound and below the 2 log N/H verification ceiling at N =31 . Points: measured entropy deciles.                               

> acceptance τ↑speedup method params capt. TextVQA DocVQA geom. ↑
> n-gram lookup 01.32 2.44 2.93 0.90 ×
> Classic SD (4B) 4.4B3.62 3.86 3.71 0.65 ×
> Classic SD (1.7B, text) 2.0B1.42 1.38 1.61 0.37 ×
> EAGLE3-VL 0.40 B2.03 2.29 2.41 1.64 ×
> GLANCE 1.05 B2.86 3.17 3.71 1.79 ×

Table 3: Sampling at T =1 on the same target, on the three tasks every sampled system shares. ViSpec and Medusa run greedy only and have no sampling row. The greedy ordering survives the change of temperature. 

0.80 on InfoVQA, 0.95 on DocVQA and, under ChartQA’s templated prompts, 1.71 . Acceptance has large round-level scatter, so fits are read against the conditional mean, where the pooled R2 is 0.71 , and Technical Appendix B bounds what any entropy-measurable predictor can do against raw rounds, a ceiling our fit realizes up to 89% of. 

The image acts through entropy. Correlation does not show the image is responsible, so we intervene on it alone. The same prompts are decoded with the correct and with a mismatched image on a shared teacher-forced trajectory. The correct image lengthens accepted blocks in every task and the effect scales with grounding, +3 .5% on caption-ing, +6 .8% on TextVQA and +11 .7% on DocVQA, every interval excluding zero. It also collapses the target’s mean next-token entropy, 0.26 against 0.72 nats on DocVQA, and once entropy is held fixed the residual image effect is no longer positive (pooled coefficient −0.07 ). With the feature-access ablation above, this places the image’s contribution in the entropy channel. 

The form transfers. If the law is a property of the target rather than of our head, its form should survive a change of drafter, target and modality, and it does on all three axes, with a positive slope every time. Two-model drafts on two further vision-language targets fit at b1=0 .67 and 0.40 , and outside vision the slope stays positive on speech recognition ( 0.49 ), chat ( 0.64 ), a non-Qwen backbone ( 0.84 ) and code ( 1.76 ). Grounding is simply one way a task collapses the target’s entropy, and the full fits are in Technical Appendix F. 

The ordering is architecture-scoped. Trained on Llama-3.2-11B-Vision, where vision enters by cross-attention rather than as a token prefix, the method still works and stays loss-less. The characterization does not, since the ordering puts captioning first and the law inverts sign on the grounded short-answer domains. 

The law bends at its own tail. At the entropy floor the single-hazard family gives out, in a direction that favors grounding. 

Theorem 2 (Grounded tail, informal) . Equation (2) caps the near-certain conditional mean at eb0 , yet a verbatim-copy fraction above an explicit threshold lifts the H → 0 mean strictly above that cap, and the measured grounded floor sits above it, 5.23 against 3.41 on DocVQA. Statement and test in Technical Appendix B. This copy regime is where one-pass drafting collects its rent, since a run of length ℓ costs an autoregressive drafter ℓ

sequential passes and this drafter one. The grading extends across tasks, since the fraction of rounds accepting eight or more tokens rises with grounding, from 0.0% on captioning to 11 .5% on ChartQA under its templated prompts, and every grounded task’s floor clears its own cap. 

## 7 Conclusion 

We presented a lossless one-pass block drafter for a frozen vision-language model. A block-diffusion head proposes a whole block in one pass from the target’s fused states, and a wide tree turns that parallelism into accepted length, bitwise identical to greedy autoregression. Our results trace the gains to an exit from the cycle that held VLM drafting back. They show vision serving as inherited context rather than overhead, depth filled in one pass rather than paid in sequential steps, and grounded generation’s verbatim runs harvested whole. An entropy law fitted on every task captures when this pays, persists when the drafter, the target and the modality change, and indicates where the method stops, with free-running text staying a chain’s regime. 

## References 

Agrawal, S.; Jeon, W.; and Lee, M. 2024. AdaEDL: Early Draft Stopping for Speculative Decoding of Large Language Models via an Entropy-based Lower Bound on Token Ac-ceptance Probability. arXiv:2410.18351. Ankner, Z.; Parthasarathy, R.; Nrusimha, A.; Rinard, C.; Ragan-Kelley, J.; and Brandon, W. 2024. Hydra: Sequentially-Dependent Draft Heads for Medusa Decod-ing. In Conference on Language Modeling (COLM) .ArXiv:2402.05109. AQ-MedAI. 2025. Qwen3-VL-8B-Instruct-EAGLE3: A production EAGLE-3 draft head for Qwen3-VL-8B. https://huggingface.co/AQ-MedAI/Qwen3-VL-8B-Instruct-eagle3. HuggingFace model checkpoint; run via SGLang native EAGLE-3 tree. Arriola, M.; Gokaslan, A.; Chiu, J. T.; Yang, Z.; Qi, Z.; Han, J.; Sahoo, S. S.; and Kuleshov, V. 2025. Block Diffusion: In-terpolating Between Autoregressive and Diffusion Language Models. arXiv:2503.09573. Bai, S.; Chen, K.; Liu, X.; Wang, J.; Ge, W.; Song, S.; Dang, K.; Wang, P.; Wang, S.; Tang, J.; et al. 2025. Qwen2.5-VL Technical Report. arXiv preprint arXiv:2502.13923 .Cai, T.; Li, Y.; Geng, Z.; Peng, H.; Lee, J. D.; Chen, D.; and Dao, T. 2024. Medusa: Simple LLM inference acceleration framework with multiple decoding heads. In International Conference on Machine Learning .Chen, C.; Borgeaud, S.; Irving, G.; Lespiau, J.-B.; Sifre, L.; and Jumper, J. 2023. Accelerating large language model decoding with speculative sampling. arXiv preprint arXiv:2302.01318 .Chen, G. H.; Chen, S.; Zhang, R.; Chen, J.; Wu, X.; Zhang, Z.; Chen, Z.; Li, J.; Wan, X.; and Wang, B. 2024a. ALLaVA: Harnessing GPT4V-Synthesized Data for Lite Vision-Language Models. arXiv:2402.11684. Chen, J.; Liang, Y.; and Liu, Z. 2026. DFlash: Block Diffu-sion for Flash Speculative Decoding. arXiv:2602.06036. Chen, Z.; May, A.; Svirschevski, R.; Huang, Y.; Ryabinin, M.; Jia, Z.; and Chen, B. 2024b. Sequoia: Scalable, Robust, and Hardware-aware Speculative Decoding. In Advances in Neural Information Processing Systems . ArXiv:2402.12374. Cheng, Z.; Yang, G.-W.; Li, J.; Deng, Z.; Guo, M.-H.; and Hu, S.-M. 2025. DEER: Draft with Diffusion, Verify with Autoregressive Models. arXiv:2512.15176. Christopher, J. K.; Bartoldson, B. R.; Ben-Nun, T.; Cardei, M.; Kailkhura, B.; and Fioretto, F. 2025. Speculative Diffu-sion Decoding: Accelerating Language Generation through Diffusion. In Proceedings of the North American Chapter of the Association for Computational Linguistics (NAACL) .ArXiv:2408.05636. Fu, Y.; Bailis, P.; Stoica, I.; and Zhang, H. 2024. Break the Sequential Dependency of LLM Inference Using Lookahead Decoding. In Proceedings of the International Conference on Machine Learning (ICML) . ArXiv:2402.02057. Gagrani, M.; Goel, R.; Jeon, W.; Park, J.; Lee, M.; and Lott, C. 2024. On Speculative Decoding for Multimodal Large Language Models. arXiv preprint arXiv:2404.08856 . ELVM @ CVPR 2024. Ganesan, M.; Segal, S.; Aggarwal, A.; Sinnadurai, N.; Lie, S.; and Thangarasa, V. 2025. MASSV: Multimodal Adap-tation and Self-Data Distillation for Speculative Decoding of Vision-Language Models. Findings of EMNLP 2025, arXiv:2505.10526. Hu, Y.; Xia, T.; Liu, Z.; Raman, R.; Liu, X.; Bao, B.; Sather, E.; Thangarasa, V.; and Zhang, S. Q. 2025. DREAM: Drafting with Refined Target Features and Entropy-Adaptive Cross-Attention Fusion for Multimodal Speculative Decod-ing. In Advances in Neural Information Processing Systems .ArXiv:2505.19201. Huang, H.; Yang, F.; Liu, Z.; Yin, X.; Li, D.; Ren, P.; and Barsoum, E. 2025. SpecVLM: Fast Speculative Decoding in Vision-Language Models. arXiv:2509.11815. Huang, K.; Guo, X.; and Wang, M. 2024. SpecDec++: Boost-ing Speculative Decoding via Adaptive Candidate Lengths. arXiv:2405.19715. Kang, J.; Shu, H.; Li, W.; Zhai, Y.; and Chen, X. 2025. ViSpec: Accelerating Vision-Language Models with Vision-Aware Speculative Decoding. NeurIPS 2025, arXiv:2509.15235. Leviathan, Y.; Kalman, M.; and Matias, Y. 2023. Fast infer-ence from transformers via speculative decoding. In Inter-national Conference on Machine Learning .Li, G.; Fu, Z.; Fang, M.; Zhao, Q.; Tang, M.; Yuan, C.; and Wang, J. 2025a. DiffuSpec: Unlocking Diffusion Language Models for Speculative Decoding. arXiv:2510.02358. Li, Y.; Wei, F.; Zhang, C.; and Zhang, H. 2024a. EAGLE-2: Faster inference of language models with dynamic draft trees. In Empirical Methods in Natural Language Processing .Li, Y.; Wei, F.; Zhang, C.; and Zhang, H. 2024b. EAGLE: Speculative sampling requires rethinking feature uncertainty. In International Conference on Machine Learning .Li, Y.; Wei, F.; Zhang, C.; and Zhang, H. 2025b. EAGLE-3: Scaling up inference acceleration of large language models via training-time test. In Advances in Neural Information Processing Systems . ArXiv:2503.01840. Lin, T.-Y.; Maire, M.; Belongie, S.; Hays, J.; Perona, P.; Ramanan, D.; Dollár, P.; and Zitnick, C. L. 2014. Microsoft COCO: Common objects in context. In ECCV .Lu, P.; Mishra, S.; Xia, T.; Qiu, L.; Chang, K.-W.; Zhu, S.-C.; Tafjord, O.; Clark, P.; and Kalyan, A. 2022. Learn to explain: Multimodal reasoning via thought chains for science question answering. In Advances in Neural Information Processing Systems .Mahmoud, S. 2026. Acceptance Dynamics Across Cognitive Domains in Speculative Decoding. arXiv:2604.14682. Masry, A.; Long, D. X.; Tan, J. Q.; Joty, S.; and Hoque, E. 2022. ChartQA: A benchmark for question answering about charts with visual and logical reasoning. In Findings of ACL .Mathew, M.; Bagal, V.; Tito, R.; Karatzas, D.; Valveny, E.; and Jawahar, C. 2022. InfographicVQA. In WACV .Mathew, M.; Karatzas, D.; and Jawahar, C. 2021. DocVQA: A dataset for VQA on document images. In WACV .Miao, X.; Oliaro, G.; Zhang, Z.; Cheng, X.; Wang, Z.; Zhang, Z.; Wong, R. Y. Y.; Zhu, A.; Yang, L.; Shi, X.; Shi, C.; Chen, Z.; Arfeen, D.; Abhyankar, R.; and Jia, Z. 2024. SpecInfer: Accelerating large language model serving with tree-based speculative inference and verification. In ASPLOS .Monea, G.; Joulin, A.; and Grave, E. 2023. PaSS: Paral-lel Speculative Sampling. arXiv preprint arXiv:2311.13581 .NeurIPS 2023 Workshop on Efficient Natural Language and Speech Processing. Nie, S.; Zhu, F.; You, Z.; Zhang, X.; Ou, J.; Hu, J.; Zhou, J.; Lin, Y.; Wen, J.-R.; and Li, C. 2025. Large Language Diffusion Models. arXiv:2502.09992. Qwen Team. 2025. Qwen3-VL Technical Report. https:// huggingface.co/Qwen/Qwen3-VL-8B-Instruct. Qwen3-VL-8B-Instruct. Ringel, L.; and Romano, Y. 2026. Accelerating Spec-ulative Decoding with Block Diffusion Draft Trees. arXiv:2604.12989. SGLang Team. 2026. SpecForge: A Flexible and Efficient Open-Source Training Framework for Speculative Decoding. 

arXiv preprint arXiv:2603.18567 .Shen, H.; Wang, X.; Zhang, P.; Hsieh, Y.; Han, Q.; Wan, Z.; Zhang, Z.; Zhang, J.; Xiong, J.; Liu, Z.; Zhang, Y.; Cao, H.; Zhao, C.; and Zhang, M. 2026. MMSpec: Bench-marking Speculative Decoding for Vision-Language Models. arXiv:2603.14989. Singh, A.; Natarajan, V.; Shah, M.; Jiang, Y.; Chen, X.; Ba-tra, D.; Parikh, D.; and Rohrbach, M. 2019. Towards VQA models that can read. In CVPR .Tong, Y.; Zhang, T.; Wan, Y.; Lin, K.; Yuan, J.; and Hu, C. 2026. SAGE: Accelerating Vision-Language Mod-els via Entropy-Guided Adaptive Speculative Decoding. arXiv:2602.00523. Wang, J.; Su, Y.; Li, J.; Xia, Q.; Ye, Z.; Duan, X.; Wang, Z.; and Zhang, M. 2025a. OPT-Tree: Speculative De-coding with Adaptive Draft Tree Structure. Transactions of the Association for Computational Linguistics (TACL) .ArXiv:2406.17276. Wang, Z.; Li, R.; Du, H.; Zhou, J. T.; Zhang, Y.; and Yang, X. 2025b. SpecFLASH: A Latent-Guided Semi-Autoregressive Speculative Decoding Framework for Efficient Multimodal Generation. arXiv:2505.12728. Wu, C.; Lan, S.; Fu, Y.; Gao, S.; Wang, J.; Yu, J.; Alvarez, J. M.; Molchanov, P.; Luo, P.; Han, S.; Zhu, L.; and Xie, E. 2026. Fast-dVLM: Efficient Block-Diffusion VLM via Direct Conversion from Autoregressive VLM. arXiv preprint arXiv:2604.06832 .Xia, H.; Yang, Z.; Dong, Q.; Wang, P.; Li, Y.; Ge, T.; Liu, T.; Li, W.; and Sui, Z. 2024. Unlocking Efficiency in Large Language Model Inference: A Comprehensive Survey of Speculative Decoding. In Findings of the Association for Computational Linguistics (ACL Findings) . Spec-Bench. Xie, Z.; Wang, P.; Qiu, S.; and Cheng, J. 2025. HiViS: Hiding Visual Tokens from the Drafter for Speculative Decoding in Vision-Language Models. arXiv:2509.23928. Yin, M.; Chen, M.; Huang, K.; and Wang, M. 2024. ATheoretical Perspective for Speculative Decoding Algo-rithm. In Advances in Neural Information Processing Sys-tems (NeurIPS) . ArXiv:2411.00841. Zhang, K.; Wang, J.; Gao, S.; Wu, C.; Cao, Y.; Han, S.; Ivanovic, B.; Liu, L.; Pavone, M.; Han, S.; Zhou, D.; and Xie, E. 2026a. Fast-dDrive: Efficient Block-Diffusion VLM for Autonomous Driving. arXiv:2605.23163. Zhang, S.; Qiu, H.; He, H.; and Dai, Y. 2026b. Cost-Aware Diffusion Draft Trees for Speculative Decoding. arXiv:2606.01813. Technical Appendix 

## A Limitations 

All claims are batch one and decode-only, and we make no larger-batch claim. The bitwise guarantee is a greedy-regime statement: the sampled-decoding results of Section 5.4 pre-serve the target’s distribution by construction, not byte-identity, and are reported on one run at each temperature. Sample sizes are small, and they are stated at each mea-surement in Appendices D and E. The bitwise guarantee is stated in fp32. Under production bf16 it holds as byte-identity through a tie-aware gate. Two of the three cross-target fits use two-model proxies rather than trained block adapters. The grounded ordering itself is architecture-scoped, and it inverts under cross-attention fusion. 

## B Theory: Statements and Proofs 

B.1 Losslessness 

Theorem 1 (full). For the decoder of Definition 1, assume (float-exactness) that at every visited prefix x ◦ b ◦ y1: k

the target arg max is unique and the top logit gap γk := 

ℓ(1) − ℓ(2) > 0 exceeds the packed-versus-unpacked logit perturbation. Then for any candidate tree the committed to-ken sequence equals the target’s autoregressive greedy se-quence exactly, and losslessness can fail only at a margin γk

below that perturbation, a tie. 

Proof. Single round. By Definition 1 the verifier row at any prefix x ◦ b ◦ y1: k reproduces p(· | x ◦ b ◦ y1: k) exactly, since ancestor-only masking and token packing change the attention layout, not the row-level conditioning. Let Yk+1 be the target greedy token there. The walk then takes one of two branches: 

y1: k ◦ Yk+1 ∈ S : descend and commit Yk+1 ;

otherwise : stop and emit Yk+1 as the next root. Either way the token produced at that position is Yk+1 , and the tree affects only how many tokens commit for the block. 

Induction over rounds. The emitted stream concatenates accepted paths and corrections, and each round’s correction is the next round’s root. Every produced token is the target greedy token at its own prefix, so induction on output position gives the committed sequence 

Y1Y2 · · · bitwise, independent of all budget choices. 

Tie sensitivity. The only breakable step is the equality of the packed and autoregressive arg max . It fails only at 

γk < (packed-versus-unpacked logit perturbation) ,

a near-tie. This is the operational bf16 tie-aware gate of Sec-tion 4.2: gating rounds whose observed margins fall under the threshold keeps the perturbation bound in force, and the fp32 audit shows zero divergences. 

B.2 Derivation of the Law 

Assumption 2 (Offset-wise survival with marginal hazards) .

Conditioned on the round (equivalently on H), let the drafter top-1 token match the target greedy token at offset j with probability pm,j , and let a be the leading run of matches before the first miss, capped at L. We assume the survival factorizes into the marginal hazards, 

P[a ≥ ℓ | H] = Y

> j≤ℓ

pm,j , E[a | H] = 

> L

X

> ℓ=1

Y

> j≤ℓ

pm,j .

(3) This is an assumption, not a consequence of the chain rule: it replaces the conditional hazards P[match j | match <j , H ]

by the marginals pm,j .

Lemma 1 (Slowly varying hazard gives a truncated-geomet-ric mean) . Under Assumption 2 and the further approxima-tion pm,j ≈ pm for all j ≤ L, Equation (3) reduces to E[a |

H] ≈ PLℓ=1 pℓ 

> m

= pm(1 − pL

> m

)/(1 − pm) → pm/(1 − pm)

as L → ∞ , the untruncated form being an upper bound for all finite L with relative truncation error pL

> m

.

Proof. Setting pm,j ≡ pm in Equation (3) gives the finite geometric series. Its truncated tail is 

X

> ℓ>L

pℓ 

> m

= pL 

> m

· pm

1 − pm

,

so the relative truncation error is pL

> m

.At L=15 :

pL 

> m

= 0 .002 , 0.035 , 0.206 at pm = 0 .65 , 0.80 , 0.90 ,

so the closed form is tight except near the entropy floor. Two errors are folded in: the truncation tail, and the slowly-varying step itself. The step overstates E[a | H] when pm,j 

decays in j. It understates the extreme-low-entropy mean when one pm is fit across a range containing a sharp low-

H spike: the fit’s supremum is 3.34 at b0=1 .226 , L=15 ,below the measured DocVQA bottom-decile mean of 5.23 

(see Theorem 2). Substituting pm(H) = σ(b0 − b1H) gives Equation (2), which we use as an operational conditional-mean character-ization, not an exact survival identity. 

Proposition 1 (Entropy controls the match, and the affine logit is a surrogate) . Let pm be the probability that the drafter’s root top-1 equals the target greedy token, and H

the target’s root entropy. (i) Rigorously, the target’s own top-

1 error e⋆ = 1 − max w p(w | x, b ) obeys Fano’s inequality 

H ≤ Hb(e⋆) + e⋆ log( |Σ| − 1) ,

so larger H forces larger e⋆ and H → 0 forces max w p → 1.(ii) Modeling the drafter’s logit error as logistic with scale 

s gives pm = σ(∆ /s ) for the target top-2 margin ∆( H),decreasing in H by (i). (iii) A first-order expansion of ∆ in 

H gives logit pm ≈ b0 − b1H with b1 = −∆′(H0)/s > 0.Part (i) predicts sign and monotonicity. Parts (ii) and (iii) are modeling steps that select the functional form. Proof. (i) View the target top-1 token as an estimator of a draw W ∼ p. Fano’s inequality bounds H by an increas-ing function of the error e⋆ on [0 , 1 − 1/|Σ|], so e⋆ grows with H and vanishes as H → 0.(ii) The drafter preserves the target ordering iff the perturbed margin stays positive. A difference of logistics is logistic, so 

logit pm = ∆ /s, 

and on a fixed-entropy shell ∆ is decreasing in H by (i). (iii) This is the first-order expansion. Its adequacy over the empirical window is what the curve R2 measures. A fuller derivation under a temperature family (target logits a fixed shape scaled by inverse temperature) shows 

logit ptgt (1) (H) is affine to R2 = 0 .84 to 0.90 over the resolv-able window H ∈ [0 .05 , 1.2] for Zipf, geometric-gap, and near-binary logit shapes (self-confidence slopes κ ≈ 3.2 to 

3.9). It reduces the match slope to a single drafter-noise scale 

s, distinct from the acceptance length τ of Equation (1), via 

b1 = κ/s . Matching the measured offset-1 slopes gives 

s = 3 .4/7.7/3.8 on captioning/TextVQA/DocVQA. The affine form is exactly a local linearization: the binary closed form is provably curved globally, so the law is stated over the empirical window only. 

Corollary (Grounded is more draftable, full). Under Equation (2), 

∂H pm = −b1pm(1 − pm),∂H E[a | H] = ∂H pm/(1 − pm)2 = −b1E[a | H] < 0,

so E[a | H] is strictly decreasing in H. Hence any regime with systematically lower next-token entropy has a longer expected accepted block. Grounded and OCR generation copies determinate glyph and answer strings from the im-age, collapsing the target’s entropy, so grounded spans are more draftable than open text and task-level mean accepted length is ordered by grounding. 

Proof. The derivative computation is displayed in the state-ment and uses only b1 > 0 and pm ∈ (0 , 1) . The ordering claim uses only the measured monotone decrease of a in H,not the fitted closed form. The measured instantiation orders mean accepted length by grounding: captioning 1.88 < TextVQA 2.12 < DocVQA 3.08 .

The later round logs extend the same ordering, 2.67 on In-foVQA and, under its templated prompts, 3.71 on ChartQA. The gap DocVQA − captioning = +1 .20 has 95% boot-strap CI [1 .07 , 1.33] and Mann-Whitney p = 8 .7 × 10 −68 ,and the grounded task carries the higher b0 and steeper b1

(Table 12). 

B.3 The Noise Ceiling 

Proposition 2 (Entropy-only noise ceiling) . Among all 

σ(H)-measurable predictors g(H) of a, the largest at-tainable coefficient of determination is ¯R2 = Var( E[a |

H]) /Var( a) = 1 − E[Var( a | H)] /Var( a), attained by the conditional mean. For a truncated-geometric a the within-round variance is of order E[a | H]2, so ¯R2 is intrinsically small regardless of predictor quality. 

Proof. For any g, conditional-mean orthogonality gives 

E[( a − g(H)) 2] = E[Var( a | H)] + E[( E[a | H] − g(H)) 2],

minimized at g = E[a | H]. The law of total variance then gives the stated ratio. For the untruncated geometric, with μ = E[a | H],

Var( a | H) = μ(1 + μ) ≥ μ2,

so within-round variance dominates unless Var( μ(H)) is comparably large. A nonparametric 50 -bin between/total variance estimate gives ceilings 

0.110 /0.144 /0.188 on captioning/TextVQA/DocVQA. The law’s round-level R2 = 0 .097 /0.091 /0.074 therefore realizes 89% /63% /39% of what any entropy-only predictor could, and the two later logs realize 62% on InfoVQA and 

50% on ChartQA (Table 12), which is why the curve R2 is the correct score. 

B.4 The Grounded Tail 

Assumption 3 (Two-state determinate or uncertain survival) .

Conditioned on the round, offset-wise matches follow a two-state hidden Markov chain over sj ∈ { D, U }: entry distri-bution (π, 1 − π), match probability ρs in state s, transi-tion matrix T upon a match, run ending at the first miss, capped at L. State D models verbatim copying ( ρD → 1,sticky tDD > 0). U is the ordinary moderate-hazard regime. The parameters governing H → 0 are estimated in regime, from near-zero-entropy rounds only, not extrapolated from the affine fit. The geometric of Lemma 1 is the degenerate case ρD = ρU .

Theorem 2 (full). Let M (p) = PLℓ=1 pℓ. (A) Under the single-hazard law (2), sup H≥0 E[a | H] = M (σ(b0)) ≤

eb0 : a rigorous, fit-free cap. (B) Under Assumption 3, with 

v = ( π, 1 − π), Dρ = diag( ρD , ρ U ), and K = T D ρ,

P[a ≥ ℓ | H] = vDρKℓ−11,

E[a | H] = vDρ

 PL−1 

> m=0

Km



1, (4) whose effective hazard hℓ is non-constant, and taking 

ρD , t DD → 1 gives E[a | H]H→0 → πL + (1 − π)M (ρU ),which exceeds eb0 exactly when π > π⋆ := (eb0 −

M (ρU )) /(L − M (ρU )) . This threshold lies in (0 , 1) be-cause M (ρU ) < e b0 < L (here L=15 , eb0 =3 .41 ). (C) With in-regime maximum-likelihood parameters, Equation (4) re-covers the measured DocVQA floor mean 5.23 versus the geometric cap 3.41 . The falsifiable validation is Proposi-tion 3. Proof. (A) M is strictly increasing on (0 , 1) , and pm(H)

is maximized as H → 0 where it equals σ(b0). Then 

M (p) ≤ p/ (1 − p) gives the eb0 bound. (B) Starting from v, the joint event “match at offset 1, now in state s” has mass (vDρ)s, and each accepted offset multiplies by K = T D ρ, so 

P[a ≥ ℓ | H] = vDρKℓ−11

and the tail-sum identity gives the mean. Survival is a nonnegative combination of the two eigenmodes of K,so the hazard hℓ is monotone toward λmax (K) and non-constant unless ρD = ρU . Its first value πρ D + (1 − π)ρU

can approach 1 and exceed σ(b0), which the constant-hazard family cannot. As ρD , t DD → 1,

P[a ≥ ℓ] → π for all ℓ ≤ L, 

giving the stated cap violation. (C) Numerical instantiation, recovering the measured floor mean against the geometric cap. 

Proposition 3 (Prompt-disjoint held-out falsification) .

On the 122 bottom-entropy-decile DocVQA rounds ( 47 

prompts), split by prompt into disjoint sets: (i) on the full floor the geometric run-length is rejected (Pearson χ2 = 105 .3,

df = 7 , p = 8 .7 × 10 −20 ) while the likelihood ratio de-cisively favors the two-state form ( 2∆ ℓ = 86 .5, df = 4 ,

p = 7 .2 × 10 −18 ). The two-state fit is itself also rejected in absolute terms ( χ2 = 29 .6, df = 3 ). (ii) Frozen on one prompt set and scored on the disjoint one, the two-state model attains a smaller held-out χ2 than the geometric in 6 of 7 tests (both split directions and five CV folds, mean held-out χ2

4.4 against 11 .9). (iii) Absolutely, the two-state survives the held-out shape test in one split direction ( χ2 = 6 .1, p = 0 .52 )and four of five folds, while the geometric survives in nei-ther direction. The load-bearing claim is the relative held-out dominance (ii). We do not claim the two-state law is the exact data-generating process. 

Proof. The proposition records the outcome of the stated estimation procedure. Construction and statistics are as listed. The frozen two-state mean prediction lands inside the un-seen prompts’ bootstrap 95% CI in both directions, 

5.14 ∈ [4 .69 , 6.02] , 5.35 ∈ [4 .53 , 5.77] ,

but so does the geometric’s: on these small folds the mean is a weak discriminator, which is why the falsifiable claim is distribution-level. The absolute rejection of the full-sample two-state fit is the finite-sample face of the noise ceiling (Proposition 2). 

B.5 Entropy-Keyed Width Allocation 

The economics of width for one-pass block drafting are cited machinery, not ours: the node-sum value of a tree, top-prefix optimality, the marginal stopping rule, and shared-budget global best-first allocation are taken as given from prior work (Chen, Liang, and Liu 2026; Ringel and Romano 2026; Zhang et al. 2026b). Our addition is the content key : the law indexes a round’s node masses by its entropy, so the round’s marginal value of width tracks the remaining miss-mass 

V (H) ∝ E[a | H] ·  1 − pm(H), (5) small when acceptance is hopeless or already deep, and peak-ing at intermediate entropy. 

Proposition 4 (Optimal content-indexed allocation) . Across rounds with entropies {Hr } under a shared verifier budget, the allocation maximizing total expected committed length is global best-first with entropy-indexed masses: marginal width goes to the rounds of highest V (Hr ), that is, the intermediate-entropy band, until the marginal mass falls be-low the token-time exchange rate. 

Proposition 5 (Small headroom, and when) . The gain 

∆adapt of the optimal content-indexed policy over uniform width at equal budget is positive iff width is genuinely scarce and non-trivial mass sits in the intermediate-entropy band, and it shrinks to zero as the entropy distribution becomes bimodal, and a large b1 thins the band (width ∼ 1/b 1) and drives ∆adapt → 0.

Proposition 6 (Regret of the entropy-keyed policy) . Let 

ΠH allocate by bV (H) from the fitted law and Π⋆ by real-ized round values. Then Regret(Π H ) ≤ P 

> r∈Π⋆△ΠH

|V ⋆r −

bV (Hr )| ≤ 2K E|V ⋆ − bV (H)|, and no σ(H)-measurable policy drives the regret below the irreducible H-conditional value variance (Proposition 2). 

Proof of Propositions 4 to 6. Proposition 4. By the cited node-sum decomposition, the shared-budget maximizer is the top-K globally highest-mass nodes. Substituting entropy-indexed masses and differentiating the truncated-geometric node sum gives 

V (H) ∝ E[a | H] (1 − pm(H)) ,

so best-first is best-V -first with the cited stopping rule keyed by H.

Proposition 5. Three regimes: 

• If width is affordable everywhere, the stopping rule al-ready saturates every round and the budget does not bind. 

• If almost all rounds have pm ≈ 1 or E[a | H] ≈ 0, then 

V ≈ 0 and reallocating moves o(1) .

• When the budget binds and V disperses, moving a slot from a low-V to a high-V round strictly gains, and by rearrangement the gain scales with the concentration of 

V , which bimodality and band-thinning kill. 

Proposition 6. A standard exchange argument: ΠH

maximizes P 

> r

bVr Πr , so the regret telescopes onto the disagreement set weighted by value-estimation errors, and conditional-mean orthogonality bounds any σ(H)-measurable value estimate from below by the conditional value variance. Empirically the headroom is as small as Proposition 5 predicts. At equal verifier budget the oracle width pol-icy improves accepted value over the best fixed width by +8 .5% /+3 .8% (captioning/TextVQA) at budget 31 and 

+3 .1% /+4 .3% at 47 (Figure 5). 30 40     

> verifier tokens / round
> 0.05 0.06 0.07 0.08 0.09
> committed acceptance per verifier token  N=31 N=47
> caption
> 35 40 45
> verifier tokens / round
> 0.06 0.08 0.10
> N=31 N=47
> TextVQA
> fixed-width entropy controller oracle ceiling

Figure 5: Accepted-value versus budget Pareto for oracle, entropy-keyed, and fixed-width policies. We therefore run fixed width in all main experiments and report the controller analysis as a characterization, not a headline lever. 

## C Classic Two-Model Speculative Decoding 

Classic two-model speculation, the original method that head-based drafting later specialized, runs on our target through the official HF assisted-generation path with three draft models: image-conditioned Qwen3-VL-4B and Qwen3-VL-2B, and a text-only Qwen3-1.7B in the spirit of the strong baseline of the first multimodal study (Gagrani et al. 2024) (Table 4). The 4B draft accepts long blocks ( τ 3.53 to 4.47 , rising with grounding) yet decodes at 0.61 to 0.80 ×, since a half-size draft costs roughly half a target forward for each drafted token and no acceptance amortizes that. The 2B draft accepts less ( 3.11 to 3.97 ) and stays below 1× throughout ( 0.66 to 

0.90 ×), so halving the draft again narrows the deficit without closing it. The text-only draft isolates what vision access is worth: acceptance collapses to 1.42 to 2.12 while the least grounded tasks still accept the least, so on grounded VLM workloads an image-conditioned drafter is not an optional refinement but the reason drafting pays. 

## D Implementation Details 

Primary drafter. The primary drafter is a block head in the DFlash family. It reads the target’s fused hidden state and proposes the whole block in one forward pass, with the vision tower and target decoder frozen. The final head is selected by held-out top-1 accuracy at each offset. Table 5 lists its configuration, with every value copied from the training run. Remaining constants of the run: intermediate size 12288 ,block horizon L=15 , image long side 768 px, AdamW betas 

0.9/0.95 with no weight decay and gradient clipping 1.0, seed 

0, one A6000-class GPU, bf16 weights ( 2.1 GB). 

OCR-retrained drafter. Replaces the 0.8offset decay with a survival-weighted coverage loss and adds 10 .2K self-generated OCR rows (DocVQA, ChartQA, InfoVQA, 26 .2Ktotal), warm-started from the primary head. The checkpoint used is a partial-epoch ( 40% ) one, sufficient to show the selective grounded-task lift of Table 17. 

Second and third targets. The Qwen3-VL-4B adapter fol-lows the primary recipe, passing the fp32 lossless gate and reproducing the transfer trends in Section 6. For Qwen2.5-VL-7B (Table 2), the head is trained from scratch on 26 ,180 

target greedy generations ( 8000 caption, 8000 TextVQA, and 

10 ,180 disjoint OCR rows) for seven epochs at global batch 

48 . Evaluated on an A100-80GB with a budget-63 tree, it matches baseline prompt counts task by task ( ≈47 ,800 tokens over ≈12 ,400 rounds) with zero fp32 divergences. An H100 crosscheck replicates τ within 2% (4.18 /3.21 /3.15 ); we re-port A100 figures as the primary benchmark to avoid batch-1

kernel launch latency distortions on H100. The Llama-3.2-11B-Vision adapter is warm-started from the Llama-3.1-8B DFlash head (held-out top-1@1 reaches 0.78 ), where cross-attention visual injection accounts for the boundary behavior in Section 6. 

Matched-training architecture comparison. Both archi-tectures are trained under identical conditions: the same 

26 ,196 -row OCR corpus, frozen target, global batch 24 , three epochs, and SpecForge. On the 256 -prompt held-out probe (greedy, 64 tokens), our budget-63 tree achieves an accep-tance ratio of 2.53 across three tasks and 2.73 (4.37 vs. 1.60 )over all five domains against the depth-3 EAGLE3 chain (depth-7 adds ≤ 0.01 ). This advantage is consistent across probe sizes ( 16 –256 prompts) and holds when retrained on ALLaVA-Instruct (Chen et al. 2024a) ( 2.04 vs. 1.29 ). In a timed decode-only A100 harness ( 20 prompts/task, 128 to-kens), GLANCE delivers 2.36 –2.59 × decode speedup over autoregressive decoding versus 1.13 –1.17 × for EAGLE3 (acceptance ratio 2.24 , decode-time ratio 2.12 ), confirming a robust 2.2–2.7× architectural advantage. These standalone probe figures are decode-only in HF; deployed system met-rics and sampling results appear in Tables 2 and 3. 

Acceptance protocol. Common decode settings: batch size 1, T =0 , maximum 256 new tokens, 20 prompts per domain (warmup excluded). 

• Engines & metric: EAGLE3-VL (production AQ-MedAI) and n-gram baselines run on vLLM 0.22 .1; ours uses an HF tree harness. Speedup is the ratio of mean per-token decode time against the autoregressive baseline, aligning with Medusa’s step-overhead formulation (Cai et al. 2024). We report end-to-end wall-clock speedup rather than theoretical τ / forward metrics to account for round-level overhead. 

• Output equivalence (Table 7): Evaluated over 60 

prompts ( 20 /task). Under fp32, GLANCE achieves 60 /60 

exact token matches against target greedy decoding, matching the drafter-free reference. In contrast, ViSpec and Medusa yield 0/21 exact matches across official, deep, and light tree configurations. Note that bf16 exhibits numerical drift even in drafter-free decoding ( 25 /60 exact matches). 

• Baselines & ablations: – Prompt lookup: Evaluated via vLLM; acceptance draft domain n τ ↑ [95% CI] speedup/AR ↑ [CI] byte-id. ↑                                                                                                                           

> Qwen3-VL-4B captioning 100 3.53 [3.42 ,3.64 ]0.61 ×[0.60 ,0.63 ]0.24
> TextVQA 100 3.79 [3.65 ,3.93 ]0.67 ×[0.65 ,0.69 ]0.30
> InfoVQA 100 3.95 [3.74 ,4.17 ]0.70 ×[0.68 ,0.72 ]0.54
> DocVQA 100 4.39 [4.19 ,4.60 ]0.76 ×[0.74 ,0.78 ]0.65
> ChartQA 100 4.47 [4.31 ,4.63 ]0.80 ×[0.79 ,0.82 ]0.65
> Qwen3-VL-2B captioning 100 3.11 [3.01 ,3.21 ]0.66 ×[0.64 ,0.68 ]0.24
> TextVQA 100 3.26 [3.15 ,3.39 ]0.72 ×[0.70 ,0.74 ]0.35
> InfoVQA 100 3.21 [3.07 ,3.35 ]0.74 ×[0.72 ,0.76 ]0.51
> DocVQA 100 3.58 [3.43 ,3.74 ]0.84 ×[0.82 ,0.87 ]0.61
> ChartQA 100 3.97 [3.81 ,4.13 ]0.90 ×[0.88 ,0.92 ]0.63
> Qwen3-1.7B (text-only) captioning 100 1.49 [1.46 ,1.51 ]0.36 ×[0.35 ,0.38 ]0.19
> TextVQA 100 1.42 [1.40 ,1.44 ]0.32 ×[0.31 ,0.34 ]0.39
> InfoVQA 100 1.90 [1.84 ,1.97 ]0.47 ×[0.45 ,0.49 ]0.49
> DocVQA 100 1.68 [1.64 ,1.72 ]0.43 ×[0.41 ,0.45 ]0.67
> ChartQA 100 2.12 [2.05 ,2.20 ]0.58 ×[0.56 ,0.61 ]0.61

Table 4: Full draft-level breakdown behind the Classic SD rows of Table 2, which are the 4B and text-only blocks. All three drafts run the same protocol on the same prompts, so the blocks are read against one another. Speedup is the within-engine ratio of the same run.                                   

> setting value
> Architecture
> layers 5
> parameters 1.05 Bblock size B16
> hidden size 4096
> attention heads 32 ×128
> kept target layers {1,9,17 ,25 ,33 }
> Objective
> loss coverage CE, 0.8offset weight reveal variable-mask block prefix
> Data
> source target greedy, 256 new tokens prompts 8000 COCO + 8000 TextVQA rows 16 K teacher-forced
> Optimization
> optimizer, lr AdamW, 4×10 −5
> epochs, accumulation 1,8
> max sequence length 2048
> warm start Qwen3-8B DFlash head

Table 5: The primary Qwen3-VL-8B drafter training card. length reflects proposed block length rather than step-level τ , with wall-clock speedup unaffected. 

– ViSpec & Medusa: Home-target ViSpec uses its default 

30 -token tree ( 3 passes/round) with τ aligned by adding the committed token. On Qwen3-VL-8B, both heads follow official two-stage training and length-controlled evaluation to prevent early-termination artifacts. 

– Ablations: Tree-versus-chain gains are isolated in Ta-ble 9. Classic SD endpoints reflect the three-draft sweep in Table 4. Mismatched-image probes use 60 

prompts/domain on a shared trajectory with prompt-clustered bootstrap CIs. We include n-gram lookup as the training-free baseline; lookahead decoding is omitted due to a lack of multimodal target support.                                        

> acceptance length τms/token chain length cap TVQA Doc cap TVQA Doc
> 32.17 2.47 2.36 6.77 6.78 7.46 5(operating point) 2.10 2.65 2.56 7.54 6.93 7.62 71.97 2.43 2.50 8.63 8.42 8.16 10 1.88 2.24 2.37 9.93 9.32 9.19 15 1.80 2.23 2.33 11 .96 10 .68 10 .68

Table 6: EAGLE3-VL chain-length sweep in vLLM, the only verifier-budget knob that engine exposes for this head. 

Timing protocol. All wall-clock numbers are decode-only, greedy, batch one, and byte-identity-gated. Absolute ms/token is never compared across engines, and only within-engine ratios are reported. An eager HF loop is roughly 17 

times slower in absolute terms than the native runner, which is exactly why cross-engine absolutes are meaningless. 

• Hardware: the Qwen3-VL-8B rows of Table 2, ours and the engine baselines alike, run on one RTX PRO 6000 Blackwell, both arms of every ratio on that card, the home-target rows on A100-80GB, the native full-loop on A6000 (the 8K bucket re-measured on A100-80GB, all systems on one card), the HF tree harness, including the budget sweep, on H100 NVL, and the HF forward-level rows and the matched-training timed harness on A100. Only within-bucket ratios are reported, so each comparison shares one card. The separation is measurable: replaying the identical home-target run at matched prompt counts on an A100 and on an H100 moves τ by at most 2.0% 

but moves the speedup by −0.9 to +8 .9% , because au-toregressive decoding is bandwidth-bound while our loop system arithmetic identical                           

> Same precision on both arms, our harness
> autoregressive, no drafter bf16 25 /60
> autoregressive, no drafter fp32 60 /60
> n-gram lookup (PLD) ‡bf16 27 /60
> GLANCE, budget-63 tree bf16 27 /60
> GLANCE, budget-63 tree fp32 60 /60
> GLANCE, matched-training head bf16 33 /60
> EAGLE3 head, matched training bf16 19 /60
> Same precision, each system in its released harness
> EAGLE3-VL (production, SGLang) bf16 48 /60
> ViSpec (official recipe) bf16 0/63
> Medusa (same codebase) bf16 0/63
> ViSpec (released head, home target) bf16 99 /520
> Cross-precision reference
> autoregressive, no drafter bf16 vs fp32 17 /60

Table 7: Output equivalence behind the lossless column of Table 2. Arithmetic : the precision both arms run in, except the last block, which scores bf16 against an fp32 reference. 

‡: measured on an A6000 while the rest of that block ran on the Blackwell card, and a bf16 identity rate depends on which kernels the card selects, so this row is read against 1.0

and not against its neighbours. is not, so the two arms rescale differently across cards. Table 2 reports the lower, A100 figures. 

• Same-engine EAGLE3-VL: both methods run in the same production engine, both CUDA-graph captured, with an eager cross-check. 

• Statistics: prompt counts differ by condition and are stated with each measurement, from 101 in each task of Table 1 to 200 on the home target. All reported ratios carry boot-strap 95% confidence intervals, listed in Appendix E. In the same-engine run the autoregressive arm lands within 

1% of itself across all five tasks, so the workloads differ in what they generate, not in what they cost to decode. Three replicates of the 2K-context same-engine compar-ison reproduce its margin. 

## E Timing Ledger 

Cost distribution. Across the five tasks, visual encoder prefill accounts for only 3–14% of a token’s end-to-end cost at the resolutions and lengths in Table 1, with the decode loop dominating the remainder. This prefill share decreases further as generated outputs grow. 

Bootstrap intervals (Table 1). The margin column (EAGLE3-VL ms /GLANCE ms − 1) matches the printed speedup ratios. Paired bootstrap over the 101 shared prompts (8000 resamples) yields: captioning −16 .2% [ −17 .4, −15 .0] 

(1/101 wins), TextVQA −19 .1% [ −20 .7, −17 .4] (1/101 

wins), InfographicVQA +7 .6% [+5 .3, +9 .7] (79 /101 wins), DocVQA +5 .4% [+2 .6, +8 .3] (71 /101 wins), and ChartQA 

+6 .0% [+4 .0, +8 .1] (70 /101 wins). All intervals exclude zero, confirming consistent prompt-level outcomes rather than outlier effects. Geometric means are +6 .3% across the three grounded tasks, −17 .7% over the two open tasks, and 

−4.0% overall. Averaging prompt-level ratios instead of tak-ing the ratio of means shifts figures by ≤ 1.1 points without reordering. 

Key ledger observations. Table 8 details wall-clock benchmarks across engines, context lengths, and samples; Table 10 presents the verifier-budget saturation sweep; Ta-ble 9 reports by-domain forward-level wall-clock; and Table 2 provides domain-level acceptance margins. Three findings stand out: 1. Value of tree width: The zero-confound contrast (identi-cal head run as tree vs. chain in the same harness) yields 

1.36 ×.2. Conservative reporting: When timed under identical harness conditions with EAGLE3-VL at its measured acceptance, grounded tasks yield 1.66 /1.76 /1.99 × (ge-omean 1.80 ×). We conservatively report the 1.26 × pro-jection, which grants EAGLE3-VL its production accep-tance rate. 3. Long-context robustness: When scaling context from 

2K to ∼6.7K tokens, acceptance drops 3.94 → 3.39 

(13 .9% ) for GLANCE versus 3.87 → 3.53 (8.7% ) for EAGLE3-VL. However, because GLANCE requires only 

1 draft pass per round versus 8 for EAGLE3-VL, end-to-end decode times remain within 2.3% of each other despite the steeper acceptance decline. The same asymmetry is visible in the production head’s own sweep, this time inside SGLang, where the engine ex-poses the draft-step count directly rather than the single chain-length knob vLLM offers in Table 6. Regressing its round time on the number of draft passes over five config-urations ( 4 to 48 draft tokens at 3 to 10 steps, captioning, one A6000) gives 24 .8 ms plus 1.82 ms for every sequential draft pass, with a constant term worth 1.03 target forwards. At batch one the verify is therefore near-flat in tree width while depth is paid a pass at a time. Widening our tree 4.2×

costs 3 to 5% of a round, whereas taking that head from two passes to eight costs 38% of one, with the component costs in Table 11. 

## F Additional Transfer and Robustness Results 

Statistics behind the characterization. Table 13 gives the task-level correlations and decile gaps behind Section 6: entropy correlates with acceptance in every task while the image-saliency signal vt does not survive conditioning on entropy (partial |r| ≤ 0.09 , within-decile gaps ≈ 1), which Figure 7 draws. Figure 8 shows the task-level law fits behind Table 12. 

The law on the two remaining tasks. Round logs col-lected after the main fits close the five-task family, and Ta-ble 12 carries their fits alongside the original three. InfoVQA follows the same streaming protocol as the originally logged tasks, while ChartQA is logged under the paper’s templated evaluation prompts and carries the steepest slope of the five. The isotonic curve R2 stays high on both, 0.979 and 0.991 ,comparison engine (mode) context n result [ 95% CI] GLANCE vs. AR same engine, CUDA graph 2K 44 2.28 ×

EAGLE3-VL vs. AR same engine, CUDA graph 2K 44 2.15 ×

GLANCE vs. AR native, full loop 2K 9 2.24 × [2 .05 , 2.53] 

GLANCE vs. AR native, full loop ∼6.7K 6 1.54 × [1 .42 , 1.69] 

GLANCE vs. EAGLE3-VL same engine, CUDA graph 2K 44 /44 +5 .8% [+2 .2, +9 .4] 

GLANCE vs. EAGLE3-VL same engine, eager 2K 44 /44 −1.0% [ −4.6, +2 .6] 

GLANCE vs. EAGLE3-VL same engine, CUDA graph ∼6.7K 24 /24 −2.3% [ −7.2, +2 .2] 

GLANCE vs. EAGLE3-VL same engine, eager ∼6.7K 24 /24 −7.1% [ −12 .5, −1.8] 

GLANCE vs. EAGLE3-VL cross-harness, eager 2K 9/12 −3.2% [ −14 .8, +9 .7] 

GLANCE vs. EAGLE3-VL cross-harness, eager ∼6.7K 6/8 −16 .5% [ −29 .3, −4.0] 

Table 8: Wall-clock comparisons by engine, mode, and context length, decode-only and byte-identity-gated. Percentages are 

(EAGLE3-VL ms − GLANCE ms )/EAGLE3-VL ms , a ratio of means over the shared prompts, signed so that positive means GLANCE is faster, with a paired bootstrap interval of that same statistic. 

ms/tok ↓ key speedups ↑

domain AR GLANCE tree GLANCE chain EAGLE3-VL (HF) vs. chain vs. EAGLE3-VL caption 18 .30 9.45 12 .88 16 .59 1.36 × 1.30 ×

TextVQA 18 .22 8.26 11 .42 15 .35 1.38 × 1.23 ×

DocVQA 18 .15 8.55 11 .33 18 .14 1.33 × 1.24 ×

geomean vs. AR 1.00 × 2.09 × 1.54 × 1.10 × 1.36 × 1.26 ×

Table 9: Domain-level ms/token behind the chain-row speedup column of Table 2. The geomean row carries the tree-versus-autoregression and zero-confound tree-versus-chain contrasts cited in Section 5.4. The EAGLE3-VL column is our faithful HF re-implementation, whose acceptance falls short of the production vLLM head’s. Dividing the printed ms/token would therefore flatter us, so the vs. EAGLE3-VL column instead charges EAGLE3-VL its production acceptance at the same round-level cost, which is the conservative comparison and the one we report. acceptance length τ ↑ speedup over AR ↑                                                 

> budget N15 31 47 63 15 31 47 63
> captioning 2.70 2.89 2.99 3.04 1.87 1.99 2.05 2.05
> TextVQA 2.95 3.14 3.25 3.29 1.95 2.21 2.24 2.25
> InfographicVQA 3.21 3.43 3.64 3.63 2.22 2.39 2.46 2.39
> DocVQA 3.35 3.61 3.68 3.78 2.31 2.49 2.51 2.55
> ChartQA 4.43 4.81 5.00 5.16 3.07 3.30 3.40 3.42

Table 10: Verifier-budget sweep on all five tasks in the same-engine tree harness. All four budgets of a task run in one process and share that task’s autoregressive baseline, so the speedup column is a function of N alone. so the monotone claim does not lean on the closed form. Both floors clear their own caps, 4.42 against 3.22 and 6.92 against 

4.50 , extending the copy-regime excess of Theorem 2 beyond DocVQA, and P (a≥8) rises monotonically with grounding, 

0.0, 0.9, 2.6, 4.4 and 11 .5% in the task order of Table 16. 

Causal-probe statistics. Behind the mismatched-image probe of Section 6 (protocol in Appendix D). The correct image lengthens accepted blocks, every interval excluding zero and the effect scaling with grounding: 

• captioning: +3 .5% (95% CI [+1 .5, +5 .7] ), 

• TextVQA: +6 .8% (95% CI [+4 .2, +9 .4] ), 

• DocVQA: +11 .7% (95% CI [+7 .7, +16 .3] ). 

component ms ×AR fwd AR decode forward 24 .5 1.00 

packed tree verify 26 .3 1.07 

one-pass draft 4.6 0.19 

one round (draft + verify) 31 .0 1.26 

Table 11: Forward-level cost of one decode round on Qwen3-VL-8B, all three components measured in one process on one A6000, decode-only. The packed tree verify and the one-pass draft replay from CUDA graphs. Matching images sharply reduces target mean next-token entropy; controlling for entropy, the residual visual effect is non-positive (pooled coefficient −0.07 ). 

Cross-attention boundary statistics. In Section 6, Llama-3.2-11B-Vision accepts tokens in inverse grounding order: captioning ( 3.76 ), DocVQA ( 3.09 ), and TextVQA ( 2.76 )in an fp32 run ( 15 prompts/task, 739 rounds). Entropy fits here and in Table 14 stem from a separate bf16 run ( 25 

prompts, 1315 rounds, captioning τ =3 .86 ), making the two non-interchangeable. Thus, the scaling law holds within cap-tioning but inverts sign on grounded short-answer domains. 

Law transfer, both axes. Table 14 details the transfer axes from Section 6. In Panel A, EAGLE3-VL uses our HF re-task rounds b0 b1 R2par R2iso R2rnd ceil. frac. mean a

caption 4026 0.819 0 .459 0 .949 0.990 0.097 89% 1.881 

TextVQA 2507 0.936 0 .578 0 .704 0.968 0.091 63% 2.120 

InfoVQA 1858 1.169 0 .804 0 .679 0.979 0.128 62% 2.672 

DocVQA 1218 1.226 0 .954 0 .452 0.964 0.074 39% 3.076 

ChartQA (templ.) 2190 1.504 1 .710 0 .543 0.991 0.138 50% 3.709 

pooled (main logs) 7751 0.964 0 .630 0 .705 0.917 0.107 62% 2.146 

Table 12: Entropy-acceptance fits behind Section 6: affine-logit coefficients, curve and round R2, ceiling fraction, and mean a.0 10 −2 10 0  

> entropy H(nats)
> 246
> accepted length

(a) By target 

> caption TextVQA DocVQA 0246
> mean accepted len. 2.80 4.38 5.39

(b) By task   

> Qwen3-VL Qwen2.5-VL Qwen2-VL

Figure 6: Entropy-acceptance fits for three targets (a) and task-level mean accepted length on Qwen2.5-VL-7B (b). caption TextVQA DocVQA 0.0 0.5 1.0 1.5 2.0 2.5 3.0 3.5 4.0    

> accepted length
> 2.1 2.3 2.2 1.7 2.3 3.4
> Acceptance by saliency
> low vt
> high vt

Figure 7: Acceptance split by image saliency within entropy deciles. implementation solely to test architectural form/sign transfer (distinct from Table 2), while the degenerate n-gram fit marks the no-model boundary. In Panel B, only Qwen3-VL-8B uses our block-diffusion adapter (other targets use same-family speculative proxies), confirming the empirical form is target-intrinsic rather than drafter-specific. Excluding a degenerate Qwen2.5-VL EAGLE3 baseline, all five pre-registered con-ditions pass. 

Beyond vision. The cross-modality fits of Table 15 span speech recognition, code, chat, and a non-Qwen backbone, all with b1 > 0. The mechanism differs by modality in exactly the way the two-state analysis of Appendix B.4 anticipates: grounded VL and code sit on the determinate-copy route (long verbatim runs), while speech behaves like a uniform position-level lift, and both are limits of the same survival 

quantity caption TextVQA DocVQA Spearman ρ(H, a ) −0.311 −0.351 −0.384 

p 3.8×10 −91 1.5×10 −73 4.0×10 −44 

low/high H-decile gap 2.30 × 2.49 × 3.04 ×

Spearman ρ(vt, a ) −0.098 −0.025 +0 .129 

partial r(vt, a | H) −0.058 −0.026 +0 .088 

within-decile vt gap 1.07 × 1.07 × 0.94 ×

Table 13: Correlations and decile gaps. Here a is accepted length, H entropy, vt image saliency. 0 10 −2 10 0  

> next-token entropy H(nats)
> 1234567
> accepted length
> caption TextVQA InfoVQA DocVQA ChartQA (templ.)

Figure 8: Task-level entropy-acceptance fits behind Table 12, ChartQA under its templated prompts. family. 

Head size, matched. Behind Figure 9: our head has 

1.05 B parameters against EAGLE3-VL’s 1-layer 399 .7M(0.80 GB). The capacity conclusion is drawn in Section 5.5. A like-for-like width match is not expressible for an autore-gressive head, whose depth costs sequential passes, which is precisely the asymmetry of Section 3.2. 

Conditioning ablation and n-gram floor. Table 16 tabu-lates the conditioning ablation cited in Section 5.5. Pooled over the five tasks and their 300 prompts, the direct-visual-fusion lift is +14 .1% (95% CI [+12 .8, +15 .5]% ), and it is the grounded tasks that carry it. The model-free n-gram lookup floor is tabulated in Table 2. It stays below the wide tree on every task. Panel A: varied drafter (same target Qwen3-VL-8B) 

task-level curve R2 ↑

drafter type b1 cap TVQA DocVQA ρ(H, a ) mean a ↑

GLANCE (block head) block-diff. 0.63 0.95 0.70 0.45 −0.37 2.15 

+ OCR retraining block-diff. 0.71 0.93 0.72 0.58 −0.40 2.20 

EAGLE3-VL AR 0.75 0.93 0.93 0.24 −0.22 0.67 

n-gram/PLD lookup 25 .0 0.00 0.59 0.57 −0.09 0.02 

Panel B: varied target (each non-adapter row a same-family proxy) 

target drafter b1 curve R2 ↑ ρ(H, a ) mean a ↑ form Qwen2.5-VL-7B 2-model proxy ( 3B→7B) 0.67 0.70 −0.42 3.67 yes Qwen2-VL-7B 2-model proxy ( 2B→7B) 0.40 0.95 −0.30 3.08 yes Llama-3.2-11B-V (captioning fit) block-diff. (ours) 0.41 0.91 −0.38 2.86 cap. only 

Table 14: Law transfer: Panel A varies the drafter on the fixed Qwen3-VL-8B target (its first row is also the reference row for Panel B). Panel B varies the target, and Figure 6 plots the same fits. Mean a is the accepted length without the bonus token in both panels. 

setting model pair b0 b1 τ ↑

speech (ASR) Qwen2.5-Omni 7B+3B 

2.07 0.49 5.41 

code Qwen2.5-Coder 7B+0.5B 

1.94 1.76 5.77 

chat two-model Wild-Chat 

1.66 0.64 4.33 

OLMo-2 (non-Qwen) OLMo-2 13B+7B 1.85 0.84 5.25 

Table 15: Functional-form transfer across modalities and to a non-Qwen backbone. 0 25 50      

> verifier budget
> 2.0 2.5 3.0
> τ (caption)
> (a) Budget sweep
> 510 15
> depth γ/ budget
> 020 40 60
> draft GFLOP / step
> (b) Draft cost
> ours one-pass tree EAGLE3-VL chain

Figure 9: Acceptance against verifier budget for the identical head run as chain and as tree (a), and draft cost against chain depth or budget (b).                               

> τ↑by what the head reads task fused VL (ours) text-only states zeroed text/ours captioning 2.92 2.84 1.10 0.97
> TextVQA 3.22 2.95 1.12 0.92
> InfoVQA 3.51 3.12 1.10 0.89
> DocVQA 3.62 2.91 1.10 0.80
> ChartQA 4.80 4.02 1.15 0.84
> mean 3.61 3.17 1.11 0.88

Table 16: Conditioning ablation on Qwen3-VL-8B: accep-tance length τ at budget 31 , sixty prompts in each task. The middle column replaces the target’s fused states with the hid-den states of a text-only Qwen3-8B at the text positions and zeros at the image positions, so that drafter never sees the image. 

τ ↑ @ budget 31 τ ↑ @ budget 63 

task base +OCR base +OCR caption 2.90 2.94 3.07 3.09 

TextVQA 3.28 3.28 3.48 3.44 

DocVQA 3.38 3.52 3.51 3.62 

Table 17: OCR-targeted retraining on Qwen3-VL-8B: accep-tance length τ before and after adding OCR-heavy training data, at budgets 31 and 63