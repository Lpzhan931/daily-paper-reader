---
title: "KV-Fold: One-Step KV-Cache Recurrence for Long-Context Inference"
title_zh: KV-Fold：用于长上下文推理的一步式 KV 缓存递归
authors: "Alireza Nadali, Patrick Cooper, Ashutosh Trivedi, Alvaro Velasquez"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12471v1"
tags: ["query:llm"]
score: 8.0
evidence: 长文本推理中的KV缓存递归协议
tldr: "KV-Fold 是一种无需训练的长文本推理协议，通过将 KV 缓存视为序列分块上的累加器（类似于函数式编程中的 foldl），实现了长程上下文的递归处理。该方法在处理当前分块时将之前的 KV 缓存作为前缀，通过简单的拼接操作实现状态传递。实验表明，该方法在 Llama-3.1 等模型上表现稳定，能在单张 40GB GPU 上实现 128K 长度的 100% 准确检索，为长文本推理提供了一种无需修改架构的高效方案。"
selection_source: fresh_fetch
motivation: 旨在解决长文本推理中内存限制与计算效率的矛盾，探索预训练 Transformer 模型在无需重训的情况下支持长程递归的可能性。
method: 将 KV 缓存作为累加器，通过分块处理并将新生成的键值对不断拼接至前缀缓存中，实现类似循环神经网络的递归推理。
result: "在 Llama-3.1-8B 上实现了 128K 长度内 100% 的大海捞针检索准确率，且数值漂移在长链条处理中表现出极高的稳定性。"
conclusion: 预训练 Transformer 模型原生支持稳定的 KV 缓存递归，KV-Fold 为长上下文推理提供了一种简单、稳定且无需训练的实用路径。
---

## 摘要
我们介绍了 KV-Fold，这是一种简单、无需训练的长上下文推理协议，它将键值（KV）缓存视为对序列块进行左折叠（left fold）时的累加器。在每一步中，模型以累积的缓存为条件处理下一个块，追加新生成的键和值，并将扩大的缓存向前传递；这种相同的一步更新被重复应用，类似于函数式编程中的 foldl。基于为潜在多智能体通信引入的 KV 缓存拼接原语，我们将其重新用途化为长上下文推理的块间递归。在处理块 t 时，模型将来自早期块的 KV 缓存作为前缀进行注意力计算，在不修改或重新训练模型的情况下跨分段重用其内部状态。尽管其简单，但由此产生的递归是稳定的：每步漂移在短暂上升后会饱和进入一个平坦的平台期，并贯穿深层链条。该平台期对 10,000 倍的数值精度变化不敏感，在不同块大小下具有鲁棒性，并且在不同模型系列中保持一致。在任务层面，KV-Fold 能够在长距离内保留准确信息。在“大海捞针”基准测试中，它在 Llama-3.1-8B 上跨越 16K 到 128K token 的上下文以及高达 511 的链深度，在 152 次试验中实现了 100% 的精确匹配检索，同时保持在单个 40GB GPU 的显存限制内。与以保真度换取有限内存的流式方法相比，KV-Fold 在作为一系列可处理的前向传递运行时，保持了长距离检索能力。总之，我们的结果表明，冻结的预训练 Transformer 已经支持一种稳定形式的 KV 缓存递归，为无需架构更改或训练的长上下文推理提供了一条实用路径。

## Abstract
We introduce KV-Fold, a simple, training-free long-context inference protocol that treats the key-value (KV) cache as the accumulator in a left fold over sequence chunks. At each step, the model processes the next chunk conditioned on the accumulated cache, appends the newly produced keys and values, and passes the enlarged cache forward; the same one-step update is applied repeatedly, analogous to foldl in functional programming. Building on the KV cache concatenation primitive introduced for latent multi-agent communication, we repurpose it as a chunk-to-chunk recurrence for long-context inference. When processing chunk t, the model attends to the KV cache carried from earlier chunks as a prefix, reusing its internal state across segments without modifying or retraining the model. Despite its simplicity, the induced recurrence is stable: per-step drift rises briefly and then saturates into a flat plateau that persists across deep chains. This plateau is insensitive to a 10,000x change in numerical precision, robust across chunk sizes, and consistent across model families. At the task level, KV-Fold preserves exact information over long distances. On a needle-in-a-haystack benchmark, it achieves 100% exact-match retrieval across 152 trials spanning contexts from 16K to 128K tokens and chain depths up to 511 on Llama-3.1-8B, while remaining within the memory limits of a single 40GB GPU. Compared to streaming methods, which trade fidelity for bounded memory, KV-Fold maintains long-range retrieval while operating as a sequence of tractable forward passes. Overall, our results show that frozen pretrained transformers already support a stable form of KV-cache recurrence, providing a practical route to long-context inference without architectural changes or training.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **KV-Fold** 的长文本推理协议，旨在通过一种简单且无需训练的方法，让预训练 Transformer 模型能够处理超出其单次前向传递限制的长序列。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：标准 Transformer 的注意力机制计算成本随序列长度呈二次方增长，导致长文本推理受限于显存。
*   **研究动机**：现有的长文本方案（如流式方法、KV 缓存压缩、递归神经网络化等）通常需要重新训练模型、修改架构，或者会因丢弃/压缩历史信息而导致检索精度下降。
*   **整体含义**：作者探索是否可以利用预训练模型现有的 KV 缓存作为一种“递归状态”，在不改变模型参数的情况下，通过分块处理和状态传递来实现长程上下文的稳定推理。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将 KV 缓存视为函数式编程中 `foldl`（左折叠）操作的累加器。将长序列划分为多个分块（Chunks），逐块处理。
*   **关键流程**：
    1.  **分块处理**：将长度为 $T$ 的序列分为 $N$ 个长度为 $C$ 的块。
    2.  **前缀注入**：在处理第 $t$ 个块时，模型不仅关注当前块的 Token，还将前 $t-1$ 个块累积生成的 KV 缓存作为“前缀”直接拼接到当前注意力计算中。
    3.  **状态更新**：当前块生成的 KV 键值对被追加到累加器中，传递给下一个块。
    4.  **位置编码对齐**：保持旋转位置嵌入（RoPE）的连续性，确保位置 ID 与原始长序列一致。
*   **递归视角**：该方法将单次庞大的前向传递转变为一系列可管理的、带有状态传递的递归步骤，且无需对模型进行任何微调。

### 3. 实验设计
*   **数据集/场景**：
    *   **PG-19 验证集**：用于测量预测的负对数似然（NLL/困惑度），评估语言建模的稳定性。
    *   **大海捞针（Needle-in-a-haystack）**：用于测试模型在长距离（最高 128K token）下的精确信息检索能力。
*   **对比方法（Baselines）**：
    *   **FULL**：全注意力模型（理论上限）。
    *   **ISOLATED**：独立处理每个分块，不携带历史信息（性能下限）。
    *   **StreamingLLM**：主流的流式推理方法（使用固定大小的滑动窗口和注意力锚点）。
*   **测试模型**：Qwen2.5-7B/1.5B、Llama-3.1-8B、OLMoE-1B-7B。

### 4. 资源与算力
*   **硬件设备**：主要使用单张 **NVIDIA A100 40GB GPU**。
*   **算力表现**：
    *   在 128K 上下文、511 层链深度的 Llama-3.1-8B 实验中，总耗时约 171 秒。
    *   显存占用随长度线性增长（约 0.13 KB/token），在 128K 长度时达到 35.6 GB 峰值。
*   **训练时长**：该方法为 **Training-free（无需训练）**，因此无训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 152 次“大海捞针”检索试验，涵盖 16K 到 128K 的不同长度。
    *   进行了多模型家族（Qwen, Llama, OLMoE）的交叉验证。
    *   **消融实验**：包括数值精度（bf16 vs fp32，跨度 10,000 倍）、分块大小（128 到 1024）以及多针检索（Multi-needle）测试。
*   **充分性评价**：实验设计较为全面，不仅关注宏观的困惑度指标，还深入探讨了数值漂移的来源（结构性而非数值性），证明了结论的稳健性。

### 6. 主要结论与发现
*   **稳定性平台期**：KV-Fold 引起的预测漂移在最初几个分块后会迅速进入一个平坦的“平台期”，不会随链条深度无限累积。
*   **高保真检索**：在 Llama-3.1-8B 的 128K 原生窗口内，即使经过 511 次递归转换，仍能保持 **100% 的检索准确率**，远超 StreamingLLM。
*   **操作可行性**：虽然总计算量未减少，但 KV-Fold 将无法在单卡运行的超长全注意力计算（如 128K 长度需要 1TB 显存存得分矩阵）分解为显存可控的分块计算。

### 7. 优点
*   **简单高效**：无需修改模型架构，无需微调，直接适用于现有的预训练模型。
*   **信息无损**：相比压缩或丢弃历史的方法，它保留了完整的 KV 历史，确保了精确检索能力。
*   **稳定性强**：发现并验证了 Transformer 内部状态在递归下的自洽性，漂移不随深度增长。

### 8. 不足与局限
*   **显存线性增长**：KV 缓存随序列长度线性增加，无法像流式方法那样实现恒定内存推理，受限于硬件显存上限。
*   **推理延迟**：随着缓存变大，后续分块的计算延迟会逐渐增加（$O(C \cdot \text{cache\_size})$）。
*   **范围限制**：实验主要在模型的原生训练窗口（如 128K）内进行，若要突破此限制，仍需结合 YaRN 或 LongRoPE 等位置外推技术。

（完）
