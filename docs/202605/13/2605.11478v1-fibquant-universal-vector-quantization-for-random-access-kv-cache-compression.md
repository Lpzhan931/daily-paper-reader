---
title: "FibQuant: Universal Vector Quantization for Random-Access KV-Cache Compression"
title_zh: FibQuant：用于随机访问 KV 缓存压缩的通用矢量量化
authors: "Namyoon Lee, Yongjune Kim"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11478v1"
tags: ["query:kvcache-survey"]
score: 9.0
evidence: 用于KV缓存压缩的向量量化方法
tldr: 针对长文本推理中KV缓存带来的内存瓶颈，本文提出FibQuant，一种通用的定率矢量量化方法。该方法在保留旋转标量编解码接口的基础上，利用归一化后的球形Beta分布特性构建径向-角度码本，结合斐波那契准均匀方向采样和Lloyd-Max优化。FibQuant在保持随机访问特性的同时，显著提升了压缩效率和保真度，在极低比特下表现远优于传统标量量化，实现了高效的KV缓存压缩。
selection_source: fresh_fetch
motivation: 现有的旋转标量量化方法在归一化和旋转后忽略了数据的几何分布特性，导致在低比特率下压缩保真度受限。
method: 引入一种通用的矢量量化器，通过结合Beta分位数半径、斐波那契准均匀方向和Lloyd-Max精细化构建径向-角度码本。
result: 在TinyLlama-1.1B模型上，FibQuant在8倍压缩下的困惑度比标量量化方法低3.6倍，且支持亚比特级别的压缩点。
conclusion: FibQuant证明了利用高维几何结构进行矢量量化可以显著提升KV缓存的压缩比和推理精度，是长文本推理的有效方案。
---

## 摘要
长上下文推理正日益成为一个内存带宽问题。罪魁祸首是键值（KV）缓存：它随上下文长度、批次大小、层数和头数而增长，且在每个解码步骤中都会被读取。基于旋转的标量编解码器通过存储范数、应用共享随机旋转并逐个坐标进行量化来满足这一系统约束。它们具有通用性和随机访问性，但丢弃了归一化步骤所产生的几何结构。在 Haar 旋转之后，由 $k$ 个连续坐标组成的块不再是乘积源，而是单位球上的球面 Beta 源。我们引入了 FibQuant，这是一种通用的固定速率矢量量化器，它保留了相同的“归一化-旋转-存储”接口，同时将标量表替换为与该规范源匹配的共享径向-角向码本。该码本结合了 Beta 分位数半径、Fibonacci/Roberts--Kronecker 准均匀方向以及多重重启的 Lloyd--Max 细化。我们证明，在相同速率下，所得矢量编码严格优于其标量乘积特例，其高比特率增益可分解为单元整形因子和密度匹配因子。同样的构造提供了一个密集的速率轴，包括分数比特和亚比特工作点，且无需校准或变长地址。在 GPT-2 small 的 KV 缓存上，FibQuant 描绘了一条内存-保真度前沿：从 0.99 注意力余弦相似度下的 5 倍压缩到 0.95 下的 34 倍压缩。在 TinyLlama-1.1B 上的端到端测试中，它在 4 倍压缩下的困惑度与 fp16 相比差距在 0.10 以内；在 $b=2$（8 倍压缩）时，其困惑度比标量 TurboQuant 低 3.6 倍，而此时标量随机访问量化已开始失效。

## Abstract
Long-context inference is increasingly a memory-traffic problem. The culprit is the key--value (KV) cache: it grows with context length, batch size, layers, and heads, and it is read at every decoding step. Rotation-based scalar codecs meet this systems constraint by storing a norm, applying a shared random rotation, and quantizing one coordinate at a time. They are universal and random-access, but they discard the geometry created by the normalization step. After a Haar rotation, a block of $k$ consecutive coordinates is not a product source; it is a spherical-Beta source on the unit ball. We introduce \textsc{FibQuant}, a universal fixed-rate vector quantizer that keeps the same normalize--rotate--store interface while replacing scalar tables by a shared radial--angular codebook matched to this canonical source. The codebook combines Beta-quantile radii, Fibonacci\,/\,Roberts--Kronecker quasi-uniform directions, and multi-restart Lloyd--Max refinement. We prove that the resulting vector code strictly improves on its scalar product specialization at matched rate, with a high-rate gain that separates into a cell-shaping factor and a density-matching factor. The same construction gives a dense rate axis, including fractional-bit and sub-one-bit operating points, without calibration or variable-length addresses. On GPT-2 small KV caches, \textsc{FibQuant} traces a memory--fidelity frontier from $5\times$ compression at $0.99$ attention cosine similarity to $34\times$ at $0.95$. End-to-end on TinyLlama-1.1B, it is within $0.10$ perplexity of fp16 at $4\times$ compression and has $3.6\times$ lower perplexity than scalar \textsc{TurboQuant} at $b = 2$ ($8\times$ compression), where scalar random-access quantization begins to fail.

---

## 论文详细总结（自动生成）

### 论文总结：FibQuant —— 用于随机访问 KV 缓存压缩的通用矢量量化

#### 1. 核心问题与整体含义
随着大语言模型（LLM）上下文长度的增加，KV 缓存（Key-Value Cache）成为了推理过程中的主要内存瓶颈。现有的压缩方法（如 TurboQuant）虽然通过随机旋转实现了通用且可随机访问的标量量化，但忽略了向量在归一化和旋转后形成的特定几何结构。
**核心问题：** 标量量化无法捕捉高维空间中的“形状增益”和“密度匹配增益”，导致在极低比特（如亚比特或 1-2 比特）下保真度迅速下降。FibQuant 旨在通过矢量量化（VQ）填补这一空白，提供一种无需校准、固定速率且支持随机访问的高效压缩方案。

#### 2. 方法论
FibQuant 沿用了“归一化-旋转-存储”的系统接口，但将量化对象从单个坐标提升为 $k$ 维向量块。
*   **规范源识别：** 论文证明，任何输入分布在经过 Haar 随机旋转和归一化后，其 $k$ 维子块均服从**球面-Beta 分布（Spherical-Beta distribution）**。这使得离线构建一个通用的、跨模型/层/头的码本成为可能。
*   **径向-角度码本构造：**
    *   **半径（Radii）：** 使用基于 Bennett-Gersho 压缩扩张理论的 Beta 分位数来匹配径向密度。
    *   **方向（Directions）：** 采用准均匀球形点集。$k=2$ 时使用斐波那契螺旋，$k=3$ 时使用斐波那契球，$k \ge 4$ 时使用 Roberts-Kronecker 序列。
    *   **精细化：** 通过多重重启的 Lloyd-Max 算法对初始码本进行离线优化，以最小化均方误差（MSE）。
*   **密集速率轴：** 通过调整块大小 $k$ 和码本大小 $N$，FibQuant 可以实现任意分数比特（如 2.5 bit）甚至亚比特（< 1 bit）的压缩，而无需改变内存寻址逻辑。

#### 3. 实验设计
*   **场景与数据集：**
    *   **内在质量：** 在规范球面-Beta 源上测试每坐标 MSE。
    *   **注意力保真度：** 在 GPT-2 small 上测试注意力输出的余弦相似度（使用 WikiText 提示词）。
    *   **端到端性能：** 在 TinyLlama-1.1B 上测试 WikiText-103 的困惑度（PPL）和 HellaSwag 的零样本准确率。
*   **对比方法（Benchmarks）：**
    *   标量量化：Per-token INT (2/4/8-bit), TurboQuant (旋转标量量化)。
    *   系统级方案：KIVI (2-bit 异步量化)。
    *   令牌剔除方案：StreamingLLM, H2O。
    *   低秩投影：SVD (需校准)。

#### 4. 资源与算力
*   **算力说明：** 论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节：** 提到码本构建是离线完成的，生成的通用码本库大小约为 5MB。在推理时，编码涉及最近邻搜索（可通过分层搜索加速），解码仅为简单的查表和逆旋转。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从理论分布模拟到真实模型（GPT-2, TinyLlama）的完整链路。
*   **充分性：** 实验设计较为充分。作者不仅对比了同类量化方法，还跨类别对比了令牌剔除和低秩投影方法。通过消融实验展示了不同 $k$ 值对性能的影响，并证明了矢量量化在低比特下的理论优势。
*   **客观性：** 实验在相同的内存预算（比特率）下进行对比，确保了公平性。

#### 6. 主要结论与发现
*   **性能碾压：** 在相同比特率下，FibQuant 的性能严格优于标量旋转量化。在 8 倍压缩（2-bit）下，其困惑度比 TurboQuant 低 3.6 倍。
*   **极端压缩：** 在 GPT-2 上实现了高达 34 倍的压缩比，且保持了 0.95 的注意力相似度，这是其他通用随机访问方法无法达到的。
*   **亚比特可行性：** 证明了在不牺牲随机访问特性的前提下，利用矢量量化可以有效运行在 1-bit 以下的机制中。

#### 7. 优点
*   **理论严谨：** 准确识别了旋转后的规范源分布，为码本设计提供了数学依据。
*   **通用性强：** 无需针对特定模型进行校准（Calibration-free），码本可跨模型复用。
*   **系统友好：** 保持了固定速率和随机访问特性，易于集成到现有的 Flash-Attention 等融合算子中。
*   **速率灵活：** 支持分数比特，允许根据硬件内存预算精确调整压缩率。

#### 8. 不足与局限
*   **编码复杂度：** 矢量量化的编码过程（最近邻搜索）比标量量化复杂。虽然附录提出了分层搜索方案，但在超大规模码本下的实时编码延迟仍需进一步验证。
*   **模型规模限制：** 端到端实验主要集中在 1.1B 规模的模型上，尚未在大规模模型（如 Llama-3 70B+）上验证其在极长上下文下的表现。
*   **硬件实现：** 论文主要侧重于算法和理论，缺乏在特定硬件（如 Triton 或 CUDA 内核）上的实际推理延迟（Latency）评测。

（完）
