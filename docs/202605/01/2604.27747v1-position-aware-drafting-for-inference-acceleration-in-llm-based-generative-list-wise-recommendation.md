---
title: Position-Aware Drafting for Inference Acceleration in LLM-Based Generative List-Wise Recommendation
title_zh: 基于大语言模型的生成式列表推荐中用于推理加速的位置感知草稿技术
authors: "Jiaju Chen, Chongming Gao, Chenxiao Fan, Haoyan Liu, Qingpeng Cai, Peng Jiang, Xiangnan He"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27747v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 基于LLM的推荐系统中的投机解码推理加速
tldr: 针对基于大语言模型的生成式列表推荐推理延迟高的问题，本文提出PAD-Rec框架。该方法通过引入位置感知草稿模型，利用项内位置嵌入和步长位置嵌入来捕捉语义槽信息及预测深度的不确定性。PAD-Rec在保持推荐质量的同时，显著提升了投机采样的接受率，在多个数据集上实现了最高3.1倍的推理加速，为高效生成式推荐提供了新思路。
selection_source: fresh_fetch
motivation: 传统的投机采样在生成式推荐中忽略了Token在项内的语义位置以及随预测深度增加而增长的不确定性，导致草稿模型接受率受限。
method: 引入PAD-Rec模块，通过项内位置嵌入和步长位置嵌入增强草稿模型，并结合可学习门控机制来动态调整这些位置信号的影响。
result: "在四个真实数据集上的实验显示，该方法实现了最高3.1倍的墙钟时间加速，且在强基线基础上平均提升了约5%的加速增益。"
conclusion: 通过显式建模位置和步长信息，PAD-Rec能以极低的额外开销显著提升生成式推荐的推理效率，同时保持推荐质量。
---

## 摘要
基于大语言模型（LLM）的生成式列表推荐发展迅速，但解码过程仍是顺序进行的，因此容易产生延迟。为了在不改变目标分布的情况下加速推理，投机采样（Speculative Decoding, SD）使用一个小型的草稿模型一次性提出多个后续 token，并由目标 LLM 进行验证并接受最长前缀，从而在每轮中跳过多个步骤。然而，在生成式推荐中，每个项目都由多个语义 ID token 表示，通常带有分隔符，而目前的草稿模型通常统一处理这些 token。这忽略了两个实际事实：(i) token 的语义取决于其在项目内的槽位（slot），以及 (ii) 不确定性往往随着投机深度的增加而增加。如果不对这些效应进行建模，SD 的加速效果可能会受到限制。我们提出了 PAD-Rec（用于生成式推荐的位置感知草稿），这是一个轻量级模块，通过两种互补信号增强了草稿模型。项目位置嵌入显式编码了每个 token 的项目内槽位，增强了结构感知能力。步骤位置嵌入编码了草稿步骤，使模型能够适应与深度相关的不确定性并提高提议质量。为了使这些信号与基础特征协调，我们添加了简单的门控机制：用于项目槽位的可学习系数和用于草稿步骤的上下文驱动门控。该模块是可训练的，易于与标准草稿模型集成，且增加的推理开销微乎其微。在四个真实世界数据集上的广泛实验表明，与强 SD 基线相比，该方法实现了高达 3.1 倍的实际时间加速，平均实际时间加速提升约 5%，同时在很大程度上保持了推荐质量。

## Abstract
Large language model (LLM)-based generative list-wise recommendation has advanced rapidly, but decoding remains sequential and thus latency-prone. To accelerate inference without changing the target distribution, speculative decoding (SD) uses a small draft model to propose several next tokens at once and a target LLM to verify and accept the longest prefix, skipping multiple steps per round. In generative recommendation, however, each item is represented by multiple semantic-ID tokens, often with separators, and current drafts typically treat these tokens uniformly. This overlooks two practical facts: (i) a token's semantics depend on its within-item slot, and (ii) uncertainty tends to increase with speculation depth. Without modeling these effects, SD's speedups can be limited. We introduce PAD-Rec, Position-Aware Drafting for generative Recommendation, a lightweight module that augments the draft model with two complementary signals. Item position embeddings explicitly encode the within-item slot of each token, strengthening structural awareness. Step position embeddings encode the draft step, allowing the model to adapt to depth-dependent uncertainty and improve proposal quality. To harmonize these signals with base features, we add simple gates: a learnable coefficient for item slots and a context-driven gate for draft steps. The module is trainable, easy to integrate with standard draft models, and adds negligible inference overhead. Extensive experiments on four real-world datasets show up to 3.1x wall-clock speedup and about 5% average wall-clock speedup gain over strong SD baselines, while largely preserving recommendation quality.

---

## 论文详细总结（自动生成）

这是一份关于论文《Position-Aware Drafting for Inference Acceleration in LLM-Based Generative List-Wise Recommendation》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：基于大语言模型（LLM）的生成式推荐系统（如将项目编码为语义ID进行生成）虽然效果显著，但受限于自回归解码的顺序性，推理延迟极高，难以满足实时推荐需求。
*   **核心问题**：现有的加速技术——投机解码（Speculative Decoding, SD）在通用文本生成上表现良好，但在**生成式推荐**场景下存在两个被忽视的挑战：
    1.  **项内语义槽位（Within-item Slots）**：推荐输出具有严格结构（如每个项目由4个语义ID组成），Token的含义高度依赖其在项目内的位置，而现有草稿模型统一处理Token，缺乏结构感知。
    2.  **深度驱动的不确定性**：随着草稿步数（Draft Steps）增加，误差累积导致不确定性上升，现有模型未能根据预测深度动态调整策略。

### 2. 方法论（PAD-Rec）
PAD-Rec 提出了一种位置感知的轻量级草稿模块，核心思想是在草稿模型中注入两种位置信号：
*   **项目位置嵌入（Item Position Embeddings, IPE）**：显式编码Token在项目元组中的索引（如第1到第K个槽位），增强模型对推荐列表结构的感知能力。
*   **步骤位置嵌入（Step Position Embeddings, SPE）**：编码当前处于投机解码的第几个步骤，使模型能够学习到随深度变化的不确定性模式。
*   **关键技术细节与门控机制**：
    *   **阶段1（槽位融合）**：将Token嵌入与IPE相加，并通过一个可学习的全局系数 $g_{item}$ 控制影响权重，随后与前一步特征拼接。
    *   **阶段2（深度融合）**：在投影回特征空间后，加入由上下文驱动门控 $g_{step}$ 调节的SPE。该门控通过Sigmoid函数根据当前隐藏状态动态决定深度信号的注入强度。
*   **训练流程**：采用多步展开（Unrolled）蒸馏策略，模拟推理时的多步草稿环境，使草稿模型在训练阶段就适应特征替换带来的偏差。

### 3. 实验设计
*   **数据集**：使用了四个真实世界数据集：Amazon 的三个子集（Beauty, Instruments, Games）以及 Yelp 商业评论数据集。
*   **目标模型（Target LLM）**：以 Llama-3.2-1B-Instruct 为主，并扩展至 3B 和 8B 版本。
*   **对比基线（Baselines）**：
    *   **EAGLE-2**：基于特征的动态树状投机解码。
    *   **HASS**：采用多步对齐训练的强力基线。
    *   **FSPAD**：通过特征采样和部分对齐蒸馏优化。
    *   **GRIFFIN**：侧重于Token对齐的投机解码方法。
*   **评价指标**：墙钟时间加速比（Speedup）、平均接受长度（$\tau$）、推荐准确度（Recall@10）和排序质量（NDCG@10）。

### 4. 资源与算力
*   **训练算力**：
    *   1B 参数模型：在单张 **NVIDIA RTX 3090** GPU 上训练。
    *   3B 和 8B 参数模型：在 4 张 **NVIDIA A100** GPU 上训练。
*   **推理测试**：所有延迟测量均在 **NVIDIA RTX 3090** 上完成（半精度 fp16，Batch Size 为 1）。
*   **实现细节**：草稿模型仅由一层 Transformer 块组成，PAD-Rec 引入的额外参数量极小（约占草稿模型的 0.01%）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个数据集、2 种采样温度（0 和 0.5）、3 种模型规模（1B, 3B, 8B）。
*   **消融实验**：详细对比了“无IPE”、“无SPE”、“无门控”等变体，验证了各组件的必要性。
*   **超参分析**：探讨了投机深度（Speculation Depth）对加速比的影响，寻找最优的推理步数。
*   **客观性**：实验采用了统一的硬件环境和相同的目标模型，对比基线均使用了官方配置，确保了结果的公平性。

### 6. 主要结论与发现
*   **加速效果**：PAD-Rec 在不同数据集上实现了 **2.2x 至 3.1x** 的墙钟时间加速。
*   **性能增益**：相比于最强基线 HASS，PAD-Rec 平均带来了约 **5%** 的额外加速提升。
*   **质量保持**：在加速的同时，Recall 和 NDCG 指标与原始 LLM 相比几乎没有损失（偏差极小），证明了其无损加速的特性。
*   **缩放规律**：随着目标模型规模增大（从 1B 到 8B），加速比呈现上升趋势，说明该方法在大模型上更具优势。

### 7. 优点与亮点
*   **结构感知**：首次针对生成式推荐的“项目-槽位”结构设计了专门的投机信号，填补了通用 SD 技术的空白。
*   **轻量高效**：通过简单的嵌入查找和标量门控实现，几乎不增加推理时的计算负担。
*   **即插即用**：该模块可以轻松集成到现有的基于特征的草稿模型（如 EAGLE 或 HASS）中。

### 8. 不足与局限
*   **结构依赖性**：该方法高度依赖于项目被编码为固定长度语义 ID（如 RQ-VAE 产生的 4 个 ID）的假设。如果推荐系统使用变长文本 ID 或非结构化描述，IPE 的效果可能会打折扣。
*   **任务局限**：目前仅在列表式推荐（List-wise）任务上验证，对于对话式推荐或更复杂的交互式生成场景，其位置信号的定义可能需要重新设计。
*   **硬件敏感性**：加速比受限于 GPU 的算力与带宽比，在不同架构的硬件上表现可能存在波动。

（完）
