---
title: "OSAQ: Outlier Self-Absorption for Accurate Low-bit LLM Quantization"
title_zh: OSAQ：用于精确低比特大语言模型量化的离群值自吸收技术
authors: "Zhikai Li, Zhen Dong, Xuewen Liu, Jing Zhang, Qingyi Gu"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04738v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于推理加速的低比特大模型量化
tldr: 针对大语言模型低比特量化中权重离群值导致的性能下降问题，本文提出OSAQ方法。该方法利用Hessian矩阵的二阶低秩特性，在不影响任务损失的前提下，通过在Hessian零空间内构建加性权重变换来抑制离群值。OSAQ可离线吸收进权重，无推理开销，在2比特量化下显著提升了模型精度。
selection_source: fresh_fetch
motivation: 权重中存在的系统性离群值是实现高精度低比特LLM量化的主要障碍，而现有缩放或旋转方法效果有限。
method: 利用Hessian矩阵在不同输入下的低秩一致性，通过其零空间向量构建加性变换来抑制权重离群值，且该变换可离线合并。
result: "实验表明OSAQ能有效抑制离群值，在2比特量化下配合GPTQ可使困惑度比原始GPTQ降低超过40%。"
conclusion: OSAQ提供了一种高效、无额外推理开销的权重变换方案，显著增强了LLM在极低比特下的量化表现。
---

## 摘要
大语言模型（LLMs）展现出了卓越的能力。然而，其庞大的参数规模导致了推理过程中显著的资源消耗和延迟。训练后仅权重（weight-only）量化通过减小模型大小并缓解内存受限问题来加速 Token 生成，提供了一种极具前景的解决方案。尽管如此，权重中固有的系统性离群值（outliers）仍然是一个主要障碍。虽然现有的缩放（scaling）和旋转（rotation）等方法试图解决这一问题，但其性能仍然不尽如人意。在本文中，我们提出了离群值自吸收量化（OSAQ），该方法在二阶低秩特性的指导下进行加性权重抑制，用于大语言模型的低比特仅权重量化。具体而言，我们观察到 Hessian 矩阵在不同输入下表现出低秩一致性，某些方向始终显示出趋于零的曲率。利用这一特性，我们识别出 Hessian 矩阵的一个稳定零空间（null space），然后通过线性组合该零空间内的向量来构建加性权重变换，从而在不影响任务损失的情况下抑制权重离群值。这种加性变换可以离线吸收进权重中，不需要层间变换，且不引入推理开销。此外，该构建过程通过闭式解（closed-form solution）高效实现，无需资源密集型的训练或迭代过程。大量实验表明，OSAQ 有效地抑制了离群值并提升了低比特量化性能。例如，在 2 比特量化中，OSAQ 与 GPTQ 结合使用时，其困惑度（perplexity）比原始 GPTQ 降低了 40% 以上。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable capabilities. However, their massive parameter scale leads to significant resource consumption and latency during inference. Post-training weight-only quantization offers a promising solution by reducing model size and accelerating token generation through alleviating the memory-bound issue. Nevertheless, the presence of inherent systematic outliers in weights continues to be a major obstacle. While existing methods, such as scaling and rotation, attempt to address this issue, the performance remains unsatisfactory. In this paper, we propose Outlier Self-Absorption Quantization (OSAQ), which performs additive weight suppression guided by the second-order low-rank property for low-bit weight-only quantization of LLMs. Specifically, we observe that the Hessian exhibits low-rank consistency across different inputs, with certain directions consistently showing vanishing curvature. Leveraging this property, we identify a stable null space of the Hessian and then construct an additive weight transformation by linearly combining the vectors within this null space, thereby suppressing weight outliers without affecting the task loss. This additive transformation can be absorbed into the weights offline, requiring no inter-layer transformations and introducing no inference overhead. Moreover, the construction is efficiently achieved by a closed-form solution, without resource-intensive training or iterative procedures. Extensive experiments demonstrate that OSAQ effectively suppresses outliers and enhances low-bit quantization performance. For instance, in 2-bit quantization, OSAQ, when integrated with GPTQ, achieves over 40% lower perplexity compared to vanilla GPTQ.

---

## 论文详细总结（自动生成）

### 论文总结：OSAQ - 用于精确低比特大语言模型量化的离群值自吸收技术

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）由于参数量巨大，在推理时面临严重的内存带宽瓶颈（Memory-bound）。**训练后量化（PTQ）**是减少内存占用、加速推理的主流方案。然而，LLM 权重中普遍存在**系统性离群值（Outliers）**，这些数值极大的异常点会撑大整体量化范围，导致量化分辨率下降，尤其在 2-bit 或 3-bit 等极低比特设置下，模型精度会发生剧烈崩塌。
现有的方法（如 AWQ 的缩放、QuIP 的旋转）主要基于层间的**乘性变换**，但仍难以彻底消除离群值的影响。本文的研究动机在于探索一种不依赖层间变换、且能有效抑制离群值的**加性变换**路径。

#### 2. 方法论：核心思想与关键技术
OSAQ 的核心思想是利用 **Hessian 矩阵的二阶低秩一致性**，在不改变模型任务损失的前提下，通过加性扰动来“吸收”离群值。

*   **核心观察**：研究者发现，虽然不同输入下的激活值分布差异很大，但任务损失对权重的二阶导数（Hessian 矩阵）表现出稳定的低秩特性。即在某些特定方向（零空间，Null Space）上，权重的微小改变对损失几乎没有影响（曲率为零）。
*   **关键技术细节**：
    1.  **零空间提取**：对 Hessian 矩阵进行特征值分解，根据“尾部能量策略”识别出曲率极小的特征向量，构建零空间矩阵 $N$。
    2.  **加性变换构建**：构造扰动项 $\Delta W = \beta N$，其中 $\beta$ 是待求解的系数矩阵。由于 $N$ 处于 Hessian 的零空间，根据泰勒展开，这种加性改变不会增加任务损失。
    3.  **Softmax-∞ 目标近似**：为了抑制离群值，目标是最小化变换后权重的 $L_\infty$ 范数。由于 $L_\infty$ 不可微，作者引入了 Softmax-∞ 近似，将其转化为可微的加权 $L_2$ 范数优化问题。
    4.  **闭式解求解**：利用一阶最优性条件，直接推导出系数矩阵 $\beta$ 的**闭式解**。这意味着无需进行耗时的反向传播训练或迭代优化，即可快速计算出最优的加性修正项。
*   **自吸收特性**：该变换在量化前离线完成，修正后的权重直接替换原权重，**不引入任何额外的推理开销或架构改动**。

#### 3. 实验设计
*   **数据集/场景**：
    *   **语言生成**：WikiText2, C4（评估困惑度 PPL）。
    *   **常识问答（Zero-shot QA）**：PIQA, ARC, WinoGrande。
    *   **综合基准**：MMLU（多任务理解）, MT-Bench（对话能力）。
*   **Benchmark 模型**：LLaMA2 (7B, 13B, 70B), LLaMA3 (8B, 70B), 以及超大规模的 Mistral-Large (123B) 和 Llama-3.1 (405B)。
*   **对比方法**：GPTQ, AWQ, QuIP, MagR, OmniQuant, WKVQuant 等主流 PTQ 算法。

#### 4. 资源与算力
*   **硬件**：实验主要在 **Nvidia A100 GPU** 上完成。
*   **耗时**：由于采用闭式解，计算效率极高。
    *   LLaMA2-7B：约 24 分钟。
    *   LLaMA2-13B：约 45 分钟。
    *   LLaMA2-70B：约 4.6 小时。
*   **对比**：相比原始 GPTQ，OSAQ 仅增加了极小的计算开销（约 10% 左右），远快于需要迭代优化的方法。

#### 5. 实验数量与充分性
论文进行了多维度的广泛实验：
*   **覆盖面**：涵盖了从 7B 到 405B 不同规模的模型，证明了算法的可扩展性。
*   **消融实验**：验证了零空间的稳定性、Softmax-∞ 近似的有效性、以及超参数（如能量阈值 $\gamma$、温度系数 $\tau$）的鲁棒性。
*   **兼容性测试**：证明了 OSAQ 可以作为“插件”与 GPTQ、AWQ、QuIP 等方法结合，并均能带来提升。
*   **客观性**：实验对比了多种 SOTA 方法，并在标准公开数据集上运行，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **精度大幅提升**：在 2-bit 量化下，OSAQ + GPTQ 的困惑度比原始 GPTQ 降低了 **40% 以上**。
*   **低比特突破**：在 3-bit 和 4-bit 下，OSAQ 能够使量化模型非常接近 FP16 原始模型的性能，甚至在 405B 模型上表现出极强的稳定性。
*   **零开销加速**：由于是离线加性变换，量化后的模型推理速度与标准量化模型一致（如 4-bit 下相比 FP16 有约 2 倍加速），且不增加显存负担。

#### 7. 优点
*   **理论优雅**：巧妙利用 Hessian 零空间实现“无损”权重调整，开辟了乘性变换之外的新路径。
*   **高效简洁**：闭式解避免了复杂的超参数调优和长时间训练，易于工程实现。
*   **高兼容性**：作为预处理步骤，能与几乎所有现有的权重/激活量化方法协同工作。
*   **效果显著**：尤其在极低比特（2-bit）下，其对离群值的抑制效果远超传统方法。

#### 8. 不足与局限
*   **依赖校准数据**：虽然只需要少量样本（如 128 条），但 Hessian 的估计仍受校准集分布的影响。
*   **FP 模型微损**：虽然理论上零空间变换不改变 Loss，但由于 Hessian 是近似估计且存在数值误差，变换后的 FP16 模型精度会有极微小的下降（通常可忽略不计）。
*   **应用限制**：主要针对权重离群值，虽然实验展示了对激活量化的辅助作用，但其核心设计并非直接解决激活离群值问题。

（完）
