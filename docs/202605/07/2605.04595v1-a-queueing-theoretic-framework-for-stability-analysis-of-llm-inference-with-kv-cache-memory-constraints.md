---
title: A Queueing-Theoretic Framework for Stability Analysis of LLM Inference with KV Cache Memory Constraints
title_zh: 针对具有 KV 缓存内存限制的大语言模型推理稳定性分析的排队论框架
authors: "Chengyi Nie, Nian Si, Zijie Zhou"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04595v1"
tags: ["query:llm"]
score: 9.0
evidence: KV缓存约束下的大模型推理稳定性分析
tldr: "本研究针对大语言模型（LLM）推理中计算资源与KV缓存内存的双重约束，提出了首个结合两者的排队论分析框架。通过推导严谨的系统稳定性与不稳定性条件，该框架能够预测推理服务在特定负载下是否会发生队列无限增长。这为GPU资源配置提供了理论指导，帮助运营商根据请求到达率计算所需的集群规模。实验证明，该理论预测在真实生产环境中的准确率极高，偏差通常在10%以内。"
selection_source: fresh_fetch
motivation: 现有的LLM推理分析往往忽略了KV缓存对GPU内存的巨大消耗，导致难以准确评估系统在资源受限下的服务能力。
method: 建立了一个将计算能力和内存容量同时纳入考量的排队论模型，并推导出维持推理服务稳定运行的数学条件。
result: "实验结果显示，该框架能准确预测系统稳定性，理论值与实际生产环境的偏差控制在10%以内。"
conclusion: 该研究为LLM推理服务的GPU资源规划提供了科学依据，有效解决了过度采购或配置不足的问题。
---

## 摘要
大语言模型（LLM）的快速普及为大规模高效推理带来了重大挑战。与传统工作负载不同，LLM 推理同时受限于计算能力和键值（KV）缓存的内存开销，KV 缓存虽然加速了解码过程，但会迅速耗尽 GPU 内存。在本文中，我们提出了首个将计算和 GPU 内存限制明确纳入 LLM 推理分析的排队论框架。基于该框架，我们推导出了严格的稳定性和不稳定性条件，用于确定 LLM 推理服务是否能在队列不发生无限增长的情况下满足持续涌入的需求。这一结果为系统部署提供了强大的工具，有望解决 GPU 资源配置的核心挑战。通过将估计的请求到达率与我们推导出的稳定服务率相结合，运营商可以计算出所需的集群规模，从而避免昂贵的过度采购以及违反性能要求的配置不足。我们通过在真实 GPU 生产环境中的广泛实验，进一步验证了我们的理论预测。结果表明，预测的稳定性条件高度准确，偏差通常在 10% 以内。

## Abstract
The rapid adoption of large language models (LLMs) has created significant challenges for efficient inference at scale. Unlike traditional workloads, LLM inference is constrained by both computation and the memory overhead of key-value (KV) caching, which accelerates decoding but quickly exhausts GPU memory. In this paper, we introduce the first queueing-theoretic framework that explicitly incorporates both computation and GPU memory constraints into the analysis of LLM inference. Based on this framework, we derive rigorous stability and instability conditions that determine whether an LLM inference service can sustain incoming demand without unbounded queue growth. This result offers a powerful tool for system deployment, potentially addressing the core challenge of GPU provisioning. By combining an estimated request arrival rate with our derived stable service rate, operators can calculate the necessary cluster size to avoid both costly over-purchasing and performance-violating under-provisioning. We further validate our theoretical predictions through extensive experiments in real GPU production environments. Our results show that the predicted stability conditions are highly accurate, with deviations typically within 10%.

---

## 论文详细总结（自动生成）

这篇论文为大语言模型（LLM）推理系统的稳定性分析提供了一个创新的排队论框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何准确预测 LLM 推理服务在特定负载下的稳定性，即在考虑 GPU 计算能力和 **KV 缓存（Key-Value Cache）内存约束**的双重限制下，系统是否会出现请求队列无限增长的情况。
*   **研究动机**：传统的排队论模型多关注计算资源，而 LLM 推理的瓶颈往往在于内存（KV 缓存随生成长度线性增长）。目前缺乏一个能同时建模计算与动态内存消耗的理论框架，导致在实际部署中难以科学地进行 GPU 资源规划（容易导致过度采购或配置不足）。

### 2. 论文提出的方法论
*   **核心思想**：将 LLM 推理建模为一个受内存约束的随机装箱（Stochastic Bin Packing）排队系统，并引入“生命周期累积内存占用”概念。
*   **关键技术细节**：
    *   **两阶段建模**：将推理分为 **Prompt（预填充）阶段**（采用分块处理，内存阶梯式增长）和 **Decode（解码）阶段**（逐 token 生成，内存线性增长）。
    *   **生命周期函数 $g(s, o)$**：定义了一个函数来计算一个具有 $s$ 个输入 token 和 $o$ 个输出 token 的请求在整个生命周期内消耗的总内存“面积”。
    *   **稳定性公式**：推导出临界服务率 $\mu = \frac{M}{\bar{b} E[g(s, o)]}$。其中 $M$ 是 GPU 有效内存容量，$\bar{b}$ 是平均批处理时间。
    *   **稳定性条件**：证明了当请求到达率 $\lambda < \mu(1-\delta)$ 时（$\delta$ 为极小余量），系统是稳定的（正复现）；反之则是不稳定的。
*   **算法流程**：通过 Lyapunov 函数证明系统状态（队列长度）在临界值以下具有负漂移，从而保证系统不会崩溃。

### 3. 实验设计
*   **实验场景**：
    *   **单 GPU 场景**：验证基础理论的准确性。
    *   **多 GPU 场景（8 张 A100）**：验证理论在分布式集群（数据并行）中的可扩展性。
*   **数据集/负载**：
    *   **合成负载**：设定不同的预填充/解码比例（PD Ratio），如 1:1, 2:1, 1:2, 8:1。
    *   **真实数据集**：使用 **LongBench v2**，其具有高度可变的输入输出长度分布，模拟真实复杂任务。
*   **Benchmark/对比**：对比了理论预测的服务率（$\mu_{theory}$）与真实 GPU 环境中测得的饱和吞吐量（$\mu_{gpu}$）。

### 4. 资源与算力
*   **硬件型号**：使用了 **8 张 NVIDIA A100 GPU**。
*   **模型配置**：使用 **Meta-Llama-3-8B** 模型。
*   **推理引擎**：基于 **vLLM v1**，启用了分块预填充（Chunked Prefill）功能。
*   **算力细节**：实验主要关注推理阶段的稳定性，未涉及模型训练，因此未提及训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种合成 PD 比例、1 种混合负载、1 个大型真实数据集（LongBench v2）以及单机/多机配置。
*   **充分性**：实验设计较为充分。通过改变到达率 $\lambda$ 观察队列增长曲线（线性增长 vs 稳定波动），直观验证了临界点的存在。
*   **客观性**：实验在真实生产级推理引擎（vLLM）上运行，而非纯模拟器，结果具有较高的实践参考价值。

### 6. 论文的主要结论与发现
*   **高精度预测**：理论预测的稳定性临界值与实际测量值的误差（GAP）通常在 **10% 以内**（最低仅 2.25%）。
*   **内存是关键**：验证了在长文本场景下，KV 缓存内存约束对系统稳定性的影响远超纯计算约束。
*   **资源规划指导**：通过公式 $\lceil \lambda / \mu \rceil$，运营商可以精确计算出维持服务稳定所需的最小 GPU 数量。

### 7. 优点（亮点）
*   **首创性**：首次将 KV 缓存的动态内存演进纳入排队论分析。
*   **理论与实践结合**：不仅有严谨的数学证明（Lyapunov 稳定性），还在真实 GPU 环境中进行了高精度的验证。
*   **实用性强**：为云服务商提供了一个简单易用的公式来优化资源配置，降低成本。

### 8. 不足与局限
*   **架构覆盖有限**：目前主要针对数据并行（Data Parallel），未深入探讨张量并行（TP）和流水线并行（PP）中复杂的通信开销对稳定性的非线性影响。
*   **静态假设**：假设平均批处理时间 $\bar{b}$ 为常数，但在极端长文本或复杂调度下，$\bar{b}$ 可能会有较大波动（文中通过 Trimmed Mean 进行了部分修正，但仍有简化）。
*   **解耦架构未涉及**：未讨论预填充和解码分离（Disaggregated Prefill-Decode）这种新兴架构下的多队列稳定性。

（完）
