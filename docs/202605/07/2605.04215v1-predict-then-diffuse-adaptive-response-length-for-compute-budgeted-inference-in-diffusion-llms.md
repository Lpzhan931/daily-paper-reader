---
title: "Predict-then-Diffuse: Adaptive Response Length for Compute-Budgeted Inference in Diffusion LLMs"
title_zh: Predict-then-Diffuse：扩散大语言模型中计算预算受限推理的自适应响应长度
authors: "Michael Rottoli, Subhankar Roy, Stefano Paraboschi"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.04215v1"
tags: ["query:llm"]
score: 8.0
evidence: 优化扩散大模型推理的吞吐量与延迟
tldr: 扩散语言模型（D-LLMs）虽具备并行生成优势，但受限于需预设固定响应长度，导致计算浪费或输出截断。本文提出 Predict-then-Diffuse 框架，通过自适应响应长度预测器（AdaRLP）预估最优长度，并结合数据驱动的安全机制减少重算开销。该方法在保证输出质量的同时显著降低了计算成本，为受限计算预算下的推理提供了高效方案。
selection_source: fresh_fetch
motivation: 扩散语言模型在推理前需固定响应长度，导致在处理变长文本时存在严重的计算浪费或因截断导致的重算延迟。
method: 提出一种模型无关的框架，利用 AdaRLP 预测器预估响应长度，并引入安全机制以平衡填充开销与重算风险。
result: 实验证明该框架在多个数据集上显著降低了计算量（FLOPs），且对偏态数据分布具有良好的鲁棒性。
conclusion: Predict-then-Diffuse 有效解决了扩散模型推理中的长度适配问题，实现了计算效率与生成质量的平衡。
---

## 摘要
基于扩散的大语言模型 (D-LLMs) 代表了生成式人工智能的一个充满前景的前沿领域，它提供了全并行 Token 生成，相比传统的自回归范式，能够带来显著的吞吐量优势和卓越的 GPU 利用率。然而，这种并行性受到生成前需预设固定响应长度的限制。这种架构限制导致了严重的权衡：响应长度过长会导致在语义上无意义的填充 Token 上产生计算浪费，而响应长度过短则会导致输出截断，进而需要昂贵的重新计算并引入不可预测的延迟峰值。为了解决这一问题，我们提出了 Predict-then-Diffuse，这是一个简单且与模型无关的框架，通过首先估计响应长度并将其用于 D-LLM 推理，实现了针对每个输入查询的计算预算受限推理。其核心是一个自适应响应长度预测器 (AdaRLP) 辅助预测器，用于根据输入查询预测最佳响应长度。为了防止低估响应长度而导致必须以更长的响应长度重新运行推理，我们引入了一种数据驱动的安全机制，该机制以微不足道的填充开销为代价进行权衡。总体而言，我们的框架限制了在填充 Token 上的显著计算浪费，并保证了输出质量。在多个数据集上的实验验证表明，与默认的 D-LLM 推理机制和基于启发式方法的基准相比，Predict-then-Diffuse 显著降低了计算成本 (FLOP)，同时对偏斜的数据分布具有鲁棒性。

## Abstract
Diffusion-based Large Language Models (D-LLMs) represent a promising frontier in generative AI, offering fully parallel token generation that can lead to significant throughput advantages and superior GPU utilization over traditional autoregressive paradigm. However, this parallelism is constrained by the requirement of a fixed-size response length prior to generation. This architectural limitation imposes a severe trade-off: oversized response length results in computational waste on semantically meaningless padding tokens, while undersized response length cause output truncation requiring costly re-computations that introduce unpredictable latency spikes. To tackle this issue, we propose Predict-then-Diffuse, a simple and model-agnostic framework, that enables compute-budgeted inference per input query by first estimating the response length and then using it to run inference with D-LLM. At its core lies a Adaptive Response Length Predictor (AdaRLP) auxiliary predictor that predicts the optimal response length given an input query. As a measure against under-predicting the response length and re-running inference with a higher response length, we introduce a data-driven safety mechanism, which trades a negligible padding overhead. As a whole, our framework limits the significant waste of computation on padding tokens and preserves output quality. Experimental validation on multiple datasets demonstrate that Predict-then-Diffuse significantly reduces computational costs (FLOP) compared to the default D-LLM inference mechanism and baselines based on heuristics, while being robust to skewed data distributions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Predict-then-Diffuse: Adaptive Response Length for Compute-Budgeted Inference in Diffusion LLMs》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：扩散大语言模型（D-LLMs，如 LLaDA）通过并行生成所有 Token，克服了自回归模型（AR-LLMs）逐个生成导致的内存带宽瓶颈。然而，D-LLM 在推理前必须预设固定的响应长度 $L$。
*   **核心痛点**：
    *   **计算浪费**：若 $L$ 设置过大，Transformer 的平方级复杂度会导致在无意义的填充符（`<PAD>`）上浪费大量算力。
    *   **输出截断**：若 $L$ 设置过小，输出会被截断，导致质量下降或需要昂贵的重新计算。
*   **目标**：在保证输出质量的前提下，通过动态预测最优响应长度来降低推理成本。

### 2. 方法论
该论文提出了 **Predict-then-Diffuse** 框架，包含以下关键技术：
*   **自适应响应长度预测器 (AdaRLP)**：
    *   在推理前，使用一个轻量级的回归模型（基于 CatBoost 梯度提升树）根据输入 Prompt 预测预期的响应长度 $\hat{L}$。
    *   实验发现，直接处理原始文本特征的 `AdaRLP text-only` 效果优于复杂的人工特征工程。
*   **数据驱动的安全边际 (Safety Margin)**：
    *   为了防止预测过短导致截断，引入缓冲值 $\delta$。$\delta$ 取自训练集预测误差的高分位数（如 95% 分位数）。
    *   最终推理长度 $L^* = \min(\hat{L} + \delta, L_{max})$。
*   **回退机制 (Fallback Strategy)**：
    *   如果生成的序列在 $L^*$ 长度内未出现 `<EOS>` 终止符，则以最大长度 $L_{max}$ 重新运行推理。

### 3. 实验设计
*   **数据集**：整合了 ShareGPT、Alpaca、Dolly-15k、OpenOrca 和 ELI5 五个开源数据集，共 39,994 条 Prompt-Response 对，具有高方差的长度分布。
*   **基准模型**：使用 LLaDA-8B 作为核心扩散模型。
*   **对比方法 (Baselines)**：
    1.  **Max Response Length**：固定使用最大长度（4096）。
    2.  **Static Response Length**：固定短长度（200），失败则翻倍。
    3.  **Mean Doubling Heuristic**：从均值长度开始，失败则翻倍。
    4.  **Oracle**：理想状态，已知真实长度。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA H100 (80GB VRAM)** GPU 进行实验。
*   **算力统计**：利用 DeepSpeed 框架精确测量总 FLOPs（浮点运算数）。
*   **预测开销**：AdaRLP 的推理延迟极低（**< 0.04ms**），相对于 D-LLM 的生成时间几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：在约 8,000 条测试数据上进行了完整评估。
*   **充分性验证**：
    *   **成本模型验证**：通过实验证实了 FLOPs 随序列长度呈二次方增长的理论推导。
    *   **鲁棒性测试**：专门模拟了“双峰分布”数据集，证明该方法在长短文本混合场景下优于简单的启发式方法。
    *   **质量评估**：引用了 LLaDA 原论文数据，证明改变画布大小（Canvas Size）在足够容纳答案的情况下不会降低生成质量。
*   **评价**：实验设计较为全面，涵盖了计算量、延迟确定性和生成质量等多个维度。

### 6. 主要结论与发现
*   **大幅减排**：相比默认的固定最大长度推理，该框架减少了 **99.34%** 的 FLOPs 消耗。
*   **延迟稳定**：通过安全边际机制，将回退重算的概率从 1.43% 降低至 **0.1%**，确保了推理延迟的确定性。
*   **自适应性**：模型能根据预设长度自动调整表达的简洁程度，在短长度限制下仍能保持答案正确。

### 7. 优点
*   **模型无关性**：作为预处理模块，可无缝集成到任何 D-LLM 推理管线中，无需修改原模型架构或重新训练。
*   **工程实用性**：解决了扩散模型落地中“固定长度”这一核心工程障碍。
*   **高效预测**：选用 CatBoost 而非小语言模型作为预测器，极大地降低了辅助开销。

### 8. 不足与局限
*   **批处理挑战**：由于每个 Query 的预测长度不同，在进行 Batch 推理时会面临对齐问题，可能需要复杂的分组（Grouping）策略来维持吞吐量。
*   **分布依赖性**：AdaRLP 的准确性依赖于训练数据的分布，如果推理时的 Prompt 风格与训练集差异巨大，预测效果可能下降。
*   **应用范围**：目前仅在离散扩散模型（LLaDA）上验证，尚未在连续空间扩散模型上进行大规模测试。

（完）
