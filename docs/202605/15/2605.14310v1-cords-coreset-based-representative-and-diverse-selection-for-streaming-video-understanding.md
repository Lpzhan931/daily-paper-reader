---
title: "CoRDS: Coreset-based Representative and Diverse Selection for Streaming Video Understanding"
title_zh: CoRDS：用于流式视频理解的基于核心集的代表性与多样性选择
authors: "Ailar Mahdizadeh, Puria Azadi, Muchen Li, Xiangteng He, Leonid Sigal"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14310v1"
tags: ["query:kvcache-survey"]
score: 8.0
evidence: 通过核心集选择进行KV缓存压缩
tldr: 针对流式视频理解中大模型KV缓存随时间增长的问题，本文提出CoRDS方法。不同于传统的局部启发式剪枝，CoRDS将缓存压缩视为核心集（Coreset）选择问题，通过平衡键值空间的覆盖范围并引入正交性多样性准则，筛选出能代表累积历史的紧凑子集。在多个模型和基准测试中，该方法显著提升了内存受限下的视频推理性能。
selection_source: fresh_fetch
motivation: 现有的流式视频KV缓存压缩方法多依赖局部启发式规则，缺乏对累积历史全局代表性的显式优化。
method: 提出一种基于核心集选择的方法，通过双准则目标平衡键值空间的覆盖率，并结合正交性驱动的多样性准则来保留最具代表性的缓存。
result: 在四种开源视觉语言模型和五个长视频/流式视频基准测试中，CoRDS在固定缓存预算下均优于现有的启发式压缩基准。
conclusion: 核心集选择比传统的逐标记剪枝更适合内存受限的流式视频理解，能更有效地保留关键历史信息。
---

## 摘要
使用大型视觉语言模型（VLM）进行流式视频理解需要紧凑的内存，以支持对不断增长的视觉历史进行未来推理。常见的解决方案是压缩键值（KV）缓存，但现有的流式方法通常依赖于局部的逐标记（token-wise）启发式方法，如近期性、时间冗余或显著性，这些方法并未显式优化保留的缓存是否具有累积历史的代表性。我们提出将 KV 缓存压缩视为一个核心集（coreset）选择问题：与其独立地对标记进行保留评分，我们选择一个能够覆盖累积视觉缓存几何结构的小子集。我们的方法在联合 KV 表示空间中运行，并引入了一个双标准目标，平衡键空间和值空间的覆盖范围，从而同时保留检索结构和输出相关信息。为了鼓励保留更具多样性的子集，我们进一步引入了一种正交驱动的多样性准则，该准则倾向于选择能为当前集合贡献新方向的候选者，并将此准则与对数行列式（log-determinant）子集选择联系起来。在四个开源 VLM 和五个长视频及流式视频基准测试中，我们的方法在固定缓存预算下优于启发式流式压缩基准。这些结果表明，对于内存受限的流式视频理解，代表性核心集选择比逐标记剪枝提供了一种更有效的原则。

## Abstract
Streaming video understanding with large vision-language models (VLMs) requires a compact memory that can support future reasoning over an ever-growing visual history. A common solution is to compress the key-value (KV) cache, but existing streaming methods typically rely on local token-wise heuristics, such as recency, temporal redundancy, or saliency, which do not explicitly optimize whether the retained cache is representative of the accumulated history. We propose to view KV-cache compression as a coreset selection problem: rather than scoring tokens independently for retention, we select a small subset that covers the geometry of the accumulated visual cache. Our method operates in a joint KV representation and introduces a bicriteria objective that balances coverage in key and value spaces, preserving both retrieval structure and output-relevant information. To encourage a more diverse retained subset, we further introduce an orthogonality-driven diversity criterion that favors candidates contributing new directions beyond the current selection, and connect this criterion to log-determinant subset selection. Across four open-source VLMs and five long-video and streaming-video benchmarks, our method improves over heuristic streaming compression baselines under a fixed cache budget. These results highlight that representative coreset selection offers a more effective principle, than token-wise pruning, for memory-constrained streaming video understanding.

---

## 论文详细总结（自动生成）

以下是对论文《CoRDS: Coreset-based Representative and Diverse Selection for Streaming Video Understanding》的结构化总结：

### 1. 论文的核心问题与整体含义
在流式视频理解（Streaming Video Understanding, SVU）中，视觉语言模型（VLM）面临着无限增长的视觉历史与有限内存（KV Cache）之间的矛盾。现有的压缩方法（如 InfiniPot-V、StreamMem）多依赖于局部启发式规则（如近期性、时间冗余或显著性评分），这些方法往往独立评估每个标记（Token），忽略了保留下来的缓存是否能从全局几何结构上代表整个历史。
**核心问题：** 如何在极低内存预算下，选择一个既具有代表性（Representative）又具有多样性（Diverse）的 KV 缓存子集，以支持对长视频历史的准确推理。

### 2. 论文提出的方法论：CoRDS
CoRDS 将 KV 缓存压缩建模为一个**核心集（Coreset）选择问题**，其核心思想是寻找一个能覆盖原始数据几何分布的小子集。

*   **联合 KV 表示空间：** 不同于仅在 Key 空间（影响检索）或 Value 空间（影响输出内容）进行选择，CoRDS 在联合空间 $\phi_i = [k_i; v_i]$ 中操作，确保保留的信息既好找又好用。
*   **双准则覆盖目标（Bicriteria Objective）：** 定义了一个平衡键值空间的距离度量 $d_\alpha(i, S)$，通过超参数 $\alpha$ 调节 Key 和 Value 的重要性。
*   **贪婪 $D^2$ 式选择：** 采用确定性的 $D^2$ 最远点采样算法（Farthest-first traversal），逐步选择与已选集合距离最远的候选标记，以最小化量化误差。
*   **正交抗冗余（Orthogonal Anti-redundancy）：** 为了避免选择方向相近的冗余标记，引入了正交性准则。该准则倾向于选择在特征空间中贡献新方向（残差大）的标记，这在数学上与对数行列式（Log-determinant）子集选择相关联。
*   **层选择性压缩与级联复用：** 仅在底层（Bottom 25%）执行完整的核心集选择，高层则复用底层的选择索引，从而在保持性能的同时大幅降低计算开销。

### 3. 实验设计
*   **模型：** 评估了四种开源 VLM：Qwen2-VL-7B、Qwen2.5-VL-3B/7B、LLaVA-NeXT-Video-7B。
*   **数据集/基准：**
    *   **长视频理解（离线）：** EgoSchema、MLVU、VideoMME。
    *   **流式视频理解（在线）：** OVO-Bench、StreamingBench。
*   **对比方法：** 包括全量缓存（Full KV）、均匀采样（Uniform）、SnapKV、InfiniPot-V、StreamMem、STC、TTC 等。
*   **预算设置：** 主要在 3K 和 6K 视觉标记（Token）的极低预算下进行测试。

### 4. 资源与算力
*   **硬件：** 实验在单张 **NVIDIA L40S (48GB)** GPU 上运行。
*   **训练时长：** 该方法是 **免训练（Training-free）** 的，直接在推理阶段应用。
*   **效率指标：** 论文报告了峰值显存（VRAM）、预填充 FPS 和解码吞吐量。通过级联复用策略，CoRDS 的推理速度和显存占用与现有的高效启发式方法（如 InfiniPot-V）持平甚至更优。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个模型系列和 5 个主流基准上进行了广泛测试，涵盖了从短视频到长达 2 小时的视频。
*   **消融实验：** 极其充分。作者对选择器类型（K-means vs $D^2$）、特征空间（K vs V vs KV）、压缩层范围、以及四个核心超参数（$\alpha, \eta, \lambda, \epsilon_0$）均进行了详尽的消融分析。
*   **客观性：** 引入了覆盖率累积分布函数（Coverage CDF）作为几何诊断工具，证明了性能提升确实源于更好的几何代表性，而非仅仅是下游任务的偶然拟合。

### 6. 论文的主要结论与发现
*   **代表性胜过数量：** 在极低预算（如 3K tokens）下，CoRDS 显著优于所有启发式基准，甚至在某些任务上超过了拥有全量缓存的模型（解决了“大海捞针”中的干扰问题）。
*   **KV 联合空间的重要性：** 实验证明同时考虑 Key 和 Value 的覆盖比单一空间选择更有效。
*   **底层选择的有效性：** 发现底层网络保留了更多的视觉结构，在此进行核心集选择并向上传递索引是性能与效率的最佳平衡点。

### 7. 优点
*   **理论支撑：** 将缓存压缩与核心集理论、对数行列式最大化联系起来，比纯启发式方法更具科学性。
*   **通用性强：** 作为一种推理时插件，无需重新训练即可适配任何基于 Transformer 的 VLM。
*   **性能卓越：** 在内存受限的流式场景下刷新了 SOTA，尤其是在需要长程记忆的推理任务中表现突出。

### 8. 不足与局限
*   **超参数依赖：** 虽然消融实验显示了一定的鲁棒性，但 $\alpha$（KV 权重）等参数在不同模型或任务上可能仍需微调以达最优。
*   **计算复杂度：** 尽管有级联复用，但 $D^2$ 选择本身的复杂度仍随缓存大小线性增长，在处理极长视频（如数小时）时，选择过程的延迟可能成为瓶颈。
*   **模态局限：** 目前仅针对视觉标记，尚未探讨在多模态（如音频+视觉）或纯文本长上下文中的表现。

（完）
