---
title: "KVServe: Service-Aware KV Cache Compression for Communication-Efficient Disaggregated LLM Serving"
title_zh: KVServe：面向通信高效的解耦式大语言模型服务的服务感知 KV 缓存压缩
authors: "Zedong Liu, Xinyang Ma, Dejun Luo, Hairui Zhao, Bing Lu, Wenjing Huang, Yida Gu, Xingchen Liu, Zheng Wei, Jinyang Liu, Dingwen Tao, Guangming Tan"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13734v1"
tags: ["query:kvcache-survey"]
score: 8.0
evidence: 服务感知且自适应的KV通信压缩框架
tldr: 针对大模型解耦推理中 KV Cache 传输导致的通信瓶颈，本文提出 KVServe 框架。它通过构建模块化压缩策略空间，利用贝叶斯性能分析引擎高效搜索 Pareto 候选集，并结合在线控制器根据实时负载和网络状况动态调整压缩方案。该框架有效解决了静态压缩配置无法适应动态环境的问题，显著提升了分布式推理系统的效率。
selection_source: fresh_fetch
motivation: 解耦式 LLM 推理架构中 KV 状态传输成为主要瓶颈，而现有静态压缩方法难以适应动态变化的负载、带宽和 SLO 要求。
method: 构建了统一的模块化压缩策略空间，并引入贝叶斯分析引擎进行高效离线搜索，配合在线控制器实现服务感知的动态策略选择。
result: 实验显示 KVServe 在 PD 分离场景下实现高达 9.13 倍的 JCT 加速，在 KV 解耦场景下将 TTFT 降低了 32.8 倍。
conclusion: KVServe 通过自适应的 KV 压缩机制，成功消除了解耦推理中的通信瓶颈，为高效、可扩展的 LLM 服务提供了有力支撑。
---

## 摘要
大语言模型（LLMs）在生产环境中被广泛采用，将推理系统推向了极限。解耦式 LLM 服务（例如 PD 分离和 KV 状态解耦）提高了可扩展性和成本效率，但也使 KV 缓存成为跨越网络和存储边界的显式负载，使其成为主要的端到端瓶颈。现有的 KV 压缩通常采用静态运行时配置，尽管生产服务环境在工作负载组合、带宽以及 SLO/质量预算方面随时间而变化。因此，固定的选择可能是次优的，甚至会增加延迟。我们提出了 KVServe，这是首个面向解耦式 LLM 服务的服务感知且自适应的 KV 通信压缩框架：KVServe (1) 将 KV 压缩统一到一个包含新组件和跨方法重组的模块化策略空间中；(2) 引入了贝叶斯分析引擎，能够高效搜索该空间并提取 3D Pareto 候选集，将离线搜索开销降低了 50 倍；(3) 部署了一个服务感知在线控制器，该控制器结合了分析延迟模型和轻量级 Bandit 算法，在约束条件下选择配置并修正离线与在线之间的不匹配。通过集成到 vLLM 并在多种数据集、模型、GPU 和网络环境下进行评估，KVServe 在 PD 分离服务中实现了高达 9.13 倍的作业完成时间（JCT）加速，在 KV 解耦服务中实现了高达 32.8 倍的首字延迟（TTFT）降低。

## Abstract
LLMs are widely adopted in production, pushing inference systems to their limits. Disaggregated LLM serving (e.g., PD separation and KV state disaggregation) improves scalability and cost efficiency, but it also turns KV into an explicit payload crossing network and storage boundaries, making KV a dominant end-to-end bottleneck. Existing KV compression are typically static runtime configurations, despite production service context varies over time in workload mix, bandwidth, and SLO/quality budgets. As a result, a fixed choice can be suboptimal or even increase latency. We present \emph{KVServe}, the first service-aware and adaptive KV communication compression framework for disaggregated LLM serving: KVServe (1) unifies KV compression into a modular strategy space with new components and cross-method recomposition; (2) introduces Bayesian Profiling Engine that efficiently searches this space and distills a 3D Pareto candidate set, reducing $50\times$ offline search overhead; and (3) deploys a Service-Aware Online Controller that combines an analytical latency model with a lightweight bandit to select profiles under constraints and correct offline-to-online mismatch. Integrated into vLLM and evaluated across datasets, models, GPUs and networks, KVServe achieves up to $9.13\times$ JCT speedup in PD-separated serving and up to $32.8\times$ TTFT reduction in KV-disaggregated serving.

---

## 论文详细总结（自动生成）

### 论文总结：KVServe - 面向通信高效的解耦式大语言模型服务的服务感知 KV 缓存压缩

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）推理架构向**解耦式服务**（如 Prefill/Decode 分离、KV 状态解耦）演进，KV Cache 不再仅是 GPU 内部状态，而是变成了需要在网络和存储间传输的显式负载。在长文本和高吞吐场景下，KV 传输带宽成为了端到端的性能瓶颈（占比可达 60% 以上）。
**核心矛盾**在于：现有的 KV 压缩方法（如 CacheGen, KIVI）多为静态配置，无法适应动态变化的生产环境（如工作负载类型、实时带宽波动、SLO 预算）。静态配置在某些情况下甚至会因为（解）压缩开销超过通信节省而导致性能下降。

#### 2. 方法论：核心思想与关键技术
KVServe 提出了首个**服务感知且自适应**的 KV 压缩框架，包含三个核心组件：
*   **模块化策略池（Modular Strategy Pool）：** 将 KV 压缩抽象为“转换器（Transformer）- 量化器（Quantizer）- 编解码器（Codec）”的流水线。引入了 **MixHQ（混合精度头量化）**，根据检索头和流式头的重要性分配不同位宽，平衡压缩率与精度。
*   **贝叶斯分析引擎（Bayesian Profiling Engine）：** 针对指数级增长的策略空间，利用**带高斯过程的贝叶斯优化（BO-GP）**进行高效搜索。通过双向剪枝和早停机制，在极少次数的评估下提取出“精度-压缩率-延迟”的 **3D Pareto 前沿候选集**。
*   **服务感知在线控制器（Service-Aware Online Controller）：** 
    *   **分析模型：** 基于推导的带宽阈值理论，快速过滤非受益策略。
    *   **残差修正 Bandit 算法：** 在线感知实时带宽和系统扰动，通过轻量级的学习机制修正离线预测与在线执行之间的偏差，确保在满足 SLO 和精度约束的前提下选择最优策略。

#### 3. 实验设计
*   **模型：** Qwen2.5 (7B, 32B), Llama-3.1 (8B)。
*   **数据集：** 
    *   离线分析：GSM8K (数学), HumanEval (代码), Multi-News (摘要), Qasper (问答)。
    *   在线验证（未见任务）：2WikiMQA, HotpotQA。
*   **场景：** PD 分离（Prefill-Decode Separation）和前缀缓存（Prefix Caching）。
*   **对比方法（Baselines）：** 默认 BF16、CacheGen、KIVI、DuoAttention。
*   **硬件环境：** 涵盖 H100 (数据中心级)、RTX Pro 6000 (工作站级)、RTX 5090/4090 (消费级)，模拟不同带宽环境（5Gbps - 100Gbps）。

#### 4. 资源与算力
*   **离线分析算力：** 使用 **4 张 NVIDIA A100 (40GB) GPU** 进行策略搜索。
*   **搜索效率：** 贝叶斯引擎将离线搜索开销从传统穷举的 1000 小时量级降低到了 **20 小时**左右（约 50 倍加速）。
*   **在线推理算力：** 解码端使用 H100，预填充端使用上述多种型号 GPU。在线控制器的决策开销极低（< 1ms）。

#### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **端到端性能：** 跨 4 种硬件、6 种数据集、多种带宽设置。
*   **精度验证：** 严格测试了压缩对模型任务准确率的影响。
*   **消融实验：** 分别验证了贝叶斯引擎中的编码、剪枝、早停模块，以及在线控制器中的 Bandit 机制的有效性。
*   **客观性：** 实验不仅展示了加速效果，还客观记录了静态方法在某些带宽下出现“负优化”的现象，并证明了 KVServe 如何通过自适应规避该问题。

#### 6. 主要结论与发现
*   **显著加速：** 在 PD 分离场景下，JCT（作业完成时间）最高提升 **9.13 倍**；在 KV 解耦场景下，TTFT（首字延迟）最高降低 **32.8 倍**。
*   **消除瓶颈：** 成功将通信受限（Network-bound）的系统转变为计算受限（Compute-bound），通信耗时占比从 80-90% 降至 10% 以下。
*   **自适应优势：** KVServe 能够根据任务类型自动选择最适合的压缩流水线，在保持 97% 以上相对精度的同时，实现比 SOTA 方法更高的压缩率。

#### 7. 优点
*   **系统性创新：** 首次将 KV 压缩从“算法选择”提升为“受限控制问题”，实现了服务感知。
*   **搜索高效：** 贝叶斯优化结合领域知识（剪枝策略）极大地降低了冷启动成本。
*   **鲁棒性强：** 在线 Bandit 机制能够处理生产环境中的性能抖动和离线模型不准的问题。
*   **兼容性好：** 模块化设计可以轻松集成未来出现的各种新压缩算子。

#### 8. 不足与局限
*   **依赖前置路由：** 框架假设上层系统能提供准确的工作负载标签（如任务类型），若路由分类错误，可能导致选错压缩策略影响精度。
*   **离线开销仍存：** 虽然降低了 50 倍，但每个新模型仍需 20 小时的 Profiling，对于模型迭代极快的场景仍有优化空间。
*   **硬件特定性：** 延迟模型和 Pareto 前沿与具体硬件绑定，更换 GPU 型号后可能需要重新进行部分性能分析。

（完）
