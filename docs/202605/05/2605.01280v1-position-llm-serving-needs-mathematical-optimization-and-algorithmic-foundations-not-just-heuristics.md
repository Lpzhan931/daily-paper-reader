---
title: "Position: LLM Serving Needs Mathematical Optimization and Algorithmic Foundations, Not Just Heuristics"
title_zh: 观点：大语言模型推理服务需要数学优化与算法基础，而非仅仅依赖启发式方法
authors: Zijie Zhou
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01280v1"
tags: ["query:llm"]
score: 9.0
evidence: LLM服务优化的算法基础
tldr: 本文指出当前的LLM推理服务系统过度依赖传统的启发式算法（如FIFO、LRU），忽略了LLM推理中KV缓存动态增长、预填充与解码阶段不对称等独特结构。作者呼吁建立数学优化模型和算法基础，通过运筹学与机器学习系统的交叉研究，开发具有理论性能保证的调度和路由算法，以应对复杂多变的工作负载，推动LLM服务从经验主义转向科学化设计。
selection_source: fresh_fetch
motivation: 现有的LLM服务系统主要依赖通用的启发式策略，无法有效处理LLM推理特有的内存动态增长和阶段不对称性。
method: 提倡建立能够捕捉LLM推理特征的数学模型，并利用运筹学方法设计具有可证明性能保证的算法。
result: 初步研究表明，基于原则设计的算法在性能上可以达到或超过启发式方法，同时提供更强的理论保障。
conclusion: LLM推理服务应从简单的启发式转向数学优化和算法基础研究，将其视为一个新的研究前沿。
---

## 摘要
本观点论文认为，大语言模型（LLM）推理服务已经超越了通用启发式方法的范畴，现在需要数学优化和算法基础。尽管 vLLM 和 SGLang 等服务系统取得了飞速进展，但其算法核心在很大程度上仍沿用经典的分布式计算：请求路由使用最短队列优先（join-shortest-queue）或轮询（round-robin），调度默认为先进先出（FIFO），而 KV 缓存置换则遵循最近最少使用（LRU）原则。这些通用策略忽略了 LLM 推理的独特结构——动态增长的 KV 缓存内存、预填充（prefill）与解码（decode）阶段的不对称性、未知的输出长度以及连续批处理（continuous batching）的约束。我们主张，该领域必须开发能够捕捉这些特征的数学模型，从而设计出在各种工作负载下都具有可证明性能保证的算法，而不是那些在某些场景下成功但在其他场景下不可预测地失败的启发式方法。运筹学与机器学习系统交叉领域的新兴研究表明，基于原则的方法在提供理论保证的同时，可以达到或超过启发式方法的性能。我们呼吁学术界将 LLM 推理服务的算法设计视为一个研究前沿。

## Abstract
This position paper argues that LLM inference serving has outgrown generic heuristics and now demands mathematical optimization and algorithmic foundations. Despite rapid advances in serving systems such as vLLM and SGLang, their algorithmic cores remain largely unchanged from classical distributed computing: request routing uses join-shortest-queue or round-robin, scheduling defaults to FIFO, and KV cache eviction follows LRU. These general-purpose policies ignore the distinctive structure of LLM inference--dynamically growing KV cache memory, prefill-decode phase asymmetry, unknown output lengths, and continuous batching constraints. We contend that the field must develop mathematical models capturing these characteristics, enabling the design of algorithms with provable performance guarantees across diverse workloads, rather than heuristics that may succeed in some scenarios but fail unpredictably in others. Emerging work at the intersection of operations research and ML systems demonstrates that principled methods can match or exceed heuristic performance while providing theoretical guarantees. We call on the community to recognize algorithmic design for LLM serving as a research frontier.

---

## 论文详细总结（自动生成）

这是一份关于论文《Position: LLM Serving Needs Mathematical Optimization and Algorithmic Foundations, Not Just Heuristics》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）部署规模达到每日数十亿次请求，推理效率已成为经济和环境层面的迫切需求。尽管 vLLM、SGLang 等系统在架构上（如 PagedAttention、连续批处理）取得了巨大进步，但其核心决策算法仍依赖于传统的启发式策略（如 FIFO 调度、LRU 缓存置换、轮询路由）。
*   **核心问题**：作者指出，LLM 推理具有独特的结构特征（KV 缓存动态增长、预填充与解码阶段的不对称性、输出长度未知、连续批处理导致的请求耦合），通用启发式方法无法有效利用这些特征，且在复杂负载下缺乏性能保证。
*   **整体含义**：本文是一篇**观点论文（Position Paper）**，旨在呼吁学术界和工业界将 LLM 服务从“经验主义的启发式”转向“基于数学优化的算法基础”，通过运筹学（OR）与系统工程的交叉研究，提升系统的鲁棒性、效率和可预测性。

### 2. 方法论：核心思想与技术细节
论文提倡将 LLM 服务中的关键决策环节建模为严谨的数学优化问题，并给出了四个代表性方向：
*   **MoE 负载均衡（线性规划）**：针对混合专家模型（MoE），将 Token 路由建模为受限分配问题。通过求解线性规划（LP）来重新分配冗余专家副本间的负载，最小化 GPU 的最大负载，从而消除启发式方法（如辅助损失函数）对模型质量的负面影响。
*   **数据并行路由（在线整数规划）**：针对屏障同步（Barrier Sync）下的解码任务，提出“平衡未来”（Balance-Future）原则。利用短时预测信息，通过在线整数规划解决 KV 缓存增长带来的负载漂移和“木桶效应”（长序列拖慢整组速度）。
*   **调度与容量规划（排队论）**：利用排队论推导系统稳定性的闭式解条件，考虑计算和内存的双重约束，从而在部署前精确计算所需的 GPU 数量，而非依赖滞后的自动扩缩容启发式。
*   **成本感知缓存（在线优化）**：针对多模态 LLM，提出“最小预期成本”（LEC）算法。该算法不仅考虑访问频率，还考虑不同对象（如高分辨率视频 vs 缩略图）重计算成本的异质性，以实现信息论意义上的最优悔值（Regret）。

### 3. 实验设计与 Benchmark
由于本文是观点论文，其论据主要基于对已有前沿研究成果的引用和总结：
*   **案例研究 1 (LPLB)**：引用了 DeepSeek 的线性规划负载均衡器，在其实际生产环境（DeepSeek V3/R1）中验证了优化算法的可行性。
*   **案例研究 2 (DP Routing)**：对比了 vLLM 默认的路由策略（Round-robin, Power-of-two-choices 等），通过理论证明和模拟实验展示了在对抗性负载下的性能提升。
*   **对比基准**：主要对比对象是目前主流推理框架（vLLM, SGLang）中内置的经典分布式系统启发式算法。

### 4. 资源与算力
*   **算力说明**：文中未详细列出单一实验的 GPU 总量或训练时长，因为其核心论点在于推理阶段的决策效率。
*   **求解开销**：论文特别提到了算法的实时性。例如，DeepSeek 的 LPLB 在 GPU 上求解一次线性规划仅需约 **100μs**，完全能够适配解码阶段（通常为 30-100ms）的时间预算。

### 5. 实验数量与充分性
*   **充分性**：论文引用的案例涵盖了从 MoE 专家路由到多模态缓存置换的多个维度，且包含了理论证明（如竞争比分析、稳定性分析）与工业级生产部署（DeepSeek）的证据。
*   **客观性**：作者客观地承认了启发式方法在当前阶段的成功，但通过对比展示了在极端或动态负载下，数学优化方法能提供启发式无法给出的“最坏情况保证”。

### 6. 主要结论与发现
*   **理论价值**：数学优化不仅能提升平均性能，更重要的是能提供**性能下界保证**，确保系统在突发流量或异常负载下不会崩溃。
*   **设计指导**：理论分析揭示的系统极限（如稳定性边界）可以指导硬件采购和容量规划，避免过度配置带来的浪费。
*   **算法演进**：LLM 推理创造了全新的算法问题类别（如动态增长内存约束下的调度），这为运筹学和算法研究提供了新的前沿阵地。

### 7. 优点（亮点）
*   **跨学科视角**：成功将航空收益管理（Airline Revenue Management）等成熟的运筹学思想引入到前沿的 AI 系统设计中。
*   **前瞻性**：不仅关注当前的 Transformer 架构，还预见了 Agent 推理（分支、依赖、变长暂停）带来的复杂调度挑战。
*   **实用性**：强调优化算法不一定要作为实时求解器运行，其推导出的“结构化洞察”可以转化为极简且高效的在线策略。

### 8. 不足与局限
*   **模型简化风险**：数学建模往往需要对硬件特性（如非线性的算子执行时间）进行简化，可能导致理论最优与实际最优存在偏差。
*   **预测依赖性**：部分优化算法（如调度）依赖于对输出长度的预测，若预测模型误差过大，算法性能可能会受限（尽管文中提到了鲁棒优化方案）。
*   **落地门槛**：相比于简单的 FIFO 或 LRU，实现和维护一套基于优化理论的决策系统对工程团队的数学素养要求更高。

（完）
