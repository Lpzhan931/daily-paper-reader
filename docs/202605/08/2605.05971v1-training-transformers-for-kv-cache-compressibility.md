---
title: Training Transformers for KV Cache Compressibility
title_zh: 针对 KV 缓存可压缩性训练 Transformer
authors: "Yoav Gelberg, Yam Eitan, Michael Bronstein, Yarin Gal, Haggai Maron"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05971v1"
tags: ["query:llm"]
score: 9.0
evidence: KV 缓存压缩与优化
tldr: 针对长文本语言模型中KV缓存带来的内存和计算瓶颈，本文提出KV压缩感知训练（KV-CAT）。研究发现KV缓存的可压缩性取决于模型学习到的表示而非仅由上下文决定。通过在持续预训练阶段引入KV稀疏化策略，强制模型使用更少的KV槽位，从而诱导产生更易压缩的表示。实验证明，KV-CAT显著提升了下游压缩方法在检索、长文本问答等任务中的性能表现。
selection_source: fresh_fetch
motivation: 现有的KV缓存压缩方法多为事后处理，受限于预训练模型内部表示的可压缩性，因此需要从训练阶段引导模型生成易压缩的表示。
method: 提出KV-CAT持续预训练方法，通过在训练时引入KV稀疏化策略（掩码KV槽位）来强制模型学习更高效的表示。
result: 实验表明KV-CAT在检索、长文本问答及困惑度评估中，均能显著改善下游压缩方法的质量与预算权衡。
conclusion: 通过在训练阶段显式优化KV缓存的可压缩性，可以有效突破长文本建模中的内存瓶颈，提升模型在有限资源下的性能。
---

## 摘要
长上下文语言建模日益受到键值（KV）缓存的限制，其内存和解码时的访问成本随前缀长度线性增长。这一瓶颈催生了一系列上下文压缩方法，从令牌级摘要到最近基于优化的 KV 压缩方法。这些事后（post-hoc）方法作用于固定预训练模型的 KV 缓存，因此其有效性从根本上受限于模型内部表示的可压缩程度。在这项工作中，我们形式化了 KV 可压缩性的概念，并证明它是学习到的表示的一种属性，而不仅仅取决于上下文。我们证明了几乎任何序列到向量函数都允许高度可压缩和本质上不可压缩的 Transformer 实现，这突显了在训练过程中引导 Transformer 学习可压缩表示的必要性。受此启发，我们提出了 KV 压缩感知训练（KV-CAT），这是一种持续预训练过程，旨在激励可压缩表示的出现。我们引入了一种训练时的 KV 稀疏化策略，在训练期间掩蔽 KV 插槽。这迫使模型使用更少的 KV 插槽，并鼓励其学习易于进行事后压缩的表示。实验表明，KV-CAT 在检索、长上下文问答以及基于困惑度的压缩前缀续写评估中，改善了下游压缩方法的质量-预算权衡。

## Abstract
Long-context language modeling is increasingly constrained by the Key-Value (KV) cache, whose memory and decode-time access costs scale linearly with the prefix length. This bottleneck has motivated a range of context-compression methods, from token-level summarization to recent optimization-based KV compression methods. These post-hoc methods operate on the KV cache of a fixed pretrained model, so their effectiveness is fundamentally limited by how well the model's internal representations can be compressed. In this work, we formalize the notion of KV compressibility and show that it is a property of the learned representations, rather than of the context alone. We prove that almost any sequence-to-vector function admits both highly compressible and inherently non-compressible transformer implementations, highlighting the need to guide transformers toward compressible representations during training. Motivated by this, we propose KV-Compression Aware Training (KV-CAT), a continued pretraining procedure that incentivizes the emergence of compressible representations. We introduce a train-time KV sparsification policy that masks KV slots during training. This forces the model to use fewer KV slots and encourages it to learn representations amenable to post-hoc compression. Empirically, we show that KV-CAT improves the quality-budget tradeoff of downstream compression methods across retrieval, long-context question answering, and perplexity-based evaluation of compressed-prefix continuation.

---

## 论文详细总结（自动生成）

这篇论文《Training Transformers for KV Cache Compressibility》探讨了如何通过训练使 Transformer 模型生成的内部表示更易于进行 KV 缓存（Key-Value Cache）压缩，从而缓解长文本推理中的内存和计算瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在长文本生成中，KV 缓存随序列长度线性增长，成为推理的主要瓶颈。现有的压缩方法（如令牌剔除、量化、优化压缩等）大多是“事后（Post-hoc）”的，即作用于已训练好的固定模型。
*   **研究动机**：作者认为，KV 缓存的可压缩性不仅取决于输入序列，更取决于模型学习到的**内部表示**。如果模型在训练时没有考虑到压缩需求，其生成的表示可能包含难以剔除的冗余或非结构化信息。
*   **整体含义**：论文试图通过改变训练过程，引导模型产生“压缩友好”的表示，从而提升所有下游事后压缩算法的效率和效果。

### 2. 方法论：KV-CAT (KV-Compression Aware Training)
*   **核心思想**：引入一种“持续预训练（Continued Pretraining）”方案，在训练中显式模拟 KV 压缩过程，迫使模型在信息受限的情况下仍能保持性能。
*   **关键技术细节**：
    *   **训练时 KV 稀疏化策略**：在训练的前向传播中，使用“路由器（Router）”模块对 KV 插槽进行掩码（Masking）。路由器基于线性注意力机制，预测每个令牌的重要性并生成二值掩码。
    *   **联合优化目标**：损失函数由三部分组成：
        1.  **$L_{mask}$（掩码损失）**：通过自蒸馏，使掩码后的模型输出分布尽可能接近未掩码（密集）模型的输出。
        2.  **$L_{anchor}$（锚点损失）**：在未掩码的原始路径上执行标准下一令牌预测（NTP），以保持模型的基础能力。
        3.  **$L_{budget}$（预算损失）**：强制路由器达到预设的 KV 保留率（如 50%）。
*   **理论支持**：论文证明了（定理 3.1）对于几乎任何序列到向量函数，都存在高度可压缩和不可压缩的两种 Transformer 实现，证明了引导训练的必要性。

### 3. 实验设计
*   **模型与数据集**：
    *   **基础模型**：Qwen 2.5-0.5B 和 Qwen 2.5-1.5B。
    *   **训练数据**：FineWeb-Edu（约 52.4 亿令牌）。
*   **评估场景与 Benchmark**：
    *   **后缀困惑度（Suffix PPL）**：评估前缀压缩后对后续文本生成的质量影响。
    *   **大海捞针（Needle-in-a-haystack）**：评估从压缩缓存中检索特定信息的能力。
    *   **长文本问答**：使用 LongBench v2 中的 7 个子任务。
    *   **基础能力评估**：使用 LightEval 评估 HellaSwag、ARC 等 6 个标准多选任务，确保模型没有退化。
*   **对比方法**：对比了原始基座模型（Base）与经过 KV-CAT 训练的模型，在应用相同的事后压缩算法（如 Attention Matching 和梯度优化压缩）时的表现。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练细节**：
    *   训练步数：40,000 步。
    *   序列长度：1024 令牌。
    *   总令牌数：5.24B。
    *   学习率：$10^{-4}$。
*   **优化耗时**：文中提到 KV-CAT 训练后的模型在进行梯度压缩优化时，达到相同效果的速度比基座模型快 **5 倍**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种模型规模（0.5B, 1.5B）、多种压缩率（5% 到 50%）、以及多种评估维度（困惑度、检索、问答）。
*   **消融实验**：作者对比了不同的训练时稀疏化策略（随机掩码、基于注意力的掩码、学习型路由器），证明了学习型路由器在保持基础能力和提升可压缩性之间取得了最佳平衡。
*   **充分性评价**：实验设计较为全面，不仅验证了压缩后的性能提升，还通过标准 Benchmark 证明了模型在“非压缩模式”下性能未受损，实验对比具有客观性和公平性。

### 6. 主要结论与发现
*   **可压缩性是可训练的**：通过 KV-CAT，模型可以学会将关键信息浓缩到少数 KV 插槽中。
*   **性能显著提升**：在相同的压缩预算下，KV-CAT 模型在长文本问答上比基座模型平均提升了 **39%**，检索准确率提升了 **68%**。
*   **加速优化**：事后压缩算法在 KV-CAT 模型上的收敛速度显著加快，降低了推理时的额外开销。
*   **无损基础能力**：KV-CAT 训练后的模型在不压缩的情况下，其标准任务表现与原模型持平。

### 7. 优点
*   **理论与实践结合**：不仅提出了算法，还提供了严谨的数学证明，解释了为什么 Transformer 表示的可压缩性存在差异。
*   **通用性强**：KV-CAT 并不是一种新的压缩算法，而是一种增强现有压缩算法效果的训练框架，具有很强的普适性。
*   **显著的效率提升**：5 倍的优化加速对于实时性要求高的长文本应用具有重要价值。

### 8. 不足与局限
*   **训练开销**：KV-CAT 需要额外的持续预训练过程，对于资源受限的团队来说，重新训练大参数量模型（如 70B+）成本较高。
*   **模型规模限制**：实验主要集中在 1.5B 以下的小规模模型，尚未在大规模模型（如 Llama-3-70B）上验证其扩展性。
*   **任务覆盖**：虽然涵盖了长文本问答和检索，但对于代码生成、复杂推理等特定长文本任务的覆盖尚不完整。
*   **架构依赖**：主要针对标准的 Decoder-only Transformer，对于其他变体架构的适用性有待研究。

（完）
