---
title: Saliency-Aware Regularized Quantization Calibration for Large Language Models
title_zh: 大语言模型的显著性感知正则化量化校准
authors: "Yanlong Zhao, Xiaoyuan Cheng, Huihang Liu, Baihua He, Xinyu Zhang, Harrison Bo Hua Zhu, Wenlong Chen, Li Zeng, Zhuo Sun"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05693v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对内存和延迟限制的训练后量化
tldr: 针对大语言模型训练后量化（PTQ）中因校准数据有限导致的泛化性能下降问题，本文提出了SARQC框架。该方法在传统的层级重建误差目标中引入了显著性感知正则化项，通过约束量化权重与原始权重的距离，提升了模型在推理时的泛化能力。SARQC可无缝集成至现有PTQ流程，在多种模型上显著提升了性能且不增加推理开销。
selection_source: fresh_fetch
motivation: 现有的PTQ方法仅最小化有限校准数据上的重建误差，可能导致量化权重过度偏离原始权重，从而引发泛化风险。
method: 提出SARQC框架，在PTQ优化目标中加入显著性感知正则化项，引导量化参数在校准过程中保持与原始权重的接近度。
result: 在稠密模型和混合专家（MoE）模型上的实验表明，该方法在不增加推理负担的情况下，显著提升了模型的困惑度和零样本准确率。
conclusion: 通过引入显著性感知正则化，SARQC有效缓解了PTQ的泛化问题，为大语言模型的高效部署提供了更稳健的量化方案。
---

## 摘要
训练后量化 (PTQ) 是在内存和延迟约束下部署大语言模型 (LLMs) 的有效方法。大多数现有的 PTQ 方法通过在预定的校准数据集上最小化逐层重构误差来确定量化参数，通常通过缩放搜索或基于 Gram 的方法进行优化。然而，从泛化风险的角度来看，现有的仅基于有限或不具代表性的校准数据上的经验重构误差的 PTQ 校准目标，可能会使量化权重偏离原始权重。这可能导致泛化风险发散，从而潜在地降低下游性能。为了解决这一问题，我们提出了显著性感知正则化量化校准 (SARQC)，这是一个统一的框架，它通过显著性感知正则化项增强了标准的 PTQ 目标。该项鼓励量化权重在校准过程中保持接近原始权重，从而提高推理时的泛化能力。SARQC 可以无缝集成到现有的 PTQ 流水线中，在统一的公式下增强了缩放搜索和基于 Gram 的方法。在稠密模型和混合专家 (MoE) LLMs 上进行的大量实验表明，在不增加推理额外计算开销的情况下，困惑度和零样本准确率得到了一致的提升。

## Abstract
Post-training quantization (PTQ) is an effective approach for deploying large language models (LLMs) under memory and latency constraints. Most existing PTQ methods determine quantization parameters by minimizing a layer-wise reconstruction error on a predetermined calibration dataset, usually optimized via either scale search or Gram-based methods. However, from the perspective of generalization risk, existing calibration objectives of PTQ based only on empirical reconstruction error on limited or unrepresentative calibration data could move the quantized weights away from the original weights. This may cause the generalization risk to diverge, potentially degrading downstream performance. To address this issue, we propose \emph{Saliency-Aware Regularized Quantization Calibration} (SARQC) a unified framework that augments the standard PTQ objective with a saliency-aware regularization term. This term encourages quantized weights to stay close to the original weights during calibration, leading to improved generalization during inference. SARQC integrates seamlessly into existing PTQ pipelines, enhancing both scale search and Gram-based methods under a unified formulation. Extensive experiments on dense and Mixture-of-Experts LLMs demonstrate consistent improvements in perplexity and zero-shot accuracy, without additional computational overhead during inference.

---

## 论文详细总结（自动生成）

这是一份关于论文《Saliency-Aware Regularized Quantization Calibration for Large Language Models》（大语言模型的显著性感知正则化量化校准）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：为了在有限的内存和延迟约束下部署大语言模型（LLM），训练后量化（PTQ）成为主流技术。
*   **核心问题**：现有的 PTQ 方法（如 AWQ、GPTQ）通常通过在极小的校准数据集上最小化“层级重构误差”来确定量化参数。
*   **研究动机**：作者发现，仅追求重构误差最小化会导致量化后的权重过度偏离原始浮点权重（即“权重漂移”）。在校准数据有限或不具代表性时，这种偏离会显著增大泛化风险，导致模型在下游任务上的性能大幅下降，尤其是在 2-bit 或 3-bit 等极低比特设置下。

### 2. 方法论
*   **核心思想**：提出 **SARQC** 框架，在传统的重构误差目标函数中引入一个**显著性感知正则化项**。该项通过约束量化权重与原始权重之间的距离，在保持输出一致性的同时，防止权重过度漂移。
*   **关键技术细节**：
    *   **统一目标函数**：$\min \mathcal{L}_{recon} + \lambda \mathcal{L}_{sar}$。其中 $\mathcal{L}_{recon}$ 是重构误差，$\mathcal{L}_{sar}$ 是显著性加权的权重差异惩罚项。
    *   **显著性矩阵 ($S_l$)**：利用激活值和权重的统计特性（如均值）来衡量不同通道的重要性。对于重要的通道，施加更大的正则化惩罚，强制其量化权重更接近原始值。
    *   **两种优化策略**：
        1.  **SARQC-GS (基于网格搜索)**：集成到类似 AWQ 的方法中，在缩放因子搜索过程中加入正则化约束。
        2.  **SARQC-GBS (基于 Gram 矩阵)**：集成到类似 GPTQ 的方法中，通过修改曲率矩阵（$G_l = X_l X_l^\top + \lambda S_l S_l^\top$）来调整权重的更新步长。

### 3. 实验设计
*   **数据集/场景**：
    *   **语言建模**：在 WikiText2 上测试困惑度（Perplexity）。
    *   **下游任务**：在 PIQA、HellaSwag、MMLU、HumanEval、BoolQ 等 8 个常识推理和知识基准上进行零样本（Zero-shot）准确率评估。
*   **Benchmark 与对比方法**：
    *   **模型**：LLaMA/LLaMA2 (7B, 13B, 30B)、DeepSeek-MoE-16B、Qwen3-MoE-30B、Mixtral-8x7B。
    *   **对比方法**：AWQ、GPTQ、GPTAQ 以及强力基准 OmniQuant。
    *   **量化设置**：主要针对权重量化（W2A16, W3A16, W4A16）。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA A100 80GB GPU**。
*   **算力说明**：论文未明确给出总的训练/校准时长，但强调了 PTQ 的特性是轻量级的。SARQC 在推理阶段**不增加任何额外计算开销**，因为正则化仅作用于离线的校准阶段。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 30B 参数规模的稠密模型和混合专家（MoE）模型，实验组数较多。
*   **充分性评估**：
    *   **消融实验**：详细探讨了正则化强度 $\lambda$、显著性因子 $\gamma$、校准样本数量（16 到 128 个）以及不同校准语料库（C4, Pile）对结果的影响。
    *   **客观性**：通过在不同架构（Dense vs MoE）和不同量化比特下的一致性表现，证明了方法的稳健性。实验对比了多种 SOTA 方法，具有较高的说服力。

### 6. 主要结论与发现
*   **权重漂移是关键**：实验证实，单纯追求极低的重构误差并不总能带来更好的下游任务表现，适度的正则化能找到性能的“甜点区”。
*   **低比特优势明显**：在 2-bit 和 3-bit 量化下，SARQC 相比 GPTQ 等方法能显著降低困惑度（例如在 LLaMA2-7B W2 下，困惑度从 1889 降至 225）。
*   **显著性感知的重要性**：带有显著性权重的正则化（Saliency-Aware）效果优于简单的恒等矩阵正则化（Identity）。
*   **小样本鲁棒性**：当校准数据极少（如仅 16 个样本）时，SARQC 的领先优势扩大，证明其能有效缓解过拟合。

### 7. 优点
*   **理论与实践结合**：从泛化风险界限（Generalization Risk Bound）出发提供理论支撑，并给出了易于实现的算法。
*   **高通用性**：作为一个通用的正则化框架，它可以无缝作为插件集成到现有的多种 PTQ 流程中（如文中展示了对 OmniQuant 的提升）。
*   **部署友好**：不改变模型结构，不增加推理延迟。

### 8. 不足与局限
*   **模型规模上限**：实验最高只覆盖到 30B 模型，未在千亿级（175B+）超大规模模型上验证。
*   **显著性因子设计**：目前的显著性因子主要基于经验统计，可能存在更优的启发式设计或学习方法。
*   **量化场景限制**：主要侧重于权重量化，对于权重-激活同时量化（W4A4, W8A8 等）的复杂场景探讨较少。

（完）
