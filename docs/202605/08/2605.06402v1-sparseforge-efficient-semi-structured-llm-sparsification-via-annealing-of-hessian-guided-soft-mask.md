---
title: "SparseForge: Efficient Semi-Structured LLM Sparsification via Annealing of Hessian-Guided Soft-Mask"
title_zh: SparseForge：通过 Hessian 引导的软掩码退火实现高效的半结构化大语言模型稀疏化
authors: "Liu Hanzuo, Chaofan Lin, Weixuan Sun, Yulong Wang, Key, Rayying, Mingyu Gao"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06402v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于大语言模型加速的半结构化剪枝
tldr: 4 稀疏度下达到甚至超过稠密模型的精度，显著提升了稀疏化的效率与效果。
selection_source: fresh_fetch
motivation: 现有的半结构化剪枝方法在保持模型精度时往往需要极高的重训练计算成本。
method: 提出一种结合 Hessian 信息与软掩码渐进退火的框架，通过直接优化稀疏掩码实现高效的稀疏恢复。
result: 4 稀疏度下超越稠密模型精度，效率远超现有 SOTA 方法。
conclusion: SparseForge 在不同模型系列上均表现出优异的精度与效率平衡，为大模型硬件加速提供了高效的稀疏化方案。
---

## 摘要
半结构化稀疏性为利用原生硬件支持加速大语言模型（LLMs）提供了一条切实可行的路径，但由于强结构耦合，训练后半结构化剪枝通常会遭受严重的质量下降。现有方法依赖大规模稀疏重训练来恢复准确率，导致了高昂的计算成本。我们提出了 SparseForge，这是一个训练后框架，它通过直接优化稀疏掩码而非增加重训练 token 的规模来提高恢复效率。SparseForge 将 Hessian 感知的显著性估计与软掩码向硬件可执行结构化稀疏性的渐进退火相结合，实现了稳定且高效的稀疏恢复。在 2:4 稀疏度下的 LLaMA-2-7B 上，SparseForge 仅使用 5B 个重训练 token 就达到了 57.27% 的平均零样本准确率，超过了稠密模型的 56.43%，并接近了使用 40B 个 token 的最先进方法所取得的 57.52% 的结果。SparseForge 在准确率与效率权衡方面的这种改进在不同模型系列中均表现出一致性。

## Abstract
Semi-structured sparsity provides a practical path to accelerate large language models (LLMs) with native hardware support, but post-training semi-structured pruning often suffers from substantial quality degradation due to strong structural coupling. Existing methods rely on large-scale sparse retraining to recover accuracy, resulting in high computational cost.   We propose SparseForge, a post-training framework that improves recovery efficiency by directly optimizing the sparsity mask rather than scaling up retraining tokens. SparseForge combines Hessian-aware importance estimation with progressive annealing of soft masks into hardware-executable structured sparsity, enabling stable and efficient sparse recovery. On LLaMA-2-7B under 2:4 sparsity, SparseForge achieves 57.27% average zero-shot accuracy with only $\textbf{5B}$ retraining tokens, surpassing the dense model's 56.43% accuracy and approaching the 57.52% result of a state-of-the-art method using $\textbf{40B}$ tokens. Such improvements on the accuracy-efficiency trade-off from SparseForge are shown to be consistent across model families.

---

## 论文详细总结（自动生成）

### 论文总结：SparseForge —— 基于 Hessian 引导软掩码退火的高效 LLM 半结构化稀疏化框架

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）的部署面临巨大的计算和存储压力。**半结构化稀疏（如 2:4 稀疏）**因其能获得 NVIDIA 等现代硬件的原生加速支持而备受关注。然而，现有的训练后剪枝方法在面对这种强结构约束时，往往会导致严重的精度下降。为了恢复精度，现有最先进（SOTA）的方法（如 CAST）通常需要消耗海量的重训练数据（如 40B tokens），计算成本极高。
论文认为，**稀疏恢复的瓶颈在于“掩码质量”而非“重训练规模”**。因此，研究动机在于如何通过优化掩码生成过程，在极低的重训练成本下实现高精度的半结构化稀疏化。

#### 2. 方法论
SparseForge 提出了一个**掩码退火（Mask Annealing）**框架，核心思想是将掩码视为连续可优化的变量，并经历从“软”到“硬”的渐进转变：
*   **双轨重训练循环（Dual-track Loop）**：在“加热”阶段，同时优化模型权重和连续的软掩码（Soft-mask）。权重通过标准梯度下降更新，而掩码则根据专门的规则定期更新。
*   **Hessian 引导的掩码更新**：利用二阶 Hessian 信息（通过 Hutchinson 随机估计器获得对角线近似）来评估权重的敏感度。相比单纯的幅值（Magnitude）准则，Hessian 信息能更好地处理半结构化约束下的组内竞争。
*   **渐进式淬火（Progressive Quenching）**：
    *   引入**二元偏好正则项（Binary-preference Regularizer）**，惩罚处于 0 和 1 之间的中间值。
    *   通过**结构化混合因子（$\beta$）**和**温度系数（$T$）**，将软掩码逐渐推向符合硬件要求的 2:4 二值化模式。
    *   最后阶段进行硬投影，并辅以短时间的微调以吸收投影误差。

#### 3. 实验设计
*   **模型覆盖**：涵盖了 GPT-2、OPT、LLaMA-2、Qwen3 以及混合专家模型（MoE）DeepSeek-MoE，参数量从 124M 到 16B。
*   **数据集**：重训练使用了 Dolmino-mix-1124 和 C4 语料库；评估使用了 WikiText-2（困惑度）以及 ARC、BoolQ、HellaSwag、PIQA 等 7-9 个零样本（Zero-shot）下游任务。
*   **对比方法**：包括非结构化剪枝（Wanda, SparseGPT）和半结构化剪枝 SOTA（MaskLLM, AST, CAST）。

#### 4. 资源与算力
*   **硬件环境**：实验在包含 **32 台 NVIDIA L20A GPU** 的集群上完成。
*   **训练时长**：以 LLaMA-2-7B 为例，使用 5B tokens 进行完整的 SparseForge 训练大约需要 **50 个 GPU 小时**。
*   **推理验证**：在 NVIDIA H800 GPU 上使用 TensorRT-LLM 测试了端到端加速效果。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了跨模型家族的广泛测试（8 种不同架构/规模的模型），并针对 LLaMA-2-7B 进行了详尽的对比。
*   **消融实验**：非常充分。作者分别验证了重训练数据质量、Token 预算、Hessian 准则的必要性、SLoRB 辅助分支以及蒸馏损失的影响。
*   **客观性**：每个配置运行了 3 个随机种子并取平均值，对比基线均采用了 SOTA 论文中的公开数据或在相同环境下重新评估，实验设计较为公正。

#### 6. 主要结论与发现
*   **高效性**：在 LLaMA-2-7B 上，SparseForge 仅需 **5B tokens** 即可达到 57.27% 的准确率，超过了稠密模型（56.43%），且接近了消耗 **40B tokens** 的 CAST 效果（57.52%），效率提升约 8 倍。
*   **通用性**：该方法在 Dense 和 MoE 架构上均有效，且能扩展到 Block-16 等其他稀疏模式。
*   **硬件收益**：在 H800 上，2:4 稀疏模型相比稠密模型实现了 **1.40x - 1.44x 的吞吐量提升**，同时显存占用减少了约 42%。

#### 7. 优点
*   **掩码优化视角新颖**：将掩码视为一等公民进行连续优化，而非简单的投影或离散选择。
*   **精度恢复极佳**：在 50% 稀疏度下实现了无损甚至超越原模型的精度。
*   **实用性强**：直接面向硬件原生支持的 2:4 模式，并提供了实际的端到端加速数据。

#### 8. 不足与局限
*   **数据依赖性**：消融实验显示，重训练数据的质量对最终结果影响较大（如 Qwen3 在 C4 数据上的表现略逊于预期），说明模型对数据分布较为敏感。
*   **超参数调优**：退火过程涉及温度、混合因子、正则化权重等多个超参数，可能需要针对不同模型进行一定的微调。
*   **模式限制**：虽然论文提到了 Block-16，但核心讨论仍集中在 2:4 模式，对于更激进或更多样化的稀疏结构探讨较少。

（完）
