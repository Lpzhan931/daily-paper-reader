---
title: "Requests of a Feather Must Flock Together: Batch Size vs. Prefix Homogeneity in LLM Inference"
title_zh: 物以类聚：大语言模型推理中批大小与前缀同质性的权衡
authors: "Saksham Rathi, Preeti, Mythili Vutukuru"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06046v1"
tags: ["query:llm"]
score: 10.0
evidence: 通过KV缓存局部性和前缀同质化优化吞吐量
tldr: 本研究针对大语言模型推理中的KV缓存访问瓶颈，提出了一种名为Feather的调度器。研究发现，相比于盲目追求大批次，由共享前缀请求组成的“同质小批次”能通过提升空间和时间局部性获得更高的吞吐量。Feather利用强化学习动态平衡批次大小与前缀同质性，并引入分块哈希树（CHT）降低前缀检测的CPU开销。实验表明，Feather在vLLM和SGLang中可提升2-10倍吞吐量，显著优化了前缀共享场景下的推理效率。
selection_source: fresh_fetch
motivation: 现有的LLM推理调度器过度追求最大化批次大小，忽视了前缀共享请求在提升KV缓存访问局部性方面的潜力，且前缀检测开销巨大。
method: 提出Feather调度器，结合强化学习优化批次同质性与大小的权衡，并使用分块哈希树（CHT）实现高效的前缀匹配与请求选择。
result: 在vLLM和SGLang上的评估显示，Feather在保持非共享场景性能的同时，将端到端吞吐量提升了2至10倍。
conclusion: 通过优化批次的前缀同质性并降低调度开销，可以显著突破内存受限的LLM推理性能瓶颈。
---

## 摘要
大语言模型中的自回归 Token 生成受限于内存，因为它需要“注意力机制”处理所有先前 Token 的键值张量（KV 缓存）。先前的研究旨在通过将多个请求批处理在一起，并在 GPU 内存限制下最大化批大小，来提高该解码过程的效率。我们工作的关键观察是，在具有前缀共享的工作负载中，较小的、前缀同质的批次（即所有请求共享一个公共前缀）由于在 KV 缓存访问期间具有更好的空间和时间局部性，可以比更大的、异质的批次实现更高的解码吞吐量。然而，最先进推理引擎中的前缀感知调度器仅为了减少 KV 缓存内存占用而最大化批次内的前缀重用，但并不会在性能可能更好的较小同质批次处停止批次构建。此外，我们表明现有调度器中的共享前缀检测依赖于基数树（radix-tree）遍历，从而产生了巨大的 CPU 开销，这通常与 GPU 执行时间相当。本文提出了 Feather，这是一种前缀感知调度器，它使用强化学习（RL）来学习批大小与前缀同质性之间的最佳权衡。我们还引入了分块哈希树（Chunked Hash Tree, CHT），这是一种轻量级数据结构，可为 RL 调度器实现快速前缀检测和高效请求选择，从而避免昂贵的树遍历。我们将 Feather 集成到 vLLM 和 SGLang 中，评估结果表明，与现有调度器相比，Feather 实现了 2 到 10 倍的端到端吞吐量提升，同时在工作负载没有足够前缀共享时，其表现也不逊于现状。Feather 通过减少 KV 缓存访问的总次数来实现这些增益，超越了具有相同目标的前缀感知注意力内核的性能。

## Abstract
Auto-regressive token generation in large language models is memory-bound because it requires "attending to" key and value tensors (KV cache) of all previous tokens. Prior work aims to improve the efficiency of this decode process by batching multiple requests together, and maximizing batch size subject to GPU memory constraints. The key observation of our work is that with prefix-sharing workloads, smaller, prefix-homogeneous batches -- where all requests share a common prefix -- can achieve higher decode throughput than larger, heterogeneous batches, due to better spatial and temporal locality during KV cache accesses. However, prefix-aware schedulers in state-of-the-art inference engines maximize prefix reuse within a batch only to reduce KV cache memory footprint, but do not stop batch formation at smaller homogeneous batches that could have performed better. Further, we show that shared prefix detection in existing schedulers relies on radix-tree traversals, incurring substantial CPU overhead that is often comparable to GPU execution time. This paper presents Feather, a prefix-aware scheduler that uses reinforcement learning (RL) to learn the optimal tradeoff between batch size and prefix homogeneity. We also introduce Chunked Hash Tree (CHT), a lightweight data structure that enables fast prefix detection and efficient request selection for the RL scheduler, avoiding expensive tree traversals. We integrate Feather into vLLM and SGLang, and our evaluation shows that Feather achieves 2--10$\times$ higher end-to-end throughput as compared to existing schedulers, while doing no worse than the status quo when the workload does not have enough prefix sharing. Feather achieves these gains by reducing the total number of KV cache accesses, surpassing the performance of prefix-aware attention kernels that have the same goal.

---

## 论文详细总结（自动生成）

这是一份关于论文《Requests of a Feather Must Flock Together: Batch Size vs. Prefix Homogeneity in LLM Inference》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）推理的解码（Decode）阶段是内存受限的，主要瓶颈在于频繁访问 KV Cache。
*   **核心发现**：作者发现，相比于盲目追求最大化批次大小（Batch Size），**前缀同质性（Prefix Homogeneity）**——即一个批次内所有请求共享公共前缀的程度——对吞吐量的影响更大。
*   **整体含义**：同质批次能显著提升 GPU 内存访问的空间和时间局部性，从而提高 DRAM 带宽利用率。现有的调度器（如 vLLM, SGLang）要么忽视了这种权衡，要么在检测前缀共享时产生了巨大的 CPU 计算开销。

### 2. 方法论
Feather 调度器由两个核心组件组成：
*   **分块哈希树（Chunked Hash Tree, CHT）**：
    *   **核心思想**：放弃昂贵的 Token 级基数树（Radix Tree）遍历，改用基于固定大小分块（Chunk）的累积哈希。
    *   **技术细节**：每个请求表示为一个哈希向量。系统维护一个“缺失计数（Missing Count）”的最小堆，用于快速识别与当前活跃批次重合度最高的候选请求。
    *   **效率**：将请求选择的复杂度从 $O(W \times T)$ 降低到 $O(\log W)$（$W$ 为等待请求数，$T$ 为序列长度）。
*   **基于强化学习（RL）的批处理策略**：
    *   **核心思想**：通过学习来决定何时停止构建批次，以平衡批次大小与前缀损失。
    *   **算法流程**：调度器观察当前状态（当前批次大小、添加新请求导致的前缀长度损失、具有相同前缀的等待请求数），利用 Contextual Bandit（UCB 算法）或 Q-Learning 决定执行“添加（Add）”还是“停止并发送（Stop）”。
    *   **奖励机制**：以解码吞吐量作为奖励信号，使策略能够自适应不同的硬件和工作负载。

### 3. 实验设计
*   **数据集/场景**：使用了 **L-Eval**（长文本评估）和 **LongBench**（长上下文多任务）数据集。
*   **模型**：覆盖了 Qwen (0.5B, 1.5B, 8B)、LLaMA 3 (8B) 和 LongChat (13B)。
*   **Benchmark 对比方法**：
    *   **vLLM**：默认的 FCFS（先来先服务）调度。
    *   **SGLang**：包括其前缀感知的 LPM（最长前缀匹配）和 DFS-W（深度优先加权）策略。
    *   **PAT**：一种先进的前缀感知注意力内核优化方法。
    *   **DynamicBatching**：基于内存约束的动态批处理方法。

### 4. 资源与算力
*   **主要硬件**：NVIDIA **RTX 6000 Ada** GPU (48GB GDDR6, 96MB L2 缓存)。
*   **对比硬件**：NVIDIA **A100-80GB** GPU（专门用于与 PAT 内核进行公平对比）。
*   **训练时长**：RL 策略采用在线学习模式。文中提到在 100 秒左右的运行时间内，Bandit 策略即可从探索阶段收敛到稳定的同质批处理行为。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   不同请求到达率（Poisson Process）下的端到端吞吐量对比。
    *   不同前缀共享长度（1K 到 10K tokens）的敏感度分析。
    *   不同前缀组数量（1 到 100 组）对性能的影响。
    *   **消融实验**：验证了 CHT 相比基数树的开销优势，以及 RL 策略相比固定启发式规则的优越性。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了吞吐量，还深入分析了 DRAM 带宽利用率、CPU 调度开销和 Tensor Core 利用率，提供了底层性能支撑。

### 6. 主要结论与发现
*   **性能提升**：Feather 在前缀共享场景下比 vLLM 和 SGLang 提升了 **2-10 倍**的端到端吞吐量。
*   **局部性胜过规模**：即使批次较小，只要是前缀同质的，其性能也优于巨大的异质批次。
*   **调度开销**：传统基数树调度器的 CPU 开销在长序列下可占总延迟的 50-90%，而 Feather 的 CHT 将此开销降低了约 1000 倍。
*   **硬件无关性**：Feather 作为调度层优化，其表现优于或等同于需要特定硬件调优的算子级优化（如 PAT）。

### 7. 优点
*   **创新视角**：首次明确提出了“前缀同质性”比“批次大小”更重要的观点，并给出了理论和实验证明。
*   **极低开销**：CHT 数据结构巧妙地解决了前缀检测的 CPU 瓶颈问题。
*   **鲁棒性**：通过 RL 策略，Feather 在没有前缀共享的工作负载下也能退化为普通的 FCFS，不会产生负面影响。
*   **易于集成**：作为调度层优化，可以轻松集成到 vLLM 和 SGLang 等现有框架中。

### 8. 不足与局限
*   **分布式扩展**：论文主要侧重于单 GPU 场景，尚未详细讨论在多 GPU 张量并行或流水线并行下的复杂调度情况。
*   **状态空间离散化**：RL 策略依赖于对状态空间的离散化处理，在极端复杂或动态变化剧烈的工作负载下，可能需要更精细的函数近似器（如神经网络）。
*   **应用限制**：主要的性能增益来自于前缀共享（如 RAG、Few-shot、多轮对话），对于完全随机、无共享的纯独立请求工作负载，提升有限。

（完）
