---
title: "Tutti: Making SSD-Backed KV Cache Practical for Long-Context LLM Serving"
title_zh: Tutti：使基于 SSD 的 KV 缓存对于长上下文 LLM 服务变得实用
authors: "Shi Qiu, Yifan Hu, Xintao Wang, Wenhao Zhu, Jianqin Yan, Hao Chen, Kaiqiang Xu, Kai Chen, Yiming Zhang"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03375v1"
tags: ["query:kvcache-survey"]
score: 8.0
evidence: 基于 SSD 的 KV 缓存优化与管理
tldr: 针对长文本LLM推理中KV缓存超出内存容量的问题，本文提出Tutti系统。Tutti通过构建以GPU为中心的SSD对象存储，消除了传统I/O路径中CPU的干预。其核心设计包括GPU原生对象抽象、GPU io_uring异步I/O及松弛感知调度。实验表明，Tutti显著降低了首字延迟（TTFT），提升了吞吐量并降低了成本，使SSD备份的KV缓存性能接近DRAM。
selection_source: fresh_fetch
motivation: 传统的SSD备份KV缓存受限于CPU干预和碎片化I/O，导致严重的GPU停顿和低效的存储带宽利用。
method: 提出GPU中心化的对象存储架构，利用GPU原生对象抽象、GPU io_uring异步I/O和松弛感知调度实现高效数据传输。
result: "相比SOTA系统，Tutti将TTFT降低了78.3%，请求处理率提升了2倍，且成本降低了27%。"
conclusion: Tutti证明了通过消除CPU干预并重构GPU存储栈，SSD可以作为长文本LLM推理中极具性价比且高性能的KV缓存介质。
---

## 摘要
LLM 服务依赖前缀缓存（prefix caching）来提高推理性能。随着上下文长度的增加，键值（KV）缓存的占用空间远超 GPU HBM 和 CPU DRAM 的容量，KV 缓存越来越多地被卸载到 NVMe SSD。然而，从 SSD 恢复 KV 缓存面临 I/O 性能差的问题，并导致显著的 GPU 停顿。这主要是因为碎片化的 GPU 内存布局导致了大量微小的随机 I/O，使得低并行度的 CPU 成为严重的瓶颈，即使使用 GPU Direct Storage (GDS) 也是如此，因为 GDS 仍依赖 CPU 干预来发起每次 I/O，本质上仍是以 CPU 为中心的。本文提出了 Tutti，一种高效的基于 SSD 的 KV 缓存解决方案，它消除了 HBM 与 SSD 之间关键数据路径和 I/O 控制路径中的 CPU 干预。Tutti 的核心是一个以 GPU 为中心的 KV 缓存对象存储，其中 CPU 仅负责每层向 GPU 异步加载一次 I/O 内核。Tutti 通过以下设计使 NVMe SSD 带宽达到饱和，并将 GPU 停顿降至接近零：(i) 提供 GPU 原生对象抽象，实现批量 KV 缓存传输与管理；(ii) 通过引入 GPU io_uring 重构 GPU 存储栈，以支持异步 GPU 直接对象 I/O；(iii) 提出松弛感知（slack-aware）的 I/O 调度，以避免 GPU 资源争用。我们实现了 Tutti 并将其集成到 vLLM 中。广泛的评估表明，与最先进的启用 GDS 且基于 SSD 的 LMCache 相比，Tutti 在严格的 SLO 约束下将首字延迟（TTFT）降低了 78.3%，并将可实现的请求率提高了 2 倍。服务成本降低了 27%。Tutti 实现了与基于 DRAM 的 LMCache 几乎相同的推理性能，同时提供了近乎无限的容量。

## Abstract
LLM serving relies on prefix caching to improve inference performance. As growing contexts push key-value (KV) cache footprint far beyond GPU HBM and CPU DRAM capacity, KV cache is increasingly offloaded to NVMe SSDs. Unfortunately, restoring KV cache from SSDs suffers from poor I/O performance and incurs significant GPU stalls. This is primarily because the fragmented GPU memory layout results in a massive number of tiny random I/Os, rendering the low-parallelism CPU a severe bottleneck even with GPU Direct Storage (GDS), which still relies on CPU intervention to initiate each I/O and thus remains CPU-centric. This paper presents Tutti, an efficient SSD-backed KV caching solution that eliminates CPU intervention from the critical data and I/O control paths between HBM and SSDs. At the core of Tutti is a GPU-centric KV cache object store, in which the CPU is only responsible for asynchronously loading I/O kernels once per layer to the GPU. Tutti saturates NVMe SSD bandwidth and reduces GPU stalls to near zero through the following designs: (i) we provide a GPU-native object abstraction that enables bulk KV cache transfers and management; (ii) we re-architect the GPU storage stack by introducing GPU io_uring to support asynchronous GPU direct object I/O; and (iii) we propose slack-aware I/O scheduling to avoid GPU resource contention. We have implemented Tutti and integrated it to vLLM. Extensive evaluation shows that compared to the state-of-the-art GDS-enabled, SSD-backed LMCache, Tutti reduces TTFT by 78.3% under strict SLO constraints and improves the achievable request rate by 2x. The serving cost is reduced by 27%. Tutti achieves nearly the same inference performance as DRAM-backed LMCache, while providing almost infinite capacity.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Tutti** 的系统，旨在解决长上下文大语言模型（LLM）推理中 KV 缓存（KV Cache）存储容量与性能之间的矛盾。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：长上下文 LLM 推理需要巨大的 KV 缓存空间。GPU 显存（HBM）和系统内存（DRAM）容量有限且昂贵，而 NVMe SSD 提供了海量且廉价的存储空间。
*   **核心问题**：现有的 SSD 卸载方案（如 LMCache）是“以 CPU 为中心”的。由于 GPU 内存布局碎片化，从 SSD 恢复 KV 缓存会产生大量微小的随机 I/O。即使使用 NVIDIA GDS 技术，I/O 的发起和控制仍需 CPU 干预，导致严重的 CPU 瓶颈和高达 70%-80% 的 GPU 停顿（Stalls），使得 KV 缓存重用甚至慢于重新计算。
*   **目标**：消除 I/O 路径中的 CPU 干预，实现以 GPU 为中心的高效 SSD KV 缓存管理。

### 2. 方法论
Tutti 的核心思想是将数据平面和 I/O 控制平面全部移交给 GPU。
*   **GPU 原生对象抽象**：
    *   将 KV 缓存块映射为“对象”，建立 GPU 文件池和 NVMe 文件池。
    *   使用 **SGL（分散-聚集列表）** 代替传统的 PRP（物理区域页）来描述内存地址，显著减少了元数据占用的显存空间（从 GB 级降至 MB 级）。
*   **GPU io_uring (gio_uring)**：
    *   在 GPU 上模拟 Linux 的 `io_uring` 机制，通过 HBM 中的无锁环形队列（SQ/CQ）实现异步 I/O。
    *   **SM 分区**：利用 NVIDIA Green Context 技术将 GPU 资源划分为“计算域”和“I/O 控制域”，确保 I/O 内核与计算内核并行执行且互不干扰。
*   **松弛感知（Slack-aware）I/O 调度**：
    *   通过离线分析（Profiling）确定模型推理各层中的“松弛窗口”（即计算负载较低、SM 资源有剩余的时间段）。
    *   调度器根据分析结果，在松弛窗口内插入 I/O 请求，并分离读写操作以避免 SSD 内部缓存竞争导致的带宽下降。

### 3. 实验设计
*   **数据集/场景**：使用了 **LEval** 和 **LooGLE** 两个长上下文基准测试集，涵盖法律、金融、科技等领域的长文档理解任务。
*   **模型**：Llama3-8B（单卡）和 GLM-4-9B-Chat-1M（双卡张量并行）。
*   **对比方法（Baselines）**：
    1.  **HBM**：标准 vLLM（仅显存）。
    2.  **LMCache-DRAM**：基于内存的层级缓存。
    3.  **LMCache-SSD**：标准 SSD 卸载。
    4.  **LMCache-GDS**：启用 GPU Direct Storage 的 SSD 卸载。
*   **集成对象**：将 Tutti 集成到了主流推理引擎 **vLLM** 中。

### 4. 资源与算力
*   **硬件环境**：
    *   **GPU**：2x NVIDIA H100 (80GB HBM)。
    *   **CPU**：64 核 Intel Xeon 6530。
    *   **内存**：512GB DRAM。
    *   **存储**：4x Solidigm D7-PS1010 7.68TB 企业级 NVMe SSD（单盘读带宽可达 14.5GB/s）。
*   **训练/推理时长**：论文未提及模型训练，主要关注推理服务阶段的延迟和吞吐量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了端到端延迟测试（TTFT 和 ITL）、不同请求率下的系统稳定性测试、原始带宽微基准测试、消融实验（SGL vs PRP、有无调度器等）以及多 GPU 扩展性测试。
*   **充分性**：实验设计较为全面，对比了不同版本的 vLLM 引擎以证明其通用性。通过消融实验清晰地展示了每个技术组件的贡献。实验在真实的高性能硬件环境下进行，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：相比最先进的 LMCache-GDS，Tutti 将首字延迟（TTFT）降低了 **78.3%**。
*   **吞吐量**：在满足 SLO（服务水平目标）的前提下，Tutti 支持的请求率提升了 **2 倍**。
*   **成本降低**：由于能够高效利用廉价 SSD 替代昂贵 DRAM，服务成本降低了 **27%**。
*   **核心发现**：Tutti 使基于 SSD 的 KV 缓存性能接近基于 DRAM 的方案，同时提供了近乎无限的存储容量。

### 7. 优点
*   **架构创新**：首次实现了完全以 GPU 为中心的 SSD KV 缓存系统，彻底移除了 CPU 在关键路径上的干扰。
*   **软硬结合**：巧妙利用了 NVIDIA 的硬件特性（如 Green Context）和现代存储接口思想（io_uring）。
*   **实用性强**：直接集成于 vLLM，且解决了 GDS 在长序列下因 Staging Buffer 导致的 OOM（显存溢出）问题。

### 8. 不足与局限
*   **硬件依赖**：依赖于较新的 NVIDIA GPU 特性（如 Green Context）和高性能 NVMe SSD，在旧硬件或消费级硬件上的表现可能受限。
*   **离线分析开销**：松弛感知调度需要针对特定模型和硬件进行预先的离线 Profile，虽然只需一次，但增加了部署的复杂性。
*   **分布式扩展**：虽然提到了多卡支持，但在超大规模集群（跨节点）环境下的 KV 缓存一致性和网络传输开销尚未深入探讨。

（完）
