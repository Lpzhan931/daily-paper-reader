---
title: Exploring the Efficiency of 3D-Stacked AI Chip Architecture for LLM Inference with Voxel
title_zh: 使用 Voxel 探索用于大语言模型推理的 3D 堆叠 AI 芯片架构的效率
authors: "Yiqi Liu, Noelle Crawford, Michael Wang, Jilong Xue, Jian Huang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26821v1"
tags: ["query:llm"]
score: 8.0
evidence: 用于LLM推理的3D堆叠AI芯片架构
tldr: 针对大语言模型推理中的内存瓶颈，本文提出了Voxel，一个快速且感知编译器的端到端模拟框架，用于探索3D堆叠AI芯片的效率。该框架支持软硬件协同探索，允许编译器定制模型执行计划。通过在真实芯片上验证，研究深入分析了计算范式、映射策略及硬件参数对性能的影响，揭示了切片到核心及内存库的映射对端到端效率的关键作用，为3D堆叠芯片生态发展提供了重要见解。
selection_source: fresh_fetch
motivation: 旨在解决AI芯片内存瓶颈问题，并克服3D堆叠架构因分布式特性导致的效率探索与软硬件协同设计难题。
method: 开发了Voxel模拟框架，通过编程接口实现编译器感知的软硬件协同探索，并对多种硬件参数和映射策略进行全面评估。
result: 实验表明，3D堆叠芯片的端到端效率不仅受硬件规格影响，还高度依赖于计算切片到AI核心及DRAM Bank的具体映射方式。
conclusion: Voxel框架为3D堆叠AI芯片的设计提供了有效工具，强调了优化映射策略是提升LLM推理性能的关键。
---

## 摘要
为了克服 AI 芯片众所周知的内存瓶颈，采用具有高密度硅通孔（TSV）引脚的先进封装技术的 3D 堆叠架构已被证明是一种极具前景的解决方案。3D 堆叠 AI 芯片通过以分布式方式在多个 AI 核心之上堆叠大量 DRAM 库（banks），实现了计算与内存之间的超高内存带宽。然而，由于其独特的分布式特性，探索 3D 堆叠 AI 芯片的效率并非易事。我们需要仔细考虑从上层计算范式到机器学习（ML）编译器优化，再到底层硬件架构的多个相互交织的因素。在本文中，我们开发了 Voxel，这是一个快速且感知编译器的端到端仿真框架，旨在促进对用于大语言模型（LLM）推理的 3D 堆叠 AI 芯片效率的探索。Voxel 通过采用允许 ML 编译器自定义模型执行计划的编程接口，实现了软硬件协同探索。在使用真实芯片上的仿真器验证 Voxel 的结果后，我们深入研究了 3D 堆叠 AI 芯片不同方面的影响和相关性，包括最先进的计算范式、分块到核心（tile-to-core）映射、张量到库（tensor-to-bank）映射、片上网络（NoC）拓扑和链路带宽、DRAM 库带宽、单核心 SRAM 容量以及功耗/散热约束。我们的研究结果表明，3D 堆叠 AI 芯片的端到端效率不仅取决于这些因素的协同作用，还显著取决于从分块到 AI 核心及 DRAM 库的映射。我们在全文中报告了我们的发现，期望它们能为 3D 堆叠 AI 芯片生态系统的发展提供启示。我们将开源 Voxel 及其研究结果以供公共研究。

## Abstract
To overcome the well-known memory bottleneck of AI chips, 3D stacked architectures that employ advanced packaging technology with high-density through-silicon vias (TSVs) pins have proven to be a promising solution. The 3D-stacked AI chip enables ultra-high memory bandwidth between compute and memory by stacking numerous DRAM banks atop many AI cores in a distributed manner. However, it is not easy to explore the efficiency of the 3D-stacked AI chip, due to its unique distributed nature. And we need to carefully consider multiple intertwined factors that range from upper-level computing paradigm to machine learning (ML) compiler optimizations, and to the underlying hardware architecture.   In this paper, we develop Voxel, a fast and compiler-aware end-to-end simulation framework to facilitate exploring the efficiency of 3D-stacked AI chips for large language model (LLM) inference. Voxel enables the software/hardware co-exploration by employing a programming interface that allows ML compilers to customize the model execution plans. After validating the results of Voxel with an emulator on real silicon, we thoroughly examine the impact and correlation of different aspects of 3D-stacked AI chips, including state-of-the-art compute paradigms, tile-to-core mapping, tensor-to-bank mapping, NoC topologies and link bandwidth, DRAM bank bandwidth, per-core SRAM capacity, and energy/thermal constraints. Our findings disclose that the end-to-end efficiency of a 3D stacked AI chip not only is determined by the cooperative function of these factors, but also significantly depends on the mappings from tiles to AI core and DRAM banks. We report our findings throughout the paper, with the expectation that they will shed light on the development of the 3D-stacked AI chip ecosystem. We will open source Voxel and our study results for public research.

---

## 论文详细总结（自动生成）

以下是对论文《Exploring the Efficiency of 3D-Stacked AI Chip Architecture for LLM Inference with Voxel》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）推理面临严重的“内存墙”瓶颈。虽然 3D 堆叠技术（通过硅通孔 TSV 将 DRAM 库直接堆叠在 AI 核心之上）能提供超高带宽，但其**分布式架构**带来了复杂的软硬件协同挑战。
*   **研究动机**：现有的模拟器要么针对传统 CPU，要么假设内存是统一访问的（2.5D 架构），无法准确模拟 3D 堆叠芯片中分布式 DRAM 带来的行缓冲冲突（Row-buffer conflict）、片上网络（NoC）竞争以及热量约束。
*   **整体含义**：本文旨在通过开发一个名为 **Voxel** 的仿真框架，探索如何通过编译器优化和硬件设计协同提升 3D 堆叠 AI 芯片的 LLM 推理效率。

### 2. 论文提出的方法论
*   **核心思想**：构建一个**感知编译器（Compiler-aware）**的端到端仿真框架，允许软件层自定义执行计划，并结合硬件层的事件驱动模拟。
*   **关键技术细节**：
    *   **软件接口**：提供 `compute()`、`copy_data()` 和 `sync()` 等 API，让编译器能指定分块（Tile）到核心的映射、张量到内存库的映射以及计算范式。
    *   **执行图生成**：将编译器决策转化为包含数据依赖的执行图，追踪核心、DRAM 和 NoC 的状态。
    *   **快速仿真技术（Coalescing）**：针对 LLM 重复的算子模式，通过 XOR 位运算生成匹配键（Match Keys），识别并复用结构相同的 DRAM 访问轨迹，将仿真速度提升了约 1100 倍。
    *   **热量建模**：根据各组件的功率密度动态调整 AI 核心频率，确保芯片运行在安全温度（<85°C）内。
    *   **硬件优化建议**：提出了“核心组（Core Group）”技术，通过硬件请求追踪器同步邻近核心的内存访问，减少行缓冲冲突。

### 3. 实验设计
*   **测试模型**：Llama2-13B、Gemma2-27B、OPT-30B、Llama3-70B 以及视觉 Transformer 模型 DiT-XL。
*   **实验场景**：涵盖 LLM 推理的 Prefill（预填充，计算密集型）和 Decode（解码，内存密集型）两个阶段。
*   **对比方法/范式**：
    *   **计算范式**：SPMD（单程序多数据）、Dataflow（数据流）、Compute-shift（计算偏移）。
    *   **映射策略**：顺序映射 vs. 维度优先映射（Dimension-ordered）。
    *   **NoC 拓扑**：2D Mesh、2D Torus、All-to-all。
    *   **内存映射**：均匀放置、启发式交错放置、软件感知放置。
*   **Benchmark 验证**：使用 Graphcore IPU Mk2 芯片构建仿真器，验证 Voxel 的准确性。

### 4. 资源与算力
*   **仿真环境**：在一台 64 线程的现代服务器上运行。
*   **验证硬件**：使用了 **Graphcore IPU Mk2** 芯片（包含 1472 个核心和 896MB SRAM），利用其分布式 SRAM 模拟分布式 DRAM 的访问行为。
*   **算力说明**：文中未提及训练 LLM 所需的算力，因为本文侧重于推理阶段的架构仿真。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验和参数搜索，包括：
    *   不同计算范式的性能对比。
    *   NoC 拓扑与映射策略的组合实验。
    *   DRAM 带宽、核心数量、SRAM 大小、脉动阵列（Systolic Array）尺寸的缩放实验。
    *   帕累托前沿（Pareto frontier）分析，寻找面积与性能的最优平衡。
*   **充分性评价**：实验设计非常全面，不仅涵盖了软件层面的编译器策略，还深入到了硬件底层的电路参数（如 DRAM 定时参数、功率密度等），并通过真实芯片验证了仿真误差（<6.8%），具有较高的客观性和说服力。

### 6. 论文的主要结论与发现
*   **计算范式**：Compute-shift 表现最优，能最大化计算、通信和内存访问的重叠，性能差异可达 1.84 倍。
*   **映射策略**：维度优先映射配合 Mesh 拓扑即可达到接近最优的性能，无需昂贵的 All-to-all 拓扑。
*   **内存瓶颈**：在 3D 架构中，单纯增加带宽会因行缓冲冲突导致收益递减。**软件感知的张量放置**可减少 80.7% 的冲突开销。
*   **核心缩放**：盲目增加核心数会加剧内存冲突。引入“核心组”同步机制后，1024 核芯片的性能可提升 58%。
*   **能效比**：增加带宽能显著提升 Decode 阶段的能效（缩短运行时间减少静态功耗），但对 Prefill 阶段影响较小。

### 7. 优点
*   **软硬件协同**：打破了传统模拟器只看硬件或只看软件的局限，提供了编译器级别的控制接口。
*   **仿真效率高**：创新的轨迹复用技术解决了大规模分布式架构仿真过慢的问题。
*   **真实性验证**：在没有商用 3D AI 芯片的情况下，巧妙利用 Graphcore IPU 进行交叉验证，增强了结果的可信度。

### 8. 不足与局限
*   **热量模型简化**：采用的是基于功率密度的线性频率调整，未考虑复杂的空间-瞬态热传导网络。
*   **架构假设**：主要针对特定的“核心-NoC-DRAM”三层堆叠架构，对于更复杂的异构堆叠（如逻辑层与逻辑层堆叠）探讨较少。
*   **应用限制**：目前仅针对 Transformer 类模型进行了深度优化，对于稀疏模型或非规则计算模式的支持尚待验证。

（完）
