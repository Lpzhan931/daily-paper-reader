---
title: "Tandem: Riding Together with Large and Small Language Models for Efficient Reasoning"
title_zh: Tandem：大模型与小模型协同实现高效推理
authors: "Zichuan Fu, Xian Wu, Guojing Li, Yejing Wang, Yijun Chen, Zihao Zhao, Yixuan Luo, Hanyu Yan, Yefeng Zheng, Xiangyu Zhao"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23623v1"
tags: ["query:llm"]
score: 8.0
evidence: 大模型与小模型协作框架，降低推理计算成本
tldr: "本研究针对大语言模型在复杂推理任务中计算开销巨大的挑战，提出了Tandem协作框架。该框架由LLM担任协调者生成关键推理见解，并引导更高效的小模型（SLM）执行完整推理过程。通过引入成本感知的终止机制，Tandem在数学推理和代码生成任务中将计算成本降低了约40%，同时保持了与独立LLM相当甚至更优的性能，实现了推理效率与质量的有效平衡。"
selection_source: fresh_fetch
motivation: 现有的链式思考推理范式虽然提升了模型性能，但冗长的生成序列导致了极高的计算成本和推理延迟。
method: 提出一种大小模型协同框架，利用LLM生成关键见解并引导SLM执行推理，同时引入成本感知机制实现LLM的提前停止。
result: "在数学推理和代码生成基准测试中，该方法在保持竞争力的性能下，比独立LLM推理减少了约40%的计算开销。"
conclusion: Tandem框架证明了大小模型协同能有效平衡推理效率与准确性，且其训练的终止分类器具有跨领域的迁移能力。
---

## 摘要
近期大语言模型（LLM）的进展催生了推理密集型推理范式的兴起，模型在生成最终答案之前会进行显式的逐步推理。虽然此类方法提高了答案质量和可解释性，但由于生成序列过长，导致了巨大的计算开销。在本文中，我们提出了 Tandem，这是一种新型协作框架，通过协同大语言模型和小语言模型（LLM 和 SLM），在显著降低计算成本的同时实现高质量推理。具体而言，LLM 作为战略协调者，高效地生成一组紧凑的关键推理见解。随后，这些见解被用于引导更小、更高效的 SLM 执行完整的推理过程并给出最终响应。为了平衡效率和可靠性，Tandem 引入了一种成本感知终止机制，能够自适应地确定何时已积累足够的推理引导，从而实现 LLM 生成的提前停止。在数学推理和代码生成基准测试上的实验表明，与独立 LLM 推理相比，Tandem 将计算成本降低了约 40%，同时实现了更优或具有竞争力的性能。此外，在某一领域训练的充分性分类器可以有效地迁移到其他领域，而无需重新训练。代码可在以下地址获取：https://github.com/Applied-Machine-Learning-Lab/ACL2026_Tandem。

## Abstract
Recent advancements in large language models (LLMs) have catalyzed the rise of reasoning-intensive inference paradigms, where models perform explicit step-by-step reasoning before generating final answers. While such approaches improve answer quality and interpretability, they incur substantial computational overhead due to the prolonged generation sequences. In this paper, we propose Tandem, a novel collaborative framework that synergizes large and small language models (LLMs and SLMs) to achieve high-quality reasoning with significantly reduced computational cost. Specifically, the LLM serves as a strategic coordinator, efficiently generating a compact set of critical reasoning insights. These insights are then used to guide a smaller, more efficient SLM in executing the full reasoning process and delivering the final response. To balance efficiency and reliability, Tandem introduces a cost-aware termination mechanism that adaptively determines when sufficient reasoning guidance has been accumulated, enabling early stopping of the LLM's generation. Experiments on mathematical reasoning and code generation benchmarks demonstrate that Tandem reduces computational costs by approximately 40% compared to standalone LLM reasoning, while achieving superior or competitive performance. Furthermore, the sufficiency classifier trained on one domain transfers effectively to others without retraining. The code is available at: https://github.com/Applied-Machine-Learning-Lab/ACL2026_Tandem.

---

## 论文详细总结（自动生成）

### 论文总结：Tandem——大模型与小模型协同实现高效推理

#### 1. 核心问题与整体含义（研究动机和背景）
随着 DeepSeek-R1 等推理型大语言模型（LLM）的兴起，模型通过生成冗长的思维链（CoT）显著提升了复杂问题的解决能力。然而，这种“思考”范式带来了巨大的**计算开销和推理延迟**：推理链往往长达数千个 Token，是普通输出的 5-10 倍。
论文的核心问题在于：**如何在保留显式推理优势的同时，大幅降低计算成本？** 
研究者提出了一种“导师-实习生”（Mentor-Intern）协作模式，让 LLM 负责高层战略规划，而让更轻量的小模型（SLM）负责具体的逻辑执行。

#### 2. 方法论：核心思想与关键技术
Tandem 框架的核心思想是将推理过程解耦，通过 LLM 生成精简的“推理见解”来引导 SLM。

*   **推理见解生成（Thinking Insight Generation）**：
    LLM 不生成完整的推理链，而是生成四个结构化模块：
    1.  **Goal（目标）**：明确任务目标和约束。
    2.  **Planning（规划）**：制定高层解题策略。
    3.  **Retrieval（检索）**：提取相关的公式、定理或知识。
    4.  **Action（行动）**：执行具体的计算或逻辑步骤。
*   **成本感知持续判断（Cost-Aware Continual Judgment）**：
    为了避免过度引导，Tandem 将 LLM 的生成分为低、中、高三个努力水平（阶段）。
    *   **特征提取**：SLM 处理当前见解时，提取 Token 级别的**困惑度（PPL）**和**熵（Entropy）**的统计特征（均值、标准差、趋势等）。
    *   **充分性分类器**：使用一个多层感知机（MLP）作为分类器，根据上述特征判断当前的引导是否足以让 SLM 独立完成任务。
*   **响应完成（Response Completion）**：
    一旦分类器判定“充分”，LLM 立即停止生成（提前停止），由 SLM 接管并输出最终答案。

#### 3. 实验设计
*   **数据集**：
    *   数学推理：**MATH**（包含 7 个学科、5 个难度等级）、**GSM8K**。
    *   代码生成：**HumanEval**（用于验证跨领域迁移能力）。
*   **Benchmark 与对比方法**：
    *   **独立模型**：单独运行 7B 或 32B 模型。
    *   **固定长度引导**：LLM 固定生成 100/500/1000 Token 后转交给 SLM。
    *   **效率基准**：**Budget Forcing**（强制截断推理链）和 **LLM Cascade**（简单的二元路由选择）。
*   **模型组合**：包括 DeepSeek-R1 (7B/32B)、Qwen3 (8B/32B) 以及 API 模型（GPT-4o-mini, gpt-oss-120b）。

#### 4. 资源与算力
*   **算力说明**：论文使用了 **vLLM** 推理框架进行部署和延迟测量。
*   **具体配置**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及数量，也未提及分类器训练的具体总时长。但提供了计算成本的估算公式（基于参数量和 Token 数转换的 TFLOPs）。

#### 5. 实验数量与充分性
*   **实验规模**：实验设计非常详尽，涵盖了从 1.5B 到 32B 的多种模型尺寸组合，并进行了跨模型家族（DeepSeek 与 Qwen 互配）的验证。
*   **充分性与客观性**：
    *   进行了**消融实验**，对比了基于统计特征、隐藏状态及 LoRA 微调的分类效果。
    *   进行了**跨领域验证**，将在 MATH 上训练的分类器直接应用于 HumanEval，证明了特征的通用性。
    *   分析了不同难度题目下的阶段分布，结果符合直觉（难题更多流向高努力水平阶段），体现了实验的客观性。

#### 6. 主要结论与发现
*   **性能超越单体大模型**：在 MATH 数据集上，32B+7B 的 Tandem 组合比独立 32B 模型准确率高出 **2.56%**。
*   **大幅降低成本**：在提升性能的同时，计算成本（TFLOPs）降低了约 **40%**。
*   **推理加速**：实测端到端延迟实现了 **1.8 倍** 的加速。
*   **能力对齐效应**：协作效果取决于模型间的差距。如果 SLM 太弱（如 1.5B），则无法有效利用 LLM 的高层见解。
*   **分类器通用性**：基于 PPL 和熵的分类器具有很强的跨领域迁移能力，无需针对新任务重新训练。

#### 7. 优点
*   **结构化引导**：通过 Goal/Planning/Retrieval/Action 模块化推理，比原始 CoT 更精炼且易于小模型理解。
*   **动态自适应**：引入成本感知机制，实现了推理深度与计算成本的自动平衡。
*   **兼容性强**：支持闭源 API 模型作为“导师”，具有很强的实际落地价值。
*   **训练成本极低**：分类器仅为轻量级 MLP，训练和推理开销几乎可以忽略不计。

#### 8. 不足与局限
*   **分类器依赖标注**：虽然分类器可迁移，但初始训练仍需要一定规模的“引导-结果”标注数据。
*   **领域覆盖有限**：目前主要集中在数学和代码等逻辑严密的任务，在常识推理或开放式问答上的表现尚待验证。
*   **协作模式单一**：目前仅限于“一师一徒”模式，未探索多模型协作或更复杂的动态角色切换。
*   **潜在偏差**：如果 LLM 在早期阶段提供了错误的见解，SLM 可能会被误导且难以纠正。

（完）
