---
title: "ReST-KV: Robust KV Cache Eviction with Layer-wise Output Reconstruction and Spatial-Temporal Smoothing"
title_zh: ReST-KV：基于逐层输出重构与时空平滑的鲁棒 KV 缓存剔除方法
authors: "Yongqi An, Chang Lu, Kuan Zhu, Tao Yu, Chaoyang Zhao, Hong Wu, Ming Tang, Jinqiao Wang"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08840v1"
tags: ["query:llm"]
score: 9.0
evidence: 基于输出重构的鲁棒KV Cache剔除
tldr: 针对大语言模型长文本推理中KV缓存带来的内存压力，本文提出ReST-KV。该方法通过逐层输出重构来最小化Token移除后的输出偏差，并结合时空平滑机制（EMA和自适应窗口）增强鲁棒性。实验表明，ReST-KV在LongBench和RULER等基准测试上显著优于现有方法，并在128k长度下实现10.61倍的解码加速。
selection_source: fresh_fetch
motivation: 现有的KV缓存剔除方法仅依赖注意力权重，忽略了Token移除后的注意力重分布效应及KV选择的时空动态性。
method: 提出一种结合逐层输出重构优化与时空平滑（指数移动平均和自适应窗口）的鲁棒KV剔除框架。
result: "在LongBench和RULER上分别提升2.58%和15.2%的性能，并在128k上下文长度下将解码延迟降低了10.61倍。"
conclusion: ReST-KV通过更全面地建模Token移除影响，为长文本高效推理提供了一种兼顾性能与效率的KV缓存管理方案。
---

## 摘要
由于 Key-Value (KV) 缓存日益增长的内存需求，大语言模型 (LLMs) 在高效生成式推理方面面临着严峻挑战，尤其是在处理长序列时。现有的剔除方法通常保留具有高注意力权重的 KV 对，但忽略了 Token 移除导致的注意力重分布影响，以及 KV 选择中的时空动态特性。在本文中，我们提出了 ReST-KV，这是一种鲁棒的 KV 剔除方法，它结合了逐层输出重构和时空平滑，为 KV 缓存剔除任务提供了一个更全面的视角。具体而言，ReST-KV 将 KV 缓存剔除建模为一个优化问题，通过高效的逐层重构来最小化输出差异。通过直接建模每个 Token 的移除如何影响模型输出，我们的方法自然地捕捉到了注意力重分布效应，超越了对原始注意力权重的简单依赖。为了进一步增强鲁棒性，我们设计了指数移动平均平滑来处理时间变化，并采用自适应窗口机制来捕捉空间模式。我们的方法 ReST-KV 在长上下文基准测试中显著提升了性能，在 LongBench 上超过了现有最先进基准 2.58%，在 RULER 上超过了 15.2%。此外，ReST-KV 在 Needle-in-a-Haystack 和 InfiniteBench 上始终优于现有方法，同时在 128k 上下文长度下实现了显著的 10.61 倍解码延迟降低。代码已在 https://github.com/an-yongqi/rest-kv 开源，以促进可复现性和进一步研究。

## Abstract
Large language models (LLMs) face growing challenges in efficient generative inference due to the increasing memory demands of Key-Value (KV) caches, especially for long sequences. Existing eviction methods typically retain KV pairs with high attention weights but overlook the impact of attention redistribution caused by token removal, as well as the spatial-temporal dynamics in KV selection. In this paper, we propose ReST-KV, a robust KV eviction method that combines layer-wise output Reconstruction and Spatial-Temporal smoothing to provide a more comprehensive perspective for the KV cache eviction task. Specifically, ReST-KV formulates KV cache eviction as an optimization problem that minimizes output discrepancies through efficient layer-wise reconstruction. By directly modeling how each token's removal affects the model output, our method naturally captures attention redistribution effects, going beyond simplistic reliance on raw attention weights. To further enhance robustness, we design exponential moving average smoothing to handle temporal variations and an adaptive window-based mechanism to capture spatial patterns. Our method, ReST-KV, significantly advances performance on long-context benchmarks. It surpasses state-of-the-art baselines by 2.58% on LongBench and 15.2% on RULER. Additionally, ReST-KV consistently outperforms existing methods on Needle-in-a-Haystack and InfiniteBench, all while achieving a remarkable 10.61$\times$ reduction in decoding latency at 128k context length. The code is publicly available at https://github.com/an-yongqi/rest-kv to facilitate reproducibility and further research.

---

## 论文详细总结（自动生成）

以下是对论文《ReST-KV: Robust KV Cache Eviction with Layer-wise Output Reconstruction and Spatial-Temporal Smoothing》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLMs）在处理长文本推理时，Key-Value (KV) 缓存会消耗巨大的显存。现有的 KV 缓存剔除（Eviction）方法（如 H2O, SnapKV）主要依赖原始注意力权重来筛选 Token，但存在两个缺陷：
    1.  **忽略注意力重分布效应**：移除某个 Token 后，剩余 Token 的注意力权重会发生变化，仅靠原始权重无法准确衡量 Token 对最终输出的贡献。
    2.  **忽略时空动态性**：KV Token 的重要性在时间（不同解码步骤）和空间（序列位置）上是动态变化的。
*   **研究动机**：提出一种更鲁棒的剔除机制，通过重构层输出并结合时空平滑技术，在极低缓存预算下保持模型性能。

### 2. 方法论
ReST-KV 的核心思想是将 KV 剔除建模为一个**最小化层输出重构误差**的优化问题。
*   **逐层输出重构指标 (LOR)**：
    *   不再只看注意力权重 $A$，而是计算移除第 $n$ 个 KV 对后，多头注意力（MHA）输出的 $L_2$ 距离变化。
    *   推导出的重要性指标公式包含两部分：一个是非线性放大器（基于注意力权重），另一部分是重构敏感度（衡量剩余 KV 对补偿缺失信息的能力）。
*   **时空平滑机制**：
    *   **时间平滑 (EMA)**：引入指数移动平均，给予近期查询更高的权重，以平滑重要性指标的瞬时波动。
    *   **自适应窗口空间平滑 (AWS)**：针对注意力模式中的“空间偏移”（如斜线模式），通过计算重要性分布的偏移量，动态调整滑动窗口的大小和偏移位置。
*   **流程**：在 Prefill（预填充）阶段计算该指标，筛选出最重要的 Top-B 个 KV 对保留，随后在解码阶段使用压缩后的缓存。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **LongBench**：涵盖单/多文档问答、摘要、few-shot 等 16 个任务。
    *   **RULER**：测试不同长度（4k 到 128k）下的复杂检索和推理能力。
    *   **Needle-in-a-Haystack**（大海捞针）：测试长文本信息检索。
    *   **InfiniteBench**：测试超过 100k 甚至达到百万级别的超长上下文。
*   **对比方法**：
    *   固定模式：StreamingLLM。
    *   动态指标：H2O, TOVA, SnapKV, LaCache。
    *   预算分配策略：PyramidKV, AdaKV。
*   **模型覆盖**：Llama2 (7B/13B), Llama3/3.1 (8B/70B), Mistral-7B, Qwen2.5-7B, Gemma-7B。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA A800 80GB GPU** 上运行。
*   **效率表现**：
    *   在 128k 上下文长度下，相比全量缓存，解码延迟降低了 **10.61 倍**。
    *   显存占用减少约 **36.0%**。
    *   预填充阶段的额外开销极低，仅比 SnapKV 增加约 **2%** 的 TTFT（首字延迟）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 种主流架构、多种参数规模（7B 到 70B）的模型上进行了测试，涵盖了从 4k 到 2000k 的上下文长度。
*   **消融实验**：详细验证了 LOR 指标、EMA 时间平滑和 AWS 空间平滑各自的贡献，并进行了超参数（如平滑因子 $\alpha$、缩放因子 $\beta$）的敏感性分析。
*   **充分性评价**：实验设计非常充分，不仅对比了准确率，还测试了吞吐量、延迟、显存占用，并验证了与 KV 量化（KIVI, KVQuant）及稀疏预填充（MInference）的兼容性，结果具有较强的说服力。

### 6. 主要结论
*   **性能领先**：ReST-KV 在 LongBench 上平均提升 2.58%，在 RULER 上平均提升 15.2%。
*   **极端压缩下的鲁棒性**：在 RULER 128k 测试中（仅保留不到 1% 的缓存），ReST-KV 依然保持了较强的检索能力。
*   **高效性**：成功解决了长序列推理中的内存瓶颈，实现了显著的加速。
*   **通用性**：该方法不依赖特定模型架构，且能与现有的层级/头级预算分配策略完美结合。

### 7. 优点
*   **理论严谨**：从输出重构的角度重新定义 KV 重要性，比单纯依赖注意力权重更具物理意义。
*   **时空感知**：AWS 机制能捕捉到注意力机制中复杂的空间偏移模式，这是此前多数方法忽略的。
*   **高兼容性**：可以作为插件集成到现有的推理加速框架中，支持量化和稀疏注意力。

### 8. 不足与局限
*   **预填充依赖**：目前的剔除主要发生在预填充阶段（Prefill-only），虽然文中提到可以扩展到解码阶段，但主要实验仍集中在一次性剔除。
*   **超参数依赖**：虽然敏感性分析显示 $\alpha$ 和 $\beta$ 相对稳定，但在极端不同的任务场景下，可能仍需微调以达到最优性能。
*   **计算复杂度**：虽然预填充开销仅增加 2%，但在对实时性要求极高的极短文本场景下，这种计算重构误差的开销可能变得敏感。

（完）
