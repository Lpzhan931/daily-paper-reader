---
title: "Make Each Token Count: Towards Improving Long-Context Performance with KV Cache Eviction"
title_zh: 让每个 Token 都有价值：通过 KV 缓存剔除提升长上下文性能
authors: "Ngoc Bui, Hieu Trung Nguyen, Arman Cohan, Rex Ying"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09649v1"
tags: ["query:llm"]
score: 8.0
evidence: 基于全局保留的长上下文推理 KV 剔除
tldr: 本研究针对长文本推理中KV缓存带来的内存与计算瓶颈，提出了一种基于全局保留的KV驱逐方法。作者发现全量缓存并非最优，无关Token会稀释注意力，因此通过轻量级保留门控学习Token的未来效用，并利用统一评分投影实现跨层、跨头的全局缓存竞争。该方法不仅显著降低了内存消耗，还在多项长文本和多模态任务中超越了全量缓存的性能，证明了选择性驱逐能有效提升推理质量。
selection_source: fresh_fetch
motivation: 现有的KV缓存驱逐方法往往导致性能下降，且全量缓存中的无关Token会稀释注意力，干扰模型对关键信息的提取。
method: 引入一种全局保留机制，利用轻量级门控和统一评分投影来预测Token的未来效用，使不同层、头和模态的Token在统一预算下竞争缓存空间。
result: 在长文本语言、视觉语言推理及多轮对话等多个基准测试中，该方法在大幅减少内存占用的同时，性能匹配甚至优于全量缓存推理。
conclusion: 经过学习和全局校准的KV驱逐不仅是一种有效的压缩技术，更是通过减少注意力稀释来增强长文本推理能力的机制。
---

## 摘要
键值（KV）缓存是长上下文推理中的主要瓶颈，其内存和计算开销随序列长度增加而增长。现有的 KV 剔除方法降低了这一成本，但通常会使性能低于全缓存推理。我们的核心见解是，全缓存注意力并不总是最优的：在长上下文中，无关的 token 可能会稀释对有用证据的注意力，因此选择性的、可学习的剔除可以改善生成效果，而不仅仅是逼近全缓存。我们提出了一种基于全局保留（retention）的 KV 剔除方法，在统一的内存预算下学习每个 token 的未来效用。轻量级的保留门控为缓存的 KV 条目分配效用分数，而共享的最终评分投影则在所有层和头之间校准这些分数。这实现了一种单一的全局剔除策略，使来自不同层、头和模态的 token 直接竞争缓存容量。我们进一步提供了理论分析，表明优先保留有用的 token 可以减少注意力稀释，并证明了几何保留（geometric retention）作为未来效用的查询无关代理的合理性。在多种长上下文语言和视觉语言推理以及多轮对话基准测试中，我们的方法在大幅减少 KV 内存的同时，达到或超过了全缓存推理的性能。这些结果表明，经过学习和全局校准的 KV 剔除不仅是一种压缩技术，也是一种提升长上下文推理能力的机制。

## Abstract
The key-value (KV) cache is a major bottleneck in long-context inference, where memory and computation grow with sequence length. Existing KV eviction methods reduce this cost but typically degrade performance relative to full-cache inference. Our key insight is that full-cache attention is not always optimal: in long contexts, irrelevant tokens can dilute attention away from useful evidence, so selective, learnable eviction can improve generation rather than merely approximate the full cache. We introduce a global retention-based KV eviction method that learns each token's future utility under a unified memory budget. Lightweight retention gates assign utility scores to cached KV entries, and a shared final scoring projection calibrates these scores across all layers and heads. This enables a single global eviction policy in which tokens from different layers, heads, and modalities compete directly for cache capacity. We further provide theoretical analysis showing that preferentially retaining useful tokens reduces attention dilution, and we justify geometric retention as a query-agnostic proxy for future utility. Across diverse long-context language and vision-language reasoning, and multi-turn dialogue benchmarks, our method substantially reduces KV memory while matching or surpassing full-cache inference. These results suggest that learned, globally calibrated KV eviction is not only a compression technique, but also a mechanism for improving long-context reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Make Each Token Count: Towards Improving Long-Context Performance with KV Cache Eviction》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心瓶颈**：在长上下文推理中，Transformer 的键值（KV）缓存随序列长度线性增长，导致内存溢出和计算延迟。
*   **现有局限**：目前的 KV 剔除（Eviction）方法多将其视为“有损压缩”，即通过牺牲模型精度来换取效率。
*   **核心见解**：作者提出“全量缓存并非最优”的观点。在长上下文中，大量无关 Token 会产生**注意力稀释（Attention Dilution）**，即 Softmax 归一化使得有用信息的权重被噪声稀释。
*   **研究目标**：开发一种可学习的、全局校准的剔除策略，不仅为了压缩，更为了通过移除“干扰项”来提升长文本推理性能。

### 2. 方法论：DBTrimKV (Dynamic Budget TrimKV)
*   **核心思想**：利用轻量级门控预测每个 Token 的“未来效用”（Future Utility），并在全局范围内（跨层、跨头、跨模态）动态分配内存预算。
*   **关键技术细节**：
    *   **几何保留（Geometric Retention）**：假设 Token 的重要性随时间呈指数衰减，通过保留因子 $\beta_i \in [0, 1]$ 建模。$\beta_i$ 越接近 1，表示该 Token 越具有长期价值。
    *   **权重共享门控（Weight-tied Gates）**：在每一层、每个头设置轻量级 MLP 预测 $\beta$。关键创新在于**共享最终的评分投影层**，确保不同层和头的分数具有可比性，从而实现全局排序。
    *   **全局评分公式**：计算 Token 在未来 $T-t$ 步的累积效用期望：$\tilde{G}_{l,h,i}(t) = \sum_{s=t+1}^{T} \beta_{l,h,i}^{s-i}$。
    *   **动态预算分配**：不再为每层/每头设置固定预算，而是将所有 KV 条目放入统一池中，保留全局评分最高的 $M_{global}$ 个条目。
    *   **系统实现**：结合 **PagedAttention** 布局，支持变长、头特定的逻辑缓存序列，解决物理存储不连续的问题。

### 3. 实验设计
*   **场景与数据集**：
    *   **短文本 VQA**：VQAText, MME, GQA（验证基础能力）。
    *   **长程推理（多模态）**：MMStar, MathVision, MMMUPro, VideoMME, VideoMathQA, VideoMMMU。
    *   **长程推理（纯文本）**：AIME24, GSM8K, MATH-500。
    *   **多轮对话**：MMDU（测试长期记忆与上下文切换）。
*   **Benchmark 与对比方法**：
    *   **启发式方法**：SnapKV, AdaKV（动态预算启发式）, Ada-Pyramid-KV, R-KV。
    *   **视觉剪枝方法**：FastV, VisionZip, CDPruner 等。
    *   **可学习方法**：TrimKV（本文方法的基准版本）, SeerAttn-R, Locret。
*   **基座模型**：Qwen3-VL-8B-Thinking, Qwen3-4B, LLaVA-1.5-7B, Phi-3-mini。

### 4. 资源与算力
*   **硬件环境**：训练使用了 **4 张 NVIDIA H100 GPU**。
*   **训练时长与数据**：
    *   训练仅针对轻量级保留门控（参数量仅占模型总量的 **0.43%**），冻结 LLM 主体。
    *   使用了混合数据集采样：M4Instruct (40%), Academic Video (30%), R1-OneVision (30%), OpenR1-Math (20%)。
    *   具体训练时长未明确标出，但由于参数量极小且只训练门控，通常在数小时至一天内。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4k 到 200k 的上下文长度，测试了从 128 到 1024 不等的极低 KV 预算。
*   **消融实验**：
    *   验证了**权重共享（Weight-tying）**的必要性（不共享会导致低预算下性能大幅下降）。
    *   对比了不同输入特征（Token Embedding vs KV Concatenation）。
    *   分析了**预测步数（Lookahead Step）**对性能的影响。
*   **公平性**：所有对比方法均在相同的全局总预算下进行，并使用了标准的 LMMs-Eval 评测框架，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **超越全量缓存**：在多个基准测试中（如 MMDU, MathVision），DBTrimKV 在仅使用少量内存的情况下，性能**超过了全量缓存（Vanilla）**。例如在 MMDU 上，512 预算下的表现比全量缓存高出 14%。
*   **缓解注意力稀释**：理论和实验均证明，剔除无关 Token 能让模型更专注于关键证据，从而提升推理准确性。
*   **动态分配优势**：全局预算分配比固定分配更有效，模型会自动在中间层或特定注意力头保留更多 Token，而在冗余层进行激进剔除。
*   **结构化锚点**：定性分析发现，模型倾向于永久保留 `<|vision_start|>` 等结构化 Token，将其作为压缩后的语义锚点。

### 7. 优点（亮点）
*   **理论支撑强**：不仅给出了算法，还通过数学证明解释了为什么剔除能减少注意力稀释。
*   **全局最优性**：打破了以往“按层/按头”分配预算的思维定式，实现了真正的全局动态竞争。
*   **多模态通用**：一套框架同时处理文本和视觉 Token，无需针对模态设计专门的启发式规则。
*   **效率与精度双赢**：在显著降低推理延迟和内存占用的同时，提升了长文本理解的质量。

### 8. 不足与局限
*   **模型规模覆盖**：实验主要集中在 4B-8B 规模的模型，尚未在 70B 或更大型的“边际效应”可能不同的模型上验证。
*   **训练依赖**：虽然门控很轻量，但仍需要针对特定模型进行微调训练，不如 SnapKV 等“即插即用”的方法便捷。
*   **Lookahead 敏感性**：实验显示 Lookahead 参数对极低预算下的表现有显著影响，但在实际应用中如何自适应选择该参数尚不明确。
*   **端到端潜力未尽**：目前仅冻结主模型训练门控，若进行端到端联合训练，可能会有更强的表现，但文中未涉及。

（完）
