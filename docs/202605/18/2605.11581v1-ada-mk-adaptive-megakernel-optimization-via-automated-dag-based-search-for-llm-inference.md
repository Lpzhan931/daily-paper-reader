---
title: "Ada-MK: Adaptive MegaKernel Optimization via Automated DAG-based Search for LLM Inference"
title_zh: Ada-MK：基于自动化 DAG 搜索的 LLM 推理自适应 MegaKernel 优化
authors: "Wenxin Dong, Mingqing Hu, Guanghui Yu, Qiang Fu, Peng Xu, Hui Xu, Yue Xing, Xuewu Jiao, Shuanglong Li, Lin Liu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11581v1"
tags: ["query:llm"]
score: 8.0
evidence: 通过算子融合优化大模型推理延迟
tldr: "针对商业广告系统中LLM推理的毫秒级延迟需求，本文提出Ada-MK。它通过MegaKernel技术融合多个算子以消除内核启动开销。Ada-MK引入了三维共享内存约束模型、基于MLIR的细粒度DAG离线搜索以及异构混合推理引擎，解决了现有方案在资源受限GPU上的移植性与效率矛盾。在NVIDIA L20上，其单批次吞吐量较TensorRT-LLM提升高达23.6%，是MegaKernel在商业广告系统中的首次工业部署。"
selection_source: fresh_fetch
motivation: 频繁的内核启动开销严重影响LLM推理的端到端延迟，而现有MegaKernel方案在资源受限GPU上存在移植性差或运行时分支开销大的问题。
method: 提出三维共享内存约束模型优化内存使用，利用MLIR进行DAG离线搜索以消除运行时分支，并构建异构混合推理引擎。
result: "在NVIDIA L20上，Ada-MK相比TensorRT-LLM和vLLM分别实现了最高23.6%和50.2%的单批次吞吐量提升。"
conclusion: Ada-MK通过自动化搜索和精细化内存管理，成功将MegaKernel应用于商业广告系统，显著降低了LLM推理延迟并提升了吞吐量。
---

## 摘要
在大规模语言模型（LLM）服务于商业在线广告系统的实时推理时，端到端延迟必须严格限制在毫秒级。然而，在解码（decode）阶段生成的每个 token 都会触发数千次算子核（kernel）启动，仅算子核启动开销就可能占端到端推理时间的 14.6%。MegaKernel 通过将多个算子融合为一个单一的持久化算子核，消除了启动开销和算子间的高带宽内存（HBM）往返。然而，现有的 MegaKernel 实现方案在 NVIDIA Ada 等资源受限的 GPU 上面临可移植性与效率之间的根本矛盾：手工调优的方案与特定架构紧密耦合且缺乏可移植性，而自动编译的方法引入了运行时动态调度，其分支开销在延迟敏感的场景下是不可接受的。我们观察到，在固定的部署配置下，MegaKernel 的最优执行路径是唯一确定的，运行时的动态决策可以完全提前到编译阶段。基于这一洞察，我们提出了 Ada-MK：(1) 一种结合 K 维度拆分的三维共享内存约束模型，将峰值共享内存占用降低了 50%；(2) 基于 MLIR 的细粒度 DAG 离线搜索，固化最优执行路径，完全消除了运行时分支；(3) 一种异构混合推理引擎，将 MegaKernel 作为插件嵌入到 TensorRT-LLM 中，结合了高吞吐的 Prefill 和低延迟的 Decode。在 NVIDIA L20 上，Ada-MK 相比原生 TensorRT-LLM 将单批次吞吐量提升了高达 23.6%，相比 vLLM 提升了 50.2%，并在所有测试场景中均实现了正向收益——这是 MegaKernel 在商业在线广告系统中的首次工业化部署。

## Abstract
When large language models (LLMs) serve real-time inference in commercial online advertising systems, end-to-end latency must be strictly bounded to the millisecond range. Yet every token generated during the decode phase triggers thousands of kernel launches, and kernel launch overhead alone can account for 14.6% of end-to-end inference time. MegaKernel eliminates launch overhead and inter-operator HBM round-trips by fusing multiple operators into a single persistent kernel. However, existing MegaKernel implementations face a fundamental tension between portability and efficiency on resource-constrained GPUs such as NVIDIA Ada: hand-tuned solutions are tightly coupled to specific architectures and lack portability, while auto-compiled approaches introduce runtime dynamic scheduling whose branch penalties are unacceptable in latency-critical settings. We observe that under a fixed deployment configuration, the optimal execution path of a MegaKernel is uniquely determined, and runtime dynamic decision-making can be entirely hoisted to compile time. Building on this insight, we propose Ada-MK: (1) a three-dimensional shared-memory constraint model combined with K-dimension splitting that reduces peak shared memory usage by 50%; (2) MLIR-based fine-grained DAG offline search that solidifies the optimal execution path, completely eliminating runtime branching; and (3) a heterogeneous hybrid inference engine that embeds MegaKernel as a plugin into TensorRT-LLM, combining high-throughput Prefill with low-latency Decode. On an NVIDIA L20, Ada-MK improves single-batch throughput by up to 23.6% over vanilla TensorRT-LLM and 50.2% over vLLM, achieving positive gains across all tested scenarios--the first industrial deployment of MegaKernel in a commercial online advertising system.

---

## 论文详细总结（自动生成）

以下是对论文《Ada-MK: Adaptive MegaKernel Optimization via Automated DAG-based Search for LLM Inference》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在商业在线广告等延迟极度敏感的场景中，大语言模型（LLM）推理的端到端延迟必须限制在毫秒级。然而，在解码（Decode）阶段，频繁的算子核（Kernel）启动开销（占总推理时间的14.6%）和算子间频繁访问全局内存（HBM）成为了性能瓶颈。
*   **研究背景**：虽然 MegaKernel 技术通过算子融合能解决上述问题，但现有方案在 NVIDIA Ada（如 L20）等资源受限的 GPU 上面临挑战：
    *   **硬件限制**：Ada 架构缺乏 TMA 硬件支持，且共享内存（Shared Memory）容量仅为 H100 的一半。
    *   **现有方案缺陷**：手工调优方案缺乏可移植性；自动编译方案（如 Mirage）引入的运行时动态分支会降低指令执行效率。

### 2. 方法论：核心思想与关键技术
Ada-MK 提出了“离线规划、在线复用”的核心思想，主要包含三个技术支柱：
*   **自适应共享内存管理**：
    *   **三维约束模型**：结合硬件规格、模型架构和动态负载，建立共享内存分配模型。
    *   **K 维度细粒度拆分**：将 K 维度 Tile 减半，每轮迭代仅加载必要的权重子块，使峰值内存需求降低 50%。
    *   **页面复用**：实现激活值、权重和输出页面在算子间的动态回收与重分配，提升流水线深度。
*   **基于细粒度 DAG 的自动化搜索**：
    *   **MLIR 分解**：利用 MLIR 将粗粒度算子分解为 PTX 级别的细粒度依赖图（DAG）。
    *   **路径固化**：通过离线 Profiling 模拟多种页面排列和角色映射，锁定最优执行轨迹并序列化为内核代码，彻底消除运行时的 `if-else` 分支开销。
*   **异构混合推理引擎**：
    *   将 MegaKernel 作为插件嵌入 TensorRT-LLM。
    *   **相位自适应**：预填充（Prefill）阶段使用原生算子（计算密集型），解码（Decode）阶段自动切换至 Ada-MK（IO 密集型），兼顾高吞吐与低延迟。

### 3. 实验设计
*   **模型与量化**：Qwen3-1.7B 和 Qwen2.5-1.5B，采用 GPTQ-W4A16 量化。
*   **测试场景**：
    *   固定短序列：输入 64 token，输出 12 token。
    *   真实数据集：CSL（中长上下文）和 Human-eval（代码生成）。
*   **对比基准（Benchmark）**：vLLM (v0.19.0)、SGLang (v0.5.10)、原生 TensorRT-LLM (v1.1.0rc5)。
*   **评估指标**：每秒生成的 Token 数（Tokens/s）。

### 4. 资源与算力
*   **硬件环境**：单台服务器，配备 Intel Xeon Platinum 8558 处理器（96核）和 1TB DDR5 内存。
*   **GPU 型号**：单张 **NVIDIA L20 GPU**（48GB GDDR6，864 GB/s 带宽，Ada 架构）。
*   **训练/搜索时长**：文中未明确给出离线搜索的具体耗时，但强调该过程在部署前完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 种不同版本的 Qwen 模型，3 种不同的任务负载（固定短序列、CSL、Human-eval），以及 5 种 Batch Size（1, 2, 4, 8, 16）。
*   **充分性评价**：实验设计较为充分，对比了当前工业界最主流的三个推理框架。通过不同 Batch Size 的对比，客观展示了 MegaKernel 在小批次下的巨大优势以及在大批次下优势收窄的趋势。

### 6. 主要结论与发现
*   **性能提升**：在单批次（BS=1）下，Ada-MK 较原生 TensorRT-LLM 提升了 **23.6%** 的吞吐量，较 vLLM 提升了 **50.2%**。
*   **场景适用性**：MegaKernel 在**小批次、短序列**的在线推理任务中收益最高。
*   **系统扩展性**：随着 Batch Size 和序列长度增加，vLLM 等框架的系统级调度优势开始显现，Ada-MK 的领先幅度会缩小（在 CSL BS=16 场景下略逊于 vLLM）。
*   **工业落地**：该方案已成功部署于百度商业在线广告系统，证明了其工业实用性。

### 7. 优点
*   **消除运行时开销**：通过离线 DAG 搜索固化路径，解决了自动编译方案中常见的指令分支延迟问题。
*   **资源利用率极高**：在内存受限的 Ada 架构上，通过 K 维度拆分和页面复用，在极小的共享内存空间内构建了高效的软件流水线。
*   **无缝集成**：作为插件嵌入 TensorRT-LLM，既保留了成熟的业务能力（如前缀树解码），又获得了核心算子的性能增益。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 1.5B-1.7B 的小参数模型上，尚未验证其在百亿或千亿级参数模型上的表现。
*   **系统级调度缺失**：论文重点在于算子核（Kernel）层面的融合优化，在处理极高并发请求时的系统级内存管理（如 KV Cache 调度）相比 vLLM 尚有优化空间。
*   **硬件依赖性**：虽然提高了可移植性，但核心优化逻辑仍高度依赖于对 NVIDIA GPU 架构（如 Warp Specialization）的深度理解。

（完）
