---
title: "Multi-Scale Dequant: Eliminating Dequantization Bottleneck via Activation Decomposition for Efficient LLM Inference"
title_zh: Multi-Scale Dequant：通过激活分解消除反量化瓶颈以实现高效大语言模型推理
authors: "Lingchao Zheng, Yuwei Fan, Jun Li, Chengqiu Hu, Qichen Liao, Junyi Fan, Rui Shi, Fangzheng Miao"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13915v1"
tags: ["query:llm"]
score: 9.0
evidence: 高效大模型推理中的KV反量化优化
tldr: 针对大语言模型推理中反量化操作导致的计算瓶颈，本文提出Multi-Scale Dequant (MSD) 框架。该方法将高精度激活值分解为多个低精度组件，使其能直接与量化权重进行硬件加速的矩阵乘法，从而消除了反量化过程。MSD在保持精度的同时，显著提升了计算效率并减少了显存带宽占用，为高效推理提供了新方案。
selection_source: fresh_fetch
motivation: 传统的反量化操作在现代AI加速器上耗时过长，成为了制约大模型推理效率的关键瓶颈。
method: 提出将高精度激活值分解为多个低精度分量，直接利用硬件原生的低精度矩阵乘法单元与量化权重进行运算。
result: 在INT8和MXFP4权重格式下，MSD在不损失精度的前提下，有效消除了流水线停顿，并将KV缓存的显存流量降低了高达2.5倍。
conclusion: MSD通过改变计算范式，成功绕过了反量化瓶颈，为高效的大语言模型推理提供了一种高性能且高精度的解决方案。
---

## 摘要
量化对于高效的大语言模型（LLM）推理至关重要，然而反量化步骤——即在矩阵乘法前将低比特权重转换回高精度——已成为现代 AI 加速器上的关键瓶颈。在具有解耦计算单元的架构（如昇腾 NPU）上，反量化操作消耗的周期可能超过矩阵乘法本身，导致高吞吐量的张量核心利用不足。本文提出了多尺度反量化（MSD），这是一种将权重/KV 反量化从 GEMM 关键路径中移除的量化框架。MSD 并非将低比特权重提升至 BF16 精度，而是将高精度 BF16 激活分解为多个低精度组件，每个组件都可以通过原生硬件加速的 GEMM 直接与量化权重相乘。这种方法将计算范式从精度转换转向多尺度近似，避免了 GEMM 之前的 INT8 到 BF16 权重转换。我们针对两种权重格式实例化了 MSD，并分别为其推导了紧致的误差界限。对于 INT8 权重（W4A16），两轮 INT8 分解可达到接近 16 位的有效精度。对于 MXFP4 权重（W4A16），两轮 MXFP4 分解可产生接近 6.6 位的有效精度，且每块误差界限为 1/64，在保持相同有效 GEMM 计算时间的同时，超越了单轮 MXFP8（5.24 位）。我们进一步推导了闭式延迟和 HBM 流量模型，结果表明 MSD 避免了由反量化引起的 Vector-Cube 流水线停顿，并将注意力机制中的 KV 缓存 HBM 流量减少了高达 2.5 倍。在矩阵乘法和 Flash Attention 算子上的数值模拟证实，与反量化基准相比，MSD 不会降低准确性，并且在许多设置下实现了更低的 L2 误差。

## Abstract
Quantization is essential for efficient large language model (LLM) inference, yet the dequantization step-converting low-bit weights back to high-precision for matrix multiplication has become a critical bottleneck on modern AI accelerators. On architectures with decoupled compute units (e.g., Ascend NPUs), dequantization operations can consume more cycles than the matrix multiplication itself, leaving the high-throughput tensor cores underutilized. This paper presents Multi-Scale Dequant (MSD), a quantization framework that removes weight/KV dequantization from the GEMM critical path. Instead of lifting low-bit weights to BF16 precision, MSD decomposes high-precision BF16 activations into multiple low-precision components, each of which can be multiplied directly with quantized weights via native hardware-accelerated GEMM. This approach shifts the computational paradigm from precision conversion to multi-scale approximation, avoiding INT8-to-BF16 weight conversion before GEMM. We instantiate MSD for two weight formats and derive tight error bounds for each. For INT8 weights (W4A16), two-pass INT8 decomposition achieves near 16 effective bits. For MXFP4 weights (W4A16), two-pass MXFP4 decomposition yields near 6.6 effective bits with error bound 1/64 per block surpassing single-pass MXFP8(5.24 bits) while maintaining the same effective GEMM compute time. We further derive closed-form latency and HBM traffic models showing that MSD avoids the Vector-Cube pipeline stall caused by dequantization and reduces KV cache HBM traffic by up to 2.5 times in attention. Numerical simulations on matrix multiplication and Flash Attention kernels confirm that MSD does not degrade accuracy compared to dequantization baselines, and in many settings achieves lower L2 error.

---

## 论文详细总结（自动生成）

### 论文总结：Multi-Scale Dequant (MSD) —— 通过激活分解消除大模型推理中的反量化瓶颈

#### 1. 核心问题与整体含义
论文针对大语言模型（LLM）推理中的**反量化瓶颈（Dequantization Bottleneck）**展开研究。在现代 AI 加速器（如华为昇腾 NPU 或 NVIDIA GPU）上，为了节省显存，权重通常以低比特（INT8/INT4）存储，而计算时需将其反量化回高精度（BF16/FP16）。
*   **研究动机**：在解耦计算单元架构中，负责反量化的向量单元（Vector Core）吞吐量远低于负责矩阵乘法的张量单元（Cube/Tensor Core）。这导致张量单元经常处于空闲等待状态，且反量化过程产生了大量的 HBM（显存）读写开销。
*   **核心意义**：提出了一种全新的计算范式，通过分解激活值而非提升权重精度，彻底从 GEMM（通用矩阵乘法）的关键路径中移除了反量化操作。

#### 2. 方法论
MSD 的核心思想是**激活分解（Activation Decomposition）**，将高精度的激活值分解为多个低精度组件的加权和。
*   **核心公式**：对于激活向量 $x$ 和量化权重 $W$，传统方法计算 $dequant(W) \cdot x$。MSD 则将其转化为：
    $x \approx \alpha \cdot x^{(1)} + \beta \cdot x^{(2)}$，其中 $x^{(1)}, x^{(2)}$ 均为低精度格式（如 INT8）。
    最终结果为：$y = \alpha(W \cdot x^{(1)}) + \beta(W \cdot x^{(2)})$。
*   **关键技术细节**：
    *   **两轮分解算法**：第一轮进行粗尺度量化捕捉主要信号，第二轮对残差进行细尺度量化。
    *   **硬件原生加速**：分解后的 $W \cdot x^{(i)}$ 是纯粹的低精度矩阵乘法，可直接在硬件张量核心上以最高吞吐量运行。
    *   **融合平铺内核（Fused Tiled Kernel）**：在算子实现中，权重分片只需从显存加载一次并驻留在片上，连续参与两次低精度 GEMM，从而大幅减少显存流量。
    *   **针对 MXFP4 的优化**：针对微缩放（Microscaling）格式，通过 $\alpha$ 松弛技术进一步提升精度。

#### 3. 实验设计
*   **实验场景**：主要针对线性层（GEMM）和 Flash Attention 算子。
*   **权重格式**：INT8 (W8A16) 和 MXFP4 (W4A16)。
*   **Benchmark（基准）**：
    *   传统的反量化基准（Dequant-based baseline）。
    *   单精度 BF16 计算（作为 Ground Truth）。
    *   单轮量化（K=1）作为消融对比。
*   **对比维度**：L2 相对误差、误差分布（超过特定阈值的元素比例）、HBM 显存流量模型、向量计算开销。

#### 4. 资源与算力
*   **硬件平台**：明确提到使用了**华为昇腾（Ascend）910B NPU**。
*   **软件集成**：该技术已集成至华为 **CANN 8.0** 算子库中。
*   **算力细节**：文中未详细列出具体的训练时长或总 GPU/NPU 小时数，因为该研究侧重于推理阶段的算子优化和数值模拟，而非模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了不同激活分布（高斯、均匀、拉普拉斯、指数等）下的鲁棒性测试。
    *   矩阵规模从 256 到 4096 进行了扩展性验证。
    *   Flash Attention 实验涵盖了从 64 到 16384 的序列长度。
*   **充分性评价**：实验设计较为充分，涵盖了数值精度分析、硬件延迟建模和显存流量分析。通过消融实验验证了“两轮分解”的必要性。然而，实验主要集中在**算子级（Operator-level）**的数值模拟，缺乏端到端模型（如 Llama-3 在具体任务上的 Perplexity）的最终评测数据（作者表示将在后续报告中提供）。

#### 6. 主要结论与发现
*   **精度提升**：对于 INT8 权重，MSD 达到了约 16 位的有效精度，L2 误差比传统反量化方法低 200 倍。
*   **性能优化**：在 Attention Decode 阶段，MSD 将 KV Cache 的显存流量降低了高达 **2.5 倍**，并消除了 Vector-Cube 之间的流水线停顿。
*   **计算效率**：虽然 MSD 增加了 GEMM 的理论浮点运算量（FLOPs），但由于低精度 GEMM 的硬件吞吐量通常是高精度的 2 倍以上，实际执行时间保持不变或更快，同时释放了向量单元的压力。

#### 7. 优点
*   **架构适配性强**：精准解决了异构计算单元（Vector vs Cube）之间的负载不均衡问题。
*   **数学完备性**：为 INT8 和 MXFP4 格式推导了严谨的误差界限（Error Bounds）。
*   **显存友好**：通过减少反量化产生的中间数据读写，极大地缓解了 LLM 推理中的带宽瓶颈。

#### 8. 不足与局限
*   **应用场景限制**：MSD 主要针对**Decode（解码）阶段**（内存受限型），在 Prefill（预填充）阶段（计算受限型），由于额外的 GEMM 开销，其优势会减弱甚至消失。
*   **端到端验证缺失**：论文中未给出完整模型在下游任务（如 MMLU 评测）上的精度表现，仅证明了算子层面的数值等价性。
*   **实现复杂度**：需要深度定制化的融合内核开发，对软件栈（如 CANN 或 CUDA）的底层优化能力要求较高。

（完）
