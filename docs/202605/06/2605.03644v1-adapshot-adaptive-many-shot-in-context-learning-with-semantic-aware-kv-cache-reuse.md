---
title: "AdapShot: Adaptive Many-Shot In-Context Learning with Semantic-Aware KV Cache Reuse"
title_zh: AdapShot：基于语义感知 KV 缓存复用的自适应多样本上下文学习
authors: "Jie Ou, Jinyu Guo, Shiyao Guo, Yuang Li, Ruiqi Wu, Zhaokun Wang, Wenyi Li, Wenhong Tian"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03644v1"
tags: ["query:llm"]
score: 9.0
evidence: 多样本上下文学习中的KV缓存复用以实现高效推理
tldr: 针对Many-Shot ICL中固定示例数量导致的效率与准确性平衡问题，本文提出AdapShot。该方法通过基于输出熵的探测机制动态确定最优示例数，并结合语义感知的KV缓存复用策略，利用解耦与重编码技术解决位置编码不兼容问题，在显著降低计算开销的同时提升了模型在复杂任务中的推理表现。
selection_source: fresh_fetch
motivation: 现有的Many-Shot ICL方法通常采用固定数量的示例，难以适应不同难度的查询且面临高昂的计算与内存成本。
method: 设计了基于输出熵的动态示例数优化机制，并引入语义感知的KV缓存复用及位置编码解耦重编码技术以实现高效推理。
result: "实验显示AdapShot相比现有SOTA方法DBSA实现了约10%的性能提升，并获得了4.64倍的推理加速。"
conclusion: AdapShot通过动态调整上下文长度和高效缓存管理，成功解决了Many-Shot ICL在灵活性与推理效率方面的瓶颈。
---

## 摘要
多样本上下文学习（Many-Shot ICL）已成为一种极具前景的范式，它利用大量示例来释放大语言模型（LLMs）的推理潜力。然而，现有方法通常依赖于预先确定的固定样本数量。这种静态方法往往无法适应不同查询的难度变化，导致上下文不足或受到噪声干扰。此外，长上下文带来的极高计算和内存成本严重限制了多样本学习的可行性。为了解决上述局限性，我们提出了 AdapShot，它能够动态优化样本数量并利用 KV 缓存复用来实现高效推理。具体而言，我们设计了一种基于探针的评估机制，利用输出熵来确定最佳样本数量。为了绕过探针和推理阶段冗余的预填充计算，我们引入了语义感知的 KV 缓存复用策略。在该复用策略中，为了解决位置编码不兼容的问题，我们引入了一种解耦与重编码方法，实现了缓存键值对的灵活重排序。广泛的实验表明，与最先进的 DBSA 相比，AdapShot 实现了约 10% 的平均性能提升和 4.64 倍的加速。

## Abstract
Many-Shot In-Context Learning (ICL) has emerged as a promising paradigm, leveraging extensive examples to unlock the reasoning potential of Large Language Models (LLMs). However, existing methods typically rely on a predetermined, fixed number of shots. This static approach often fails to adapt to the varying difficulty of different queries, leading to either insufficient context or interference from noise. Furthermore, the prohibitive computational and memory costs of long contexts severely limit Many-Shot's feasibility. To address the above limitations, we propose AdapShot, which dynamically optimizes shot counts and leverages KV cache reuse for efficient inference. Specifically, we design a probe-based evaluation mechanism that utilizes output entropy to determine the optimal number of shots. To bypass the redundant prefilling computation during both the probing and inference phases, we incorporate a semantics-aware KV cache reuse strategy. Within this reuse strategy, to address positional encoding incompatibilities, we introduce a decoupling and re-encoding method that enables the flexible reordering of cached key-value pairs. Extensive experiments demonstrate that AdapShot achieves an average performance gain of around 10% and a 4.64x speedup compared to state-of-the-art DBSA.

---

## 论文详细总结（自动生成）

这篇论文提出了 **AdapShot**，一种针对大语言模型（LLM）多样本上下文学习（Many-Shot ICL）的自适应推理框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多样本上下文学习（Many-Shot ICL）通常采用**固定数量**的示例（Shots）。
*   **研究动机**：
    *   **性能非单调性**：并非示例越多越好。过多的示例可能引入噪声，干扰模型推理；过少的示例则不足以让模型掌握复杂任务。
    *   **任务难度差异**：不同的查询（Query）对示例的需求量不同，静态策略无法实现最优的“准确-效率”平衡。
    *   **计算开销巨大**：长上下文导致预填充（Prefilling）阶段的计算延迟和显存占用极高，限制了大规模应用。

### 2. 方法论
AdapShot 的核心思想是**按需分配计算资源**，通过动态确定示例数量并复用 KV 缓存来加速推理。

*   **基于探针的动态评估（Probe-based Evaluation）**：
    *   在推理前，系统先对示例进行语义相关性排序。
    *   通过插入“探针提示词”（如“你是否有信心回答？”）并计算模型输出分布的**信息熵（Entropy）**。
    *   若熵值低于阈值，说明模型信心充足，停止增加示例；否则继续增加。
    *   利用**树状注意力（Tree-structured Attention）**机制，在单次前向传播中并行测试多个示例规模，降低探测开销。
*   **语义感知 KV 缓存复用（Semantically-Aware KV Cache）**：
    *   **离线阶段**：预先计算所有候选示例的 KV 缓存并存储。
    *   **在线阶段**：根据查询向量与缓存键（Key）的注意力得分进行检索，直接提取最相关的示例 KV 块，避免重复计算。
*   **位置解耦与重编码（Position Decoupling and Re-encoding, PDR）**：
    *   **技术细节**：由于缓存的示例是独立编码的（位置从0开始），直接拼接会导致位置冲突。
    *   **核心公式**：利用旋转位置编码（RoPE）的数学特性，通过低成本的向量旋转操作（$\Delta$ 偏移量旋转），将缓存的 KV 对动态映射到新的逻辑位置，实现无损且灵活的示例重排。

### 3. 实验设计
*   **数据集**：涵盖 7 个代表性任务，包括自然语言理解（CoLA, QNLI, PIQA）、问答（SQuAD v2）和数学推理（SVAMP, GSM8K, MathQA）。
*   **模型**：Llama-3.2 (3B), Qwen2.5 (7B/14B/32B), 以及 openPangu-Embedded-1B。
*   **对比方法（Baselines）**：
    *   Zero-shot, Few-shot (8-shot)。
    *   固定长度的 Many-shot (64 到 1024-shot)。
    *   **DBSA**（Dynamic Block-Sparse Attention）：目前最先进的动态块稀疏注意力方法。
    *   基于 BM25 检索的 Many-shot。

### 4. 资源与算力
*   **硬件平台**：华为昇腾（Huawei Ascend）910B2 NPU 集群。
*   **配置**：共使用 8 台 NPU，每台配备 64GB 显存。
*   **CPU**：华为鲲鹏（Kunpeng）920 5250（48核）。
*   **内存**：系统配置 1.5TB 内存。
*   **训练/推理时长**：文中未明确给出总训练时长，但强调了推理阶段的加速比。

### 5. 实验数量与充分性
*   **实验规模**：在 4 种不同架构、多个参数规模的模型上进行了测试，涵盖了从 1B 到 32B 的参数区间。
*   **消融实验**：专门验证了 PDR（位置重编码）模块对准确性的贡献，以及 SAKVC（语义感知缓存）对延迟的降低作用。
*   **充分性评价**：实验设计较为全面，不仅对比了准确率（EM），还详细分析了推理速度、模型扩展性（Scaling）以及对不同检索算法的鲁棒性，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：AdapShot 在 Qwen2.5-7B 上相比 DBSA 实现了约 **10% 的平均性能增益**。
*   **推理加速**：相比 DBSA 实现了 **4.64 倍的平均加速**，在数学任务（MathQA）上最高达到 6.33 倍。
*   **噪声抑制**：实验发现大模型（如 32B）对上下文噪声更敏感，AdapShot 通过动态精简示例，有效缓解了长上下文带来的性能下降。
*   **显存优化**：避免了 1024-shot 等超长上下文导致的显存溢出（OOM）问题。

### 7. 优点与亮点
*   **动态性**：首次将基于探针的动态预算分配引入 Many-shot ICL，解决了“示例越多越好”的误区。
*   **技术创新**：巧妙利用 RoPE 的旋转特性解决 KV 缓存拼接时的位置编码不兼容问题，实现了真正的“无损复用”。
*   **高效并行**：通过树状注意力机制将多次探测合并为一次，极大地降低了决策成本。

### 8. 不足与局限
*   **数据依赖**：仍依赖于检索真实的示例数据，这带来了额外的数据存储开销和样本筛选成本。
*   **阈值敏感性**：虽然文中提到对阈值 $\tau$ 不敏感，但在极端多变的任务场景下，如何自动设定最优熵阈值仍有待探索。
*   **应用限制**：目前主要针对具有 RoPE 编码的模型，对于使用其他位置编码（如 ALiBi 或绝对位置编码）的模型，其 PDR 模块的适用性需重新设计。

（完）
