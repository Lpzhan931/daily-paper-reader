---
title: "SparKV: Overhead-Aware KV Cache Loading for Efficient On-Device LLM Inference"
title_zh: SparKV：面向高效端侧大语言模型推理的开销感知 KV 缓存加载
authors: "Hongyao Liu, Liuqun Zhai, Junyi Wang, Zhengru Fang"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21231v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对端侧大模型推理的自适应KV缓存加载与流式传输
tldr: 针对端侧大模型推理中预填充阶段计算开销大、硬件资源受限的问题，本文提出 SparKV 框架。该框架通过结合云端 KV 缓存流式传输与本地计算，动态决定每个 KV 块的加载方式，并利用并行执行降低延迟。SparKV 还能根据实时网络和资源状况动态调整策略，在保证生成质量的同时，显著提升了首字生成速度并降低了能耗，为端侧 LLM 部署提供了高效方案。
selection_source: fresh_fetch
motivation: 端侧设备硬件资源有限，导致大模型推理的预填充阶段在处理长上下文时面临极高的计算延迟和能耗挑战。
method: 提出一种自适应 KV 加载框架，通过建模评估 KV 块的传输与计算成本，动态分配云端流式传输与本地计算任务并实现并行执行。
result: 实验显示该方法将首字生成时间缩短了 1.3 至 5.1 倍，同时将单次请求的能耗降低了 1.5 至 3.3 倍，且对模型精度影响极小。
conclusion: SparKV 通过灵活平衡通信与计算开销，有效解决了端侧大模型推理的性能瓶颈，具有极强的实用性和鲁棒性。
---

## 摘要
由于硬件资源有限以及预填充（prefill）阶段的高昂成本，端侧大语言模型（LLM）的高效推理仍面临挑战。预填充阶段需要处理完整的输入上下文以构建键值（KV）缓存。我们提出了 SparKV，这是一个自适应 KV 加载框架，它结合了基于云端的 KV 流式传输与端侧计算。SparKV 对单个 KV 块的成本进行建模，并决定每个块应当通过流式传输获取还是在本地计算，同时通过重叠这两条执行路径来降低延迟。为了应对无线连接和边缘资源可用性的波动，SparKV 在运行时进一步优化离线生成的调度方案，以重新平衡通信与计算成本。在不同数据集、LLM 和边缘设备上的实验表明，SparKV 在对响应质量影响微乎其微的前提下，将首字延迟（Time-to-First-Token）降低了 1.3 倍至 5.1 倍，同时将单次请求的能耗降低了 1.5 倍至 3.3 倍，证明了其在真实端侧部署中的鲁棒性和实用性。

## Abstract
Efficient inference for on-device Large Language Models (LLMs) remains challenging due to limited hardware resources and the high cost of the prefill stage, which processes the full input context to construct Key-Value (KV) caches. We present SparKV, an adaptive KV loading framework that combines cloud-based KV streaming with on-device computation. SparKV models the cost of individual KV chunks and decides whether each chunk should be streamed or computed locally, while overlapping the two execution paths to reduce latency. To handle fluctuations in wireless connectivity and edge resource availability, SparKV further refines offline-generated schedules at runtime to rebalance communication and computation costs. Experiments across diverse datasets, LLMs, and edge devices show that SparKV reduces Time-to-First-Token by 1.3$x-5.1x with negligible impact on response quality, while lowering per-request energy consumption by 1.5x to 3.3x, demonstrating its robustness and practicality for real-world on-device deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《SparKV: Overhead-Aware KV Cache Loading for Efficient On-Device LLM Inference》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在端侧设备（如智能手机、边缘网关）上部署大语言模型（LLM）时，**首字生成延迟（TTFT）**是影响用户体验的核心瓶颈。这主要是因为在“预填充（Prefill）”阶段，模型需要处理长上下文以构建键值（KV）缓存，该过程对计算和内存带宽要求极高。
*   **研究动机**：现有的优化方案要么侧重于从云端流式传输压缩后的 KV 缓存（受限于无线网络波动），要么侧重于本地稀疏计算（受限于端侧算力）。
*   **核心目标**：提出 SparKV 框架，通过**云端流式传输与本地计算的自适应混合加载**，充分利用通信与计算的并行性，降低 TTFT 和能耗。

### 2. 方法论：核心思想与关键技术
SparKV 的核心思想是将 KV 缓存划分为细粒度的块（Chunks），并根据实时开销感知动态决定每个块是“从云端传”还是“在本地算”。
*   **KV 块调度器（Scheduler）**：
    *   将 KV 缓存按 Token、注意力头和层维度进行切分。
    *   建立了一个混合整数线性规划（MILP）模型来最小化总耗时，并设计了一种**潜力感知贪婪启发式算法**，在满足 Transformer 严格的层间和 Token 间依赖关系的同时，优先处理能解锁更多后续计算任务的块。
*   **开销预测模型（Overhead Model）**：
    *   使用轻量级多层感知机（MLP）预测本地稀疏注意力的计算延迟。
    *   输入特征包括：Token 索引、注意力掩码中的活跃块数量、实时 GPU 利用率。
*   **运行时控制器（Runtime Controller）**：
    *   动态监测无线带宽波动和端侧算力占用。
    *   **动态迁移机制**：如果网络变差，将原定传输的块转为本地计算；如果 GPU 繁忙，则增加流式传输比例。

### 3. 实验设计
*   **数据集/场景**：涵盖了 9 个主流数据集，包括文本问答（TriviaQA, HotpotQA）、长文本理解（LongChat, LongBench）、代码编辑（RepoBench-P）和多模态视频理解（VideoMME）。
*   **对比方法（Benchmarks）**：
    1.  **CacheGen**：纯流式传输方案（采用层级量化和算术编码）。
    2.  **Local Prefill**：纯本地计算方案（采用先进的 SpargeAttention 稀疏注意力加速）。
    3.  **Strong Hybrid**：简单的并行方案（固定比例的传输与计算重叠）。
*   **测试模型**：Qwen3 (4B/14B), Llama-3.1 (8B), Qwen2.5-VL (7B), InternVL2 (8B)。

### 4. 资源与算力
*   **硬件平台**：
    *   移动端：Redmi K80 Pro (骁龙 8 Elite, Hexagon NPU)。
    *   边缘计算平台：NVIDIA Jetson AGX (64GB), Jetson Orin (16GB)。
    *   笔记本 GPU：NVIDIA RTX 5080 Mobile。
*   **训练/运行开销**：
    *   MLP 预测器非常轻量，在 Jetson Orin 上仅需 **17.6 秒** 即可完成 6000 个样本的离线训练。
    *   推理时，每个块的预测开销仅为 2.6 毫秒。

### 5. 实验数量与充分性
*   **实验规模**：在 5 种不同架构的模型和 9 个数据集上进行了广泛测试，涵盖了从 10K 到 49K Token 的不同上下文长度。
*   **维度丰富**：除了 TTFT 性能外，还进行了能耗测试、无线干扰下的鲁棒性测试、并发请求压力测试以及消融实验（如预测器准确性对比）。
*   **客观性**：实验在真实的 Wi-Fi 6 环境下进行，考虑了网络抖动等现实因素，对比基准均采用了当前最优的开源实现（如 SpargeAttention），评价较为公平。

### 6. 主要结论与发现
*   **性能提升**：SparKV 相比纯本地计算提升了 **1.3x - 5.1x** 的 TTFT 速度。
*   **能耗降低**：相比本地计算，单次请求能耗降低了 **1.5x - 3.3x**，因为无线网卡的功耗（2-3W）远低于 GPU 高负载功耗（20-30W）。
*   **质量保持**：在大幅加速的同时，模型生成的 F1 分数、Rouge-L 等质量指标几乎没有损失（与全量计算相比）。
*   **鲁棒性**：在网络拥塞或多任务竞争资源时，SparKV 的动态调整机制能有效避免性能剧烈下降。

### 7. 优点
*   **开销感知精准**：不同于以往固定比例的混合方案，SparKV 识别到了 KV 块之间巨大的异构性（计算开销差异可达 17 倍）。
*   **软硬件协同**：结合了云端存储优势与端侧稀疏计算内核，适应移动互联网的实际环境。
*   **实用性强**：不仅关注延迟，还重点优化了移动设备最敏感的能耗指标。

### 8. 不足与局限
*   **应用限制**：目前主要针对**单次重用上下文**的场景，对于 RAG 中常见的多个零散上下文块的联合处理（Multi-context blending）尚需扩展。
*   **硬件覆盖**：虽然在 NPU 上做了初步测量，但核心调度逻辑和稀疏算子目前主要在 NVIDIA GPU 栈上实现，在移动端 NPU 上的深度集成和优化细节较少。
*   **隐私权衡**：虽然论文提到了隐私，但流式传输 KV 缓存本质上仍涉及数据在云端的存储，对于极高隐私要求的场景可能存在合规挑战。

（完）
