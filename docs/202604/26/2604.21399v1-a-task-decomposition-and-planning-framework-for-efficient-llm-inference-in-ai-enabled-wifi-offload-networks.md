---
title: A Task Decomposition and Planning Framework for Efficient LLM Inference in AI-Enabled WiFi-Offload Networks
title_zh: AI赋能的WiFi卸载网络中高效大语言模型推理的任务分解与规划框架
authors: "Mingqi Han, Xinghua Sun"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21399v1"
tags: ["query:llm"]
score: 8.0
evidence: 通过任务分解和卸载实现高效LLM推理
tldr: 针对资源受限设备在WiFi网络中进行大语言模型（LLM）推理的挑战，本文提出一种用户-边缘协作框架。该框架利用LLM规划器进行任务分解，并预测子任务难度和输出长度，以优化异构节点间的任务分配。通过分解感知调度策略，在满足通信和计算约束的同时，显著降低了推理延迟并提升了准确性，为高效的AI WiFi卸载提供了新方案。
selection_source: fresh_fetch
motivation: 传统边缘计算难以应对LLM推理中异构模型能力、无线竞争及任务复杂性带来的挑战。
method: 提出一种基于LLM规划器的协作框架，通过任务分解、难度推断及分解感知调度策略，实现本地与多个边缘节点间的协同执行。
result: "实验表明，该框架相比基准方案降低了20%的平均延迟，并将整体奖励提升了80%，且蒸馏后的轻量级规划器表现优异。"
conclusion: 该研究证明了任务分解与智能规划在优化WiFi网络中LLM推理性能方面的有效性，为边缘AI部署提供了实用参考。
---

## 摘要
AI WiFi卸载正成为向资源受限的无线设备提供大语言模型（LLM）服务的一种极具前景的方法。然而，与传统的边缘计算不同，基于WiFi的LLM推理必须共同解决异构模型能力、无线竞争、不确定的任务复杂度以及推理任务之间的语义相关性。在本文中，我们研究了多用户多边缘WiFi网络中的LLM推理卸载，其中每个任务可以本地执行、直接卸载到附近的边缘接入点（AP），或者分解为多个子任务在本地和边缘节点之间协作执行。为此，我们提出了一个带有基于LLM的规划器的用户-边缘协作框架，该规划器不仅执行任务分解，还推断子任务难度和预期的输出Token长度，从而能够更准确地估计异构节点上的执行质量和延迟。基于这些估计，我们进一步设计了一种分解感知的调度策略，在通信、排队和计算约束下联合优化子任务的分配、执行和聚合。仿真结果表明，所提出的框架比仅本地执行和最近边缘执行的基准方案实现了更好的延迟-准确度权衡，平均延迟降低了20%，总奖励提升了80%。此外，经过蒸馏的轻量级规划器在性能上接近大型教师模型，同时更适合实际的边缘部署。

## Abstract
AI WiFi offload is emerging as a promising approach for providing large language model (LLM) services to resource-constrained wireless devices. However, unlike conventional edge computing, LLM inference over WiFi must jointly address heterogeneous model capabilities, wireless contention, uncertain task complexity, and semantic correlation among reasoning tasks. In this paper, we investigate LLM inference offloading in a multi-user multi-edge WiFi network, where each task can be executed locally, directly offloaded to a nearby edge access point (AP), or decomposed into multiple subtasks for collaborative execution across local and edge nodes. To this end, we propose a user-edge collaborative framework with an LLM-based planner that not only performs task decomposition but also infers subtask difficulty and expected output token length, enabling more accurate estimation of execution quality and latency on heterogeneous nodes. Based on these estimates, we further design a decomposition-aware scheduling strategy that jointly optimizes subtask assignment, execution, and aggregation under communication, queuing, and computation constraints. Simulation results show that the proposed framework achieves a better latency-accuracy tradeoff than local-only and nearest-edge baselines, reducing the average latency by $20\%$ and improving the overall reward by $80\%$. Moreover, the distilled lightweight planner approaches the performance of the large teacher model while remaining more suitable for practical edge deployment.

---

## 论文详细总结（自动生成）

这篇论文针对在 WiFi 边缘网络中部署大语言模型（LLM）推理任务提出了一个创新的任务分解与规划框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着 LLM 应用的普及，资源受限的无线设备（如穿戴设备、机器人）难以独立承担高昂的计算开销。WiFi 卸载（WiFi-Offload）被视为一种可行的解决方案。
*   **核心问题**：传统的边缘计算卸载方案（如二进制卸载或简单部分卸载）无法直接应用于 LLM，原因有三：
    1.  **任务不确定性**：LLM 任务的难度和输出长度在执行前难以准确量化。
    2.  **模型异构性**：边缘节点和本地设备运行的模型规模、计算能力和内存带宽各异。
    3.  **语义相关性**：LLM 任务（如思维链推理）具有复杂的逻辑依赖，简单的切分会破坏语义完整性。
*   **研究目标**：在多用户、多边缘节点的 WiFi 环境下，通过智能任务分解与调度，平衡推理准确度与端到端延迟。

### 2. 方法论
*   **核心思想**：引入一个基于 LLM 的“规划器（Planner）”，将复杂任务分解为可并行或协作执行的子任务，并根据预测的子任务属性进行最优调度。
*   **关键技术细节**：
    *   **任务分解与属性推断**：规划器不仅负责拆分任务，还需预测每个子任务的**难度**（用于估计准确率）和**预期输出 Token 长度**（用于估计延迟）。
    *   **分解感知调度策略**：采用贪心算法，基于综合评分函数 $J_{k,i} = \omega_a \hat{a}_{k,i} - \omega_d T_{tot, k,i}$ 选择执行节点。该函数联合考虑了预期的准确度（$\hat{a}$）、通信延迟、排队延迟和计算延迟（$T_{tot}$）。
    *   **模型蒸馏**：为了让规划器能在资源受限的边缘 AP 上运行，研究者使用强大的教师模型（DeepSeek-v3.2）生成规划数据，通过监督微调（SFT）训练轻量级的学生模型（Qwen2.5-7B-Instruct）。
    *   **WiFi 通信模型**：详细建模了 WiFi 7 协议行为，包括二进制指数退避、RTS/CTS 握手、触发帧（TF）交互及竞争开销。

### 3. 实验设计
*   **实验场景**：50m × 50m 的室内环境，包含 2-4 个异构边缘 AP 和 5-7 个 UE。
*   **数据集**：
    *   **AIME-2024**：考察数学推理能力。
    *   **LiveBench-Reasoning**：考察日常逻辑推理。
    *   **GPQA**：考察科学领域专业知识。
*   **对比方法（Benchmarks）**：
    1.  **Local-Only**：仅在 UE 本地执行。
    2.  **Nearest-Edge**：整体卸载至最近的边缘 AP，不进行分解。
    3.  **Original Qwen-7B Planner**：未经蒸馏微调的原始规划器。
    4.  **DS-v3.2 Planner**：作为性能上限的教师模型规划器。

### 4. 资源与算力
*   **硬件参数设定**：论文在仿真参数中设定了边缘 AP 的算力为 120-312 TFLOPS，内存带宽为 0.6-2 TB/s；UE 算力为 20-48 TFLOPS。
*   **训练细节**：文中提到对 Qwen2.5-7B 进行了监督微调（SFT），但**未明确说明**具体的训练硬件（如 GPU 型号、数量）及具体的训练时长。

### 5. 实验数量与充分性
*   **实验规模**：每个场景进行了 10 组独立的随机实验（Episodes），每组模拟时长为 600 秒。
*   **充分性评价**：
    *   **客观性**：实验覆盖了数学、科学、逻辑三种不同类型的任务，具有较好的代表性。
    *   **公平性**：对比了本地、最近边缘以及不同规模规划器的表现，验证了分解机制和蒸馏算法的有效性。
    *   **局限性**：实验主要在仿真环境下完成，虽然建模了复杂的 WiFi 协议，但缺乏真实物理环境中的干扰测试。

### 6. 主要结论与发现
*   **性能提升**：所提框架相比“最近边缘卸载”方案，平均延迟降低了 **20%**，整体奖励（综合准确度与延迟）提升了 **80%**。
*   **分解的必要性**：在处理 AIME 等高难度任务时，本地执行几乎无法得出正确答案，而通过分解协作显著提升了准确率。
*   **蒸馏效果**：经过微调的 7B 规划器在性能上非常接近 200B+ 规模的教师模型，且由于参数量小，其规划过程产生的自身延迟更低，更适合边缘部署。

### 7. 优点
*   **联合优化**：首次在 WiFi 卸载场景下联合考虑了任务的语义分解、异构模型能力和复杂的 MAC 层通信开销。
*   **属性感知**：通过 LLM 预测输出长度和难度，解决了 LLM 推理开销“不可预知”的痛点。
*   **实用性强**：通过模型蒸馏解决了规划器自身的部署门槛问题。

### 8. 不足与局限
*   **规划器开销**：虽然使用了轻量级模型，但规划器本身的推理（Pre-fill 和 Decode）仍会产生额外延迟，在极低延迟需求的场景下可能成为瓶颈。
*   **子任务依赖简化**：实验中假设最大子任务数为 3，且主要考虑并行执行，对于具有复杂嵌套依赖的任务处理能力尚待验证。
*   **移动性考虑不足**：实验场景相对静态，未深入探讨 UE 高速移动或频繁切换 AP 时的性能表现。

（完）
