---
title: "DiP-SD: Distributed Pipelined Speculative Decoding for Efficient LLM Inference at the Edge"
title_zh: DiP-SD：面向边缘端高效大语言模型推理的分布式流水线投机解码
authors: "Yaodan Xu, Sheng Zhou, Zhisheng Niu"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20919v1"
tags: ["query:llm-sd"]
score: 9.0
evidence: 边缘推理的分布式流水线投机解码
tldr: 针对边缘计算场景下的多用户大模型推理，本文提出DiP-SD框架。该框架利用分布式投机采样技术，让用户设备本地生成草稿令牌并由边缘服务器批量验证。通过结合设备级分布式草稿生成与阶段级流水线并行，并联合优化批处理策略与草稿长度，DiP-SD显著提升了推理吞吐量，解决了边缘环境下资源受限与高并发推理的矛盾。
selection_source: fresh_fetch
motivation: 在多用户边缘推理场景中，如何通过协同优化批处理调度和草稿长度来最大化投机采样的系统吞吐量是一个关键挑战。
method: 提出DiP-SD框架，结合设备端分布式草稿生成与服务器端流水线验证，并采用交替优化算法解决批次分配与草稿长度的联合优化问题。
result: 实验显示，DiP-SD在Qwen模型部署下比传统自回归解码提升了高达17.89倍的吞吐量，比带贪婪批处理的自回归解码提升了1.93倍。
conclusion: DiP-SD通过协同优化分布式草稿生成与流水线验证，为边缘侧高效、高吞吐的大模型推理提供了有效的解决方案。
---

## 摘要
投机解码已成为一种极具前景的大语言模型（LLM）推理技术，通过“先草拟后验证”的方式加速自回归解码。本文研究了一种新的多用户推理边缘场景，其中草拟令牌（draft tokens）在设备本地生成，随后被卸载到中心化边缘服务器进行批量验证。关键挑战在于如何在 (i) 批处理与流水线调度以及 (ii) 每用户草拟令牌长度的耦合决策下维持高吞吐量。我们提出了 DiP-SD，它利用了两个互补的并行维度：设备级分布式草拟和阶段级草拟-验证流水线。我们制定了一个吞吐量最大化目标（定义为单位时间内预期接受的令牌数量），并联合优化批次数、用户到批次的分配以及整数草拟长度。为了解决由此产生的分式混合整数规划问题，DiP-SD 通过扫描批次数，并在关联子问题和草拟长度子问题之间迭代交替。在 Qwen3-1.7B/Qwen3-32B 设备-边缘部署下的数值结果表明，DiP-SD 的吞吐量相比自回归解码（AD）提升了高达 17.89 倍，相比采用贪婪批处理的 AD 提升了 1.93 倍。

## Abstract
Speculative decoding has emerged as a promising technique for large language model (LLM) inference by accelerating autoregressive decoding via draft-then-verify. This paper studies a new edge scenario with multi-user inference, where draft tokens are generated locally on devices and subsequently offloaded to a centralized edge server for batch verification. The key challenge is to sustain high throughput under coupled decisions of (i) batching and pipeline scheduling and (ii) per user draft token length. We propose DiP-SD, which exploits two complementary parallelism dimensions: device-level distributed drafting and phase-level draft-verify pipelining. We formulate a throughput-maximization objective, defined as the expected number of accepted tokens per unit time, and jointly optimize the number of batches, user-to-batch assignment, and integer draft lengths. To solve the resulting fractional mixed-integer program, DiP-SD scans the batch number and iteratively alternates between an association subproblem and a draft-length subproblem. Numerical results under a Qwen3-1.7B/Qwen3-32B device-edge deployment show that DiP-SD achieves up to 17.89x throughput over autoregressive decoding (AD) and 1.93x over AD with greedy batching.

---

## 论文详细总结（自动生成）

这是一份关于论文《DiP-SD: Distributed Pipelined Speculative Decoding for Efficient LLM Inference at the Edge》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）的自回归解码过程受限于显存带宽，导致推理延迟高。投机解码（Speculative Decoding, SD）通过“小模型草拟-大模型验证”机制加速推理，但在边缘计算场景下，多用户并发请求会导致边缘服务器的验证环节成为瓶颈。
*   **核心问题**：在边缘多用户场景中，如何协同优化**批处理调度（Batching）**、**流水线并行（Pipelining）**以及**每个用户的草拟令牌长度（Draft Length）**，以在资源受限和通信延迟存在的环境下实现系统吞吐量的最大化。

### 2. 方法论
*   **核心思想**：提出 **DiP-SD** 框架，利用两个维度的并行性：
    1.  **设备级分布式草拟**：多个边缘设备并行运行小模型生成草拟令牌。
    2.  **阶段级流水线验证**：将用户请求分组（Batching），在服务器验证当前批次的同时，其他设备并行生成下一轮的草拟令牌，从而消除计算气泡。
*   **关键技术细节**：
    *   **系统模型**：构建了一个包含本地草拟、批处理验证、同步和状态更新四个阶段的循环模型。
    *   **数学建模**：将吞吐量定义为“单位时间内预期接受的令牌数”，建立了一个分式混合整数规划（Fractional Mixed-Integer Program）问题。
    *   **联合优化算法**：
        *   **外层循环**：扫描可能的批次数量 $N$。
        *   **内层交替优化**：固定草拟长度优化用户-批次关联（$x$-子问题）；固定关联通过 Dinkelbach 算法优化整数草拟长度（$l$-子问题）。
        *   使用 SCIP 求解器处理生成的混合整数线性规划（MILP）。

### 3. 实验设计
*   **场景设置**：模拟边缘部署环境，用户设备运行 Qwen3-1.7B（草拟模型），边缘服务器运行 Qwen3-32B（目标模型）。
*   **Benchmark 与对比方法**：
    *   **AD**：传统的自回归解码（无投机，无批处理）。
    *   **AD w/ greedy batching**：带贪婪批处理的自回归解码。
    *   **DiP-SD w/o batching**：仅优化草拟长度，不进行批处理。
    *   **DiP-SD w/ fixed $l_m=7$**：固定草拟长度，仅优化批处理关联。
*   **测试维度**：涵盖同质用户（改变用户数 $M$、接受率 $\alpha$）和异质用户（改变前缀长度、接受率、硬件延迟系数）等多种实验。

### 4. 资源与算力
*   **硬件设备**：
    *   **用户端**：NVIDIA GeForce RTX 3090（用于分析草拟模型延迟）。
    *   **边缘服务器端**：NVIDIA A100-80GB（用于分析验证模型延迟）。
*   **算力实现**：论文通过在上述硬件上进行性能分析（Profiling）获取延迟参数（如仿射函数系数），随后在数值仿真环境中进行大规模实验。未提及具体的训练时长，因为该研究侧重于推理阶段的调度优化而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组数值仿真实验，包括用户数从 2 增加到 14 的扩展性测试，以及针对接受率、前缀长度和硬件异构性的敏感度分析。
*   **充分性评价**：实验设计较为全面，对比了多种消融变体（如固定长度、无批处理等），能够客观证明流水线并行与动态长度优化的必要性。但由于主要基于数值仿真，其实际系统中的动态抖动（如网络波动）表现尚需实测验证。

### 6. 主要结论与发现
*   **性能提升**：DiP-SD 相比原生自回归解码（AD）实现了高达 **17.89倍** 的吞吐量提升；相比带贪婪批处理的 AD 提升了 **1.93倍**。
*   **关键因素**：批处理在多用户场景下至关重要，而自适应草拟长度能显著平衡计算开销与接受收益。
*   **流水线增益**：通过流水线化处理，系统能够有效吸收设备端草拟和通信带来的延迟，使吞吐量随用户数增加呈近线性增长。

### 7. 优点
*   **首创性**：首次在分布式投机解码中引入了批次级的流水线并行机制。
*   **理论严谨**：将复杂的调度问题转化为数学规划问题，并给出了高效的交替优化求解算法。
*   **实用性强**：考虑了边缘端的内存限制（KV Cache）和通信开销，贴近真实部署需求。

### 8. 不足与局限
*   **静态假设**：实验假设工作负载是平稳的（Stationary），但在真实边缘场景中，用户请求的到达和退出是动态的，模型参数可能随时间剧烈波动。
*   **通信模型简化**：将通信延迟视为常数，未深入探讨无线信道衰落或网络拥塞对流水线同步的影响。
*   **求解开销**：虽然使用了 MILP 求解器，但在用户规模极大时，频繁求解优化问题的计算开销可能会抵消部分推理加速收益。

（完）
