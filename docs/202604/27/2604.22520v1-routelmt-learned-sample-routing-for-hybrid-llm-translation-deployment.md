---
title: "RouteLMT: Learned Sample Routing for Hybrid LLM Translation Deployment"
title_zh: RouteLMT：面向混合大语言模型翻译部署的学习型样本路由
authors: "Yingfeng Luo, Hongyu Liu, Dingyang Lin, Kaiyan Chang, Chenglong Wang, Bei Li, Quan Du, Tong Xiao, Jingbo Zhu"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22520v1"
tags: ["query:llm"]
score: 8.0
evidence: 通过学习样本路由优化大语言模型服务的吞吐量和延迟
tldr: 针对大语言模型翻译部署成本高昂的问题，本文提出RouteLMT框架。该方法将路由建模为预算分配问题，通过预测大模型相对于小模型的“边际收益”来决定路由决策。RouteLMT利用小模型的内部表示进行高效预测，无需外部模型或解码过程，在多项实验中实现了更优的质量-成本平衡，并能通过防御机制有效降低翻译质量回退风险。
selection_source: fresh_fetch
motivation: 现有的混合翻译系统路由策略多依赖启发式规则或绝对质量评估，无法准确衡量大模型相比小模型带来的实际质量提升（边际收益）。
method: 提出RouteLMT，通过探测小模型的提示词Token表示来预测大模型的边际收益，从而在预算约束下实现最优路由决策。
result: 实验表明RouteLMT在质量-预算帕累托前沿上显著优于启发式和质量评估基线，且其防御变体能有效减少严重的质量损失。
conclusion: 预测模型间的相对增益是实现高效混合LLM部署的关键，RouteLMT为低成本、高质量的翻译部署提供了有效且高效的方案。
---

## 摘要
大语言模型（LLMs）在机器翻译（MT）领域取得了显著的性能，但大规模部署这些模型的成本依然高得令人望而却步。一种被广泛采用的补救方案是混合系统范式，它通过使用小模型处理大部分请求，并有选择地将一小部分请求路由到大模型，从而平衡成本和质量。然而，现有的路由策略通常依赖于启发式方法、外部预测器或绝对质量评估，这些方法无法捕捉到大模型相对于小模型是否真正提供了值得的改进。在本文中，我们将路由建模为一个预算分配问题，并将边际收益（即大模型相对于小模型的改进程度）确定为预算决策的最优信号。基于此，我们提出了 RouteLMT（面向基于 LLM 的机器翻译的路由），这是一种高效的模型内路由器，它通过探测小翻译器的提示词标记（prompt-token）表示来预测这种预期收益，而无需外部模型或假设解码。大量实验表明，我们的 RouteLMT 优于启发式方法、质量/难度评估基准，实现了更优的质量-预算帕累托前沿。此外，我们分析了回归风险，并展示了一个简单的受保护变体可以减轻严重的质量损失。

## Abstract
Large Language Models (LLMs) have achieved remarkable performance in Machine Translation (MT), but deploying them at scale remains prohibitively expensive. A widely adopted remedy is the hybrid system paradigm, which balances cost and quality by serving most requests with a small model and selectively routing a fraction to a large model. However, existing routing strategies often rely on heuristics, external predictors, or absolute quality estimation, which fail to capture whether the large model actually provides a worthwhile improvement over the small one. In this paper, we formulate routing as a budget allocation problem and identify marginal gain, i.e., the large model's improvement over the small model, as the optimal signal for budgeted decisions. Building on this, we propose \textbf{RouteLMT} (routing for LLM-based MT), an efficient in-model router that predicts this expected gain by probing the small translators prompt-token representation, without requiring external models or hypothesis decoding. Extensive experiments demonstrate that our RouteLMT outperforms heuristics, quality/difficulty estimation baselines, achieving a superior quality-budget Pareto frontier. Furthermore, we analyze regression risks and show that a simple guarded variant can mitigate severe quality losses.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RouteLMT** 的新型路由框架，旨在优化混合大语言模型（LLM）翻译系统的部署效率。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：虽然大语言模型在机器翻译（MT）中表现优异，但全量部署大模型（如 8B+ 参数）成本极高且延迟大。工业界常采用“混合部署”：小模型处理简单请求，大模型处理复杂请求。
*   **核心问题**：现有的路由策略（如基于长度的启发式规则或基于质量评估 QE 的模型）存在两个痛点：
    1.  **目标错位**：它们关注输入难度或小模型的绝对质量，而忽略了“大模型是否能比小模型做得更好”（即边际收益）。
    2.  **开销过大**：许多方法需要先让小模型生成翻译（解码）再进行评估，或者需要部署额外的外部路由模型。
*   **研究意义**：本文旨在寻找一种既轻量（无需解码、无需外部模型）又能精准识别“高增益样本”的路由机制。

### 2. 方法论
*   **核心思想**：将路由建模为**预算分配问题**。在固定的大模型调用预算下，优先路由那些“大模型相比小模型质量提升最大”的样本。
*   **关键技术细节**：
    *   **边际收益（Marginal Gain）**：定义为 $g = q_{large} - q_{small}$。路由器的目标是预测这个 $g$ 值。
    *   **模型内路由器（In-model Router）**：RouteLMT 直接嵌入在小模型中。它提取小模型在处理翻译提示词（Prompt）最后一个 Token 时的隐藏层表示（Hidden State）。
    *   **轻量化训练**：在小模型上添加一个简单的线性回归头，使用 **LoRA** 插件进行参数高效微调。
    *   **推理流程**：输入源句子 $\rightarrow$ 小模型 Prefill（预填充） $\rightarrow$ 提取特征并预测增益 $\rightarrow$ 根据增益分数决定是继续由小模型解码，还是直接切换到大模型。

### 3. 实验设计
*   **数据集**：
    *   训练集：ComMT（通用翻译拆分，约 10 万条）。
    *   测试集：FLORES-200、WMT24++、BOUQuET。涵盖通用、医疗、口语三个领域。
*   **语言对**：中英互译（En $\leftrightarrow$ Zh）、俄英互译（En $\leftrightarrow$ Ru）。
*   **模型配置**：小模型（$M_s$）为 LMT-60-0.6B，大模型（$M_\ell$）为 LMT-60-8B。
*   **对比方法（Baselines）**：
    *   **启发式**：随机路由、长度（Length）、稀有度（Rarity）、熵（Entropy）。
    *   **学习型外部模型**：XLM-R（预测增益或质量）、sentinel-src（专门的难度估计模型）。
    *   **变体对比**：RouteLMT-Q（预测绝对质量） vs RouteLMT-Δ（预测边际收益，即本文方法）。

### 4. 资源与算力
*   **训练配置**：使用了 LoRA（Rank=8, Alpha=32），全线性层适配，训练 1 个 Epoch，Batch Size 为 64。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但从模型规模（0.6B）和训练数据量（10万条）来看，该训练过程属于轻量级，通常单张消费级显卡（如 RTX 3090/4090）即可在数小时内完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个翻译方向、3 个不同领域（跨领域泛化测试）、多种预算比例（0% 到 100% 的路由率）。
*   **充分性评价**：实验设计较为充分。作者不仅对比了最终的翻译质量（Pareto 前沿），还引入了 Spearman 相关系数、HitRate@p（命中率）等指标评估路由器的排序能力。此外，还进行了风险分析（回归分析）和案例研究，增强了结果的可信度。

### 6. 主要结论与发现
*   **增益预测优于质量预测**：直接预测“大模型带来的提升”比预测“小模型做得有多差”更能有效分配预算。
*   **模型内特征更有效**：利用小模型自身的内部表示（In-model）比使用外部编码器（如 XLM-R）效果更好，因为内部表示包含了模型对特定输入的“感知”。
*   **帕累托前沿领先**：在相同的计算预算下，RouteLMT 达到的翻译质量显著高于所有基线方法。
*   **风险可控**：虽然任何路由都有可能导致质量回退（Regression），但通过引入简单的“质量守卫（Guarded variant）”机制可以显著降低严重损失。

### 7. 优点（亮点）
*   **高效性**：实现了“Pre-route”（预路由），无需等待小模型解码完成即可做决策，极大地降低了系统延迟。
*   **方向感知**：由于特征提取自翻译 Prompt，路由器天然具备处理多语言对的能力，无需为每个方向单独建模。
*   **理论严谨**：将路由问题转化为边际收益最大化的数学模型，为启发式方法提供了理论替代方案。

### 8. 不足与局限
*   **指标依赖**：训练监督信号依赖于自动评价指标（如 XCOMET），可能存在指标本身的偏见。
*   **模型对限制**：实验仅在 0.6B 和 8B 这一对模型上验证，尚未证明在更大规模差距（如 1B vs 70B）或不同架构模型间的普适性。
*   **静态预算**：研究主要集中在固定样本比例路由，未深入探讨动态延迟约束下的实时路由策略。

（完）
