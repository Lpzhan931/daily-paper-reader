---
title: Unlocking the Edge deployment and ondevice acceleration of multi-LoRA enabled one-for-all foundational LLM
title_zh: 解锁支持多 LoRA 的通用基础大语言模型的边缘部署与端侧加速
authors: "Sravanth Kodavanti, Sowmya Vajrala, Srinivas Miriyala, Utsav Tiwari, Uttam Kumar, Utkarsh Kumar Mahawar, Achal Pratap Singh, Arya D, Narendra Mutyala, Vikram Nelvoy Rajendiran, Sharan Kumar Allur, Euntaik Lee, Dohyoung Kim, HyeonSu Lee, Gyusung Cho, JungBae Kim"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18655v2"
tags: ["query:llm"]
score: 9.0
evidence: 硬件感知的端侧大模型高效推理框架及多流解码机制
tldr: 本研究针对智能手机端部署大语言模型的内存和延迟挑战，提出了一种硬件感知的多LoRA推理框架。通过在三星Galaxy S24/S25上集成动态LoRA输入、多流解码机制（实现一次推理生成多种风格）以及动态自投机解码（DS2D）技术，并结合INT4量化，实现了在支持9种语言和8个任务的同时，将内存和延迟优化了4-6倍，显著提升了移动端生成式AI的商用可行性。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在智能手机端部署时面临的内存受限、高延迟及运行时灵活性不足等核心工程难题。
method: 集成多LoRA动态切换框架，采用多流解码生成风格变体，并结合动态自投机解码（DS2D）与INT4量化技术。
result: 在三星Galaxy S24/S25上实现了4-6倍的内存与延迟优化，解码速度提升2.3倍，且支持9种语言和8类任务。
conclusion: 该研究验证了在边缘设备上高效部署多用途大语言模型的技术可行性，推动了移动端生成式AI的商业化进程。
---

## 摘要
在智能手机上部署大语言模型（LLMs）面临着严峻的工程挑战，主要源于对内存、延迟和运行时灵活性的严格限制。在本研究中，我们提出了一个硬件感知框架，用于在分别搭载 SM8650 和 SM8750 高通芯片组的 Samsung Galaxy S24 和 S25 设备上，实现基于 LLaMA 的多语言基础模型的高效端侧推理，并支持多种应用场景。我们的方法将特定应用的 LoRA 作为运行时输入集成到单个冻结的推理图中，从而在无需重新编译或产生额外内存开销的情况下实现动态任务切换。我们进一步引入了一种多流解码机制，能够在单次前向传递中并发生成多种风格变体（如正式、礼貌或幽默的回复），将延迟降低了高达 6 倍。为了加速 Token 生成，我们应用了动态自投机解码（DS2D），这是一种基于树的策略，无需草稿模型即可预测未来的 Token，使解码时间提速高达 2.3 倍。结合 INT4 量化和架构级优化，我们的系统在保持 9 种语言和 8 项任务准确性的同时，在内存和延迟方面实现了 4-6 倍的整体提升。这些结果证明了在边缘设备上部署多场景 LLM 的实际可行性，推动了生成式人工智能在移动平台上的商业化进程。

## Abstract
Deploying large language models (LLMs) on smartphones poses significant engineering challenges due to stringent constraints on memory, latency, and runtime flexibility. In this work, we present a hardware-aware framework for efficient on-device inference of a LLaMA-based multilingual foundation model supporting multiple use cases on Samsung Galaxy S24 and S25 devices with SM8650 and SM8750 Qualcomm chipsets respectively. Our approach integrates application-specific LoRAs as runtime inputs to a single frozen inference graph, enabling dynamic task switching without recompilation or memory overhead. We further introduce a multi-stream decoding mechanism that concurrently generates stylistic variations - such as formal, polite, or jovial responses - within a single forward pass, reducing latency by up to 6x. To accelerate token generation, we apply Dynamic Self-Speculative Decoding (DS2D), a tree-based strategy that predicts future tokens without requiring a draft model, yielding up to 2.3x speedup in decode time. Combined with quantization to INT4 and architecture-level optimizations, our system achieves 4-6x overall improvements in memory and latency while maintaining accuracy across 9 languages and 8 tasks. These results demonstrate practical feasibility of deploying multi-use-case LLMs on edge devices, advancing the commercial viability of Generative AI in mobile platforms.

---

## 论文详细总结（自动生成）

这篇论文由三星研究院（印度与韩国）的研究团队撰写，重点介绍了如何将支持多任务（Multi-LoRA）的基础大语言模型（LLM）高效部署在智能手机端。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在移动设备（如智能手机）上部署 LLM 面临内存受限、推理延迟高以及运行时灵活性差（难以在不同任务间快速切换）的严峻挑战。
*   **研究动机**：传统的 LoRA 适配通常在训练后静态合并到模型中，这导致每个任务都需要独立的模型副本或频繁的重新编译。为了实现“一个模型服务所有任务（One-for-all）”，需要一种硬件感知的框架，在不增加内存负担的前提下，支持多任务动态切换和极速推理。

### 2. 方法论：核心思想与关键技术
论文提出了一个综合性的端侧加速框架，包含以下核心技术：
*   **多 LoRA 动态切换（Multi-LoRA Enablement）**：
    *   将 LoRA 权重视为推理图的**运行时输入（Placeholder）**，而非静态合并。
    *   通过在冻结的基础模型图中预留占位符，实现了在不重新编译图的情况下，仅通过更换输入权重即可完成任务切换（如从“语法纠错”切换到“风格转换”）。
*   **硬件感知优化（Hardware-aware Optimizations）**：
    *   **算子转换**：将多头注意力（MHA）分解为并行的单头注意力（SHA），并将线性层转换为 1x1 卷积，以利用移动端 NPU/GPU 对卷积运算的深度优化。
    *   **量化**：采用量化感知训练（QAT），将权重压缩至 **INT4**，激活值保持 INT8，实现 3 倍以上的内存压缩。
*   **并发 Token 生成（CTG）**：
    *   针对需要同时提供多种回复风格（如正式、礼貌、幽默）的场景，设计了**掩码解码方案**。
    *   通过共享 KV-Cache 和张量布局，在单次前向传递中并发生成 8 个输出流，将延迟降低了 6 倍。
*   **动态自投机解码（DS2D）**：
    *   基于前缀微调（Prefix Tuning）使模型具备半自回归（SAR）能力。
    *   采用**树状分支策略**（如 3,2 分支配置）在没有独立草稿模型的情况下预测未来 Token，通过 NPU 并行验证，提升解码速度。

### 3. 实验设计与 Benchmark
*   **实验场景**：在 **Samsung Galaxy S24 (SM8650)** 和 **S25 (SM8750)** 旗舰手机上进行实测。
*   **模型规模**：测试了基于 LLaMA 架构的 **1B** 和 **3B** 参数模型。
*   **任务与语言**：涵盖 8 类任务（纠错、风格转换、智能回复、摘要、健康能量评分等）和 9 种语言（韩、英、德、西、法、意、荷、中、日）。
*   **对比基准**：
    *   对比了全精度（FP32/FP16）与量化（INT4）模型的准确率。
    *   对比了有无 DS2D 加速的解码速度（Tokens/sec）。
    *   对比了三种不同的 LoRA 实现方案（多图切换、掩码过滤、权重输入）。

### 4. 资源与算力
*   **推理算力**：明确指出使用了高通骁龙 8 Gen 3 (SM8650) 和 8 Gen 4 (SM8750) 的 **NPU** 进行加速。
*   **训练算力**：论文**未明确说明**训练这些基础模型和 LoRA 所使用的具体 GPU 型号、数量或训练时长，这部分属于三星的专有技术细节。

### 5. 实验数量与充分性
*   **实验规模**：实验覆盖了从 1B 到 3B 的模型跨度，并在真实商用手机上进行了详尽的性能分析（包括首字延迟、每秒 Token 数、峰值内存等）。
*   **充分性**：论文提供了 9 种语言在不同任务下的准确率对比（使用 G-Eval 和 GPT-4.5 作为评估器），并进行了消融实验（如不同分支配置对 DS2D 性能的影响）。实验结果基于真实硬件测量，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能飞跃**：通过 INT4 量化和架构优化，系统实现了 **4-6 倍** 的整体内存和延迟改进。
*   **加速效果**：DS2D 技术使解码速度提升了 **2-2.3 倍**；多流解码（CTG）在生成 8 种风格时比串行生成快 6 倍。
*   **精度保持**：量化后的模型在 9 种语言上的相对准确率保持在 FP32 模型的 95% 以上，部分任务甚至有所提升。
*   **商用落地**：该框架已成功应用于 Galaxy S25 系列的商用功能（Galaxy AI）中。

### 7. 优点与亮点
*   **极高的实用性**：不同于纯理论研究，该工作直接面向商用部署，解决了端侧多任务共存的痛点。
*   **创新的解码机制**：CTG 机制巧妙利用了端侧 NPU 的并行能力，解决了风格化写作中重复推理的浪费问题。
*   **无需草稿模型的投机解码**：DS2D 避免了额外加载草稿模型带来的内存开销，非常适合内存受限的移动端。

### 8. 不足与局限
*   **LoRA 维度限制**：目前的“权重作为输入”方案要求所有任务的 LoRA 秩（Rank）必须一致，缺乏处理异构 LoRA 尺寸的灵活性。
*   **静态预测嵌入**：DS2D 中的预测嵌入在推理过程中是静态的，未根据输入序列动态调整，可能在处理长程依赖或高语义方差输入时降低 Token 接受率。
*   **风格预定义**：多流解码依赖于编译时预定义的风格变体，不支持用户在运行时动态自定义新风格。

（完）
