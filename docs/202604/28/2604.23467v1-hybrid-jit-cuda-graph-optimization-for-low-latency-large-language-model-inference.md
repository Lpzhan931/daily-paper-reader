---
title: Hybrid JIT-CUDA Graph Optimization for Low-Latency Large Language Model Inference
authors: "Divakar Kumar Yadav, Tian Zhao"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23467v1"
tags: ["query:llm"]
score: 10.0
evidence: 混合 JIT-CUDA 图优化用于低延迟大语言模型推理
tldr: 结合 JIT 编译和 CUDA 图技术，减少 LLM 解码过程中的内核启动开销和延迟。
selection_source: fresh_fetch
motivation: 混合 JIT-CUDA 图优化用于低延迟大语言模型推理。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Large Language Models (LLMs) have achieved strong performance across natural language and multimodal tasks, yet their practical deployment remains constrained by inference latency and kernel launch overhead, particularly in interactive, short-sequence settings. This paper presents a hybrid runtime framework that combines Just-In-Time (JIT) compilation with CUDA Graph execution to reduce launch overhead while preserving runtime flexibility during autoregressive decoding. The framework partitions transformer inference into static components executed via CUDA Graph replay and dynamic components handled through JIT-compiled kernels, enabling asynchronous graph capture and reuse across decoding steps.   We evaluate the proposed approach on LLaMA-2 7B using single-GPU, batch-size-one inference across prompt lengths from 10 to 500 tokens. Experimental results show that the hybrid runtime reduces Time-to-First-Token (TTFT) by up to 66.0% and achieves lower P99 latency compared with TensorRT-LLM in this regime. These results indicate that hybrid JIT-CUDA Graph execution can effectively reduce inference latency and variance for short-sequence LLM workloads, making it a practical optimization strategy for latency-sensitive AI applications.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合即时编译（JIT）与 CUDA 图（CUDA Graph）的混合运行时框架，旨在解决大语言模型（LLM）在交互式、短序列推理场景下的高延迟和内核启动开销问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在交互式 LLM 推理（如对话、代码助手）中，GPU 内核的频繁启动（Kernel Launch）和主机-设备（Host-Device）间的协调开销成为了主要的延迟瓶颈，尤其是在小批量（Batch Size = 1）和短序列生成时。
*   **研究动机**：
    *   **CUDA 图的局限性**：虽然能显著减少启动开销，但要求静态张量形状和确定性控制流，无法直接处理 LLM 推理中的动态操作（如变长注意力、KV 缓存更新、随机采样）。
    *   **JIT 编译的局限性**：虽然能处理动态逻辑，但仍保留了部分主机端的调度开销。
    *   **现有框架不足**：如 TensorRT-LLM 等生产级框架在处理极短序列和高度动态逻辑时，仍存在非平凡的延迟抖动。

### 2. 方法论
该论文的核心思想是将 Transformer 推理流水线划分为**静态组件**和**动态组件**，并采用异步协调机制：
*   **静态与动态划分**：
    *   **静态部分**：包括线性投影、层归一化、矩阵乘法和注意力得分计算。这些操作形状固定，使用 **CUDA 图重放（Replay）** 执行，实现零主机干预。
    *   **动态部分**：包括 Token 采样、KV 缓存更新和位置编码扩展。这些操作依赖运行时条件，使用 **PyTorch JIT 编译** 处理。
*   **关键技术细节**：
    *   **异步图捕获**：引入后台线程在不阻塞推理的情况下，针对不同序列长度异步捕获新的 CUDA 图。
    *   **滚动图缓冲区（Rolling Buffer）**：维护一个已捕获图的缓冲区，支持按需重用和过期驱逐，平衡内存占用与性能。
    *   **进程间通信（IPC）**：通过上下文生成器（JIT）和图生成器（CUDA Graph）之间的 IPC 协调执行流。
*   **算法流程**：初始化阶段预捕获短序列图；推理时，若匹配到对应长度的图则直接重放，否则执行 JIT 路径并异步启动新图的捕获。

### 3. 实验设计
*   **模型与场景**：使用 **LLaMA-2 7B** 模型，专注于单 GPU、Batch Size = 1 的交互式推理。
*   **测试范围**：Prompt 长度从 10 到 500 个 Token。
*   **Benchmark 与对比方法**：
    1.  **HuggingFace Transformers**（PyTorch Eager 模式）。
    2.  **TensorRT-LLM**（NVIDIA 官方优化框架）。
    3.  **Hybrid JIT + CUDA Graph**（本文提出的方法）。
*   **评估指标**：首字延迟（TTFT）、P99 逐 Token 解码延迟。

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA H100 GPU** (94 GB HBM3)。
*   **软件栈**：PyTorch 2.3, CUDA 12.4, TensorRT-LLM 1.0。
*   **训练/运行时间**：文中未提及模型训练时长（使用预训练权重），推理延迟测量基于 1000 次迭代的平均值和分位数。

### 5. 实验数量与充分性
*   **实验规模**：针对 11 种不同的 Prompt 长度（10-500）进行了详尽测试，每组配置运行 1000 次以确保统计显著性。
*   **消融实验**：论文进行了消融研究，分别验证了“禁用异步图生成”和“移除动态部分 JIT 编译”对延迟的影响。
*   **充分性评价**：实验设计较为充分，对比了行业标杆 TensorRT-LLM，且涵盖了 TTFT 和尾部延迟（P99），能够客观反映系统在低延迟需求下的表现。

### 6. 主要结论与发现
*   **TTFT 显著降低**：相比 PyTorch Eager，首字延迟降低了高达 66.0%；相比 TensorRT-LLM，在短序列下也表现出更低的绝对延迟。
*   **尾部延迟优化**：P99 延迟平均比 TensorRT-LLM 降低 20.2%，比 PyTorch Eager 降低 43.5%，证明了混合运行时在减少延迟抖动方面的有效性。
*   **线性扩展性**：混合运行时在 50-500 Token 范围内表现出更平滑的延迟增长曲线，受主机端调度干扰更小。

### 7. 优点
*   **灵活性与性能兼顾**：无需像 TensorRT-LLM 那样编写复杂的 C++ 算子插件，通过 Python 侧的划分即可实现接近底层的优化。
*   **异步机制**：通过异步捕获解决了 CUDA 图无法处理变长序列的痛点，避免了捕获过程阻塞推理。
*   **内存效率**：通过共享激活工作区和滚动缓冲区，在提升性能的同时控制了显存占用。

### 8. 不足与局限
*   **形状爆炸问题**：CUDA 图要求形状完全匹配，随着序列长度增加，需要捕获的图数量增多，可能导致显存压力增大。
*   **并发限制**：受限于 PyTorch 当前 cuBLAS 集成的全局上下文，图捕获的并发能力尚未完全释放。
*   **适用范围**：目前仅验证了单 GPU 和 500 Token 以内的场景，尚未探讨多 GPU 并行（如 Tensor Parallelism）或长文本（数千 Token）下的表现。
*   **随机性处理**：随机采样等操作必须严格隔离在图之外，这在一定程度上限制了全流程图优化的潜力。

（完）
