---
title: "FlatAttention: Dataflow and Fabric Collectives Co-Optimization for Large Attention-Based Model Inference on Tile-Based Accelerators"
title_zh: FlatAttention：基于瓦片结构加速器的大型注意力机制模型推理的数据流与织构集合通信协同优化
authors: "Chi Zhang, Luca Colagrande, Renzo Andri, Luca Benini"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02110v1"
tags: ["query:llm"]
score: 8.0
evidence: 大模型推理的高效注意力机制
tldr: 针对混合专家模型（MoE）推理中Attention计算占比增加的问题，本文提出FlatAttention。这是一种专为瓦片式（Tile-based）加速器设计的注意力机制数据流，通过将集合通信原语集成到片上网络织网中，显著减少了高带宽内存（HBM）访问。该方法在多种注意力变体上实现了极高的硬件利用率，相比FlashAttention-3和GH200有显著的性能提升，并有效优化了DeepSeek-v3等大模型的端到端推理表现。
selection_source: fresh_fetch
motivation: 随着MoE模型推理中Attention计算占比提升，如何在瓦片式加速器上通过优化数据流来减少HBM访问瓶颈成为关键。
method: 提出FlatAttention数据流，利用片上网络织网集成的集合通信原语来优化瓦片间的数据传输，从而最小化昂贵的HBM访问。
result: "在多种Attention变体上实现高达92.3%的利用率，相比FlashAttention-3提速4.1倍，并在DeepSeek-v3推理中实现1.9倍的吞吐量提升。"
conclusion: FlatAttention通过软硬件协同优化，证明了在瓦片式架构上通过优化片上通信数据流可以大幅提升大模型推理的效率和吞吐量。
---

## 摘要
在混合专家（MoE）模型的推理过程中，注意力机制在总计算量中所占的比例日益增大，使得高效加速变得至关重要。新兴的用于大模型推理的领域专用加速器正转向芯片级和晶圆级基于瓦片的架构。瓦片包含大型矩阵和向量引擎，并通过片上互连进行连接，支持瓦片间通信以缓解瓦片到主存的流量瓶颈。因此，数据流管理对于实现高利用率至关重要。我们提出了 FlatAttention，一种适用于基于瓦片加速器的现代注意力变体的数据流。FlatAttention 通过利用集成在片上网络织构中的集合原语，最大限度地减少了昂贵的高带宽内存（HBM）访问，实现了高达 92.3% 的利用率，相比 FlashAttention-3 提速 4.1 倍，并降低了 16 倍的 HBM 流量。在峰值性能与 NVIDIA GH200 相当的 32x32 瓦片配置下，FlatAttention 可推广至多种注意力变体，在计算受限的注意力机制上实现了平均 86% 的利用率，在内存受限的注意力机制上实现了 78% 的 HBM 带宽利用率，相比 GH200 上的注意力实现平均提速 1.9 倍。最后，我们在晶圆级多芯片系统上评估了使用 FlatAttention 的端到端 DeepSeek-v3 FP8 解码，尽管峰值系统性能比现有最先进方案低 1.5 倍，但仍实现了 1.9 倍的系统吞吐量提升和 1.4 倍的单用户 Token 输出延迟降低。

## Abstract
Attention accounts for an increasingly dominant fraction of total computation during inference for mixture-of-experts (MoE) models, making efficient acceleration critical. Emerging domain-specific accelerators for large model inference are shifting toward chip-scale and wafer-scale tile-based architectures. Tiles contain large matrix and vector engines and are connected through on-chip interconnects, which support tile-to-tile traffic to reduce the tile-to-main-memory traffic bottleneck. Hence, dataflow management is crucial to achieve high utilization. We propose FlatAttention, a dataflow for modern attention variants on tile-based accelerators. FlatAttention minimizes expensive high-bandwidth memory (HBM) accesses by exploiting collective primitives integrated into the on-chip network fabric, achieving up to 92.3% utilization, 4.1x speedup over FlashAttention-3, and 16x lower HBM traffic. On a 32x32 tile configuration with peak performance comparable to NVIDIA GH200, FlatAttention generalizes across multiple attention variants, achieving an average of 86% utilization for compute-bound attentions and 78% HBM bandwidth utilization for memory-bound ones, resulting in an average 1.9x speedup over attention implementations on GH200. Finally, we evaluate end-to-end DeepSeek-v3 FP8 decoding with FlatAttention on a wafer-scale multi-die system, achieving a 1.9x improvement in system throughput and a 1.4x reduction in per-user token output latency, despite operating with 1.5x lower peak system performance compared to the state-of-the-art solution.

---

## 论文详细总结（自动生成）

### FlatAttention：基于瓦片结构加速器的大型注意力机制模型推理的数据流与织构集合通信协同优化

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）从传统的解码器架构演进到混合专家模型（MoE，如 DeepSeek-v3），注意力机制（Attention）在推理计算中的占比显著增加（在 DeepSeek-v3 解码阶段可达 71%），成为新的计算瓶颈。现有的 GPU 方案（如 NVIDIA GH200）虽然有 FlashAttention 等优化，但在实际利用率上仍存在较大缺口（仅达到理论峰值的 26%-64%），且 HBM 内存访问成本极高。
论文的研究动机在于：针对新兴的**瓦片式（Tile-based）**和**晶圆级（Wafer-scale）**加速器架构，如何通过协同设计数据流与片上网络（NoC）的集合通信原语，减少昂贵的 HBM 访问，提升硬件利用率，从而降低大模型推理的单位 Token 成本。

#### 2. 论文提出的方法论：FlatAttention
**核心思想**：将多个计算瓦片（Tiles）视为一个统一的计算单元（Group），利用片上网络硬件支持的集合通信（如多播 Multicast 和归约 Reduction）在瓦片间共享数据，从而在不增加 HBM 访问的前提下，扩大注意力机制的有效分块大小（Block Size）。

**关键技术细节**：
*   **硬件支持的集合原语**：在 NoC 路由器中集成硬件级的数据复制和归约功能，相比传统的软件点对点通信，显著降低了延迟。
*   **二维分块策略**：将注意力计算任务分配给 $G_x \times G_y$ 的瓦片组。对角线瓦片负责从 HBM 加载数据，并通过行/列多播分发给组内其他瓦片。
*   **异步调度（FlatAsync）**：利用 DMA、向量引擎和矩阵引擎的并行性，将 Softmax 计算、数据搬运与矩阵乘法（GEMM）重叠执行，确保矩阵引擎（计算核心）始终处于高负载状态。
*   **通用性适配**：不仅支持标准的多头注意力（MHA），还通过“权重吸收”等数学变换，高效适配了分组查询注意力（GQA）和多头潜在注意力（MLA）。

#### 3. 实验设计
*   **场景与模型**：涵盖了 LLM 推理的预填充（Prefill）和解码（Decode）阶段。重点评估了 MHA、GQA 和 DeepSeek-v3 使用的 MLA 变体。
*   **Benchmark（基准测试）**：
    *   **软件算法**：FlashAttention-2、FlashAttention-3、FlashMLA。
    *   **硬件平台**：NVIDIA GH200 GPU（作为性能对标）；基于 RTL 校准的瓦片式加速器模拟器（SoftHier）。
*   **对比方法**：对比了使用软件集合通信（顺序或树状）与硬件集合通信的 FlatAttention 性能差异。

#### 4. 资源与算力
*   **模拟平台**：使用了基于事件的 **GVSoC** 全平台模拟器，并针对 RedMulE 矩阵引擎和 FlooNoC 进行了 RTL 级的周期精确校准。
*   **目标硬件配置**：模拟了一个 32×32 瓦片的芯片，峰值性能 988 TFLOPS (FP16)，HBM 带宽 2-4 TB/s，规格与 NVIDIA GH200 相当。
*   **端到端评估**：模拟了一个包含 **64 个加速器芯片**的晶圆级多芯片系统，用于运行 DeepSeek-v3-671B 模型。
*   **训练/推理时长**：文中未提及模型训练，重点在于推理性能的仿真评估。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **校准实验**：验证了模拟器与 RTL 实现的一致性（误差在 0.17% - 12% 之间）。
*   **算子级实验**：测试了不同序列长度（512 到 4096）和头维度（64, 128）下的多种 Attention 变体。
*   **消融实验**：研究了瓦片组规模（4x4 到 32x32）对利用率的影响，识别了“过度扁平化（Over-flattening）”导致的性能下降。
*   **端到端实验**：针对 DeepSeek-v3-671B 进行了完整的解码性能分析，包括吞吐量与延迟（TPOT）的权衡。
**评价**：实验设计覆盖了从底层硬件原语到顶层端到端应用的完整链路，对比对象均为目前工业界最先进的方案，实验结果具有较高的说服力和客观性。

#### 6. 论文的主要结论与发现
*   **性能飞跃**：在相同硬件配置下，FlatAttention 相比 FlashAttention-3 提速达 **4.1 倍**，HBM 流量减少 **16 倍**。
*   **硬件利用率**：在计算受限场景下实现平均 **86%** 的利用率，在内存受限场景下实现 **78%** 的 HBM 带宽利用率。
*   **端到端优势**：在 DeepSeek-v3 推理中，尽管系统峰值性能比 96 块 H800 组成的系统低 1.5 倍，但 FlatAttention 实现了 **1.9 倍** 的吞吐量提升和 **1.4 倍** 的延迟降低。
*   **协同优化必要性**：单纯增加瓦片数量而不优化片上通信会导致性能下降，硬件支持的集合通信是发挥瓦片架构潜力的关键。

#### 7. 优点
*   **软硬协同深度优化**：不只是改进算法，而是深入到 NoC 架构层面进行原语级优化。
*   **极高的通用性**：成功将复杂的 MLA 架构简化并适配到统一的 FlatAttention 框架中。
*   **显著的能效潜力**：通过大幅减少 HBM 访问（这是 GPU 功耗的大头），预示了极佳的能效比。
*   **前瞻性**：针对晶圆级芯片这一前沿趋势提供了切实可行的数据流方案。

#### 8. 不足与局限
*   **硬件依赖性**：该方法高度依赖于具有特定硬件集合通信支持的 NoC 架构，在现有的通用 GPU 上难以直接应用。
*   **模拟局限**：虽然经过了校准，但核心结论仍基于模拟器，实际流片后的物理约束（如布线拥塞、热量分布）可能对性能产生额外影响。
*   **互连拓扑限制**：在多芯片晶圆级系统中，目前仅探讨了 2D Mesh 拓扑，对于更复杂的互连结构（如多平面网络）的讨论较少。
*   **软件栈挑战**：实现这种精细的数据流控制需要复杂的编译器支持和手动优化，对开发者要求较高。

（完）
