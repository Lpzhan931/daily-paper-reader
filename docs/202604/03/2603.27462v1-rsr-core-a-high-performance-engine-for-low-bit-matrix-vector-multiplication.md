---
title: "RSR-core: A High-Performance Engine for Low-Bit Matrix-Vector Multiplication"
title_zh: RSR-core：一种用于低比特矩阵-向量乘法的高性能引擎
authors: "Mohsen Dehghankar, Abolfazl Asudeh"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27462v1"
tags: ["query:llm"]
score: 8.0
evidence: LLM推理的高效矩阵向量乘法
tldr: RSR-core是一个针对低比特（1比特或1.58比特）矩阵-向量乘法的高性能计算引擎。针对现有低比特量化算法在硬件底层实现上的不足，该研究将Redundant Segment Reduction (RSR)算法转化为优化的CPU和CUDA内核。RSR-core支持二进制和三进制权重矩阵，并集成了HuggingFace生态，显著提升了LLM推理效率，在CPU上实现高达62倍加速，在CUDA上实现1.9倍加速。
selection_source: fresh_fetch
motivation: 现有的低比特量化算法缺乏高效的底层硬件内核实现，导致其理论加速优势难以在实际推理流水线中充分发挥。
method: 开发了RSR-core引擎，通过在CPU和CUDA环境中实现优化的底层内核，将RSR算法集成到生产级的推理框架中。
result: 实验显示，RSR-core在CPU上比PyTorch基准快62倍，在CUDA上为主流三进制大语言模型的Token生成提速1.9倍。
conclusion: RSR-core填补了低比特算法与硬件加速之间的空白，为高效部署低比特大语言模型提供了实用的高性能解决方案。
---

## 摘要
矩阵-向量乘法是神经网络、向量数据库和大型语言模型中的基础构建模块，特别是在推理过程中。因此，高效的矩阵-向量乘法引擎能直接转化为更高效的推理。近期的研究探索了模型权重的低比特量化，其中矩阵使用二进制（1比特）或三进制（1.58比特）值表示，而激活值则保持较高的精度。这些表示方式实现了高效的硬件级计算。与此同时，诸如冗余段归约（Redundant Segment Reduction, RSR）之类的算法为加速低比特矩阵-向量乘法提供了理论保证。然而，现有的实现运行在应用层，无法高效地集成到硬件内核中，从而限制了实际性能。为了弥补这一差距，我们提出了 RSR-core，这是一个高性能引擎，它将 RSR 算法实现为针对 CPU 和 CUDA 环境优化的底层内核。RSR-core 支持二进制和三进制权重矩阵与通用向量的高效矩阵-向量乘法，同时支持 RSR 算法在实际推理流水线中的部署。RSR-core 作为一个生产级引擎提供，并集成了 HuggingFace 以用于预处理低比特模型和运行加速推理。实验结果表明，与基准 HuggingFace PyTorch 乘法相比，该引擎具有显著的性能提升，在 CPU 上实现了高达 62 倍的加速，在 CUDA 上针对主流三进制大语言模型的 Token 生成实现了高达 1.9 倍的加速。源代码已在 https://github.com/UIC-InDeXLab/RSR-core 公开。

## Abstract
Matrix-vector multiplication is a fundamental building block in neural networks, vector databases, and large language models, particularly during inference. As a result, efficient matrix-vector multiplication engines directly translate into more efficient inference. Recent work has explored low-bit quantization of model weights, where matrices are represented using binary (1-bit) or ternary (1.58-bit) values while activation is kept in higher precision. These representations enable efficient hardware-level computation. In parallel, algorithms such as Redundant Segment Reduction (RSR) provide theoretical guarantees for accelerating low-bit matrix-vector multiplication. However, existing implementations operate at the application level and cannot be efficiently integrated into hardware kernels, limiting practical performance. To bridge this gap, we present RSR-core, a high-performance engine that implements the RSR algorithm as optimized low-level kernels for both CPU and CUDA environments. RSR-core supports efficient matrix-vector multiplication for binary and ternary weight matrices and general vectors while enabling practical deployment of RSR algorithm in real inference pipelines. RSR-core is provided as a production-ready engine with HuggingFace integration for preprocessing low-bit models and running accelerated inference. Experimental results demonstrate significant performance improvements over baseline HuggingFace PyTorch multiplication, achieving up to 62x speedup on CPU and up to 1.9x speedup for token generation on CUDA for popular ternary LLMs. The source code is publicly available at https://github.com/UIC-InDeXLab/RSR-core.

---

## 论文详细总结（自动生成）

这是一份关于论文《RSR-core: A High-Performance Engine for Low-Bit Matrix-Vector Multiplication》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：在大语言模型（LLM）推理中，矩阵-向量乘法（MVM）是主要的计算瓶颈。虽然低比特量化（如1比特或1.58比特）能显著降低计算开销，且冗余段归约（RSR）算法在理论上能通过利用权重矩阵中的模式冗余来加速计算，但现有的 RSR 实现多处于应用层，缺乏高效的底层硬件内核支持，导致理论上的加速优势无法在实际推理流水线中完全体现。
*   **研究背景**：随着 LLM 规模扩大，推理能耗和资源分配成为关键挑战。低比特量化（Binary/Ternary）已成为主流趋势，急需能够直接集成到生产环境（如 HuggingFace、PyTorch）中的高性能底层算子。

### 2. 方法论
RSR-core 的核心思想是将 RSR 算法转化为针对 CPU 和 GPU 优化的底层内核，其流程分为两个阶段：
*   **离线预处理（Offline Preprocessing）**：
    *   将权重矩阵 $M$ 划分为高度为 $k$ 的水平行块。
    *   识别每个块中相同的列模式（Column Patterns），并使用**计数排序**（复杂度 $O(n + buckets)$）对相同模式进行分组。
    *   生成辅助数据结构：包括列排列索引、组边界和散布（Scatter）索引。
    *   **优化**：将元数据压缩为 16 位整数或位掩码（Bitmasks），以减少推理时的带宽压力。
*   **在线推理（Online Inference）**：
    *   **聚合阶段**：根据预处理的索引，将对应相同列模式的输入向量元素进行预聚合。
    *   **计算与散布阶段**：对每个唯一模式仅执行一次乘法，然后将结果散布到输出向量中。
    *   **内核优化**：在 CPU 上融合了量化与乘法操作，并支持多线程并行；在 CUDA 上，每个线程块处理一个行块，利用共享内存减少写冲突，并跳过零贡献组。

### 3. 实验设计
*   **实验场景**：
    1.  **算子级微基准测试**：直接对比二进制/三进制矩阵与 float32 向量的乘法速度。
    2.  **端到端 LLM 推理**：测试大模型在 Token 生成阶段的吞吐量。
*   **对比方法（Benchmarks）**：
    *   **PyTorch (bfloat16)**：HuggingFace 默认使用的硬件优化内核。
    *   **BitNet**：专门针对低比特矩阵乘法的现有实现。
*   **测试模型**：Falcon3-10B-1.58b、Llama3-8B-1.58b、BitNet-2B-4T 等主流三进制 LLM。

### 4. 资源与算力
*   **算力说明**：论文提到了在 CPU（C 内核）和 GPU（CUDA 内核）环境下进行了测试。
*   **具体型号**：文中**未明确说明**具体的 CPU 型号和 GPU 型号（如 A100 或 H100），仅以“CPU”和“CUDA”作为平台分类展示了加速比。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从底层算子到端到端模型推理的多个维度，测试了不同规模（2B 到 10B 参数）的模型。
*   **充分性评价**：实验设计较为充分，不仅展示了峰值加速比，还通过 Web 界面演示了不同块高度 $k$ 对性能的影响。对比了 PyTorch 官方优化库，证明了其在特定低比特场景下的优越性。不过，缺乏在不同硬件架构（如不同代际的 NVIDIA GPU）上的兼容性对比。

### 6. 主要结论与发现
*   **显著加速**：在 CPU 上，RSR-core 相比 PyTorch 基准实现了高达 **62倍** 的加速；在 CUDA 上，针对三进制 LLM 的 Token 生成速度提升了 **1.9倍**。
*   **精度保持**：RSR 算法通过数学上的等价变换减少运算量，因此推理结果是**完全精确**的，不损失模型精度。
*   **生产可用性**：通过集成 HuggingFace 和提供 `RSRLinear` 模块，该引擎可以无缝替换现有的三进制线性层。

### 7. 优点
*   **底层优化**：填补了 RSR 算法从理论到高性能 C/CUDA 内核实现的空白。
*   **内存高效**：通过位掩码和紧凑元数据设计，减少了推理时的内存访问开销。
*   **易用性强**：提供了完整的预处理、推理及可视化监控界面，降低了非专家用户的使用门槛。
*   **内核融合**：在 CPU 上融合激活量化与 MVM，消除了冗余的 Python 调度开销。

### 8. 不足与局限
*   **预处理开销**：虽然是一次性成本，但对于超大规模模型，预处理仍需一定的时间和存储空间（尽管论文称存储增加极小）。
*   **适用范围**：该引擎专门针对二进制（1-bit）和三进制（1.58-bit）权重，对于更高比特（如 4-bit 或 8-bit）或其他量化格式的适用性有限。
*   **硬件依赖性**：实验表明最优块高度 $k$ 随硬件和矩阵形状变化，用户可能需要针对特定硬件进行参数微调以获得最佳性能。

（完）
