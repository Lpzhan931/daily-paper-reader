---
title: Rethinking KV Cache Eviction via a Unified Information-Theoretic Objective
title_zh: 通过统一的信息论目标重新审视 KV 缓存逐出
authors: "Jiaming Yang, Chenwei Tang, Liangli Zhen, Jiancheng Lv"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25975v1"
tags: ["query:llm"]
score: 9.0
evidence: 基于信息论的KV缓存逐出策略
tldr: 针对大语言模型长文本生成中KV缓存带来的内存瓶颈，本文从信息瓶颈原理出发，提出了一个统一的理论框架。研究发现现有启发式逐出策略均可视为信息容量最大化的不同近似。基于此，作者提出CapKV方法，利用统计杠杆得分实现信息保留的最大化。实验证明CapKV在保持生成质量的同时显著提升了内存效率，为KV缓存管理提供了严谨的理论支撑。
selection_source: fresh_fetch
motivation: 现有的KV缓存逐出策略大多依赖经验启发式规则，缺乏严谨的理论基础来优化长文本生成的内存开销。
method: 基于信息瓶颈原理推导出互信息目标函数，并提出利用统计杠杆得分来最大化保留预测信号的CapKV方法。
result: 实验表明CapKV在多个模型和长文本基准测试中均优于现有方法，实现了更优的内存效率与生成质量平衡。
conclusion: 该研究通过统一的信息论视角重新审视了KV缓存逐出问题，证明了理论驱动的机制在提升大模型推理性能方面的潜力。
---

## 摘要
键值（KV）缓存对于大语言模型推理至关重要，但其内存开销构成了长文本生成的关键瓶颈。现有的逐出策略主要依赖于经验启发式方法，缺乏严谨的理论基础。本研究从信息瓶颈（Information Bottleneck）原则的角度重新审视了 KV 缓存逐出。在注意力机制的线性高斯代理模型下，我们推导出了一个闭式互信息目标，用于表征保留的 KV 缓存子集的有效信息容量。这一公式揭示了广泛的现有逐出策略可以被解释为同一容量最大化原则的不同近似。在此见解的指导下，我们提出了 CapKV，这是一种容量感知逐出方法，通过使用统计杠杆得分（statistical leverage scores）的对数行列式近似，直接以信息保留为目标。该方法用具有理论依据的机制取代了启发式选择，从而保留了最大的预测信号。在多个模型和长文本基准测试上的广泛实验表明，CapKV 一致优于先前的方法，在内存效率和生成保真度之间实现了更好的权衡。

## Abstract
Key-value (KV) caching is essential for large language model inference, yet its memory overhead poses a critical bottleneck for long-context generation. Existing eviction policies predominantly rely on empirical heuristics, lacking a rigorous theoretical foundation. This work rethinks KV cache eviction through the lens of the Information Bottleneck principle. Under a linear-Gaussian surrogate of attention, we derive a closed-form mutual information objective that characterizes the effective information capacity of a retained KV cache subset. This formulation reveals that a wide range of existing eviction strategies can be interpreted as different approximations of the same capacity-maximization principle. Guided by this insight, we introduce CapKV, a capacity-aware eviction method that directly targets information preservation via a log-determinant approximation using statistical leverage scores. This approach replaces heuristic selection with a theoretically grounded mechanism that preserves the maximum predictive signal. Extensive experiments across multiple models and long-context benchmarks show that CapKV consistently outperforms prior methods, achieving a better trade-off between memory efficiency and generational fidelity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking KV Cache Eviction via a Unified Information-Theoretic Objective》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在大语言模型（LLM）的长文本推理中，KV Cache（键值缓存）随上下文长度线性增长，导致巨大的内存瓶颈。
*   **研究动机**：现有的 KV 缓存逐出（Eviction）策略（如 SnapKV、H2O、KeyDiff 等）大多基于经验启发式规则（如注意力分数、向量范数或几何多样性），缺乏严谨的理论基础。
*   **整体含义**：本文试图通过**信息论**（具体为信息瓶颈原则）为 KV 缓存管理建立统一的理论框架，解释为什么现有的各种启发式方法有效，并据此推导出一种更优的、有理论支撑的逐出算法。

### 2. 方法论
*   **核心思想**：将 KV 缓存视为一个通信信道，将逐出问题转化为：在固定容量预算下，选择一个 KV 子集，使未来查询（Query）与模型输出之间的**互信息（Mutual Information）最大化**。
*   **关键技术细节**：
    *   **线性高斯代理模型**：为了使计算可行，作者将注意力机制简化为线性高斯传输模型 $Y = UC KC q + \epsilon$，其中 $q$ 是随机变量（未来查询），$KC$ 和 $UC$ 分别是保留的键和值。
    *   **统一目标函数**：推导出闭式解 $I(q; Y | Z_C) = \frac{1}{2} \log \det(I + \Sigma_{noise}^{-1} U_C K_C \Lambda_Q K_C^\top U_C^\top)$。该公式揭示了信息保留受键的可访问性、值通道强度、查询统计特性和噪声水平四个因素共同影响。
    *   **CapKV 算法流程**：
        1.  **计算查询权重**：利用历史查询的均值作为先验，计算每个 Token 的查询对齐权重 $w_i$。
        2.  **构建容量矩阵**：在值空间构建近似容量矩阵 $A$。
        3.  **统计杠杆得分（Leverage Scores）**：利用矩阵行列式引理，计算每个 KV 对对整体容量的边际贡献（即得分 $s_i$）。
        4.  **贪婪逐出**：保留得分最高的 $B$ 个 Token。
*   **复杂度**：计算复杂度为 $O(Nd^2 + d^3)$，其中 $N$ 是缓存长度，$d$ 是维度。这在长文本场景下比注意力计算本身的开销要小得多。

### 3. 实验设计
*   **数据集/场景**：
    *   **LongBench**：包含 16 个任务，涵盖单文档 QA、多文档 QA、摘要、少样本学习、合成任务和代码任务。
    *   **AIME25**：用于评估推理模型（Nemotron-7B）在多步推理中的表现。
*   **对比方法**：
    *   **EA (Expected Attention)**：基于未来查询分布预测的方法。
    *   **KeyDiff**：基于键向量几何差异的方法。
    *   **SnapKV**：基于局部窗口注意力观察的方法。
    *   **Knorm**：基于 $L_2$ 范数的方法。
    *   **Sink**：保留初始 Token 的方法。
*   **模型**：Qwen3 (4B, 8B, 14B)、Llama 3.1-8B、Mistral-7B、Nemotron-7B。

### 4. 资源与算力
*   **硬件使用**：论文明确指出实验使用了 **4 张 NVIDIA L40S GPU**。
*   **运行效率**：作者测量了不同压缩率下的总生成时间，证明 CapKV 的运行开销与现有的启发式方法处于同一数量级，没有引入显著的推理延迟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 种不同的模型架构上进行了测试。
    *   覆盖了 LongBench 的全部 16 个子任务。
    *   进行了 4 种压缩率（0.25, 0.5, 0.75, 0.9）的对比。
    *   **相关性分析**：专门设计实验验证了“理论容量”与“下游任务表现”之间的 Spearman 秩相关系数（高达 0.75-0.85）。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了最终得分，还通过消融实验验证了超参数 $\tau$（查询感知权重）的影响，并提供了推理阶段动态逐出的鲁棒性测试。

### 6. 主要结论与发现
*   **理论统一性**：现有的多种逐出策略（如基于范数的、基于多样性的）实际上都是本文提出的统一信息容量目标的某种特定近似。
*   **容量预测性能**：保留的 KV 缓存的信息容量与模型在下游任务中的表现呈强正相关。
*   **CapKV 的优越性**：在所有测试的模型和压缩率下，CapKV 一致优于先前的 SOTA 方法，尤其是在极高压缩率（如 0.9）下，性能下降更为平缓。
*   **推理鲁棒性**：在复杂的数学推理任务中，CapKV 表现出比局部启发式方法更强的稳定性。

### 7. 优点
*   **理论高度**：首次将 KV 缓存逐出问题从“经验调优”提升到了“信息论优化”的高度，提供了清晰的数学解释。
*   **全局视角**：不同于以往方法孤立地看待 Token，CapKV 考虑了 Token 之间的冗余性和互补性（通过矩阵行列式体现）。
*   **即插即用**：方法不依赖模型训练，且计算开销低，易于集成到现有的推理流水线中。

### 8. 不足与局限
*   **线性假设**：理论推导基于线性高斯代理模型，这虽然在数学上可处理，但可能无法完全捕捉 Transformer 中复杂的非线性动态。
*   **历史依赖**：查询权重的计算依赖于已观察到的历史查询统计，如果生成过程中发生剧烈的分布偏移（Distribution Shift），该权重的有效性可能会下降。
*   **维度敏感性**：虽然复杂度可控，但在超大维度（$d$ 极大）的模型中，矩阵运算的开销仍需进一步优化。

（完）
