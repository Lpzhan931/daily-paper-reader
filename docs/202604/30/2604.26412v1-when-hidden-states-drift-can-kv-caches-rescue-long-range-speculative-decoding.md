---
title: "When Hidden States Drift: Can KV Caches Rescue Long-Range Speculative Decoding?"
title_zh: 当隐藏状态发生漂移：KV 缓存能否挽救长程投机解码？
authors: "Tianyu Liu, Yuhao Shen, Xinyi Hu, Baolin Zhang, Hengxin Zhang, Jun Dai, Jun Zhang, Shuang Ge, Lei Chen, Yue Li, MingCheng Wan"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26412v1"
tags: ["query:llm-sd"]
score: 8.0
evidence: 投机解码与KV缓存上下文保留
tldr: 投机采样中基于隐藏状态的草稿模型在长距离预测时准确率会下降。本文认为这是因为隐藏状态对上下文进行了有偏压缩，导致后续步骤信息丢失。为此提出了KV-Reuse假设，即复用目标模型的KV缓存以提供更丰富的上下文。通过KVShot框架验证发现，KV复用能提升长距离接受率，但也揭示了浅层模型查询估计难和梯度稀疏等瓶颈，为未来架构设计提供了指导。
selection_source: fresh_fetch
motivation: 现有的基于隐藏状态的草稿模型在长距离投机采样时存在准确率衰减问题，且现有的测试时训练方法无法完全解决。
method: 提出KVShot诊断框架，通过对比隐藏状态复用、KV复用及混合复用三种范式，验证KV缓存对长距离预测的增益。
result: 实验表明KV复用能有效提升长距离接受率，但受限于浅层模型对查询的估计能力和梯度稀疏问题，端到端加速效果仍有待提升。
conclusion: 实现高效的KV感知投机解码需要从简单的测试时训练转向块状训练范式，以克服当前架构的结构性瓶颈。
---

## 摘要
投机解码加速了大语言模型（LLM）的推理，但最先进的（SOTA）基于隐藏状态的草稿模型面临长程衰减问题：即草稿准确率随着投机步数的增加而下降。现有工作将这种衰减归因于训练与推理的不匹配，并提出测试时训练（TTT）作为补救措施，但我们观察到，即使在经过 TTT 训练的草稿模型中，长程衰减依然存在。我们从上下文信息保留的角度重新审视了长程衰减。在隐藏状态重用中，我们认为目标隐藏状态充当了一种有偏的上下文压缩：它根据当前位置的注意力查询聚合历史 token 信息，从而产生一个为即时下一 token 预测而优化的紧凑表示。这种压缩可能会抑制与当前查询相关性较低、但对后续投机步骤至关重要的信息。相比之下，目标模型的 KV 缓存作为显式上下文，保留了完整的逐 token KV 表示。因此，我们提出了 KV 重用假设（KV-Reuse Hypothesis）：允许草稿模型重用目标 KV 缓存可以为长程投机提供更丰富的信号。为了验证这一假设，我们引入了 KVShot，这是一个比较三种重用范式（仅隐藏状态、仅 KV 和混合）的诊断框架。在 Qwen3-8B 上的广泛评估表明，KV 重用提高了长程接受率，尽管在目前的训练流程下，端到端的加速效果仍然有限。我们的分析确定了两个关键的结构瓶颈：浅层草稿模型难以准确估计目标查询，且草稿端的 KV 投影接收到的梯度信号稀疏。这些发现表明，要实现 KV 感知解码的全部潜力，需要超越 TTT，转向块状训练（block-wise training）范式。通过揭示这些瓶颈，KVShot 提供了一个基础的诊断测试平台，并为设计下一代推理架构提供了清晰的路线图。

## Abstract
Speculative decoding accelerates LLM inference, but SOTA hidden-state-based drafters suffer from long-range decay: draft accuracy degrades as the speculative step increases. Existing work attributes this decay to train-inference mismatch and proposes test-time training (TTT) as a remedy, yet we observe that long-range decay persists even in TTT-trained drafters. We revisit long-range decay from the perspective of context information preservation. In hidden-state reuse, we argue the target hidden state acts as a biased context compression: it aggregates historical token information according to the attention query at the current position, yielding a compact representation optimized for immediate next-token prediction. This compression can suppress information less relevant to the current query but important for later speculative steps. In contrast, the target model's KV cache serves as an explicit context, retaining the complete set of token-wise KV representations. We therefore posit the KV-Reuse Hypothesis: allowing the draft model to reuse the target KV cache can provide richer signals for long-horizon drafting. To test this hypothesis, we introduce KVShot, a diagnostic framework that compares three reuse paradigms: hidden-only, KV-only, and hybrid. Extensive evaluations on Qwen3-8B show that KV-Reuse improves long-range acceptance, although end-to-end speedups remain marginal under current training pipelines. Our analysis identifies two key structural bottlenecks: shallow drafters struggle to estimate target queries accurately, and draft-side KV projections receive sparse gradient signals. These findings suggest that realizing the full potential of KV-aware decoding requires moving beyond TTT toward block-wise training paradigms. By exposing these bottlenecks, KVShot provides a foundational diagnostic testbed and a clear roadmap for designing next-generation inference architectures.

---

## 论文详细总结（自动生成）

这篇论文对投机解码（Speculative Decoding）中的长程准确率衰减问题进行了深入的诊断性研究。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在投机解码中，基于隐藏状态（Hidden-state）的草稿模型（如 EAGLE-3、MTP）存在“长程衰减”现象，即随着投机步数 $k$ 的增加，草稿准确率显著下降。
*   **研究动机**：传统观点认为衰减源于“训练-推理不匹配”，但即使采用测试时训练（TTT）对齐分布，衰减依然存在。作者提出，隐藏状态本质上是对上下文的“有偏压缩”，它只保留了对预测“下一个”token 有用的信息，而丢弃了对更远未来 token 重要的信息。
*   **整体含义**：探讨复用目标模型的 KV Cache（保留了完整的 token 级信息）是否能解决这一信息丢失问题，并提升长程投机的准确率。

### 2. 方法论：核心思想与关键技术
*   **核心思想（KV-Reuse 假设）**：KV 缓存保留了未聚合的键值对，草稿模型可以通过自己的查询（Query）重新关注（Re-attention）前缀信息，从而避免隐藏状态带来的信息压缩损失。
*   **KVShot 诊断框架**：
    *   **仅隐藏状态（Hidden-only）**：基准方法，将目标隐藏状态拼接到草稿模型输入。
    *   **仅 KV（KV-only）**：通过交叉注意力（Cross-Attention）让草稿模型直接访问目标 KV 缓存。
    *   **混合重用（Hybrid）**：引入**门控增量规则（Gated Delta Rule）**。公式为：$h_i = n_i + g_i \odot W_\Delta (o_i - n_i)$。其中 $n_i$ 是自注意力输出（隐藏状态路径），$o_i$ 是交叉注意力输出（KV 路径），$g_i$ 是学习到的门控信号，用于控制 KV 信息的注入量。
*   **训练流程**：采用自回归测试时训练（TTT），模拟推理时的隐藏状态漂移。

### 3. 实验设计
*   **目标模型**：Qwen3-8B。
*   **数据集**：
    *   消融实验：ShareGPT（约 7 万样本）。
    *   端到端评估：ShareGPT + UltraChat（共 28 万样本），并使用目标模型重新生成回答以对齐分布。
*   **对比基准（Benchmark）**：1 层的 EAGLE-3 草稿模型。
*   **评估指标**：步进接受率（$\alpha_k$）、平均接受 Token 数（MAT）以及在树状解码配置下的端到端 MAT。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：实验基于 SpecForge 框架实现，训练参数包括 3 个 epoch（在 ShareGPT 上）。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了多维度实验，包括：
    *   KV 注入策略对比（头部拼接 vs. 线性投影）。
    *   草稿模型深度缩放（1 层到 4 层）。
    *   混合重用范式的消融（随机初始化 vs. 预训练权重热启动）。
    *   端到端性能测试。
*   **充分性评价**：实验设计非常系统且具有诊断性。通过控制变量法（相同的 TTT 流程、相同的目标模型）隔离了“表示选择”对性能的影响，客观地验证了 KV 复用的优劣。

### 6. 主要结论与发现
*   **KV 复用的长程优势**：KV 复用在长距离步数（$k \ge 3$）时比隐藏状态复用表现更稳健，衰减更慢。
*   **查询估计瓶颈**：KV 复用的效果高度依赖于草稿模型估计未来查询的能力。1 层草稿模型由于深度不足，难以准确估计目标查询，导致短程效果不如隐藏状态。
*   **端到端增益微小**：虽然步进接受率有所提升，但由于交叉注意力增加了 5-10% 的推理开销，且当前 TTT 流程存在梯度稀疏问题，最终端到端加速效果并不显著。
*   **门控饥饿现象**：在混合模型中，由于自注意力分支已有预训练权重，随机初始化的 KV 分支在训练初期容易被门控信号抑制，导致“梯度饥饿”。

### 7. 优点
*   **视角新颖**：从“上下文信息保留”而非单纯的“分布对齐”角度解释长程衰减。
*   **诊断深入**：不仅展示了结果，还深入分析了自回归 TTT 流程在处理 KV 复用时的结构性缺陷（如梯度稀疏、查询估计难）。
*   **设计严谨**：提出的 Gated Delta Rule 能够有效结合两种信号，为后续研究提供了参考架构。

### 8. 不足与局限
*   **端到端性能未达预期**：目前的方法在实际 wall-clock 速度上提升有限，未能完全克服引入 KV 路径带来的计算开销。
*   **模型覆盖有限**：主要基于 Qwen3-8B 进行实验，尚未在更大规模或其他架构（如 Llama）上验证普适性。
*   **训练范式限制**：作者指出当前的自回归训练可能不适合 KV 复用，暗示需要转向更复杂的块状训练（Block-wise training），这增加了实现难度。

（完）
