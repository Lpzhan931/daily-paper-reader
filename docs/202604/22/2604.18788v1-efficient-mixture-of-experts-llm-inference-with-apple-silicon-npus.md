---
title: Efficient Mixture-of-Experts LLM Inference with Apple Silicon NPUs
title_zh: 基于 Apple Silicon NPU 的高效混合专家模型（MoE）大语言模型推理
authors: "Afsara Benazir, Felix Xiaozhu Lin"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18788v1"
tags: ["query:llm"]
score: 9.0
evidence: 在NPU上实现高效的MoE大模型推理
tldr: 针对 Apple Silicon NPU 在处理 MoE 模型时面临的动态张量形状、不规则算子及内核调度开销等挑战，本文提出 NPUMoE 推理引擎。该引擎通过将密集静态计算卸载至 NPU，并结合静态专家容量分层、分组专家执行及负载感知图驻留技术，在保持 CPU/GPU 兼容性的同时，显著提升了 MoE 模型在 Apple 设备上的推理速度和能效。
selection_source: fresh_fetch
motivation: 旨在解决 MoE 模型在 Apple NPU 上因动态路由、不规则算子及高昂的内核调度开销导致的推理效率低下问题。
method: 提出 NPUMoE 引擎，通过离线校准实现静态专家容量分层、分组专家执行以及负载感知的计算图驻留优化。
result: 实验显示 NPUMoE 在 Apple M 系列芯片上将延迟降低了 1.32x-5.55x，能效提升了 1.81x-7.37x，并显著减少了 CPU 负载。
conclusion: NPUMoE 证明了通过合理的静态化与卸载策略，可以有效利用移动端 NPU 加速复杂的 MoE LLM 推理任务。
---

## 摘要
Apple Neural Engine (ANE) 是集成在每颗 Apple Silicon 芯片中的专用神经处理单元 (NPU)。混合专家模型 (MoE) 大语言模型通过稀疏激活提高了推理效率，但对 NPU 提出了三方面的挑战：专家路由具有不可预测性，并引入了与 NPU 形状特定约束相冲突的动态张量形状；一些不规则算子（如 top-k、scatter/gather 等）对 NPU 不友好；启动大量小型专家内核会产生巨大的调度和同步开销。NPU 旨在分担 CPU 和 GPU 的 AI 计算任务；我们的目标是实现 MoE 推理的此类卸载，特别是在预填充（prefill）阶段，此时长上下文工作负载会消耗大量系统资源。本文提出了 NPUMoE，这是一种运行时推理引擎，通过将密集的静态计算卸载到 NPU，同时为动态操作保留 CPU/GPU 回退路径，从而加速 Apple Silicon 上的 MoE 执行。NPUMoE 利用离线校准来估计专家容量和热度，从而驱动三项关键技术：(1) 用于应对动态专家路由的专家容量静态分层；(2) 用于缓解 NPU 并发限制的分组专家执行；(3) 用于减少 CPU-NPU 同步开销的负载感知专家计算图驻留。在 Apple M 系列设备上使用三种代表性 MoE 大语言模型和四种长上下文工作负载进行的实验表明，NPUMoE 始终优于基准方案，通过有效的 NPU 卸载，将延迟降低了 1.32 倍至 5.55 倍，能效提高了 1.81 倍至 7.37 倍，并将 CPU 周期使用率降低了 1.78 倍至 5.54 倍。

## Abstract
Apple Neural Engine (ANE) is a dedicated neural processing unit (NPU) present in every Apple Silicon chip. Mixture-of-Experts (MoE) LLMs improve inference efficiency via sparse activation but are challenging for NPUs in three ways: expert routing is unpredictable and introduces dynamic tensor shapes that conflict with the shape-specific constraints of NPUs; several irregular operators, e.g., top-k, scatter/gather, etc., are not NPU-friendly; and launching many small expert kernels incurs substantial dispatch and synchronization overhead. NPUs are designed to offload AI compute from CPU and GPU; our goal is to enable such offloading for MoE inference, particularly during prefill, where long-context workloads consume substantial system resources.   This paper presents NPUMoE, a runtime inference engine that accelerates MoE execution on Apple Silicon by offloading dense, static computation to NPU, while preserving a CPU/GPU fallback path for dynamic operations. NPUMoE uses offline calibration to estimate expert capacity and popularity that drives three key techniques: (1) Static tiers for expert capacity to address dynamic expert routing (2) Grouped expert execution to mitigate NPU concurrency limits (3) Load-aware expert compute graph residency to reduce CPU-NPU synchronization overhead. Experiments on Apple M-series devices using three representative MoE LLMs and four long-context workloads show that NPUMoE consistently outperforms baselines, reducing latency by 1.32x-5.55x, improving energy efficiency by 1.81x-7.37x, and reducing CPU-cycle usage by 1.78x-5.54x through effective NPU offloading.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **NPUMoE** 的推理引擎，专门用于在 Apple Silicon 的神经处理单元（NPU/ANE）上高效运行混合专家模型（MoE）。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：MoE 架构通过稀疏激活实现了模型容量的扩展，但在移动端 NPU（如 Apple ANE）上运行面临巨大挑战。
*   **核心矛盾**：NPU 设计初衷是处理**静态、密集、形状固定**的计算图，而 MoE 推理具有**动态路由、不规则算子**（如 Top-K、Scatter/Gather）以及**大量细碎内核调度**的特点。
*   **研究意义**：通过将 MoE 的密集计算卸载到 NPU，可以显著降低 CPU/GPU 的负载，提升长上下文处理（Prefill 阶段）的能效和系统响应速度。

### 2. 方法论
NPUMoE 的核心思想是**“静态化动态过程”**，通过离线校准驱动运行时的混合执行。
*   **静态专家容量分层 (Static Tiers for Expert Capacity)**：
    *   针对 NPU 不支持动态张量形状的问题，NPUMoE 预先估计每个专家的负载，将其映射到不同的静态容量层级（Tier）。
    *   高频专家分配大容量，低频专家分配小容量，通过填充（Padding）对齐形状，通过剪枝（Pruning）处理溢出。
*   **分组专家执行 (Grouped Expert Execution)**：
    *   为了减少 NPU 频繁启动小内核的调度开销，将多个专家合并为一个大的静态 FFN 计算图。
    *   一次 NPU 调用即可并行处理一组专家，共享启动开销。
*   **负载感知计算图驻留 (Load-Aware Residency)**：
    *   基于离线校准确定的专家“热度”，将热专家组驻留在 NPU，而将冷专家组留在 CPU 执行。
    *   这避免了为极少数 Token 频繁进行 CPU-NPU 同步带来的延迟浪费。

### 3. 实验设计
*   **模型对象**：Phi-3.5-MoE-Instruct (42.6B)、Phi-tiny-MoE-Instruct (3.8B)、Qwen3-30B-A3B。
*   **硬件平台**：Apple M2 Max (64GB RAM)、M2 Ultra (192GB RAM)。
*   **测试场景**：侧重于长上下文的 **Prefill（预填充）** 阶段。
*   **数据集**：HellaSwag（常识推理）、BoolQ（问答）、RULER（长上下文检索）。
*   **对比基准 (Benchmarks)**：
    1.  **Core ML (CPU only)**：全 CPU 执行。
    2.  **Core ML (naïve)**：原生 Core ML 调度，专家 FFN 尝试卸载到 NPU 但缺乏优化。
    3.  **ANEMLL**：现有的 ANE 加速库（主要针对密集 Transformer）。

### 4. 资源与算力
*   **推理算力**：使用了 M2 Ultra（32核 ANE，峰值 31.6 TOPS）和 M2 Max（16核 ANE）。
*   **训练/校准算力**：文中未提及模型训练所需的算力，因为该研究专注于推理优化。离线校准过程在相同设备上完成，耗时相对推理过程可忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3.8B 到 42.6B 不同参数规模的模型，专家数量从 16 个到 128 个不等。
*   **维度覆盖**：测试了不同的 Prompt 长度（1024, 4096）、Chunk 大小（256, 512, 1024）以及消融实验。
*   **充分性评价**：实验设计较为充分。作者不仅对比了延迟和能效，还深入分析了 CPU 周期消耗、Token 丢弃率对精度的影响以及离线校准的跨数据集稳定性，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：相比基准方案，NPUMoE 将预填充延迟降低了 **1.32x–5.55x**。
*   **能效优化**：能效提升了 **1.81x–7.37x**，显著延长了电池续航。
*   **资源释放**：CPU 周期使用率降低了 **1.78x–5.54x**，为系统其他任务留出了空间。
*   **精度保持**：尽管采用了静态容量和 Token 丢弃策略，但在测试数据集上的精度损失极小（**<1.1%**）。

### 7. 优点
*   **软硬结合**：深入挖掘了 Apple ANE 的硬件特性，解决了 MoE 动态性与 NPU 静态约束之间的根本矛盾。
*   **系统性优化**：并非单一算子优化，而是从容量管理、内核分组到设备驻留的全栈式运行时设计。
*   **实用性强**：针对统一内存架构（Unified Memory）进行了优化，能够处理参数量巨大的 MoE 模型。

### 8. 不足与局限
*   **Decode 阶段优化有限**：由于自回归生成的 Decode 阶段是内存受限型（Memory-bound）且缺乏并行性，该引擎的优势主要体现在 Prefill 阶段。
*   **依赖离线校准**：专家热度分布可能随特定领域数据变化，虽然实验证明了稳定性，但在极端分布变化下可能需要重新校准。
*   **静态容量风险**：在极端不平衡的路由情况下，可能会出现较高的 Token 丢弃率，虽然文中显示对精度影响小，但在严苛任务中仍存在风险。

（完）
