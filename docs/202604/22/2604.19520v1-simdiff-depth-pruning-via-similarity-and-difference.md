---
title: "SimDiff: Depth Pruning via Similarity and Difference"
title_zh: SimDiff：基于相似性与差异性的深度剪枝
authors: "Yuli Chen, Shuhao Zhang, Fanshen Meng, Bo Cheng, Jiale Han, Qiang Tong, Xiulei Liu"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19520v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于提升LLM效率的深度剪枝
tldr: 针对大语言模型深度剪枝中仅依赖余弦相似度导致性能不稳定的问题，本文提出SimDiff方法。该方法结合表示相似度与变换差异（MSSD和MASD）两个正交维度来评估层重要性。实验证明，SimDiff在多种规模模型上优于现有基准，在保持高性能的同时显著提升推理速度，且剪枝后模型易于恢复。
selection_source: fresh_fetch
motivation: 现有的基于相似度的深度剪枝方法在不同架构上表现不稳定，甚至会出现性能崩溃。
method: 提出SimDiff准则，通过相似度和变换差异（包含对离群值敏感的MSSD和稳健的MASD）共同评估层的重要性。
result: "在LLaMA系列模型上，SimDiff在25%剪枝率下保留了超过91%的性能，并实现了高达1.49倍的推理加速。"
conclusion: SimDiff是一种高效且稳健的深度剪枝方案，能有效识别冗余层并显著提升大模型的部署效率。
---

## 摘要
深度剪枝通过识别并移除冗余层来提高大语言模型（LLMs）的部署效率。这一识别过程的一个广泛接受的标准是使用余弦距离来衡量层间的相似性。然而，我们发现仅依赖这种一维启发式方法在不同架构上可能会表现出不可预测的性能，甚至导致灾难性崩溃。为了解决这个问题，我们提出了 SimDiff，这是一种新颖的层重要性准则，它从两个正交的角度共同评估层：表示相似性（representational similarity）和变换差异（transformation difference）。差异性通过两个不同的指标进行量化：MSSD，它对离群值敏感并能识别做出决定性修正的层；以及 MASD，它能稳健地衡量层的平均贡献。在参数量从 0.5B 到 13B 的多个模型上进行的广泛实验表明，SimDiff 在各种剪枝比例下均显著优于最先进的基准方法。值得注意的是，我们的方法在 25% 的剪枝比例下保留了 LLaMA2-7B 超过 91% 的性能，并在 LLaMA3.1-8B 上剪枝 12 层时实现了高达 1.49 倍的推理加速。我们还展示了剪枝后的模型可以通过极少的微调得到有效恢复。

## Abstract
Depth pruning improves the deployment efficiency of large language models (LLMs) by identifying and removing redundant layers. A widely accepted standard for this identification process is to measure the similarity between layers using cosine distance. However, we find that methods relying solely on this one-dimensional heuristic can exhibit unpredictable performance and even catastrophic collapse across different architectures. To address this issue, we propose SimDiff, a novel layer importance criterion that jointly evaluates layers from two orthogonal perspectives: representational similarity and transformation difference. The difference is quantified using two distinct metrics: MSSD, which is sensitive to outliers and identifies layers that make decisive corrections, and MASD, which robustly measures a layer's average contribution. Extensive experiments on multiple models ranging from 0.5B to 13B parameters demonstrate that SimDiff significantly outperforms state-of-the-art baselines across various pruning ratios. Notably, our method retains over 91% of LLaMA2-7B's performance at a 25% pruning ratio and achieves up to a 1.49x inference speedup when pruning 12 layers on LLaMA3.1-8B. We also show that pruned models can be effectively recovered with minimal fine-tuning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SimDiff** 的大语言模型（LLM）深度剪枝方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的深度剪枝方法（如 ShortGPT）主要依赖**余弦相似度**这一单一维度来识别冗余层。
*   **研究背景**：虽然相似度能反映表示上的冗余，但作者发现这种启发式方法在不同模型架构（如 LLaMA2 vs. Mistral/LLaMA3.1）上表现极不稳定。在某些模型上，移除超过一定数量的层会导致性能**灾难性崩溃**（见论文图1）。
*   **动机**：余弦相似度仅关注向量方向，忽略了层变换的**幅值（Magnitude）**。作者认为，一个稳健的剪枝准则需要同时考虑层间的“相似性”与“差异性”。

### 2. 方法论：SimDiff 核心思想与技术细节
SimDiff 通过两个正交维度评估层的重要性：
*   **表示相似性（Similarity）**：使用余弦距离衡量输入与输出隐藏状态的相似度。相似度越高，说明该层越接近恒等变换，重要性越低。
*   **变换差异性（Difference）**：量化层对数据流产生的修改幅度。作者提出了两种指标：
    *   **MSSD（均方状态差异）**：对差异进行平方求和，对离群值（Outliers）极度敏感，旨在识别做出决定性修正的关键层。
    *   **MASD（平均绝对状态差异）**：计算差异的绝对值平均，更稳健地衡量层的常规贡献。
*   **统一重要性评分**：
    1.  将相似度和差异性指标通过 Sigmoid 或缩放归一化到 [0, 1] 区间。
    2.  使用加权系数 $\alpha$ 进行融合：$I = \alpha \cdot I_{diff} + (1-\alpha) \cdot I_{sim}$。
*   **超参数优化**：引入**三分搜索（Ternary Search）**算法，在验证集上快速寻找最优的 $\alpha$ 值，使模型能够自适应不同架构和规模。
*   **剪枝流程**：计算所有层得分，按得分升序排列，移除得分最低的 $K$ 层。

### 3. 实验设计
*   **实验对象**：涵盖 0.5B 到 13B 参数规模的模型，包括 LLaMA2 (7B/13B)、LLaMA3.1-8B、Mistral-7B-v0.3、Qwen2.5 (0.5B/1.5B/3B)。
*   **数据集/场景**：
    *   **NLU 任务**：使用 OpenCompass 框架，包含 CMNLI、PIQA、BoolQ、XSum 等 10 个数据集。
    *   **零样本（Zero-shot）任务**：EleutherAI LM Harness 上的 8 个常识推理基准。
    *   **语言建模**：WikiText2 上的困惑度（PPL）。
*   **对比基准（Baselines）**：包括深度剪枝方法（LaCo, ShortGPT, Shortened-LLM, SLEB, EntroDrop）和宽度剪枝方法（LLM-Pruner, SliceGPT）。

### 4. 资源与算力
*   **硬件使用**：推理评估和吞吐量测试在单张 **NVIDIA A40 GPU** 上完成。
*   **校准数据**：仅需从 WikiText2 中随机抽取 512 个样本（序列长度 2048）。
*   **微调成本**：在 Alpaca 数据集上进行 1 个 Epoch 的 LoRA 微调，耗时约 **142 分钟**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 种不同规模和架构的模型上进行了测试，涵盖了从轻量级（0.5B）到中量级（13B）的范围。
*   **剪枝比例**：测试了从 10.9% 到 32.6%（移除 4 到 12 层）的多种剪枝比例。
*   **消融实验**：详细探讨了 $\alpha$ 参数对 MSSD 和 MASD 变体的影响。
*   **评价**：实验设计非常充分，对比了多种 SOTA 方法，且在不同架构上的交叉验证证明了该方法的客观性和普适性。

### 6. 主要结论与发现
*   **性能卓越**：在 LLaMA2-7B 剪枝 25% 时，SimDiff 保留了超过 91% 的原始性能，显著优于 ShortGPT 和 LaCo。
*   **推理加速**：在 LLaMA3.1-8B 上移除 12 层后，实现了 **1.49 倍** 的推理吞吐量提升。
*   **架构稳健性**：SimDiff 有效解决了 ShortGPT 在 Mistral 和 LLaMA3.1 上出现的性能崩溃问题。
*   **恢复能力**：剪枝后的模型通过极少量的 LoRA 微调，性能可迅速恢复至原始水平的 80% 以上。
*   **指标特性**：MSSD 适合中度剪枝，而 MASD 在极端剪枝（高比例）下表现更稳健。

### 7. 优点与亮点
*   **多维度评估**：打破了单一相似度准则的局限，结合了方向（相似度）和量级（差异性）。
*   **自适应性**：通过三分搜索自动调整权重，无需人工繁琐调参即可适配新模型。
*   **硬件友好**：深度剪枝直接缩短计算路径，相比宽度剪枝更易在现有硬件上获得实际加速。
*   **开源潜力**：方法简单高效，对校准数据和算力要求极低，适合资源受限的部署场景。

### 8. 不足与局限
*   **性能损失**：在不进行微调的情况下，当剪枝比例超过 30% 时，模型在复杂推理任务上的性能仍有明显下降。
*   **任务偏差**：虽然在通用任务上表现良好，但未深入探讨剪枝对特定领域（如代码编写、长文本生成）的潜在负面影响。
*   **搜索开销**：虽然三分搜索比网格搜索快，但每一步仍需计算 PPL，对于超大规模模型（如 70B+）可能会增加预处理时间。

（完）
