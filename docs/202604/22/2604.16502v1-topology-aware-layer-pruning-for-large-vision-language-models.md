---
title: Topology-Aware Layer Pruning for Large Vision-Language Models
title_zh: 面向大视觉语言模型的拓扑感知层剪枝
authors: "Pengcheng Zheng, Chaoning Zhang, Ya Wen, Wang Liu, Qigan Sun, Jiarong Mo, Jiaquan Zhang, Jewon Lee, Tae-Ho Kim, Kuien Liu, Tianyu Li, Caiyan Qin, Yang Yang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.16502v1"
tags: ["query:llm"]
score: 9.0
evidence: 大模型的层剪枝
tldr: 针对大型视觉语言模型（LVLM）计算开销大的问题，本文提出了一种拓扑感知的层剪枝框架。该方法将隐藏状态视为点云，利用单纯复形和之字形持久同调（zigzag persistent homology）来量化层间拓扑一致性，从而识别并保留关键的表示转换层。实验证明，该方法在多种多模态基准测试中均优于现有剪枝技术，为高效部署提供了新思路。
selection_source: fresh_fetch
motivation: 现有剪枝方法多依赖局部相似性，难以捕捉模型深度的全局动态演化，导致关键转换层被误删。
method: 将层隐藏状态表示为点云，通过之字形持久同调量化层间拓扑一致性，实现保留关键特征的自适应剪枝。
result: 在多个多模态基准测试中，该框架在不同稀疏比例下均显著优于现有的层剪枝基准方法。
conclusion: 通过引入拓扑分析，该研究证明了保留模型内部表示的拓扑结构对于维持LVLM性能至关重要。
---

## 摘要
大语言模型（LLMs）在自然语言理解和推理方面展现出强大的能力，而近期引入视觉输入的扩展使其能够处理多模态信息。尽管取得了这些进展，大视觉语言模型（LVLMs）仍面临巨大的计算和内存开销，阻碍了其在资源受限场景下的部署。现有的层剪枝方法通常依赖于局部相似性度量或静态代理信号，未能捕捉到表征随模型深度变化的全局和动态演化，这往往导致关键转换层被移除。为了解决这一局限性，我们为 LVLMs 提出了一种拓扑感知层剪枝框架。具体而言，我们将逐层隐藏状态表示为点云，并使用单纯复形（simplicial complexes）对其演化进行建模。通过利用 Zigzag 持久同调（zigzag persistent homology），我们量化了层间拓扑一致性，并实现了能够保留关键表征转换的自适应剪枝。在多种多模态基准测试上的广泛实验表明，所提出的框架在各种稀疏率下均一致优于现有的剪枝方法。我们的代码可在 https://github.com/zpc456/TopoVLM 获取。

## Abstract
Large Language Models (LLMs) have demonstrated strong capabilities in natural language understanding and reasoning, while recent extensions that incorporate visual inputs enable them to process multimodal information. Despite these advances, Large Vision-Language Models (LVLMs) incur substantial computational and memory costs, hindering deployment in resource-constrained scenarios. Existing layer pruning methods typically rely on local similarity metrics or static proxy signals, failing to capture the global and dynamic evolution of representations across model depth, which often leads to the removal of transition-critical layers. To address this limitation, we propose a topology-aware layer pruning framework for LVLMs. Specifically, we represent layer wise hidden states as point clouds and models their evolution using \textit{simplicial complexes}. By leveraging \textit{zigzag persistent homology}, we quantify inter-layer topological consistency and enable adaptive pruning that preserves critical representational transitions. Extensive experiments on diverse multimodal benchmarks demonstrate that the proposed framework consistently outperforms existing pruning methods across a wide range of sparsity ratios. Our code is available at https://github.com/zpc456/TopoVLM.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **TopoVLM** 的拓扑感知层剪枝框架，旨在解决大视觉语言模型（LVLM）在部署时的计算与内存开销问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：LVLM（如 LLaVA、Qwen-VL）虽然强大，但其深层的 Transformer 架构带来了巨大的计算负担。
*   **研究动机**：
    *   **现有方法局限性**：目前的层剪枝方法主要依赖“局部相似性”（如余弦相似度）或“静态代理信号”（如权重大小、激活统计）。
    *   **忽视动态演化**：LVLM 的表征随深度动态演化（从视觉编码到多模态对齐再到推理）。局部指标容易误删那些在语义转换中起关键“桥梁”作用的层，导致性能非线性下降。
    *   **缺乏全局视角**：需要一种能够捕捉模型深度方向上全局结构演化的剪枝准则。

### 2. 方法论：拓扑感知层剪枝
核心思想是利用**拓扑数据分析（TDA）**中的**之字形持久同调（Zigzag Persistent Homology）**来量化层间的结构冗余。
*   **表征建模**：将每一层的隐藏状态（Hidden States）视为高维空间中的**点云**。
*   **单纯复形构建**：通过 k-最近邻（k-NN）图和团复形（Clique Expansion）将点云转化为单纯复形，捕捉高阶结构特征。
*   **之字形过滤（Zigzag Filtration）**：构建一个跨越相邻层的序列，允许特征的出现（Birth）和消失（Death）。这能追踪拓扑特征（如连通分量、环、空洞）在模型深度方向上的演化。
*   **关键指标定义**：
    *   **有效持久图像（EPI）**：将拓扑特征的生命周期转化为连续图像。
    *   **层级拓扑活动度（Topological Activity）**：衡量某一层引入新结构特征的能力。
    *   **层间拓扑一致性（Inter-Layer Consistency）**：衡量层与层之间结构特征的重合度。
*   **剪枝规则**：自适应地移除那些“拓扑一致性”极高（即结构冗余）的层，保留具有高“拓扑活动度”或作为结构转换关键点的层。

### 3. 实验设计
*   **实验模型**：LLaVA-NeXT (8B), VideoLLaMA2 (7B), LLaVA-OneVision (7B), Qwen2-VL (7B)。
*   **评估基准（Benchmarks）**：
    *   **图像任务**：MME, MMMU, MathVista, ChartQA, AI2D, MMBench 等。
    *   **视频/音频任务**：VideoMME, NextQA, EgoSchema, Clotho-AQA, MUSIC-QA 等。
*   **对比方法**：
    *   相似性方法：LLM-Pruner, LLM-Streamline。
    *   信号驱动方法：Magnitude, SparseGPT, Wanda, OWL, ECoFLaP, TAMP。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 × NVIDIA A100 (80GB) GPU**。
*   **效率表现**：
    *   **搜索阶段**：在 LLaVA-NeXT (8B) 上，TopoVLM 仅需 **5.7 分钟** 即可完成剪枝搜索，仅需 1 次校准迭代（Calibration Pass），显著快于 SparseGPT (22.3 min) 和 Wanda (18.9 min)。
    *   **推理加速**：在 50% 稀疏度下，显存占用降低 43%，推理延迟从 105.4ms 降至 60.3ms，实现 **1.75× 的加速**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流 LVLM 家族上进行了验证，涵盖了图像、视频、音频三种模态。
*   **消融实验**：
    *   验证了 k-NN 的 k 值、权重指数 $\alpha$ 的敏感性。
    *   对比了“仅文本”、“仅视觉”和“多模态”输入对拓扑建模的影响。
    *   测试了 Token 下采样对拓扑稳定性的影响。
*   **充分性评价**：实验设计非常充分且客观。通过多次随机采样校准集并计算标准差，证明了该方法在不同校准数据下的**鲁棒性**显著优于其他方法。

### 6. 主要结论与发现
*   **性能领先**：在 50%-60% 的高稀疏比例下，TopoVLM 的性能保留率（Relative Performance）显著高于现有方法（如在 LLaVA-NeXT 上达到 91.6%）。
*   **阶段性特征**：实验发现模型浅层拓扑活动度高（构建基础结构），中深层一致性高（存在结构冗余）。
*   **拓扑稳定性**：即使对视觉 Token 进行大幅下采样（保留 25%），剪枝决策依然保持高度一致（>90% 重合度），说明拓扑结构是模型内在的稳定属性。

### 7. 优点（亮点）
*   **理论新颖**：首次将之字形持久同调引入 LVLM 剪枝，从全局几何视角解释了层冗余。
*   **高效且无损**：作为一种 One-shot 剪枝方法，不需要重新训练或微调，且搜索速度极快。
*   **解释性强**：通过拓扑图像可视化了模型内部的表征演化过程，增强了对“黑盒”模型的理解。
*   **通用性**：在不同架构（LLaMA 系和 Qwen 系）上均表现出色。

### 8. 不足与局限
*   **硬件感知缺失**：虽然减少了层数，但未直接针对特定硬件的能效（Energy Efficiency）进行协同设计（如内存访问模式优化）。
*   **高阶拓扑限制**：为了计算效率，目前仅使用了 0 维和 1 维同调特征，更高维度的拓扑特征（如 2 维空洞）因计算复杂度和不稳定性被舍弃，可能丢失极细微的结构信息。
*   **应用范围**：主要针对 Transformer 解码器层进行剪枝，未涉及视觉编码器（Encoder）的拓扑压缩。

（完）
