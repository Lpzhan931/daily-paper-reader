---
title: "AudioKV: KV Cache Eviction in Efficient Large Audio Language Models"
title_zh: AudioKV：高效大型音频语言模型中的 KV 缓存剔除
authors: "Yuxuan Wang, Peize He, Xiyan Gui, Xiaoqian Liu, Junhao He, Xuyang Liu, Zichen Wen, Xuming Hu, Linfeng Zhang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06694v1"
tags: ["query:llm"]
score: 9.0
evidence: 大型音频语言模型中的 KV 缓存剔除与压缩
tldr: 针对大型音频语言模型（LALM）在长上下文推理中KV缓存占用内存过大的问题，本文提出AudioKV框架。该框架通过语义-声学对齐机制识别音频关键注意力头，并结合基于FFT的光谱分数平滑（SSS）策略，在抑制高频噪声的同时保留全局趋势。实验表明，AudioKV在大幅压缩缓存的同时保持了极高的准确率，显著优于传统方法。
selection_source: fresh_fetch
motivation: 现有的KV缓存压缩技术在音频领域效果不佳，因为它们忽略了声学信号内在的时域连续性。
method: 提出AudioKV框架，通过分析ASR任务识别音频关键头并动态分配预算，同时利用FFT进行光谱分数平滑以优化Token选择。
result: "在Qwen和Gemma等模型上的实验显示，AudioKV在40%压缩率下仅有0.45%的精度损失，远超传统基准。"
conclusion: AudioKV为长上下文音频语言模型提供了一种高效且鲁棒的KV缓存管理方案，平衡了计算效率与模型性能。
---

## 摘要
大型音频语言模型 (LALMs) 在语音处理领域树立了新的基准，但其部署受限于长上下文推理过程中键值 (KV) 缓存的内存占用。虽然通用的 KV 缓存压缩技术在大型语言模型 (LLMs) 中表现出色，但由于忽视了声学信号内在的时间连续性，它们在音频领域往往表现不佳。为了弥补这一差距，我们提出了 AudioKV，这是一个通过硬件友好的语义-声学对齐机制，稳健地优先处理音频关键注意力头的创新框架。具体而言，我们通过分析自动语音识别 (ASR) 任务中的注意力分数来识别这些模态专门化头，并动态地优先为它们分配 KV 缓存预算。此外，我们引入了频谱分数平滑 (SSS)，这是一种基于快速傅里叶变换 (FFT) 的全局滤波策略，旨在抑制高频噪声并从重要性分数中恢复平滑的全局趋势，从而以空前的精度确保更均衡的 Token 选择。在包括 Qwen 和 Gemma 系列在内的多个 LALM 上进行的广泛评估表明，AudioKV 在提高计算效率的同时，显著优于基准方法。值得注意的是，在 40% 的压缩率下，AudioKV 在 Qwen3-Omni-30B 上保持了近乎完整的准确度，下降仅为 0.45%，而传统方法则面临灾难性的性能下降和重复问题。我们的代码将在论文接收后发布。

## Abstract
Large Audio-Language Models (LALMs) have set new benchmarks in speech processing, yet their deployment is hindered by the memory footprint of the Key-Value (KV) cache during long-context inference. While general KV cache compression techniques excel in LLMs, they often fail in the audio domain by overlooking the intrinsic temporal continuity of acoustic signals. To bridge this gap, we propose AudioKV, a novel framework that robustly prioritizes audio-critical attention heads through a hardware-friendly semantic-acoustic alignment mechanism. Specifically, we identify these modality-specialized heads by analyzing attention scores in ASR tasks and dynamically allocate KV cache budgets preferentially to them. Furthermore, we introduce Spectral Score Smoothing (SSS), an FFT-based global filtering strategy designed to suppress high-frequency noise and recover smooth global trends from importance scores, ensuring more balanced token selection with unprecedented precision. Extensive evaluations across multiple LALMs, including Qwen and Gemma series, demonstrate that AudioKV significantly outperforms baselines while enhancing computational efficiency. Notably, at a 40% compression ratio, AudioKV maintains near-full accuracy on Qwen3-Omni-30B with only a 0.45% drop, whereas traditional methods suffer from catastrophic performance degradation and repetition. Our code will be released after acceptance.

---

## 论文详细总结（自动生成）

这篇论文提出了 **AudioKV**，一种专门为大型音频语言模型（LALMs）设计的高效 KV 缓存（Key-Value Cache）剔除框架，旨在解决长音频推理过程中的内存瓶颈问题。

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：大型音频语言模型（如 Qwen-Omni 系列）在处理长音频时，KV 缓存的内存占用随音频长度线性增长，严重限制了在端侧设备或流式场景下的部署。
*   **核心问题**：现有的针对纯文本 LLM 的 KV 压缩技术（如 SnapKV、AdaKV）直接应用于音频模态时效果不佳。
*   **研究动机**：
    1.  **模态专门化**：模型内部的注意力头具有功能差异，只有部分头专门负责捕捉声学信息。
    2.  **信号连续性**：音频信号是时间连续的，传统的 Top-K Token 选择方法容易导致注意力集中在局部，忽略了全局声学趋势，且容易受到高频噪声干扰。

### 2. 方法论：核心思想与关键技术
AudioKV 包含两个核心组件：
*   **音频感知头缓存分配（Audio-aware KV Cache Allocation）**：
    *   **离线识别**：通过 ASR 任务，利用 WhisperX 获取单词的时间戳，计算每个注意力头的高分 Token 与真实音频跨度的重合率（Hit Rate），从而识别出“音频关键头”。
    *   **动态预算分配**：在推理时，不再对所有头进行均匀压缩，而是优先为音频关键头分配更多的 KV 缓存预算，对非关键头执行更激进的剔除。
*   **频谱分数平滑（Spectral Score Smoothing, SSS）**：
    *   **核心思想**：将 Token 的重要性分数视为一维时域信号。
    *   **技术细节**：利用 **快速傅里叶变换（FFT）** 将分数序列转换到频域，应用全局低通滤波器抑制代表噪声的高频成分，保留代表语义趋势的低频成分。
    *   **残差混合**：通过逆 FFT 将平滑后的信号转回时域，并与原始分数进行加权融合（混合系数 $\alpha$），确保 Token 选择既能捕捉全局趋势又能保留必要的局部特征。

### 3. 实验设计
*   **数据集**：
    *   **ASR（语音识别）**：KeSpeech（中文）、LibriSpeech-long（长英文）、Multilingual LibriSpeech（多语种：法、德、西）。
    *   **ST（语音翻译）**：CoVoST2（英译中、中译英）。
    *   **AQA（音频问答）**：UltraEval-Audio 框架下的四个数据集（Alpaca-eval, Web-questions 等）。
*   **模型（Backbones）**：Qwen2.5-Omni (3B, 7B), Qwen3-Omni-30B, Gemma-3n (E2B, E4B)。
*   **对比基准（Baselines）**：Full KV（全量缓存）、SnapKV、AdaKV、PyramidKV、H2O。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A100 GPU** 上进行。
*   **算力细节**：文中未明确给出具体的训练/识别时长或 GPU 数量，但强调了该方法是“硬件友好”且“计算高效”的插件式模块，不需要对模型进行重新训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2B 到 30B 参数规模的 5 种主流 LALM 模型，并在 ASR、ST、AQA 三大任务上进行了验证。
*   **消融实验**：分别验证了“头感知分配”和“SSS 平滑”的有效性，并对 SSS 的超参数（截止频率 $k^*$ 和混合系数 $\alpha$）进行了敏感性分析。
*   **充分性评价**：实验设计较为全面，对比了多种最先进的文本 KV 压缩方法，并覆盖了长音频、多语种和复杂问答场景，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能卓越**：在 40% 的极低缓存保留率下，AudioKV 在 Qwen3-Omni-30B 上的准确率仅下降 0.45%，而传统方法（如 SnapKV）会出现灾难性的性能崩溃。
*   **稳定性**：AudioKV 有效解决了长音频解码中的“重复循环”问题（即模型陷入重复输出同一片段的困境）。
*   **通用性**：该方法在不同架构（Qwen 和 Gemma）和不同任务上均表现出优于基准的效果。

### 7. 优点
*   **物理特性结合**：首次将信号处理中的频谱分析（FFT）引入音频模型的 KV 缓存管理，充分利用了音频信号的连续性。
*   **即插即用**：无需微调模型，通过简单的离线分析即可显著提升推理效率。
*   **鲁棒性强**：在极高压缩比下依然能保持模型生成的连贯性，避免了传统方法常见的退化现象。

### 8. 不足与局限
*   **离线依赖**：识别音频关键头需要预先在 ASR 任务上进行离线分析，虽然识别一次即可通用，但对于全新架构的模型仍需额外步骤。
*   **任务差异**：实验显示 AudioKV 在 ASR 上的提升比在语音翻译（ST）上更显著，这可能与 ST 任务中语言建模的权重更高有关。
*   **实时性探讨**：虽然 FFT 计算量小，但论文未详细对比在极低延迟流式推理场景下，SSS 模块带来的额外计算开销。

（完）
