---
title: "DiffuMask: Diffusion Language Model for Token-level Prompt Pruning"
title_zh: DiffuMask：用于 Token 级提示词剪枝的扩散语言模型
authors: "Caleb Zheng, Jyotika Singh, Fang Tu, Weiyi Sun, Sujeeth Bharadwaj, Yassine Benajiba, Sujith Ravi, Eli Shlizerman, Dan Roth"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06627v1"
tags: ["query:llm"]
score: 9.0
evidence: 基于扩散模型的快速Token级提示词剪枝框架
tldr: "针对大语言模型在上下文学习和思维链提示中面临的提示词冗余及计算成本高的问题，本文提出 DiffuMask。这是一种基于扩散模型的提示词剪枝框架，通过分层级（样本级和标记级）的剪枝信号，利用迭代掩码预测实现快速并行的提示词压缩。该方法在大幅缩减提示词长度（最高达80%）的同时，保持甚至提升了模型在多种场景下的推理准确性，为高效提示词工程提供了新方案。"
selection_source: fresh_fetch
motivation: 现有的提示词剪枝方法通常依赖于计算密集型的顺序标记移除，导致压缩过程缓慢且效率低下。
method: 提出 DiffuMask 框架，将分层剪枝信号集成到扩散模型中，通过去噪步骤并行预测掩码以实现高效剪枝。
result: "该方法能减少高达 80% 的提示词长度，并在域内、跨域及跨模型设置下维持或提升推理准确率。"
conclusion: DiffuMask 为大语言模型提供了一个通用且可控的提示词压缩框架，显著提升了上下文推理的速度与可靠性。
---

## 摘要
上下文学习（In-Context Learning）和思维链（Chain-of-Thought）提示提高了大语言模型（LLMs）的推理能力。这些方法通常以更长、更昂贵的提示词为代价，且其中可能包含冗余信息。基于剪枝的提示词压缩提供了一种实用的解决方案，但现有方法依赖于顺序的词元移除，计算开销较大。我们提出了 DiffuMask，这是一个基于扩散的框架，集成了分层的示例级（shot-level）和词元级（token-level）剪枝信号，通过迭代掩码预测实现了快速且并行的提示词剪枝。DiffuMask 通过在每个去噪步骤中掩盖多个词元，显著加速了压缩过程。它提供了对保留内容的可调控性，在保留关键推理上下文的同时，实现了高达 80% 的提示词长度缩减。同时，它在域内、跨域和跨模型设置下均能保持或提高准确率。我们的结果表明，DiffuMask 为提示词压缩提供了一个通用且可控的框架，促进了大语言模型中更快速、更可靠的上下文推理。

## Abstract
In-Context Learning and Chain-of-Thought prompting improve reasoning in large language models (LLMs). These typically come at the cost of longer, more expensive prompts that may contain redundant information. Prompt compression based on pruning offers a practical solution, yet existing methods rely on sequential token removal which is computationally intensive. We present DiffuMask, a diffusion-based framework integrating hierarchical shot-level and token-level pruning signals, that enables rapid and parallel prompt pruning via iterative mask prediction. DiffuMask substantially accelerates the compression process via masking multiple tokens in each denoising step. It offers tunable control over retained content, preserving essential reasoning context and achieving up to 80\% prompt length reduction. Meanwhile, it maintains or improves accuracy across in-domain, out-of-domain, and cross-model settings. Our results show that DiffuMask provides a generalizable and controllable framework for prompt compression, facilitating faster and more reliable in-context reasoning in LLMs.

---

## 论文详细总结（自动生成）

### DiffuMask: 用于 Token 级提示词剪枝的扩散语言模型论文总结

#### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）依赖上下文学习（ICL）和思维链（CoT）来处理复杂任务，但这导致提示词（Prompt）变得极长，增加了推理延迟和计算成本。
*   **核心问题**：现有的提示词剪枝方法（如基于强化学习或进化算法的硬压缩）通常采用**顺序移除词元**的策略。这种方式计算极其密集，处理单个提示词往往需要 10 到 48 小时，难以在大规模实际应用中扩展。
*   **研究目标**：提出一种高效、并行且可控的框架，在大幅缩减提示词长度的同时，保留关键推理信息并维持模型性能。

#### 2. 方法论
*   **核心思想**：将提示词剪枝建模为一个**迭代去噪的扩散过程**。DiffuMask 不再逐个删除词元，而是通过扩散语言模型（DLM）并行预测整个序列的二值保留掩码（Retention Mask）。
*   **关键技术细节**：
    *   **分层数据生成**：首先进行**示例级（Shot-level）剪枝**（减少示例数量），随后进行**词元级（Token-level）剪枝**（利用阈值接受算法 TA-Pruning 移除冗余词元），构建“原始-剪枝”对数据集。
    *   **模型架构**：基于 LLaDA-8B（一种大规模离散扩散模型）进行微调。
    *   **训练目标**：模型学习预测掩码 $m$。损失函数结合了二元交叉熵（BCE）和**抗掩码惩罚（Anti-mask penalty）**，后者用于防止模型过度剪枝导致丢失关键信息。
    *   **并行推理**：在推理阶段，通过 64 步扩散迭代，利用 Top-k 策略和置信度阈值 $\tau$ 来决定哪些词元被掩盖（删除）。

#### 3. 实验设计
*   **数据集**：
    *   **域内（In-domain）**：GSM8K（数学推理基准）。
    *   **跨域（Out-of-domain）**：Yelp-5（情感分析）、Yahoo Answers（主题分类）、AG’s News（新闻分类）。
*   **Benchmark 与对比方法**：
    *   **基准**：原始 32-shot 全量提示词。
    *   **对比方法**：LLMLingua、LLMLingua2（知名压缩工具）、CoT-Influx（示例级剪枝）、以及作为教师模型的分层剪枝（HP）流水线。
*   **跨模型验证**：将 DiffuMask 生成的剪枝提示词应用于 GPT-4o、Gemini-2.5-Pro、Grok-3、Llama-3.3-70B 等 8 个主流模型。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA A100 GPU**。
*   **训练/生成时长**：
    *   **数据准备**：成本极高，使用 TA-Pruning 生成一个训练样本需 10-48 小时。
    *   **DiffuMask 推理**：剪枝过程在 **1 分钟内**完成，相比顺序剪枝方法实现了数个数量级的加速。
*   **微调设置**：在 203 个高质量提示词对上微调了 20 个 Epoch。

#### 5. 实验数量与充分性
*   **实验组数**：涵盖了从 1-shot 到 32-shot 的多种配置，进行了超参数搜索（Top-k 和 $\tau$ 的网格搜索）、跨领域迁移实验、跨模型兼容性实验以及词性（POS）分布分析。
*   **充分性与客观性**：实验设计较为严谨。通过在完全未见过的分类任务上测试，证明了模型捕捉的是“上下文冗余”这一通用模式，而非简单的过拟合。跨模型实验进一步证明了其生成的剪枝提示词具有架构无关的通用性。

#### 6. 主要结论与发现
*   **高效压缩**：DiffuMask 实现了高达 **80%** 的长度缩减。
*   **性能保持/提升**：在 GSM8K 上维持了准确率；在 AG's News 等分类任务中，剪枝后的提示词准确率显著高于全量提示词（例如 20-shot 下准确率从 25.37% 提升至 84.40%），证明了冗余信息确实会干扰模型。
*   **极速推理**：将原本需要数小时的剪枝任务缩短至分钟级，使其具备了实际部署的可能性。

#### 7. 优点
*   **并行化突破**：利用扩散模型的特性解决了硬剪枝无法并行处理的痛点。
*   **泛化能力强**：仅在数学推理数据上训练，却能在文本分类任务和不同厂商的 LLM 上表现出色。
*   **可控性**：提供了 Top-k 和阈值调节机制，允许用户根据需求灵活调整压缩比。

#### 8. 不足与局限
*   **数据获取成本**：虽然 DiffuMask 推理快，但其训练所需的“金标准”标签（通过 TA-Pruning 获得）生成极其缓慢，限制了训练数据的规模。
*   **非实时性**：尽管比顺序方法快，但 64 步扩散迭代对于极低延迟要求的实时场景（如即时对话）仍显稍慢。
*   **任务覆盖**：目前主要验证了推理和分类任务，在长文本摘要、创意写作或多模态提示词压缩方面的效果尚待验证。

（完）
