---
title: "TRAPTI: Time-Resolved Analysis for SRAM Banking and Power Gating Optimization in Embedded Transformer Inference"
title_zh: TRAPTI：嵌入式 Transformer 推理中 SRAM 分体与功率门控优化的时间分辨分析
authors: "Jan Klhufek, Alberto Marchisio, Vojtech Mrazek, Lukas Sekanina, Muhammad Shafique"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06955v1"
tags: ["query:llm"]
score: 9.0
evidence: 嵌入式硬件上的KV缓存内存占用分析与优化
tldr: 针对嵌入式Transformer推理中KV缓存带来的内存压力，本文提出TRAPTI分析框架。该框架通过周期级仿真获取内存占用轨迹，并结合离线优化流程，探索SRAM分体组织与电源门控配置。实验对比了MHA与GQA架构，发现DeepSeek-R1在相同配置下峰值内存占用降低了2.72倍，显著提升了能效优化空间。
selection_source: fresh_fetch
motivation: 嵌入式硬件中Transformer推理受限于KV缓存随序列长度增长带来的内存、延迟和功耗挑战。
method: 提出TRAPTI两阶段方法，结合周期级推理仿真获取内存轨迹，并利用该轨迹进行离线SRAM分体与电源门控优化。
result: 在相同加速器配置下，采用GQA的DeepSeek-R1比采用MHA的GPT-2 XL峰值内存占用降低了2.72倍。
conclusion: 通过时间分辨的内存占用分析，可以有效指导嵌入式Transformer加速器的存储架构设计与低功耗优化。
---

## 摘要
Transformer 神经网络在语言和视觉任务中达到了最先进的准确率，但其在嵌入式硬件上的部署受到严格的面积、延迟和能耗限制的阻碍。在推理过程中，性能和效率日益受到键值（KV）缓存的主导，其内存占用随序列长度增加而增长，给片上内存利用率带来了压力。尽管与多头注意力（MHA）相比，分组查询注意力（GQA）等现有机制减少了 KV 缓存需求，但要有效利用这种减少，需要了解片上内存需求随时间演变的情况。本研究提出了 TRAPTI，这是一种两阶段方法论，它将周期级推理模拟与片上内存占用的时间分辨分析相结合，以指导设计决策。在第一阶段，该框架从模拟中获取内存占用轨迹和内存访问统计数据。在第二阶段，该框架利用这些轨迹在离线优化流程中探索分体内存组织和功率门控配置。我们将该方法应用于相同加速器配置下的 GPT-2 XL 和 DeepSeek-R1-Distill-Qwen-1.5B，从而实现了 MHA 和 GQA 内存特性的直接比较。分析表明，在此设置下，与 GPT-2 XL 相比，DeepSeek-R1-Distill-Qwen-1.5B 的峰值片上内存利用率降低了 2.72 倍，为功率门控优化释放了更多机会。

## Abstract
Transformer neural networks achieve state-of-the-art accuracy across language and vision tasks, but their deployment on embedded hardware is hindered by stringent area, latency, and energy constraints. During inference, performance and efficiency are increasingly dominated by the Key--Value (KV) cache, whose memory footprint grows with sequence length, straining on-chip memory utilization. Although existing mechanisms such as Grouped-Query Attention (GQA) reduce KV cache requirements compared to Multi-Head Attention (MHA), effectively exploiting this reduction requires understanding how on-chip memory demand evolves over time. This work presents TRAPTI, a two-stage methodology that combines cycle-level inference simulation with time-resolved analysis of on-chip memory occupancy to guide design decisions. In the first stage, the framework obtains memory occupancy traces and memory access statistics from simulation. In the second stage, the framework leverages the traces to explore banked memory organizations and power-gating configurations in an offline optimization flow. We apply this methodology to GPT-2 XL and DeepSeek-R1-Distill-Qwen-1.5B under the same accelerator configuration, enabling a direct comparison of MHA and GQA memory profiles. The analysis shows that DeepSeek-R1-Distill-Qwen-1.5B exhibits a 2.72x reduction in peak on-chip memory utilization in this setting compared to GPT-2 XL, unlocking further opportunities for power-gating optimization.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TRAPTI** 的两阶段分析框架，旨在优化嵌入式 Transformer 推理中的片上 SRAM 存储组织与功耗。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：Transformer 模型（尤其是大语言模型）在嵌入式设备上部署时，面临严峻的内存、延迟和能耗挑战。其中，**KV 缓存（Key-Value Cache）** 随序列长度线性增长，成为片上内存占用的主导因素。
*   **研究动机**：虽然 GQA（分组查询注意力）等机制减少了 KV 缓存，但现有的设计空间探索（DSE）工具通常只关注峰值内存或总量，缺乏**时间分辨（Time-Resolved）**的动态分析。如果不了解内存占用随时间的变化规律，就无法有效利用“分体（Banking）”和“功率门控（Power Gating）”技术来降低静态功耗（漏电功耗）。

### 2. 方法论：TRAPTI 框架
TRAPTI 采用两阶段工作流，将周期级仿真与离线优化解耦：
*   **第一阶段：仿真与内存占用分析**
    *   使用 **TransInferSim** 仿真器对特定工作负载（如 GPT-2）和硬件配置进行周期级仿真。
    *   提取**时间分辨的 SRAM 占用轨迹**，区分“必需数据（Needed）”、“废弃数据（Obsolete）”和“空闲空间（Free）”。
    *   通过迭代调整，确定不产生容量诱导写回（Capacity-induced write-backs）的最小 SRAM 容量。
*   **第二阶段：SRAM 分体与功率门控探索**
    *   **分体映射**：将总容量 $C$ 划分为 $B$ 个分体。引入**裕量因子 $\alpha$**（如 0.9）来模拟非理想的数据打包。
    *   **动态计算活跃分体数**：公式为 $B_{act}(t) = \lceil o(t) / (\alpha \cdot C/B) \rceil$，其中 $o(t)$ 是 $t$ 时刻的内存占用。
    *   **能耗评估**：结合 CACTI 工具提供的面积、访问能耗和漏电功率，计算总能耗（动态访问 + 静态漏电 + 状态切换开销）。
    *   **功率门控策略**：仅当空闲间隔超过“盈亏平衡点（Break-even duration）”时才关闭分体，以抵消唤醒开销。

### 3. 实验设计
*   **对比模型**：
    1.  **GPT-2 XL**：代表传统的多头注意力（MHA）机制。
    2.  **DeepSeek-R1-Distill-Qwen-1.5B**：代表现代的分组查询注意力（GQA）机制。
*   **硬件配置**：
    *   4 个 128×128 的脉动阵列（Systolic Array），频率 1GHz。
    *   共享片上 SRAM（初始 128 MiB），8位量化。
*   **实验变量**：
    *   SRAM 容量（48 MiB 至 128 MiB）。
    *   分体数量 $B$（1, 2, 4, 8, 16, 32）。
    *   内存层级（单层共享 SRAM vs. 多层专用内存 DM）。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的训练 GPU 型号或时长，因为该研究侧重于**推理阶段的架构仿真**。
*   **仿真环境**：实验基于 TransInferSim 周期级仿真器和 CACTI 7 存储建模工具，技术节点设定为 45nm。

### 5. 实验数量与充分性
*   **实验规模**：论文对两种主流注意力机制进行了深入对比，并对 SRAM 容量和分体数进行了详尽的参数扫描（Sweep）。此外，还包含了一个多层内存层级的扩展案例分析。
*   **充分性评价**：实验设计较为客观，通过固定硬件模板来公平对比 MHA 和 GQA 的内存特性。引入 $\alpha$ 因子考虑了实际实现的开销，增强了结论的可靠性。但实验仅涵盖了两个模型，样本量略显单一。

### 6. 主要结论与发现
*   **GQA 的优势**：在相同配置下，采用 GQA 的 DeepSeek-R1 比采用 MHA 的 GPT-2 XL **峰值内存占用降低了 2.72 倍**，推理速度快 1.89 倍。
*   **能效提升**：通过 TRAPTI 优化，SRAM 能耗最高可降低 **78%**。
*   **优化空间**：GQA 模型由于内存占用波动大且存在较长空闲期，比 MHA 模型更能从功率门控中获益（优化潜力高出约 20%）。
*   **分体边际效应**：当分体数超过 16 后，能耗降低趋于平缓，而面积开销持续增加。

### 7. 优点
*   **时间维度分析**：突破了传统 DSE 仅看静态指标的局限，捕捉了推理过程中的动态内存需求。
*   **解耦设计**：两阶段流程允许在不重新运行耗时仿真的情况下，快速评估多种存储组织方案。
*   **实用性强**：直接针对当前大模型推理中的 KV 缓存痛点，为嵌入式加速器设计提供了量化指导。

### 8. 不足与局限
*   **模型覆盖有限**：仅对比了 GPT-2 和 DeepSeek-R1，未测试更多规模或不同任务（如视觉 Transformer）的模型。
*   **简化假设**：忽略了位置编码（Positional Encoding）的操作，虽然其对内存趋势影响小，但在极端紧凑的设计中仍有意义。
*   **仿真局限**：所有结论均基于仿真模型（TransInferSim + CACTI），缺乏真实硅片（Tape-out）或 FPGA 原型的实测数据验证。
*   **数据打包假设**：假设数据在分体中是连续打包的，实际硬件中的内存分配可能更复杂。

（完）
