---
title: Does a Global Perspective Help Prune Sparse MoEs Elegantly?
title_zh: 全局视角是否有助于优雅地剪枝稀疏混合专家模型？
authors: "Zeliang Zhang, Nikhil Ghosh, Jiani Liu, Bin Yu, Xiaodong Liu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06542v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对稀疏混合专家大语言模型的全局冗余感知剪枝策略
tldr: 针对稀疏混合专家模型（MoE）因专家参数过多导致内存消耗巨大的问题，本文提出了GRAPE。该方法摒弃了传统层内均匀分配剪枝预算的做法，通过全局视角动态评估跨层冗余度并分配预算。在Mixtral、DeepSeek等多个主流模型上的实验证明，GRAPE在相同剪枝比例下显著优于现有局部剪枝方法，有效提升了模型压缩效率。
selection_source: fresh_fetch
motivation: 现有的MoE剪枝方法通常在各层均匀分配预算，忽略了不同层之间专家冗余度的异质性。
method: 提出GRAPE策略，通过全局冗余感知动态分配剪枝预算，实现跨层专家的优化剪枝。
result: "在多个主流MoE模型上的实验显示，GRAPE相比最强局部基准平均准确率提升了1.40%，最高提升达2.45%。"
conclusion: 采用全局视角进行冗余评估能更优雅且高效地压缩稀疏MoE模型，在保持性能的同时显著降低内存开销。
---

## 摘要
语言模型的经验性缩放法则促使了超大规模语言模型（LLMs）的发展，尽管其计算和内存成本不断增加。稀疏混合专家模型（Sparse MoEs）提供了一种极具前景的替代方案，通过在每次前向传播中仅激活一部分专家，在不牺牲性能的情况下提高了效率。然而，大量的专家参数仍然导致了巨大的内存消耗。现有的剪枝方法通常在各层之间均匀分配预算，忽略了稀疏 MoE 中出现的异构冗余。我们提出了 GRAPE（全局冗余感知专家剪枝），这是一种根据跨层冗余动态分配剪枝预算的全局剪枝策略。在 Mixtral-8x7B、Mixtral-8x22B、DeepSeek-MoE、Qwen-MoE 和 GPT-OSS 上的实验表明，在相同的剪枝预算下，GRAPE 始终能获得最佳的平均性能。在论文报告的三个主要模型中，在各种剪枝设置下，它比最强的局部基准模型平均准确率提高了 1.40%，最高增幅达 2.45%。

## Abstract
Empirical scaling laws for language models have encouraged the development of ever-larger LLMs, despite their growing computational and memory costs. Sparse Mixture-of-Experts (MoEs) offer a promising alternative by activating only a subset of experts per forward pass, improving efficiency without sacrificing performance. However, the large number of expert parameters still leads to substantial memory consumption.   Existing pruning methods typically allocate budgets uniformly across layers, overlooking the heterogeneous redundancy that arises in sparse MoEs. We propose GRAPE (Global Redundancy-Aware Pruning of Experts, a global pruning strategy that dynamically allocates pruning budgets based on cross-layer redundancy. Experiments on Mixtral-8x7B, Mixtral-8x22B, DeepSeek-MoE, Qwen-MoE, and GPT-OSS show that, under the same pruning budget, GRAPE consistently achieves the best average performance. On the three main models reported in the paper, it improves average accuracy over the strongest local baseline by 1.40% on average across pruning settings, with gains of up to 2.45%.

---

## 论文详细总结（自动生成）

这篇论文由罗切斯特大学、加州大学伯克利分校和微软研究院的研究人员共同完成，探讨了如何通过全局视角更有效地剪枝稀疏混合专家模型（MoE）。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：稀疏混合专家模型（MoE）虽然通过仅激活部分专家提高了计算效率，但其庞大的参数量带来了巨大的**内存开销**。
*   **研究动机**：现有的 MoE 剪枝方法（如基于访问频率、路由引导或特征相似度的方法）通常采用“局部均匀分配”策略，即在每一层剪掉相同数量的专家。然而，作者观察到 **MoE 不同层之间的冗余度存在显著差异**（通常后期层冗余度更高）。这种忽略层间异质性的做法限制了剪枝的效率和模型性能的保持。

### 2. 方法论：GRAPE 策略
GRAPE（Global Redundancy-Aware Pruning of Experts）的核心思想是**根据跨层冗余度动态分配剪枝预算**。
*   **冗余度度量**：利用中心核对齐（CKA）或均方误差（MSE）构建每层专家的成对相似度矩阵 $D_l$，并计算层平均冗余得分 $R_l$。
*   **全局目标函数**：旨在最小化全模型范围内保留专家的总相似度，而非仅仅是单层内的相似度。
*   **全局熵正则化（Global Entropy）**：为了防止剪枝过度集中在某些高冗余层（导致模型崩溃或层失衡），引入了基于保留专家分布的熵 $E$。如果某层的剪枝导致熵低于设定阈值，则暂时“冻结”该层。
*   **带重启的贪婪剪枝算法**：
    1.  初始化所有专家为独立簇。
    2.  在未冻结的层中，选择冗余度最高的层。
    3.  在该层内合并最相似的一对专家。
    4.  更新全局熵，若低于阈值则冻结该层。
    5.  若所有层都被冻结但未达到剪枝目标，则重置冻结状态（重启）继续剪枝，直至达到目标专家总数 $K$。

### 3. 实验设计
*   **实验对象（Models）**：Mixtral-8x7B、Mixtral-8x22B、DeepSeek-MoE-16B、Qwen-MoE、GPT-OSS（20B）。
*   **评估基准（Benchmarks）**：MMLU（多任务理解）、BoolQ（布尔问答）、OpenBookQA、RTE（蕴含识别）。
*   **对比方法（Baselines）**：
    *   **Router-guided**：基于路由策略合并专家。
    *   **Count-guided (Expert Trimming)**：基于专家访问频率剪枝。
    *   **Enumerate**：基于损失景观搜索冗余专家。
    *   **DEK**：基于输出表示的特征相似度剪枝（目前的局部最强基准）。
*   **剪枝设置**：对比了每层平均剪掉 2 个专家（2e）和 4 个专家（4e）的场景。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练/剪枝时长。
*   **效率提示**：由于该方法属于“One-shot”剪枝且在实验中禁用了所有微调阶段（Task-agnostic setting），其计算开销主要集中在计算专家相似度矩阵（通常使用校准数据集），相比需要重新训练的方法，其算力需求较低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种主流 MoE 架构，并在 4 个核心评测集（含 MMLU 的多个子类）上进行了测试。
*   **充分性**：实验设计较为充分，不仅对比了多种不同原理的基准方法，还通过可视化展示了不同模型的层间冗余分布，证明了“全局视角”的必要性。
*   **客观性**：通过在相同总预算下对比“局部均匀”与“全局动态”分配，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：在相同剪枝预算下，GRAPE 始终优于所有局部剪枝基准。在三个主要模型上，平均准确率比最强基准提升了 **1.40%**，最高提升达 **2.45%**。
*   **层间差异**：实验证实了 MoE 模型中早期层通常比后期层更重要（冗余度低），全局策略能自动识别并保护这些关键层。
*   **稳定性**：在大幅度剪枝（如 4e 设置）时，GRAPE 的领先优势通常比轻微剪枝时更明显，显示出更好的鲁棒性。

### 7. 优点与亮点
*   **视角创新**：首次系统性地提出并证明了 MoE 剪枝中“跨层预算分配”比“层内专家筛选”更关键。
*   **即插即用**：作为一种后处理剪枝策略，不需要昂贵的微调，且能与现有的专家相似度度量方法结合。
*   **安全性设计**：引入全局熵和重启机制，有效解决了全局优化可能导致的层失衡问题。

### 8. 不足与局限
*   **极端情况下的风险**：在 DeepSeek-MoE 等层间冗余极度不均的模型上，虽然 GRAPE 仍有提升，但提升幅度收窄，且存在模型崩溃的潜在风险。
*   **度量指标依赖**：剪枝效果高度依赖于相似度矩阵（如 CKA）的准确性，目前尚缺乏一种公认的、能完美衡量 MoE 专家重要性的单一指标。
*   **硬件加速未讨论**：论文主要关注参数量和内存的减少，未深入探讨剪枝后的非均匀结构在实际推理引擎中的硬件加速效率。

（完）
