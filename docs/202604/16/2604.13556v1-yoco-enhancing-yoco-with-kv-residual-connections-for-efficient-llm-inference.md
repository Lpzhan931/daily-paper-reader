---
title: "YOCO++: Enhancing YOCO with KV Residual Connections for Efficient LLM Inference"
title_zh: YOCO++：通过 KV 残差连接增强 YOCO 以实现高效的大语言模型推理
authors: "You Wu, Ziheng Chen, Yizhen Zhang, Haoyi Wu, Chengting Yu, Yuchi Xu, Wenbo Su, Bo Zheng, Kewei Tu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13556v1"
tags: ["query:llm"]
score: 10.0
evidence: 跨层KV压缩与KV残差连接
tldr: "针对大语言模型推理中KV缓存带来的内存压力，本文提出了YOCO++。它在YOCO跨层KV共享机制的基础上，通过在前半部分各层的KV与底层KV之间引入加权残差连接，增强了模型的表达能力。在保持50% KV缓存压缩率和高效推理效率的同时，YOCO++在性能上超越了原始YOCO及标准Transformer，实现了推理效率与模型精度的平衡。"
selection_source: fresh_fetch
motivation: 现有的跨层KV压缩方法虽然能显著降低推理时的内存消耗，但通常会带来不可忽视的模型性能退化。
method: 在YOCO架构基础上，通过在前半部分各层与底层KV之间建立加权残差连接来增强模型容量。
result: "实验证明YOCO++在50% KV缓存压缩率下取得了SOTA性能，且表现优于标准的Transformer模型。"
conclusion: YOCO++证明了通过改进KV连接结构，可以在不牺牲推理效率的前提下有效提升跨层KV压缩模型的性能。
---

## 摘要
跨层键值（KV）压缩已被发现在大语言模型（LLM）的高效推理中非常有效。尽管这些方法减少了 KV 缓存（KV cache）的内存消耗，但通常会引入不可忽视的性能下降。在这项工作中，我们旨在增强 YOCO 的性能。YOCO 是一种跨层 KV 压缩方法，它将中间层的 KV 与上半部分层共享。我们提出了 YOCO++，这是一种增强型的 YOCO，它在每个下半部分层的 KV 与底层之间引入了加权残差连接。与 YOCO 相比，YOCO++ 在保持相同训练和推理效率的同时提升了模型容量。我们的实验表明，在 50% 的 KV 缓存压缩率下，YOCO++ 在跨层 KV 压缩方法中达到了最先进的性能，优于标准的 Transformer。

## Abstract
Cross-layer key-value (KV) compression has been found to be effective in efficient inference of large language models (LLMs). Although they reduce the memory consumption of the KV cache, such methods usually introduce non-negligible performance degradation. In this work, we aim to enhance the performance of YOCO, a cross-layer KV compression method that shares the KVs of the middle layer with the top-half layers. We propose YOCO++, an enhanced YOCO that incorporates a weighted residual connection between the KVs of each bottom-half layer and the bottom layer. Compared to YOCO, YOCO++ increases model capacity while maintaining the same training and inference efficiency. Our experiments show that YOCO++ achieves state-of-the-art performance among the cross-layer KV compression methods at a 50% KV cache compression rate, outperforming the standard Transformer.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **YOCO++**，这是一种针对大语言模型（LLM）推理效率优化的增强型架构。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：Transformer 架构在推理时的 KV 缓存（KV Cache）是主要的内存瓶颈，限制了批处理大小和上下文长度。
*   **核心问题**：现有的跨层 KV 压缩方法（如 YOCO）虽然能显著减少内存占用，但往往会导致模型性能（如准确率）出现不可忽视的下降。
*   **研究动机**：在不增加推理 I/O 开销的前提下，通过改进 KV 的连接机制，恢复甚至超越标准 Transformer 的性能，同时保持 50% 的 KV 缓存压缩率。

### 2. 论文提出的方法论
*   **核心思想**：在 YOCO 的基础上引入 **KV 残差连接（KV Residual Connections）**。YOCO 原本是将模型分为下半部分（生成 KV）和上半部分（复用中间层 KV），YOCO++ 则在下半部分各层中引入了与底层 KV 的加权组合。
*   **关键技术细节**：
    *   **加权组合**：对于下半部分的每一层 $i$，不再直接缓存该层的 $K_i, V_i$，而是缓存它与第一层 $K_1, V_1$ 的加权和：
        *   $K'_i = \lambda(\alpha_{Ki,1}K_1 + \alpha_{Ki,2}K_i)$
        *   $V'_i = \lambda(\alpha_{Vi,1}V_1 + \alpha_{Vi,2}V_i)$
    *   **可学习权重**：$\alpha$ 是每层特有的可学习参数。
    *   **缩放因子 ($\lambda$)**：引入固定的超参数 $\lambda$（实验设定为 35），用于放大残差权重的梯度，解决训练初期权重难以学习的问题。
    *   **推理优化**：在解码阶段，直接缓存组合后的 $K'_i, V'_i$，因此不会增加额外的显存读取（I/O）负担。

### 3. 实验设计
*   **模型配置**：基于 TinyLlama 架构（1.1B 参数，22 层，32 个注意力头）。
*   **数据集**：使用 SlimPajama 数据集的 100B token 子集进行从头训练。
*   **Benchmark**：
    *   **常识推理任务**：HellaSwag, OpenBookQA, WinoGrande, ARC-e/c, PIQA。
    *   **效率指标**：首字延迟（TTFT）、解码吞吐量。
*   **对比方法**：标准 Transformer、原始 YOCO、FusedKV、FusedKV-Lite。

### 4. 资源与算力
*   **训练算力**：
    *   主实验：使用 **32 台 NVIDIA H800 (80G) GPU**。
    *   消融实验：使用 8 台 NVIDIA H800 (80G) GPU。
*   **推理测试**：在 **NVIDIA H20 (96GB) GPU** 上进行。
*   **训练细节**：Batch size 为 512K tokens，采用 AdamW 优化器，最高学习率 $3 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：在 100B token 上进行完整训练，这对于 1.1B 规模的模型来说是非常充分的验证。
*   **消融实验**：针对“缩放因子 $\lambda$ 的必要性”以及“是否需要 Key 的残差连接”进行了专门的对比实验（基于 10B token 训练）。
*   **公平性**：所有对比模型均在相同的训练配置和硬件环境下运行，确保了结果的客观性。

### 6. 主要结论与发现
*   **性能超越**：YOCO++ 在所有下游任务的平均准确率上不仅超过了原始 YOCO，还超过了未压缩的标准 Transformer（48.99% vs 48.37%）。
*   **效率保持**：YOCO++ 的推理延迟和吞吐量与 YOCO 持平，预填充延迟仅为标准 Transformer 的一半，且支持更大的 Batch Size。
*   **残差作用**：实验发现 Value 的残差连接比 Key 更为关键，但两者结合效果最好；缩放因子 $\lambda$ 对权重学习至关重要。

### 7. 优点
*   **极低开销**：每层仅增加 4 个标量参数，计算量增加几乎可忽略不计。
*   **I/O 友好**：通过缓存组合后的 KV，巧妙避开了 FusedKV 等方法在解码时需要读取多层 KV 的 I/O 瓶颈。
*   **打破权衡**：证明了 KV 压缩不一定以牺牲精度为代价，合理的结构设计可以实现“压缩且增强”。

### 8. 不足与局限
*   **模型规模限制**：实验仅在 1.1B 参数的小模型上完成，尚未在大规模模型（如 70B+）上验证其扩展性（Scaling Law）。
*   **超参数敏感性**：缩放因子 $\lambda=35$ 是经验值，不同规模或架构的模型可能需要重新调优。
*   **架构依赖**：该方法目前主要针对 YOCO 这种特定的跨层共享架构进行优化，对其他压缩方案（如稀疏注意力）的兼容性未知。

（完）
