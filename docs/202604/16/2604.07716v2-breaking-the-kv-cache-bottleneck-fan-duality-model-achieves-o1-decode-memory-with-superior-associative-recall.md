---
title: "Breaking the KV Cache Bottleneck: Fan Duality Model Achieves O(1) Decode Memory with Superior Associative Recall"
title_zh: 突破 KV 缓存瓶颈：Fan 对偶模型实现具有卓越关联召回能力的 O(1) 解码显存
authors: Yasong Fan
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07716v2"
tags: ["query:llm"]
score: 9.0
evidence: 实现O(1)解码内存的架构以突破KV缓存瓶颈
tldr: 针对 Transformer KV 缓存随序列长度增长的瓶颈，本文提出 Fan Duality Model (FDM)。该模型通过结合基于 Givens 旋转的波动组件（压缩长程信息）和固定槽位的粒子组件（实现关联检索），实现了 O(1) 的解码内存占用。通过 Freeze-Scan 训练策略和全息参考光束解码，FDM 在保持极低内存的同时，在关联回想任务和语言建模性能上显著优于 Transformer 和纯循环模型，为长序列处理提供了高效的新架构。
selection_source: fresh_fetch
motivation: 旨在解决序列建模中内存效率与关联回想能力之间的矛盾，消除 Transformer KV 缓存随长度线性增长的瓶颈。
method: 提出 FDM 架构，结合 Givens 旋转循环扫描与固定大小的关联缓存，并采用 Freeze-Scan 两阶段训练策略和全息参考光束解码。
result: "FDM 实现了恒定的解码内存占用，在 MQAR 任务上准确率达 0.966（比 Transformer 高 59.5%），且在 WikiText-103 上收敛速度和性能显著提升。"
conclusion: FDM 证明了通过波粒二象性设计可以实现高效且具备强大检索能力的线性序列模型，成功打破了 KV 缓存的瓶颈。
---

## 摘要
我们提出了 FDM（Fan 对偶模型），这是一种线性序列架构，解决了序列建模中内存效率与关联召回之间的根本矛盾。FDM 将序列处理分为两个组件：波动组件（通过保相 Givens 旋转进行的循环扫描），将长程模式压缩到固定大小的复数隐藏状态中；以及粒子组件（局部-全局缓存），通过学习到的关联寻址检索特定标记，其具有 W+K=272 个与序列长度 N 无关的插槽。这实现了严格的 O(1) 解码显存：在 128 到 8,192 个标记的所有提示长度下，显存固定为 867 MB，而 Transformer 的显存为 853-4,247 MB（在 N=8,192 时减少了 4.9 倍）。除了架构之外，我们发现联合训练波动和粒子组件会导致次优收敛。我们提出了 Freeze-Scan，这是一种两阶段训练策略，它冻结循环扫描并与嵌入层联合优化缓存，在 WikiText-103 上仅用 44K 步就达到了 PPL=64.9——比全量微调（PPL=487）提升了 7.5 倍。在多查询关联召回（MQAR）任务上，FDM 达到了 0.966 的准确率，比 Transformer (0.606) 高出 59.5%，而没有缓存的纯扫描得分仅为 0.011，证实了粒子组件的必要性。最后，我们引入了全息参考光束解码（Holographic Reference Beam Decoding），将复数隐藏状态 h_t 解释为编码整个时间历史的全息板。使用当前输入 x_t 作为参考光束来调制 h_t，通过仅使用 1.3M 额外参数的 4 头正交参考光束，将 PPL 降低了多达 2.13 点（PPL=62.79），为全息解释提供了实证支持。代码和预训练权重见：https://github.com/YasongFan/FDM

## Abstract
We present FDM (Fan Duality Model), a linear sequence architecture that resolves the fundamental tension between memory efficiency and associative recall in sequence modeling. FDM separates sequence processing into two components: a wave component (recurrent scan via phase-preserving Givens rotations) that compresses long-range patterns into a fixed-size complex hidden state, and a particle component (local-global cache) that retrieves specific tokens via learned associative addressing with W+K=272 slots independent of sequence length N. This yields strictly O(1) decode memory: 867 MB fixed across all prompt lengths 128-8,192 tokens, versus Transformer's 853-4,247 MB (4.9x reduction at N=8,192).   Beyond the architecture, we discover that jointly training the wave and particle components leads to suboptimal convergence. We propose Freeze-Scan, a two-phase training strategy that freezes the recurrent scan and optimizes the cache jointly with embeddings, achieving PPL=64.9 on WikiText-103 in 44K steps -- a 7.5x improvement over full fine-tuning (PPL=487).   On Multi-Query Associative Recall (MQAR), FDM achieves 0.966 accuracy, surpassing Transformer (0.606) by 59.5%, while pure scan without cache scores only 0.011, confirming the necessity of the particle component.   Finally, we introduce Holographic Reference Beam Decoding, interpreting the complex hidden state h_t as a holographic plate encoding the entire temporal history. Using the current input x_t as a reference beam to modulate h_t reduces PPL by up to 2.13 points (PPL=62.79) with a 4-head orthogonal reference beam using only 1.3M additional parameters, providing empirical support for the holographic interpretation.   Code and pretrained weights: https://github.com/YasongFan/FDM

---

## 论文详细总结（自动生成）

### 论文总结：Breaking the KV Cache Bottleneck (FDM)

#### 1. 核心问题与整体含义
论文旨在解决 Transformer 架构在处理长序列时的核心瓶颈：**KV 缓存（KV Cache）随序列长度线性增长导致的显存爆炸问题**。虽然现有的线性循环模型（如 Mamba、RWKV）能实现恒定的推理显存，但它们在“关联召回”（Associative Recall）任务上表现较弱。作者提出了“波粒二象性僵局”理论，认为单一线性算子无法同时兼顾“保持所有历史信息（波动性）”和“选择性遗忘/检索（粒子性）”。FDM 模型通过显式分离这两个组件，试图在保持 $O(1)$ 解码显存的同时，获得超越 Transformer 的检索能力。

#### 2. 方法论
FDM 模型由以下核心技术构成：
*   **波动组件（Fan Operator）：** 使用基于 Givens 旋转的循环扫描机制。它通过保相旋转将长程模式压缩进一个固定维度的复数隐藏状态中，并采用“两遍扫描（Born 近似）”来增强对信息的扰动修正能力。
*   **粒子组件（局部-全局缓存）：** 引入一个固定大小的缓存机制，包含 $W=256$ 个局部窗口插槽和 $K=16$ 个基于学习到的显著性得分（$s_{eff}$）选出的全局插槽。由于插槽总数（272）固定，解码显存不随序列长度 $N$ 变化，实现严格的 $O(1)$ 复杂度。
*   **Freeze-Scan 训练策略：** 发现联合训练波动和粒子组件会导致“梯度汇聚”问题（扫描参数主导梯度，导致缓存训练不足）。该策略先进行全参数训练，随后冻结波动组件，专门优化缓存和嵌入层，显著提升了收敛效率。
*   **全息参考光束解码（Holographic Decoding）：** 将隐藏状态视为“全息板”，利用当前输入作为“参考光束”对隐藏状态进行相位调制，从而精准提取时间相关的语义信息。

#### 3. 实验设计
*   **数据集：** 主要在 **WikiText-103** 上进行语言建模实验，并在 **MQAR**（多查询关联召回）任务上测试检索能力。
*   **Benchmark：** 对标 122M 参数的标准 Transformer 模型。
*   **对比维度：** 验证集困惑度（PPL）、MQAR 准确率、推理显存占用、推理速度（tok/s）以及下游零样本任务（WinoGrande, LAMBADA, HellaSwag）。
*   **消融实验：** 针对缓存大小、全息解码的头数、以及层级全息信息分布进行了深入分析。

#### 4. 资源与算力
*   **模型规模：** 实验使用了 137M 参数的 FDM 模型。
*   **训练细节：** 在 WikiText-103 上训练了 44K 步，序列长度为 1024。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或总训练时长，但提到了解码速度的对比数据。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **充分性：** 涵盖了从基础语言能力（PPL）到特定功能（MQAR），再到工程效率（显存/速度）的全面测试。
*   **客观性：** 通过与 Transformer 的直接对比，展示了 FDM 在显存节省（4.9倍）和检索准确率（提升59.5%）上的显著优势。
*   **消融实验：** 尤其是关于“全息信息集中在第 0 层”的发现，为架构设计提供了理论支撑。不过，实验主要集中在 100M 级规模，尚未在大规模参数（如 7B+）上验证。

#### 6. 主要结论与发现
*   **显存突破：** FDM 在 8,192 长度下解码显存固定为 867 MB，而 Transformer 需 4,247 MB。
*   **检索能力：** FDM 在 MQAR 任务上达到 0.966 的准确率，远超 Transformer 的 0.606。
*   **训练效率：** Freeze-Scan 策略使训练效率提升了 14 倍，PPL 从 487 降至 64.9。
*   **全息特性：** 发现超过 90% 的全息可恢复信息集中在模型的最底层（Layer 0），这与物理学中的 AdS/CFT 边界理论相呼应。

#### 7. 优点
*   **架构优雅：** 成功解决了线性模型在关联召回上的短板，同时保持了极高的推理效率。
*   **显存恒定：** 真正实现了 $O(1)$ 的解码显存，对长文本生成极具吸引力。
*   **理论结合：** 引入量子力学（Born 近似）和全息原理来解释和优化模型，具有较强的跨学科创新性。

#### 8. 不足与局限
*   **语言建模精度：** 在小规模数据训练下，其 PPL（64.9）仍高于 Transformer（36.3），需要更大规模的数据来弥补差距。
*   **工程实现：** 目前的解码速度受限于 Python 循环，尚未开发高性能的 CUDA 融合算子（Fused Kernel）。
*   **预填阶段：** 极长序列的预填（Prefill）仍需实现分块优化以进一步降低峰值显存。

（完）
