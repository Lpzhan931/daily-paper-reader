---
title: "MOONSHOT : A Framework for Multi-Objective Pruning of Vision and Large Language Models"
title_zh: MOONSHOT：一种视觉与大语言模型多目标剪枝框架
authors: "Gabriel Afriat, Xiang Meng, Shibal Ibrahim, Hussein Hazimeh, Rahul Mazumder"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13287v1"
tags: ["query:llm"]
score: 10.0
evidence: 大语言模型的多目标剪枝
tldr: 针对大模型压缩中的训练后单次剪枝问题，本文提出MOONSHOT框架。该框架打破了以往方法仅优化单一目标（如层重建误差或二阶泰勒近似）的局限，通过联合优化这两个目标来提升剪枝效果。MOONSHOT作为通用包装器，在保持十亿级参数模型扩展性的同时，显著降低了Llama系列模型的困惑度，并提升了视觉模型在ImageNet上的准确率，为高效模型压缩提供了新方案。
selection_source: fresh_fetch
motivation: 现有的单次剪枝方法通常只优化单一目标，但在不同架构和稀疏度下，没有任何单一目标能始终保持最优效果。
method: 提出一种通用的多目标优化框架，通过联合优化层重建误差和训练损失的二阶泰勒近似，并引入高效的海森矩阵逆计算过程以支持大规模模型。
result: "在Llama-3.2/2上将C4困惑度降低高达32.6%，在ViT和ResNet-50等视觉模型上也实现了显著的准确率提升。"
conclusion: MOONSHOT证明了多目标优化在单次剪枝中的优越性，为视觉和语言模型提供了一种灵活且高效的压缩增强方案。
---

## 摘要
权重剪枝是压缩大型神经网络的一种常用技术。我们关注具有挑战性的训练后单次（post-training one-shot）设置，即在不进行任何重训练的情况下压缩预训练模型。现有的单次剪枝方法通常优化单一目标，例如逐层重构损失或训练损失的二阶泰勒近似。我们强调，在不同的架构和稀疏度水平下，没有任何单一目标能始终保持最有效。受此启发，我们提出了 MOONSHOT，这是一个通用且灵活的框架，通过联合优化逐层重构误差和训练损失的二阶泰勒近似，将任何单目标剪枝方法扩展为多目标形式。MOONSHOT 作为现有剪枝算法的封装器。为了实现这种集成并保持对十亿参数规模模型的可扩展性，我们提出了建模决策，并引入了一种计算逆海森矩阵（inverse Hessian）的高效程序，从而保留了最先进单次剪枝器的效率。当与 Llama-3.2 和 Llama-2 模型上的最先进剪枝方法结合时，MOONSHOT 在 2:4 稀疏度下将 C4 困惑度降低了高达 32.6%，并在七个分类基准测试中将零样本平均准确率提高了多达 4.9 个百分点。在 Vision Transformer 上，它在 70% 稀疏度下将 ImageNet-1k 的准确率提高了 5 个百分点以上；在 ResNet-50 上，它在 90% 稀疏度下获得了 4 个百分点的提升。

## Abstract
Weight pruning is a common technique for compressing large neural networks. We focus on the challenging post-training one-shot setting, where a pre-trained model is compressed without any retraining. Existing one-shot pruning methods typically optimize a single objective, such as a layer-wise reconstruction loss or a second-order Taylor approximation of the training loss. We highlight that neither objective alone is consistently the most effective across architectures and sparsity levels. Motivated by this insight, we propose MOONSHOT, a general and flexible framework that extends any single-objective pruning method into a multi-objective formulation by jointly optimizing both the layer-wise reconstruction error and second-order Taylor approximation of the training loss. MOONSHOT acts as a wrapper around existing pruning algorithms. To enable this integration while maintaining scalability to billion-parameter models, we propose modeling decisions and introduce an efficient procedure for computing the inverse Hessian, preserving the efficiency of state-of-the-art one-shot pruners. When combined with state-of-the-art pruning methods on Llama-3.2 and Llama-2 models, MOONSHOT reduces C4 perplexity by up to 32.6% at 2:4 sparsity and improves zero-shot mean accuracy across seven classification benchmarks by up to 4.9 points. On Vision Transformers, it improves accuracy on ImageNet-1k by over 5 points at 70% sparsity, and on ResNet-50, it yields a 4-point gain at 90% sparsity.

---

## 论文详细总结（自动生成）

以下是对论文《MOONSHOT : A Framework for Multi-Objective Pruning of Vision and Large Language Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**训练后单次剪枝（Post-training One-shot Pruning）**，即在不进行任何重训练的情况下压缩预训练模型。
*   **研究动机**：现有的剪枝方法通常只优化单一目标，要么是**逐层重构误差**（如 SparseGPT、Wanda），要么是**训练损失的二阶泰勒近似/Fisher 信息**（如 CAP）。
*   **核心发现**：作者通过实验证明，没有任何单一目标在所有模型架构和稀疏度下都表现最优。这两个目标捕捉的是互补的参数重要性信号。
*   **整体含义**：提出 MOONSHOT 框架，通过联合优化上述两个目标，提升剪枝后模型的性能边界。

### 2. 方法论
MOONSHOT 是一个通用的“包装器”框架，可增强现有的单目标剪枝算法。
*   **核心思想**：将剪枝问题建模为多目标优化，通过加权系数 $\lambda$ 融合归一化后的重构损失和 Fisher 损失。
*   **关键技术细节**：
    *   **目标函数**：$L_\lambda = \lambda \cdot \text{归一化重构损失} + (1-\lambda) \cdot \text{归一化 Fisher 损失}$。
    *   **海森矩阵（Hessian）处理**：多目标融合会导致海森矩阵失去原有的块对角一致性（不同行的块不再相同），这在处理十亿级参数模型时会带来巨大的计算开销。
    *   **高效逆矩阵计算**：引入 **Woodbury 矩阵恒等式**。利用 Fisher 损失分量的低秩特性（样本数 $N \ll$ 权重维度），通过一次基础逆矩阵计算和若干次低秩修正，实现了与单目标方法相当的计算效率。
*   **算法流程**：MOONSHOT 适配了多种剪枝模式，包括非结构化、2:4 半结构化和结构化剪枝。

### 3. 实验设计
*   **数据集**：
    *   **语言模型**：C4（用于校准和测试困惑度）、WikiText2、PTB；下游任务包括 BoolQ、PIQA、HellaSwag 等 7 个零样本分类基准。
    *   **视觉模型**：ImageNet-1k。
*   **模型（Benchmark）**：
    *   **LLMs**：Llama-3.2 (1B, 3B), Llama-2 (13B)。
    *   **视觉**：DeiT (Tiny, Small, Base), ResNet-50。
*   **对比方法**：SparseGPT、Wanda、OSSCAR（结构化）、CAP、OBC。同时结合了 OWL 和 AlphaPruning 等非均匀稀疏分配策略。

### 4. 资源与算力
论文明确记录了硬件使用情况：
*   **GPU 型号**：NVIDIA A100 (80GB) 用于 Llama-3.2-3B 和 Llama-2-13B；NVIDIA L40 (40GB) 用于 Llama-3.2-1B 和视觉模型。
*   **剪枝耗时**：
    *   Llama-3.2-3B：少于 40 分钟。
    *   Llama-3.2-1B：约 8 分钟。
    *   视觉模型：通过 4 张 L40 并行处理，耗时极短（DeiT-Tiny 约 70 秒）。

### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度覆盖**：涵盖了视觉与语言两大领域，测试了从 5M 到 13B 不同规模的参数模型。
*   **稀疏模式全覆盖**：对比了非结构化、n:m（2:4）和结构化剪枝。
*   **消融实验**：对超参数 $\lambda$ 进行了详尽的扫描（从 0 到 1），验证了多目标融合的必要性。
*   **公平性**：所有对比均在相同的校准数据（128 样本）和相同的评估脚本下进行，并报告了 3 次随机种子的均值和标准差。

### 6. 主要结论与发现
*   **性能提升显著**：在 Llama-3.2 上，MOONSHOT 将 2:4 稀疏下的困惑度降低了多达 32.6%，零样本准确率提升高达 4.9 个百分点。
*   **视觉模型增益**：在 70% 稀疏度下，DeiT-Small 的准确率比 CAP 基准高出 5 个百分点以上。
*   **正交性**：MOONSHOT 可以与现有的层间稀疏分配策略（如 OWL）叠加使用，产生额外的性能增益。
*   **最优权重**：实验发现 $\lambda$ 取中间值（如视觉模型 0.5，LLM 0.9）通常能获得比任何单目标更好的结果。

### 7. 优点
*   **通用性强**：作为一个 Wrapper，可以轻松集成到现有的剪枝算法中。
*   **理论与工程结合**：利用 Woodbury 恒等式解决了多目标带来的计算瓶颈，使多目标剪枝在十亿级模型上具备实用性。
*   **鲁棒性**：通过融合局部（层重构）和全局（训练损失近似）信息，降低了剪枝决策对单一准则失效的敏感度。

### 8. 不足与局限
*   **超参数依赖**：虽然作者给出了默认建议值，但最优的 $\lambda$ 在不同模型和稀疏度下仍有波动，可能需要少量的调优。
*   **计算开销略增**：虽然通过算法优化保持了扩展性，但相比于最简单的 Wanda 等方法，计算梯度和逆矩阵修正仍增加了额外的显存和时间开销。
*   **层级限制**：为了极致的效率，在 LLM 实验中主要针对 Attention 层应用了多目标优化，而 Projection 层有时仍回退到单目标。

（完）
