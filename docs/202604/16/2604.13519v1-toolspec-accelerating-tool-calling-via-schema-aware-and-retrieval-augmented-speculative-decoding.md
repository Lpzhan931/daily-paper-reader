---
title: "ToolSpec: Accelerating Tool Calling via Schema-Aware and Retrieval-Augmented Speculative Decoding"
title_zh: ToolSpec：通过模式感知与检索增强的投机解码加速工具调用
authors: "Heming Xia, Yongqi Li, Cunxiao Du, Mingbo Song, Wenjie Li"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13519v1"
tags: ["query:llm"]
score: 10.0
evidence: 用于加速大模型工具调用的投机解码
tldr: ToolSpec 针对大语言模型工具调用中的高延迟问题，提出了一种结合模式感知与检索增强的投机采样方法。该方法利用工具调用的结构化特征，通过有限状态机（FSM）引导确定性字段生成，并检索历史调用记录作为草稿，实现了即插即用的加速方案。实验表明，ToolSpec 在多个基准测试中显著提升了推理速度，最高可达 4.2 倍加速。
selection_source: fresh_fetch
motivation: 工具调用过程中的多轮交互导致了显著的推理延迟，且工具调用序列具有高度结构化和模式重复的特点。
method: 结合有限状态机（FSM）进行模式约束生成，并利用检索增强技术复用历史工具调用记录作为投机采样的草稿。
result: 在多个基准测试中实现了高达 4.2 倍的加速比，性能显著优于现有的无需训练的投机采样方法。
conclusion: ToolSpec 是一种高效、即插即用的工具调用加速方案，通过挖掘结构化模式和历史规律大幅提升了 LLM 的实时服务能力。
---

## 摘要
工具调用通过使大语言模型（LLM）能够与外部应用程序交互，极大地扩展了其实际效用。随着 LLM 能力的提升，有效的工具使用越来越多地涉及多步、多轮交互以解决复杂任务。然而，随之而来的工具交互增长带来了显著的延迟，为实时 LLM 服务提出了关键挑战。通过实证分析，我们发现工具调用轨迹具有高度结构化、符合受限模式（Schema），且通常表现出重复的调用模式。受此启发，我们提出了 ToolSpec，一种用于加速工具调用的模式感知、检索增强投机解码方法。ToolSpec 利用预定义的工具模式生成准确的草稿，使用有限状态机在确定性的模式标记填充与变量字段的投机生成之间交替。此外，ToolSpec 检索相似的历史工具调用并将其重用为草稿，以进一步提高效率。ToolSpec 提供了一种即插即用的解决方案，可以无缝集成到现有的 LLM 工作流中。在多个基准测试上的实验表明，ToolSpec 实现了高达 4.2 倍的加速，显著优于现有的无需训练的投机解码方法。

## Abstract
Tool calling has greatly expanded the practical utility of large language models (LLMs) by enabling them to interact with external applications. As LLM capabilities advance, effective tool use increasingly involves multi-step, multi-turn interactions to solve complex tasks. However, the resulting growth in tool interactions incurs substantial latency, posing a key challenge for real-time LLM serving. Through empirical analysis, we find that tool-calling traces are highly structured, conform to constrained schemas, and often exhibit recurring invocation patterns. Motivated by this, we propose ToolSpec, a schema-aware, retrieval-augmented speculative decoding method for accelerating tool calling. ToolSpec exploits predefined tool schemas to generate accurate drafts, using a finite-state machine to alternate between deterministic schema token filling and speculative generation for variable fields. In addition, ToolSpec retrieves similar historical tool invocations and reuses them as drafts to further improve efficiency. ToolSpec presents a plug-and-play solution that can be seamlessly integrated into existing LLM workflows. Experiments across multiple benchmarks demonstrate that ToolSpec achieves up to a 4.2x speedup, substantially outperforming existing training-free speculative decoding methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ToolSpec** 的创新方法，旨在通过结合模式感知（Schema-Aware）和检索增强（Retrieval-Augmented）的投机解码技术，显著加速大语言模型（LLM）在工具调用场景下的推理速度。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着 LLM 在复杂任务中频繁进行多步、多轮的工具调用，推理延迟（Latency）成为实时服务的瓶颈。
*   **研究动机**：
    *   **生成延迟是主因**：实证分析发现，在工具调用流程中，生成工具调用序列的时间占总延迟的 80%-96%，远超工具本身的执行时间。
    *   **结构化特征**：工具调用输出（通常为 JSON）具有高度的结构化特征，大部分 Token（如键名、标点）是预定义的，只有少数参数值是变量。
    *   **重复性模式**：LLM 在处理相似请求时，往往会重复调用相同的工具及其参数结构。

### 2. 方法论
ToolSpec 是一种**无需训练、即插即用**的投机解码方案，核心思想是利用工具的先验知识和历史记录来构建高质量的“草稿（Draft）”：
*   **模式感知草稿生成（Schema-Aware Drafting）**：
    *   将工具文档解析为结构化模式（Schema）。
    *   构建**有限状态机（FSM）**，将生成过程划分为：工具名状态（$q_t$）、参数名状态（$q_p$）、参数值状态（$q_v$）和普通文本状态（$q_o$）。
    *   在确定性状态（如填充工具名和参数名）时，直接根据 Schema 生成草稿；在变量状态（参数值）时，结合投机采样生成。
*   **检索增强投机（Retrieval-Augmented Speculation）**：
    *   维护一个历史成功调用的数据存储库。
    *   对于新查询，通过计算隐藏层表示的余弦相似度检索 Top-k 个相似调用。
    *   利用**后缀匹配（Suffix Matching）**从历史记录中提取后续 Token 作为草稿，从而加速重复出现的调用模式。
*   **并行验证**：使用基于树状注意力的机制（Tree-Based Attention），让目标模型在一次前向传播中并行验证多个草稿候选序列。

### 3. 实验设计
*   **数据集/场景**：
    *   **API-Bank**：评估多轮对话中的工具选择和调用。
    *   **ToolAlpaca**：评估通用工具使用能力。
    *   **BFCLv2**（Berkeley Function Calling Leaderboard）：涵盖单步/多步推理及实时执行。
    *   **ToolBench**：大规模真实世界 API 调用基准。
*   **对比方法**：
    *   **Vanilla**：标准自回归解码。
    *   **PLD** (Prompt Lookup Decoding)：基于提示词查找的解码。
    *   **TR** (Token Recycling)：Token 回收技术。
    *   **SAMD**：基于后缀自动机的投机解码。
    *   **Eagle-2/3**：需要额外训练的先进投机解码模型（作为参考）。
*   **测试模型**：Qwen2.5 系列（7B, 14B）、Llama-3.1-8B、Llama-3.2-3B 以及专门训练的 ToolLLaMA。

### 4. 资源与算力
*   **硬件环境**：实验在 2 台 **NVIDIA A100-PCIE (40GB)** GPU 上运行，配备 40 核 CPU。
*   **软件环境**：PyTorch 2.5.1, CUDA 12.4。
*   **训练时长**：由于 ToolSpec 是**无需训练（Training-free）**的方法，因此不存在训练时长，仅涉及推理时的检索和 FSM 维护开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流基准测试和 5 种不同规模/类型的模型。
*   **消融实验**：分别验证了模式感知（SAD）和检索增强（RAS）的贡献，证明两者结合效果最佳。
*   **分析深度**：进行了格式遵循度（Format Adherence）与加速比的相关性分析、时间分配分析（Time Allocation）以及接受 Token 长度的分布分析。
*   **评价**：实验设计全面，对比了多种 SOTA 插件式方法，并涵盖了通用模型与专用模型，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **显著加速**：ToolSpec 在各基准测试中实现了 **3.5x 至 4.2x** 的加速比。
*   **超越 SOTA**：相比之前最强的无需训练方法（如 TR），ToolSpec 实现了高达 61% 的相对性能提升。
*   **低开销**：ToolSpec 引入的额外操作（检索、树构建）仅占总延迟的约 6%，而带来的加速收益巨大。
*   **正相关性**：模型的格式遵循能力越强，ToolSpec 的加速效果越明显（皮尔逊相关系数达 0.90）。

### 7. 优点
*   **即插即用**：不需要对原模型进行任何微调或添加辅助模型，易于集成到现有工作流。
*   **分布保持**：作为投机解码的一种，它完全保持了目标模型的原始输出概率分布，不损失生成质量。
*   **精准预测**：通过 FSM 强制遵循 Schema，解决了工具调用中结构化 Token 生成的冗余问题。
*   **历史复用**：检索机制有效捕捉了工具调用中的长尾重复模式。

### 8. 不足与局限
*   **依赖格式遵循度**：如果模型本身不擅长遵循 JSON 格式或 Schema，该方法的加速效果会打折扣。
*   **冷启动限制**：检索增强部分依赖于历史数据，在没有任何历史记录的全新场景下，RAS 模块无法发挥作用。
*   **模型规模覆盖**：受限于算力，论文未在超大规模模型（如 Qwen2.5-72B）上进行完整测试，尽管作者预期效果会更好。
*   **Schema 假设**：假设工具调用必须严格遵循预定义的 Schema，对于极少数高度动态或非结构化的工具交互可能不适用。

（完）
