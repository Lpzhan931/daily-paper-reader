---
title: "SLaB: Sparse-Lowrank-Binary Decomposition for Efficient Large Language Models"
title_zh: SLaB：面向高效大语言模型的稀疏-低秩-二进制分解
authors: "Ziwei Li, Yuang Ma, Yi Kang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04493v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于剪枝的稀疏-低秩-二进制分解
tldr: "针对大语言模型部署中计算和内存需求巨大的挑战，本文提出了一种名为SLaB的新型压缩框架。该方法将线性层权重分解为稀疏矩阵、低秩矩阵和二进制矩阵三个互补部分。SLaB无需重新训练，并利用激活感知剪枝分数指导分解过程。实验表明，在50%压缩率下，SLaB在Llama系列模型上实现了SOTA性能，显著降低了困惑度并提升了零样本任务的准确率。"
selection_source: fresh_fetch
motivation: 现有模型压缩方法在高压缩率下往往难以维持良好的性能，限制了大语言模型的低成本部署。
method: 提出将权重矩阵分解为稀疏、低秩和二进制三个互补矩阵的SLaB框架，并利用激活感知剪枝分数进行指导。
result: "在Llama模型上，SLaB在50%压缩率下将困惑度降低了36%，零样本任务准确率比基准提升了8.98%。"
conclusion: SLaB通过创新的三矩阵分解机制，在无需重训的情况下实现了高效且高性能的大语言模型压缩。
---

## 摘要
大语言模型（LLMs）的快速增长因其巨大的计算和内存需求而带来了显著的部署挑战。虽然模型压缩（如网络剪枝）提供了潜在的解决方案，但大多数现有方法在面对高压缩率时往往难以保持良好的性能。为解决这一问题，我们提出了 SLaB，这是一个将每个线性层权重分解为三个互补组件的新型框架：稀疏矩阵、低秩矩阵和二进制矩阵。SLaB 无需重新训练，并利用激活感知的剪枝分数来指导分解过程。在 Llama 系列模型上的实验表明，SLaB 达到了最先进的性能，在 50% 压缩率下，与现有方法相比困惑度降低了高达 36%，在零样本任务上比基准模型准确率提高了高达 8.98%。

## Abstract
The rapid growth of large language models (LLMs) presents significant deployment challenges due to their massive computational and memory demands. While model compression, such as network pruning, offers potential solutions, most existing methods often fail to maintain good performance at high compression ratios. To address this, we propose SLaB, a novel framework that decomposes each linear layer weight into three complementary components: a sparse matrix, a low-rank matrix, and a binary matrix. SLaB eliminates the need for retraining and leverages activation-aware pruning scores to guide the decomposition process. Experiments on Llama-family models demonstrate that SLaB achieves state-of-the-art performance, reducing perplexity by up to 36% compared to existing methods at 50% compression and improving accuracy by up to 8.98% over the baseline on zero-shot tasks.

---

## 论文详细总结（自动生成）

### SLaB：面向高效大语言模型的稀疏-低秩-二进制分解论文总结

#### 1. 核心问题与整体含义
随着大语言模型（LLMs）规模的剧增，其巨大的计算和内存需求给资源受限设备的部署带来了挑战。现有的模型压缩技术（如剪枝和低秩分解）在面对高压缩率时，往往会出现显著的性能下降。本文的研究动机在于：单纯的剪枝会丢失关键权重信息，而简单的“剪枝+低秩”组合效果并不理想。因此，论文提出了一种名为 **SLaB** 的新型分解框架，旨在通过协同稀疏、低秩和二进制三种矩阵形式，在无需重新训练的情况下，实现高压缩率与高性能的平衡。

#### 2. 方法论
SLaB 的核心思想是将每个线性层的权重矩阵 $W$ 分解为三个互补组件：
*   **核心公式**：$W = W_S + W_L \odot W_B$
    *   $W_S$：**稀疏矩阵**，保留权重中最重要的部分。
    *   $W_L$：**低秩矩阵**，用于补偿剪枝带来的性能损失。
    *   $W_B$：**二进制矩阵**（元素为 $\{+1, -1\}$），与低秩矩阵配合，在极低参数量下提供补偿能力。
*   **关键技术细节**：
    *   **交替优化策略**：通过固定其他矩阵、更新单一矩阵的方式进行迭代优化。
    *   **激活感知剪枝**：借鉴 Wanda 方法，利用输入激活值的二范数来衡量权重的重要性，指导 $W_S$ 的生成。
    *   **SVD 初始化**：利用截断奇异值分解（SVD）初始化低秩组件，并证明了在秩为 1 的设置下即可获得显著的性能提升。
    *   **二进制化处理**：通过符号函数（Sign function）将补偿项的一部分转化为二进制形式，以降低存储开销并保持硬件友好性。

#### 3. 实验设计
*   **模型对象**：Llama-2 7B、Llama-3 8B、Llama-3.2 1B。
*   **数据集**：
    *   **校准数据**：从 C4 数据集中随机抽取 128 个序列（长度 2048）。
    *   **评估指标**：WikiText-2 上的困惑度（Perplexity）；7 个零样本（Zero-shot）任务（ARC-C/E, BoolQ, HellaSwag, PIQA, RTE, WinoGrande）的准确率。
*   **Benchmark 与对比方法**：
    *   对比了两种最先进的（SOTA）一发式（One-shot）剪枝方法：**SparseGPT** 和 **Wanda**。
    *   涵盖了非结构化剪枝（50% 到 80% 压缩率）和结构化剪枝（2:4, 4:8 模式）。

#### 4. 资源与算力
*   **文中说明**：论文提到使用了 PyTorch 和 Hugging Face 的 Transformers 库进行实现。
*   **缺失信息**：文中**未明确说明**具体的 GPU 型号、数量以及具体的运行/训练时长。由于该方法属于 One-shot（无需重训）方法，通常其计算开销远低于需要微调的方法。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同规模的模型上进行了测试，涵盖了从 50% 到 80% 的多种压缩梯度，并对比了结构化与非结构化两种剪枝模式。
*   **消融实验**：进行了针对 $W_S$、$W_L$ 和 $W_B$ 各组件贡献的消融研究，以及针对秩（Rank）大小和迭代次数（Iterations）的超参数探索。
*   **充分性评价**：实验设计较为全面，对比了主流的 SOTA 基准，且在不同架构（Llama-2/3）上表现出一致性，证明了方法的鲁棒性和公平性。

#### 6. 主要结论与发现
*   **性能领先**：在 50% 压缩率下，SLaB 在 Llama-3.2 1B 上比现有最佳方法降低了 36.04% 的困惑度，零样本任务准确率提升了 8.98%。
*   **低秩补偿效应**：实验发现，将秩设为 1 就能显著降低压缩模型与原始模型之间的 Frobenius 范数差异，更高的秩带来的边际收益递减。
*   **无需重训**：该方法在不进行任何参数微调的情况下，依然能保持极强的语言建模能力。

#### 7. 优点
*   **创新性分解**：首次提出将稀疏、低秩与二进制三种特性结合，利用二进制矩阵的硬件友好性来降低低秩部分的参数负担。
*   **高效性**：作为一种 One-shot 方法，它避免了昂贵的重训过程，适合快速部署。
*   **高压缩率下的稳定性**：在 70%-80% 的极高压缩率下，性能衰减远小于传统的剪枝方法。

#### 8. 不足与局限
*   **模型覆盖面**：实验仅限于 Llama 系列模型，尚未在 Encoder-only（如 BERT）或 MoE 架构上验证。
*   **硬件实测缺失**：虽然理论上二进制和稀疏矩阵是硬件友好的，但论文未提供在特定硬件（如移动端或 FPGA）上的实际推理延迟（Latency）数据。
*   **秩的限制**：为了简化计算采用了 Rank-1 设置，对于极其复杂的层，这可能限制了补偿能力的上限。

（完）
