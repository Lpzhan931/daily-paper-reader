---
title: "MUXQ: Mixed-to-Uniform Precision MatriX Quantization via Low-Rank Outlier Decomposition"
title_zh: MUXQ：通过低秩离群值分解实现的混合到均匀精度矩阵量化
authors: "Seoungsub Lee, In Seo Kim, Seon Wook Kim"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04701v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于LLM推理加速的量化技术
tldr: 针对大语言模型在边缘设备上推理时面临的内存和计算开销问题，本文提出MUXQ量化方法。该方法通过低秩离群值分解，利用小型辅助矩阵重新分配激活值中的离群通道量级，将复杂的混合精度需求转化为硬件友好的均匀精度量化。在GPT-2模型上的实验证明，MUXQ能在保持接近FP16精度的同时，实现高效的INT8量化推理，显著提升了边缘侧的推理效率。
selection_source: fresh_fetch
motivation: 现有量化方法在处理大模型激活值离群点时，往往难以兼顾量化精度与硬件计算效率。
method: 通过检测激活值中的离群通道并引入小型辅助矩阵重新分配其量级，使离群点也能在低比特均匀量化下保持精度。
result: 在GPT-2系列模型上的测试表明，MUXQ在INT8全张量量化下的困惑度表现优于传统方法，且接近FP16水平。
conclusion: MUXQ通过低秩分解有效解决了激活值离群点问题，为边缘设备上的高效大模型推理提供了新途径。
---

## 摘要
大语言模型（LLMs）在广泛的自然语言处理任务中取得了卓越的性能，但其庞大的参数量带来了巨大的内存和计算开销。这一挑战在基于 NPU 的端侧环境中尤为严峻，因为在这些环境中 FP16/FP32 计算效率低下，因此整数（INT）量化变得至关重要。然而，包括 ZeroQuant、LLM.int8() 和 SmoothQuant 在内的现有方法未能充分解决输入激活值中的离群值（outliers）及其相关的硬件效率低下问题。为了克服这些局限性，我们提出了 MUXQ（混合到均匀量化）。MUXQ 检测输入激活中的离群通道，并引入一个小型辅助矩阵，将离群值的量级重新分配到各个通道中，从而缓解离群值问题。这使得即使是激活离群值也能在保持硬件友好计算结构的同时，以低精度 INT 级别进行量化。在 WikiText-2 数据集上对三种规模（0.1B、0.3B 和 0.7B 参数）的 GPT-2 模型进行的实验表明，MUXQ 始终比朴素量化获得更低的困惑度。特别是在逐张量（per-tensor）量化下，MUXQ 将激活值和权重均量化为 INT8，同时保持了接近 FP16 的准确度。凭借极小的计算开销，MUXQ 实现了稳定的低精度推理，并可轻松与其他量化技术相结合。这些结果表明，MUXQ 为边缘设备上高效且准确的 LLM 推理提供了一个极具前景的方向。

## Abstract
Large language models (LLMs) have achieved outstanding performance across a wide range of natural language processing tasks, but their enormous parameter counts impose ubstantial memory and computational overheads. This challenge is particularly critical in NPU-based on-device environments, where FP16/FP32 computation is inefficient and integer (INT) quantization is therefore essential. However, existing methods, including ZeroQuant, LLM.int8(), and SmoothQuant, do not fully address input-activation outliers and the associated hardware inefficiencies. To overcome these limitations, we propose MUXQ (Mixed-to-Uniform Quantization). MUXQ detects outlier channels in input activations and introduces a small auxiliary matrix that redistributes outlier magnitudes across channels, thereby alleviating the outlier problem. This enables even activation outliers to be quantized at low-precision INT levels while preserving a hardware-friendly computation structure. Experiments on GPT-2 models at three scales (0.1B, 0.3B, and 0.7B parameters) using the WikiText-2 dataset show that MUXQ consistently achieves lower perplexity than naive quantization. In particular, under per-tensor quantization, MUXQ quantizes both activations and weights to INT8 while maintaining accuracy close to that of FP16. With only modest computational overhead, MUXQ enables stable low-precision inference and can be readily combined with other quantization techniques. These results suggest that MUXQ provides a promising direction for efficient and accurate LLM inference on edge devices.

---

## 论文详细总结（自动生成）

以下是对论文《MUXQ: Mixed-to-Uniform Precision MatriX Quantization via Low-Rank Outlier Decomposition》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）在边缘设备（如 NPU）上部署时，面临巨大的内存和计算开销。虽然量化（INT8/INT4）是有效的压缩手段，但激活值中存在的**离群点（Outliers）**（即少数通道中数值极大的元素）会导致量化误差剧增。
*   **研究背景**：
    *   现有方法如 `LLM.int8()` 采用混合精度（对离群点用 FP16，其余用 INT8），但这会导致硬件内存访问不规则，降低计算效率。
    *   `SmoothQuant` 尝试通过平滑激活值来缓解压力，但未能从根本上消除离群点对均匀量化的干扰。
*   **整体含义**：本文提出 **MUXQ**，旨在通过低秩分解技术，将原本需要混合精度处理的离群点重新分配，从而实现硬件友好的**全均匀 INT8 量化**。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：借鉴 LoRA 的低秩思想，但不作用于权重，而是作用于**激活值**。通过引入一个小型辅助矩阵（Auxiliary Matrix），将离群通道的量级“拆分”并“重分布”，使主矩阵的数值分布变得平衡。
*   **关键技术流程**：
    1.  **离群点检测**：识别激活矩阵 $X$ 中绝对值超过阈值（如 6）的离群通道。
    2.  **低秩分解**：将离群通道子矩阵 $X_{outlier}$ 分解为两个部分：
        *   **Body（主体）**：通过位移/缩放降低量级后的矩阵。
        *   **Aux（辅助）**：存储被削减的量级信息。
    3.  **数学表达**：$X_{outlier} = Body_{outlier} + (2^{exp\_factor} - 1) \cdot Aux$。通过设置 $exp\_factor$（如 2），将离群点的动态范围压缩到与普通值相近的水平。
    4.  **硬件友好计算**：在推理时，分别进行两个整数矩阵乘法（GEMM）：$Y_{body} = Body \cdot W$ 和 $Y_{Aux} = Aux \cdot W$，最后通过简单的加权求和合并结果。所有核心运算均在 INT8 下完成。

### 3. 实验设计
*   **数据集**：使用 **WikiText-2** 语言建模数据集。
*   **模型对象**：GPT-2 系列模型，包括 **GPT-2 Small (0.1B)**、**Medium (0.3B)** 和 **Large (0.7B)**。
*   **对比方法（Benchmark）**：
    *   **Naive Quantization**（朴素绝对最大值量化）。
    *   **LLM.int8()**（经典的混合精度量化方法）。
    *   **FP16**（原始浮点精度作为上限）。
*   **量化配置**：涵盖了逐向量（Per-vector）和逐张量（Per-tensor）粒度，并测试了不同激活值位宽（8/7/6/5-bit）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA GeForce RTX 3080 (12 GB)** GPU 上完成。
*   **实现细节**：基于 Hugging Face Transformers 框架，通过“伪量化”（Fake Quantization）来评估准确度。
*   **说明**：文中未明确提及具体的训练/评估总时长，主要侧重于推理阶段的准确性模拟。

### 5. 实验数量与充分性
*   **实验规模**：针对三种不同规模的模型进行了多维度测试，包括不同位宽下的困惑度（Perplexity）对比。
*   **充分性评价**：
    *   **优点**：涵盖了从 0.1B 到 0.7B 的模型梯度，验证了方法在不同规模下的稳定性；对比了不同量化粒度，证明了 MUXQ 在最具挑战性的“逐张量量化”下的优越性。
    *   **不足**：实验仅限于 GPT-2 这一较老模型家族，未在更现代的 Llama 或 Mistral 等模型上验证；仅使用了一个数据集（WikiText-2），缺乏更多样化的 NLP 任务测试。

### 6. 主要结论与发现
*   **性能提升**：MUXQ 的困惑度始终低于朴素量化。在 7-bit 激活量化下，当朴素量化崩溃时，MUXQ 仍能保持接近 FP16 的性能。
*   **均匀量化的可行性**：MUXQ 成功实现了逐张量（Per-tensor）的 INT8 量化，这对于 NPU 等对计算规整性要求极高的硬件具有重要意义。
*   **与 LLM.int8() 的差距**：虽然 MUXQ 的精度略低于 LLM.int8()（因为后者保留了部分 FP16 计算），但 MUXQ 提供了更好的硬件执行效率潜力。

### 7. 优点与亮点
*   **硬件友好性**：消除了混合精度带来的不规则内存访问，使模型能够完全运行在整数算术单元上。
*   **低开销**：辅助矩阵是低秩的，引入的额外计算量相对较小。
*   **高兼容性**：该结构不与其他量化技术冲突，可以与 `SmoothQuant` 等方法叠加使用以进一步提升效果。

### 8. 不足与局限
*   **模型时效性**：实验对象（GPT-2）较为陈旧，无法完全代表当前千亿级参数 LLM 的离群点特性。
*   **缺乏实测性能数据**：论文主要通过伪量化评估准确度，**未提供在真实 NPU 或 GPU 上的推理延迟（Latency）和功耗对比数据**。
*   **计算冗余**：虽然是低秩的，但毕竟增加了一次额外的矩阵乘法和加法操作，在极小模型上可能会抵消量化带来的收益。

（完）
