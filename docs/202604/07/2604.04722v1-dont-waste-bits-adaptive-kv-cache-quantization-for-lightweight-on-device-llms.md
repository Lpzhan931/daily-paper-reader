---
title: "Don't Waste Bits! Adaptive KV-Cache Quantization for Lightweight On-Device LLMs"
title_zh: 别浪费比特！面向轻量化端侧大语言模型的自适应 KV 缓存量化
authors: "Sayed Pedram Haeri Boroujeni, Niloufar Mehrabi, Patrick Woods, Gabriel Hillesheim, Abolfazl Razi"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04722v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对轻量化端侧大模型推理的自适应KV缓存量化
tldr: 针对移动端LLM推理中KV缓存带来的内存和带宽瓶颈，本文提出了一种自适应KV缓存量化框架。该方法受哈夫曼编码启发，通过轻量级控制器根据Token重要性动态分配2/4/8位或FP16精度。实验表明，该方法在SmolLM系列模型上显著降低了推理延迟和内存占用，同时保持了接近全精度的模型性能，优化了端侧部署的效率与精度权衡。
selection_source: fresh_fetch
motivation: 现有的KV缓存量化方案多采用固定精度，导致重要Token被过度压缩或低影响Token浪费存储空间，限制了端侧LLM的推理效率。
method: 提出一种基于学习的自适应量化策略，利用Token频率、注意力方差等特征，通过紧凑控制器动态选择每个Token的KV缓存位宽。
result: "在SmolLM-360M模型上，该方法相比静态量化降低了17.75%的解码延迟，准确率提升7.60分，表现接近FP16推理水平。"
conclusion: 自适应KV缓存量化能有效平衡端侧LLM的内存占用与推理精度，是实现轻量化、高性能端侧AI部署的有效途径。
---

## 摘要
大语言模型（LLMs）在推理、生成和决策任务中取得了显著进展，但在移动、嵌入式和边缘设备上部署它们仍然极具挑战性。端侧 LLM 推理受到键值（KV）缓存的内存和带宽开销的严重限制，该开销随上下文长度线性增长，且通常在解码成本中占据主导地位。现有的 KV 缓存量化方案通常依赖于固定精度或手工设计的启发式方法，从而在低影响的 token 上浪费了比特，同时对信息量大的 token 过度压缩，导致了本可避免的精度下降。受哈夫曼编码（Huffman coding）变长分配原则的启发，我们提出了自适应 KV 缓存量化，这是一种学习到的策略，根据 token 的重要性按比例分配位宽，在不牺牲竞争性精度的前提下，最大限度地减少预期的内存占用和延迟。我们的框架提取轻量级的 token 级特征，包括 token 频率、质量得分、注意力方差和基于熵的不确定性，并将它们输入到一个紧凑的数据驱动控制器中，该控制器在解码过程中动态地从 {2-bit, 4-bit, 8-bit, FP16} 中选择 KV 精度。这种自适应精度策略减少了 KV 内存占用和延迟，同时提高了精度，并在标准 LLM 基准测试中保持了接近 FP16 推理的竞争性精度。在 SmolLM-135M、SmolLM-360M 和 SmolLM-1.7B 上进行的多个常识推理基准测试的大量实验表明，我们的控制器持续改进了精度与延迟之间的权衡。例如，在 HellaSwag 基准测试中使用 SmolLM-360M 时，我们的方法相对于静态 KV 量化将解码延迟（ms/token）降低了 17.75%，精度提高了 7.60 个百分点，且与 FP16 推理的差距仅在 0.30 个百分点以内。

## Abstract
Large Language Models (LLMs) have achieved remarkable progress across reasoning, generation, and decision-making tasks, yet deploying them on mobile, embedded, and edge devices remains particularly challenging. On-device LLM inference is heavily constrained by the memory and bandwidth overhead of the key-value (KV) cache, which grows linearly with context length and often dominates decoding cost. Existing KV-cache quantization schemes typically rely on fixed precision or hand-crafted heuristics, thereby wasting bits on low-impact tokens while over-compressing informative ones, leading to avoidable accuracy degradation. Inspired by Huffman coding's principle of variable-length allocation, we propose adaptive KV-cache quantization, a learned policy that assigns bit-width proportional to token importance, minimizing expected memory and latency without sacrificing competitive accuracy. Our framework extracts lightweight token-level features, including token frequency, quality score, attention variance, and entropy-based uncertainty, and feeds them into a compact data-driven controller that dynamically selects KV precision from {2-bit, 4-bit, 8-bit, FP16} during decoding. This adaptive precision policy reduces KV memory footprint and latency while improving accuracy compared to static KV quantization and rule-based baselines, and maintaining competitive accuracy close to FP16 inference across standard LLM benchmarks. Extensive experiments across multiple commonsense reasoning benchmarks using SmolLM-135M, SmolLM-360M, and SmolLM-1.7B demonstrate that our controller consistently improves the accuracy-latency trade-off. For instance, with SmolLM-360M on HellaSwag, our method reduces decoding latency (ms/token) by 17.75% relative to static KV quantization, improves accuracy by 7.60 points, and remains within only 0.30 points of FP16 inference.

---

## 论文详细总结（自动生成）

以下是对论文《Don't Waste Bits! Adaptive KV-Cache Quantization for Lightweight On-Device LLMs》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在移动端和边缘设备上部署大语言模型（LLM）时，**KV 缓存（Key-Value Cache）**成为了主要的内存和带宽瓶颈。KV 缓存随上下文长度线性增长，消耗大量显存并增加推理延迟。
*   **研究动机**：现有的 KV 缓存量化方法多采用**静态精度**（如统一 4-bit）或简单的手工启发式规则。这种“一刀切”的方法忽略了不同 Token 对模型推理贡献的差异性，导致重要 Token 被过度压缩（损失精度），而无关紧要的 Token 浪费了存储位（浪费比特）。

### 2. 方法论：自适应 KV 缓存量化框架
*   **核心思想**：受哈夫曼编码（Huffman Coding）变长分配原则的启发，提出一种**数据驱动的自适应策略**。根据 Token 的重要性动态分配位宽：重要 Token 使用高精度（如 FP16），次要 Token 使用低精度（如 2-bit）。
*   **关键技术细节**：
    *   **特征提取**：在解码过程中提取四个轻量级 Token 信号：
        1.  **熵（Entropy）**：衡量预测分布的不确定性。
        2.  **稀有度（Rarity）**：基于 Token 频率的自信息得分。
        3.  **注意力方差（Attention Variance）**：反映注意力分布的尖锐程度。
        4.  **置信度（Confidence）**：模型对当前预测的确定程度。
    *   **轻量级控制器**：使用一个紧凑的三层多层感知机（MLP）作为控制器。它接收上述特征向量，输出该 Token 对应的 KV 精度类别（{2, 4, 8, 16} bit）。
    *   **训练目标**：设计了一个多任务损失函数，结合了分类交叉熵（保证预测准确）、预期延迟惩罚（鼓励压缩）和预期质量惩罚（保证模型性能），通过拉格朗日乘子平衡精度与效率。
    *   **算法流程**：在自回归解码的每一步，控制器实时决定新生成 Token 的 KV 存储精度，随后将其量化并存入缓存。

### 3. 实验设计
*   **模型 backbones**：使用了 SmolLM 系列轻量化模型，包括 **SmolLM-135M、360M 和 1.7B**。
*   **数据集/场景**：针对常识推理任务，使用了 **HellaSwag、OpenBookQA (OBQA) 和 ARC-Challenge** 三个基准测试。
*   **对比方法（Baselines）**：
    *   **FP16 推理**：全精度参考。
    *   **静态 4-bit 量化**：固定位宽量化。
    *   **基于规则的动态 KV 策略**：手工设计的启发式规则。
    *   **其他 LLM 家族**：与 OPT、Pythia、Cerebras-GPT、Galactica 等同规模模型进行横向对比。

### 4. 资源与算力
*   **硬件环境**：所有实验均在一台配备 **24 GB 显存的 NVIDIA RTX 4090 GPU** 上完成。
*   **训练细节**：文中提到了学习率（3e-3 或 5e-4）和 Batch Size（1M 或 2M tokens），但未明确说明训练控制器的具体时长。由于控制器（MLP）极其轻量，预计训练开销远小于模型微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 135M 到 1.7B 三种不同参数规模的模型，并在三个主流推理数据集上进行了验证。
*   **充分性评价**：实验设计较为充分。不仅对比了量化基准，还通过与多种同类开源模型的对比证明了 SmolLM 结合该方法后的优越性。实验结果提供了精度与延迟（ms/token）的详细权衡曲线，客观展示了该方法在不同约束下的表现。

### 6. 主要结论与发现
*   **性能提升**：该方法在显著降低延迟的同时，保持了接近 FP16 的精度。例如，SmolLM-360M 在 HellaSwag 上比静态 4-bit 量化降低了 **17.75% 的延迟**，且精度高出 **7.60 个百分点**。
*   **帕累托最优**：自适应策略在“精度-延迟-内存”三者之间找到了比静态量化更好的平衡点，证明了“按需分配比特”的有效性。
*   **端侧友好**：由于控制器和特征提取开销极低，该方法非常适合在资源受限的移动端设备上实时运行。

### 7. 优点与亮点
*   **高效性**：控制器极其轻量（3层 MLP），不会成为推理过程中的新瓶颈。
*   **即插即用**：无需对原始 Transformer 模型进行重新训练或大规模架构修改，易于集成到现有推理引擎中。
*   **理论支撑**：将信息论中的哈夫曼编码思想引入 KV 缓存管理，具有较强的直观解释性。

### 8. 不足与局限
*   **模型覆盖范围**：实验主要集中在 SmolLM 这种小规模模型（<2B）上，尚未验证该策略在 7B 或更大规模模型（如 Llama-3）上的扩展性。
*   **硬件实现细节**：虽然论文讨论了延迟，但在实际移动端 SoC（如骁龙或苹果 A 系列芯片）上的底层算子支持（如异构位宽的混合精度算子）可能存在工程实现难度。
*   **特征依赖**：特征提取依赖于模型内部状态（如注意力权重和 Logits），对于某些仅提供 API 或高度封装的推理框架，可能难以获取这些中间信号。

（完）
