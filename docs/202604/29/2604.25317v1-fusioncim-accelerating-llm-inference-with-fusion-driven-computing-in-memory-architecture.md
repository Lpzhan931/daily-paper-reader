---
title: "FusionCIM: Accelerating LLM Inference with Fusion-Driven Computing-in-Memory Architecture"
authors: "Zihao Xuan, Jia Chen, Yewen Li, Wei Xuan, Hegan Chen, Xiao Huo, Fengbin Tu"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25317v1"
tags: ["query:llm"]
score: 9.0
evidence: 存内计算架构加速大模型推理
tldr: 提出FusionCIM存内计算加速器架构，通过算子融合和数据流优化提升大模型推理效率。
selection_source: fresh_fetch
motivation: 存内计算架构加速大模型推理。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
In this paper, we propose FusionCIM, an operator-fusion-driven compute-in-memory (CIM) accelerator architecture for efficient and scalable LLM inference, with three key innovations: (1) a hybrid CIM pipeline architecture that maps QKT computation on inner-product-based CIM (IP-CIM) and PV aggregation on outer-product-based CIM (OP-CIM) for efficient matrix multiplications fusion; (2) a QO-stationary dataflow that eliminates repeated KV loading in CIM and K-matrix access in buffer under transpose fusion, significantly improving data reuse on chip; and (3) a pattern-aware online-softmax mechanism that exploits distribution regularities of attention scores to reduce exponential rescaling overhead for non-linear fusion. Experimental results on LLaMA-3 model show that FusionCIM achieves up to 3.86x energy saving, and 1.98x speedup compared with prior SOTA CIM-based designs with 29.4 TOPS/W energy efficiency at the system level.

---

## 论文详细总结（自动生成）

这是一份关于论文《FusionCIM: Accelerating LLM Inference with Fusion-Driven Computing-in-Memory Architecture》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：大语言模型（LLM）推理面临严重的计算和存储瓶颈。尽管存内计算（CIM）能减少权重读取开销，但在长文本场景下仍面临三大挑战：
    1.  **激活值访问开销大**：注意力机制中的得分矩阵（$O(N^2)$）导致频繁的片外内存访问。
    2.  **KV缓存重复加载与转置**：现有CIM架构通常将KV作为权重，导致在不同Query（Q）块下KV需重复加载，且K矩阵转置需要额外的缓冲和周期。
    3.  **Online-Softmax计算冗余**：为了融合算子，Online-Softmax需要频繁的指数缩放（Rescaling），增加了非线性单元的负担。
*   **整体含义**：本文旨在通过一种“算子融合驱动”的混合CIM架构，从硬件底层优化注意力机制的数据流和计算逻辑，提升LLM推理的能效和速度。

### 2. 方法论
FusionCIM 提出了三大关键技术创新：
*   **混合CIM流水线架构（Hybrid CIM Pipeline）**：
    *   **IP-CIM（内积型）**：用于计算 $QK^T$（得分矩阵），将Q矩阵固定在SRAM中。
    *   **OP-CIM（外积型）**：用于计算 $PV$（上下文聚合），将输出O矩阵固定在SRAM中。
    *   这种设计实现了GEMM、转置和非线性算子的深度融合，减少了中间激活值写回片外DRAM的需求。
*   **QO-Stationary 数据流**：
    *   **核心思想**：让Query (Q) 和 Output (O) 在CIM宏内保持静止（Stationary），而让Key (K) 和 Value (V) 以流式输入。
    *   **优势**：消除了KV在CIM中的重复加载，并支持原位（In-situ）K矩阵转置，无需额外缓冲区。
*   **模式感知在线Softmax（Pattern-Aware Online-Softmax）**：
    *   **算法逻辑**：利用注意力得分分布的规律（最大值通常出现在对角线附近），调度计算从对角线位置开始。
    *   **效果**：更早识别行最大值（Row Maxima），从而减少后续计算中不必要的指数缩放操作。

### 3. 实验设计
*   **模型与数据集**：主要使用 **LLaMA-3-8B** 模型作为工作负载，涵盖了从 128 到 4096 的不同序列长度。
*   **Benchmark（基准）**：
    *   **Baseline 1**：传统的数字CIM架构（带转置缓冲区）。
    *   **Baseline 2**：类似 TranCIM 的架构（采用 KV-Stationary 数据流）。
*   **对比指标**：延迟（Latency）、能耗（Energy）、片外/片内内存访问次数、系统级能效（TOPS/W）和面积效率（TOPS/mm²）。

### 4. 资源与算力
*   **硬件实现/仿真**：论文基于 **TSMC 28nm** 工艺进行评估。
*   **算力参数**：
    *   系统包含 16 个混合引擎（HE）和 1MB 全局缓冲区。
    *   运行频率为 **400 MHz**。
    *   精度支持 INT8（CIM宏）和 FP16（Softmax单元）。
*   **训练/仿真工具**：使用了 Cacti 7.0（SRAM建模）、Synopsys Design Compiler（逻辑综合）和 PrimeTime PX（功耗分析）。文中未提及使用特定 GPU 进行训练，因为该研究侧重于推理加速器的硬件架构设计。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了不同序列长度（128-4096）下的性能和能效。
    *   针对内存访问（片内/片外）进行了详细的消融实验。
    *   在 LLaMA-3（RoPE编码）和 BERT（ALiBi编码）等不同模型、不同 Prompt 和 Attention Head 上验证了 Pattern-Aware 调度的有效性。
*   **充分性评价**：实验设计较为全面，涵盖了从宏观系统性能到微观算子开销的多个维度，对比对象包含了近年来的 SOTA（如 TranCIM, P3ViT），具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：相比 Baseline 1，FusionCIM 实现了高达 **1.98倍** 的加速。
*   **能效显著**：相比现有 SOTA CIM 设计，能耗降低了 **3.86倍**。
*   **系统指标**：达到了 **29.4 TOPS/W** 的系统级能效和 **2.03 TOPS/mm²** 的面积效率。
*   **访存优化**：QO-Stationary 数据流减少了 64.3%–71.0% 的片内内存访问；Pattern-Aware 调度在 LLaMA-3 上减少了约 60% 的输出缩放操作。

### 7. 优点
*   **架构创新**：巧妙结合了内积和外积两种CIM模式，解决了注意力机制中不同阶段的数据流冲突。
*   **软硬协同**：利用注意力机制的数学特性（对角线分布）来优化硬件调度，这种软硬结合的设计非常高效。
*   **系统级融合**：不仅关注矩阵乘法，还解决了转置和非线性算子的融合问题，更符合实际 LLM 推理需求。

### 8. 不足与局限
*   **仿真局限**：所有数据均基于 28nm 工艺的仿真和综合结果，缺乏实际流片（Tape-out）的测试数据。
*   **模型依赖性**：Pattern-Aware 调度依赖于注意力得分的分布规律，虽然在主流 LLM 中有效，但对于某些注意力分布极其分散的特殊模型，增益可能会减弱。
*   **硬件复杂度**：混合 IP/OP 架构增加了控制逻辑的复杂性，对片上网络（NoC）的带宽协调要求较高。

（完）
