---
title: "Valve: Production Online-Offline Inference Colocation with Jointly-Bounded Preemption Latency and Rate"
title_zh: Valve：具有联合约束抢占延迟与抢占率的生产级在线-离线推理混部系统
authors: "Fangyue Liu, Hua Liu, Xinyuan Lyu, Shuo Ai, Hao Liang, Lingpeng Chen, Ziqian Hu, Chong Zha, Xin Jin, Hanmei Luo, Peng Chen"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07874v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过在线-离线推理混部优化吞吐量和延迟
tldr: "Valve是一个针对大模型推理的在线-离线任务混部系统。针对混部中抢占延迟高、频率高以及系统修改复杂的挑战，Valve通过GPU运行时实现了亚毫秒级计算抢占和速率受限的内存回收。它在保证在线任务极低干扰（TTFT <5%）的前提下，将8,054张GPU的集群利用率提升了34.6%，节省了2,170张GPU，且部署成本极低。"
selection_source: fresh_fetch
motivation: 旨在解决LLM推理流量波动导致的资源低效利用，以及现有混部方案中抢占干扰大、系统侵入性强的问题。
method: 提出一种结合通道控制计算隔离、无缺页内存回收和动态内存预留的GPU运行时，实现对抢占延迟和频率的双重约束。
result: "在生产环境大规模部署后，提升集群利用率34.6%，同时在线任务的首字延迟增加小于5%，Token间延迟增加小于2%。"
conclusion: Valve证明了通过轻量级的系统级优化，可以在保证生产级在线服务质量的同时，显著提升大规模GPU集群的资源效率。
---

## 摘要
大语言模型（LLM）推理目前支撑着对延迟敏感的生产级服务。推理流量的突发性导致了资源的过度配置，进而造成资源利用率低下。虽然在线-离线混部有望利用空闲容量，但广泛的生产部署必须克服两个主要挑战：（i）由于抢占缓慢或频繁导致的严重在线干扰，以及（ii）为了混部不同模型并支持抢占而需要对框架和驱动程序进行的大量修改。我们提出了 Valve，这是一个对生产环境友好的混部系统，能够联合约束抢占延迟和抢占率。具体而言，Valve 实现了每个在线请求最多一次的亚毫秒级计算抢占，以及速率受限的子层级内存回收。这些保证由一个 GPU 运行时提供，该运行时结合了通道控制的计算隔离、无缺页异常的内存回收以及动态内存预留。至关重要的是，Valve 具有很高的部署实用性，仅需一行驱动程序修改和 20 行框架补丁。在生产环境中的 8,054 个 GPU 上部署后，Valve 将集群利用率提高了 34.6%，相当于节省了 2,170 个 GPU。这种效率提升是在极小的在线干扰下实现的，在各种工作负载中，首字延迟（TTFT）增加小于 5%，逐字延迟（TPOT）增加小于 2%。

## Abstract
LLM inference powers latency-critical production services nowadays. The bursty nature of inference traffic results in over-provisioning, which in turn leads to resource underutilization. While online-offline colocation promises to utilize idle capacity, broad production deployment must overcome two major challenges: (i) large online interference due to slow or frequent preemptions, and (ii) extensive frameworks and drivers modifications, to colocate different models and support preemptions. We present Valve, a production-friendly colocation system that jointly bounds preemption latency and preemption rate. Specifically, Valve enables sub-millisecond compute preemption at most once per online request, and rate-limited sub-layer memory reclamation. These guaranties are provided by a GPU runtime that combines channel-controlled compute isolation, page-fault-free memory reclamation, and dynamic memory reservation. Critically, Valve is practical to deploy, requiring one line of driver modification and 20 lines of framework patch. Deployed on 8,054 GPUs in production, Valve improves cluster utilization by 34.6%, which translates to a 2,170 GPU save. This efficiency gains is achieved with minimal online interference, incurring <5% TTFT increase and <2% TPOT increase across workloads.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Valve** 的生产级在线-离线推理混部系统，旨在解决大语言模型（LLM）推理中资源利用率低与在线服务质量（SLA）保障之间的矛盾。

### 1. 核心问题与研究动机
*   **核心问题**：LLM 推理流量具有高度突发性，为了满足严苛的延迟 SLA，运营商通常会过度配置资源，导致 GPU 平均利用率低下。
*   **研究动机**：
    *   **现有方案的局限**：现有的混部技术（如内核级抢占或层级抢占）要么抢占延迟过高（达数十毫秒），要么抢占过于频繁（干扰在线任务的 Decode 间隙），且往往需要对 GPU 驱动或推理框架进行数千行的深度修改，难以在生产环境大规模部署。
    *   **目标**：设计一个既能保证亚毫秒级抢占延迟，又能限制抢占频率，且对现有系统侵入性极小的混部方案。

### 2. 核心方法论
Valve 通过联合约束**抢占延迟**和**抢占速率**来实现低干扰混部，核心技术包括：
*   **通道控制的计算隔离（Channel-Controlled Compute Isolation）**：
    *   利用 GPU 硬件通道（Channel）控制 ioctl 接口，通过禁用/启用通道实现离线任务的暂停与恢复。
    *   **亚毫秒延迟**：在 Pascal 架构及以上的 GPU 上，硬件支持上下文保存，抢占延迟小于 1ms。
    *   **在线感知调度**：通过监控在线请求的生命周期，设置冷却时间（$T_{cool}$），确保在在线任务的 Decode 迭代间隙不频繁唤醒离线任务，实现“每个在线请求最多只抢占一次”。
*   **子层级内存回收与动态预留（Sub-layer Memory Reclamation）**：
    *   **安全回收**：在抢占计算后再回收内存，将回收的页面映射到“隔离页（Quarantine Page）”，避免触发非法内存访问导致的崩溃。
    *   **MIAD 动态预留**：采用“乘性增加、加性减少（MIAD）”策略动态调整在线任务的 KV Cache 预留空间，从而将内存回收频率控制在目标范围内。
    *   **框架协同**：仅需 20 行代码修改，让框架感知被回收的块 ID 并触发重计算，而非直接终止任务。
*   **集群级感知调度**：
    *   建立性能模型，考虑空闲计算比例、内存波动以及多卡同步行为（避免多卡异步导致的性能瓶颈），将离线任务调度到最合适的节点。

### 3. 实验设计
*   **实验场景**：从生产环境中采样了 10 组典型的在线-离线工作负载对（包括对话助手、代码生成、多模态任务等）。
*   **对比基准（Baselines）**：
    *   **计算抢占对比**：KernelPreempt（内核级抢占，如 TGS）、GPreempt（基于时间片的抢占）。
    *   **内存管理对比**：UVM（统一虚拟内存）、Prism（VMM 共享内存）、StaticMem（静态内存分配）。
*   **评估指标**：首字延迟（TTFT）增加比例、Token 间延迟（TPOT）增加比例、离线任务归一化吞吐量。

### 4. 资源与算力
*   **部署规模**：Valve 在腾讯生产环境的 **8,054 张 GPU** 上进行了大规模部署。
*   **硬件型号**：文中提到了支持 Pascal、Turing 及更新架构的 NVIDIA GPU（如 A100/H100 等生产常用型号）。
*   **运行时间**：系统已在生产环境中稳定运行超过 3 个月。

### 5. 实验数量与充分性
*   **实验规模**：除了 8,000+ GPU 的长期生产数据外，还针对 10 组特定负载进行了重放实验。
*   **充分性**：实验覆盖了计算抢占、内存回收、驱逐策略等多个维度的消融实验。
*   **客观性**：对比了学术界和工业界最新的多种基准方案，并使用了生产环境的真实流量轨迹（Trace），具有很高的说服力和实用参考价值。

### 6. 主要结论与发现
*   **利用率提升**：Valve 将集群平均 GPU 利用率提升了 **34.6%**。
*   **资源节省**：通过混部释放的算力相当于节省了 **2,170 张 GPU**。
*   **极低干扰**：在线任务的 TTFT 增加小于 **5%**，TPOT 增加小于 **2%**，远优于其他对比方案。
*   **离线性能**：通过选择性句柄回收算法，离线任务的吞吐量损失比 FIFO 策略降低了 22.9%–40.1%。

### 7. 优点
*   **生产友好（Production-friendly）**：仅需 1 行驱动修改和 20 行框架补丁，部署成本极低，兼容性强。
*   **精细化控制**：首次提出了联合约束抢占延迟和速率的思想，解决了 Decode 间隙频繁切换的痛点。
*   **硬件级优化**：深入 GPU 驱动底层，利用硬件通道特性实现了比亚毫秒级的快速响应。

### 8. 不足与局限
*   **硬件依赖**：该方案依赖于 NVIDIA Pascal 架构及以上的硬件特性（如硬件上下文保存），在旧型号或非 NVIDIA 硬件上可能无法直接应用。
*   **重计算开销**：虽然子层级内存回收避免了任务崩溃，但被回收的离线任务仍需进行部分重计算，这在内存极度紧张的场景下可能会影响离线任务的最终效率。
*   **多卡同步假设**：调度器假设离线任务是同步运行的，对于某些特殊的异步分布式推理架构，其性能模型可能需要进一步修正。

（完）
