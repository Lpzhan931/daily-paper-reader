---
title: "HAWK: Head Importance-Aware Visual Token Pruning in Multimodal Models"
title_zh: HAWK：多模态模型中感知注意力头重要性的视觉 Token 剪枝
authors: "Qihui Zhu, Tao Zhang, Yuchen Wang, Zijian Wen, Mengjie Zhang, Shuangwu Chen, Xiaobin Tan, Jian Yang, Yang Liu, Zhenhua Dong, Xianzhi Yu, Yinfei Pan"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07812v1"
tags: ["query:llm"]
score: 9.0
evidence: 多模态模型中的视觉Token剪枝以减少推理开销
tldr: 针对多模态大模型推理中视觉标记过多导致的计算开销问题，本文提出 HAWK，一种无须训练的视觉标记剪枝方法。HAWK 首次揭示了不同注意力头在视觉处理中的重要性差异，通过结合头权重和文本引导的注意力机制，精准识别并保留关键视觉标记。实验证明，该方法在大幅削减视觉标记的同时，能保持极高的模型精度，并显著降低推理延迟和显存占用。
selection_source: fresh_fetch
motivation: 现有剪枝方法忽略了不同注意力头在视觉语义捕捉中的角色差异，通常假设所有头对视觉解释的贡献是均等的。
method: 提出一种感知头重要性的剪枝策略，利用头权重和文本引导的注意力来评估并保留任务相关的关键视觉标记。
result: "在 Qwen2.5-VL 上剪除 80.2% 的视觉标记后仍能维持 96.0% 的精度，并显著降低了端到端延迟和显存消耗。"
conclusion: HAWK 证明了考虑注意力头重要性对于高效视觉标记剪枝的关键作用，为提升多模态大模型的推理效率提供了通用且有效的方案。
---

## 摘要
在多模态大语言模型（MLLMs）中，视觉 Token 的激增显著增加了推理时间和计算开销，使其在实时或资源受限的应用场景中变得不切实际。视觉 Token 剪枝是通过移除冗余视觉 Token 来降低 MLLM 推理成本的一种极具前景的策略。现有研究通常假设所有注意力头对视觉解释的贡献是均等的。然而，我们的研究表明，不同的注意力头可能会捕捉到不同的视觉语义，并在视觉处理中固有地发挥着截然不同的作用。基于这一观察，我们提出了 HAWK，一种感知注意力头重要性的视觉 Token 剪枝方法，该方法通过感知注意力头在视觉任务中的不同重要性，以最大限度地保留关键 Token。通过利用注意力头重要性权重和文本引导的注意力来评估视觉 Token 的显著性，HAWK 有效地保留了与任务相关的视觉 Token，同时移除了冗余 Token。所提出的 HAWK 完全无需训练，并可无缝应用于各种 MLLM。在多个主流视觉语言基准测试上的广泛实验表明，HAWK 达到了最先进的准确率。当应用于 Qwen2.5-VL 时，HAWK 在剪枝了 80.2% 的视觉 Token 后，仍保留了原始准确率的 96.0%。此外，它将端到端延迟降低至原始的 74.4%，并进一步减少了所测试模型中的 GPU 显存占用。代码可在 https://github.com/peppery77/HAWK.git 获取。

## Abstract
In multimodal large language models (MLLMs), the surge of visual tokens significantly increases the inference time and computational overhead, making them impractical for real-time or resource-constrained applications. Visual token pruning is a promising strategy for reducing the cost of MLLM inference by removing redundant visual tokens. Existing research usually assumes that all attention heads contribute equally to the visual interpretation. However, our study reveals that different heads may capture distinct visual semantics and inherently play distinct roles in visual processing. In light of this observation, we propose HAWK, a head importance-aware visual token pruning method that perceives the varying importance of attention heads in visual tasks to maximize the retention of crucial tokens. By leveraging head importance weights and text-guided attention to assess visual token significance, HAWK effectively retains task-relevant visual tokens while removing redundant ones. The proposed HAWK is entirely training-free and can be seamlessly applied to various MLLMs. Extensive experiments on multiple mainstream vision-language benchmarks demonstrate that HAWK achieves state-of-the-art accuracy. When applied to Qwen2.5-VL, HAWK retains 96.0% of the original accuracy after pruning 80.2% of the visual tokens. Additionally, it reduces end-to-end latency to 74.4% of the original and further decreases GPU memory usage across the tested models. The code is available at https://github.com/peppery77/HAWK.git.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HAWK** 的新型视觉 Token 剪枝方法，旨在提升多模态大语言模型（MLLM）的推理效率。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大模型（如 Qwen2.5-VL）在处理高分辨率图像或长视频时，会产生海量的视觉 Token（动辄数千甚至数万），导致推理时的计算开销（FLOPs）、显存占用（KV Cache）和延迟（Latency）急剧增加。
*   **研究动机**：
    *   现有的剪枝方法（如基于相似度或简单注意力平均的方法）通常假设所有注意力头（Attention Heads）对视觉信息的贡献是均等的。
    *   作者通过实验发现，**不同的注意力头在视觉处理中扮演的角色截然不同**：某些头对视觉语义至关重要，而某些头则几乎不参与视觉理解。
    *   因此，忽略注意力头的重要性差异会导致误删关键信息或保留冗余信息。

### 2. 方法论（HAWK）
HAWK 是一种**感知头重要性**且**无需训练**的剪枝框架，其核心流程分为两步：
*   **静态头重要性权重计算（Offline）**：
    *   通过离线消融实验，系统性地禁用单个注意力头对视觉 Token 的访问，观察模型在基准测试上的性能下降程度。
    *   性能下降越明显的头，其权重 $w_i$ 越高。这一步是单次离线完成的，具有通用性。
*   **动态文本引导的注意力评分（Online）**：
    *   在推理时，利用 LLM 第一层的 Query（来自文本指令）和 Key（来自视觉 Token）计算相关性。
    *   **关键细节**：在计算相关性时去除了旋转位置编码（RoPE），以消除位置偏差，纯粹基于语义匹配。
*   **重要性融合与剪枝**：
    *   将每个头的动态注意力分数与该头的静态重要性权重进行加权求和，得到每个视觉 Token 的最终得分 $I_k$。
    *   根据预设的剪枝比例，保留得分最高的 Top-k 个 Token，其余丢弃。

### 3. 实验设计
*   **模型对象**：最先进的开源模型 Qwen2.5-VL-7B 和 InternVL3-8B。
*   **数据集/基准测试**：
    *   **图像任务（10个）**：包括通用推理（MME, HallBench, POPE, ScienceQA）、文本/图表识别（TextVQA, OCRVQA, ChartQA, AI2D）等。
    *   **视频任务（2个）**：Video-MME 和 WorldSense。
*   **对比方法**：
    *   基于注意力的 FastV。
    *   基于相似度的 DivPrune。
    *   混合方法 CDPruner。
    *   其他最新方法如 VisionZip 和 DART。

### 4. 资源与算力
*   **硬件环境**：实验在 8 张 96GB 显存的 GPU 上进行（使用 VLMEvalkit 框架）。
*   **计算开销**：
    *   **离线阶段**：计算头重要性权重在 8 张 GPU 上仅需 0.39 到 0.83 小时（取决于数据集），相比需要数百小时的微调方法，成本极低。
    *   **推理阶段**：在 Qwen2.5-VL 上，剪枝 80.2% 的 Token 可实现 **1.34 倍的端到端加速**，并显著降低 KV Cache 显存占用。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从图像到视频、从固定分辨率到原生分辨率（Native Resolution）的广泛场景。
*   **消融实验**：作者针对“头重要性权重”、“文本引导策略”以及“RoPE 的影响”分别做了消融，证明了每个组件的必要性。
*   **客观性**：实验在多个不同架构的模型（Qwen 和 InternVL）上均表现出一致的 SOTA 性能，证明了方法的普适性和公平性。

### 6. 主要结论与发现
*   **性能卓越**：在 Qwen2.5-VL 上，剪掉 **80.2%** 的视觉 Token 后，模型仍能保持 **96.0%** 的原始精度，远超同类方法。
*   **头功能特化**：实验证实某些注意力头是“视觉敏感型”，而另一些是“冗余型”，且这种特性在不同数据集间具有高度一致性。
*   **效率提升**：HAWK 不仅提高了准确率，还通过大幅减少 Token 数量，有效缓解了长文本/多图场景下的显存压力。

### 7. 优点
*   **训练零成本（Training-free）**：不需要对模型进行任何参数微调，即插即用。
*   **感知语义**：通过文本引导，确保剪枝过程是“任务相关”的（例如：问颜色时保留颜色相关的区域）。
*   **通用性强**：适用于不同分辨率、不同模态（图/影）以及不同的 MLLM 架构。

### 8. 不足与局限
*   **离线依赖**：虽然开销小，但仍需一次性的离线校准来获取头权重。如果模型架构发生剧烈变化，权重可能需要重新计算。
*   **极端剪枝下的损失**：在剪枝率超过 90% 的极端情况下，虽然优于基准，但性能仍有较明显下降（约 10%），对于极高精度要求的场景仍需权衡。
*   **早期层依赖**：目前主要依赖第一层注意力进行评分，尚未深入探讨深层注意力信息对剪枝的潜在贡献。

（完）
