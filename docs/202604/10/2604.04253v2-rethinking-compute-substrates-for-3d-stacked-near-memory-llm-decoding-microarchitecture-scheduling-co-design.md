---
title: "Rethinking Compute Substrates for 3D-Stacked Near-Memory LLM Decoding: Microarchitecture-Scheduling Co-Design"
title_zh: 重新思考 3D 堆叠近内存大语言模型解码的计算基底：微架构与调度协同设计
authors: "Chenyang Ai, Yixing Zhang, Haoran Wu, Yudong Pan, Lechuan Zhao, Wenhui OU"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04253v2"
tags: ["query:llm"]
score: 9.0
evidence: LLM解码加速的微架构协同设计
tldr: 针对大语言模型（LLM）推理中解码阶段的带宽瓶颈，本文重新审视了3D堆叠近存计算（NMP）的计算架构。由于NMP极高的带宽使部分算子转为计算受限，且逻辑层面积受限，研究者提出了一种可重构脉动阵列与向量核心融合的微架构，并配合多核调度框架。该设计在保持低面积开销的同时，显著提升了处理多样化算子的效率，在Dense和MoE模型上均表现出优异的性能和能效。
selection_source: fresh_fetch
motivation: 3D堆叠近存计算提供的高带宽使LLM解码算子从带宽受限转为计算受限，但在有限的逻辑层面积下，现有计算单元难以兼顾高效能与灵活性。
method: 提出一种将面积高效的脉动阵列与向量核心统一的微架构，支持形状和数据流的可重构性，并结合多核调度框架优化执行。
result: 实验表明，该设计相比现有先进方案Stratum，在Dense和MoE模型上平均实现了2.91倍的加速和2.40倍的能效提升。
conclusion: 通过微架构与调度的协同设计，3D堆叠NMP能够有效解决LLM解码中的计算瓶颈，是未来高性能推理架构的重要方向。
---

## 摘要
大语言模型 (LLM) 解码是主要的推理瓶颈，因为其较低的算术强度使得性能对内存带宽高度敏感。3D 堆叠近内存处理 (NMP) 提供了比传统片外接口高得多的本地内存带宽，使其成为加速解码的极具前景的基底。然而，我们的分析表明，这种带宽优势也使得 3D 堆叠 NMP 上的许多解码算子重新回到了计算受限的状态。在逻辑层有限的面积预算下，计算基底本身的设计因此成为了首要挑战。因此，我们重新思考了先前 3D 堆叠 NMP 设计的计算微架构。首先，我们将先前基于乘累加 (MAC) 树的计算单元替换为面积效率更高的脉动阵列，并进一步观察到解码算子表现出显著的形状多样性，这使得脉动阵列形状和数据流的可重构性对于维持高利用率至关重要。基于这一见解，我们继续利用两个关键机会：高本地内存带宽减少了对大型片上缓冲区的需求，而原本设计用于处理辅助张量计算的现有向量核心已经提供了脉动阵列细粒度灵活性所需的大部分控制逻辑和多端口缓冲，使我们能够以极高的面积效率统一这两种结构。基于这些见解，我们提出了首个专为 3D 堆叠 NMP LLM 解码定制的计算微架构，旨在明确满足低面积成本、高带宽运行和细粒度可重构性的共同要求。我们还提出了一个多核调度框架。与 Stratum 相比，我们的设计在稠密模型和 MoE 模型上平均实现了 2.91 倍的加速和 2.40 倍的能效提升。

## Abstract
Large language model (LLM) decoding is a major inference bottleneck because its low arithmetic intensity makes performance highly sensitive to memory bandwidth. 3D-stacked near-memory processing (NMP) provides substantially higher local memory bandwidth than conventional off-chip interfaces, making it a promising substrate for decode acceleration. However, our analysis shows that this bandwidth advantage also shifts many decode operators on 3D-stacked NMP back into the compute-bound regime. Under the tight area budget of the logic die, the design of the compute substrate itself therefore becomes a first-order challenge. Therefore, we rethink the compute microarchitecture of prior 3D-stacked NMP designs. First, we replace prior MAC tree-based compute units with a more area-efficient systolic array, and we further observe that decode operators exhibit substantial shape diversity, making reconfigurability in both systolic array shape and dataflow essential for sustaining high utilization. Building on this insight, we continue to exploit two key opportunities: the high local memory bandwidth reduces the need for large on-chip buffers, and the existing vector core, originally designed to handle auxiliary tensor computations, already provides much of the control logic and multi-ported buffering required for fine-grained flexibility for systolic array, allowing us to unify the two structures in a highly area-efficient manner. Based on these insights, we present the first compute microarchitecture tailored to 3D-stacked NMP LLM decoding, explicitly designed to satisfy the joint requirements of low area cost, high-bandwidth operation, and fine-grained reconfigurability. We further propose an multi-core scheduling framework. Compared with Stratum, our design achieves an average 2.91x speedup and 2.40x higher energy efficiency across both dense and MoE models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking Compute Substrates for 3D-Stacked Near-Memory LLM Decoding: Microarchitecture–Scheduling Co-Design》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）的解码阶段（Decoding）受限于内存带宽。虽然 3D 堆叠近内存处理（NMP）技术提供了极高的本地带宽，但这种带宽优势使得解码算子从“带宽受限”转变为“计算受限”。
*   **研究动机**：
    *   **面积瓶颈**：3D 堆叠芯片的逻辑层（Logic Die）面积极其有限，传统的 MAC 树（MAC-Tree）架构面积效率低，无法在有限空间内提供足够的计算力。
    *   **算子多样性**：解码算子的形状（Shape）随 Batch Size 和模型结构动态变化，固定形状的计算单元利用率低下。
    *   **架构错配**：现有的 NMP 设计未能充分利用高带宽带来的“缓冲区缩减”机会。

### 2. 方法论：SNAKE 架构与调度协同设计
*   **核心思想**：提出一种名为 **SNAKE** 的可重构脉动阵列（Systolic Array, SA）微架构，通过面积高效的硬件设计和灵活的软件调度，最大化 NMP 的计算密度。
*   **关键技术细节**：
    *   **面积重分配**：利用 NMP 的高带宽特性，缩减了传统设计中巨大的 SRAM 缓冲区，将节省的面积用于部署更多的处理元件（PE）。
    *   **统一计算基底**：将向量核心（Vector Core）与脉动阵列融合，共享多端口缓冲和控制逻辑，减少冗余。
    *   **SNAKE-like 映射**：支持“蛇形”逻辑重构，使物理上的 64x64 阵列能灵活变阵为 8x512 或 16x256 等形状，以匹配解码算子的长瘦矩阵特征。
    *   **可重构数据流**：支持输出驻留（OS）和输入驻留（IS）数据流的动态切换。
*   **调度框架**：
    *   提出算子感知的多核调度，支持**空间划分**（Spatial）和**时空划分**（Spatio-temporal）四种模式（IS-S, IS-ST, OS-S, OS-ST），根据算子维度（N 和 K 的比例）自动选择最优策略。

### 3. 实验设计
*   **数据集与模型**：涵盖了主流的 Dense 和 MoE 模型，包括 OPT 66B、LLaMA3 70B、Mixtral 8x22B、Qwen3 30B-A3B 和 DeepSeek 236B。
*   **Benchmark（基准方法）**：
    *   **Stratum**：最先进的基于 MAC 树的 3D NMP 架构。
    *   **GPU**：NVIDIA H100（作为 Prefill 引擎及对比基准）。
    *   **固定形状 SA**：48x48（正方形）和 8x288（长条形）脉动阵列。
*   **场景**：评估了单算子性能、端到端服务延迟（E2E Latency）和 Token 间延迟（TBT）。

### 4. 资源与算力
*   **算力建模**：使用 SystemVerilog 进行 RTL 实现，基于 **7nm ASAP7** 预测性工艺库进行综合。
*   **模拟器**：使用扩展的 Scale-Sim v3 和 Ramulator 进行周期精确级模拟。
*   **硬件配置**：逻辑层功耗预算限制在 62W（基于 HotSpot 热建模），HBM3 内部带宽设定为 24 TB/s。
*   **训练时长**：文中未提及模型训练，主要关注推理阶段的架构模拟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了面积和功耗的细粒度分解实验。
    *   针对 5 种主流模型在不同 Batch Size（8-64）下的性能测试。
    *   多核调度策略的选择分布及消融实验。
    *   热力学模拟（Thermal Characterization）以验证 3D 堆叠的可行性。
*   **充分性与公平性**：实验采用了**面积归一化**对比，即在相同的逻辑层面积预算下对比不同架构，并考虑了可重构性带来的频率降级（从 1GHz 降至 800MHz），评估较为客观公平。

### 6. 主要结论与发现
*   **面积效率**：脉动阵列的面积效率比 MAC 树高出 **8.23 倍**；SNAKE 整体计算密度是 Stratum 的 **4 倍**。
*   **性能提升**：相比 Stratum，SNAKE 平均实现 **2.91 倍**的加速，能效提升 **2.40 倍**。
*   **调度价值**：对于 MoE 模型，灵活的调度策略相比固定策略可避免高达 6.43 倍的性能损失。
*   **带宽红利**：在 NMP 环境下，增加计算单元比增加缓冲区容量更能有效降低延迟。

### 7. 优点
*   **协同设计范式**：不仅改进了微架构，还解决了多核环境下的算子映射调度问题。
*   **洞察敏锐**：准确识别了 NMP 从带宽受限向计算受限转化的趋势，并提出了“带宽换面积”的优化思路。
*   **高度适配 LLM**：专门针对解码阶段的非对称矩阵运算进行了硬件级优化。

### 8. 不足与局限
*   **频率牺牲**：为了实现可重构性，引入了额外的多路复用器和布线开销，导致工作频率略低于固定架构。
*   **极小算子支持**：对于 Batch Size 极小（如 M=1）的 GEMV 算子，虽然有改进，但仍存在一定的 PE 利用率闲置。
*   **物理实现挑战**：虽然进行了热建模，但 3D 堆叠逻辑层的散热和成品率在实际大规模量产中仍面临巨大挑战。

（完）
