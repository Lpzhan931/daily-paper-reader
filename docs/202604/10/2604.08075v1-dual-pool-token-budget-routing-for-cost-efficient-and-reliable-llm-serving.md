---
title: Dual-Pool Token-Budget Routing for Cost-Efficient and Reliable LLM Serving
title_zh: 面向高性价比且可靠 LLM 推理服务的双池 Token 预算路由
authors: "Xunzhuo Liu, Bowei He, Xue Liu, Andy Luo, Haichen Zhang, Huamin Chen"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08075v1"
tags: ["query:llm"]
score: 10.0
evidence: 优化vLLM集群的吞吐量和KV缓存分配
tldr: 针对LLM推理中长短文本混合导致的KV缓存分配不均和资源浪费问题，本文提出了一种双池令牌预算路由机制。该方法将同构集群划分为高吞吐短上下文池和高容量长上下文池，通过在线学习的字节-令牌比例估算请求预算并进行路由。实验表明，该方案在显著降低GPU成本的同时，提升了系统可靠性和响应速度，且无需分词器，具有极低的调度开销。
selection_source: fresh_fetch
motivation: 生产环境常按最长上下文配置实例，导致占绝大多数的短请求造成严重的KV缓存浪费、吞吐量下降及OOM风险。
method: 提出双池路由机制，利用在线学习的字节-令牌比估算请求长度，将流量分流至针对性优化的短上下文或长上下文实例池。
result: "在Llama-3-70B等模型上减少了31-42%的GPU工时，降低抢占率5.4倍，并显著提升了P99 TTFT性能。"
conclusion: 该方法通过解决配置与流量的不匹配问题，实现了低开销、高可靠且成本效益极高的大规模LLM服务部署。
---

## 摘要
生产环境中的 vLLM 集群通常根据最坏情况下的上下文长度为每个实例分配资源，导致严重的 KV 缓存（KV-cache）过度分配和并发利用不足。在实践中，80-95% 的请求属于短请求，却在针对长上下文优化的配置下运行，这浪费了 4-8 倍的吞吐能力，并引发了 OOM 崩溃、抢占和请求拒绝等可靠性问题。我们发现这些低效现象的根本原因在于配置与流量的不匹配。为此，我们提出了双池 Token 预算路由（dual-pool token-budget routing），这是一种轻量级的调度机制，将同构集群划分为两个专门的池：高吞吐短上下文池和高容量长上下文池。每个请求根据其估计的总 Token 预算进行路由，该预算通过按类别划分的字节-Token 比例计算得出，并利用 usage.prompt_tokens 反馈通过指数移动平均进行在线学习，从而无需分词器。我们还开发了一个简单的分析模型，可根据工作负载特征和实测吞吐量差异预测集群层面的成本节约，使从业者能够在部署前估算收益。在 Azure LLM 推理数据集和 LMSYS-Chat-1M 真实轨迹上的评估（在 A100 GPU 上运行 Llama-3-70B）表明，该方法可减少 31-42% 的 GPU 小时数，相当于集群规模下每年节省 286 万美元，同时将抢占率降低了 5.4 倍，并将 P99 TTFT 优化了 6%。在 AMD MI300X 上以 10,000 req/s 运行 Qwen3-235B-A22B 的案例研究预计每年可节省 1540 万美元。该方法仅产生 O(1) 的调度开销，能自动适应异构工作负载，并可与 PagedAttention、连续批处理（continuous batching）以及预填充-解码分离（prefill-decode disaggregation）等现有优化无缝结合。

## Abstract
Production vLLM fleets typically provision each instance for the worst-case context length, leading to substantial KV-cache over-allocation and under-utilized concurrency. In practice, 80-95% of requests are short, yet are served under configurations optimized for long contexts, wasting 4-8$\times$ throughput capacity and triggering reliability issues such as OOM crashes, preemption, and request rejections. We identify a common root cause for these inefficiencies: configuration-traffic mismatch. We propose dual-pool token-budget routing, a lightweight dispatch mechanism that partitions a homogeneous fleet into two specialized pools: a high-throughput short-context pool and a high-capacity long-context pool. Each request is routed based on its estimated total token budget, computed using a per-category bytes-to-token ratio that is learned online via exponential moving average from usage.prompt_tokens feedback, eliminating the need for a tokenizer. We also develop a simple analytical model that predicts fleet-level cost savings from workload characteristics and measured throughput differences, enabling practitioners to estimate benefits prior to deployment. Evaluations on real-world traces from the Azure LLM Inference Dataset and LMSYS-Chat-1M, serving Llama-3-70B on A100 GPUs, show that our approach reduces GPU-hours by 31-42%, corresponding to \$2.86M annual savings at fleet scale, while lowering preemption rates by 5.4$\times$ and improving P99 TTFT by 6%. A case study with Qwen3-235B-A22B on AMD MI300X at 10,000 req/s projects \$15.4M in annual savings. The method incurs only O(1) dispatch overhead, adapts automatically to heterogeneous workloads, and composes seamlessly with existing optimizations such as PagedAttention, continuous batching, and prefill-decode disaggregation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dual-Pool Token-Budget Routing for Cost-Efficient and Reliable LLM Serving》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在生产环境中，vLLM 集群通常按照“最坏情况”（即模型支持的最大上下文长度，如 64K+）为所有实例配置资源。然而，实际流量中 80-95% 的请求是短文本（< 2K）。
*   **核心矛盾**：这种“同构配置”导致了严重的 **配置-流量不匹配**。为短请求预留长上下文的 KV 缓存造成了 4-8 倍的并发能力浪费，并在高负载下引发 OOM 崩溃、频繁的请求抢占（Preemption）和首字延迟（TTFT）过高。
*   **研究意义**：通过在集群层面优化资源分配，旨在不牺牲性能的前提下大幅降低 GPU 成本并提升系统可靠性。

### 2. 论文提出的方法论
核心思想是**双池 Token 预算路由（Dual-Pool Token-Budget Routing）**：
*   **集群分区**：将同构的 GPU 集群划分为两个专门的池：
    1.  **短上下文池 (Ps)**：配置较小的 `max_model_len`（如 8K），从而获得极高的并发数（Nseq）。
    2.  **长上下文池 (Pl)**：保持原始的长上下文配置，处理剩余请求。
*   **自校准 Token 估算**：
    *   **无需分词器**：为了降低路由开销，不使用模型分词器，而是通过请求的字节长度除以“字节-Token 比例”($\hat{c}_k$) 来估算。
    *   **在线学习**：利用 LLM 返回的 `usage.prompt_tokens` 字段，通过指数移动平均（EMA）实时更新不同类别（如代码、中文、英文）的 $\hat{c}_k$。
    *   **保守偏置**：使用标准差进行保守估计，防止将长请求误分到短池。
*   **路由逻辑**：计算请求的总预算 $L_{total} = L_{in} + L_{out}$。若 $L_{total} \le B_{short}$ 则路由至短池，否则进入长池。支持**负载感知溢出**，即短池满时可临时借用长池资源。
*   **成本模型**：提出了闭式公式 $\Delta G/G_{homo} = \alpha (1 - 1/\rho)$，其中 $\alpha$ 是短流量占比，$\rho$ 是短池相对于长池的吞吐增益比。

### 3. 实验设计
*   **数据集/场景**：
    *   **Azure LLM Inference Dataset**：具有高度偏态分布的真实生产轨迹。
    *   **LMSYS-Chat-1M**：真实的聊天对话数据集。
*   **Benchmark 与对比方法**：
    *   **基准 (Baseline)**：标准的同构 vLLM 集群配置（所有实例均配置为最大上下文）。
    *   **对比维度**：GPU 实例数量（成本）、抢占率、OOM 事件数、P99 TTFT（首字延迟）和 P99 TPOT（每 Token 延迟）。
*   **模型对象**：主要使用 Llama-3-70B，案例研究涉及 Qwen3-235B。

### 4. 资源与算力
*   **GPU 型号**：NVIDIA A100-80GB（张量并行 TP=2）和 AMD Instinct MI300X（TP=8）。
*   **实验环境**：使用了基于 Vidur 校准的离散事件模拟器，模拟了 100,000 个请求的泊松到达过程。
*   **算力规模**：实验涵盖了从 100 req/s 到 10,000 req/s 的大规模集群模拟。文中未详细说明模拟器运行的具体物理机耗时，但强调了路由算法本身的开销是 $O(1)$ 级别的微秒级延迟。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   针对两个不同特征的真实数据集进行了完整对比。
    *   进行了**阈值敏感性分析**（测试了 $B_{short}$ 从 1K 到 32K 的变化）。
    *   验证了**在线校准的收敛性**（不同语言/类别在 50 次请求内即可收敛）。
    *   包含了针对最新硬件（MI300X）和超大规模模型（235B）的案例研究。
*   **评价**：实验设计较为充分，考虑了生产环境中的多种异常情况（如溢出、误判），对比基准公平，且使用了工业界公认的数据集。

### 6. 论文的主要结论与发现
*   **成本大幅降低**：在 Azure 轨迹上减少了 **41.9%** 的 GPU 需求，在 LMSYS 上减少了 **31.3%**。
*   **可靠性显著提升**：抢占率降低了 **5.4 倍**，OOM 事件减少了 **5.3 倍**。
*   **延迟优化**：P99 TTFT 改善了 **6%**，P50 TTFT 改善了 **33%**，解决了长预填充导致的“队头阻塞”问题。
*   **经济效益**：在 10,000 req/s 的规模下，预计每年可节省 **1540 万美元**（MI300X 案例）。

### 7. 优点
*   **简单且非侵入性**：不需要修改 LLM 核心架构或分词器，作为 API 网关层的轻量级插件即可运行。
*   **解耦优化**：与现有的 PagedAttention、连续批处理、预填充-解码分离等技术完全兼容且互补。
*   **自适应性**：通过 EMA 自动适应不同语言和任务类型的 Token 密度变化。
*   **理论支撑**：提供的成本预测模型允许企业在实际部署前通过流量统计数据预估收益。

### 8. 不足与局限
*   **依赖输出长度预测**：路由决策依赖于用户提供的 `max_output_tokens`。如果用户习惯性设置极大的输出上限，会导致短请求被错误地路由到长池，降低优化效率。
*   **静态分区限制**：虽然支持溢出，但两个池的 GPU 数量比例仍需预先设定。如果流量分布发生剧烈且持久的偏移，可能需要重新调整分区比例。
*   **模拟器依赖**：核心性能数据主要来自模拟器（尽管经过了 Vidur 校准），在极端复杂的真实网络环境和多租户干扰下的表现仍需进一步验证。

（完）
