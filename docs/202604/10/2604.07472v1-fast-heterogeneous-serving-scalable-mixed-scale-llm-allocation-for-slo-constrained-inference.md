---
title: "Fast Heterogeneous Serving: Scalable Mixed-Scale LLM Allocation for SLO-Constrained Inference"
title_zh: 快速异构推理服务：受 SLO 约束的推理中可扩展的混合规模 LLM 分配
authors: "Jiaming Cheng, Duong Tung Nguyen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07472v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对SLO约束推理服务的可扩展大语言模型分配
tldr: 针对大规模异构LLM推理部署中模型选择、资源分配与SLO约束的复杂性，本文提出了一种快速且可扩展的分配框架。通过引入贪婪启发式（GH）和自适应贪婪启发式（AGH）算法，结合张量并行感知机制，在保证延迟、精度和预算约束的前提下，实现了毫秒级的决策速度。实验表明，AGH在保持接近最优成本的同时，比传统MILP求解器提速260倍以上，且在压力测试下表现出更强的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的混合整数线性规划方法在处理大规模异构LLM推理分配时，因计算复杂度过高而难以满足实时扩展和严格的SLO约束需求。
method: 提出了自适应贪婪启发式算法（AGH），通过多起点构建、局部搜索、GPU整合以及TP感知机制来优化资源分配。
result: AGH在1秒内即可生成可行解，在保持接近最优成本的同时，比精确求解器提速超过260倍，且在压力测试中表现更稳定。
conclusion: 该研究证明了启发式算法在大规模异构LLM服务分配中的高效性，为复杂约束下的推理任务提供了实用的扩展方案。
---

## 摘要
大规模部署大语言模型（LLM）推理需要联合选择基础模型、配置异构 GPU、设定并行策略，并在严格的延迟、准确性和预算约束下分配工作负载。精确的混合整数线性规划（MILP）方法虽然能保证最优性，但可扩展性较差。我们提出了两种约束感知的启发式算法：一种是用于单次分配的贪婪启发式算法（GH），另一种是通过多起点构建、基于重定位的局部搜索和 GPU 整合来增强 GH 的自适应贪婪启发式算法（AGH）。三种约束感知机制——张量并行（TP）感知的可行性选择、单位有效覆盖成本排名以及 TP 升级——确保了在内存、延迟、误差和预算等紧耦合约束下的可行性。在基于 Azure LLM 推理追踪（2025）校准的工作负载上，两种启发式算法均能在不到一秒的时间内生成可行解，其中 AGH 在大规模实例上实现了超过 260 倍的加速，且成本接近最优。在参数膨胀高达 1.5 倍的样本外压力测试下，AGH 保持了可控的 SLO 违规率和稳定的成本，而精确求解器的部署方案则急剧恶化。

## Abstract
Deploying large language model (LLM) inference at scale requires jointly selecting base models, provisioning heterogeneous GPUs, configuring parallelism, and distributing workloads under tight latency, accuracy, and budget constraints. Exact mixed-integer linear programming (MILP) approaches guarantee optimality but scale poorly. We propose two constraint-aware heuristics: a Greedy Heuristic (GH) for single-pass allocation, and an Adaptive Greedy Heuristic (AGH) that enhances GH via multi-start construction, relocate-based local search, and GPU consolidation. Three constraint-aware mechanisms -- TP-aware feasibility selection, cost-per-effective-coverage ranking, and TP upgrade -- ensure feasibility under tightly coupled memory, delay, error, and budget constraints. On workloads calibrated with the Azure LLM Inference Trace (2025), both heuristics produce feasible solutions in under one second, with AGH closely approaching optimal cost while achieving over 260x speedup on large-scale instances. Under out-of-sample stress tests with up to 1.5x parameter inflation, AGH maintains controlled SLO violations and stable cost, whereas the exact solver's placement degrades sharply.

---

## 论文详细总结（自动生成）

这篇论文针对大规模大语言模型（LLM）推理服务中的资源分配与调度问题提出了高效的启发式算法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
随着 LLM 应用的普及，服务提供商面临一个复杂的联合优化难题：如何在满足严格的服务等级目标（SLO，如延迟、精度）和预算限制的前提下，从异构的 GPU 资源池中选择合适的硬件，配置最优的并行策略（张量并行 TP 和流水线并行 PP），并路由多样化的查询负载。
**研究动机**：现有的精确求解方法（如 MILP）虽然能找到最优解，但在处理大规模集群时计算耗时呈指数级增长（需数分钟甚至数小时），无法应对动态变化的需求。而简单的启发式方法往往忽略了内存、延迟和并行度之间的紧耦合关系，容易导致不可行的分配方案。

### 2. 方法论
论文首先将问题建模为混合整数线性规划（MILP），随后提出了两种启发式算法：
*   **核心机制（三个约束感知机制）**：
    *   **M1 - TP感知配置选择**：在分配前预筛选满足内存容量和延迟阈值的最小成本 TP/PP 组合。
    *   **M2 - 单位有效覆盖成本排名**：不按绝对成本排序，而是按“单位有效服务需求所花费的成本”进行候选排序，综合考虑了误差和延迟预算。
    *   **M3 - 并行度升级**：对于已激活的 GPU，若延迟不达标，优先尝试提升其并行度而非新购资源。
*   **贪婪启发式（GH）**：采用两阶段分配，先进行覆盖预分配（确保每种查询都有可行配置），再按需求量降序进行顺序分配。
*   **自适应贪婪启发式（AGH）**：在 GH 基础上引入增强策略：
    *   **多起点构建**：尝试多种启发式排序（如按需求、按存储、按误差敏感度等）并取最优。
    *   **局部搜索（Relocate）**：通过移动已分配的负载来进一步降低成本。
    *   **整合（Consolidate）**：将轻负载 GPU 上的任务迁移并关闭冗余实例。

### 3. 实验设计
*   **数据集与场景**：使用了 **Azure LLM Inference Trace (2025)** 校准的工作负载数据。
*   **实验设置**：包含 6 种查询类型（摘要、代码、翻译等）、6 种 Llama-3 模型（1B 到 70B）以及 10 种异构 GPU 层级（如 H100, A100, RTX 4090 等，涵盖 FP16/INT8/INT4 精度）。
*   **Benchmark（对比方法）**：
    *   **DM (Deterministic MILP)**：使用 Gurobi 求解器获得的精确最优解。
    *   **GH (Greedy Heuristic)**：本文提出的基础贪婪算法。
    *   **AGH (Adaptive Greedy Heuristic)**：本文提出的增强型启发式算法。
*   **评估流程**：采用两阶段评估，第一阶段做决策，第二阶段在参数扰动（延迟/误差波动）的压力测试下评估实际表现。

### 4. 资源与算力
*   **软件环境**：Python 3.13, Gurobi 11 优化器。
*   **硬件模拟**：实验模拟了包括 NVIDIA H100-80GB、A100-40GB、A6000-24GB 和 RTX 4090-24GB 在内的多种 GPU。
*   **计算耗时**：论文重点在于算法本身的运行时间。在最大规模测试中，精确求解器耗时超过 600 秒，而 GH 仅需 0.9 秒，AGH 仅需 2.3 秒。
*   **训练时长**：由于本文解决的是推理分配优化问题而非模型训练，因此不涉及模型训练时长。

### 5. 实验数量与充分性
*   **实验规模**：进行了 500 组随机场景的压力测试，涵盖了需求波动（±20%）和性能扰动（±25%）。
*   **消融实验**：专门针对 M1、M2、M3 三个机制进行了消融分析，证明了 M1 和 M3 是确保方案“可行性”的前提。
*   **敏感性分析**：调整了延迟阈值、误差限制、预算和 GPU 租金等多个变量，观察算法的稳定性。
*   **充分性评价**：实验设计较为全面，不仅对比了静态性能，还通过“滚动时域（Rolling-horizon）”模拟了 24 小时动态环境下的表现，客观地展示了启发式算法在实时重优化中的优势。

### 6. 主要结论与发现
*   **高效性**：AGH 在大规模实例上实现了比精确求解器 **260 倍以上** 的加速，且成本非常接近最优解。
*   **鲁棒性**：在 1.5 倍参数膨胀的压力测试下，精确求解器的方案因过于“紧凑”而导致 SLO 违规激增，而 AGH 凭借其机制中自带的保守性，保持了更低的实际成本和违规率。
*   **动态优势**：由于运行极快，AGH 支持每 5 分钟进行一次重优化，在需求高波动环境下比静态 MILP 方案节省高达 **48%** 的成本。

### 7. 优点
*   **约束感知能力强**：不同于传统的通用启发式算法，该方法深度结合了 LLM 推理的特性（如 TP 对延迟和内存的非线性影响）。
*   **实时性**：亚秒级的运行速度使其能够部署在生产环境的实时调度层。
*   **鲁棒性设计**：通过实验证明了“名义上的最优解”在实际波动中往往不是“运行时的最优解”，强调了启发式算法的实用价值。

### 8. 不足与局限
*   **模拟环境限制**：实验主要基于模拟器和数学模型（如内存带宽受限的延迟模型），尚未在真实的大规模物理集群中进行端到端验证。
*   **批处理动态**：虽然考虑了吞吐量，但对“连续批处理（Continuous Batching）”和“KV 缓存管理”的动态排队细节简化较多。
*   **应用范围**：主要针对服务提供商的资源配置，对于单次请求的极低延迟调度优化涉及较少。

（完）
