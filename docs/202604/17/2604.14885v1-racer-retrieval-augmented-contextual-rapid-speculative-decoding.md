---
title: "RACER: Retrieval-Augmented Contextual Rapid Speculative Decoding"
title_zh: RACER：检索增强的上下文快速投机解码
authors: "Zihong Zhang, Zuchao Li, Lefei Zhang, Ping Wang, Hai Zhao"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14885v1"
tags: ["query:llm"]
score: 10.0
evidence: 检索增强的投机解码以实现更快推理
tldr: 针对大语言模型自回归生成速度慢的问题，本文提出RACER，一种轻量级且无需训练的投机采样方法。RACER创新性地结合了检索到的精确上下文模式与基于Logit的未来预测线索，克服了现有方法在缺乏精确匹配或结构引导时的局限性。实验证明RACER在多个基准测试中实现了超过2倍的加速，为高效LLM推理提供了可扩展的即插即用方案。
selection_source: fresh_fetch
motivation: 现有的无需训练的投机解码方法在检索不到精确匹配或缺乏结构化引导时表现不佳，导致推理效率受限。
method: 提出RACER方法，通过整合检索到的上下文模式与Logit驱动的预测线索，生成更丰富且可靠的投机草稿。
result: 在Spec-Bench、HumanEval和MGSM-ZH等测试中，RACER相比自回归解码实现了超过2倍的加速，并优于现有的无需训练的方法。
conclusion: RACER是一种高效、可扩展且无需训练的即插即用方案，显著提升了大语言模型的推理速度。
---

## 摘要
大语言模型（LLM）中的自回归解码每步仅生成一个 token，导致了较高的推理延迟。投机解码（SD）通过“猜测-验证”策略缓解了这一问题，但现有的无需训练的变体面临权衡：基于检索的草稿在不存在精确匹配时会失效，而基于 Logit 的草稿则缺乏结构性引导。我们提出了 RACER（检索增强的上下文快速投机解码），这是一种轻量级且无需训练的方法，它将检索到的精确模式与 Logit 驱动的未来线索相结合。这种统一既提供了可靠的锚点，又提供了灵活的外推，从而产生了更丰富的投机草稿。在 Spec-Bench、HumanEval 和 MGSM-ZH 上的实验表明，RACER 能够持续加速推理，相比自回归解码实现了超过 2 倍的加速，并优于先前的无需训练的方法，为高效的 LLM 解码提供了一种可扩展、即插即用的解决方案。我们的源代码可在 https://github.com/hkr04/RACER 获取。

## Abstract
Autoregressive decoding in Large Language Models (LLMs) generates one token per step, causing high inference latency. Speculative decoding (SD) mitigates this through a guess-and-verify strategy, but existing training-free variants face trade-offs: retrieval-based drafts break when no exact match exists, while logits-based drafts lack structural guidance. We propose $\textbf{RACER}$ ($\textbf{R}$etrieval-$\textbf{A}$ugmented $\textbf{C}$ont$\textbf{e}$xtual $\textbf{R}$apid Speculative Decoding), a lightweight and training-free method that integrates retrieved exact patterns with logit-driven future cues. This unification supplies both reliable anchors and flexible extrapolation, yielding richer speculative drafts. Experiments on Spec-Bench, HumanEval, and MGSM-ZH demonstrate that RACER consistently accelerates inference, achieving more than $2\times$ speedup over autoregressive decoding, and outperforms prior training-free methods, offering a scalable, plug-and-play solution for efficient LLM decoding. Our source code is available at $\href{https://github.com/hkr04/RACER}{https://github.com/hkr04/RACER}$.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **RACER**（Retrieval-Augmented Contextual Rapid Speculative Decoding），一种旨在提升大语言模型（LLM）推理速度的创新方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 的自回归解码（每步生成一个 token）存在严重的推理延迟。
*   **研究背景**：
    *   **投机解码（SD）**通过“猜测-验证”机制来加速，但现有的“无需训练（Training-free）”方法存在局限。
    *   **基于检索的方法**（如 PLD）依赖精确匹配，当上下文中没有重复模式时会失效。
    *   **基于 Logit 的方法**（如 Token Recycling）虽然能预测未来，但缺乏外部结构引导，预测范围较窄且质量不稳定。
*   **整体含义**：RACER 旨在通过统一“已见信息”（检索到的精确模式）和“未见信息”（Logit 驱动的未来预测），构建一个轻量级、无需训练且即插即用的加速方案。

### 2. 论文提出的方法论
RACER 的核心思想是结合 **Logits Tree** 和 **Retrieval Tree**，通过一个统一的缓存模块生成高质量的投机草稿。
*   **Logits Tree（Logit 树）**：
    *   **Copy-Logit 策略**：不同于传统的 Last-Logit（重用上一步 Logit），RACER 重用该 token 最近一次出现时的 Logit 分布，实验证明这能提供更尖锐、更准确的预测。
    *   **k-ary 剪枝规则**：根据节点深度动态分配宽度，确保树的上层积极扩张，深层逐步收缩，以平衡覆盖范围和计算开销。
*   **Retrieval Tree（检索树）**：
    *   **AC 自动机 + LRU 驱逐**：使用 Aho-Corasick 自动机维护上下文中的 n-gram 模式。引入 LRU（最近最少使用）机制，在达到容量上限时自动剔除过时状态，确保内存占用稳定。
    *   **故障链接（Failure Links）**：利用 AC 自动机的特性，在匹配失败时快速跳转，增强草稿的多样性。
*   **集成策略**：
    *   在固定的投机容量（如 64 个 token）下，优先填充检索到的可靠模式，剩余预算分配给 Logits Tree 进行外推探索。最后通过树状注意力（Tree Attention）进行并行验证。

### 3. 实验设计
*   **数据集/场景**：
    *   **Spec-Bench**：涵盖多轮对话、翻译、摘要、问答、数学推理和 RAG 等综合场景。
    *   **HumanEval**：代码生成任务。
    *   **MGSM-ZH**：中文数学推理任务。
*   **目标模型**：Vicuna (7B, 13B, 33B), LLaMA3.1-8B, OpenPangu-7B, Qwen3 (0.6B 到 32B 多个版本)。
*   **对比方法**：
    *   检索类：PLD, REST, SAM Decoding。
    *   Logit 类：LogitSpec, Token Recycling (TR)。
    *   基于模型类：EAGLE-3（当前 SOTA）。

### 4. 资源与算力
*   **硬件环境**：
    *   7B/8B 规模模型：单张 **NVIDIA RTX 4090 (24GB)**，20 核 CPU。
    *   13B 及以上模型：单张 **NVIDIA A800 (80GB)**，64 核 CPU。
*   **训练时长**：**无需训练**。该方法是完全的推理侧优化，不涉及模型参数更新。
*   **软件框架**：基于 PyTorch 2.8.0 和 HuggingFace Transformers。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个主流基准测试、4 个模型家族、超过 10 种不同参数规模的模型上进行了测试。
*   **消融实验**：
    *   验证了 Logits 和 Retrieval 两个组件的各自贡献。
    *   对比了不同的集成策略（Merge vs. Hard switching）。
    *   测试了超参数（草稿大小、节点容量、n-gram 长度、Top-k 宽度）的鲁棒性。
    *   测试了不同采样温度（Greedy vs. Nucleus Sampling）下的表现。
*   **充分性评价**：实验设计非常全面，涵盖了不同语言（中英）、不同任务（通用/代码/数学）和不同架构，对比基准包含了最新的同类技术，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **加速效果**：RACER 在所有测试中均实现了显著加速，平均加速比超过 **2.0×**，最高可达 **2.89×**。
*   **优越性**：在无需训练的方法中，RACER 持续优于 PLD、REST 和 Token Recycling。
*   **与模型类方法对比**：虽然 EAGLE-3（有损/需训练）在某些任务的接受率（MAT）上更高，但 RACER 由于没有额外的辅助模型计算开销，在**端到端加速比**上往往更具优势，且在跨语言任务（如中文数学）中表现更稳健。
*   **Copy-Logit 的有效性**：证明了重用相同 token 的历史 Logit 比重用当前步 Logit 更有助于预测未来。

### 7. 优点
*   **轻量且高效**：无需训练辅助模型，不增加显存负担（LRU 机制控制在几十 MB 级别）。
*   **即插即用**：适用于任何 Transformer 架构的 LLM，兼容性极强。
*   **结构化引导**：通过 AC 自动机引入了比简单 n-gram 匹配更强大的结构化检索能力。
*   **鲁棒性**：在长文本和复杂推理任务（如 Qwen3 思考模型）中表现稳定。

### 8. 不足与局限
*   **多模态缺失**：目前仅在纯文本任务上验证，尚未测试其在视觉或语音 token 上的适用性。
*   **批处理限制**：实验主要集中在 Batch Size = 1 的场景，虽然论文提到其轻量化设计利于扩展，但大规模并发下的性能表现有待进一步数据支持。
*   **检索依赖性**：在完全没有任何重复模式的极端随机文本生成中，其检索增益会退化，主要依赖 Logits Tree。

（完）
