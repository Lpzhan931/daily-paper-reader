---
title: "Prefill-as-a-Service: KVCache of Next-Generation Models Could Go Cross-Datacenter"
title_zh: Prefill-as-a-Service：下一代模型的 KVCache 可实现跨数据中心传输
authors: "Ruoyu Qin, Weiran He, Yaoyu Wang, Zheming Li, Xinran Xu, Yongwei Wu, Weimin Zheng, Mingxing Zhang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15039v1"
tags: ["query:llm"]
score: 9.0
evidence: 优化跨数据中心LLM推理服务的吞吐量与延迟
tldr: 针对大模型推理中Prefill和Decode分离架构受限于高带宽网络的问题，本文提出Prefill-as-a-Service (PrfaaS)。利用混合注意力模型KVCache较小的特性，PrfaaS通过选择性卸载长上下文Prefill任务到远程计算密集型集群，并利用普通以太网传输KVCache。结合带宽感知调度和缓存感知放置，该架构实现了跨数据中心的异构资源弹性扩展，显著提升了吞吐量并降低了对低延迟网络的依赖。
selection_source: fresh_fetch
motivation: 传统的Prefill-Decode分离架构因KVCache传输量巨大而受限于单一高带宽网络域，难以实现跨数据中心的异构资源调度与弹性扩展。
method: 提出PrfaaS架构，通过选择性卸载长上下文任务、带宽感知调度和缓存感知请求放置，利用普通以太网实现跨集群的KVCache传输与服务。
result: "在1T参数混合模型测试中，PrfaaS比同构PD架构吞吐量提升54%，比朴素异构方案提升32%，且仅需较低的跨数据中心带宽。"
conclusion: PrfaaS证明了在混合注意力模型背景下，跨数据中心的大规模LLM推理服务是可行且高效的，有效打破了计算资源的物理边界。
---

## 摘要
预填充-解码（PD）分离已成为大规模大语言模型（LLM）推理服务的标准架构，但在实践中，其部署边界仍受限于 KVCache 的传输。在传统的稠密注意力（dense-attention）模型中，预填充阶段会产生巨大的 KVCache 流量，使得预填充和解码必须紧密耦合在单个高带宽网络域内，限制了异构部署和资源弹性。近期的混合注意力（hybrid-attention）架构显著减小了 KVCache 的大小，使得跨集群的 KVCache 传输变得日益可行。然而，仅靠更小的 KVCache 并不足以让异构跨数据中心 PD 服务变得实用：实际工作负载仍然具有突发性，请求长度高度偏斜，前缀缓存分布不均，且集群间带宽存在波动。因此，将预填充完全外部化的朴素设计仍可能面临拥塞、不稳定的排队以及利用率低下的问题。我们提出了 Prefill-as-a-Service (PrfaaS)，这是一种跨数据中心的服务架构，它选择性地将长上下文预填充任务卸载到独立的计算密集型预填充集群，并通过商用以太网将生成的 KVCache 传输到本地 PD 集群进行解码。PrfaaS 并非仅仅依赖缩减后的 KVCache，而是将模型端的 KV 效率与系统端的选择性卸载、带宽感知调度以及缓存感知请求放置相结合。该设计消除了异构加速器必须共享同一低延迟 RDMA 网络的需求，从而实现了预填充和解码能力在松耦合集群间的独立扩展。在一项使用内部 1 万亿参数混合模型的案例研究中，采用 PrfaaS 增强的异构部署相比同构 PD 基准和朴素异构基准，服务吞吐量分别提升了 54% 和 32%，且仅消耗了适度的跨数据中心带宽。

## Abstract
Prefill-decode (PD) disaggregation has become the standard architecture for large-scale LLM serving, but in practice its deployment boundary is still determined by KVCache transfer. In conventional dense-attention models, prefill generates huge KVCache traffics that keep prefill and decode tightly coupled within a single high-bandwidth network domain, limiting heterogeneous deployment and resource elasticity. Recent hybrid-attention architectures substantially reduce KVCache size, making cross-cluster KVCache transport increasingly plausible. However, smaller KVCache alone does not make heterogeneous cross-datacenter PD serving practical: real workloads remain bursty, request lengths are highly skewed, prefix caches are unevenly distributed, and inter-cluster bandwidth fluctuates. A naive design that fully externalizes prefill can therefore still suffer from congestion, unstable queueing, and poor utilization.   We present Prefill-as-a-Service (PrfaaS), a cross-datacenter serving architecture that selectively offloads long-context prefill to standalone, compute-dense prefill clusters and transfers the resulting KVCache over commodity Ethernet to local PD clusters for decode. Rather than treating reduced KVCache as sufficient, PrfaaS combines model-side KV efficiency with system-side selective offloading, bandwidth-aware scheduling, and cache-aware request placement. This design removes the requirement that heterogeneous accelerators share the same low-latency RDMA fabric, enabling independent scaling of prefill and decode capacity across loosely coupled clusters. In a case study using an internal 1T-parameter hybrid model, a PrfaaS-augmented heterogeneous deployment achieves 54% and 32% higher serving throughput than homogeneous PD and naive heterogeneous baselines, respectively, while consuming only modest cross-datacenter bandwidth.

---

## 论文详细总结（自动生成）

这篇论文由月之暗面（Moonshot AI）与清华大学联合发布，探讨了如何利用下一代大模型架构的特性，打破地理限制，实现跨数据中心的推理服务优化。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的“预填充-解码分离”（PD Disaggregation）架构虽然提升了效率，但由于预填充（Prefill）阶段产生的 KVCache 极其庞大，导致这两个阶段必须紧密耦合在同一个高带宽网络（如单数据中心内的 RDMA 网络）中。这限制了异构硬件（如擅长计算的芯片做 Prefill，擅长带宽的芯片做 Decode）的灵活部署和跨地域的资源调度。
*   **研究动机**：
    *   **模型演进**：新型混合注意力架构（如 Kimi Linear、MLA 等）显著降低了 KVCache 的体积（通常降低一个数量级）。
    *   **硬件趋势**：市场上出现了专门针对 Prefill（如 NVIDIA Rubin CPX）或 Decode（如 Groq LPU）优化的异构芯片，但它们往往不在同一个物理集群中。
    *   **系统挑战**：即便 KVCache 变小，跨数据中心传输仍面临流量突发、请求长度分布极度不均、带宽波动等实际挑战。

### 2. 方法论：PrfaaS-PD 架构
*   **核心思想**：提出 **Prefill-as-a-Service (PrfaaS)**，将长上下文的预填充任务选择性地卸载到远程的计算密集型集群，通过普通以太网传输 KVCache，而本地集群负责解码和短请求处理。
*   **关键技术细节**：
    *   **选择性卸载（Selective Offloading）**：基于长度阈值 $t$ 进行路由。只有当请求的未缓存长度 $l > t$ 时，才发送至远程 PrfaaS 集群，避免短请求浪费跨集群带宽。
    *   **混合前缀缓存池（Hybrid Prefix Cache Pool）**：针对混合模型中“请求级线性状态”和“块级注意力 KVCache”的不同特性，建立统一的块管理机制，支持跨集群的缓存感知路由。
    *   **双时序调度（Dual-Timescale Scheduling）**：
        *   **短期**：带宽与缓存感知路由，根据实时出口带宽和队列深度动态调整路由决策。
        *   **长期**：基于流量分布的资源重分配，动态调整本地集群中 Prefill 和 Decode 节点的比例。
    *   **吞吐量模型**：通过数学建模平衡 PrfaaS 集群计算能力、出口带宽以及本地解码能力，寻找最优的路由阈值 $t$。

### 3. 实验设计
*   **场景与模型**：使用内部 **1 万亿（1T）参数** 的混合架构模型（KDA:MLA 比例为 3:1），模拟长上下文工作负载（平均 27K tokens，最高 128K）。
*   **Benchmark 与对比方法**：
    *   **Homogeneous PD（同构基准）**：全部使用 H20 GPU 的单集群 PD 分离部署。
    *   **Naive Heterogeneous（朴素异构）**：简单地将所有 Prefill 放在 H200，所有 Decode 放在 H20，不进行选择性路由。
    *   **PrfaaS-PD（本文方法）**：异构部署 + 智能调度。
*   **网络环境**：跨集群使用 100 Gbps 的 VPC 以太网连接，集群内部使用 800 Gbps RDMA。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **PrfaaS 集群**：32 块 NVIDIA **H200** GPU（4 个 8-GPU 实例）。
    *   **本地 PD 集群**：64 块 NVIDIA **H20** GPU。
    *   **对比基准**：96 块 NVIDIA H20 GPU。
*   **训练/推理时长**：文中未提及模型训练时长，实验侧重于在线推理服务的吞吐量和延迟表现。

### 5. 实验数量与充分性
*   论文通过一个深度案例研究（Case Study）展开，利用实际生产环境的 Profiling 数据驱动仿真模型。
*   **实验维度**：涵盖了不同输入长度下的 KV 吞吐量测试、不同部署策略下的系统总吞吐量对比、P90 首字延迟（TTFT）分析以及带宽利用率监测。
*   **充分性**：虽然是单一模型的案例研究，但该模型规模（1T）和架构具有代表性，且通过网格搜索（Grid Search）验证了参数选择的科学性，实验结果较为客观。

### 6. 主要结论与发现
*   **带宽可行性**：对于混合架构模型，跨数据中心传输 KVCache 是完全可行的。在 1T 模型测试中，PrfaaS 集群仅消耗了约 13 Gbps 的带宽（占 100G 链路的 13%）。
*   **性能提升**：PrfaaS-PD 相比同构 PD 架构，**吞吐量提升了 54%**，P90 TTFT 降低了 64%。
*   **调度价值**：相比朴素的异构方案，PrfaaS 的智能调度带来了额外 32% 的吞吐量增益，证明了“选择性卸载”是跨数据中心服务的关键。

### 7. 优点
*   **前瞻性**：敏锐捕捉到模型架构演进（KVCache 缩减）对系统架构（跨地域部署）的深远影响。
*   **实用性**：不要求昂贵的跨中心 RDMA，利用现有的商用以太网即可实现异构资源池化。
*   **软硬协同**：将模型层的 KV 压缩效率与系统层的带宽感知调度完美结合。

### 8. 不足与局限
*   **模型依赖性**：该方案高度依赖于混合注意力架构（如线性注意力、SWA 等）。对于传统的全注意力（Dense MHA）模型，由于 KVCache 随长度线性/二次方增长，该方案依然难以逾越带宽瓶颈。
*   **实验覆盖面**：主要基于月之暗面内部的 1T 模型和特定硬件组合（H200+H20），对于其他比例的混合模型或更极端的网络波动环境（如跨洲际链路）缺乏更广泛的测试。
*   **复杂性**：引入了全局 KVCache 管理器和复杂的双时序调度，增加了系统的运维复杂度。

（完）
