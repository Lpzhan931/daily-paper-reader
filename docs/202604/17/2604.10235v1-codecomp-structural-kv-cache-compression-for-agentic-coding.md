---
title: "CodeComp: Structural KV Cache Compression for Agentic Coding"
title_zh: CodeComp：面向智能体编程的结构化 KV 缓存压缩
authors: "Qiujiang Chen, Jing Xiong, Chenyang Zhao, Sidi Yang, Ngai Wong"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10235v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对代码任务的结构化KV缓存压缩
tldr: CodeComp 是一种针对智能体编程任务的免训练 KV 缓存压缩框架。针对现有方法仅依赖注意力信号而忽略代码结构关键标记（如调用点、分支条件）的问题，该框架引入了基于 Joern 提取的代码属性图（CPG）静态分析先验。CodeComp 在缺陷定位和代码生成任务中表现优异，在大幅压缩缓存的同时，能恢复大部分全上下文精度，并可无缝集成至 SGLang 等推理流水线中。
selection_source: fresh_fetch
motivation: 现有的 KV 缓存压缩方法仅依赖注意力信号，容易误删对代码理解至关重要的结构化标记（如函数调用、分支条件和赋值）。
method: 提出 CodeComp 框架，通过 Joern 提取代码属性图（CPG）作为静态分析先验，将其融入 LLM 推理过程以保留关键结构标记。
result: 在缺陷定位和代码生成基准测试中，CodeComp 在相同内存预算下显著优于仅依赖注意力的基线方法，并能匹配未压缩推理的生成质量。
conclusion: CodeComp 为长上下文编程任务提供了一种高效且无需训练的压缩方案，在显著降低内存开销的同时保持了极高的代码理解与生成能力。
---

## 摘要
智能体代码任务（如故障定位和补丁生成）需要在严格的内存限制下处理长代码库，其中键值（KV）缓存已成为主要的推理瓶颈。现有的压缩方法完全依赖注意力信号来估计 token 的重要性，从而系统性地丢弃了对代码理解至关重要的结构性关键 token，例如调用点、分支条件和赋值语句。我们提出了 CodeComp，这是一个无需训练的 KV 缓存压缩框架，它通过 Joern 提取的代码属性图（Code Property Graph）先验，将静态程序分析融入到大语言模型（LLM）的推理中。在错误定位和代码生成基准测试中，CodeComp 在同等内存预算下始终优于仅依赖注意力的压缩基准方法。在激进的 KV 缓存压缩下，它恢复了大部分全上下文准确率，同时达到了与未压缩全上下文推理相当的补丁生成质量，并能无缝集成到基于 SGLang 的智能体编程流水线中，无需修改模型。

## Abstract
Agentic code tasks such as fault localization and patch generation require processing long codebases under tight memory constraints, where the Key-Value (KV) cache becomes the primary inference bottleneck. Existing compression methods rely exclusively on attention signals to estimate token importance, systematically discarding structurally critical tokens such as call sites, branch conditions, and assignments that are essential for code understanding. We present CodeComp, a training-free KV cache compression framework that incorporates static program analysis into LLM inference via Code Property Graph priors extracted by Joern. Across bug localization and code generation benchmarks, CodeComp consistently outperforms attention-only compression baselines under equal memory budgets, recovering the majority of full-context accuracy under aggressive KV cache compression, while matching the patch generation quality of uncompressed full-context inference and integrating seamlessly into SGLang-based agentic coding pipelines without model modification.

---

## 论文详细总结（自动生成）

这是一份关于论文《CodeComp: Structural KV Cache Compression for Agentic Coding》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在处理大规模代码库的智能体任务（如缺陷定位、补丁生成）时，KV 缓存（Key-Value Cache）随上下文长度线性增长，成为显存瓶颈（可占 GPU 内存的 70%）。
*   **研究动机**：
    *   **注意力机制的局限性**：现有的 KV 缓存压缩方法（如 SnapKV, H2O）主要依赖注意力分数来筛选 token。但在代码场景中，注意力分数与程序语义重要性之间存在严重错位（Jaccard 重合度仅为 0.0944）。
    *   **结构性误删**：仅靠注意力信号会误删对代码逻辑至关重要的结构化标记，如函数调用点（Call sites）、分支条件（Branch conditions）和赋值语句（Assignments），导致模型无法正确理解跨文件的依赖关系和控制流。

### 2. 方法论（CodeComp）
CodeComp 是一个**无需训练**的框架，其核心思想是将**静态程序分析**引入推理过程。
*   **关键技术流程**：
    1.  **查询条件块选择（Step 1）**：利用基于困惑度（PPL）的评分，从大规模代码库中筛选出与当前查询（Query）最相关的代码块（Chunks）。
    2.  **结构特征提取（Step 2）**：使用 **Joern** 工具提取代码属性图（CPG），识别函数调用、控制流、返回语句和赋值等语义锚点。
    3.  **结构感知预算分配（Step 3）**：根据代码块的结构复杂度和重要性评分，动态分配 KV 缓存预算。结构越复杂的块，保留的 token 越多。
    4.  **跨度级结构保护（Step 4）**：显式保护（不删除）关键语义跨度（如整个函数签名或条件表达式），确保这些逻辑单元的完整性。
    5.  **注意力残差压缩（Step 5）**：在满足结构保护的前提下，利用注意力分数填充剩余的缓存容量。
*   **位置编码处理**：采用独立评分和位置偏移策略，确保压缩后的代码块在输入模型时保持正确的位置关系。

### 3. 实验设计
*   **数据集/场景**：
    *   **缺陷定位**：InfiniteBench-CodeDebug, DebugBench, LongCodeQA。
    *   **代码生成**：SWE-bench Lite, LCA (Library-based Code Generation)。
*   **对比基准（Baselines）**：
    *   **无压缩（Full Context）**：作为性能上限。
    *   **SnapKV**：基于观察窗口的注意力压缩方法。
    *   **ParallelComp**：基于块的并行压缩方法。
*   **评估指标**：F1 分数、API 准确率、Jaccard 相似度、补丁有效性及编辑距离。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 训练时长，因为该方法是**免训练（Training-free）**的。
*   **推理环境**：实验基于 **SGLang** 推理引擎实现。在吞吐量测试中，使用了 Qwen2.5-Coder-32B-Instruct 模型。
*   **效率表现**：端到端延迟在不同压缩率下保持稳定（约 112-118s），表明 Joern 静态分析带来的额外开销极低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流代码基准测试，使用了 Llama-3-8B 和 Qwen3-8B 等多个模型。
*   **消融实验**：
    *   对比了“仅预算分配”与“仅跨度保护”的效果，证明**跨度级保护**是性能提升的核心。
    *   对不同结构特征（调用、控制流等）进行了特征级消融。
*   **客观性**：实验在相同的内存预算（如 0.4x, 0.6x 压缩率）下进行对比，确保了公平性。引入了“结构得分（Str. Score）”这一新指标来量化压缩质量。

### 6. 主要结论与发现
*   **性能优异**：CodeComp 在所有基准测试中均显著优于仅依赖注意力的基线。在 0.4 倍压缩率下，其在 DebugBench 上的表现比 ParallelComp 高出 14 倍。
*   **质量匹配**：在激进压缩下，CodeComp 生成补丁的质量（编辑距离）能与未压缩的全上下文推理相媲美。
*   **结构重要性**：实验证明，保留函数调用和控制流锚点对代码理解的贡献最大。

### 7. 优点
*   **结构感知**：首次将静态分析（CPG）与 KV 缓存压缩结合，解决了代码语义丢失的问题。
*   **即插即用**：无需重新训练模型，可直接集成到现有的 SGLang 等高性能推理流水线中。
*   **高效性**：在大幅降低显存占用的同时，几乎不增加推理延迟。

### 8. 不足与局限
*   **依赖外部工具**：性能受限于静态分析工具（Joern）的准确性和支持的语言范围。
*   **静态分析开销**：虽然在长文本推理中开销占比小，但对于极短文本，静态分析可能成为额外的负担。
*   **任务局限**：主要针对结构化特征明显的编程语言，对于自然语言或其他非结构化数据的泛化能力有限。

（完）
