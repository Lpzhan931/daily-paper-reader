---
title: Continuous Semantic Caching for Low-Cost LLM Serving
title_zh: 面向低成本 LLM 服务的连续语义缓存
authors: "Baran Atalar, Xutong Liu, Jinhang Zuo, Siwei Wang, Wei Chen, Carlee Joe-Wong"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.20021v1"
tags: ["query:llm"]
score: 8.0
evidence: 低成本LLM服务的语义缓存
tldr: 针对大语言模型推理成本高的问题，本文提出了首个在连续语义空间下进行响应缓存的理论框架。传统方法局限于离散查询集，而本研究通过动态 ε-net 离散化和核岭回归技术，实现了对无限连续查询空间的有效覆盖。该框架能量化不确定性并减少缓存切换开销，在保证亚线性遗憾界的同时，显著降低了计算成本和延迟。
selection_source: fresh_fetch
motivation: 现有的语义缓存方法假设查询空间是离散且有限的，难以处理现实中无限且连续的语义嵌入空间。
method: 提出结合动态 ε-net 离散化与核岭回归的算法，通过量化不确定性实现跨语义邻域的成本反馈泛化并优化切换成本。
result: 理论证明了算法具有亚线性遗憾界，且实验显示其在逼近最优缓存的同时显著降低了计算和切换开销。
conclusion: 该框架为连续空间下的 LLM 语义缓存提供了严谨的理论基础和高效的在线优化方案，有效提升了服务效率。
---

## 摘要
随着大语言模型（LLM）日益普及，缓存响应以便具有语义相似查询的用户重复使用，已成为降低推理成本和延迟的关键策略。现有的缓存框架通常假设存在一个有限且已知的离散查询全集，并通过学习这些查询的服务成本和到达概率来决定缓存哪些响应。然而，随着 LLM 用户群和查询量的扩大，这种假设变得越来越难以成立：现实世界中的 LLM 查询存在于一个无限且连续的嵌入空间中。在本文中，我们建立了首个在不确定性下，针对连续查询空间中语义 LLM 响应缓存的严谨理论框架。为了弥合离散优化与连续表示空间之间的鸿沟，我们引入了动态 ε-网（ε-net）离散化，并结合了核岭回归（Kernel Ridge Regression）。该设计使系统能够形式化地量化估计不确定性，并在连续的语义查询邻域内泛化关于 LLM 查询成本的部分反馈。我们开发了离线学习和在线自适应算法，旨在减少因更改缓存响应而产生的切换成本。我们证明了我们的在线算法相对于最优连续预言机（oracle）实现了次线性遗憾界（sublinear regret bound），且该界限可以退化为离散查询模型的现有界限。广泛的实证评估表明，我们的框架能够很好地逼近连续最优缓存，同时与现有方法相比，降低了计算和切换开销。

## Abstract
As Large Language Models (LLMs) become increasingly popular, caching responses so that they can be reused by users with semantically similar queries has become a vital strategy for reducing inference costs and latency. Existing caching frameworks have proposed to decide which query responses to cache by assuming a finite, known universe of discrete queries and learning their serving costs and arrival probabilities. As LLMs' pool of users and queries expands, however, such an assumption becomes increasingly untenable: real-world LLM queries reside in an infinite, continuous embedding space. In this paper, we establish the first rigorous theoretical framework for semantic LLM response caching in continuous query space under uncertainty. To bridge the gap between discrete optimization and continuous representation spaces, we introduce dynamic $ε$-net discretization coupled with Kernel Ridge Regression. This design enables the system to formally quantify estimation uncertainty and generalize partial feedback on LLM query costs across continuous semantic query neighborhoods. We develop both offline learning and online adaptive algorithms optimized to reduce switching costs incurred by changing the cached responses. We prove that our online algorithm achieves a sublinear regret bound against an optimal continuous oracle, which reduces to existing bounds for discrete query models. Extensive empirical evaluations demonstrate that our framework approximates the continuous optimal cache well while also reducing computational and switching overhead compared to existing methods.

---

## 论文详细总结（自动生成）

这篇论文《Continuous Semantic Caching for Low-Cost LLM Serving》针对大语言模型（LLM）推理成本高昂的问题，提出了首个在**连续查询空间**下进行语义缓存的严谨理论框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：LLM 推理存在高延迟、高能耗和高计算成本。语义缓存通过存储并复用相似查询的响应来降低成本。
*   **核心挑战**：现有缓存研究大多假设查询来自一个**有限且已知**的离散集合。然而，现实中的 LLM 查询存在于**无限且连续**的嵌入空间中。
*   **整体含义**：论文旨在解决如何在无法穷举查询、且查询成本和到达概率未知的情况下，在连续空间内优化缓存决策，并提供理论上的性能保证（如次线性遗憾界）。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：通过离散化技术将连续空间映射到有限表示，并利用核方法在连续空间内泛化学习到的成本信息。
*   **关键技术细节**：
    *   **动态 $\epsilon$-net 离散化**：将连续的嵌入空间划分为 Voronoi 区域。每个区域由一个代表性查询（中心）覆盖，确保空间内任一点到中心的距离不超过 $\epsilon$。
    *   **核岭回归 (Kernel Ridge Regression, KRR)**：由于 LLM 推理成本仅在调用时可见（部分反馈），算法使用 KRR（如 RBF 核）来估计连续空间中未知点的服务成本，并量化估计的不确定性（置信区间）。
    *   **算法流程**：
        1.  **离线算法 (CUCB-SC-Cont)**：利用历史日志构建 $\epsilon$-net，通过 KRR 进行悲观成本估计，并使用“反向贪婪 (Reverse Greedy)”策略选择最优缓存集。
        2.  **在线算法 (CLCB-SC-LS-Cont)**：在查询流到达时动态构建 $\epsilon$-net。引入**低切换成本 (Low-Switching)** 约束，通过设置触发阈值来平衡“更新缓存以获取最新知识”与“减少因重新运行查询产生的开销”之间的矛盾。
    *   **理论保证**：证明了在线算法在连续空间下具有 $\tilde{O}(\sqrt{T})$ 的次线性遗憾界，这意味着随着时间推移，算法性能趋近于拥有完全信息的最优预言机。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：基于 50 个多样化提示词，通过对嵌入向量添加高斯噪声来模拟连续查询流。
    *   **真实数据集 (NQ+TriviaQA)**：结合了 Natural Questions 和 TriviaQA 两个数据集，共 5000 个真实查询，模拟具有“爆发性”和非均匀分布特征的查询流。
*   **Benchmark 与对比方法**：
    *   **对比方法**：CUCB-SC（离散版语义缓存）、Greedy（贪婪算法）、Epsilon-Greedy、LFU（最不经常使用缓存算法）。
    *   **评估指标**：次优性间隙 (Suboptimality Gap)、平均遗憾值 (Average Regret)、运行时间 (Runtime)。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **效率体现**：实验部分给出了算法的运行时间（Runtime），显示其在线算法在保持高性能的同时，计算开销与现有的启发式语义缓存方法（如 LFU）处于竞争水平（通常在数百秒量级完成数千轮实验）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了离线和在线两种设置下的对比。
    *   **消融实验**：针对 $\epsilon$ 半径、流长度 (Stream Length)、缓存大小 ($k$) 进行了详细的参数敏感性分析。
    *   **重复性**：实验结果基于 20 个不同的随机种子取平均值。
*   **充分性评价**：实验设计较为充分，涵盖了从合成数据到真实复杂分布的过渡，验证了算法在不同维度（成本、延迟、切换频率）下的有效性，客观地展示了理论结果在实际场景中的转化。

### 6. 主要结论与发现
*   **性能提升**：在离线设置下，提出的算法比现有离散方法（CUCB-SC）在次优性间隙上提升了高达 **73.32%**。
*   **遗憾降低**：在在线设置下，该算法在合成流和真实爆发性流中分别实现了 **72.41%** 和 **43.14%** 的平均遗憾降低。
*   **理论验证**：实验证实了 $\epsilon$ 的选择存在权衡——过大的 $\epsilon$ 导致离散化误差，过小的 $\epsilon$ 导致估计不确定性增加，这与论文的理论推导一致。

### 7. 优点
*   **理论严谨性**：建立了首个针对连续空间语义缓存的数学模型，并给出了严格的收敛性证明。
*   **泛化能力强**：通过 KRR 解决了“未见查询”的成本估计问题，比传统的精确匹配或离散分类更符合自然语言的连续性特征。
*   **实用性优化**：考虑了缓存切换成本，这在实际 LLM 服务系统中非常重要，避免了频繁更新缓存带来的额外推理负担。

### 8. 不足与局限
*   **超参数敏感性**：算法性能高度依赖于 $\epsilon$ 半径和核函数参数的选择，虽然论文给出了理论指导，但在完全未知的分布中手动调优可能较难。
*   **计算复杂度**：随着观测数据增加，KRR 的核矩阵计算开销会增大，虽然论文采用了阶段性更新，但在极大规模在线服务中的扩展性仍有待进一步验证。
*   **应用限制**：主要针对事实性、非用户特定信息的查询。对于高度个性化或实时变化的查询，缓存的有效性会下降。

（完）
