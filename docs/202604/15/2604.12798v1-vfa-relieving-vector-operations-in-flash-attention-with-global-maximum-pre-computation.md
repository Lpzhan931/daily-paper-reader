---
title: "VFA: Relieving Vector Operations in Flash Attention with Global Maximum Pre-computation"
title_zh: VFA：通过全局最大值预计算缓解 Flash Attention 中的向量操作
authors: "Yupeng Sun, Yanzhao Li, Zhiqiang Zou, Bai Du, Zhiyuan Zhang, Hui Dong, Gaoyige Fan, Hui Wang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12798v1"
tags: ["query:llm"]
score: 10.0
evidence: 向量缓解Flash Attention (VFA) 减少注意力机制中的非矩阵乘法延迟
tldr: FlashAttention 虽然解决了内存问题，但其在线 Softmax 中的向量操作（如 rowmax 规约和重缩放）在高性能硬件上逐渐成为瓶颈。本文提出 VFA，通过预计算全局最大值的近似值来初始化运行最大值，并结合键块重排序（优先处理 Sink 和局部块）来尽早稳定最大值，从而减少后续的规约和重缩放开销。实验表明，VFA 在保持精度的同时显著提升了推理速度，在特定硬件配置下可实现数倍加速。
selection_source: fresh_fetch
motivation: 随着张量核心吞吐量接近极限，FlashAttention 中在线 Softmax 的非矩阵乘法向量操作（如规约和重缩放）成为了主要的性能瓶颈。
method: 提出 VFA 方法，通过键块摘要初始化最大值并重排序块遍历顺序，在最大值稳定后冻结更新以消除重复的向量计算。
result: 在 MMLU 和 MATH500 等基准测试中，VFA 和 VSA 在不损失性能的前提下，相比基准模型实现了近 2 倍甚至最高 6 倍的加速。
conclusion: VFA 有效缓解了在线 Softmax 的向量计算瓶颈，是优化现代加速器上注意力机制性能的一种硬件友好型方案。
---

## 摘要
FlashAttention 风格的在线 Softmax 通过在片上内存中流式传输分数分块，并维护运行最大值和归一化因子，实现了线性内存占用的精确注意力计算。然而，随着现代加速器上的注意力算子接近张量核心/立方核心的峰值吞吐量，在线 Softmax 的非矩阵乘法组件——特别是逐分块的行最大值与行和归约以及重缩放链——可能会受到向量或 SIMD 限制，并主导延迟。本文重新审视了 FlashAttention，并提出了向量缓解 Flash Attention（VFA），这是一种硬件友好的方法，在保留在线 Softmax 结构的同时，减少了由行最大值驱动的运行最大值更新。VFA 通过键块表示的廉价近似来初始化运行最大值，重新排列键块遍历顺序以优先处理高影响力的汇聚块和局部块，并冻结剩余块的最大值以避免重复的归约和重缩放。我们进一步将 VFA 与 BLASST 等块稀疏跳过方法集成，形成向量缓解稀疏注意力（VSA），从而同时减少块数量和逐块开销。值得注意的是，VFA 和 VSA 完全避免了 FA4.0 更新阶段中使用的条件重缩放操作。在包括 MMLU 和 MATH500 在内的基准测试上的广泛评估，结合注意力统计数据，验证了我们的设计：(i) 汇聚和局部重排序能尽早稳定运行最大值；(ii) 由于块内异质性，简单的 Q 和 K 块摘要会失效；(iii) 当最大值出现在中间块时，需要进行 m 初始化。总体而言，VFA 和 VSA 在不损失性能的情况下，有效地缓解了在线 Softmax 的归约瓶颈。与 C16V32 基准相比，C8V32、C4V32 和 C4V16 在现代硬件上触及向量瓶颈时实现了近两倍的加速。随着未来架构的改进，C4V16 将通过增强指数容量提供六倍的加速。

## Abstract
FlashAttention-style online softmax enables exact attention computation with linear memory by streaming score tiles through on-chip memory and maintaining a running maximum and normalizer. However, as attention kernels approach peak tensor-core/cube-core throughput on modern accelerators, non-matmul components of online softmax -- especially per-tile rowmax and rowsum reductions and rescale chains -- can become vector or SIMD limited and dominate latency. This paper revisits FlashAttention and proposes Vector Relieved Flash Attention (VFA), a hardware-friendly method that reduces rowmax-driven updates of the running maximum while retaining the online-softmax structure. VFA initializes the running maximum via a cheap approximation from key-block representations, reorders key-block traversal to prioritize high-impact sink and local blocks, and freezes the maximum for remaining blocks to avoid repeated reductions and rescaling. We further integrate VFA with block-sparse skipping methods such as BLASST to form Vector Relieved Sparse Attention (VSA), which reduces both block count and per-block overhead. Notably, VFA and VSA completely avoid the conditional rescale operation in the update stage used in FA4.0. Extensive evaluations on benchmarks including MMLU and MATH500, together with attention statistics, verify our design: (i) sink and local reordering stabilizes the running maximum early; (ii) simple Q and K block summaries fail due to intra-block heterogeneity; (iii) m-initialization is required when maxima appear in middle blocks. Overall, VFA and VSA efficiently alleviate online-softmax reduction bottlenecks without performance loss. Compared to the C16V32 baseline, C8V32, C4V32 and C4V16 achieve nearly two times speedup on modern hardware while hitting the vector bottleneck. With upcoming architecture improvements, C4V16 will deliver six times speedup by enhancing exponent capacity.

---

## 论文详细总结（自动生成）

### VFA: 通过全局最大值预计算缓解 Flash Attention 中的向量操作 —— 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着深度学习硬件（如 GPU 的 Tensor Core 或 NPU 的 Cube Core）算力的飞速提升，矩阵乘法（MatMul）的执行效率已接近极限。在这种背景下，**FlashAttention (FA)** 算法中的非矩阵乘法组件（Non-MatMul）逐渐成为新的性能瓶颈。
*   **瓶颈所在**：在线 Softmax 需要频繁进行逐分块的行最大值（rowmax）规约、行和（rowsum）计算以及为了数值稳定性而进行的重缩放（Rescale）操作。
*   **研究动机**：在低精度（如 FP8, FP4）计算普及的趋势下，向量/SIMD 操作（通常仍需高精度）的耗时占比显著增加。论文旨在减少这些向量操作的频率，从而进一步压榨硬件性能。

#### 2. 论文提出的方法论：VFA 与 VSA
论文提出了 **VFA (Vector Relieved Flash Attention)**，其核心思想是通过“预估+冻结”的策略减少运行最大值（running maximum）的更新频率。

*   **关键技术细节**：
    1.  **快速 m-初始化 (m-initialization)**：在正式计算前，利用预计算的键块（Key-block）摘要（采用 `sabsmax`，即保留符号的绝对值最大值）与查询块（Query）进行廉价的点积运算，为每一行初始化一个接近真实值的运行最大值 $m$。
    2.  **汇聚+局部重排序 (Sink + Local Reordering)**：改变 Key 块的遍历顺序，优先处理“汇聚块”（序列开头的 Token）和“局部块”（当前 Query 附近的 Token）。实验证明，注意力机制的最大值通常在这些区域就已稳定。
    3.  **最大值冻结 (Max-freezing)**：在处理完上述高影响力块后，冻结运行最大值 $m$。对于后续的剩余块，不再计算 `rowmax`，也不再执行复杂的 `rescale` 链，仅进行指数累加。
*   **扩展方法 VSA**：将 VFA 与动态块稀疏跳过技术（如 BLASST）结合，既减少了处理的块数，又降低了每个块的向量计算开销。

#### 3. 实验设计
*   **数据集/场景**：涵盖了数学推理（MATH500）、通用知识（MMLU 子集）、代码生成（HumanEval）和中文综合评估（CMMLU 子集）。
*   **Benchmark**：以 **FlashAttention-2 (FA2)** 作为主要基准。
*   **对比模型**：使用了 Qwen3-30B-A3B、Qwen3-8B 和 Llama3-8B 等主流开源大模型进行验证。
*   **消融实验**：针对 `m-init` 的必要性、重排序的影响、以及不同块表示方法（Mean, Max, sabsmax）进行了详细对比。

#### 4. 资源与算力
*   **算力说明**：论文作者来自华为技术（Huawei Technologies），实验基于“最新架构”（Latest Architecture）的计算能力进行评估。
*   **具体细节**：文中给出了不同硬件配置比例（如 C16V32, C4V16，代表张量核心与向量核心的吞吐比例）下的延迟分解。
*   **缺失信息**：论文**未明确说明**具体的 GPU/NPU 型号（如 H100 或 Ascend 910B/C）、具体的集群规模以及训练/推理的总时长。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个不同规模的模型上进行了 7 类任务的准确率测试，并提供了详尽的消融实验（表 3）和数据统计分析（图 2-5）。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅关注端到端的准确率（确保算法改进不掉点），还深入分析了注意力分数的分布特性（如最大值稳定位置、块内异质性），从数值统计角度证明了方法论的合理性。

#### 6. 主要结论与发现
*   **性能提升**：在现代硬件架构下，VFA 相比基准可实现约 **2 倍**的加速；在未来指数运算能力增强的架构下，理论加速比可达 **6 倍**。
*   **精度保持**：VFA 在所有测试任务中均保持了与 FA2 相当的准确率，证明了“冻结最大值”策略在引入微小近似误差的情况下对下游任务无显著影响。
*   **统计发现**：简单的块摘要（如 Mean）会失效，是因为块内 Token 存在巨大的范数差异和方向异质性，因此必须使用 `sabsmax` 和行级初始化。

#### 7. 优点
*   **硬件友好**：直接针对现代加速器中向量单元与张量单元的负载失衡问题，具有很强的工程落地价值。
*   **兼容性强**：VFA 可以作为插件与现有的稀疏注意力（Sparse Attention）或 FlashAttention-4 的优化技术无缝结合。
*   **理论支撑扎实**：通过对注意力矩阵统计特性的深入挖掘（如 Sink/Local 效应），为算法的启发式设计提供了科学依据。

#### 8. 不足与局限
*   **分布依赖性**：VFA 依赖于注意力最大值早期稳定的假设。如果某些特定任务（如长文本中的随机检索）导致注意力分布极其分散或最大值出现在序列末尾，该方法的加速效果或精度可能会受损。
*   **硬件绑定**：其加速效果高度依赖于硬件中向量单元与张量单元的比例，在某些向量算力充沛的旧架构上，收益可能不明显。
*   **评估范围**：虽然涵盖了主流任务，但对于超长文本（如 100K+ context）的数值稳定性压力测试尚待进一步补充。

（完）
