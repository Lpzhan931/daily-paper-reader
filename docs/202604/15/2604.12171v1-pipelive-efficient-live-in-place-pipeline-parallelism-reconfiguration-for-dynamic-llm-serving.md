---
title: "PipeLive: Efficient Live In-place Pipeline Parallelism Reconfiguration for Dynamic LLM Serving"
title_zh: PipeLive：面向动态大语言模型推理服务的高效在线原位流水线并行重配置
authors: "Xu Bai, Muhammed Tawfiqul Islam, Chen Wang, Adel N. Toosi"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12171v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过流水线并行优化动态LLM服务的吞吐量和延迟
tldr: 针对大语言模型推理中静态流水线并行（PP）配置难以适应动态环境且重配置停机时间长的问题，本文提出PipeLive。它支持在不中断推理的情况下进行实时原位PP重配置。PipeLive通过重新设计的KV缓存布局和PagedAttention扩展实现KV动态缩放，并借鉴虚拟机迁移技术引入增量KV补丁机制以保证状态一致性。该系统显著降低了重配置开销，在保证服务连续性的同时提升了动态环境下的推理效率。
selection_source: fresh_fetch
motivation: 现有的流水线并行配置无法灵活应对动态变化的GPU资源，且传统的停机重部署方式会造成不可接受的服务中断。
method: 引入了支持动态缩放的KV缓存布局与PagedAttention扩展，并采用增量KV补丁机制在不停止推理的情况下同步状态。
result: "成功将重配置开销缩减至10毫秒以内，并在动态场景下使TTFT和TPOT分别优化了高达54.7%和14.7%。"
conclusion: PipeLive为动态LLM服务提供了一种高效、低延迟的流水线并行重配置方案，解决了资源受限下的状态一致性与性能瓶颈。
---

## 摘要
流水线并行（PP）被广泛用于将大语言模型（LLM）的层划分到多个 GPU 上，从而实现大模型的可扩展推理。然而，现有系统依赖于静态 PP 配置，无法适应无服务器平台和异构 GPU 环境等动态场景。通过停止并重新部署服务来重配置 PP 会产生高昂的停机时间，因此重配置必须以在线原位的方式进行，且不中断推理。然而，在线原位 PP 重配置面临根本性的挑战：GPU 资源已被模型权重和 KV 缓存占满，几乎没有空间放置新层，且需要调整 KV 缓存大小，这与 vLLM 等为保证吞吐量而进行预分配的系统相冲突。此外，在执行过程中保持 KV 一致性非常困难：“停止并复制”会引入较长的停顿，而后台同步则面临状态演变导致的不一致风险。我们提出了 PipeLive，它能以极小的中断实现在线原位 PP 重配置。PipeLive 引入了重新设计的 KV 缓存布局以及针对 PageAttention 的协同设计扩展，形成了一种统一的在线 KV 调整机制。它进一步采用了一种受虚拟机在线迁移启发的增量 KV 补丁机制，用于同步源配置与目标配置之间的 KV 状态，并确定安全的切换点。实验表明，与禁用 KV 调整相比，PipeLive 在不发生 KV 缓存溢出的情况下，将首字延迟（TTFT）降低了 2.5 倍。此外，与不带 KV 补丁的变体相比，它将重配置开销从秒级降低到 10 毫秒以下，并将 TTFT 和逐字延迟（TPOT）分别提升了高达 54.7% 和 14.7%。

## Abstract
Pipeline parallelism (PP) is widely used to partition layers of large language models (LLMs) across GPUs, enabling scalable inference for large models. However, existing systems rely on static PP configurations that fail to adapt to dynamic settings, such as serverless platforms and heterogeneous GPU environments. Reconfiguring PP by stopping and redeploying service incurs prohibitive downtime, so reconfiguration must instead proceed live and in place, without interrupting inference. However, live in-place PP reconfiguration is fundamentally challenging. GPUs are already saturated with model weights and KV cache, leaving little room for new layer placements and necessitating KV cache resizing, at odds with systems like vLLM that preallocate for throughput. Moreover, maintaining KV consistency during execution is difficult: stop-and-copy introduces large pauses, while background synchronization risks inconsistency as states evolve. We present PipeLive, which enables live in-place PP reconfiguration with minimal disruption. PipeLive introduces a redesigned KV cache layout together with a co-designed extension to PageAttention, forming a unified mechanism for live KV resizing. It further adopts an incremental KV patching mechanism, inspired by live virtual machine migration, to synchronize KV states between source and target configurations and identify a safe switch point. PipeLive achieves a 2.5X reduction in time-to-first-token (TTFT) without KV cache overflow compared to disabling KV resizing. Furthermore, compared to a variant without KV patching, it reduces reconfiguration overhead from seconds to under 10ms, and improves TTFT and time-per-output-token (TPOT) by up to 54.7% and 14.7%, respectively.

---

## 论文详细总结（自动生成）

以下是对论文《PipeLive: Efficient Live In-place Pipeline Parallelism Reconfiguration for Dynamic LLM Serving》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）推理通常采用流水线并行（PP）将模型层分布在多个GPU上。现有的推理系统（如vLLM）多采用静态PP配置，在启动前确定层划分。
*   **核心问题**：在动态负载（如Prefill密集型与Decode密集型交替）或异构GPU环境下，固定的PP配置会导致严重的性能瓶颈。
*   **研究动机**：现有的重配置方法需要停止服务并重启，造成分钟级的停机时间。因此，迫切需要一种**在线（Live）且原位（In-place）**的重配置机制，在不中断推理的前提下动态调整模型层的分布。

### 2. 方法论：PipeLive 核心技术
PipeLive 引入了一套协同设计的架构，主要包含以下关键技术：
*   **重配置协调协议（5阶段流程）**：
    1.  **可行性评估**：检查目标配置在当前GPU显存约束下是否可行。
    2.  **KV缓存缩减**：为即将迁入的层腾出显存空间。
    3.  **异步加载与迁移**：后台加载模型权重，同时流式迁移KV缓存。
    4.  **收敛监控**：跟踪源与目标GPU之间的KV状态差异。
    5.  **原子切换**：在差异极小时瞬间切换到新配置，并清理旧层。
*   **动态KV缓存管理**：
    *   **非连续PageAttention**：扩展了PageAttention，使其能访问非连续的GPU内存块，从而支持块级别的动态缩放（Resize），避免了重新分配大块连续内存的开销。
    *   **层堆叠（Layer Stacking）**：将多个层的KV状态打包进一个物理内存单元（如2MB），以对齐GPU内存分配粒度，减少内部碎片。
*   **增量KV补丁机制（KV Patching）**：
    *   借鉴虚拟机迁移技术，使用“脏位图（Dirty Bitmap）”记录推理过程中新生成的KV条目。
    *   通过独立的NCCL通信组异步推送补丁，并配合“两阶段握手协议”防止推理与迁移之间的死锁。

### 3. 实验设计
*   **实验场景**：异构GPU环境下的模式切换负载（Pattern-shifting workload），模拟从Prefill密集型（长输入短输出）到Decode密集型（短输入长输出）的动态转变。
*   **数据集/模型**：使用了 **Llama 3-70B** 和 **Qwen3-30B**。
*   **Benchmark与对比方法**：
    *   **静态基准**：Prefill-Optimal（针对预填充优化的固定配置）、Decode-Optimal（针对解码优化的固定配置）、Balanced（平衡配置）。
    *   **消融实验**：对比了禁用KV缩放、禁用KV补丁、禁用异步加载等不同变体。
*   **评价指标**：首字延迟（TTFT）、逐字延迟（TPOT）、总吞吐量（Throughput）以及综合性能评分。

### 4. 资源与算力
*   **硬件环境**：构建了一个异构测试平台，包含 **1张 NVIDIA A100 (80GB)** 和 **1张 NVIDIA L40S (48GB)**。
*   **通信**：两台服务器通过 **InfiniBand (100 Gbps)** 连接，使用 NCCL 进行 GPU 间直接通信。
*   **训练/运行**：论文侧重于推理服务阶段的动态调整，未涉及模型训练算力。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了端到端性能测试、KV缩放效果分析、层堆叠碎片化分析、异步加载与补丁机制的消融实验。
*   **充分性评价**：实验设计较为充分。通过在不同请求速率（RPS）下对比多种静态与动态策略，客观展示了系统在重负载下的鲁棒性。特别是针对异构硬件（A100与L40S性能特性差异）的分析，有力支撑了动态重配置的必要性。

### 6. 主要结论与发现
*   **性能提升**：在异构环境下，PipeLive 相比最佳静态配置，综合性能提升了 **33%–36%**。
*   **中断极低**：通过KV补丁机制，将重配置导致的服务停顿时间从秒级降低至 **10毫秒以下**。
*   **显存优化**：动态KV缩放使系统能支持比静态配置高 **2.5倍** 的请求速率而不发生显存溢出。
*   **延迟优化**：相比无补丁的异步迁移，TTFT 和 TPOT 分别优化了高达 **54.7%** 和 **14.7%**。

### 7. 优点
*   **极低侵入性**：实现了真正的“在线”切换，对用户感知的服务中断几乎可以忽略不计。
*   **内存高效**：层堆叠技术巧妙解决了PageAttention在块级别分配时的显存碎片问题。
*   **死锁避免**：设计的两阶段握手协议解决了多NCCL通信组并发时的系统稳定性问题。

### 8. 不足与局限
*   **策略选择缺失**：论文重点在于“如何执行”重配置，但未深入探讨“何时触发”以及“如何自动计算最优目标配置”的决策算法。
*   **硬件覆盖有限**：实验主要基于双GPU（A100+L40S）环境，在更大规模集群（如数十个GPU）上的扩展性及通信开销有待进一步验证。
*   **并行模式单一**：目前仅聚焦于流水线并行（PP）的重配置，尚未涉及张量并行（TP）或数据并行（DP）的联合动态调整。

（完）
