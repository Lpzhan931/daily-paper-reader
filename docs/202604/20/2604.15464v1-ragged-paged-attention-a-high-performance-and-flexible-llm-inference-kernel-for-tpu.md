---
title: "Ragged Paged Attention: A High-Performance and Flexible LLM Inference Kernel for TPU"
title_zh: Ragged Paged Attention：一种面向 TPU 的高性能且灵活的大语言模型推理算子
authors: "Jevin Jiang, Ying Chen, Blake A. Hechtman, Fenghui Zhang, Yarong Mu"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15464v1"
tags: ["query:llm"]
score: 10.0
evidence: 用于TPU推理的高性能Paged Attention内核
tldr: 针对大语言模型在TPU上推理效率低下的问题，本文提出了Ragged Paged Attention (RPA)内核。RPA利用Pallas和Mosaic实现，通过细粒度分块、KV缓存融合计算及分布感知编译策略，解决了TPU在处理动态不规则内存时的性能瓶颈。该内核在Llama 3 8B测试中表现优异，已集成至vLLM和SGLang，为TPU推理提供了高性能的基础设施。
selection_source: fresh_fetch
motivation: 现有的LLM推理系统多以GPU为中心，缺乏针对TPU架构下动态且不规则执行模式的高效注意力机制实现。
method: 采用细粒度分块技术处理不规则内存，通过自定义软件流水线融合KV缓存更新与注意力计算，并实施分布感知的编译策略生成专用内核。
result: "在TPU7x上运行Llama 3 8B时，解码阶段达到86%的内存带宽利用率，预填充阶段达到73%的模型算力利用率。"
conclusion: RPA为TPU上的LLM推理提供了高性能且灵活的生产级内核方案，并为未来AI加速器的内核设计提供了实践见解。
---

## 摘要
大语言模型（LLM）的部署正日益转向谷歌张量处理单元（TPU）等高性价比加速器，同时优先考虑性能和总体拥有成本（TCO）。然而，现有的 LLM 推理算子和推理系统在很大程度上仍以 GPU 为中心，目前尚无成熟的方法能将 LLM 工作负载高效映射到 TPU 架构上，特别是在现代推理服务中常见的动态且不规则（ragged）的执行模式下。在本文中，我们提出了 Ragged Paged Attention (RPA)，这是一种使用 Pallas 和 Mosaic 实现的面向 TPU 的高性能且灵活的注意力算子。RPA 通过三项关键技术应对这些挑战：(1) 细粒度分块（tiling），以实现在不规则内存上的高效动态切片；(2) 自定义软件流水线，将 KV 缓存更新与注意力计算融合；(3) 分布感知编译策略，为解码（decode）、预填充（prefill）和混合工作负载生成专用算子。在 TPU7x 上对 Llama 3 8B 进行的评估显示，RPA 在解码阶段实现了高达 86% 的内存带宽利用率（MBU），在预填充阶段实现了 73% 的模型 FLOPs 利用率（MFU）。RPA 已作为主要 TPU 后端集成到 vLLM 和 SGLang 中，为高效的 TPU 推理提供了生产级基础，并为算子设计提供了实用见解。

## Abstract
Large Language Model (LLM) deployment is increasingly shifting to cost-efficient accelerators like Google's Tensor Processing Units (TPUs), prioritizing both performance and total cost of ownership (TCO). However, existing LLM inference kernels and serving systems remain largely GPU-centric, and there is no well-established approach for efficiently mapping LLM workloads onto TPU architectures--particularly under the dynamic and ragged execution patterns common in modern serving. In this paper, we present Ragged Paged Attention (RPA), a high-performance and flexible attention kernel for TPUs, implemented using Pallas and Mosaic. RPA addresses these challenges through three key techniques: (1) fine-grained tiling to enable efficient dynamic slicing over ragged memory, (2) a custom software pipeline that fuses KV cache updates with attention computation, and (3) a distribution-aware compilation strategy that generates specialized kernels for decode, prefill, and mixed workloads. Evaluated on Llama 3 8B on TPU7x, RPA achieves up to 86% memory bandwidth utilization (MBU) in decode and 73% model FLOPs utilization (MFU) in prefill. Integrated as the primary TPU backend in vLLM and SGLang, RPA provides a production-grade foundation for efficient TPU inference and offers practical insights into kernel design.

---

## 论文详细总结（自动生成）

这是一份关于论文《Ragged Paged Attention: A High-Performance and Flexible LLM Inference Kernel for TPU》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）部署规模的扩大，谷歌的 TPU 因其高性价比（TCO）成为重要选择。然而，现有的高性能注意力算子（如 FlashAttention 系列）主要针对 GPU 架构设计。TPU 面临两大挑战：
*   **静态与动态的矛盾**：TPU 的 XLA 编译器擅长处理静态形状，而现代推理框架（如 vLLM）采用 PagedAttention 和混合批处理（Mixed Batch），产生大量“不规则（Ragged）”且动态变化的内存访问模式。
*   **算子缺失**：缺乏开源且能高效处理 KV 缓存更新与注意力计算融合的 TPU 原生算子。
**核心目标**：开发一种名为 **Ragged Paged Attention (RPA)** 的高性能算子，填补 TPU 在动态 LLM 推理场景下的性能空白。

### 2. 方法论：核心思想与关键技术
RPA 基于 JAX 的底层内核语言 **Pallas** 和编译器 **Mosaic** 实现，核心技术包括：
*   **细粒度分块（Fine-Grained Tiling）**：通过引入“打包维度（Packing Dimension）”，强制 XLA 选择最小的分块大小，从而在不规则内存缓冲区上实现高效的动态切片，避免了昂贵的解包和重新打包开销。
*   **KV 缓存更新融合流水线**：利用异步 DMA 操作，将 KV 缓存的更新（Scatter）与注意力计算（FlashAttention）重叠。通过自定义软件流水线，在计算当前块的同时预取下一块数据并写回新生成的 KV。
*   **KV 合并与转置融合**：将 K 和 V 在内存中交错存储（Merged KV），减少 DMA 传输次数并提高带宽利用率；同时在数据加载路径中通过步进向量加载（Strided Load）融合转置操作。
*   **分布感知编译策略**：根据序列长度分布（解码、预填充、混合）分发专门优化的内核，以减少 TensorCore 在处理不规则长度时的填充浪费。

### 3. 实验设计
*   **模型与场景**：主要使用 **Llama 3 8B** 模型。
*   **测试场景**：
    *   **Decode-only**：模拟自回归生成，关注内存带宽利用率（MBU）。
    *   **Prefill-only**：模拟提示词处理，关注模型算力利用率（MFU）。
    *   **Mixed-batch**：模拟真实推理服务中的混合负载。
*   **Benchmark**：对比了不同头维度（128, 256）和是否开启因果掩码（Causal Mask）的情况。
*   **消融实验**：通过选择性禁用 KV 更新、DMA 操作或注意力计算，量化各组件对延迟的影响。

### 4. 资源与算力
*   **硬件平台**：使用了 **TPU7x (Ironwood)**，每个芯片包含 2 个 TensorCore，拥有 192GB HBM 和 7380 GB/s 的峰值带宽。
*   **软件集成**：该算子已集成到 **vLLM** 和 **SGLang** 的 TPU 后端中。
*   **训练/推理时长**：文中未提及模型训练，重点在于推理算子的微秒级延迟表现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 512 到 32K 的多种序列长度，以及 128 并发的解码测试。
*   **消融实验**：设计了详尽的消融实验（见表 10-12），证明了流水线重叠能有效隐藏 KV 更新的延迟。
*   **客观性**：实验不仅报告了算子本身的性能，还展示了集成到 vLLM 后的端到端吞吐量提升（2x-5x），具有较强的说服力和实际应用参考价值。

### 6. 主要结论与发现
*   **解码性能**：在 HBM 饱和的情况下，RPA 实现了高达 **86% 的内存带宽利用率 (MBU)**。
*   **预填充性能**：在计算密集型场景下，实现了 **73% 的模型算力利用率 (MFU)**。
*   **流水线效率**：通过自定义流水线，KV 缓存更新的开销几乎被计算完全掩盖。
*   **硬件适配**：RPA 具有良好的通用性，仅需调整块大小即可运行在 TPU v4 至 v7 各代硬件上。

### 7. 优点
*   **高性能与灵活性**：成功解决了 TPU 处理不规则内存的难题，性能接近硬件极限。
*   **生产级可用性**：不仅是理论研究，已在 vLLM 等主流开源框架中落地，显著提升了 TPU 的推理吞吐量。
*   **底层控制**：展示了如何利用 Pallas/Mosaic 绕过 XLA 的限制，进行精细的硬件资源调度。

### 8. 不足与局限
*   **预处理开销**：为了适配内核布局，需要在算子执行前进行张量重塑（Reshape），在短序列场景下这部分开销占比较高（2%-8%）。
*   **混合批处理的复杂性**：在高度不规则的混合批次中，由于必须对齐到固定块大小，仍存在不可避免的计算浪费。
*   **硬件覆盖**：目前尚未利用 TPU 的 SparseCore 硬件单元，未来仍有优化空间。
*   **量化支持**：目前仅支持每张量（Per-tensor）量化，更精细的量化方案（如 Per-head）需要进一步修改内核。

（完）
