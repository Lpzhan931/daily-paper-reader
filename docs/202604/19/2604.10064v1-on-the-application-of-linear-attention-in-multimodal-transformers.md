---
title: On The Application of Linear Attention in Multimodal Transformers
title_zh: 论线性注意力在多模态 Transformer 中的应用
authors: "Armin Gerami, Seyedehanita Madani, Ramani Duraiswami"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10064v1"
tags: ["query:llm"]
score: 9.0
evidence: 线性注意力作为二次项注意力的替代方案
tldr: 针对多模态Transformer中Softmax注意力机制计算复杂度随序列长度呈平方增长的瓶颈，本文研究了线性注意力（LA）的应用。通过在LAION-400M数据集上训练不同规模的ViT模型，验证了LA能将复杂度降至线性，同时保持与标准注意力相似的扩展定律和竞争性能，为大规模多模态任务提供了高效可扩展的方案。
selection_source: fresh_fetch
motivation: 旨在解决多模态Transformer在处理大规模数据集时，因平方级注意力复杂度导致的计算效率低下和扩展性受限问题。
method: 在ViT-S/B/L等不同规模的架构中引入线性注意力机制，并利用LAION-400M数据集进行大规模预训练与验证。
result: 实验证明线性注意力在显著降低计算开销的同时，其性能表现遵循与标准Softmax注意力相同的扩展定律。
conclusion: 线性注意力是处理复杂多模态数据的一种高效且稳健的替代方案，具备良好的可扩展性。
---

## 摘要
多模态 Transformer 是最先进视觉-语言模型的核心架构，但其二次方级别的注意力复杂度仍是限制可扩展性的关键障碍。在本研究中，我们探讨了线性注意力（LA）作为多模态框架内高效替代方案的可行性。通过集成线性注意力，我们将相对于序列长度的计算开销从二次方降低到了线性，同时保持了具有竞争力的性能。我们在 LAION-400M 数据集上训练的 ViT-S/16、ViT-B/16 和 ViT-L/16 架构上评估了我们的方法，验证重点在于 ImageNet-21K 的零样本准确率。我们的系统性评估表明，线性注意力不仅显著节省了计算资源，而且遵循与标准 Softmax 注意力相同的缩放法则。这些发现使线性注意力成为处理日益庞大且复杂数据集的下一代多模态 Transformer 的一种稳健且可扩展的解决方案。

## Abstract
Multimodal Transformers serve as the backbone for state-of-the-art vision-language models, yet their quadratic attention complexity remains a critical barrier to scalability. In this work, we investigate the viability of Linear Attention (LA) as a high-efficiency alternative within multimodal frameworks. By integrating LA, we reduce the computational overhead from quadratic to linear relative to sequence length while preserving competitive performance. We evaluate our approach across ViT-S/16, ViT-B/16, and ViT-L/16 architectures trained on the LAION-400M dataset, with validation focused on ImageNet-21K zero-shot accuracy. Our systematic evaluation demonstrates that Linear Attention not only yields significant computational savings but also adheres to the same scaling laws as standard softmax attention. These findings position Linear Attention as a robust, scalable solution for next-generation multimodal Transformers tasked with processing increasingly large and complex datasets.

---

## 论文详细总结（自动生成）

这是一份关于论文《On The Application of Linear Attention in Multimodal Transformers》的结构化总结：

### 1. 核心问题与整体含义
多模态 Transformer（如 CLIP、PaLI）在处理视觉和文本跨模态任务时表现卓越，但其核心机制——**标准 Softmax 注意力**具有二次方计算复杂度 $O(N^2)$。随着图像分辨率提高或序列长度增加，计算开销和内存瓶颈变得不可忽视。
本文旨在研究**线性注意力（Linear Attention, LA）**在多模态模型中的可行性，探索其是否能在显著降低计算成本的同时，保持与标准模型相当的性能和可扩展性。

### 2. 方法论
论文提出了一种改进的线性注意力机制，核心思想是将 Softmax 操作替换为可分解的特征映射，从而利用矩阵乘法的结合律将复杂度降至 $O(ND^2)$。

*   **核心公式：** 采用仿射核函数 $f(x) = 1 + x$。
*   **关键改进：**
    *   **归一化处理：** 对 Query ($Q$) 和 Key ($K$) 进行向量归一化（L2 Norm），确保注意力得分 $1 + q_i \cdot k_n$ 始终非负（在 0 到 2 之间）。
    *   **移除分母：** 传统的线性注意力公式包含一个分母项用于权重归一化，但研究发现随着序列增长，分母会导致注意力得分趋于均匀（过度平滑），使模型难以捕捉显著特征。作者提出**移除分母**，直接计算加权和，以保持注意力得分的对比度和模型的表达能力。
*   **算法流程：** 通过先计算 $K^T V$ 再与 $Q$ 相乘，实现线性时间复杂度的推理和训练。

### 3. 实验设计
*   **数据集：** 使用 **LAION-400M**（4亿图文对）进行大规模预训练。
*   **Benchmark：** 验证集采用 **ImageNet-21K**，评估指标为 **Zero-shot（零样本）分类准确率**。
*   **对比方法：** 
    *   标准 Softmax 注意力（使用 OpenCLIP 框架）。
    *   常规线性注意力（带分母的原始公式）。
    *   本文改进的线性注意力。
*   **模型架构：** 涵盖了 ViT-S/16、ViT-B/16 和 ViT-L/16 三种不同规模的视觉 Transformer。

### 4. 资源与算力
*   **训练设备：** 使用了 **4 台 NVIDIA A5500 GPU** 进行模型预训练。
*   **性能测试设备：** 使用 **NVIDIA H200 GPU** 测量推理和反向传播的时间缩放曲线。
*   **训练细节：** 针对不同规模模型设置了不同的全局 Batch Size（S: 64, B: 16, L: 4）。

### 5. 实验数量与充分性
*   **实验组：** 论文针对三种模型规模（22M、86M、304M 参数）分别进行了标准注意力与线性注意力的对比实验。
*   **充分性：** 
    *   包含了**时间缩放实验**（验证 $O(N)$ 复杂度）。
    *   包含了**训练曲线对比**（验证收敛性）。
    *   包含了**扩展定律（Scaling Laws）分析**（验证在大规模参数下的潜力）。
*   **客观性：** 实验在统一的 OpenCLIP 框架下进行，对比基准明确，且通过可视化注意力热图解释了改进的必要性，逻辑较为严密。

### 6. 主要结论与发现
*   **性能对等：** 改进后的线性注意力在最终准确率上能达到与标准 Softmax 注意力相近的水平。
*   **高效缩放：** 线性注意力在序列长度达到 $10^3$ 时优势开始显现，在极长序列（$4 \times 10^6$）下，计算时间比标准注意力低约 3 个数量级。
*   **遵循扩展定律：** 实验证明线性注意力遵循与标准模型相似的幂律缩放法则（$L(N) \approx aN^{-\alpha}$），这意味着通过增加参数量，线性注意力模型的性能提升是可预测且稳定的。
*   **收敛速度：** 虽然最终性能接近，但线性注意力通常需要比标准注意力更多的 Epoch 才能达到相同的准确率。

### 7. 优点
*   **数学简化与优化：** 通过移除分母解决了线性注意力常见的“过度平滑”问题，提升了模型表达力。
*   **极高的扩展性：** 成功将线性注意力应用于多模态大规模预训练，证明了其在处理高分辨率图像或长文本时的巨大潜力。
*   **硬件友好：** 提供了针对 GPU 优化的内核实现参考，具有实际工程价值。

### 8. 不足与局限
*   **收敛效率：** 线性注意力在训练初期的收敛速度慢于 Softmax 注意力，这可能增加训练的时间成本。
*   **对比度损失：** 尽管做了改进，但线性核函数在产生“尖锐”注意力权重（即识别极少数关键 Token）的能力上仍略逊于指数级的 Softmax 核。
*   **任务覆盖：** 验证主要集中在零样本分类上，未广泛测试在下游微调任务（如目标检测、视觉问答）中的表现。

（完）
