---
title: "CLASP: Class-Adaptive Layer Fusion and Dual-Stage Pruning for Multimodal Large Language Models"
title_zh: CLASP：面向多模态大语言模型的类别自适应层融合与双阶段剪枝
authors: "Yunkai Dang, Yizhu Jiang, Yifan Jiang, Qi Fan, Yinghuan Shi, Wenbin Li, Yang Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12767v1"
tags: ["query:llm"]
score: 9.0
evidence: 多模态大语言模型的双阶段剪枝
tldr: 针对多模态大语言模型（MLLM）中视觉Token冗余导致的计算开销问题，本文提出CLASP框架。该框架通过类别自适应的层融合构建特定视觉表示，并采用双阶段剪枝策略，在关注相关性的显著Token与保证覆盖度的补充Token之间动态分配预算。CLASP实现了提示词驱动的特征融合与鲁棒的Token缩减，在多个基准测试和模型架构上均表现优异。
selection_source: fresh_fetch
motivation: 现有的视觉Token缩减方法多采用单层特征和静态剪枝，难以应对多样化指令且在复杂场景下表现脆弱。
method: 提出一种包含类别自适应多层特征融合与双阶段剪枝（显著枢轴Token+冗余感知补充Token）的即插即用框架。
result: 实验证明CLASP在多种剪枝比例和MLLM架构下，性能均一致优于现有主流方法。
conclusion: CLASP通过动态调整视觉表示和Token预算分配，为MLLM提供了一种高效且鲁棒的视觉Token压缩方案。
---

## 摘要
多模态大语言模型 (MLLMs) 由于视觉 Token 序列中存在高度冗余，面临着巨大的计算开销。现有方法通常采用单层 Vision Transformer (ViT) 特征和静态剪枝策略来解决这一问题。然而，这种固定配置在面对多样化的指令时往往表现得较为脆弱。为了克服这些局限性，我们提出了 CLASP，这是一个基于类别自适应层融合和双阶段剪枝的即插即用 Token 缩减框架。具体而言，CLASP 首先通过多层视觉特征融合构建特定类别的视觉表示。随后，它执行双阶段剪枝，在用于保证相关性的注意力显著枢纽 Token (pivot tokens) 和用于保证覆盖范围的冗余感知补充 Token (completion tokens) 之间分配 Token 预算。通过类别自适应剪枝，CLASP 实现了基于提示词条件的特征融合与预算分配，从而能够进行激进且鲁棒的视觉 Token 缩减。广泛的实验表明，CLASP 在各种基准测试、剪枝率和 MLLM 架构中均一致优于现有方法。代码将在 https://github.com/Yunkaidang/CLASP 提供。

## Abstract
Multimodal Large Language Models (MLLMs) suffer from substantial computational overhead due to the high redundancy in visual token sequences. Existing approaches typically address this issue using single-layer Vision Transformer (ViT) features and static pruning strategies. However, such fixed configurations are often brittle under diverse instructions. To overcome these limitations, we propose CLASP, a plug-and-play token reduction framework based on class-adaptive layer fusion and dual-stage pruning. Specifically, CLASP first constructs category-specific visual representations through multi-layer vision feature fusion. It then performs dual-stage pruning, allocating the token budget between attention-salient pivot tokens for relevance and redundancy-aware completion tokens for coverage. Through class-adaptive pruning, CLASP enables prompt-conditioned feature fusion and budget allocation, allowing aggressive yet robust visual token reduction. Extensive experiments demonstrate that CLASP consistently outperforms existing methods across a wide range of benchmarks, pruning ratios, and MLLM architectures. Code will be available at https://github.com/Yunkaidang/CLASP.

---

## 论文详细总结（自动生成）

### 论文总结：CLASP —— 面向多模态大语言模型的类别自适应层融合与双阶段剪枝

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理高分辨率图像时，视觉 Token 序列极长（如 LLaVA-NeXT 可达 2880 个 Token），导致计算开销呈平方级增长，严重限制了推理效率。
*   **现有局限：** 现有的 Token 缩减方法通常依赖单一的 ViT 层特征，且采用固定的剪枝策略（如仅基于注意力或仅基于相似度）。
*   **研究动机：** 作者发现不同的指令对视觉特征的抽象程度需求不同（例如：计数任务需要浅层细节，而场景理解需要深层语义）。因此，静态的特征提取和剪枝策略在多样化任务面前表现脆弱。

#### 2. 方法论：核心思想与技术细节
CLASP 提出了一种**即插即用**的框架，通过指令类别动态调整视觉表示和剪枝逻辑：
*   **提示词到类别的路由（Prompt-to-class Routing）：** 使用轻量级路由器将输入指令映射到预定义的类别（如 OCR、计数、场景理解等）。
*   **类别自适应层融合（Class-Adaptive Layer Fusion）：** 
    *   不再使用单一 ViT 层，而是根据类别权重 $w_{c_n}$ 对多个 ViT 层的特征进行凸组合（Convex Combination）融合。
    *   公式：$\bar{z}_{n,t} = \sum_{l=1}^{L} \alpha_{n,l} z_{n,t}^{(l)}$，其中 $\alpha$ 是通过温度控制的 Softmax 生成的权重。
*   **类别自适应双阶段剪枝（Class-Adaptive Dual-Stage Pruning）：**
    *   **预算分配：** 将总 Token 预算 $R$ 分配为 $K_1$（相关性枢轴）和 $K_2$（覆盖度补充）。分配比例 $a_c$ 随类别动态调整。
    *   **第一阶段（相关性）：** 基于交叉模态注意力分数选择前 $K_1$ 个最相关的 Token。
    *   **第二阶段（覆盖度）：** 在剩余 Token 中，通过**冗余感知初始化**和**球面 K-Means 聚类**选择 $K_2$ 个 Token，以确保视觉信息的空间覆盖度和多样性。
*   **渐进式剪枝：** 在解码器的第 2、6、15 层执行三阶段剪枝。

#### 3. 实验设计
*   **数据集/场景：**
    *   **图像理解（8个）：** GQA, MMBench, MME, POPE, ScienceQA, VQAv2, TextVQA, MMVet。
    *   **视频理解（3个）：** TGIF, MSVD, MSRVTT。
*   **基准模型（Backbones）：** LLaVA-1.5 (7B/13B), LLaVA-NeXT (7B/13B), Video-LLaVA, Qwen2.5-VL-7B, InternVL2-26B。
*   **对比方法：** 包括 ToMe, FastV, SparseVLM, DART, VisionZip, HiRED, PyramidDrop 等十余种 SOTA 方法。

#### 4. 资源与算力
*   **硬件：** 实验主要在 **NVIDIA A100 (80GB)** GPU 上运行。
*   **开销：** 
    *   **离线校准：** 层权重校准约 1.5 小时，剪枝比例校准约 1.0 小时（单卡）。
    *   **在线推理：** 意图分类器延迟 < 2ms，特征融合 < 0.5ms，K-Means 聚类约 17-27ms。
    *   **显存：** 推理时显存占用约 17.6GB（LLaVA-1.5-7B 配置下）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了从 7B 到 26B 不同规模的模型，测试了从 66.7% 到 94.4% 的多种剪枝比例。
*   **消融实验：** 详细分析了层融合、预算分配比例、聚类迭代次数、路由器模型选择以及校准集大小对性能的影响。
*   **充分性评价：** 实验设计非常充分，不仅在图像任务上验证，还扩展到了视频领域，并对比了最新的 ICLR 2026 投稿级别的竞争对手（如 Nuwa），体现了极高的客观性和前瞻性。

#### 6. 主要结论与发现
*   **高效性：** 在缩减 88.9% 的视觉 Token 时，CLASP 仍能保留原始模型约 94.7% 的性能。
*   **自适应价值：** 实验证实了“不同任务需要不同层特征”的假设，例如文本识别任务在融合特定层后准确率大幅提升。
*   **相关性与覆盖度的平衡：** 纯注意力剪枝容易导致 Token 聚集在局部，而 CLASP 的双阶段策略能有效保留背景上下文。
*   **视频增益：** 在视频任务中，CLASP 甚至超过了全 Token 的基准线，表明其具有去噪效果。

#### 7. 优点
*   **即插即用：** 无需对 MLLM 进行任何重新训练或微调。
*   **意图感知：** 首次系统性地将指令类别与视觉特征提取、剪枝策略动态关联。
*   **鲁棒性强：** 在极端低 Token 预算（如仅保留 32 或 64 个 Token）下，性能衰减远慢于同类方法。
*   **理论支撑：** 提供了关于层融合凸性、Lipschitz 连续性以及聚类收敛性的数学证明。

#### 8. 不足与局限
*   **分类依赖：** 路由器的效果依赖于预定义的 9 类意图划分，对于边界模糊或全新的指令类别，可能退化到“默认”策略。
*   **校准需求：** 虽然不需要训练，但仍需少量的离线校准数据（约 9k 样本）来确定各任务的最优超参数。
*   **硬件加速限制：** 尽管减少了 FLOPs，但 K-Means 等操作在某些硬件架构上可能不如纯矩阵运算易于优化。

（完）
