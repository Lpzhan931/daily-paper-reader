---
title: "MIPT-SSM: Scaling Language Models with $O(1)$ Inference Cache via Phase Transitions"
title_zh: MIPT-SSM：通过相变实现具有 $O(1)$ 推理缓存的可扩展语言模型
authors: Yasong Fan
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07716v1"
tags: ["query:llm"]
score: 8.0
evidence: 通过相变实现O(1)推理缓存
tldr: MIPT-SSM 是一种基于测量诱导相变（MIPT）理论的新型序列架构。它通过学习测量率 $p_t$ 在“波相”（分布式传播）和“粒子相”（局部存储）之间切换，解决了线性算子在信息传播与存储上的矛盾。该模型在保持与 Transformer 相当的语言建模性能的同时，实现了推理缓存从 $O(N)$ 到 $O(1)$ 的跨越，显著降低了长序列下的内存开销，并展现出极高的稀疏存储效率。
selection_source: fresh_fetch
motivation: 针对 Transformer 推理时 KV 缓存随序列长度线性增长的内存瓶颈，以及线性算子无法同时实现高效全局传播与精确局部存储的局限性。
method: 借鉴物理学中的测量诱导相变理论，通过可学习的测量率 $p_t$ 动态调节计算模式，在分布式干涉与状态坍缩之间取得平衡。
result: "在 AG News 任务上准确率大幅领先 Transformer，在长序列下实现 42.8 倍内存缩减，并能以 99.8% 的稀疏率自主学习存储关键信息。"
conclusion: 该研究证明了利用物理相变机制可构建具备 $O(1)$ 推理缓存的高效语言模型，为解决长文本处理的扩展性问题提供了新方案。
---

## 摘要
我们提出了 MIPT-SSM，这是一种基于测量诱导相变（MIPT）物理原理构建的神经序列架构。其核心思想是一个可学习的测量率 $p_{t}\in(0,1)$，它在两种状态之间路由计算：波动相（$p_{t}\rightarrow0$），其中信息以分布式复数相位干涉的形式传播；以及粒子相（$p_{t}\rightarrow1$），其中状态坍缩到当前 token 上，从而实现精确的局部存储。这两种状态在单个线性算子中被证明是不相容的——这是序列建模中为数不多的“不可行定理”（no-go theorems）之一——而 $p_{t}$ 是我们绕过这一限制的方法。预测该模型在临界序列长度 $N^{*}\approx1024$ 处表现出相变，此时信息密度比 $N/D$ 跨越单位值，这与我们的内存缩放观察结果一致。在 AG News（四分类任务）上，MIPT 达到了 0.905 的准确率，而 Transformer 为 0.736（提升了 16.6%），且在 3 个随机种子下表现稳定。在 $N=8192$ 时，MIPT 需要 810 MB 内存，而 Transformer 需要 34,651 MB，内存减少了 42.8 倍。在精确召回（“大海捞针”）测试中，我们的因果稀疏 KV 缓存达到了 0.968 的准确率。值得注意的是，在无限缓存容量下，$p_{t}$ 门控能够自主学习仅存储单个关键 token（平均使用 $1.0/512$ 个插槽），过滤掉所有噪声并实现 99.8% 的稀疏率。在语言建模（WikiText-103，3100 万参数）方面，使用 $K=64$ 缓存的 MIPT-LM 达到了 92.1 的困惑度（PPL），而 Transformer 为 90.5（差距为 1.8%），同时推理 KV 缓存从 $O(N)$ 缩小到 $O(64)$。

## Abstract
We present MIPT-SSM, a neural sequence architecture built on the physics of Measurement-Induced Phase Transitions (MIPT). The central idea is a learned measurement rate $p_{t}\in(0,1)$ that routes computation between two regimes: wave phase $(p_{t}\rightarrow0)$, where information propagates as distributed complex-phase interference; and particle phase $(p_{t}\rightarrow1)$ where the state collapses onto the current token, enabling precise local storage. These two regimes are provably incompatible in a single linear operator one of the few "no-go theorems" in sequence modeling and $p_{t}$ is our way around it. The model is predicted to exhibit a phase transition at critical sequence length $N^{*}\approx1024$, where the information density ratio $N/D$ crosses unity, consistent with our memory scaling observations. On AG News (four-class classification), MIPT achieves 0.905 accuracy versus Transformer's 0.736 (+16.6%), stable across 3 seeds. At $N=8192$ MIPT requires 810 MB versus Transformer's 34,651 MB a 42.8x memory reduction. On exact-recall ("needle-in-a-haystack"), our causal sparse KV cache achieves 0.968 accuracy. Remarkably, under unbounded cache capacity, the $p_{t}$ gate autonomously learns to store only the single critical token (averaging $1.0/512$ slots used), filtering out all noise and achieving a 99.8% sparsity rate. On language modeling (WikiText-103, 31M parameters), MIPT-LM with $K=64$ cache reaches PPL 92.1 versus Transformer's 90.5 (gap: 1.8%) while inference KV cache shrinks from $O(N)$ to $O(64)$.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MIPT-SSM** 的新型序列建模架构，旨在通过物理学中的“测量诱导相变”（Measurement-Induced Phase Transitions）理论，解决 Transformer 内存开销大和状态空间模型（SSM）长程记忆缺失的矛盾。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心矛盾（波粒二象性僵局）：** 论文指出序列建模中存在一个“不可行定理”：单个线性算子无法同时实现“全局信息保留”（波状，范数保持）和“选择性局部存储/遗忘”（粒子状，耗散）。
*   **现有模型局限：** Transformer 通过存储所有 KV 对实现精确检索，但内存开销随序列长度 $N$ 呈 $O(N)$ 增长；SSM（如 Mamba）虽然推理是 $O(1)$，但其指数衰减特性导致长程信息丢失严重。
*   **研究目标：** 借鉴量子电路理论，通过引入可学习的“测量率”来动态切换计算模式，从而在保持 $O(1)$ 推理缓存的同时，实现高效的长程依赖建模。

### 2. 方法论
*   **核心思想：** 引入测量率 $p_t \in (0, 1)$。当 $p_t \to 0$ 时，系统处于“波动相”，信息以复数相位干涉形式传播；当 $p_t \to 1$ 时，系统处于“粒子相”，状态向当前 token 坍缩，实现精确存储。
*   **关键公式：** 状态更新方程为 $h_t = (1 - p_t) \cdot e^{i\theta_t} \cdot h_{t-1} + p_t \cdot (W x_t)$。其中 $p_t$（测量率）和 $\theta_t$（相位旋转角）均由输入数据动态决定。
*   **并行训练：** 利用关联线性递归特性，通过 Hillis-Steele 并行前缀扫描实现 $O(N \log N)$ 的训练复杂度。
*   **因果稀疏 KV 缓存：** 基于 $p_t$ 的大小自动筛选关键 token 进入缓存。只有当 $p_t$ 超过阈值 $\tau$ 时，才将该 token 的 KV 对存入固定容量 $K$ 的缓存中。
*   **相变理论：** 预测在临界长度 $N^* \approx 1024$ 处发生相变，此时信息密度比跨越单位值，模型从体积律（信息均匀分布）转向面积律（信息集中）。

### 3. 实验设计
*   **数据集与场景：**
    *   **文本分类：** AG News（短文本四分类）。
    *   **长文档理解：** 关键信息位于文档后三分之一的分类任务。
    *   **精确召回（大海捞针）：** 在大量随机噪声中检索特定 token。
    *   **语言建模：** WikiText-103（31M 参数规模）。
*   **Benchmark 与对比方法：** 主要对比标准 Transformer（GPT 架构）以及带有 KV 缓存压缩策略（如 H2O, StreamingLLM）的模型。

### 4. 资源与算力
*   **硬件环境：** 使用单张 **NVIDIA RTX 5880 (48 GB)** 显卡。
*   **软件框架：** PyTorch 2.1.2。
*   **训练细节：** 论文未明确给出总训练时长，但提到分类任务使用了 32 的 Batch Size，语言建模使用了 10M token 的训练数据。

### 5. 实验数量与充分性
*   **实验覆盖面：** 涵盖了分类、长文本、检索和自回归生成四大维度，实验设计较为全面。
*   **消融与验证：** 针对 $p_t$ 阈值、缓存容量 $K$、序列长度 $N$ 进行了详细的敏感度分析和内存缩放实验。
*   **客观性：** 实验使用了多个随机种子（3 seeds）并报告了标准差，且诚实地指出了在纯语言建模任务上与 Transformer 的性能差距（1.8% - 12.9%）。

### 6. 主要结论与发现
*   **性能提升：** 在 AG News 分类上，MIPT 准确率（0.905）远超 Transformer（0.736），提升达 16.6%。
*   **内存优势：** 在 $N=8192$ 时，MIPT 内存占用仅为 810 MB，而 Transformer 为 34,651 MB，实现 **42.8 倍** 的缩减。
*   **极高稀疏度：** 在“大海捞针”任务中，$p_t$ 门控能以 99.8% 的稀疏率自主识别并存储唯一的关键 token。
*   **推理效率：** 在语言建模中，通过仅 64 个缓存插槽，MIPT 就能将与 Transformer 的 PPL 差距缩小到 1.8%，同时保持 $O(1)$ 的推理缓存复杂度。

### 7. 优点
*   **理论创新：** 首次将量子物理中的测量诱导相变（MIPT）引入序列建模，为解决“波粒矛盾”提供了严谨的数学框架。
*   **内存效率：** 真正实现了推理阶段 $O(1)$ 的 KV 缓存，极大地缓解了长文本生成的显存压力。
*   **自适应性：** $p_t$ 能够自动识别名词、实体等高信息量 token，具有物理意义上的可解释性。

### 8. 不足与局限
*   **模型规模：** 实验模型参数量较小（14M - 31M），尚未在 1B 以上的大规模参数上验证其扩展性。
*   **工程优化：** 目前使用 Python 实现的并行扫描比高度优化的 Transformer CUDA 内核慢 3-5 倍，缺乏底层算子优化。
*   **性能差距：** 在不使用缓存的情况下，其语言建模的困惑度（PPL）仍比 Transformer 高出约 12.9%，说明纯相位压缩仍有信息损失。

（完）
