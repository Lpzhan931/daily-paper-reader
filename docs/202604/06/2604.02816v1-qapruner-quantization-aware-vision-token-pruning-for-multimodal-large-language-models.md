---
title: "QAPruner: Quantization-Aware Vision Token Pruning for Multimodal Large Language Models"
title_zh: QAPruner：面向多模态大语言模型的量化感知视觉 Token 剪枝
authors: "Xinhao Wang, Zhonyu Xia, Zhiwei Lin, Zhe Li, Yongtao Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02816v1"
tags: ["query:llm"]
score: 9.0
evidence: 量化感知的视觉Token剪枝
tldr: 针对多模态大语言模型（MLLM）部署成本高的问题，本文揭示了视觉Token剪枝与后训练量化（PTQ）之间的耦合关系，指出盲目剪枝会移除对量化稳定性至关重要的激活离群值。为此，作者提出了QAPruner框架，通过结合量化误差、离群值强度和语义相关性的混合敏感度指标来筛选Token。实验证明，该方法在极低比特（如W4A4）和高剪枝率下显著优于传统方法，实现了高效且准确的MLLM推理。
selection_source: fresh_fetch
motivation: 传统的视觉Token剪枝忽略了量化过程中的数值稳定性，导致在低比特量化环境下剪枝会加剧量化误差。
method: 提出一种量化感知剪枝框架，利用结合了模拟分组量化误差、离群值强度及语义相关性的混合敏感度指标来保留关键Token。
result: "在LLaVA架构上，即使仅保留12.5%的视觉Token，该方法在准确率上仍比基线高出2.24%，甚至超过了未剪枝的量化模型。"
conclusion: 本文首次实现了视觉Token剪枝与PTQ的协同优化，为资源受限环境下的低比特MLLM推理提供了有效方案。
---

## 摘要
多模态大语言模型（MLLMs）展现出强大的推理能力，但其高昂的计算和内存成本阻碍了其在资源受限环境下的部署。虽然训练后量化（PTQ）和视觉 Token 剪枝是标准的压缩技术，但它们通常被视为独立的优化手段。在本文中，我们证明了这两种技术是强耦合的：在经过 PTQ 优化的 MLLM 上简单地应用基于语义的 Token 剪枝，可能会丢弃对数值稳定性至关重要的激活离群值（outliers），从而加剧低比特机制（例如 W4A4）下的量化误差。为了解决这一问题，我们提出了一种量化感知的视觉 Token 剪枝框架。我们的方法引入了一种轻量级的混合敏感度指标，该指标结合了模拟的分组量化误差与离群值强度。通过将该指标与标准的语义相关性评分相结合，该方法能够保留既具有语义信息量又对量化具有鲁棒性的 Token。在标准 LLaVA 架构上的实验表明，我们的方法始终优于简单的集成基准。在仅保留 12.5% 视觉 Token 的激进剪枝比例下，我们的框架比基准方法提高了 2.24% 的准确率，甚至超过了不进行剪枝的稠密量化结果。据我们所知，这是首个明确协同优化视觉 Token 剪枝和 PTQ 以实现精确低比特 MLLM 推理的方法。

## Abstract
Multimodal Large Language Models (MLLMs) have shown strong reasoning ability, but their high computational and memory costs hinder deployment in resource-constrained settings. While Post-Training Quantization (PTQ) and vision token pruning are standard compression techniques, they are usually treated as independent optimizations. In this paper, we show that these two techniques are strongly coupled: naively applying semantic-based token pruning to PTQ-optimized MLLMs can discard activation outliers that are important for numerical stability and thus worsen quantization errors in low-bit regimes (\textit{e.g.}, W4A4). To address this issue, we propose a quantization-aware vision token pruning framework. Our method introduces a lightweight hybrid sensitivity metric that combines simulated group-wise quantization error with outlier intensity. By combining this metric with standard semantic relevance scores, the method retains tokens that are both semantically informative and robust to quantization. Experiments on standard LLaVA architectures show that our method consistently outperforms naive integration baselines. At an aggressive pruning ratio that retains only 12.5\% of visual tokens, our framework improves accuracy by 2.24\% over the baseline and even surpasses dense quantization without pruning. To the best of our knowledge, this is the first method that explicitly co-optimizes vision token pruning and PTQ for accurate low-bit MLLM inference.

---

## 论文详细总结（自动生成）

### QAPruner：面向多模态大语言模型的量化感知视觉 Token 剪枝论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型（MLLM）在部署时面临巨大的计算和内存开销。虽然**后训练量化（PTQ）**和**视觉 Token 剪枝**是两种常用的压缩手段，但以往研究通常将二者视为独立优化。
*   **研究动机**：论文发现这两种技术存在强耦合关系。传统的剪枝方法仅依据“语义相关性”筛选 Token，这可能会误删包含**激活离群值（Outliers）**的 Token。虽然这些 Token 语义贡献看似较小，但对维持低比特量化（如 W4A4）的数值稳定性至关重要。删除它们会导致量化误差剧增，严重损害模型精度。

#### 2. 方法论
论文提出了一种**量化感知视觉 Token 剪枝框架（QAPruner）**，核心思想是在筛选 Token 时同时考虑语义重要性和量化敏感度。
*   **混合量化敏感度指标 ($S_Q$)**：由两个正交维度组成：
    1.  **分组量化模拟误差 ($E_i$)**：通过模拟低比特（如 INT4）分组量化过程，计算原始 Token 与量化后 Token 之间的 L2 重建误差，捕捉局部细节损失。
    2.  **全局离群值强度 ($R_i$)**：计算 Token 在所有通道上的动态范围（最大值减最小值），识别包含极端离群值的 Token，以保护模型的涌现特性。
*   **评分融合机制**：将量化敏感度评分 $S_Q$ 与传统的语义相关性评分 $S_P$（如基于注意力或多样性的评分）进行加权融合：
    $$S_{Final} = \alpha S_P + (1 - \alpha) S_Q$$
    通过超参数 $\alpha$ 平衡语义保留与数值稳定性，最后选取 Top-K 个 Token 进入 LLM 后端。

#### 3. 实验设计
*   **模型架构**：选取了代表性的 LLaVA 系列模型，包括 LLaVA-1.3 (7B, 13B) 和 LLaVA-1.5 (7B)。
*   **数据集**：主要在 **ScienceQA** 基准测试集上进行评估，涵盖自然科学、社会科学和语言科学等多个维度的多模态推理任务。
*   **量化配置**：采用 **W4A4**（4比特权重、4比特激活）作为默认的极低比特挑战场景，使用 QVLM 作为基础 PTQ 框架。
*   **对比基准（Baseline）**：
    *   FP（全精度未剪枝模型）。
    *   Dense W4A4（量化但未剪枝）。
    *   Naive Pruning（直接将 CDPruner 等语义剪枝方法应用于量化模型）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **性质分析**：由于该方法属于**训练后（Post-Training）**优化且无需微调（Training-free），其计算开销主要集中在推理侧的 Token 筛选计算上。相比于模型本身的推理，这种轻量级指标计算的额外开销极低。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 3 种不同规模和版本的 LLaVA 模型进行了测试。
    *   设置了多种剪枝比例（如保留 128 Token 和 32 Token，对应约 50% 到 12.5% 的保留率）。
    *   **消融实验**：对比了 9 种不同的量化敏感度计算方法（如 L-norm、方差、AbsMax 等），验证了混合指标的有效性。
*   **充分性评价**：实验设计较为充分，涵盖了不同参数规模的模型和多种剪枝强度，对比实验直接针对“剪枝+量化”的痛点，逻辑严密且具有说服力。

#### 6. 主要结论与发现
*   **协同效应显著**：在 W4A4 极低比特下，QAPruner 显著优于传统的语义剪枝。在仅保留 12.5% Token 时，准确率比基线提升了 **2.24%**。
*   **超越稠密基准**：在多个设置下，QAPruner 筛选出的少量 Token 甚至比保留全部 Token 的量化模型（Dense W4A4）表现更好，说明该方法能有效剔除对量化有害的噪声 Token。
*   **离群值保护至关重要**：实验证明，保护具有极端激活值的 Token 对维持量化后 MLLM 的逻辑推理能力具有决定性作用。

#### 7. 优点
*   **首创性**：首次明确揭示并解决了 MLLM 中视觉 Token 剪枝与 PTQ 之间的耦合冲突。
*   **无需训练**：作为一种 Training-free 方法，部署成本极低，易于集成到现有的推理引擎中。
*   **鲁棒性强**：在极高剪枝率下依然能保持甚至提升量化模型的精度。

#### 8. 不足与局限
*   **任务覆盖面**：实验主要集中在 ScienceQA 这一类视觉问答任务上，对于长视频理解、多图对话等更复杂的场景尚未进行广泛验证。
*   **模型多样性**：虽然测试了 LLaVA 系列，但未涵盖基于其他架构（如 Qwen-VL 或 InternVL）的 MLLM，不同模型的激活分布差异可能会影响指标的普适性。
*   **超参数依赖**：融合因子 $\alpha$ 可能需要针对不同的量化比特数和模型架构进行手动微调。

（完）
