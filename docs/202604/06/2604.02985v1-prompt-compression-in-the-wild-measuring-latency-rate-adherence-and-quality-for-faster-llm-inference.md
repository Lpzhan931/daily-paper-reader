---
title: "Prompt Compression in the Wild: Measuring Latency, Rate Adherence, and Quality for Faster LLM Inference"
title_zh: 实际应用中的提示词压缩：衡量延迟、压缩率遵循度及质量以实现更快的大语言模型推理
authors: "Cornelius Kummer, Lena Jurkschat, Michael Färber, Sahar Vahdati"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02985v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过提示词压缩实现更快的大语言模型推理并降低延迟
tldr: "针对RAG系统中长上下文导致的LLM推理延迟问题，本文对提示词压缩技术进行了首次大规模系统性研究。通过在多种开源模型和GPU硬件上进行3万次查询测试，评估了压缩开销与解码加速之间的权衡。研究发现，在硬件与压缩比匹配时，LLMLingua可实现高达18%的端到端加速且质量稳定，并能显著降低显存占用。此外，研究还发布了性能分析工具，指导实际应用中压缩技术的选择。"
selection_source: fresh_fetch
motivation: RAG系统中的长提示词导致推理延迟成为瓶颈，而提示词压缩的实际端到端加速效果与质量权衡尚缺乏系统性评估。
method: 在三种GPU级别和多个开源LLM上，通过3万次查询实验，分离并测量了压缩开销、解码延迟、输出质量及显存占用。
result: "LLMLingua在特定条件下可提升18%的端到端速度且不损质量，但在不匹配的硬件或参数下，压缩耗时会抵消加速收益。"
conclusion: 提示词压缩的有效性高度依赖于硬件与任务配置，开发者应利用本文提供的分析器来确定实现实际收益的平衡点。
---

## 摘要
随着语言模型在信息检索（IR）——特别是检索增强生成（RAG）系统中的广泛应用，底层大语言模型（LLM）的延迟成为了关键瓶颈，因为检索段落的长上下文会导致提示词过长，从而增加计算量。提示词压缩旨在减小输入提示词的大小，同时力求保持下游任务的性能，已成为加速大语言模型推理的一种高性价比且低延迟的方法。然而，其有效性取决于生成过程中额外的预处理时间是否能被更快的解码速度所抵消。我们展示了针对这一权衡的首次系统性、大规模研究，涵盖了多个开源大语言模型和三类 GPU 级别，进行了数千次运行和 30,000 次查询。我们的评估将压缩开销与解码延迟分开，同时追踪输出质量和内存使用情况。研究发现，当提示词长度、压缩率和硬件能力良好匹配时，LLMLingua 可实现高达 18% 的端到端加速，且在摘要、代码生成和问答任务中的响应质量在统计学上保持不变。然而，在此运行窗口之外，压缩步骤会占据主导地位并抵消收益。我们还表明，有效的压缩可以显著减少内存使用，足以将工作负载从数据中心级 GPU 迁移到消费级显卡，而延迟仅增加 0.3 秒。我们的开源分析器可以预测每种模型-硬件配置的延迟盈亏平衡点，为提示词压缩何时能带来实际收益提供实践指导。

## Abstract
With the wide adoption of language models for IR -- and specifically RAG systems -- the latency of the underlying LLM becomes a crucial bottleneck, since the long contexts of retrieved passages lead large prompts and therefore, compute increase. Prompt compression, which reduces the size of input prompts while aiming to preserve performance on downstream tasks, has established itself as a cost-effective and low-latency method for accelerating inference in large language models. However, its usefulness depends on whether the additional preprocessing time during generation is offset by faster decoding. We present the first systematic, large-scale study of this trade-off, with thousands of runs and 30,000 queries across several open-source LLMs and three GPU classes. Our evaluation separates compression overhead from decoding latency while tracking output quality and memory usage. LLMLingua achieves up to 18% end-to-end speed-ups, when prompt length, compression ratio, and hardware capacity are well matched, with response quality remaining statistically unchanged across summarization, code generation, and question answering tasks. Outside this operating window, however, the compression step dominates and cancels out the gains. We also show that effective compression can reduce memory usage enough to offload workloads from data center GPUs to commodity cards, with only a 0.3s increase in latency. Our open-source profiler predicts the latency break-even point for each model-hardware setup, providing practical guidance on when prompt compression delivers real-world benefits.

---

## 论文详细总结（自动生成）

以下是对论文《Prompt Compression in the Wild: Measuring Latency, Rate Adherence, and Quality for Faster LLM Inference》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在检索增强生成（RAG）和信息检索（IR）系统中，长上下文提示词导致大语言模型（LLM）推理延迟显著增加。虽然“提示词压缩”技术被提出用于减少 Token 数量以加速推理，但现有的研究大多关注输出质量，忽略了**压缩过程本身的计算开销**。
*   **核心问题**：提示词压缩带来的解码加速，是否真的能抵消其在预处理阶段引入的额外延迟？在不同的硬件、模型和软件框架下，这种权衡的“盈亏平衡点”在哪里？

### 2. 论文提出的方法论
*   **核心思想**：通过系统性实验，将推理过程拆解为“压缩开销”和“目标模型解码延迟”两部分，量化评估 LLMLingua 系列压缩算法的实际效用。
*   **关键技术细节**：
    *   **LLMLingua**：基于困惑度（Perplexity）的剪枝。使用小型解码器模型（如 GPT-2 Small 或 Llama-2 7B）计算 Token 的自信息量，移除低信息量的 Token。
    *   **LLMLingua-2**：基于 Token 分类的压缩。在编码器模型（如 XLM-RoBERTa 或 BERT）后添加线性分类层，通过跨熵损失微调，直接预测 Token 是否应被保留。
    *   **指标定义**：定义了压缩率（$\tau = \text{目标长度} / \text{原始长度}$）和压缩比（$1/\tau$），并重点关注首字生成时间（TTFT）。

### 3. 实验设计
*   **数据集与场景**：使用 **LongBench** 基准测试，涵盖代码生成、单/多文档问答（QA）、摘要生成、合成任务（如段落检索）和少样本学习（Few-shot）。
*   **目标模型（Target LLMs）**：
    *   开源模型：Mistral 7B, Llama 3.1 70B。
    *   商业 API：GPT-3.5 Turbo, GPT-4o mini。
*   **对比方法**：无压缩基线 vs. 不同压缩率（1.5x, 2x, 3x, 5x）下的 LLMLingua 和 LLMLingua-2。
*   **推理框架**：对比了 Hugging Face Transformers、vLLM 和 TGI（Text Generation Inference）。

### 4. 资源与算力
*   **硬件配置**：
    1.  **数据中心级**：Nvidia A100 (40 GB VRAM)。
    2.  **消费级显卡**：Nvidia GTX 1080 Ti (11 GB VRAM)。
    3.  **移动端 SoC**：Apple M1 Pro (16 GB 统一内存)。
*   **算力细节**：论文未明确给出总训练时长（因为主要评估推理），但详细记录了 30,000 次端到端推理任务的耗时。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 **30,000 次查询**和数千次实验运行。
*   **覆盖范围**：提示词长度从 100 跨越到 50,000 Token，涵盖了从 7B 到 70B 的模型参数规模。
*   **客观性与公平性**：实验在相同硬件上多次重复（12 次运行，取后 8 次均值以排除预热影响），并对比了不同推理框架（如 vLLM 的优化效果），评估维度全面（延迟、质量、显存、压缩率遵循度），具有很高的客观性。

### 6. 论文的主要结论与发现
*   **延迟权衡**：LLMLingua-2 比 LLMLingua 快约 7 倍。在未优化的框架（Transformers）中，压缩可实现 3-4 倍加速；但在高度优化的框架（vLLM）或商业 API 中，压缩开销往往抵消了收益，甚至导致总延迟增加。
*   **端到端加速**：仅在提示词 > 5,000 Token 且压缩比匹配时，才能获得最高约 **18%** 的实际端到端加速。
*   **显存优化**：压缩可减少高达 **75%** 的 GPU 显存占用，使得原本需要在 A100 上运行的长上下文任务能够迁移到 GTX 1080 Ti 等消费级显卡上。
*   **质量影响**：摘要和 QA 任务对压缩具有鲁棒性；但代码生成、合成任务（依赖结构信息）和少样本分类任务在压缩后质量大幅下降。
*   **压缩率遵循度**：LLMLingua-2 能严格遵守设定的压缩率，而 LLMLingua 在长文本下偏差较大。

### 7. 优点
*   **实战导向**：不同于以往只看指标的研究，本文深入探讨了工程实践中的硬件和框架限制。
*   **工具化贡献**：发布了开源分析器（Profiler），帮助开发者预测特定硬件/模型组合下的延迟盈亏平衡点。
*   **多维度评估**：不仅看速度，还揭示了压缩对显存占用的巨大优化潜力，这对于本地化部署具有重要意义。

### 8. 不足与局限
*   **方法局限**：主要研究了“硬剪枝”（Token Pruning）方法，未涵盖“软压缩”（Soft Compression，如将上下文编码为向量）或摘要式压缩。
*   **API 黑盒**：对于商业 API（如 GPT-4o），由于无法控制其内部缓存和优化机制，实验观察到的延迟波动具有随机性，难以得出确定性结论。
*   **任务偏差**：实验发现某些任务（如代码生成）在压缩后质量下降严重，说明该技术在通用性上仍有局限，不适合所有 RAG 场景。

（完）
