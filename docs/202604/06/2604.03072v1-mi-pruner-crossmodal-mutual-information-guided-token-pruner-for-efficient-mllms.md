---
title: "MI-Pruner: Crossmodal Mutual Information-guided Token Pruner for Efficient MLLMs"
title_zh: MI-Pruner：面向高效多模态大语言模型的跨模态互信息引导 Token 剪枝器
authors: "Jiameng Li, Aleksei Tiulpin, Matthew B. Blaschko"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03072v1"
tags: ["query:llm"]
score: 9.0
evidence: 跨模态互信息引导的Token剪枝
tldr: 针对多模态大模型（MLLM）中视觉信息冗余导致的推理效率问题，本文提出了 MI-Pruner 剪枝方法。该方法摒弃了传统的注意力权重依赖，通过在模态交互前直接计算视觉与文本特征间的互信息（MI）来衡量 Token 的重要性。MI-Pruner 具有非侵入性、简单高效的特点，在不修改模型架构的情况下，实现了优于现有注意力剪枝方案的性能，并显著降低了推理延迟。
selection_source: fresh_fetch
motivation: 现有的视觉剪枝方法通常依赖于特定的注意力机制信号，难以直接且高效地衡量视觉与文本之间的跨模态依赖性。
method: 在视觉与文本特征交互之前，直接计算两者间的互信息，并据此筛选高依赖性的视觉 Token 进行保留。
result: 实验证明 MI-Pruner 在多个基准测试中优于基于注意力的剪枝方法，且带来的额外计算开销极小。
conclusion: MI-Pruner 证明了基于互信息的特征筛选是提升多模态大模型推理效率的一种简单且极具竞争力的方案。
---

## 摘要
对于多模态大语言模型 (MLLMs)，视觉信息相对于文本而言较为稀疏。因此，针对高效推理的视觉剪枝研究应运而生。目前的方法通常基于视觉编码器或 LLM 解码器中的注意力分数来衡量 Token 的重要性，进而选择注意力分数较高的视觉 Token 并剪除其余部分。在本文中，我们采用了一种不同且更为精准的方法。我们不依赖于特定机制的信号，而是在视觉和文本特征交互之前，直接计算两者之间的互信息 (MI)。这使我们能够在特征层面显式地衡量跨模态依赖关系。我们的 MI-Pruner 简单、高效且具有非侵入性，无需访问内部注意力图或修改架构。实验结果表明，我们的方法在极低延迟的情况下优于以往基于注意力的剪枝方法。

## Abstract
For multimodal large language models (MLLMs), visual information is relatively sparse compared with text. As a result, research on visual pruning emerges for efficient inference. Current approaches typically measure token importance based on the attention scores in the visual encoder or in the LLM decoder, then select visual tokens with high attention scores while pruning others. In this paper, we pursue a different and more surgical approach. Instead of relying on mechanism-specific signals, we directly compute Mutual Information (MI) between visual and textual features themselves, prior to their interaction. This allows us to explicitly measure crossmodal dependency at the feature levels. Our MI-Pruner is simple, efficient and non-intrusive, requiring no access to internal attention maps or architectural modifications. Experimental results demonstrate that our approach outperforms previous attention-based pruning methods with minimal latency.

---

## 论文详细总结（自动生成）

以下是对论文《MI-Pruner: Crossmodal Mutual Information-guided Token Pruner for Efficient MLLMs》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理高分辨率图像时，视觉 Token 数量巨大，导致自注意力机制的计算开销呈平方级增长。现有的视觉 Token 剪枝方法主要依赖**注意力分数**（来自视觉编码器或 LLM 解码器）来衡量重要性。然而，作者指出注意力分数存在局限性：
*   **噪声与偏差**：受位置偏差、注意力汇聚（Attention Sinks）和多头多样性影响，注意力分数并不等同于信息重要性。
*   **实现瓶颈**：提取内部注意力图与 FlashAttention 等高效算子不兼容，且增加内存开销。
*   **动机**：本文提出 **MI-Pruner**，回归信息论本质，在视觉与文本特征交互前的投影空间中，直接计算两者的**互信息（Mutual Information, MI）**，以实现更精准、非侵入式且高效的 Token 剪枝。

### 2. 方法论：核心思想与技术细节
MI-Pruner 的核心思想是将 Token 剪枝建模为**子模函数最大化问题**，通过最大化跨模态互信息来保留与查询最相关的视觉信息。
*   **投影空间操作**：在视觉编码器之后、进入 LLM 之前的投影空间提取视觉特征 $V$ 和文本特征 $T$。
*   **概率分布构建**：
    1.  计算视觉-文本（跨模态）和视觉-视觉（自模态）的余弦相似度矩阵。
    2.  利用 Softmax 函数将相似度转化为条件概率分布 $p(t|v)$ 和 $p(v'|v)$。
    3.  基于全概率公式推导边缘分布。
*   **逐点互信息（PMI）计算**：计算每个视觉 Token 与文本 Token 之间的 PMI，量化语义相关性；同时计算视觉 Token 间的 PMI 以衡量冗余度。
*   **贪婪搜索策略**：定义评分函数 $S_i = \lambda S_{cross} - (1-\lambda) S_{self}$，其中 $\lambda$ 用于平衡“查询相关性”与“内部冗余度”。通过贪婪搜索（复杂度为 $O(N_V)$）筛选出最优 Token 子集。
*   **MI-Attention 变体**：还提出了一种结合视觉编码器注意力和互信息的统一方法，增强灵活性。

### 3. 实验设计
*   **数据集**：涵盖了广泛的基准测试，包括：
    *   **开放式 QA**：GQA, MMVet。
    *   **闭式/选择题**：SQA, TextVQA。
    *   **幻觉评估**：MME P, POPE（包含随机、流行、对抗三种设置）。
    *   **视频 QA**：TGIF-QA, MSVD-QA, MSRVTT-QA。
*   **模型（Backbones）**：LLaVA-1.5 (7B/13B), Qwen2VL, Qwen2.5VL, Qwen3VL (2B/8B/30B), Video-LLaVA。
*   **对比方法**：
    *   **基于注意力**：FastV, SparseVLM, VisionZip, VisPruner。
    *   **非注意力/多样性**：DART, CDPruner, Random, Similarity。

### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA A100 GPU** 上完成。
*   **训练/推理时长**：论文未提及训练时长（因为 MI-Pruner 是即插即用的推理加速方法，无需重新训练）。在推理延迟方面，MI-Pruner 在保留 64 个 Token 时，延迟仅为约 77-85ms，优于大多数对比方法。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个模型系列、多个参数规模（2B 到 30B）、以及超过 10 个视觉/视频数据集上进行了测试。
*   **消融实验**：针对聚合策略（Max vs. Avg）、平衡因子 $\lambda$、温度系数 $\tau$ 以及归一化方式进行了详细讨论。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了准确率，还详细分析了内存占用、推理延迟，并提供了可视化结果来证明 MI-Pruner 能够准确捕捉查询相关的区域（如“衬衫的颜色”对应衬衫区域）。

### 6. 主要结论与发现
*   **性能领先**：MI-Pruner 在多个 MLLM 架构上均达到了 SOTA 性能，尤其在极高剪枝率下（如仅保留 32 或 64 个 Token）表现稳健。
*   **缓解幻觉**：在 POPE 幻觉测试中，MI-Pruner 显著优于基于注意力的剪枝，能更好地平衡 Yes/No 的分布，减少因误删关键信息导致的幻觉。
*   **高效性**：由于不依赖复杂的注意力图提取，MI-Pruner 的推理延迟极低，且与 FlashAttention 兼容。
*   **理论验证**：证明了注意力分数并非衡量语义重要性的唯一标准，基于互信息的统计分析为黑盒 MLLM 提供了新的解释视角。

### 7. 优点
*   **模型无关（Model-agnostic）**：作为插件模块，可无缝集成到任何 Enc-MLP-Dec 架构的 MLLM 中。
*   **非侵入性**：无需修改模型内部架构，无需访问复杂的注意力矩阵。
*   **理论严谨**：基于互信息和子模函数最大化，比启发式的注意力剪枝更具解释性。
*   **高效**：计算开销极小，适合资源受限的部署场景。

### 8. 不足与局限
*   **先验假设**：假设视觉 Token 具有相等的先验概率（均匀分布），这可能忽略了摄影构图中的空间先验（如黄金分割点）。
*   **条件独立性假设**：为了计算简便假设了 Token 间的条件独立，这在处理需要多个 patch 共同表达的复杂语义时可能存在偏差。
*   **仅限视觉剪枝**：目前仅针对视觉 Token，未涉及文本 Token 的压缩（如处理长文本提示词时的冗余）。
*   **动态预算缺失**：目前仍需手动设定保留 Token 的数量（Budget），未能实现根据输入内容自动调整剪枝强度的完全自适应。

（完）
