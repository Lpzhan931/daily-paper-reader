---
title: "AXELRAM: Quantize Once, Never Dequantize"
title_zh: AXELRAM：一次量化，永不反量化
authors: Yasushi Nishida
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02638v1"
tags: ["query:llm"]
score: 9.0
evidence: 直接从量化KV缓存索引计算注意力分数
tldr: 本研究提出AXELRAM，一种无需反量化的智能SRAM架构，直接利用量化后的KV缓存索引计算注意力分数。该架构通过正交变换将数据分布标准化，结合查表法实现非对称路径设计，显著降低了计算开销。针对特定模型出现的性能崩溃问题，研究揭示了符号模式敏感性这一根源，并提出无梯度符号选择策略，在不增加硬件成本的前提下确保了模型稳定性，实现了高效且稳健的KV缓存处理。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型推理中KV缓存量化带来的高额反量化计算开销以及在特定模型上的性能不稳定问题。
method: 采用基于正交变换的固定码本量化和查表法计算，并引入无梯度符号模式选择来优化层级范数异质性。
result: 该方法将每查询乘法运算减少了102.4倍，并通过符号优化成功消除了Qwen2.5等模型中的灾难性困惑度激增。
conclusion: AXELRAM通过创新的软硬件协同设计，在显著提升KV缓存处理效率的同时，有效解决了量化旋转方差导致的稳定性挑战。
---

## 摘要
我们提出了 AXELRAM，一种智能 SRAM 宏架构，它直接从量化后的 KV 缓存索引计算注意力分数，而无需反量化。其关键赋能技术是设计时固定的码本：基于正交变换的量化将每个坐标的分布集中到 N(0,1/d)，因此最优量化器仅取决于维度 d 和位宽 b，而与输入数据无关。这种非对称路径设计——写入时变换，读取时查表且无需逆变换——将每次查询的乘法运算减少了 102.4 倍（这是一个数学恒等式）。通过多种子评估（10 个种子 x 3 个模型），我们发现符号模式敏感性在某些模型（如 Qwen2.5-3B）上会导致灾难性的困惑度（PPL）激增（Delta > 50），而其他模型（如 LLaMA-3.1-8B）则完全稳定。这一现象将 SpinQuant 在权重量化中观察到的旋转方差扩展到了 KV 缓存领域，且其影响在性质上更为严重。我们将根本原因追溯到层级范数异质性，并提出了一种无梯度的符号模式选择方法（200 个候选，8 个校准样本，一次性完成），在零额外硬件成本的情况下消除了灾难性的激增。所有源代码均可在 https://github.com/Axelidea/AXELRAM 获取。

## Abstract
We propose AXELRAM, a smart SRAM macro architecture that computes attention scores directly from quantized KV cache indices without dequantization. The key enabler is a design-time fixed codebook: orthogonal-transform-based quantization concentrates each coordinate's distribution to N(0,1/d), so the optimal quantizer depends only on dimension d and bit-width b, not on input data. The asymmetric path design -- transform on write, table-lookup on read with no inverse transform -- reduces per-query multiplications by 102.4x (a mathematical identity).   Through multi-seed evaluation (10 seeds x 3 models), we discover that sign pattern sensitivity causes catastrophic PPL spikes (Delta > 50) on certain models (Qwen2.5-3B), while others (LLaMA-3.1-8B) are fully stable. This phenomenon extends SpinQuant's observation of rotation variance in weight quantization to the KV cache domain, where the effect is qualitatively more severe. We trace the root cause to layer-wise norm heterogeneity and propose a gradient-free sign pattern selection (200 candidates, 8 calibration samples, one-time) that eliminates catastrophic spikes with zero additional hardware cost. All source code is available at https://github.com/Axelidea/AXELRAM.

---

## 论文详细总结（自动生成）

### AXELRAM：一次量化，永不反量化 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）推理的瓶颈在于 **KV 缓存（KV Cache）** 的内存占用。虽然将 KV 缓存量化至 2-4 位可以减少存储，但传统方法在每次查询（Query）计算注意力分数时，必须先将量化的 Key 反量化回高精度，这带来了巨大的计算开销。
论文提出了 **AXELRAM**，一种智能 SRAM 宏架构。其核心动机是：**能否直接利用量化后的索引计算注意力分数，从而彻底消除反量化过程？** 该研究旨在通过软硬件协同设计，在降低存储压力的同时，大幅提升计算效率。

#### 2. 方法论：核心思想与技术细节
AXELRAM 的核心在于利用正交变换的特性实现“非对称路径”设计：
*   **固定码本（Fixed Codebook）：** 基于正交变换（如 Hadamard 变换）的量化会将数据分布集中为标准正态分布 $N(0, 1/d)$。因此，最优量化器（Lloyd-Max）仅取决于维度 $d$ 和位宽 $b$，无需随输入数据动态调整，可预先存储在 ROM 中。
*   **非对称读写路径：**
    *   **写入路径（Write Path）：** 对 Key 进行正交变换（FWHT，快速哈达玛变换）并量化为索引，存入 SRAM。此过程无需乘法器。
    *   **读取路径（Read Path）：** 关键创新点。不再对 Key 进行逆变换，而是**仅对 Query 进行一次正交变换**。利用正交变换下的内积不变性 $\langle q, k \rangle = \langle Rq, Rk \rangle$，通过预计算的查表（Table Lookup）和加法树直接得出注意力分数。
*   **符号模式优化（Sign Pattern Optimization）：** 针对特定模型出现的性能崩溃，提出一种无梯度的符号选择算法。通过在少量校准样本上从 200 个随机符号向量候选者中筛选出最优的一个，以应对层级范数异质性（Layer-wise Norm Heterogeneity）。

#### 3. 实验设计
*   **评估模型：** LLaMA-3.1-8B-Instruct、Qwen2.5-3B-Instruct、Qwen3-8B。
*   **数据集：** WikiText-2 测试集（用于测量困惑度 PPL）。
*   **基准测试（Benchmark）：** 以 FP16 精度为基准，对比不同位宽（2/3/4-bit）下的量化表现。
*   **对比维度：** 重点对比了旋转量化中的“种子敏感性”，并与 SpinQuant（权重量化领域）的研究结论进行了跨领域对比。

#### 4. 资源与算力
*   **计算平台：** 论文提到部分计算工作在东京科学大学的 **TSUBAME4.0 超级计算机**上完成。
*   **具体消耗：** 论文未明确给出具体的 GPU 型号、数量或总训练时长。但指出符号模式优化过程非常轻量：每个模型约 36 层，每层校准仅需约 1 秒，总计约 36 秒即可完成。

#### 5. 实验数量与充分性
*   **实验规模：** 采用了 **10 个随机种子 × 3 个模型** 的多重评估方案。
*   **充分性：** 这种多重种子评估在 KV 缓存量化研究中较为罕见（以往研究多为单次运行），成功揭示了此前被忽视的“灾难性 PPL 激增”现象。
*   **客观性：** 实验覆盖了不同架构的模型（LLaMA 与 Qwen），并针对 2-bit 量化的局限性进行了坦诚讨论，表现出较高的客观性。

#### 6. 主要结论与发现
*   **计算效率：** AXELRAM 将每查询的乘法运算次数减少了 **102.4 倍**（从 524,288 次降至 5,120 次，基于 $d=128, T=4096$）。
*   **稳定性发现：** 发现符号模式敏感性会导致某些模型（如 Qwen2.5）的 PPL 激增超过 50 点，而 LLaMA 则非常稳定。
*   **根源分析：** 性能崩溃的根源在于**层级范数异质性**（某些层的范数远大于其他层），导致正交变换后的分布偏离了固定码本的假设。
*   **优化效果：** 提出的无梯度符号选择策略在零额外硬件成本下，将 Qwen2.5 的 PPL 激增降低了 99%。

#### 7. 优点
*   **架构创新：** “一次量化，永不反量化”的概念极具创新性，通过数学恒等式而非近似值实现了巨大的计算缩减。
*   **软硬协同：** 巧妙地将复杂的旋转量化简化为 SRAM 内部的查表操作，且固定码本极大地简化了硬件实现。
*   **鲁棒性提升：** 深入探讨了旋转方差问题，并提供了简单有效的离线校准方案。

#### 8. 不足与局限
*   **硬件实现：** 目前所有结果均基于**位精确软件模拟（Bit-accurate Simulation）**，尚未有实际的物理芯片流片验证。
*   **精度地板：** 在 2-bit 量化下，部分模型（如 Qwen3-8B）即使经过优化仍存在明显的精度损失，这反映了标量量化的物理极限。
*   **应用限制：** 该方案高度依赖于特定的正交变换硬件实现，对于现有的通用 GPU 架构可能无法直接发挥其 SRAM 级查表的优势。

（完）
