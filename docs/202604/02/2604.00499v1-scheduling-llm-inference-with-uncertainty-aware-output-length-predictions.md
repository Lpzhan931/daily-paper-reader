---
title: Scheduling LLM Inference with Uncertainty-Aware Output Length Predictions
title_zh: 基于不确定性感知输出长度预测的大语言模型推理调度
authors: "Haoyu Zheng, Yongqiang Zhang, Fangcheng Fu, Xiaokai Zhou, Hao Luo, Hongchao Zhu, Yuanyuan Zhu, Hao Wang, Xiao Yan, Jiawei Jiang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00499v1"
tags: ["query:llm"]
score: 9.0
evidence: 调度LLM推理以避免队头阻塞
tldr: LLM推理调度通常采用最短作业优先（SJF）策略，但现有方法依赖单一的输出长度预测，忽略了生成过程的随机性。本文提出将输出长度建模为log-t分布，并引入“尾部膨胀期望”（TIE）指标来衡量任务优先级。该方法有效应对了长输出带来的风险，在在线推理和离线生成任务中显著降低了延迟并提升了吞吐量。
selection_source: fresh_fetch
motivation: 传统的点估计预测无法捕捉LLM随机解码过程中输出长度的不确定性，导致调度效率受限。
method: 提出使用log-t分布拟合输出长度，并设计了TIE指标，通过结合期望值与尾部概率来优化SJF调度优先级。
result: 实验表明，TIE调度器将在线推理的每Token延迟降低了2.31倍，并将离线数据生成的吞吐量提高了1.42倍。
conclusion: 考虑输出长度的不确定性并采用重尾分布建模，是提升LLM推理调度性能的关键。
---

## 摘要
在调度大语言模型（LLM）推理时，“短作业优先”（SJF）原则通过优先处理输出长度较短的请求来避免队头（HOL）阻塞，因而备受青睐。现有方法通常为每个请求预测一个单一的输出长度以辅助调度。我们认为，这种“点估计”与 LLM 推理的“随机”解码过程不匹配，因为输出长度本质上是不确定的，且取决于何时采样到序列结束（EOS）标记。因此，每个请求的输出长度应该用分布而非单一数值来拟合。通过对经验数据和随机解码过程的深入分析，我们观察到输出长度遵循重尾分布，并可以用 log-t 分布进行拟合。在此基础上，我们提出了一种名为“尾部膨胀期望”（Tail Inflated Expectation, TIE）的简单指标，用于替代 SJF 调度中的输出长度。该指标利用 log-t 分布的尾部概率来调整其期望值，以考虑请求生成长输出的风险。为了评估 TIE 调度器，我们将其与三个强基准模型进行了比较。结果表明，TIE 在在线推理中将逐 token 延迟降低了 2.31 倍，在离线数据生成中将吞吐量提高了 1.42 倍。

## Abstract
To schedule LLM inference, the \textit{shortest job first} (SJF) principle is favorable by prioritizing requests with short output lengths to avoid head-of-line (HOL) blocking. Existing methods usually predict a single output length for each request to facilitate scheduling. We argue that such a \textit{point estimate} does not match the \textit{stochastic} decoding process of LLM inference, where output length is \textit{uncertain} by nature and determined by when the end-of-sequence (EOS) token is sampled. Hence, the output length of each request should be fitted with a distribution rather than a single value. With an in-depth analysis of empirical data and the stochastic decoding process, we observe that output length follows a heavy-tailed distribution and can be fitted with the log-t distribution. On this basis, we propose a simple metric called Tail Inflated Expectation (TIE) to replace the output length in SJF scheduling, which adjusts the expectation of a log-t distribution with its tail probabilities to account for the risk that a request generates long outputs. To evaluate our TIE scheduler, we compare it with three strong baselines, and the results show that TIE reduces the per-token latency by $2.31\times$ for online inference and improves throughput by $1.42\times$ for offline data generation.

---

## 论文详细总结（自动生成）

### 论文总结：基于不确定性感知输出长度预测的大语言模型推理调度

#### 1. 核心问题与整体含义（研究动机和背景）
在 LLM 推理服务中，调度策略直接影响用户体验（延迟）和运营成本（吞吐量）。传统的“先来先服务”（FCFS）容易导致**队头阻塞（HOL Blocking）**，即长文本生成任务阻塞了后面的短任务。虽然“最短作业优先”（SJF）能缓解此问题，但现有的 SJF 变体依赖于对输出长度的**点估计（Point Estimate）**。
论文指出，由于 LLM 解码过程具有随机性（Stochastic Decoding），同一个 Prompt 多次运行的长度并不固定。因此，单一数值预测无法捕捉这种不确定性。一旦预测偏短而实际偏长，仍会引发严重的调度失效。

#### 2. 方法论：核心思想与关键技术
论文提出了 **TIE (Tail Inflated Expectation)** 调度框架，核心思想是将输出长度视为一个**概率分布**而非单一数值，并据此评估风险。

*   **分布建模**：通过对真实数据集（LMSYS-Chat-1M）的分析和理论证明，论文发现 LLM 输出长度呈现明显的**重尾分布（Heavy-tailed Distribution）**特征。实验对比后，选定 **log-t 分布**作为拟合模型（优于 Log-normal 和 Gamma 分布）。
*   **参数预测器**：
    *   使用 **DeBERTa-v3-base** 作为编码器提取 Prompt 语义。
    *   通过多池化融合策略（CLS、Mean、Max pooling）捕捉特征。
    *   接两个 MLP 分别预测 log-t 分布的参数 $\mu$ 和 $\sigma$（自由度 $\nu$ 固定为 3.5）。
*   **TIE 指标计算**：
    *   提出 **TIE Score = 期望值 ($E[\tilde{X}]$) + $\beta \times$ 条件风险价值 ($CVaR_\alpha[\tilde{X}]$)**。
    *   $CVaR$ 衡量了分布尾部（最长的那部分可能）的预期长度。$\beta$ 是一个根据系统压力动态调整的风险敏感系数。
*   **系统优化**：
    *   **异步预测**：将预测逻辑从主调度循环中解耦，避免预测本身的延迟阻塞请求入队。
    *   **动态批处理**：对预测请求进行攒批，提高预测器的吞吐。
    *   **饥饿预防**：引入基于等待时间的得分衰减机制，确保长任务不会被无限期推迟。

#### 3. 实验设计
*   **场景与指标**：
    *   **在线 Chatbot 服务**：关注平均/P90 的每 Token 延迟（PTLA）和首 Token 延迟（TTFT）。
    *   **离线合成数据生成（SDG）**：关注完成固定任务量的时间和单位时间吞吐量。
*   **数据集**：LMSYS-Chat-1M（真实对话）、ShareGPT（用户分享）、Alpaca（指令微调数据）。
*   **Benchmark 与对比方法**：
    *   **FCFS**：vLLM 默认策略。
    *   **SSJF**：基于 BERT 的点估计长度预测调度。
    *   **LTR (Learning-to-Rank)**：基于排序学习的调度方法。
*   **模型覆盖**：主要在 Llama-3-8B/70B 上测试，并扩展到 Qwen3、Mixtral (MoE)、DeepSeek-R1 等多种架构。

#### 4. 资源与算力
*   **硬件环境**：服务器配备 **8 × NVIDIA A6000 (48 GB) GPU**，128 核虚拟 CPU，512 GB 内存。
*   **部署细节**：70B 模型使用 8 路张量并行（TP8），8B 模型使用单卡。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到预测器是基于 DeBERTa-v3-base（86M 参数）进行微调，且训练数据量为 900K 样本（其中 TIE 使用 45K 独特 Prompt × 20 次采样），属于轻量级微调。

#### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **跨模型与跨数据集验证**：在不同规模（8B 到 80B）和不同类型（稠密、MoE、推理模型）的模型上均验证了有效性。
*   **泛化性测试**：在 LMSYS 数据上训练，在 ShareGPT 和 Alpaca 上测试，证明了模型的鲁棒性。
*   **消融实验**：分别验证了分布家族选择（log-t vs 其他）、评分公式组成（期望 vs CVaR）、以及 $\beta$ 系数的影响。
*   **客观性**：通过 KS 检验（Kolmogorov-Smirnov test）量化拟合质量，实验对比均在相同的硬件和基础框架（vLLM）下进行，确保了公平性。

#### 6. 主要结论与发现
*   **性能提升**：在 100 RPS 的高负载下，TIE 相比最强的基准方法（LTR），将在线平均每 Token 延迟降低了 **2.31 倍**；在离线 SDG 任务中，吞吐量提升了 **1.42 倍**。
*   **不确定性的价值**：单纯预测“平均长度”是不够的，考虑“长尾风险”能显著减少因预测失误导致的 HOL 阻塞。
*   **重尾特性**：理论和实证均证明 LLM 输出长度符合重尾分布，这是 TIE 优于其他方法的基础。

#### 7. 优点（亮点）
*   **视角独特**：首次系统性地将 LLM 输出长度的不确定性引入调度决策，跳出了“点估计”的思维定式。
*   **理论与实践结合**：不仅有严谨的数学证明（证明重尾性），还有完整的系统级实现（异步预测、动态批处理）。
*   **高泛化能力**：实验证明该方法不需要针对每个新模型重新训练，具有很强的工业应用潜力。

#### 8. 不足与局限
*   **数据采集成本**：为了拟合分布，训练阶段每个 Prompt 需要生成多次（文中为 20 次）响应，这比点估计方法的数据标注成本更高。
*   **静态分布假设**：目前的预测是在生成开始前完成的。虽然文中提到了动态更新的潜力，但当前实现尚未利用生成过程中的中间状态来实时修正分布。
*   **极端长文本限制**：对于极长文本（超过 max_tokens），分布会被截断，此时 TIE 的优势可能会减弱。

（完）
