---
title: "TENT: A Declarative Slice Spraying Engine for Performant and Resilient Data Movement in Disaggregated LLM Serving"
title_zh: TENT：一种用于解耦式大语言模型服务中高性能且具弹性的数据传输的声明式切片喷射引擎
authors: "Feng Ren, Ruoyu Qin, Teng Ma, Shangming Cai, Zheng Liu, Chao Lei, Dejiang Zhu, Ke Yang, Zheming Li, Jialei Cui, Weixiao Huang, Yikai Zhao, Yineng Zhang, Hao Wu, Xiang Gao, Yuhao Fu, Jinlei Jiang, Yongwei Wu, Mingxing Zhang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00368v1"
tags: ["query:llm"]
score: 9.0
evidence: 解耦式LLM服务中的高性能数据移动
tldr: 针对大模型解耦推理中异构互连架构导致的通信瓶颈和可靠性问题，本文提出了TENT。TENT是一个声明式数据传输引擎，通过将传输意图与物理执行解耦，将异构链路统一为动态资源池。它采用细粒度切片喷射技术，根据实时链路质量动态分配流量，消除了队头阻塞并实现了亚50毫秒的故障自愈。在H800集群上的测试表明，TENT显著提升了LLM推理吞吐量并降低了延迟，已在多个工业现场投入生产。
selection_source: fresh_fetch
motivation: 现有的数据传输框架采用静态路径选择，难以应对异构互连环境下的链路拥塞和硬件故障，导致通信效率低下且运维脆弱。
method: 提出声明式切片喷射引擎，将大流量分解为细粒度切片，并根据实时遥测数据在异构链路池中动态调度传输。
result: "在LLM推理中相比Mooncake TE提升了高达1.36倍吞吐量并降低了26%的P90首字延迟，同时在RL流水线中显著加速了参数更新。"
conclusion: TENT通过解耦传输意图与执行，有效解决了大规模GPU集群中异构互连的性能与鲁棒性挑战，为高性能LLM服务提供了可靠的数据平面方案。
---

## 摘要
现代 GPU 集群构建在复杂的异构互连层级之上，涵盖了从多轨 RDMA 到诸如多节点 NVLink 和昇腾 UB 等专用架构。在解耦式大语言模型（LLM）服务中，如何有效地编排这些多样化的链路仍然是一个关键挑战。在数千个 GPU 上运行 Mooncake TE 暴露了现有框架共同的一个关键局限性：命令式、静态绑定的路径选择。这种僵化迫使引擎依赖于忽略拥塞信号的状态盲条带化（striping），从而产生通信孤岛，因队头阻塞浪费多轨带宽，并导致运维脆弱性，使得常规故障需要人工干预。我们提出了 TENT，这是一种将传输意图与物理执行解耦的数据传输引擎。TENT 不再将工作负载锁定在固定的后端，而是将异构互连统一为一个单一的动态资源池。应用程序只需声明传输意图，而 TENT 会根据瞬时链路质量，动态地将大流（elephant flows）分解为细粒度切片，并将其“喷射”到各个链路上。这种遥测驱动的编排消除了队头阻塞，并通过在无需应用逻辑干预的情况下绕过故障重新路由切片，实现了透明的、低于 50 毫秒的自愈。TENT 已作为生产级数据平面，应用于多个工业现场的 LLM 推理和强化学习（RL）流水线。我们在 H800 HGX 集群上的评估表明，TENT 的性能优于包括 Mooncake TE、NIXL 和 UCCL 在内的现有最先进基准。在结合 SGLang HiCache 的 LLM 推理中，TENT 的吞吐量比 Mooncake TE 高出 1.36 倍，P90 首字延迟（TTFT）降低了 26%。在强化学习流水线中，TENT 将 Moonshot Checkpoint Engine 的参数更新速度提升了 20-26%。

## Abstract
Modern GPU clusters are built upon a complex hierarchy of heterogeneous interconnects, ranging from multi-rail RDMA to proprietary fabrics such as Multi-Node NVLink and Ascend UB. Orchestrating these diverse links effectively remains a critical challenge in disaggregated LLM serving. Operating Mooncake TE on thousands of GPUs exposed a critical limitation shared by existing frameworks: imperative, statically bound path selection. This rigidity forces engines to rely on state-blind striping that ignores congestion signals, creating communication silos, wasting multi-rail bandwidth due to head-of-line blocking, and leading to operational fragility where routine faults require manual intervention. We present TENT, a data-movement engine that decouples transfer intent from physical execution. Instead of locking workloads to fixed backends, TENT unifies heterogeneous interconnects into a single dynamic resource pool. Applications simply declare transfer intents, while TENT dynamically decomposes elephant flows into fine-grained slices and "sprays" them across links based on instantaneous link quality. This telemetry-driven orchestration eliminates head-of-line blocking and enables transparent, sub-50 ms self-healing by rerouting slices around failures without application logic. TENT serves as the production data plane for LLM inference and RL pipelines at multiple industrial sites. Our evaluation on H800 HGX clusters shows that TENT outperforms state-of-the-art baselines, including Mooncake TE, NIXL, and UCCL. In LLM inference with SGLang HiCache, TENT achieves up to 1.36x higher throughput and 26% lower P90 TTFT than Mooncake TE. In RL pipelines, TENT accelerates parameter updates in Moonshot Checkpoint Engine by 20-26%.

---

## 论文详细总结（自动生成）

### TENT：用于解耦式 LLM 服务的高性能弹性数据传输引擎总结

#### 1. 核心问题与研究动机
在现代解耦式大语言模型（LLM）服务架构中（如首字预填充与后续解码分离、多级 KV 缓存管理），数据传输（如 KV 缓存迁移、模型权重更新）已成为性能瓶颈。
*   **现有挑战**：目前的传输引擎（如 Mooncake TE、NIXL、UCCL）采用**命令式、静态绑定**的路径选择。
*   **痛点**：
    1.  **通信孤岛**：静态绑定导致不同后端（如 NVLink 与 RDMA）无法互操作，形成资源碎片。
    2.  **状态盲条带化**：简单的轮询（Round-Robin）忽略了链路实时拥塞，导致“队头阻塞”（HoL），整体速度受限于最慢的链路。
    3.  **运维脆弱**：硬件故障（如网卡抖动）通常需要人工干预或应用层重试，恢复时间长。

#### 2. 方法论：声明式切片喷射（Slice Spraying）
TENT 实现了从“命令式路径选择”到“声明式切片喷射”的范式转变：
*   **核心思想**：将传输意图（做什么）与物理执行（怎么做）解耦。应用层只需声明源/目的段，由引擎动态决定最优路径。
*   **关键技术细节**：
    1.  **统一段抽象（Segment Abstraction）**：将主机内存、显存、SSD 统一建模，屏蔽底层硬件差异。
    2.  **动态编排**：在运行时根据拓扑图进行“延迟绑定”，支持多跳路由（如 GPU-Host-GPU）的透明流水线化。
    3.  **遥测驱动的切片喷射**：将大流量（Elephant Flows）分解为细粒度切片（默认 64KB），利用**预测成本模型**（考虑队列长度、带宽和拓扑惩罚）将切片动态分配给预期完成时间最短的链路。
    4.  **双层弹性机制**：在链路层监控实时质量，在传输层支持后端透明替换。若切片失败，引擎会在亚 50 毫秒内自动重路由，无需应用层感知。

#### 3. 实验设计与基准测试
*   **应用场景**：
    1.  **LLM 推理**：集成到 SGLang HiCache 中，测试多轮对话下的 KV 缓存重用。
    2.  **强化学习（RL）流水线**：集成到 Moonshot Checkpoint Engine，测试模型权重的频繁更新。
    3.  **微基准测试（Microbenchmarks）**：测试主机间（H2H）和设备间（D2D）的原始吞吐量与延迟。
*   **对比方法（Baselines）**：Mooncake TE（前代生产系统）、NVIDIA NIXL、UCCL-P2P。
*   **测试模型**：DeepSeek-R1、Qwen3-235B、GLM-4.5-Air、DeepSeek-V3.1 等。

#### 4. 资源与算力
*   **主要测试环境**：配备 8 张 NVIDIA H800 GPU（80GB HBM）和 8 块 200 Gbps RoCE 网卡的 HGX 节点。
*   **扩展性测试**：在包含 **256 张 H20 GPU** 的半生产集群上验证了万亿参数模型的权重更新。
*   **异构支持**：实验涵盖了 MNNVL（多节点 NVLink）、华为昇腾（Ascend UB/HIXL）以及基于 io_uring 的 SSD 存储。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从底层算子（微基准）到端到端应用（SGLang、RL）的完整测试，并包含了长达一年的生产环境部署数据。
*   **充分性**：实验设计较为充分，不仅对比了吞吐量和延迟（P90/P99），还进行了参数敏感性分析（如拓扑惩罚因子 $P$ 的影响）和故障注入实验。
*   **客观性**：通过在标准 H800 集群上运行开源基准测试，确保了结果的可复现性。

#### 6. 主要结论与发现
*   **性能提升**：在 SGLang 推理中，TENT 相比 Mooncake TE 提升了 **1.36 倍**吞吐量，降低了 **26%** 的 P90 首字延迟（TTFT）。
*   **加速更新**：在 RL 流水线中，模型参数更新速度提升了 **20%~26%**。
*   **微观表现**：在多轨 RDMA 环境下，TENT 的吞吐量比现有基准高出约 **33%**，P99 延迟降低至基准的 **27.6%**。
*   **高可靠性**：在模拟网卡故障时，TENT 能在 **50ms 内**完成自愈，吞吐量仅出现短暂波动，应用层无感知。

#### 7. 优点
*   **架构解耦**：声明式 API 极大地简化了上层应用的开发，使其无需关心底层复杂的异构拓扑。
*   **动态自适应**：切片级调度能有效解决异构集群中常见的“长尾链路”问题，最大化利用了多轨带宽。
*   **工业级鲁棒性**：亚 50ms 的自愈能力对于大规模、高并发的在线推理服务至关重要。
*   **高度兼容**：支持 NVIDIA 和华为等多种硬件生态，具有很强的通用性。

#### 8. 不足与局限
*   **控制面开销**：虽然论文提到使用了轻量级控制块，但在极端小包、超高并发场景下，切片级调度的 CPU 开销可能成为新的瓶颈（尽管文中未显现）。
*   **全局最优性**：目前的调度主要基于局部遥测，在跨多交换机的大规模网络拥塞控制方面，是否能达到全局最优仍有待进一步探讨。
*   **应用限制**：主要针对大流量（Elephant Flows）优化，对于极小规模的零星数据传输，其复杂的编排逻辑可能带来额外延迟。

（完）
