---
title: Why and When Visual Token Pruning Fails? A Study on Relevant Visual Information Shift in MLLMs Decoding
title_zh: 视觉 Token 剪枝为何以及何时失效？关于多模态大语言模型解码过程中相关视觉信息偏移的研究
authors: "Jiwan Kim, Kibum Kim, Wonjoong Kim, Byung-Kwan Lee, Chanyoung Park"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12358v1"
tags: ["query:llm"]
score: 8.0
evidence: 多模态大语言模型解码中的视觉标记剪枝
tldr: 针对多模态大模型视觉Token剪枝在复杂推理任务中失效的问题，本文揭示了解码过程中“相关视觉信息偏移（RVIS）”这一关键诱因。为此，作者提出了DSTP框架，这是一种无需训练的通用插件，能使剪枝方法在解码阶段动态对齐不断变化的推理需求。实验表明，DSTP显著提升了现有剪枝方法在复杂任务中的表现，且计算开销极小，适用于多种主流模型架构。
selection_source: fresh_fetch
motivation: 现有剪枝方法在处理复杂视觉推理时表现不佳，其根源在于忽略了解码过程中所需视觉信息的动态偏移。
method: 提出DSTP框架，通过在解码阶段引入偏移感知机制，使视觉Token的保留策略能随生成内容实时调整。
result: DSTP在多个复杂推理和视觉理解基准测试中均实现了显著的性能增益，并展现出极高的架构兼容性。
conclusion: 解决解码阶段的信息偏移问题是优化MLLM视觉剪枝的关键，DSTP为实现高效且精准的多模态推理提供了有效方案。
---

## 摘要
最近，视觉 Token 剪枝已被用于处理多模态大语言模型（MLLMs）中大量的视觉 Token。然而，我们观察到，虽然现有的剪枝方法在简单的视觉理解任务上表现可靠，但在有效泛化到复杂的视觉推理任务时却面临困难，这是以往研究中尚未充分探索的关键差距。通过系统分析，我们发现解码过程中的相关视觉信息偏移（RVIS）是导致失效的主要原因。为解决这一问题，我们提出了解码阶段偏移感知 Token 剪枝（DSTP），这是一个无需训练的附加框架，使现有的剪枝方法能够在解码阶段将视觉 Token 与不断变化的推理需求相对齐。大量实验表明，DSTP 显著缓解了剪枝方法在复杂推理任务中的性能下降，同时在视觉理解基准测试中也持续带来了性能提升。此外，DSTP 在多种先进架构上均表现出有效性，突显了其泛化能力以及极低计算开销下的高效性。

## Abstract
Recently, visual token pruning has been studied to handle the vast number of visual tokens in Multimodal Large Language Models. However, we observe that while existing pruning methods perform reliably on simple visual understanding, they struggle to effectively generalize to complex visual reasoning tasks, a critical gap underexplored in previous studies. Through a systematic analysis, we identify Relevant Visual Information Shift (RVIS) during decoding as the primary failure driver. To address this, we propose Decoding-stage Shift-aware Token Pruning (DSTP), a training-free add-on framework that enables existing pruning methods to align visual tokens with shifting reasoning requirements during the decoding stage. Extensive experiments demonstrate that DSTP significantly mitigates performance degradation of pruning methods in complex reasoning tasks, while consistently yielding performance gains even across visual understanding benchmarks. Furthermore, DSTP demonstrates effectiveness across diverse state-of-the-art architectures, highlighting its generalizability and efficiency with minimal computational overhead.

---

## 论文详细总结（自动生成）

### 论文总结：视觉 Token 剪枝为何以及何时失效？关于多模态大语言模型解码过程中相关视觉信息偏移的研究

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理高分辨率图像时会产生海量的视觉 Token，导致巨大的计算和内存开销。现有的视觉 Token 剪枝方法（如 FastV, DivPrune 等）主要在**预填充（Prefill）阶段**根据初始注意力权重剔除冗余 Token。
然而，作者发现这些方法在简单的视觉问答（VQA）中表现良好，但在**复杂的视觉推理（VMR）**（如数学、逻辑题）中性能大幅下降。研究揭示了其根本原因：在多模态推理的解码过程中，模型所需的关键视觉区域会随推理步骤动态变化，这种现象被定义为**相关视觉信息偏移（Relevant Visual Information Shift, RVIS）**。静态剪枝在初始阶段丢弃了后续推理步骤所需的 Token，导致推理失败。

#### 2. 方法论：DSTP 框架
作者提出了**解码阶段偏移感知 Token 剪枝（DSTP）**，这是一种无需训练、即插即用的插件框架，核心思想是在解码过程中动态监测并恢复所需的视觉 Token。
*   **RISD（相关视觉信息偏移检测模块）：** 在解码的每一步，计算当前 Token 对视觉区域的注意力分布与预填充阶段初始分布的余弦相似度。当相似度低于设定阈值 $\tau$ 时，判定发生了 RVIS。
*   **CPTS（上下文保留视觉 Token 交换模块）：** 一旦检测到 RVIS，模型会重新评估所有原始视觉 Token（包括之前被剪枝掉的）。它不是简单的替换，而是将初始 Token 与新识别的关键 Token 进行**并集（Union）操作**，形成一个临时的扩展 Token 集，并维持 $L$ 个解码步骤，以确保推理的连贯性和上下文稳定性。

#### 3. 实验设计
*   **数据集/场景：**
    *   **视觉推理（VMR）：** MathVerse, WeMath, DynaMath（数学推理）；LogicVista, MMMU-Pro（逻辑与专家级分析）。
    *   **视觉理解（VQA）：** ScienceQA (SQA), TextVQA, GQA。
*   **基准模型（Backbones）：** Qwen3-VL-4B, InternVL3.5-8B，以及它们的“Thinking”版本和更大规模版本（如 14B）。
*   **对比方法：** FastV (ECCV'24), DivPrune (CVPR'25), VisionZip (CVPR'25)。

#### 4. 资源与算力
*   **硬件设备：** 使用了 **NVIDIA L40S GPU**。
*   **算力细节：** 评估过程在 4 张 L40S 上运行；效率指标（延迟、显存、TPS）在单张 L40S 上测量。
*   **训练时长：** 该方法为 **Training-free（无需训练）**，因此没有训练时长开销，仅涉及推理阶段的计算。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 8 个主流基准测试集，跨越了从 4B 到 14B 不同参数规模的模型。
*   **消融实验：** 针对检测阈值 $\tau$、上下文保留长度 $L$、Token 交换策略（硬交换 vs. 并集交换）以及不同 RVIS 频率下的成功率进行了详尽分析。
*   **充分性评价：** 实验设计非常充分且客观。作者不仅对比了准确率，还深入分析了 TFLOPs（理论计算量）、延迟（实际速度）和显存占用，证明了该方法在提升性能的同时保持了极高的效率。

#### 6. 主要结论与发现
*   **RVIS 是剪枝失效的主因：** 复杂推理任务中 RVIS 发生频率极高，而简单 VQA 任务中视觉关注点相对固定。
*   **动态调整优于增加预算：** 在 33.3% 的 Token 保留率下使用 DSTP，其表现甚至优于保留 66.6% Token 的静态剪枝方法，且计算成本更低。
*   **效率增益：** DSTP 虽然在检测到偏移时会增加少量 Token，但由于它能引导模型更精准地推理，减少了冗余的文本生成，在某些情况下总推理延迟反而比基础剪枝方法更低。

#### 7. 优点
*   **揭示了新机制：** 首次系统性地定义并量化了解码阶段的 RVIS 现象，为视觉剪枝研究提供了新视角。
*   **即插即用且通用：** 无需重新训练模型，可直接应用于各种基于 Attention 的剪枝方法和不同的 MLLM 架构。
*   **性能与效率平衡：** 巧妙地通过“检测-交换”机制，以极小的计算代价解决了复杂推理中的信息缺失问题。

#### 8. 不足与局限
*   **层级一致性限制：** 目前 DSTP 在所有 Transformer 层中统一应用相同的视觉 Token 集，未考虑不同层对视觉信息需求的差异（如浅层关注细节，深层关注语义）。
*   **超参数依赖：** 阈值 $\tau$ 和长度 $L$ 目前是固定的，对于不同难度或类型的题目，可能需要更动态的自适应调整策略。
*   **应用范围：** 主要针对推理密集型任务，对于极短文本生成的简单识别任务，提升空间有限。

（完）
