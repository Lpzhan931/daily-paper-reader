---
title: "OSC: Hardware Efficient W4A4 Quantization via Outlier Separation in Channel Dimension"
title_zh: OSC：通过通道维度离群值分离实现的硬件高效 W4A4 量化
authors: "Zhiyuan Zhang, Yanzhao Li, Zhiqiang Zou, Bai Du, Yupeng Sun, Hui Dong, Hui Wang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12782v1"
tags: ["query:llm"]
score: 8.0
evidence: 用于高吞吐部署的硬件高效W4A4量化
tldr: 针对大语言模型4比特量化中激活离群值导致精度下降的问题，本文提出OSC框架。研究发现离群值在通道维度具有结构化聚集特性，据此设计了双路径计算架构：主路径执行低精度W4A4 GEMM，分支路径处理高精度离群值。通过离线识别和在线提取，OSC实现了硬件友好的离群值保护，在保持高吞吐量的同时显著降低了精度损失，在Qwen3模型上实现了1.78倍的加速。
selection_source: fresh_fetch
motivation: 激活值中的离群值严重限制了低比特量化的动态范围，导致模型精度大幅下降。
method: 提出OSC框架，通过离线识别固定通道中的离群值，并在推理时利用双路径（W4A4+FP16）及结构化子张量提取技术进行高效处理。
result: 在Qwen3-8B/30B上精度损失仅为2.19/1.12点，且在现代AI加速器上比W8A8基准快1.78倍。
conclusion: OSC通过利用离群值的通道聚集特性，实现了兼顾硬件效率与模型精度的W4A4量化部署。
---

## 摘要
虽然 4-bit 量化对于大语言模型的高吞吐部署至关重要，但由于低比特格式的动态范围受限，激活值中的离群值（outliers）往往会导致显著的精度下降。在本文中，我们系统地研究了离群值的空间分布，并证明了一种 Token 持久性的结构化聚类效应，即高幅值的离群值在不同 Token 之间始终占据固定的通道。基于这一发现，我们提出了 OSC，一个用于离群值抑制的硬件高效框架。在推理过程中，OSC 执行双路径计算，包括一条低精度的 4-bit 通用矩阵乘法（GEMM）路径和一条高精度的 16-bit 分支 GEMM 路径。具体而言，OSC 使用离线的逐组（group-wise）策略来识别离群值所在的通道，然后执行结构化的子张量提取，在线将这些分散的激活通道合并为一个紧凑的稠密张量。该机制通过正则化且高吞吐的 GEMM 操作实现离群值保护，实现了与现代 4-bit 微缩放（micro-scaling）硬件的无缝适配。此外，针对离群值聚类效应较不明显的 W2 输入，我们集成了一种回退到 FP8 的策略。在 Qwen3-8B 和 Qwen3-30B 上的评估结果显示，平均精度下降分别被限制在 2.19 和 1.12 个百分点。值得注意的是，OSC 具有极高的硬件友好性，在现代 AI 加速器上相比 W8A8 GEMM 基准实现了高达 1.78 倍的峰值加速。

## Abstract
While 4-bit quantization is essential for high-throughput deployment of Large Language Models, activation outliers often lead to significant accuracy degradation due to the restricted dynamic range of low-bit formats. In this paper, we systematically investigate the spatial distribution of outliers and demonstrate a token-persistent structural clustering effect, where high-magnitude outliers consistently occupy fixed channels across tokens. Building on this insight, we propose OSC, a hardware-efficient framework for outlier suppression. During inference, OSC executes a dual-path computation consisting of a low-precision 4-bit General Matrix Multiplication (GEMM) path and a high-precision 16-bit branch GEMM path. Specifically, OSC uses an offline group-wise strategy to identify the channels where outliers are located and then performs structured sub-tensor extraction to coalesce these scattered activation channels into a compact dense tensor online. This mechanism implements outlier protection through regularized and high-throughput GEMM operations, achieving a seamless fit with modern 4-bit micro-scaling hardware. Furthermore, for the inputs of W2 where outlier clustering is less pronounced, we integrate a fallback strategy to FP8. Evaluation on Qwen3-8B and Qwen3-30B restricts the average accuracy drop to 2.19 and 1.12 points, respectively. Notably, OSC is highly hardware-friendly, achieving a peak speedup of 1.78x over the W8A8 GEMM baseline on a modern AI accelerator.

---

## 论文详细总结（自动生成）

这是一份关于论文《OSC: Hardware Efficient W4A4 Quantization via Outlier Separation in Channel Dimension》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLM）的 4-bit 量化（如 W4A4）对于提高推理吞吐量至关重要，但激活值中的“离群值”（Outliers）由于其极大的幅值，会严重限制低比特格式的动态范围，导致精度大幅下降。
*   **核心问题**：现有的离群值处理方法（如动态检测或全局重参数化）要么引入了巨大的计算开销，要么导致内存碎片化，难以在现代 AI 加速器上实现确定性的高效执行。
*   **核心发现**：论文通过系统分析发现，离群值在通道维度上具有**Token 持久性的结构化聚类效应**，即在大多数 Transformer 模块中，离群值始终出现在固定的通道中。

### 2. 方法论
OSC（Outlier Separation in Channel Dimension）框架包含三个关键阶段：
*   **离线分析与索引构建**：在离线阶段，通过少量校准数据识别每个量化组（Group）中统计意义上最显著的离群值通道，并将其索引存储在三维查找表（Lookup Table）中。
*   **静态离群值抑制（双路径执行）**：
    *   **路径 A（低精度主路径）**：根据离线索引，在量化前将激活值中的离群值通道清零（Zero-out），从而缩小量化组的动态范围，提高剩余数值的量化精度，随后执行 4-bit GEMM。
    *   **路径 B（高精度分支路径）**：将提取出的离群值通道组成一个紧凑的稠密子张量，与对应的高精度权重行执行 16-bit GEMM。
    *   **融合**：将两条路径的结果相加，恢复最终输出。
*   **混合精度自适应回退（Fallback）**：研究发现 FFN 的 W2（下投影）层离群值分布较为分散，不适合静态索引。因此，OSC 对 W2 层采用 FP8 格式，而对其他层采用 W4A4+OSC 保护。

### 3. 实验设计
*   **模型与架构**：评估了 Qwen3-8B（稠密模型）和 Qwen3-30B-A3B（混合专家模型 MoE）。
*   **数据集/任务**：涵盖 MMLU（5-shot）、GSM8K（8-shot）、ARC-Challenge 和 ARC-Easy（25-shot）等主流推理基准。
*   **对比方法**：
    *   W16A16（全精度基准）
    *   W8A8（8比特基准）
    *   MXFP4-Direct（直接 4-bit 量化）
    *   MXFP4-W2FP8（仅 W2 回退）
    *   MXFP4-Dynamic（理想化的动态最大值检测保护）

### 4. 资源与算力
*   **硬件平台**：论文在“现代高性能 AI 加速器”上进行了评估（根据作者背景推测为华为昇腾系列）。
*   **算力细节**：文中未明确给出具体的 GPU/NPU 型号和总训练时长，但提供了硬件性能比例假设（FP16:FP8:FP4 = 1:2:4），并展示了在不同 Batch Size（M=16 到 128+）下的实测加速比。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度消融实验，包括不同模块（Attention, Wo, W1/W3, W2）的离群值聚类密度分析、不同量化组大小（G=16, 32, 64）的硬件效率对比。
*   **充分性评价**：实验设计较为充分，不仅验证了精度恢复能力，还深入探讨了硬件层面的执行效率（如内存受限 vs 计算受限场景）。通过对比动态保护方案，证明了静态 OSC 方案在硬件确定性上的优势。

### 6. 主要结论与发现
*   **精度表现**：在 Qwen3-8B 上，OSC 将 W4A4 的平均精度损失从 6.09 点降低至 2.19 点；在 30B MoE 模型上，损失仅为 1.12 点，接近无损。
*   **硬件效率**：OSC 相比 W8A8 基准实现了 **1.50x 至 1.78x** 的峰值加速。
*   **空间异质性**：证实了不同层对离群值保护的敏感度不同，W2 层是 4-bit 量化的主要瓶颈，需要更高精度的回退策略。

### 7. 优点
*   **硬件友好性极佳**：通过静态索引将不规则的离群值处理转化为规则的稠密矩阵运算，避免了动态搜索带来的计算开销和指令分支预测失败。
*   **针对性强**：识别并利用了离群值的通道聚类特性，而非盲目处理所有异常值。
*   **普适性**：方案可适配 MXFP4、NVFP4 和 HIF4 等多种现代微缩放数据格式。

### 8. 不足与局限
*   **回退策略粒度较粗**：目前对所有 W2 层统一采用 FP8 回退，可能存在进一步优化的空间（例如仅针对部分敏感层回退）。
*   **系统级延迟模拟**：性能评估部分主要基于算子级的分析建模，实际端到端部署中的系统级因素（如调度开销、多卡通信）可能对最终加速比产生微调。
*   **依赖校准数据**：虽然仅需极少量数据，但静态索引的有效性仍依赖于校准集与实际推理分布的一致性。

（完）
