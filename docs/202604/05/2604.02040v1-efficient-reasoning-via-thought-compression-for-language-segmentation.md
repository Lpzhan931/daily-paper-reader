---
title: Efficient Reasoning via Thought Compression for Language Segmentation
title_zh: 通过思维压缩实现语言分割的高效推理
authors: "Qing Zhou, Shiyu Zhang, Yuyu Jia, Junyu Gao, Weiping Ni, Junzheng Wu, Qi Wang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02040v1"
tags: ["query:llm"]
score: 8.0
evidence: 通过思维压缩实现高效推理以降低计算成本
tldr: 针对大模型在语言引导分割任务中思维链（CoT）推理计算成本高的问题，本文提出WISE框架。该框架遵循“思考两次”原则，通过训练模型生成“简洁理由-答案-详细解释”的结构，并结合自蒸馏目标，将详细推理压缩为紧凑形式。推理阶段仅保留简洁理由，显著降低了计算开销。实验表明，WISE在ReasonSeg基准上达到SOTA性能，且推理长度缩减了近5倍。
selection_source: fresh_fetch
motivation: 传统的思维链推理虽然提升了分割性能，但生成的冗长理由导致了极高的计算成本，限制了实际应用。
method: 提出WISE范式，通过结构化输出顺序和自蒸馏目标，强制模型将详细推理内化为简洁理由，并在推理时通过特定提示激活该策略。
result: 在ReasonSeg基准上取得58.3 cIoU的SOTA表现，同时将平均推理长度从112个token大幅缩减至23个。
conclusion: WISE成功实现了推理性能与效率的平衡，证明了通过思维压缩可以在保持高精度的同时显著提升多模态模型的推理速度。
---

## 摘要
思维链（CoT）推理显著提升了大多模态模型在语言引导分割任务中的性能，但由于生成冗长的推理过程导致计算成本极高，限制了其在现实场景中的应用。我们提出了 WISE（Wisdom from Internal Self-Exploration），这是一种遵循“深思熟虑——一次为了学习，一次为了速度”原则的新型高效推理范式。WISE 训练模型生成一个结构化序列：简洁的推理过程、最终答案，以及随后的详细解释。通过将简洁推理置于首位，我们的方法利用自回归条件机制，确保简洁推理能够作为生成详细解释的充分摘要。该结构通过自蒸馏目标进行强化，该目标同时奖励语义保真度和简洁性，迫使模型将其详细推理过程内化为紧凑形式。在推理阶段，详细解释被省略。为了解决由此产生的条件分布偏移，我们的推理策略 WISE-S 采用了一种简单的提示技术，在用户查询中注入关注简洁性的指令。这一调整促进了所学简洁策略的稳健激活，从而释放了该框架的全部潜力。大量实验表明，WISE-S 在 ReasonSeg 基准测试中实现了 58.3 cIoU 的最先进零样本性能，同时将平均推理长度缩短了近 5 倍——从 112 个 token 减少到仅 23 个。

## Abstract
Chain-of-thought (CoT) reasoning has significantly improved the performance of large multimodal models in language-guided segmentation, yet its prohibitive computational cost, stemming from generating verbose rationales, limits real-world applicability. We introduce WISE (Wisdom from Internal Self-Exploration), a novel paradigm for efficient reasoning guided by the principle of \textit{thinking twice -- once for learning, once for speed}. WISE trains a model to generate a structured sequence: a concise rationale, the final answer, and then a detailed explanation. By placing the concise rationale first, our method leverages autoregressive conditioning to enforce that the concise rationale acts as a sufficient summary for generating the detailed explanation. This structure is reinforced by a self-distillation objective that jointly rewards semantic fidelity and conciseness, compelling the model to internalize its detailed reasoning into a compact form. At inference, the detailed explanation is omitted. To address the resulting conditional distribution shift, our inference strategy, WISE-S, employs a simple prompting technique that injects a brevity-focused instruction into the user's query. This final adjustment facilitates the robust activation of the learned concise policy, unlocking the full benefits of our framework. Extensive experiments show that WISE-S achieves state-of-the-art zero-shot performance on the ReasonSeg benchmark with 58.3 cIoU, while reducing the average reasoning length by nearly \textbf{5$\times$} -- from 112 to just 23 tokens. Code is available at \href{https://github.com/mrazhou/WISE}{WISE}.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient Reasoning via Thought Compression for Language Segmentation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型多模态模型（LMMs）在“推理分割”（Reasoning Segmentation）任务中，虽然依靠**思维链（CoT）**显著提升了准确性，但 CoT 生成的推理过程通常极其冗长，导致计算成本高昂、推理延迟大，难以应用于机器人或交互助手等实时场景。
*   **研究背景**：现有的推理分割模型（如 LISA、Seg-Zero）存在“性能-效率悖论”——准确率高度依赖于长文本输出。
*   **整体含义**：本文旨在解耦模型的推理能力与输出长度，通过“思维压缩”技术，让模型在保持（甚至提升）推理精度的同时，大幅减少推理所需的 Token 数量。

### 2. 方法论：WISE 框架
核心思想是“思考两次：一次为了学习，一次为了速度”。
*   **结构化动作空间**：改变传统的“推理 $\rightarrow$ 答案”顺序，训练模型生成：**简洁理由 ($R_c$) $\rightarrow$ 最终答案 ($A$) $\rightarrow$ 详细解释 ($R_d$)**。
    *   **自回归条件机制**：由于 $R_d$ 是在 $R_c$ 之后生成的，强制要求 $R_c$ 必须包含生成 $R_d$ 所需的全部核心逻辑，使其成为整个推理过程的“充分统计量”。
*   **自蒸馏奖励函数 (GRPO 算法)**：
    *   **语义相似度奖励 ($S_{sim}$)**：利用预训练的 SentenceTransformer 计算 $R_c$ 与 $R_d$ 的余弦相似度，确保简洁理由不丢失核心语义。
    *   **简洁性奖励 ($S_{concise}$)**：根据 $R_c$ 相对于 $R_d$ 的长度缩减比例给予奖励。
    *   **条件触发**：仅当最终答案 $A$ 的 IoU > 0.5 时才激活蒸馏奖励，避免模型学习错误的推理路径。
*   **WISE-S 推理策略**：在推理阶段直接省略 $R_d$。为了解决训练（预期有 $R_d$）与推理（无 $R_d$）之间的分布偏移，引入 **WISE-S**，即在用户指令中注入“简洁提示词”（如 "one sentence"），激活模型已内化的紧凑推理策略。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：仅使用 RefCOCOg 的 2000 个样本（无需人工标注推理过程，纯强化学习）。
    *   **测试集**：核心基准为 **ReasonSeg**（测试零样本推理能力）；同时评估 RefCOCO、RefCOCO+、RefCOCOg（测试域内泛化性）。
*   **对比方法**：
    *   **SOTA 模型**：LISA (7B/13B), Seg-Zero, SAM4MLLM 等。
    *   **效率基准**：L1-Exact, L1-Max（直接通过长度惩罚优化效率的方法）。
*   **评估指标**：cIoU（累计交并比）、gIoU、平均推理 Token 数量。

### 4. 资源与算力
*   **模型架构**：推理模型为 **Qwen2.5-VL-7B**，分割模型为冻结的 **SAM2-Large**。
*   **训练细节**：使用 GRPO 强化学习算法，DeepSpeed 库优化。总 Batch Size 为 16，学习率 $1 \times 10^{-6}$，使用 AdamW 优化器。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体的训练总时长，但提到了推理时的墙钟时间（Wall-clock time）对比。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流分割基准数据集，并扩展到了 VisionReasoner 统一任务（检测、计数、分割）。
*   **消融实验**：非常充分。分别验证了：
    1.  **生成顺序**（证明 $R_c \rightarrow A \rightarrow R_d$ 优于其他顺序）；
    2.  **奖励组件**（证明相似度、简洁度和条件指标缺一不可）；
    3.  **推理提示词**（对比了不同提示词对性能的影响）。
*   **客观性**：通过 KDE 分布图展示了 Token 长度的稳定性，并对比了不同参数规模（3B vs 7B）和不同模型家族（Qwen2 vs Qwen2.5），证明了方法的普适性。

### 6. 主要结论与发现
*   **高效性**：WISE-S 在 ReasonSeg 上将推理长度从 111.9 tokens 缩减至 **22.7 tokens**（约 **5 倍压缩**），推理速度提升约 5 倍。
*   **高性能**：在大幅压缩长度的同时，ReasonSeg 的 cIoU 提升了 **3.9**（达到 58.3），证明简洁的推理反而能减少冗余信息对模型的干扰。
*   **任务无关性**：在目标检测和物体计数任务中同样有效，甚至在检测任务中实现了更高的 mAP。

### 7. 优点与亮点
*   **解耦设计**：巧妙地利用输出顺序实现了“训练时学习复杂逻辑，推理时输出精简逻辑”。
*   **无需人工理由**：完全基于强化学习自探索，降低了对高质量 CoT 数据集的依赖。
*   **鲁棒性**：WISE-S 提示词策略极大地稳定了模型的输出长度分布，解决了强化学习模型输出不稳定的通病。

### 8. 不足与局限
*   **底层能力限制**：定性分析显示，当模型发生分割错误（如幻觉或逻辑环）时，简洁理由往往只是忠实地总结了错误的详细推理。这意味着 WISE 只能压缩推理，不能从根本上修复底座模型本身的视觉定位缺陷。
*   **提示词依赖**：虽然 WISE-S 效果好，但仍需在推理时手动注入提示词来达到最佳压缩效果。
*   **训练开销**：强化学习（GRPO）通常比监督微调（SFT）消耗更多算力，且文中未给出具体的训练资源消耗对比。

（完）
