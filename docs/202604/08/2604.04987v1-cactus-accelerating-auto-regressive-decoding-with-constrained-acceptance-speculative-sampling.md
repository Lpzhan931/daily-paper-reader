---
title: "Cactus: Accelerating Auto-Regressive Decoding with Constrained Acceptance Speculative Sampling"
title_zh: Cactus：通过约束接受投机采样加速自回归解码
authors: "Yongchang Hao, Lili Mou"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04987v1"
tags: ["query:llm-sd"]
score: 10.0
evidence: 具有宽松分布匹配约束的接受投机采样
tldr: 针对自回归大语言模型解码速度慢的问题，投机采样（SpS）通过小模型加速，但其严格匹配验证模型分布的限制降低了效率。本文提出Cactus，通过约束优化框架重新定义投机采样，在保证与验证模型分布偏差可控的前提下，显著提高了Token接受率。该方法在维持输出质量的同时，有效提升了推理吞吐量，并在多个基准测试中验证了其优越性。
selection_source: fresh_fetch
motivation: 现有的投机采样方法在严格匹配分布以保证质量与放宽限制以提升效率之间难以取得理想平衡。
method: 将投机采样过程形式化为约束优化问题，通过控制与验证模型分布的散度来最大化Token接受率。
result: 实验结果表明，Cactus在多种基准测试下均能显著提高接受率并加速解码，且输出质量保持稳定。
conclusion: Cactus为优化投机采样提供了一种理论完备的方法，在保证生成质量的前提下有效提升了大模型的推理效率。
---

## 摘要
投机采样 (SpS) 通过利用较小的草稿模型，成功加速了自回归大语言模型的解码吞吐量。SpS 严格强制生成的分布与验证器 LLM 的分布一致。这种限制是不必要的，因为验证器分布的轻微变化（例如使用 top-$k$ 或温度采样）也是可以接受的。典型的接受采样 (TAS) 通过使用基于熵的启发式方法接受更多 token，从而缓解了这一问题。然而，这种方法会扭曲验证器分布，当验证器编码关键信息时，可能会降低输出质量。在这项工作中，我们从约束优化的视角对投机采样算法进行了形式化。基于这一形式化，我们提出了 Cactus（约束接受投机采样），该方法保证了与验证器分布之间的散度受控，并提高了接受率。在广泛的基准测试上的实验结果证实了我们方法的有效性。

## Abstract
Speculative sampling (SpS) has been successful in accelerating the decoding throughput of auto-regressive large language models by leveraging smaller draft models. SpS strictly enforces the generated distribution to match that of the verifier LLM. This is unnecessarily restrictive as slight variations of the verifier's distribution, such as sampling with top-$k$ or temperature, would also be acceptable. Typical acceptance sampling (TAS) alleviates this issue by accepting more tokens using entropy-based heuristics. However, this approach distorts the verifier distribution, potentially degrading output quality when the verifier encodes critical information. In this work, we formalize the speculative sampling algorithm through the lens of constrained optimization. Based on this formulation, we propose Cactus (constrained acceptance speculative sampling), a method that guarantees controlled divergence from the verifier distribution and increasing acceptance rates. Empirical results across a wide range of benchmarks confirm the effectiveness of our approach.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Cactus**（Constrained Acceptance Speculative Sampling，约束接受投机采样）的新方法，旨在加速大语言模型（LLM）的推理过程。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
自回归 LLM 的推理受限于显存带宽，每生成一个 token 都需要完整的模型前向传播。**投机采样（Speculative Sampling, SpS）**通过“草稿模型生成-验证模型并行校验”的机制缓解了这一问题。
*   **现有问题：** 传统的 SpS 要求生成的分布必须与验证模型完全一致（无损），这过于严格，限制了 token 的接受率。而现有的有损方法（如 TAS）虽然提高了接受率，但往往通过启发式手段扭曲了分布，导致在处理关键信息时出现语义偏移或质量下降。
*   **研究动机：** 在保证与原分布偏差可控的前提下，如何通过数学化的手段最大化 token 接受率，从而在不牺牲质量的情况下实现更高的加速比。

### 2. 方法论
Cactus 将投机采样重新定义为一个**约束优化问题**：
*   **核心思想：** 在每一个解码步骤中，动态寻找一个目标分布 $h$，使其在满足与验证模型分布 $q$ 的 $f$-散度（如 KL 散度）小于预设阈值 $\delta$ 的约束下，最大化草稿 token 的接受概率。
*   **关键技术细节：**
    *   **理论形式化：** 证明了最优目标分布 $h$ 应该在草稿 token 上增加概率“奖励”，并按比例缩小其他 token 的概率。
    *   **闭式解推导：** 由于 KL 散度的精确解涉及超越方程，作者利用二阶泰勒展开（Taylor expansion）推导出了一个高效的近似闭式解。
    *   **Cactus 算法：** 最终公式通过验证模型对草稿 token 的置信度以及散度约束 $\delta$ 共同决定增加的概率。该方法无需训练，仅涉及简单的元素级运算。

### 3. 实验设计
*   **数据集/场景：**
    *   **GSM8K：** 小学数学应用题，评估逻辑推理。
    *   **IFEval：** 指令遵循评估，检查模型对格式和约束的遵守。
    *   **GPQA：** 研究生水平的科学问题，评估高难度知识处理。
*   **Benchmark 与对比方法：**
    *   **基准：** 验证模型直接生成的原始性能。
    *   **对比方法：** 传统的无损 SpS、典型的接受采样（TAS）、Top-$k$ 采样、以及分布插值法（Interpolation）。
    *   **模型对：** 主要使用 Qwen 3 系列（0.6B 到 32B），并扩展到 Gemma、DeepSeek R1 和 LLaMA 系列。

### 4. 资源与算力
*   **硬件：** 实验主要在 **NVIDIA A100 40GB GPU** 上进行。
*   **软件框架：** 使用了 **vLLM** 推理框架进行端到端吞吐量测量。
*   **说明：** 论文未明确给出总的训练时长（因为该方法是 **Training-free** 的，不需要训练），但详细记录了推理时的墙钟时间（Wall-time）和接受长度（AL）。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 0.6B 到 32B 不同参数规模的模型组合，测试了多种草稿长度（$m=10, 20$）。
*   **充分性：** 实验设计非常充分。除了主实验外，还包括：
    *   **跨模型系列验证：** 证明了方法在不同架构（LLaMA, DeepSeek 等）上的通用性。
    *   **消融实验：** 探讨了散度控制的重要性，并与简单的分布插值进行了对比。
    *   **扩展性测试：** 在 Spec-Bench 等综合基准上进行了测试。
*   **客观性：** 采用了标准化的正则匹配评估准确率，并同时报告了接受长度（AL）和实际加速比，评估维度全面。

### 6. 主要结论与发现
*   **性能提升：** Cactus 在所有基准测试中均实现了比 SpS 和 TAS 更高的 token 接受率。例如，在 Qwen 3 32B 模型上，Cactus 显著提升了接受长度。
*   **质量保持：** 相比 TAS，Cactus 在高难度任务（如 GPQA）上表现更稳定，能够有效防止因分布扭曲导致的性能下降。
*   **效率：** 在 14B 验证模型设置下，Cactus 实现了接近 1.9 倍的推理加速，且计算开销极低。

### 7. 优点
*   **理论完备：** 将投机采样提升到约束优化的高度，提供了严谨的数学证明。
*   **即插即用：** 无需对模型进行微调或额外训练，部署成本极低。
*   **平衡性好：** 通过超参数 $\delta$ 提供了清晰的“质量-速度”权衡杠杆。
*   **计算高效：** 相比需要数值优化的方法（如 Mentored Decoding），Cactus 的近似闭式解几乎不增加推理延迟。

### 8. 不足与局限
*   **超参数敏感性：** 阈值 $\delta$ 的选择可能需要根据具体任务进行微调，以达到最优平衡。
*   **草稿模型依赖：** 尽管优化了接受逻辑，但整体加速效果仍高度依赖于草稿模型与验证模型的匹配程度。
*   **内存占用：** 作为投机采样的一种，它依然需要额外的显存来同时加载草稿模型和维护 KV Cache。

（完）
