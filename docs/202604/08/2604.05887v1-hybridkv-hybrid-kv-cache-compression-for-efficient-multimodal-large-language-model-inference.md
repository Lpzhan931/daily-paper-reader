---
title: "HybridKV: Hybrid KV Cache Compression for Efficient Multimodal Large Language Model Inference"
title_zh: HybridKV：用于高效多模态大语言模型推理的混合 KV 缓存压缩
authors: "Bowen Zeng, Feiyang Ren, Jun Zhang, Xiaoling Gu, Ke Chen, Lidan Shou, Huan Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05887v1"
tags: ["query:llm"]
score: 10.0
evidence: 混合KV缓存压缩以实现高效推理
tldr: 针对多模态大模型（MLLM）推理中视觉Token导致的KV缓存爆炸问题，本文提出HybridKV压缩框架。该框架通过识别注意力头的异构性，将头分为静态和动态两类，并采用自顶向下的预算分配方案。静态头利用文本先验进行剪枝，动态头采用块检索策略。实验表明，在保持性能的同时，该方法可减少高达7.9倍的内存占用并提升1.52倍的解码速度。
selection_source: fresh_fetch
motivation: MLLM推理中视觉输入产生的海量KV缓存导致了巨大的内存开销和延迟，且现有压缩方法忽略了注意力头之间不同的行为特征。
method: 提出HybridKV框架，通过文本中心注意力对头进行分类，实施自顶向下的预算分配，并结合文本先验剪枝与块检索策略进行混合压缩。
result: 在Qwen2.5-VL-7B上的实验显示，KV缓存内存减少达7.9倍，解码速度提升1.52倍，且在11个基准测试中几乎无性能损失。
conclusion: HybridKV通过针对性地处理不同类型的注意力头，有效解决了MLLM长上下文推理的效率瓶颈。
---

## 摘要
多模态大语言模型 (MLLMs) 推动了文本、图像和视频的统一推理，但其推理过程受到键值 (KV) 缓存快速增长的阻碍。每个视觉输入都会扩展为数千个 token，导致缓存随上下文长度线性增长，并在整个解码过程中驻留在 GPU 显存中，即使在高端 GPU 上也会产生极高的内存开销和延迟。一种常见的解决方案是在固定的分配预算下，以不同粒度压缩缓存：token 级统一丢弃次要 token，层级在不同层之间改变保留量，而头级则在不同注意力头之间重新分配预算。然而，这些方法仅止步于分配，忽略了注意力头的异构行为，而这些行为需要不同的压缩策略。我们提出了 HybridKV，这是一个混合 KV 缓存压缩框架，分三个阶段整合了互补策略：首先利用以文本为中心的注意力将注意力头分类为静态或动态类型；然后采用自顶向下的预算分配方案分层分配 KV 预算；最后，静态头通过文本先验剪枝进行压缩，动态头通过分块检索进行压缩。在 Qwen2.5-VL-7B 上进行的 11 个多模态基准测试实验表明，HybridKV 将 KV 缓存内存减少了高达 7.9 倍，并实现了 1.52 倍的解码加速，且相对于全缓存 MLLM 几乎没有性能下降，甚至有所提升。

## Abstract
Multimodal Large Language Models (MLLMs) have advanced unified reasoning over text, images, and videos, but their inference is hindered by the rapid growth of key-value (KV) caches. Each visual input expands into thousands of tokens, causing caches to scale linearly with context length and remain resident in GPU memory throughout decoding, which leads to prohibitive memory overhead and latency even on high-end GPUs. A common solution is to compress caches under a fixed allocated budget at different granularities: token-level uniformly discards less important tokens, layer-level varies retention across layers, and head-level redistributes budgets across heads. Yet these approaches stop at allocation and overlook the heterogeneous behaviors of attention heads that require distinct compression strategies. We propose HybridKV, a hybrid KV cache compression framework that integrates complementary strategies in three stages: heads are first classified into static or dynamic types using text-centric attention; then a top-down budget allocation scheme hierarchically assigns KV budgets; finally, static heads are compressed by text-prior pruning and dynamic heads by chunk-wise retrieval. Experiments on 11 multimodal benchmarks with Qwen2.5-VL-7B show that HybridKV reduces KV cache memory by up to $7.9\times$ and achieves $1.52\times$ faster decoding, with almost no performance drop or even higher relative to the full-cache MLLM.

---

## 论文详细总结（自动生成）

### 论文总结：HybridKV —— 用于高效多模态大语言模型推理的混合 KV 缓存压缩

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理高分辨率图像和视频时，视觉输入会转化为数千个 Token，导致键值（KV）缓存随上下文长度线性爆炸。这不仅占用了海量的 GPU 显存（例如 72B 模型处理 20 张图片需 13GB 缓存），还因频繁的内存访问显著增加了推理延迟。
现有的压缩方法（如 Token 级、层级或头级分配）主要关注“分配多少预算”，但忽略了**注意力头（Attention Heads）行为的异构性**。论文发现，不同注意力头在推理时表现出不同的模式：有些是“静态”的（关注点固定），有些是“动态”的（关注点随解码演变）。因此，单一的压缩策略无法兼顾效率与精度。

#### 2. 方法论：核心思想与关键技术
论文提出了 **HybridKV** 框架，其核心思想是“针对不同类型的注意力头实施不同的压缩策略”。具体分为三个阶段：
*   **以文本为中心的头分类（Text-centric Classification）：** 在预填充（Prefill）阶段，利用文本 Token 作为锚点，计算“文本中心注意力稀疏得分”。得分高的归类为**静态头**（关注点稳定），得分低的归类为**动态头**（关注点分散且易变）。
*   **自顶向下的预算分配（Top-down Budget Allocation）：** 
    *   **类型级分配：** 在静态头和动态头之间分配总预算。
    *   **个体级分配：** 对静态头根据其稀疏得分进行自适应分配（重要的头多给预算）；对动态头则采用均匀分配以适配高效的 CUDA 实现。
*   **混合压缩策略（Hybrid Compression）：**
    *   **静态头：** 采用“文本优先剪枝”，保留所有文本 Token、局部窗口 Token 以及最显著的视觉 Token，直接驻留在 GPU。
    *   **动态头：** 采用“分块动态检索”，将 KV 缓存卸载（Offload）到 CPU，解码时根据查询向量动态检索最相关的块并加载回 GPU。

#### 3. 实验设计
*   **模型：** Qwen2.5-VL (3B/7B), LLaVA-OneVision-7B。
*   **数据集/基准：** 
    *   **图像任务（MileBench）：** 包含 DocVQA、WebQA、WikiVQA、MM-QA 等 8 个涉及多图推理和复杂问答的任务。
    *   **视频任务：** VATEX（视频描述）、NextQA（时间因果推理）、Video-ChatGPT（对话能力）。
*   **对比方法：** SnapKV、LOOK-M（Token 级）、MADA KV（层级）、SparseMM（头级剪枝）。

#### 4. 资源与算力
*   **硬件：** 实验主要在 **NVIDIA L40S GPU** 上运行。
*   **算力细节：** 论文未详细说明总训练时长（因为该方法是 Plug-and-play，无需重新训练），但提供了推理效率数据：在 10% 预算下，Qwen2.5-VL-7B 的解码延迟从 58.94 ms/token 降至 38.65 ms/token。

#### 5. 实验数量与充分性
*   **实验规模：** 在 11 个多模态基准测试上进行了广泛评估，涵盖了图像和视频两大领域。
*   **消融实验：** 针对“头分类”的有效性、“预算分配策略”的影响以及“超参数敏感性（如阈值 $\theta$ 和分配系数 $r$）”进行了详细对比。
*   **客观性：** 实验对比了多种不同粒度的前沿压缩方案，并使用了 ROUGE-L、BLEU-4、METEOR、GPT-4o-mini 评分等多种客观指标，实验设计较为全面、公正。

#### 6. 主要结论与发现
*   **高效压缩：** HybridKV 在仅保留 **10%** KV 缓存的情况下，性能几乎无损，甚至在某些视频任务（如 VATEX）上超过了全缓存模型（Full Cache），这表明压缩过程起到了“去噪”作用。
*   **性能提升：** 相比全缓存模型，HybridKV 实现了高达 **7.9 倍** 的显存节省和 **1.52 倍** 的解码加速。
*   **异构性验证：** 实验证实了将注意力头分为静态和动态并匹配不同策略的必要性，单一策略（全动态或全静态）都会导致精度下降或延迟增加。

#### 7. 优点（亮点）
*   **上下文感知：** 分类和分配过程是动态的，能根据输入内容（Task-dependent）自动调整，比离线统计方法更灵活。
*   **策略互补：** 结合了剪枝（快）和检索（准）的优点，解决了传统方法在极端压缩比下性能骤降的问题。
*   **即插即用：** 无需对模型进行微调或重新训练，具有很强的实用性。

#### 8. 不足与局限
*   **分类依赖阈值：** 头分类依赖于预设的稀疏得分阈值，虽然论文做了敏感性分析，但在不同规模或架构的模型上可能需要重新调优。
*   **CPU-GPU 通信开销：** 动态检索涉及 CPU 到 GPU 的数据传输，虽然采用了分块优化，但在带宽受限的系统上可能成为瓶颈。
*   **模态局限：** 目前主要针对图像和视频，尚未探讨在音频或其他长文本模态下的表现。

（完）
