---
title: Collaborative Multi-Mode Pruning for Vision-Language Models
title_zh: 视觉-语言模型的协同多模式剪枝
authors: "Zimeng Wu, Yunhong Wang, Donghao Wang, Jiaxin Chen"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02956v1"
tags: ["query:llm"]
score: 9.0
evidence: 联合参数与Token剪枝用于模型压缩
tldr: 视觉语言模型（VLM）计算复杂度高，难以在受限设备部署。现有剪枝方法多关注单一模式（参数或Token），在高剪枝率下性能下降严重。本文提出协同多模式剪枝（CoMP）框架，通过联合参数与Token剪枝来减少冗余。核心包括考虑两者相互干扰的协同重要性指标（CIM）和基于成本自适应切换剪枝模式的多模式剪枝策略（MPS），在保持高性能的同时实现了高效压缩。
selection_source: fresh_fetch
motivation: 现有VLM剪枝方法通常只针对参数或Token单一维度，忽略了两者间的冗余耦合，导致高压缩比下性能损失巨大。
method: 提出了包含协同重要性指标（CIM）和多模式剪枝策略（MPS）的CoMP框架，实现了参数与Token的联合优化剪枝。
result: 在多种视觉语言任务和模型上的实验表明，CoMP在高剪枝率下的表现优于现有的最先进方法。
conclusion: 协同考虑参数与Token的冗余并采用动态切换策略，是提升视觉语言模型压缩效率和性能的关键。
---

## 摘要
视觉-语言模型 (VLMs) 在统一的 Transformer 架构下取得了飞速发展，但由于计算复杂度高，在资源受限设备上的部署仍然具有挑战性。虽然剪枝已成为压缩 VLMs 的有效技术，但现有方法主要集中在单一模式上，即仅剪枝参数或仅剪枝 token，忽略了对每种模式中固有冗余的充分探索，这导致在高剪枝率下性能大幅下降。为了解决上述局限性，我们提出了协同多模式剪枝 (CoMP)，这是一个专门为 VLMs 设计的新型框架，通过执行联合参数和 token 剪枝。具体而言，我们首先设计了一种协同重要性指标 (CIM)，用于研究耦合参数和 token 之间的相互干扰。它将 token 的不同显著性纳入参数重要性分数的计算中，同时减轻了被剪枝参数对 token 重要性分数的影响。此外，我们开发了一种多模式剪枝策略 (MPS)，将整个剪枝过程分解为一系列剪枝阶段，在每个阶段中，我们根据剪枝成本估计不同剪枝模式的优先级，并自适应地切换到最优模式。此外，MPS 整合了历史成本和随机探索，以实现稳定的剪枝过程并避免局部最优。在各种视觉-语言任务和模型上的广泛实验表明，与最先进的方法相比，我们的方法在剪枝率较高的情况下有效地提升了性能。源代码可在 https://github.com/Wuzimeng/CoMP.git 获取。

## Abstract
Vision-Language Models (VLMs) have advanced rapidly within the unified Transformer architecture, yet their deployment on resource-constrained devices remains challenging due to high computational complexity. While pruning has emerged as an effective technique for compressing VLMs, existing approaches predominantly focus on a single mode by pruning either parameters or tokens, neglecting fully exploring the inherent redundancy in each mode, which leads to substantial performance degradation at high pruning ratios. To address the above limitations, we propose Collaborative Multi-Mode Pruning (CoMP), a novel framework tailored for VLMs by performing joint parameter and token pruning. Specifically, we first design a Collaborative Importance Metric (CIM) that investigates the mutual interference between the coupled parameters and tokens. It incorporates distinct significance of tokens into the computation of parameter importance scores, while simultaneously mitigating the affect of pruned parameters on token importance scores. Moreover, we develop a Multi-Mode Pruning Strategy (MPS) that decomposes the overall pruning process into a sequence of pruning stages, while in each stage we estimate the priory of different pruning modes based on their pruning cost and adaptively shift to the optimal one. Additionally, MPS integrates the historical cost and random exploration, in order to achieve a stable pruning process and avoid local optimum. Extensive experiments across various vision-language tasks and models demonstrate that our method effectively promotes the performance under high pruning ratios by comparing to the state-of-the-art approaches. The source code is available at https://github.com/Wuzimeng/CoMP.git.

---

## 论文详细总结（自动生成）

这是一份关于论文《Collaborative Multi-Mode Pruning for Vision-Language Models》（视觉-语言模型的协同多模式剪枝）的结构化总结：

### 1. 核心问题与整体含义
视觉-语言模型（VLMs）虽然性能强大，但其基于 Transformer 的架构带来了巨大的计算开销，限制了在移动端等资源受限设备上的部署。
*   **研究动机**：现有的剪枝技术通常分为**参数剪枝**（减少特征维度）和 **Token 剪枝**（减少序列长度）。然而，现有方法大多只关注单一模式，忽略了参数与 Token 之间存在的固有冗余耦合。
*   **核心挑战**：简单地将两者结合会遇到两个问题：1) **重要性度量不一致**（不重要的 Token 可能会干扰参数重要性的评估，反之亦然）；2) **剪枝模式应用僵化**（在不同剪枝阶段，最优的剪枝对象——是视觉还是语言，是参数还是 Token——是动态变化的）。

### 2. 方法论：CoMP 框架
论文提出了 **CoMP (Collaborative Multi-Mode Pruning)** 框架，核心包含两个关键模块：

*   **协同重要性指标 (CIM)**：旨在消除参数与 Token 评估之间的相互干扰。
    *   **Token 加权参数重要性**：在计算参数重要性分数时，引入 Token 的显著性权重。通过抑制不重要 Token 的贡献，使参数重要性评估更聚焦于核心输入数据。
    *   **自修正 Token 重要性**：在评估 Token 重要性时，直接将参数剪枝的掩码（Mask）应用到注意力权重矩阵中。这可以防止那些已被判定为冗余的注意力头（Heads）产生的均匀分布噪声干扰 Token 的筛选。
*   **多模式剪枝策略 (MPS)**：一种自适应的动态调度机制。
    *   **成本感知模式切换**：将剪枝过程分为多个阶段，定义了五种模式（视觉/语言/跨模态参数、视觉/语言 Token）。每一轮通过计算“性能下降/计算量减少”的比例（即剪枝成本）来决定下一轮优先剪枝哪种模式。
    *   **探索与历史信息**：为了防止陷入局部最优（例如过度剪枝某一模式），引入了随机探索机制和基于 EMA（指数移动平均）的历史成本记录，确保剪枝过程的稳定性和多样性。

### 3. 实验设计
*   **数据集与场景**：
    *   **任务特定模型 (BLIP/CLIP)**：在 NLVR2 (视觉推理)、COCO/Flickr30K (图文检索)、VQAv2 (问答)、MSR-VTT (视频检索) 上进行测试。
    *   **大语言模型基础的 VLM (LLaVA-v1.5-7B)**：在 MME、MMBench、GQA、TextVQA、POPE 等 6 个综合基准上评估。
*   **Benchmark 与对比方法**：
    *   **参数剪枝**：UPop, M-Pruning。
    *   **Token 剪枝**：MADTP, STP, Turbo, FastV, SparseVLM, PDrop, VisionZip, DART, DivPrune。
    *   **联合剪枝**：简单顺序组合（如 UPop → MADTP）。

### 4. 资源与算力
*   **硬件设备**：
    *   对于 BLIP 和 CLIP 模型的实验，使用了 **2 张 NVIDIA A800 GPU**。
    *   对于 LLaVA-v1.5-7B 的实验，使用了 **8 张 NVIDIA A800 GPU**。
*   **训练时长**：
    *   在 BLIP-NLVR2 任务上，CoMP 的总耗时约为 6.7 小时（包含剪枝和微调），介于纯 Token 剪枝（5.8h）和纯参数剪枝（12.3h）之间。
    *   LLaVA 模型的压缩在 1 个 Epoch 的指令微调（SFT）内完成。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从传统 VLM 到最新的大模型基础 VLM，跨越了图像推理、检索、生成、问答及视频任务，实验组数非常多。
*   **充分性与客观性**：
    *   进行了详尽的**消融实验**，分别验证了 CIM 和 MPS 及其子组件的有效性。
    *   提供了**统计显著性测试**（5 次随机种子实验），证明了性能提升并非偶然。
    *   通过**可视化分析**展示了剪枝过程中模式切换的趋势（早期倾向于剪 Token，后期倾向于剪参数），符合直觉且具有说服力。

### 6. 主要结论与发现
*   **高剪枝率优势**：CoMP 在中低剪枝率下表现稳健，但在**高剪枝率（如 0.7-0.85）**下优势极其明显，显著超越了所有单一模式方法。
*   **协同效应**：证明了参数和 Token 剪枝是互补的。通过 CIM 修正后的指标能更精准地定位冗余。
*   **动态性**：实验发现模型在不同阶段的冗余分布不同，动态切换模式比固定顺序剪枝效果更好。

### 7. 优点与亮点
*   **视角新颖**：首次深入探讨了 VLM 中参数与 Token 剪枝之间的相互干扰问题。
*   **自适应强**：MPS 策略无需人工预设复杂的剪枝比例，能够根据模型状态自动平衡各模态、各组件的压缩程度。
*   **通用性广**：不仅适用于中等规模模型，也能无缝扩展到 LLaVA 等大规模多模态模型。

### 8. 不足与局限
*   **搜索开销**：MPS 策略在剪枝阶段需要频繁在验证集上评估性能以计算“成本”，这虽然比重新训练快，但相比于训练前一次性确定比例的方法，增加了剪枝过程的时间成本。
*   **验证集依赖**：MPS 的有效性依赖于一个高质量且具有代表性的验证集，对于某些缺乏标注数据的垂直领域任务，可能难以准确估计剪枝成本。
*   **硬件加速限制**：虽然结构化剪枝对硬件友好，但 Token 剪枝带来的动态序列长度在某些推理框架上可能需要额外的优化才能实现理论上的加速比。

（完）
