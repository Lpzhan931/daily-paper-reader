---
title: "ZoomR: Memory Efficient Reasoning through Multi-Granularity Key Value Retrieval"
title_zh: ZoomR：通过多粒度键值检索实现内存高效的推理
authors: "David H. Yang, Yuxuan Zhu, Mohammad Mohammadi Amiri, Keerthiram Murugesan, Tejaswini Pedapati, Subhajit Chaudhury, Pin-Yu Chen"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10898v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对长输出的动态KV缓存选择与压缩
tldr: 针对大语言模型在复杂推理任务中因生成长链条中间思考而导致的KV缓存内存瓶颈，本文提出ZoomR。该方法通过将推理过程自适应压缩为摘要，并采用多粒度KV检索策略，在解码时利用摘要作为粗粒度索引来动态调取关键细节。实验表明，ZoomR在保持数学和推理任务性能的同时，将推理内存需求降低了4倍以上，显著提升了长输出生成的效率。
selection_source: fresh_fetch
motivation: 大模型在推理时生成的长中间过程会导致KV缓存随输出长度剧增，而现有优化多关注输入端而非输出端的内存压力。
method: 引入ZoomR方法，通过将推理步骤压缩为摘要并结合动态KV选择策略，实现对细粒度细节的按需“缩放”检索。
result: 在多项数学和推理任务中，该方法在保证模型性能的前提下，成功将推理过程中的内存占用降低了4倍以上。
conclusion: 多粒度KV检索策略证明了在不牺牲推理能力的情况下，可以大幅优化长输出生成的内存效率。
---

## 摘要
大语言模型（LLMs）在复杂推理任务中表现优异，但在得出最终答案前往往需要生成冗长的中间思考过程。在生成过程中，LLMs 依赖键值（KV）缓存进行自回归解码。然而，KV 缓存的内存占用随输出长度的增加而增长。以往关于 KV 缓存优化的工作大多集中在压缩长输入上下文，而在解码时保留完整的 KV 缓存。对于需要长输出生成的任务，这会导致计算和内存成本增加。在本文中，我们提出了 ZoomR，这是一种新颖的方法，它使 LLMs 能够自适应地将冗长的推理思考压缩为摘要，并采用一种动态 KV 缓存选择策略，在利用这些摘要的同时，策略性地“放大”细粒度细节。通过在解码过程中将摘要键作为粗粒度索引，ZoomR 利用查询仅检索最重要思考的细节。这种分层策略通过避免在每一步进行全缓存注意力计算，显著减少了内存使用。在数学和推理任务上的实验表明，与基准模型相比，我们的方法在保持竞争性性能的同时，将推理内存需求降低了 4 倍以上。这些结果证明，多粒度 KV 选择能够实现更高效的内存解码，尤其是在长输出生成场景下。

## Abstract
Large language models (LLMs) have shown great performance on complex reasoning tasks but often require generating long intermediate thoughts before reaching a final answer. During generation, LLMs rely on a key-value (KV) cache for autoregressive decoding. However, the memory footprint of the KV cache grows with output length. Prior work on KV cache optimization mostly focus on compressing the long input context, while retaining the full KV cache for decoding. For tasks requiring long output generation, this leads to increased computational and memory costs. In this paper, we introduce ZoomR, a novel approach that enables LLMs to adaptively compress verbose reasoning thoughts into summaries and uses a dynamic KV cache selection policy that leverages these summaries while also strategically "zooming in" on fine-grained details. By using summary keys as a coarse-grained index during decoding, ZoomR uses the query to retrieve details for only the most important thoughts. This hierarchical strategy significantly reduces memory usage by avoiding full-cache attention at each step. Experiments across math and reasoning tasks show that our approach achieves competitive performance compared to baselines, while reducing inference memory requirements by more than $4\times$. These results demonstrate that a multi-granularity KV selection enables more memory efficient decoding, especially for long output generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《ZoomR: Memory Efficient Reasoning through Multi-Granularity Key Value Retrieval》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）在执行复杂推理（如思维链 CoT）时，往往需要生成极长的中间思考过程。由于自回归解码依赖键值（KV）缓存，且缓存大小随输出长度线性增长，这导致了巨大的 GPU 内存压力和计算开销，限制了在消费级硬件上处理长输出推理任务的能力。
*   **研究背景**：现有的 KV 缓存优化技术（如 SnapKV、H2O）大多侧重于压缩**输入上下文**（Prefill 阶段），而忽略了**长输出生成**（Decoding 阶段）带来的内存瓶颈。
*   **整体含义**：论文提出了一种名为 **ZoomR** 的方法，模仿人类记忆的分层机制（保留大纲，按需调取细节），通过动态的多粒度检索策略，在保证推理准确性的同时大幅降低内存占用。

### 2. 核心方法论
ZoomR 结合了轻量级微调和推理时的动态缓存管理，核心流程如下：
*   **推理摘要化（Reasoning with Summarization）**：
    *   首先通过微调（LoRA）使模型学会在每段推理后自动生成简短摘要（使用特定的起始/结束标签）。
    *   训练数据通过 Llama3-70B 对现有的推理数据集（如 Bespoke-17K）进行段落级摘要增强。
*   **多粒度 KV 检索算法**：
    1.  **表示（Representation）**：将每个摘要段落的 Key 向量取平均，生成“平均摘要键”（Mean Summary Key），常驻 GPU。
    2.  **评分与投票（Scoring & Voting）**：在解码每一步，用当前的 Query 与所有历史摘要键计算注意力分数。每个注意力头选出得分最高的 Top-$k$ 个摘要进行“投票”。
    3.  **全局共识（Consensus）**：汇总所有层和头的投票，选出得票最高的 Top-$c$ 个摘要。
    4.  **动态缩放（Zooming In）**：对于进入“共识集”的摘要，从 CPU 调取其对应的**原始细粒度 KV** 加载至 GPU；对于其他相关但未达共识的摘要，仅保留其**压缩后的摘要 KV**。
*   **系统实现**：采用 CPU-GPU 异步流水线，将全量 KV 存储在 CPU 内存中，仅将选中的多粒度子集加载到 GPU 进行计算。

### 3. 实验设计
*   **数据集/场景**：
    *   **MATH500**：数学竞赛题目。
    *   **GPQA Diamond**：高难度科学常识。
    *   **AIME2025**：美国数学邀请赛最新题目。
*   **基准模型（Backbones）**：基于 DeepSeek-R1-Distill 的 Qwen2.5-7B 和 Llama-3.1-8B。
*   **对比方法**：
    *   **Vanilla**：全量 KV 缓存（性能上限）。
    *   **StreamingLLM**：基于滑动窗口和注意力锚点的静态策略。
    *   **H2O**：基于重要性得分的动态 Token 剔除策略。
    *   **SumR**：ZoomR 的变体，仅使用摘要 KV，不进行细节“放大”。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA H100 GPU** 上运行。
*   **微调参数**：使用 LoRA（Rank=16, Alpha=32），训练 3 个 Epoch，最大上下文长度 16,384。
*   **数据准备**：使用 Llama3-70B 生成摘要标签。
*   **说明**：文中未明确给出具体的总训练时长（小时/天），但提到微调是“一次性且轻量级”的。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流推理榜单上进行了完整测试，涵盖了不同架构（Qwen 和 Llama）。
*   **消融实验**：针对核心超参数 $c$（共识段落数）和 $k$（每头选择数）进行了详细分析，验证了“放大”机制对准确性的贡献。
*   **分析深度**：引入了“共识动态性”（Agreeability）分析，探讨了正确答案与错误答案在注意力头共识度上的差异，增加了实验的解释性。
*   **客观性**：通过设定相同的内存预算（Budget）来对比 H2O 和 StreamingLLM，确保了对比的公平性。

### 6. 主要结论与发现
*   **内存效率**：ZoomR 相比全量 GPU 缓存节省了 **20 倍以上**的内存；在采用 CPU Offloading 的情况下，比标准方法节省了 **4 倍以上**的 GPU 内存。
*   **推理性能**：在 AIME2025 等高难度任务上，ZoomR 的表现显著优于 StreamingLLM 和 H2O，且非常接近全量缓存的 Vanilla 表现（例如 Llama 模型在 AIME 上达到 26.7% 的满分水平）。
*   **共识规律**：研究发现，**正确推理路径**在早期往往具有更高的注意力头共识度（Agreeability），且稳定性更高；而错误路径的共识度波动剧烈。

### 7. 优点与亮点
*   **多粒度创新**：不同于非黑即白的“保留或丢弃”，ZoomR 提供了“摘要+细节”的折中方案，解决了长程推理中的信息瓶颈问题。
*   **认知启发**：方法论受人类分层记忆启发，具有较强的直观合理性。
*   **即插即用**：虽然需要微调，但推理逻辑是动态的，且能显著提升小显存设备处理长推理链的能力。

### 8. 不足与局限
*   **吞吐量权衡**：由于涉及 CPU 与 GPU 之间的频繁数据交换，ZoomR 的**生成速度（Throughput）**低于全量 GPU 缓存模式，受限于 PCIe 带宽。
*   **摘要依赖性**：性能高度依赖于微调阶段生成的摘要质量，若摘要无法捕捉关键逻辑，则“放大”机制可能失效。
*   **领域局限**：目前主要在数学和科学推理任务上验证，尚未在代码生成、长文本创作等其他长输出场景中测试。
*   **固定粒度**：摘要的切分目前固定在段落级别，缺乏更灵活的自适应粒度划分。

（完）
