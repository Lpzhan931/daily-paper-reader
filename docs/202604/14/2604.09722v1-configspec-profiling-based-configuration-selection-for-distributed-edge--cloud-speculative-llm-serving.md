---
title: "ConfigSpec: Profiling-Based Configuration Selection for Distributed Edge--Cloud Speculative LLM Serving"
title_zh: ConfigSpec：面向分布式边缘-云投机性 LLM 服务的基于性能分析的配置选择
authors: "Xiangchen Li, Saeid Ghafouri, Jiakun Fan, Babar Ali, Hans Vandierendonck, Dimitrios S. Nikolopoulos"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.09722v1"
tags: ["query:llm"]
score: 10.0
evidence: 分布式投机大模型服务的配置选择
tldr: ConfigSpec是一个针对边缘-云分布式投机采样LLM推理的配置选择框架。它通过分析边缘设备性能和草稿模型与目标模型的对齐度，对吞吐量、接受率和功耗进行建模，以解决由草稿模型变体、量化级别和投机长度构成的复杂配置空间优化问题。研究发现，吞吐量、成本和能效的最优配置存在结构性冲突，证明了基于分析的动态配置选择在异构边缘环境中的必要性。
selection_source: fresh_fetch
motivation: 在边缘-云协作的投机解码中，由于配置空间巨大且边缘设备异构，难以手动确定能平衡性能、成本和能效的最优参数。
method: 开发了ConfigSpec框架，通过对边缘设备和模型对齐情况进行画像，建模并评估不同配置下的有效吞吐量、成本效率和能效。
result: 实验发现吞吐量在小型快速模型和特定长度下达到最优，而成本和能效则分别倾向于高接受率的大模型和低功耗的小模型。
conclusion: 由于不同优化目标之间存在结构性冲突，单一固定配置无法实现全局最优，必须采用基于画像的配置选择策略。
---

## 摘要
投机解码（Speculative decoding）通过将轻量级的 Token 草拟（drafting）与重量级的验证（verification）分离，实现了跨云端和边缘的大语言模型（LLM）协同推理。尽管现有系统展示了性能和成本优势，但实际部署需要应对巨大的配置空间，涵盖草拟模型变体、量化级别、投机长度以及异构边缘设备。本文提出了 ConfigSpec，一个用于分布式投机性 LLM 服务的配置选择框架。ConfigSpec 对边缘设备和草拟-目标模型对齐进行性能分析，并对草拟吞吐量、接受率和功耗进行建模，以评估联合配置空间中的有效吞吐量（goodput）、验证成本效率和能源效率。我们在三个边缘平台和两个 LLM 家族上的分析揭示了结构性冲突的最优点。首先，在依赖于设备的投机长度（K*=2-10）下，最小、最快的草拟模型可实现有效吞吐量的最大化。其次，由于显著的奖励 Token（bonus-token）效应，成本和能源效率均收敛于 K=2——其中成本因高接受率而倾向于最大的草拟模型，而能源因低功耗而倾向于最小的草拟模型。这些冲突证实了没有任何单一的固定配置能同时优化所有目标，强调了在解耦的边缘-云 LLM 推理中进行基于性能分析的配置选择的必要性。

## Abstract
Speculative decoding enables collaborative Large Language Model (LLM) inference across cloud and edge by separating lightweight token drafting from heavyweight verification. While prior systems show performance and cost benefits, practical deployment requires navigating a large configuration space spanning draft model variants, quantisation levels, speculative lengths, and heterogeneous edge devices. This paper presents ConfigSpec, a configurationselection framework for distributed speculative LLM serving. ConfigSpec profiles edge devices and draft-target alignment, and models drafting throughput, acceptance rate, and power to evaluate goodput, verification cost efficiency, and energy efficiency across the joint configuration space. Our analysis across three edge platforms and two LLM families reveals structurally conflicting optima. Firstly, goodput is maximised by the smallest, fastest draft model at device-dependent speculative lengths (K*=2-10). Secondly, both cost and energy efficiency converge to K=2 due to a dominant bonus-token effect-with cost favouring the largest drafter for its high acceptance rate and energy favouring the smallest for its low power draw. These conflicts confirm that no single fixed configuration can simultaneously optimise all objectives, underscoring the need for profiling-based configuration selection in disaggregated edge-cloud LLM inference.

---

## 论文详细总结（自动生成）

以下是对论文《ConfigSpec: Profiling-Based Configuration Selection for Distributed Edge–Cloud Speculative LLM Serving》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在边缘-云协作的投机解码（Speculative Decoding）场景下，如何从巨大的配置空间（包括草稿模型变体、量化级别、投机长度 $K$ 以及异构边缘硬件）中选择最优配置，以平衡性能、成本和能效。
*   **研究动机**：
    *   **边缘推理受限**：在边缘设备上直接运行高参数 LLM 受到计算和内存限制。
    *   **投机解码的潜力**：通过在边缘运行轻量级草稿模型（Draft Model）生成 Token，在云端使用目标模型（Target Model）验证，可以加速推理。
    *   **配置复杂性**：现有的系统缺乏对配置参数（如 $K$ 值、模型大小）如何影响最终指标（吞吐量、金钱成本、功耗）的系统性理解。不同目标（如追求最快速度 vs. 追求最省钱）之间存在非直观的冲突。

### 2. 方法论
ConfigSpec 框架通过**性能分析（Profiling）**和**解析建模（Analytical Modeling）**来评估配置质量：
*   **核心思想**：通过测量边缘侧的原始指标，结合投机解码的数学模型，预测不同配置下的系统表现。
*   **关键技术细节**：
    *   **性能画像（Profiling）**：测量三个关键原始指标：草拟吞吐量 $v_d$（tokens/s）、草拟-目标接受率 $\alpha(K)$、以及设备功耗 $P$。
    *   **解析模型**：
        *   **有效吞吐量（Goodput, $G$）**：考虑了每轮投机中期望接受的 Token 数（含 1 个奖励 Token）与总延迟（本地草拟时间 + 远程验证延迟 $T_{verify}$）的比值。
        *   **验证成本效率（$\eta_{cost}$）**：基于云端 Token 计费模型，计算每美元能获得的验证 Token 数。
        *   **能源效率（$E$）**：计算边缘设备每生成一个验证 Token 所消耗的焦耳数。
    *   **奖励 Token 效应（Bonus-token Effect）**：模型强调了投机解码中即使所有草拟 Token 被拒绝，目标模型也会生成一个正确 Token 的特性，这在 $K$ 值较小时对效率贡献巨大。

### 3. 实验设计
*   **硬件平台**：
    *   Raspberry Pi 4B (Cortex-A72, 8GB RAM)
    *   Raspberry Pi 5 (Cortex-A76, 8GB RAM)
    *   NVIDIA Jetson AGX Orin (Ampere GPU, 64GB RAM)
*   **模型与软件**：
    *   **目标模型**：Llama-3.1-70B, Qwen3-32B（部署在云端）。
    *   **草稿模型**：Llama-3.2 (1B, 3B), Llama-3.1-8B; Qwen3 (0.6B, 1.7B, 4B, 8B)。
    *   **量化**：使用 llama.cpp 的 GGUF 格式，涵盖 Q4_K_M 到 Q8_0。
*   **数据集**：Databricks Dolly 15K 指令遵循数据集。
*   **对比维度**：在联合配置空间 $(M, Q, K)$ 中穷举搜索，对比不同优化目标下的最优配置。

### 4. 资源与算力
*   **边缘算力**：使用了从低功耗 CPU（树莓派）到高性能边缘 GPU（Jetson Orin）的梯度设备。
*   **云端算力**：文中未明确说明云端具体 GPU 型号，但引用了 Fireworks AI 和 Groq 的商业 API 价格作为验证成本基准（如 $0.90/1M tokens）。
*   **训练时长**：本文专注于推理阶段的配置优化，不涉及模型训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个主流模型家族、3 种硬件平台、多种量化级别以及 $K$ 从 2 到 10 的完整扫描。
*   **充分性**：实验设计较为充分，通过跨平台对比揭示了硬件性能对最优 $K$ 值的影响。
*   **客观性**：使用了标准化的解析公式和真实的商业定价数据，评估过程具有较强的客观性和可重复性。

### 6. 主要结论与发现
*   **结构性冲突**：
    1.  **吞吐量（Goodput）**：倾向于选择**最小、最快**的草稿模型。最优 $K^*$ 随设备性能增加而增大（树莓派 $K^* \approx 2$，Jetson $K^* \approx 8-10$）。
    2.  **成本效率**：倾向于选择**最大**的草稿模型（接受率高）且 $K=2$。因为 $K=2$ 时“奖励 Token”的占比最高，平摊了验证成本。
    3.  **能源效率**：倾向于选择**最小**的草稿模型（功耗低）且 $K=2$。
*   **性能权衡**：在同一设备上，优化吞吐量的配置与优化能效的配置相比，能效可能差 7.8 倍；与优化成本的配置相比，吞吐量可能差 2.9 倍。

### 7. 优点
*   **系统性**：首次深入探讨了分布式投机解码中 $(M, Q, K)$ 联合空间的复杂权衡。
*   **实用性**：ConfigSpec 框架不需要实际部署所有组合，仅需轻量级的画像即可预测性能，极大地降低了部署成本。
*   **洞察力**：揭示了“奖励 Token 效应”在小 $K$ 值下对成本和能效的决定性作用。

### 8. 不足与局限
*   **网络假设**：模型中对云端验证延迟 $T_{verify}$ 的处理较为简化，未深入探讨网络抖动（Jitter）和带宽波动对分布式投机的影响。
*   **计费模型单一**：主要基于 Token 计费模型，未考虑预留实例或按时计费等其他云端商业模式。
*   **动态性不足**：目前主要是在部署前进行静态配置选择，尚未实现根据实时网络或负载情况进行动态调整。

（完）
