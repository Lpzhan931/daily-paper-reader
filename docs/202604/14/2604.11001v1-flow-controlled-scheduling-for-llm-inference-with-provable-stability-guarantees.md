---
title: Flow-Controlled Scheduling for LLM Inference with Provable Stability Guarantees
title_zh: 具有可证明稳定性保证的大语言模型推理流量控制调度
authors: "Zhuolun Dong, Junyu Cao"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11001v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过流量控制调度优化大模型服务的吞吐量与稳定性
tldr: 针对大语言模型推理中生成长度未知导致的内存溢出和系统不稳定问题，本文提出了一种具有可证明稳定性保证的流量控制调度框架。该框架通过控制提示词进入活跃集的速率来管理KV缓存，并推导了系统稳定的充要条件。实验证明，该方法在提升吞吐量、降低长尾延迟以及稳定内存利用率方面显著优于现有常用策略。
selection_source: fresh_fetch
motivation: LLM推理过程中生成长度不可知，导致内存需求随Token生成动态增长，易引发系统溢出和性能波动。
method: 提出一种流量控制调度框架，通过限制提示词进入活跃集的速率来确保系统稳定性，并提供理论证明。
result: 实验显示该方法实现了更高的Token和请求吞吐量，同时显著降低了平均及尾部延迟。
conclusion: 该研究为LLM推理调度提供了具备理论稳定性保证的高效方案，有效平衡了系统吞吐量与内存安全。
---

## 摘要
大语言模型（LLM）因其在广泛应用中的卓越性能而被广泛采用。ChatGPT 和 Gemini 目前服务于数亿活跃用户，每天处理数十亿次用户请求，这使得优化 LLM 推理成为了关注的焦点。LLM 推理中的一个关键挑战是解码长度是未知的。每个请求的内存占用随生成的 token 增加而增长，这可能导致溢出并引起系统不稳定。为了解决这一问题，我们提出了一个简单的流量控制框架，用于控制提示词（prompts）进入活跃集的速率。我们推导出了任何稳定系统必须满足的必要条件，并确立了我们的算法在何种充分条件下能够证明实现稳定性。实验表明，与实践中常用的策略相比，我们的方法实现了更高的 token 和请求吞吐量、更低的平均延迟和尾部延迟，以及更稳定的 KV 缓存利用率。

## Abstract
Large language models (LLMs) have been widely adopted due to their great performance across a wide range of applications. ChatGPT and Gemini now serve hundreds of millions of active users and handle billions of user requests per day, which puts optimizing LLM inference into the spotlight. A key challenge in LLM inference is that decode lengths are unknown. The memory usage for each request grows with generated tokens, which may lead to overflow and cause system instability. To address this concern, we propose a simple flow-control framework that controls the rate at which prompts join the active set. We derive a necessary condition that any stable system must satisfy and establish sufficient conditions under which our algorithm provably achieves stability. Experiments show that, compared to commonly used strategies in practice, our approach achieves higher token and request throughput, lower average and tail latency, and more stable KV cache utilization.

---

## 论文详细总结（自动生成）

以下是对论文《Flow-Controlled Scheduling for LLM Inference with Provable Stability Guarantees》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于大语言模型（LLM）推理服务中的**内存管理与调度稳定性**问题。
*   **研究动机**：LLM推理具有自回归特性，分为预填充（Prefill）和解码（Decode）阶段。解码长度在请求到达时是未知的，导致KV缓存（Key-Value Cache）的内存占用随时间动态增长。
*   **核心挑战**：随机的请求到达和波动的输出长度会导致GPU内存（KV缓存）频繁溢出。现有的推理引擎（如vLLM、Orca）多采用启发式调度策略，缺乏理论上的稳定性保证，在高负载下容易出现请求被频繁抢占或丢弃，导致延迟激增和系统不稳定。
*   **整体含义**：本文通过排队论视角，将LLM推理建模为随机服务系统，提出了一种通过控制“激活速率”来保证系统稳定性的流量控制框架。

### 2. 方法论
论文提出了一种**流量控制调度框架（Flow-Control Framework）**，其核心思想是“平滑准入”，而非激进地填满批次。
*   **核心思想**：不立即处理所有到达的请求，而是设置一个激活预算（Activation Budget），限制每个时间步进入活跃集（Active Set）开始推理的请求数量。这样可以避免内存占用的瞬时峰值，使KV缓存的使用更加平滑。
*   **关键技术细节**：
    *   **稳定性必要条件**：推导出任何稳定系统必须满足的硬性约束，即期望的总工作量（预填充+解码所需的KV缓存总量）必须小于系统总容量 $M$。
    *   **算法1（已知输出长度）**：针对不同类别的请求设置固定的激活速率 $b_k$，证明了只要 $b_k$ 略大于到达率 $\lambda_k$ 且满足内存约束，系统即为正递归（稳定）。
    *   **算法2（未知输出长度）**：引入启发式的**LIFO（后进先出）抢占机制**。当内存溢出时，优先驱逐最近激活的请求。理论证明，随着内存容量 $M$ 的增加，溢出概率呈指数级衰减。
*   **公式逻辑**：通过 Lyapunov 函数证明了在流量控制下，等待队列长度不会无限增长。

### 3. 实验设计
*   **数据集/场景**：
    1.  **合成数据**：设定三种不同预填充和解码长度组合的请求，模拟受控环境。
    2.  **真实数据**：使用 **LMSYS-Chat-1M** 数据集（包含21万条真实对话），随机抽取5万条请求，模拟真实世界的输入/输出长度分布。
*   **Benchmark（对比方法）**：
    *   **$\alpha$-protection greedy**：设置内存保护阈值的贪婪算法。
    *   **MC / MC-SF**：基于未来内存预测的先到先服务或短作业优先算法。
    *   **Amin**：针对预测不确定性的鲁棒优化算法。
    *   **Hindsight Optimal**：使用 Gurobi 求解整数规划得到的后验最优解（理论上限）。

### 4. 资源与算力
*   **算力说明**：论文主要基于**模拟实验**。
*   **硬件配置**：实验中模拟了单GPU推理系统，KV缓存容量 $M$ 设置为 16492，这与在两张 **NVIDIA A100 GPU** 上部署 **Llama2-70B** 模型的规模相匹配。
*   **模拟工具**：使用了微软的 **Vidur** 模拟框架来精确计算不同批次组合下的处理时间。
*   **训练/运行时间**：文中未明确提及模拟运行的具体时长，但提到使用 Gurobi 求解了复杂的离线优化问题。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对合成数据，测试了从 1000 到 10000 个时间步的不同跨度。
    *   针对真实数据，设置了低负载（$\lambda=10$）和高负载（$\lambda=50$）两种典型场景。
*   **充分性评价**：实验设计较为充分。作者不仅对比了多种最先进的启发式算法，还通过后验最优解（Hindsight Optimal）量化了算法与理论极限的差距。涵盖了已知和未知输出长度两种情况，增强了结果的客观性。

### 6. 主要结论与发现
*   **性能提升**：在所有指标上（Token吞吐量、请求完成率、平均延迟、95%长尾延迟），流量控制算法均优于现有的启发式基准。
*   **稳定性**：通过限制激活速率，KV缓存的利用率曲线更加平滑，极大地减少了因内存溢出导致的请求驱逐和重复计算。
*   **负载适应性**：在高负载场景下，流量控制的优势尤为明显，能够有效防止系统进入“延迟爆炸”的状态。

### 7. 优点
*   **理论与实践结合**：不仅提出了算法，还给出了严谨的排队论证明，填补了LLM调度领域缺乏理论保证的空白。
*   **简单易实现**：算法逻辑简洁（仅需控制激活速率和LIFO驱逐），不需要复杂的模型预测即可显著提升性能。
*   **鲁棒性强**：在输出长度预测不准的情况下，依然能保持接近最优的性能。

### 8. 不足与局限
*   **离散时间假设**：模型将时间离散化，可能简化了预填充阶段的计算开销（虽然实验中用了 Vidur 补偿，但理论模型仍有简化）。
*   **单机局限**：主要针对单工作节点（Single Worker）设计，未深入探讨多GPU流水线并行或张量并行下的复杂通信开销。
*   **上下文忽略**：未考虑多轮对话中的前缀缓存（Prompt Caching）优化，这在实际应用中对内存影响巨大。
*   **参数敏感性**：激活预算 $b$ 的选择对性能有影响，低负载下可能过于保守，需要动态调整机制。

（完）
