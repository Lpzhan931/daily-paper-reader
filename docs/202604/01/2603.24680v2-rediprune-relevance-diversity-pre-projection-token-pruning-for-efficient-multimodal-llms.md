---
title: "ReDiPrune: Relevance-Diversity Pre-Projection Token Pruning for Efficient Multimodal LLMs"
title_zh: ReDiPrune：面向高效多模态大语言模型的投影前相关性-多样性 Token 剪枝
authors: "An Yu, Ting Yu Tsai, Zhenfei Zhang, Weiheng Lu, Felix X. -F. Ye, Ming-Ching Chang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24680v2"
tags: ["query:llm"]
score: 8.0
evidence: 多模态大语言模型的高效Token剪枝
tldr: 针对多模态大模型计算开销大的问题，本文提出ReDiPrune，一种在投影层前进行的免训练视觉Token剪枝方法。该方法通过结合文本相关性和最大最小多样性评分，直接从视觉编码器输出中筛选关键Token，保留了细粒度的空间和语义信息。ReDiPrune无需重新训练，可即插即用，在多个视频和图像基准测试中显著提升了效率并保持甚至提高了准确率。
selection_source: fresh_fetch
motivation: 多模态大模型中大量的视觉Token导致了巨大的计算开销，且现有的投影后剪枝方法容易丢失细粒度信息。
method: 提出一种在投影前应用的轻量级规则，通过联合考虑文本相关性和多样性来筛选非冗余且与查询相关的视觉Token。
result: "在多个基准测试中实现了更优的精度-效率平衡，例如在EgoSchema上仅保留15%的Token即可提升2%的准确率并减少6倍以上的计算量。"
conclusion: ReDiPrune证明了在投影前进行基于相关性和多样性的Token剪枝是提升多模态大模型效率且不损性能的有效途径。
---

## 摘要
最近的多模态大语言模型计算开销巨大，因为 Transformer 必须处理大量的视觉 Token。我们提出了 ReDiPrune，这是一种在视觉-语言投影器（projector）之前应用的无需训练的 Token 剪枝方法，此时视觉特征仍保持着丰富的判别性。与在压缩表示上运行的投影后剪枝方法不同，ReDiPrune 直接从视觉编码器输出中选择信息丰富的 Token，从而保留了细粒度的空间和语义线索。每个 Token 都由一个轻量级规则进行评分，该规则共同考虑了文本条件下的相关性和最大最小多样性，确保所选 Token 既与查询相关又非冗余。ReDiPrune 完全是即插即用的，不需要重新训练或修改架构，并且可以无缝插入到编码器和投影器之间。在四个视频和五个图像基准测试中，它一致地改善了准确率与效率之间的权衡。例如，在 EgoSchema 基准上使用 LLaVA-NeXT-Video-7B 时，仅保留 15% 的视觉 Token 即可获得 +2.0% 的绝对准确率提升，同时将 TFLOPs 计算量减少 6 倍以上。代码可在 https://github.com/UA-CVML/ReDiPrune 获取。

## Abstract
Recent multimodal large language models are computationally expensive because Transformers must process a large number of visual tokens. We present ReDiPrune, a training-free token pruning method applied before the vision-language projector, where visual features remain rich and discriminative. Unlike post-projection pruning methods that operate on compressed representations, ReDiPrune selects informative tokens directly from vision encoder outputs, preserving fine-grained spatial and semantic cues. Each token is scored by a lightweight rule that jointly consider text-conditioned relevance and max-min diversity, ensuring the selected tokens are both query-relevant and non-redundant. ReDiPrune is fully plug-and-play, requiring no retraining or architectural modifications, and can be seamlessly inserted between the encoder and projector. Across four video and five image benchmarks, it consistently improves the accuracy-efficiency trade-off. For example, on EgoSchema with LLaVA-NeXT-Video-7B, retaining only 15% of visual tokens yields a +2.0% absolute accuracy gain while reducing computation by more than $6\times$ in TFLOPs. Code is available at https://github.com/UA-CVML/ReDiPrune.

---

## 论文详细总结（自动生成）

### ReDiPrune：面向高效多模态大语言模型的投影前相关性-多样性 Token 剪枝论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理高分辨率图像或长视频时，会产生数以千计的视觉 Token，导致 Transformer 的注意力机制计算开销呈平方级增长，严重限制了推理速度并增加了内存消耗。
*   **现有方法的局限：** 现有的 Token 剪枝方法大多在“投影层（Projector）之后”进行，此时视觉特征已与语言空间对齐并被压缩，容易丢失细粒度的空间和语义细节；或者需要昂贵的重新训练（如 TRIM）。
*   **研究目标：** 提出一种**免训练（Training-free）**、**投影前（Pre-projection）**的剪枝方案，在视觉特征最丰富、最具判别性的阶段进行筛选，以实现更高的效率和更好的性能平衡。

#### 2. 核心方法论
ReDiPrune 的核心思想是在视觉编码器输出之后、投影层之前，通过一个轻量级的评分规则筛选出既与用户查询相关又具有视觉多样性的 Token 子集。
*   **文本驱动的查询构建：** 对用户提示词（Prompt）进行加权聚合（推荐使用指数加权，以突出末尾的关键动词和实体），生成查询向量 $q$。
*   **相关性评分（Relevance）：** 计算每个视觉 Token 与查询向量 $q$ 之间的余弦相似度 $s_i$。
*   **多样性评分（Diversity）：** 计算 Token 之间的余弦不相似度 $D_{ij}$，以避免选择重复的背景或冗余信息。
*   **统一选择目标：** 采用贪婪算法求解一个联合优化目标：在满足预算 $k$ 的前提下，迭代选择使“与已选集合的最小距离”和“文本相关性”之和最大的 Token。
*   **即插即用：** 该模块直接插入编码器和投影器之间，剪枝后的 Token 恢复原始时空顺序后进入后续流程，无需修改模型架构。

#### 3. 实验设计
*   **数据集：**
    *   **视频任务（4个）：** NextQA, EgoSchema, ActivityNet-QA, Video-ChatGPT。
    *   **图像任务（5个）：** GQA, MMBench, MME, POPE, ScienceQA-IMG。
*   **基准模型（Backbones）：** Video-LLaVA-7B, LLaVA-NeXT-Video-7B, LLaVA-1.5-7B。
*   **对比方法：** 包括原始未剪枝模型、DivPrune（仅多样性）、CDPruner（投影后剪枝）、PruMerge、TRIM、DART 等。

#### 4. 资源与算力
*   **硬件环境：** 实验在 **4 张 NVIDIA A100 (80GB) GPU** 上运行。
*   **训练时长：** 该方法是**免训练（Training-free）**的，因此不需要训练时长。
*   **推理效率：** 论文详细报告了 TFLOPs、预填充时间（Prefill Time）和端到端延迟。例如在 ActivityNet-QA 上，将 Token 从 2056 减至 206，延迟从 0.447s 降至 0.146s。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 9 个主流多模态基准测试，跨越了图像和视频两种模态，实验设置较为全面。
*   **消融实验：** 针对查询加权策略（均匀、自注意力、指数加权等）、剪枝的去噪效果、不同保留率（Keep Ratio）下的性能变化进行了深入分析。
*   **公平性：** 所有对比实验均在统一的硬件和评估框架（lmmsevals）下进行，并使用了相同的 Token 预算进行公平比较。

#### 6. 主要结论与发现
*   **精度不降反升：** 在多个基准上，ReDiPrune 在大幅减少 Token 的情况下，准确率反而有所提升。例如在 EgoSchema 上，保留 15% Token 时准确率提升了 2.0%。
*   **去噪作用：** 剪枝过程实际上起到了“去噪”作用，帮助模型将注意力集中在与问题相关的关键帧和区域上，减少了冗余信息的干扰。
*   **投影前剪枝的优越性：** 相比投影后剪枝，在原始视觉特征空间进行筛选能更好地保留语义保真度。
*   **效率提升显著：** 在视频模型中，计算量（TFLOPs）可降低 6 倍以上，推理速度提升约 2.7 倍。

#### 7. 优点与亮点
*   **高效且低成本：** 无需任何微调或重新训练，部署成本极低。
*   **理论支撑：** 结合了最大最小多样性（Max-Min Diversity）和文本相关性，兼顾了覆盖面和针对性。
*   **通用性强：** 适用于各种 LLaVA 架构的模型，且在长视频理解中表现尤为突出。

#### 8. 不足与局限
*   **架构局限：** 实验主要集中在 LLaVA 风格的架构上，对于其他类型的多模态架构（如基于 Q-Former 或 Perceivers 的模型）的适用性有待验证。
*   **局部优化：** 目前采用的是逐帧（Per-frame）选择策略，尚未实现全局的时空联合优化剪枝。
*   **超参数敏感性：** 虽然提供了默认值，但平衡因子 $\alpha$ 和保留率 $r$ 在不同任务中可能需要微调以达到最优效果。

（完）
