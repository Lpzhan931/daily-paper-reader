---
title: "VecAttention: Vector-wise Sparse Attention for Accelerating Long Context Inference"
title_zh: VecAttention：用于加速长上下文推理的向量级稀疏注意力机制
authors: "Anmin Liu, Ruixuan Yang, Huiqiang Jiang, Bin Lin, Minmin Sun, Yong Li, Chen Zhang, Tao Xie"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29494v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于加速长文本推理的向量级稀疏注意力
tldr: 针对长视频Transformer模型自注意力计算开销大的问题，本文提出VecAttention框架。通过观察到视频注意力图具有显著的垂直向量稀疏模式，该方法动态选择关键向量并利用优化算子进行计算。在保持与全注意力相当精度的前提下，实现了显著的推理加速，为长上下文视频理解与生成提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的粗粒度稀疏注意力方法在处理长视频时存在计算冗余和性能不佳的问题。
method: 提出一种基于垂直向量稀疏模式的注意力机制，通过轻量级重要向量选择和优化内核动态处理关键信息。
result: 在多个视频理解和生成基准测试中，VecAttention比全注意力快2.65倍，比现有SOTA稀疏方法快1.83倍。
conclusion: VecAttention通过向量级稀疏化在长上下文视频任务中实现了优异的精度与效率平衡。
---

## 摘要
长上下文视频理解与生成由于自注意力机制的二次复杂度，给基于 Transformer 的视频模型带来了巨大的计算挑战。虽然现有的稀疏注意力方法采用粗粒度模式来提高效率，但它们通常会导致冗余计算和次优性能。为了解决这一问题，本文提出了 VecAttention，这是一种新颖的向量级稀疏注意力框架，为视频模型实现了卓越的准确率-效率权衡。我们观察到视频注意力图表现出强烈的垂直向量稀疏模式，并进一步证明，与现有的粗粒度稀疏模式相比，这种垂直向量模式提供了始终更好的准确率-稀疏度权衡。基于这一观察，VecAttention 通过最小化内存访问开销的轻量级重要向量选择和优化的向量稀疏注意力内核，动态地选择并仅处理包含信息的垂直向量。在视频理解（VideoMME、LongVideoBench 和 VCRBench）和生成（VBench）任务上的全面评估表明，VecAttention 相比全注意力机制实现了 2.65 倍的加速，相比最先进的稀疏注意力方法实现了 1.83 倍的加速，且准确率与全注意力机制相当。我们的代码可在 https://github.com/anminliu/VecAttention 获取。

## Abstract
Long-context video understanding and generation pose a significant computational challenge for Transformer-based video models due to the quadratic complexity of self-attention. While existing sparse attention methods employ coarse-grained patterns to improve efficiency, they typically incur redundant computation and suboptimal performance. To address this issue, in this paper, we propose \textbf{VecAttention}, a novel framework of vector-wise sparse attention that achieves superior accuracy-efficiency trade-offs for video models. We observe that video attention maps exhibit a strong vertical-vector sparse pattern, and further demonstrate that this vertical-vector pattern offers consistently better accuracy-sparsity trade-offs compared with existing coarse-grained sparse patterns. Based on this observation, VecAttention dynamically selects and processes only informative vertical vectors through a lightweight important-vector selection that minimizes memory access overhead and an optimized kernel of vector sparse attention. Comprehensive evaluations on video understanding (VideoMME, LongVideoBench, and VCRBench) and generation (VBench) tasks show that VecAttention delivers a 2.65$\times$ speedup over full attention and a 1.83$\times$ speedup over state-of-the-art sparse attention methods, with comparable accuracy to full attention. Our code is available at https://github.com/anminliu/VecAttention.

---

## 论文详细总结（自动生成）

以下是对论文《VecAttention: Vector-wise Sparse Attention for Accelerating Long Context Inference》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：长上下文视频模型（包括视频理解 VLM 和视频生成 DiT）面临 Transformer 自注意力机制的**二次复杂度**挑战，导致推理延迟随视频长度增加而剧增。
*   **研究动机**：现有的稀疏注意力方法通常采用**粗粒度**模式（如整行/整列的 Line 模式或矩形块 Block 模式）。作者观察到，视频注意力图中的显著区域实际上呈现出**细粒度、碎片化的垂直向量（Vertical-Vector）**模式。粗粒度模式会包含大量无用的计算冗余，无法达到最优的精度-效率平衡。

### 2. 方法论：核心思想与关键技术
VecAttention 框架的核心思想是**动态选择并仅处理信息量大的垂直向量**，主要包含两个阶段：
*   **重要向量选择（Important-Vector Selection）**：
    *   **查询池化（Query Pooling）**：通过对相邻 Query 进行均值池化（$Q_p$），利用视频 token 的时空相似性来降低重要性估计的开销。
    *   **无排序 minS 过滤器**：放弃了昂贵的 Top-K 或 Top-P 排序操作，提出基于阈值的 `minS` 过滤。它直接在点积得分上操作，通过计算行最大值并减去偏移量 $\alpha$ 来确定保留区域，极大地减少了 GPU 上的分支预测开销。
    *   **分块融合选择（TilingSelect）**：将向量选择逻辑直接融合进分块（Tiled）GEMM 算子中。在 SRAM 中即时计算统计量，避免了将巨大的中间注意力矩阵写回显存（HBM），显著降低了内存带宽压力。
*   **向量稀疏计算（Vector-Sparse Computation）**：
    *   基于选择出的索引，开发了优化的 GPU 内核，动态地从 HBM 中抓取离散的 Key 和 Value 向量进行注意力计算，类似于 FlashAttention 的分片策略，但针对非连续向量访问进行了优化。

### 3. 实验设计
*   **任务场景**：涵盖了**视频理解**（感知、识别、推理）和**视频生成**（保真度、一致性）。
*   **数据集/基准测试**：
    *   视频理解：VideoMME、LongVideoBench、VCRBench。
    *   视频生成：VBench（评估指标包括 PSNR, SSIM, LPIPS）。
*   **对比方法**：
    *   全注意力（Full Attention，基于 FlashAttention-2）。
    *   SOTA 稀疏方法：FlexPrefill（行/块模式）、XAttention（对角线块模式）、AnchorAttention（条带模式）、Sparse VideoGen。
*   **测试模型**：Qwen2.5-VL-7B、InternVL-3.5-8B（理解类）；Wan2.1-14B、HunyuanVideo-13B（生成类）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **NVIDIA A100 (80GB)** GPU 上进行。
*   **算力细节**：文中未明确提及具体的总训练时长（因为该方法是**无需训练（Training-free）**的推理加速方案），但详细记录了不同上下文长度（从 8K 到 128K）下的推理延迟和显存带宽占用情况。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流大模型和 4 个权威基准测试上进行了广泛实验。
*   **消融实验**：针对向量大小（$P_q$）、Key 分块大小（$B_k$）以及分块组大小（$G_k$）进行了详细的参数敏感性分析。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了最终精度，还通过 Pareto 前沿曲线展示了“精度 vs. 速度”和“精度 vs. 稀疏度”的权衡，并提供了注意力图的可视化证据来支撑其“垂直向量模式”的观察。

### 6. 主要结论与发现
*   **垂直向量模式的优越性**：在相同稀疏度下，垂直向量模式的召回率（Recall）显著高于行模式和块模式，最接近理想的 Oracle 模式。
*   **显著加速**：VecAttention 相比全注意力实现了 **2.65 倍**的加速，相比现有最强稀疏方法提升了 **1.83 倍**。
*   **高稀疏度下的稳定性**：在 VideoMME 等任务上，VecAttention 可以在达到 **93% 稀疏度**的情况下，依然保持与全注意力相当的准确率。
*   **跨模态普适性**：观察发现垂直向量模式不仅存在于视频模型，在纯文本 LLM 和多模态 Omni 模型中也广泛存在。

### 7. 优点
*   **算法-系统协同优化**：不仅提出了新的稀疏模式，还通过 TilingSelect 等技术解决了细粒度稀疏带来的内存访问瓶颈。
*   **无需训练**：作为一种即插即用的推理加速技术，不需要对原模型进行任何微调。
*   **粒度适中**：向量级稀疏比块级更精确，比 Token 级更易于硬件加速，找到了极佳的平衡点。

### 8. 不足与局限
*   **应用场景覆盖**：虽然在理解和生成上表现优异，但在更复杂的动态交互场景（如 Agent 推理或长程 RAG）中的泛化能力尚待进一步验证。
*   **模式单一性**：目前主要聚焦于垂直向量，虽然证明了其主导地位，但未来可能需要结合水平或对角线向量来应对更极端的注意力分布。
*   **硬件依赖**：优化的内核高度依赖 NVIDIA GPU 架构（如对 SRAM 和分块计算的利用），在其他硬件平台（如昇腾、AMD）上的移植效率未知。

（完）
