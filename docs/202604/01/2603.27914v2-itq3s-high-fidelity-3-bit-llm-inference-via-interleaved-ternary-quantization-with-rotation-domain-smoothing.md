---
title: "ITQ3_S: High-Fidelity 3-bit LLM Inference via Interleaved Ternary Quantization with Rotation-Domain Smoothing"
title_zh: ITQ3_S：通过旋转域平滑的交错三值量化实现高保真 3-bit 大语言模型推理
authors: Edward J. Yoon
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27914v2"
tags: ["query:llm"]
score: 9.0
evidence: 用于高效大模型推理的3比特权重量化
tldr: 针对大语言模型在3位量化中因权重分布长尾和离群值导致的精度损失问题，本文提出ITQ3_S。该方法结合TurboQuant旋转域策略，利用快速沃尔什-哈达玛变换（FWHT）预旋转权重空间，使分布趋于高斯化。通过将逆变换融合进CUDA共享内存加载阶段，解决了传统方法中的域失配误差。在RTX 5090上，ITQ3_S实现了接近FP16的困惑度，且吞吐量比4位量化提升1.5倍以上，为消费级硬件上的高保真LLM部署提供了有效方案。
selection_source: fresh_fetch
motivation: 传统3位量化方法受限于权重的重尾分布和通道间离群值，导致推理精度大幅下降。
method: 采用FWHT进行旋转域平滑，并将逆变换与IQ3_S格式的CUDA量化内核进行一体化协同设计。
result: 在RTX 5090上实现了与FP16相当的困惑度，且推理吞吐量超过4位量化方案的1.5倍。
conclusion: ITQ3_S是一种兼具数学严谨性与工程实用性的3位量化方案，显著提升了消费级显卡上的LLM推理性能。
---

## 摘要
我们提出了 ITQ3_S（交错三值量化——专用版），这是一种针对大语言模型（LLM）的新型 3-bit 权重量化格式，它集成了 TurboQuant (TQ)，这是一种基于快速沃尔什-阿达玛变换 (FWHT) 的旋转域策略。传统的 3-bit 方法由于重尾权重分布和通道间离群值而遭受精度损失。ITQ3_S 在量化前通过 FWHT 对权重空间进行预旋转，将离群值能量分散到整个向量中，并诱导出一种适用于均匀三值编码的近高斯分布。我们推导了一个严谨的反量化过程，将 256 点逆 FWHT 融合到 CUDA 共享内存加载阶段，确保重构误差仅受限于三值量化网格，而不会产生来自变换逆过程的额外误差。对于任何权重向量 $\mathbf{w} \in \mathbb{R}^{256}$，重构满足 $\|\hat{\mathbf{w}} - \mathbf{w}\|_2 \leq ε_q$，严格小于未利用旋转诱导分布归一化的均匀 3-bit 基准模型。TurboQuant 缺乏原生的 CUDA 算子，阻碍了直接部署；简单地将 TQ 与现有的权重量化器组合会引入域不匹配误差，这些误差会在层间累积，导致质量降至标准 3-bit 基准以下。ITQ3_S 通过将 FWHT 旋转和量化算子协同设计为基于 IQ3_S 权重格式的统一流水线解决了这一问题，并将逆变换融合到 CUDA MMQ 算子中。实验表明，在 NVIDIA RTX 5090 (Blackwell) 上，ITQ3_S 实现了与 FP16 相当的困惑度，同时通过优化的 DP4A 和 Tensor Core 调度，其吞吐量超过了 4-bit 替代方案的 1.5 倍。我们的结果证明 ITQ3_S 是一种在消费级硬件上实现高保真 LLM 部署的实用且具有数学基础的解决方案。

## Abstract
We present ITQ3_S (Interleaved Ternary Quantization -- Specialized), a novel 3-bit weight quantization format for LLMs integrating TurboQuant (TQ), a rotation-domain strategy based on the Fast Walsh-Hadamard Transform (FWHT). Conventional 3-bit methods suffer precision loss from heavy-tailed weight distributions and inter-channel outliers. ITQ3_S pre-rotates the weight space via FWHT before quantization, spreading outlier energy across the vector and inducing a near-Gaussian distribution amenable to uniform ternary coding.   We derive a rigorous dequantization procedure fusing a 256-point Inverse FWHT into the CUDA shared-memory loading stage, ensuring reconstruction error is bounded exclusively by the ternary quantization grid with no additional error from the transform inversion. For any weight vector $\mathbf{w} \in \mathbb{R}^{256}$, the reconstruction satisfies $\|\hat{\mathbf{w}} - \mathbf{w}\|_2 \leq ε_q$, strictly smaller than uniform 3-bit baselines that do not exploit rotation-induced distribution normalization.   TurboQuant lacks a native CUDA kernel, precluding direct deployment; naively composing TQ with existing weight quantizers introduces domain mismatch errors that accumulate across layers, degrading quality below standard 3-bit baselines. ITQ3_S resolves this by co-designing the FWHT rotation and quantization kernel as a unified pipeline grounded in the IQ3_S weight format, with the inverse transform fused into the CUDA MMQ kernel.   Empirically, on the NVIDIA RTX 5090 (Blackwell), ITQ3_S achieves perplexity competitive with FP16 while delivering throughput exceeding 1.5x that of 4-bit alternatives via optimized DP4A and Tensor Core scheduling. Our results establish ITQ3_S as a practical, mathematically grounded solution for high-fidelity LLM deployment on consumer hardware.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ITQ3_S** 的新型 3-bit 权重量化方案，旨在解决大语言模型（LLM）在极低比特量化下的精度退化问题，并实现在消费级显卡上的高效部署。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3-bit 量化通常被认为是 LLM 逻辑能力的“崩溃点”。传统的均匀量化方法在处理 Transformer 权重的**重尾分布（Heavy-tailed）**和**通道间离群值（Outliers）**时，会导致严重的精度损失（困惑度激增、幻觉增加）。
*   **研究动机**：为了缩小 70B 等大模型与消费级 GPU（如 RTX 5090，32GB 显存）之间的显存差距。现有的旋转量化方法（如 QuIP#）虽然理论效果好，但缺乏高效的 CUDA 内核实现，且存在域不匹配导致的误差累积。

### 2. 方法论
*   **核心思想**：利用**快速沃尔什-哈达玛变换（FWHT）**在量化前对权重空间进行确定性旋转。这种旋转能将离群值的能量均匀分散到整个向量中，使权重分布趋于高斯分布，从而更适合均匀的三值编码。
*   **关键技术细节**：
    *   **旋转域平滑**：采用 256 点的 FWHT 作为变换单元。根据中心极限定理，变换后的条目趋向于 $N(0, \sigma^2)$。
    *   **交错三值编码（Interleaved Ternary Coding）**：将 3-bit 值打包进 32-bit 字中，通过交错布局优化 NVIDIA DP4A 指令的吞吐量。
    *   **算子融合（Kernel Fusion）**：将 256 点**逆 FWHT（IFWHT）**直接集成到 CUDA 共享内存加载阶段。这意味着权重在从显存加载到计算单元的过程中完成重构，不产生额外的显存访问开销。
    *   **数学证明**：论文证明了该变换是等距的（Isometry），重构误差仅由量化网格决定，且推导出了高斯分布下的最优三值缩放因子 $\alpha^* \approx 0.798\sigma$。

### 3. 实验设计
*   **模型对象**：LLaMA-3 (8B/70B), Mistral 7B v0.3, Qwen2.5 32B。
*   **数据集**：WikiText-2 和 C4（用于评估困惑度 PPL）。
*   **对比基准（Benchmarks）**：
    *   精度对比：FP16（基准）、Q8_0、Q4_K_M、IQ4_XS、IQ3_S（llama.cpp 原生 3-bit）、QuIP#-3bit。
    *   性能对比：解码（Decode）和预填充（Prefill）的吞吐量（tokens/s）。

### 4. 资源与算力
*   **硬件环境**：主要在 **NVIDIA RTX 5090** (Blackwell 架构，32GB GDDR7) 上进行测试。
*   **算力细节**：论文未详细说明离线量化过程的具体耗时，但强调了推理时的开销。IFWHT 带来的计算开销仅为 2.1%，且利用了 Blackwell 架构的 192KB 共享内存和 DP4A 指令集。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 70B 不同规模的主流模型。
*   **消融实验**：对 FWHT 的块大小（$n=32$ 到 $512$）进行了消融研究，验证了 $n=256$ 是质量与效率的最佳平衡点。
*   **充分性评价**：实验设计较为全面，不仅对比了精度（PPL），还对比了实际推理速度和显存占用，证明了该方法在消费级硬件上的实用性。

### 6. 主要结论与发现
*   **精度提升**：在 LLaMA-3 8B 上，ITQ3_S 将 3-bit 与 FP16 之间的困惑度差距缩小了 57%（相比 IQ3_S 基准）。
*   **性能优异**：在 RTX 5090 上，ITQ3_S 的预填充吞吐量超过了 4-bit 方案的 1.5 倍，解码速度接近 1000 tokens/s。
*   **单卡运行 70B**：ITQ3_S 使得 70B 规模的模型（约 27.3 GiB）能够完全装入单张 32GB 显存的显卡，并留有空间处理约 16K 上下文的 KV Cache。

### 7. 优点
*   **数学严谨性**：提供了关于分布平滑和误差上界的理论证明。
*   **工程实现极佳**：通过 CUDA 内核融合解决了旋转量化的推理延迟问题，将理论优势转化为实际速度。
*   **确定性**：相比 QuIP# 的随机旋转，FWHT 是确定性的，无需存储随机种子，更易于集成。

### 8. 不足与局限
*   **激活量化缺失**：目前仅针对权重量化，未涉及激活量化，在长文本推理时带宽压力仍存。
*   **后处理局限**：属于训练后量化（PTQ），虽然精度高，但若结合量化感知训练（QAT）可能会有更好表现。
*   **架构限制**：对于隐藏层维度不是 256 倍数的非标准模型，需要额外的填充（Padding）策略，这可能影响部分模型的效率。

（完）
