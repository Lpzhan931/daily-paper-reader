---
title: "Neural Garbage Collection: Learning to Forget while Learning to Reason"
title_zh: 神经垃圾回收：在学习推理的同时学习遗忘
authors: "Michael Y. Li, Jubayer Ibn Hamid, Emily B. Fox, Noah D. Goodman"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18002v1"
tags: ["query:llm"]
score: 10.0
evidence: 学习剔除KV缓存条目以提高效率
tldr: 针对思维链推理中KV缓存随步数增长导致的效率瓶颈，本文提出神经垃圾回收（NGC）机制。该方法让语言模型在学习推理的同时，通过强化学习自主学习如何“忘记”不重要的KV缓存条目。模型在推理过程中定期暂停并决定剔除哪些缓存，仅凭任务结果奖励进行端到端优化。实验表明，NGC在保持高准确率的同时实现了2-3倍的缓存压缩，显著优于传统启发式策略。
selection_source: fresh_fetch
motivation: 现有的KV缓存管理多依赖人工设计的启发式规则，限制了模型在长链推理中的扩展性与灵活性。
method: 提出NGC框架，将缓存剔除视为离散动作，利用强化学习根据最终任务奖励同步优化推理逻辑与内存管理策略。
result: 在Countdown、AMC和AIME等任务中，NGC在压缩KV缓存2-3倍的情况下仍保持了接近全量缓存的准确率，优于基准方法。
conclusion: 证明了通过端到端优化可以使模型自主平衡推理能力与内存效率，为构建更高效的推理模型提供了新方向。
---

## 摘要
思维链（Chain-of-thought）推理推动了语言模型能力的显著进步，但每一个推理步骤都会增加 KV 缓存（KV cache），从而形成了进一步扩展该范式的瓶颈。目前的方法通常使用人工设计的准则来代模型管理这些约束。遵循深度学习中更广泛的模式，一种更具扩展性的方法是让端到端学习完全取代这一设计选择。毕竟，如果模型可以学习推理，为什么它不能学习遗忘呢？我们引入了神经垃圾回收（Neural Garbage Collection, NGC），在这种方法中，语言模型在学习推理的同时学习遗忘，且仅通过基于结果的任务奖励进行端到端训练。当模型进行推理时，它会定期暂停，决定驱逐哪些 KV 缓存条目，并基于剩余的缓存继续推理。通过将思维链中的标记（tokens）和缓存驱逐决策视为从语言模型中采样的离散动作，我们可以利用强化学习来联合优化模型的推理方式及其管理自身内存的方式：模型驱逐的内容塑造了它记忆的内容，它记忆的内容塑造了它的推理，而推理的正确性决定了它的奖励。至关重要的是，模型完全从单一的学习信号——基于结果的任务奖励——中学习这种行为，无需监督微调或代理目标。在 Countdown、AMC 和 AIME 任务上，NGC 在峰值 KV 缓存大小压缩 2-3 倍的情况下，相对于全缓存上限保持了强大的准确性，并显著优于驱逐基准。我们的结果是迈向更宏伟愿景的第一步，即端到端优化同时驱动语言模型的能力和效率。

## Abstract
Chain-of-thought reasoning has driven striking advances in language model capability, yet every reasoning step grows the KV cache, creating a bottleneck to scaling this paradigm further. Current approaches manage these constraints on the model's behalf using hand-designed criteria. A more scalable approach would let end-to-end learning subsume this design choice entirely, following a broader pattern in deep learning. After all, if a model can learn to reason, why can't it learn to forget? We introduce Neural Garbage Collection (NGC), in which a language model learns to forget while learning to reason, trained end-to-end from outcome-based task reward alone. As the model reasons, it periodically pauses, decides which KV cache entries to evict, and continues to reason conditioned on the remaining cache. By treating tokens in a chain-of-thought and cache-eviction decisions as discrete actions sampled from the language model, we can use reinforcement learning to jointly optimize how the model reasons and how it manages its own memory: what the model evicts shapes what it remembers, what it remembers shapes its reasoning, and the correctness of that reasoning determines its reward. Crucially, the model learns this behavior entirely from a single learning signal - the outcome-based task reward - without supervised fine-tuning or proxy objectives. On Countdown, AMC, and AIME tasks, NGC maintains strong accuracy relative to the full-cache upper bound at 2-3x peak KV cache size compression and substantially outperforms eviction baselines. Our results are a first step towards a broader vision where end-to-end optimization drives both capability and efficiency in language models.

---

## 论文详细总结（自动生成）

### 论文总结：神经垃圾回收（Neural Garbage Collection, NGC）

#### 1. 核心问题与整体含义（研究动机和背景）
随着思维链（CoT）推理能力的提升，模型生成的推理路径越来越长，导致 **KV 缓存（KV Cache）** 迅速膨胀。这不仅消耗大量显存，还限制了模型处理更复杂问题的能力。
*   **现状：** 现有的缓存管理方法（如 SnapKV、StreamingLLM）多依赖于人工设计的启发式规则（如保留最近标记或高注意力权重标记）。
*   **动机：** 论文作者认为，既然模型可以学习如何推理，也应该能学习如何“遗忘”。他们提出将效率（Efficiency）视为一种模型能力，通过端到端的强化学习（RL），让模型自主决定哪些信息是冗余的“垃圾”并将其回收，从而在有限的内存预算下实现更优的推理表现。

#### 2. 方法论：核心思想与关键技术
NGC 的核心是将**缓存驱逐（Eviction）**建模为模型在推理过程中的一种离散动作，并与 Token 生成同步优化。
*   **增长-驱逐循环（Grow-then-evict）：** 模型每生成 $\delta$ 个 Token（驱逐步长），就会暂停并进入驱逐轮次。
*   **动作空间扩展：** 驱逐决策被视为从模型中采样的离散动作。利用模型现有的注意力机制（Attention Scores）作为评分函数，无需引入额外参数。
*   **关键技术细节：**
    *   **块级驱逐（Block-level Eviction）：** 为了降低信用分配（Credit Assignment）的难度并适配硬件（如 vLLM 的分页管理），模型以块为单位进行驱逐。
    *   **Gumbel-top-k 采样：** 使用该技巧对要保留的缓存子集进行随机采样，使得离散的驱逐决策能够通过策略梯度（Policy Gradient）进行优化。
    *   **重放注意力掩码（Replay Attention Masks）：** 这是训练成功的关键。在 RL 训练的后向传播中，通过特殊的掩码重现推理时真实的上下文可见性，解决因缓存删除导致的“策略偏离（Off-policyness）”问题。
    *   **预算感知内省（Budget-aware Interoception）：** 在 Prompt 中加入当前的驱逐率（如 `<eviction_rate> 50% </eviction_rate>`），让模型意识到自身的内存约束。

#### 3. 实验设计
*   **数据集/场景：**
    *   **Countdown（倒计时任务）：** 组合算术任务，用于受控分析和消融实验。
    *   **数学推理：** 使用 DAPO-17k 数据集进行大规模训练，并在 AMC 2023/2025、AIME 2025 等高难度数学竞赛题目上进行验证。
*   **基准模型：** DeepSeek-R1-Distill-Qwen-1.5B。
*   **对比方法：**
    *   **SnapKV：** 基于注意力权重的启发式方法。
    *   **KeyDiff：** 基于键（Key）多样性的方法。
    *   **KNorm：** 基于键范数统计的方法。
    *   **StreamingLLM：** 滑动窗口加注意力锚点（Attention Sinks）的方法。

#### 4. 资源与算力
*   **模型规模：** 主要针对 1.5B 参数的蒸馏模型进行实验。
*   **训练细节：** Countdown 任务训练了 250 步；数学任务（DAPO-17k）训练了 469 步。
*   **算力说明：** 论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但从其使用的 GRPO 算法和 1.5B 模型来看，属于学术实验室可负担的算力规模。

#### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了从简单算术到复杂数学竞赛的跨度，并进行了多维度的消融实验（如验证重放掩码的必要性、驱逐决策梯度的贡献等）。
*   **充分性：** 实验设计较为全面，不仅对比了多种 SOTA 启发式方法，还测试了模型对未见过的驱逐率的泛化能力。
*   **客观性：** 通过 Pass@k 等标准指标衡量，并设置了“全量缓存（Full-cache）”作为性能上限，对比清晰。

#### 6. 主要结论与发现
*   **性能领先：** 在 2-3 倍的缓存压缩率下，NGC 的准确率显著超过所有启发式基准。在 Countdown 任务中，NGC 准确率（49.6%）是次优方法（21.2%）的两倍以上。
*   **端到端优势：** 实验证明，仅靠任务奖励（正确与否）就能引导模型学会复杂的内存管理策略，无需任何人工标注或代理损失函数。
*   **泛化性：** 具备“内省”能力（感知预算）的模型在面对更严苛的内存限制时，表现出更强的鲁棒性和泛化能力。

#### 7. 优点
*   **哲学创新：** 遵循“苦涩的教训（The Bitter Lesson）”，将内存管理从人工设计转向端到端学习。
*   **参数高效：** 复用现有的注意力机制，不增加模型参数量。
*   **训练严谨：** 引入重放掩码解决了 RL 训练中的策略偏离问题，确保了训练的稳定性。
*   **硬件友好：** 块级驱逐的设计与现代推理框架（如 vLLM）的 PagedAttention 机制高度契合。

#### 8. 不足与局限
*   **模型规模限制：** 实验主要集中在 1.5B 模型上，尚未在大规模模型（如 70B 或更高）上验证其扩展性。
*   **驱逐频率固定：** 目前采用固定的驱逐步长（$\delta$），未来若能实现动态触发驱逐（即模型觉得该忘的时候才忘）会更理想。
*   **训练成本：** 虽然推理更高效，但端到端 RL 训练本身比简单的启发式规则需要更多的计算资源和数据。
*   **任务局限：** 主要集中在数学和逻辑推理任务，对于长文本摘要或多轮对话等其他长上下文场景的验证较少。

（完）
