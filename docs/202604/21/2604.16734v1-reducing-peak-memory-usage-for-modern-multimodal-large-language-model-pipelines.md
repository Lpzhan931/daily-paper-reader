---
title: Reducing Peak Memory Usage for Modern Multimodal Large Language Model Pipelines
title_zh: 降低现代多模态大语言模型流水线的峰值显存占用
authors: "Junwan Kim, Hyunkyung Bae"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16734v1"
tags: ["query:llm"]
score: 9.0
evidence: 减少 KV 缓存中的峰值内存使用
tldr: 针对多模态大语言模型（MLLM）在处理高分辨率图像和长视频时，视觉Token导致的KV缓存峰值显存过高问题，本文提出了一种顺序输入压缩机制。该方法利用模型内部的结构规律性和表示冗余，在预填充阶段即执行结构感知的KV缓存压缩，从而在维持生成性能的同时，显著降低了推理过程中的峰值显存占用，使多模态推理更具实用性。
selection_source: fresh_fetch
motivation: 现有的视觉Token压缩方法多在输入处理完成后才进行，导致预填充阶段的峰值显存成为推理的主要瓶颈。
method: 提出一种顺序输入压缩机制，在预填充过程中利用结构感知特性对KV缓存进行实时压缩，以维持固定的内存预算。
result: 该方法在显著降低峰值显存占用的同时，仅造成了极小的生成性能损失。
conclusion: 通过在预填充阶段引入结构感知的顺序压缩，可以有效缓解MLLM的显存压力，实现更高效的多模态推理。
---

## 摘要
多模态大语言模型 (MLLMs) 最近在理解和生成来自多样化视觉输入（包括高分辨率图像和长视频序列）的响应方面展现了强大的能力。随着这些模型扩展到更丰富的视觉表示，推理过程越来越依赖于在键值 (KV) 缓存中存储大量的视觉 token，使得显存消耗成为核心瓶颈。现有方法通过识别视觉 token 中的冗余并压缩缓存来解决这一问题，但此类压缩通常仅在处理完所有输入后才应用，导致预填充 (prefill) 阶段出现极高的峰值显存占用。在这项工作中，我们展示了 MLLMs 表现出固有的结构规律性和表示冗余，可用于在整个推理过程中控制显存增长。基于这一见解，我们提出了一种顺序输入压缩机制，通过在预填充过程中执行结构感知的键值缓存压缩，来强制执行固定的显存预算。该方法在保持生成性能仅有极小退化的同时，大幅降低了峰值显存占用，从而实现了更实用且显存高效的多模态推理。

## Abstract
Multimodal large language models (MLLMs) have recently demonstrated strong capabilities in understanding and generating responses from diverse visual inputs, including high-resolution images and long video sequences. As these models scale to richer visual representations, inference increasingly relies on storing large numbers of vision tokens in the key-value (KV) cache, making memory consumption a central bottleneck. Existing methods address this issue by identifying redundancy in vision tokens and compressing the cache, but such compression is typically applied only after all inputs are processed, resulting in high peak memory usage during the prefill stage. In this work, we show that MLLMs exhibit inherent structural regularities and representational redundancy that can be exploited to control memory growth throughout inference. Based on this insight, we propose a sequential input-compression mechanism that enforces a fixed memory budget by performing structure-aware key-value cache compression during the prefill process. This approach substantially reduces peak memory usage while maintaining generative performance with only minimal degradation, enabling more practical and memory-efficient multimodal inference.

---

## 论文详细总结（自动生成）

### 论文总结：降低现代多模态大语言模型流水线的峰值显存占用

#### 1. 核心问题与研究动机
现代多模态大语言模型（MLLM）通过引入高分辨率图像（切片技术）和长视频序列（密集采样）来提升推理能力，但这导致了视觉 Token 数量的爆炸式增长。
*   **瓶颈：** 传统的 KV 缓存压缩方法通常在“预填充（Prefill）”阶段完成后才进行，这意味着在处理输入的过程中，显存占用会随 Token 数量线性增长，导致预填充阶段出现显存峰值，极易引发显存溢出（OOM）。
*   **目标：** 论文旨在打破“先处理、后压缩”的常规模式，实现“边预填充、边压缩”，从而在整个输入处理过程中维持一个恒定的、可控的显存预算。

#### 2. 核心方法论
论文提出了一种**预填充感知、结构感知的顺序 KV 缓存压缩框架**。
*   **核心思想：** 将输入序列划分为连续的块（Blocks），顺序处理每个块，并在每个块处理完后立即根据预设的显存预算 $M$ 进行 KV 缓存驱逐。
*   **关键技术细节：**
    *   **块状处理（Block-wise Processing）：** 将输入划分为与视觉结构（如图像的 Tile 或视频的帧组）对齐的块，确保驱逐决策具有语义合理性。
    *   **查询感知驱逐（Query-Aware Eviction）：** 针对单轮对话，利用文本提示（Prompt）作为代理查询，计算缓存 Key 的重要性得分，保留最相关的视觉区域。
    *   **查询无关驱逐（Query-Agnostic Eviction）：** 针对多轮对话，通过保留与平均表示差异最大的 Key（离群值）来维持表示的多样性，不依赖特定查询。
    *   **混合执行策略：** 为了平衡延迟，前 $M$ 个 Token 采用单次前向传播（Bulk Forward），超出部分再进入块状顺序处理。

#### 3. 实验设计
*   **数据集/场景：**
    *   **图像：** ImageNeedleInHaystack (MileBench)、V*（侧重密集视觉定位）。
    *   **视频：** MLVU、Video-MME（长视频设置）。
    *   **通用能力：** MMMU、POPE。
*   **Benchmark 与对比方法：**
    *   **模型：** InternVL3.5 (8B, 14B)、Qwen2.5-VL (7B, 32B)。
    *   **对比基准：** 全量缓存（Full Cache）、SnapKV（后压缩）、KeyDiff（后压缩）。
    *   **评估指标：** 任务准确率、平均准确率、显存峰值、推理延迟。

#### 4. 资源与算力
*   **硬件：** 实验主要在 **NVIDIA A100 GPU** 上进行。
*   **训练/推理：** 该方法属于推理时优化，无需重新训练模型。文中未明确提及具体的推理总时长，但详细记录了不同 Token 规模下的首字延迟（TTFT）和显存占用数据。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 7B 到 32B 不同参数规模的模型，测试了从 1024 到 4096 的多种 KV 缓存预算。
*   **消融实验：** 进行了丰富的分析，包括块大小的影响、静态与动态预算对比、压缩与降低输入分辨率的对比。
*   **充分性评价：** 实验设计较为充分，不仅验证了长上下文任务，还通过 MMMU 等通用榜单证明了该方法不会损害模型的基础能力。对比实验考虑了显存与延迟的权衡，具有较强的客观性和实用参考价值。

#### 6 主要结论与发现
*   **显存控制：** 该方法能将 KV 缓存压缩率提升至 **90%**，且预填充阶段的显存峰值保持恒定，成功避免了处理超大规模视觉输入时的 OOM 问题。
*   **性能保持：** 在大幅压缩的情况下，性能下降极小（如 InternVL3.5-8B 在 1024 预算下平均准确率仅下降 0.78%）。
*   **结构对齐的重要性：** 实验发现，当块大小与模型的原生视觉 Token 结构（如 Qwen2.5-VL 的 28x28 结构）对齐时，压缩效果最佳。
*   **视频任务的特殊性：** 在某些视频任务中，适度压缩反而能提升性能，因为压缩过程过滤掉了冗余的视觉噪声。

#### 7. 优点
*   **解决痛点：** 直接针对 MLLM 推理中最脆弱的“预填充峰值显存”问题，而非仅仅是解码阶段。
*   **即插即用：** 无需重新训练，兼容现有的多种压缩算法（如 SnapKV, KeyDiff）。
*   **结构感知：** 充分利用了图像空间相干性和视频时间冗余性，比单纯的 Token 剪枝更科学。

#### 8. 不足与局限
*   **延迟权衡：** 顺序处理块会增加首字延迟（TTFT），存在明显的“显存换时间”的权衡。
*   **对齐依赖：** 压缩效果高度依赖于块大小与模型视觉编码结构的对齐，对于结构不明的模型可能需要额外调优。
*   **任务局限：** 查询无关策略在极端压缩下（如 1024 预算）对某些复杂推理任务仍有一定性能损害。

（完）
