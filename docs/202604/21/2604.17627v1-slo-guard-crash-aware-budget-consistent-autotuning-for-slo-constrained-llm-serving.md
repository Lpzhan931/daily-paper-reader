---
title: "SLO-Guard: Crash-Aware, Budget-Consistent Autotuning for SLO-Constrained LLM Serving"
title_zh: SLO-Guard：面向 SLO 约束下大语言模型服务的崩溃感知且预算一致的自动调优
authors: Christian Lysenstøen
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17627v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对SLO约束的LLM服务自动调优
tldr: 针对大语言模型推理服务中配置复杂且易导致系统崩溃的问题，本文提出 SLO-Guard 自动调优框架。该框架将崩溃视为一类观测指标，结合了可行性优先的热预算退火探索与热启动的 TPE 开发阶段。通过引入配置修复和显存保护机制，SLO-Guard 在满足延迟服务水平目标的同时，显著提升了调优过程的预算一致性和结果稳定性，减少了无效尝试。
selection_source: fresh_fetch
motivation: LLM 推理配置搜索空间巨大且极易触发系统崩溃或延迟违规，而现有优化器通常将这些失败视为无效数据，导致调优效率低下。
method: 采用结合热预算退火（TBA）与 Tree-structured Parzen Estimator（TPE）的两阶段调优策略，并配合显存保护和配置修复机制。
result: 实验表明 SLO-Guard 在快速推理区间的尝试比例更高，且最佳延迟结果的跨种子标准差比随机搜索降低了 4.4 倍。
conclusion: SLO-Guard 的主要贡献在于其预算一致性，能够在固定调优预算内更可预测地找到并维持高性能的推理配置。
---

## 摘要
在延迟服务水平目标（SLO）约束下提供大语言模型（LLM）服务是一个涉及大量配置的系统问题，其搜索空间极易发生故障：许多看似合理的配置会直接导致崩溃或无法达到用户可见的延迟目标，而标准的黑盒优化器通常将这些失败视为无效尝试。我们提出了 SLO-Guard，这是一种针对 vLLM 服务的崩溃感知自动调优器，它将崩溃视为一等观测值。SLO-Guard 结合了可行性优先的热预算退火（TBA）探索阶段和热启动的树状 Parzen 估计器（TPE）利用阶段；在阶段切换时会重放所有探索历史，包括被编码为极端约束违反的崩溃情况。此外，我们还贡献了一个配置修复环节、一个 GPU 感知的 KV 缓存内存保护机制以及一个四类崩溃分类法。我们在 NVIDIA A100 40GB 上使用 vLLM 0.19 对 Qwen2-1.5B 进行了评估。在一项预设的五种子研究中，在修正后的并发测试框架下，SLO-Guard 和均匀随机搜索均实现了 75/75 的可行性且零崩溃，并且在最佳延迟方面在统计学上持平（Mann-Whitney 双侧 p=0.84）。SLO-Guard 的优势在于预算一致性：在快速服务区间内的尝试次数更多（15 次中占 10.20 次 vs. 7.40 次；单侧 p=0.014），且阶段切换后的连贯性更高（0.876 vs. 0.539；p=0.010）。在并发负载下，SLO-Guard 在最佳延迟上的跨种子标准差比随机搜索小 4.4 倍（2.26 ms vs. 10.00 ms）。测试框架复现分析表明，一致性结论在独立的顺序调度测量条件下依然成立。核心论点并非 SLO-Guard 能找到更好的最终配置，而是在找到快速区间后，它能更可预测地利用固定的调优预算。

## Abstract
Serving large language models under latency service-level objectives (SLOs) is a configuration-heavy systems problem with an unusually failure-prone search space: many plausible configurations crash outright or miss user-visible latency targets, and standard black-box optimizers treat these failures as wasted trials. We present SLO-Guard, a crash-aware autotuner for vLLM serving that treats crashes as first-class observations. SLO-Guard combines a feasible-first Thermal Budget Annealing (TBA) exploration phase with a warm-started Tree-structured Parzen Estimator (TPE) exploitation phase; the handoff replays all exploration history, including crashes encoded as extreme constraint violations. We additionally contribute a configuration-repair pass, a GPU-aware KV-cache memory guard, and a four-category crash taxonomy.   We evaluate SLO-Guard on Qwen2-1.5B served with vLLM 0.19 on an NVIDIA A100 40GB. Across a pre-specified five-seed study, both SLO-Guard and uniform random search attain 75/75 feasibility with zero crashes under the corrected concurrent harness, and are statistically tied on best-achieved latency (Mann-Whitney two-sided p=0.84). SLO-Guard's advantage is in budget consistency: more trials in the fast-serving regime (10.20 vs. 7.40 out of 15; one-sided p=0.014) and higher post-handoff consistency (0.876 vs. 0.539; p=0.010). Under concurrent load, SLO-Guard's cross-seed standard deviation on best latency is 4.4x tighter than random search's (2.26 ms vs. 10.00 ms). A harness-replication analysis shows that the consistency findings survive an independent sequential-dispatch measurement condition.   The central claim is not that SLO-Guard finds a better final configuration, but that it spends a fixed tuning budget more predictably once the fast regime has been found.

---

## 论文详细总结（自动生成）

### 论文总结：SLO-Guard —— 面向 SLO 约束下 LLM 服务的崩溃感知自动调优

#### 1. 论文的核心问题与整体含义
在生产环境中部署大语言模型（LLM）时，工程师需要调整大量运行时参数（如批处理大小、Token 预算、内存利用率等）。这些参数与用户可见的延迟（TTFT、ITL）和系统稳定性之间存在复杂的非线性关系。
*   **研究动机**：LLM 服务的调优面临三大挑战：**易崩溃性**（许多配置会导致引擎崩溃）、**条件相关性**（参数间存在依赖）和**小预算限制**（每次尝试成本高，通常只能进行几十次试验）。
*   **核心问题**：现有的黑盒优化器通常将系统崩溃视为无效数据而丢弃。本文探讨如何通过“崩溃感知”机制，在有限的调优预算内，更稳定、更可预测地找到满足服务水平目标（SLO）的最佳配置。

#### 2. 方法论
SLO-Guard 采用了一种两阶段的调优策略，并辅以工程化的修复机制：
*   **核心思想**：将崩溃视为“一等公民”观测值，通过编码崩溃信息来指导优化器避开不稳定区域，并确保在找到高性能区间后能够持续停留。
*   **关键技术细节**：
    1.  **配置修复图（Repair Map）与显存保护**：在执行前自动修正不兼容的参数组合，并根据 GPU 显存（VRAM）自动计算 KV 缓存预算，防止因显存溢出（OOM）导致的崩溃。
    2.  **TBA-explore 阶段**：采用“可行性优先”的热预算退火（Thermal Budget Annealing）算法。在未找到可行点前最小化约束违反，找到后转向优化 Goodput（满足 SLO 的吞吐量）。
    3.  **TPE-exploit 阶段**：当积累足够数据后，切换至热启动的树状 Parzen 估计器（TPE）。该阶段会重放所有历史记录，将崩溃编码为极大的约束违反值，使 TPE 能够识别并避开崩溃边界。
    4.  **崩溃分类法**：将失败分为启动失败、预检失败、运行时失败和健康运行四类，用于更精细的区域追踪。

#### 3. 实验设计
*   **场景与模型**：使用 vLLM 0.19 引擎服务 Qwen2-1.5B 模型。
*   **数据集/负载**：固定模板提示词，100 Token 输出上限，模拟并发请求。
*   **SLO 约束**：P99 TTFT（首字延迟）≤ 500ms，P99 ITL（字间延迟）≤ 100ms。
*   **Benchmark（对比方法）**：主要对比**均匀随机搜索（Uniform Random Search）**。作者认为在极小预算（15 次尝试）和离散条件空间下，随机搜索是极具挑战性的基准。
*   **测试框架**：对比了“顺序调度”和“真实并发调度”两种测量环境，以验证结论的鲁棒性。

#### 4. 资源与算力
*   **硬件**：使用 Google Colab 提供的 **NVIDIA A100 40GB** GPU。
*   **算力消耗**：总计进行了 300 次试验（150 次并发测试 + 150 次顺序测试）。每次试验包含引擎启动、预热、负载测试和关闭。虽然单次试验耗时未明确给出，但总预算设定为极小的 15 次尝试/组，体现了对低算力消耗调优的追求。

#### 5. 实验数量与充分性
*   **实验规模**：采用了 5 个随机种子进行重复实验（5 seeds × 2 optimizers × 15 trials）。
*   **充分性评价**：虽然 5 个种子在统计学上属于小样本，但作者应用了非参数检验（Mann-Whitney U 检验）和 Holm-Bonferroni 校正，并提供了效应量（方差比）分析。
*   **客观性**：作者主动披露并修正了早期实验中负载生成器的并发缺陷，并进行了复现分析，证明了结论在不同测量条件下的一致性，表现出较高的学术严谨性。

#### 6. 主要结论与发现
*   **预算一致性（核心结论）**：SLO-Guard 在最终达到的“最佳延迟”上与随机搜索统计持平，但在**预算分配**上表现优异。它能将更多尝试（10.20 vs 7.40）分配给快速响应区间。
*   **稳定性提升**：在找到高性能配置后，SLO-Guard 维持在该区域的连贯性显著更高（0.876 vs 0.539）。
*   **方差缩减**：在并发负载下，SLO-Guard 跨种子找到的最佳延迟标准差比随机搜索小 **4.4 倍**，意味着其调优结果更可预测，不易受随机性影响。
*   **搜索空间特征**：发现 vLLM 的搜索空间呈明显的双峰分布，主要受 `enforce_eager` 参数影响。

#### 7. 优点
*   **实用性强**：针对生产中常见的“崩溃”问题提出了系统化的处理方案，而非理想化地假设所有配置都能运行。
*   **工程细节扎实**：引入的配置修复图和 GPU 感知内存保护机制，解决了自动调优中常见的 API 级别报错问题。
*   **严谨的统计分析**：不只关注峰值性能，更关注调优过程的鲁棒性和预算利用率，这更符合工业界需求。

#### 8. 不足与局限
*   **模型与硬件覆盖有限**：仅在 Qwen2-1.5B 和 A100 上测试，未验证更大规模模型（如 Llama-3-70B）或多 GPU 环境下的表现。
*   **搜索空间结构简单**：实验发现性能主要由一个二进制开关决定，这可能掩盖了在更复杂、多峰分布空间下优化器的潜在问题。
*   **负载真实性**：使用了固定模板而非真实的生产流量轨迹（Trace），未考虑长文本或多轮对话的动态压力。
*   **对比基准较少**：未在大规模多种子实验中完整对比受限贝叶斯优化（Constrained BO）等其他高级优化器。

（完）
