---
title: "River-LLM: Large Language Model Seamless Exit Based on KV Share"
title_zh: River-LLM：基于 KV 共享的大语言模型无缝退出
authors: "Yingtao Shen, An Zou"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18396v1"
tags: ["query:llm"]
score: 9.0
evidence: 提前退出中的 KV 缓存缺失问题
tldr: 针对大语言模型推理延迟高的问题，本文提出River-LLM框架。该框架针对Decoder-only架构中Early Exit导致的KV缓存缺失难题，设计了无需训练的“KV共享出口河”机制，使跳过层能自然生成并保留KV缓存。结合状态转移相似度预测退出决策，在保持高生成质量的同时，实现了1.71至2.16倍的实际推理加速，有效解决了理论层数减少与实际加速之间的鸿沟。
selection_source: fresh_fetch
motivation: 传统的Early Exit方法在Decoder-only架构中因KV缓存缺失导致重计算开销大或精度损失严重，难以实现真正的推理加速。
method: 提出一种无需训练的River-LLM框架，通过轻量级的KV共享机制在退出过程中同步生成缺失的KV缓存，并利用状态转移相似度指导退出决策。
result: 在数学推理和代码生成任务上，River-LLM实现了1.71至2.16倍的实际墙钟时间加速，且保持了极高的生成质量。
conclusion: River-LLM通过创新的KV共享机制解决了Early Exit的性能瓶颈，为大模型的高效推理提供了一种无损且高效的新方案。
---

## 摘要
大语言模型（LLMs）在多个领域展现了卓越的性能，但日益受到高推理延迟的限制。早期退出（Early Exit）已成为一种通过动态跳过冗余层来加速推理的有前景的解决方案。然而，在仅解码器（decoder-only）架构中，早期退出的效率受到 KV 缓存缺失（KV Cache Absence）问题的严重制约，即被跳过的层无法为后续 token 提供必要的历史状态。现有的解决方案（如重计算或掩码）要么引入了显著的延迟开销，要么导致严重的精度损失，无法弥合理论层数减少与实际墙钟时间（wall-clock）加速之间的差距。在本文中，我们提出了 River-LLM，这是一个无需训练的框架，可实现无缝的 token 级早期退出。River-LLM 引入了一个轻量级的 KV 共享退出流（KV-Shared Exit River），允许在退出过程中自然地生成并保留主干网络缺失的 KV 缓存，从而消除了昂贵的恢复操作。此外，我们利用解码器块内的状态转移相似性来预测累积 KV 误差，并指导精确的退出决策。在数学推理和代码生成任务上的广泛实验表明，River-LLM 在保持高生成质量的同时，实现了 1.71 到 2.16 倍的实际加速。

## Abstract
Large Language Models (LLMs) have demonstrated exceptional performance across diverse domains but are increasingly constrained by high inference latency. Early Exit has emerged as a promising solution to accelerate inference by dynamically bypassing redundant layers. However, in decoder-only architectures, the efficiency of Early Exit is severely bottlenecked by the KV Cache Absence problem, where skipped layers fail to provide the necessary historical states for subsequent tokens. Existing solutions, such as recomputation or masking, either introduce significant latency overhead or incur severe precision loss, failing to bridge the gap between theoretical layer reduction and practical wall-clock speedup. In this paper, we propose River-LLM, a training-free framework that enables seamless token-level Early Exit. River-LLM introduces a lightweight KV-Shared Exit River that allows the backbone's missing KV cache to be naturally generated and preserved during the exit process, eliminating the need for costly recovery operations. Furthermore, we utilize state transition similarity within decoder blocks to predict cumulative KV errors and guide precise exit decisions. Extensive experiments on mathematical reasoning and code generation tasks demonstrate that River-LLM achieves 1.71 to 2.16 times of practical speedup while maintaining high generation quality.

---

## 论文详细总结（自动生成）

这篇论文由上海交通大学的研究人员提出，旨在解决大语言模型（LLM）在采用“早期退出”（Early Exit）策略时面临的核心瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在仅解码器（Decoder-only）架构的 LLM 中，**KV 缓存缺失（KV Cache Absence）**是阻碍早期退出技术从理论走向实践的最大障碍。
*   **研究动机**：当某个 Token 在中间层提前退出以节省计算量时，它会跳过后续的解码器层，导致这些层无法生成该 Token 的 Key-Value (KV) 缓存。由于 LLM 是自回归生成的，后续 Token 在这些层进行注意力计算时会因为缺少历史 KV 状态而无法正常工作。
*   **现状局限**：现有的补救措施（如重计算、状态传播或掩码）要么引入了巨大的延迟开销（抵消了加速效果），要么导致严重的精度下降。

### 2. 方法论：River-LLM 框架
River-LLM 提出了一个**无需训练（Training-free）**的“无缝退出”框架，核心思想是构建一条“KV 共享出口河”（KV-Shared Exit River）：
*   **KV 共享出口层（KV-Shared Exit Layer）**：为每个主干解码器块设计一个轻量级的插件层。这些层继承主干网络的架构和参数，但通过 4 位权重仅量化（W4A16）进行压缩。
*   **无缝 KV 生成**：当 Token 决定提前退出时，它不再直接停止，而是快速流经这条由量化层组成的“出口河”。在流经过程中，这些轻量级层会自然地生成并填充主干网络缺失的 KV 缓存，从而保证了后续 Token 的计算完整性。
*   **状态转移相似度决策**：利用当前层输入与输出 Hidden State 之间的余弦相似度（State Transition Similarity）来预测累积误差。如果相似度高于阈值 $\tau$，则认为后续层是冗余的，触发退出。
*   **主干网络卸载（Backbone Offloading）**：由于大部分 Token 会早退，框架可以动态地将未激活的深层主干参数从显存中卸载，从而降低显存占用。

### 3. 实验设计
*   **模型架构**：Llama 3.2 1B、Llama 3.1 8B、Phi4-mini、Ministral3 8B。
*   **数据集/场景**：
    *   **常识推理**：BoolQ, HellaSwag, ARC-Challenge/Easy, MMLU。
    *   **长序列生成**：GSM8K（数学）、MATH（复杂数学）、HumanEval（代码）。
*   **对比方法（Baselines）**：
    *   **KV 策略对比**：Batching Recompute（重计算）、Mono-Decreasing Exit（单调递减退出）、State Propagation（状态传播）、KV Mask（掩码）。
    *   **现有框架对比**：Balcony、EE-LLM、全量化模型（Full Quantization）。

### 4. 资源与算力
*   **硬件平台**：所有实验均在 **NVIDIA A40 GPU** 上完成。
*   **训练时长**：该方法是**无需训练**的。论文提到权重转移和量化过程非常快，通常在 **1 分钟内**即可完成。
*   **推理设置**：采用了 5-shot (GSM8K) 和 4-shot (MATH) 设置，其余为 0-shot。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 8B 参数规模的 4 种主流模型，并在 8 个不同维度的基准测试上进行了验证。
*   **消融与对比**：
    *   对比了不同退出阈值 $\tau$ 对精度和速度的影响。
    *   测试了不同量化后端（HQQ, AWQ）在“出口河”中的表现。
    *   详细分析了显存占用随序列长度（最高 64K）的变化。
*   **评价**：实验设计较为充分，不仅关注了准确率，还重点测量了“墙钟时间”（实际推理速度），这在动态推理研究中是非常客观且具有说服力的。

### 6. 主要结论与发现
*   **实际加速显著**：River-LLM 在数学和代码任务上实现了 **1.71× 至 2.16×** 的实际墙钟加速。
*   **精度保持良好**：在大幅加速的同时，其生成质量接近原始全精度模型，显著优于传统的 KV 掩码或重计算方法。
*   **决策开销极低**：退出决策逻辑仅占每 Token 推理时间的 0.0688%（约 100 微秒）。
*   **显存优势**：通过主干卸载策略，其显存占用接近全量化模型，远低于其他早期退出框架。

### 7. 优点与亮点
*   **无缝集成**：解决了 KV 缓存缺失的顽疾，使 Token 级早期退出在 Decoder-only 模型中真正具备了实用价值。
*   **无需训练**：通过 PTQ（后训练量化）和权重共享实现，部署成本极低，易于适配各种预训练模型。
*   **帕累托前沿**：在“速度-精度”权衡曲线上定义了更优的边界，尤其在处理复杂推理任务时表现稳健。

### 8. 不足与局限
*   **模型规模覆盖**：实验主要集中在 8B 及以下规模的模型。虽然理论上可扩展，但在 70B 或更大规模模型上的表现（如通信开销、层间冗余度变化）尚未验证。
*   **Prefill 阶段优化有限**：目前在 Prefill（预填充）阶段仍采用序列级退出以保持并行效率，因此对于 Prefill 主导的任务（如极长文本理解）加速效果不如生成阶段明显。
*   **硬件依赖**：实际加速效果高度依赖于量化算子的推理内核优化。

（完）
